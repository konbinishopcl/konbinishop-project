---
phase: ad-hoc — articles seeder + WP import
status: paused
last_updated: 2026-05-29T15:11:44Z
---

<current_state>
Sesión ad-hoc completada. Se construyó el sistema completo de importación de artículos desde WordPress. 435/437 artículos reales en la BD local con galerías y WebP. Vercel preview configurado para apuntar a dev-api.konbinishop.cl.
</current_state>

<completed_work>

- Vercel preview env: API_URL → https://dev-api.konbinishop.cl/api (solo preview, producción sin cambios)
- Migration: nuevo modelo ArticleImage (galería con cascade delete)
- apps/api/prisma/export-wp-articles.ts — jala 437 posts de konbinishop.com WP API, descarga imágenes a uploads/, genera prisma/data/articles.json
- Todas las imágenes convertidas a WebP max 1200px con sharp (648 archivos)
- seed.ts reemplaza 8 artículos falsos por loader del JSON real
- seed.ts integra las 39 categorías de artículo (antes estaban en script separado)
- Seed funciona end-to-end: 435 artículos, 1956 tags, galerías vinculadas
</completed_work>

<remaining_work>

- Eliminar apps/api/prisma/convert-uploads-to-webp.mjs (script temporal, ya no se necesita)
- Exponer articleImages en respuesta del API GET /articles/:slug
- Renderizar galería en página de detalle de artículo (website)
</remaining_work>

<decisions_made>

- Export a JSON estático (no fetch en runtime del seed) — menos RAM en CI/staging
- Imágenes inline del body → galería ArticleImage, eliminadas del contenido markdown
- Todo a WebP max 1200px — más liviano, formato moderno
- NEXT_PUBLIC_API_ORIGIN no existe en el código real — solo API_URL y API_KEY necesarios
</decisions_made>

<blockers>
- 2 artículos con caracteres hex inválidos rechazados por MySQL — aceptable, 435/437 es suficiente
</blockers>

<context>
Seed es completamente autónomo: `cd apps/api && npx prisma db seed`
Datos reales en: apps/api/prisma/data/articles.json
Imágenes en: apps/api/uploads/ (648 WebP)
Para regenerar el JSON desde WP: npx ts-node prisma/export-wp-articles.ts
</context>

<next_action>
1. `rm apps/api/prisma/convert-uploads-to-webp.mjs`
2. Exponer `articleImages` en ArticlesService.findOne()
3. Renderizar galería en el article detail page del website
</next_action>
