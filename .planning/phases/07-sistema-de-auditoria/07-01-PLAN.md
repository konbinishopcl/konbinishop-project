---
phase: 07-sistema-de-auditoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - apps/api/jest.config.js
  - apps/api/package.json
  - apps/api/prisma/seed.ts
autonomous: true
requirements: [AUD-01, AUD-02, AUD-03, AUD-04]
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md lista AUD-01..04 con descripción y la tabla de trazabilidad los mapea a Phase 7"
    - "jest se ejecuta en apps/api sin error de configuración (npx jest --passWithNoTests)"
    - "El contenido de PRIVACY_POLICY menciona el registro de auditoría, IP/userAgent como datos personales y la retención de 24 meses"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Definición formal de AUD-01..04 y su trazabilidad"
      contains: "AUD-01"
    - path: "apps/api/jest.config.js"
      provides: "Configuración base de Jest para el API NestJS"
      contains: "ts-jest"
    - path: "apps/api/package.json"
      provides: "Scripts de test y devDependencies de Jest"
      contains: "jest"
  key_links:
    - from: "apps/api/jest.config.js"
      to: "apps/api/src"
      via: "rootDir + testRegex"
      pattern: "testRegex|rootDir"
---

<objective>
Preparar el terreno para la Phase 7: definir formalmente los requisitos AUD-01..04 en
REQUIREMENTS.md (hoy solo existen como IDs sueltos en ROADMAP.md), configurar Jest en el API
NestJS (no existe configuración de tests hoy) y declarar el logging de auditoría en la
Política de Privacidad para cumplir la Ley 21.719.

Purpose: Sin requisitos definidos no hay trazabilidad; sin Jest no se pueden escribir los
tests automatizados que el resto de los planes exigen; sin la declaración legal, agregar
columnas de IP/userAgent sería incumplimiento de la Ley 21.719.

Output: REQUIREMENTS.md actualizado, `apps/api/jest.config.js`, scripts y devDeps de Jest en
`apps/api/package.json`, y el contenido del LegalDocument PRIVACY_POLICY actualizado en el
seed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-sistema-de-auditoria/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir AUD-01..04 en REQUIREMENTS.md</name>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/07-sistema-de-auditoria/07-RESEARCH.md</read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Agregar a `.planning/REQUIREMENTS.md` una nueva sección de requisitos para auditoría,
    inmediatamente después de la sección `### Production Hardening` y antes de
    `## v2 Requirements (diferido)`.

    Insertar exactamente:

    ```markdown
    ### Audit

    - [ ] **AUD-01**: Migración Prisma que crea el modelo `AuditLog` con los campos
      `userId` (Int?, sin FK), `action` (enum `AuditAction`), `entity` (enum `AuditEntity`),
      `entityId` (Int), `metadata` (Json), `ip` (String?), `userAgent` (String?),
      `url` (String?), `createdAt` — más índices de consulta
    - [ ] **AUD-02**: `AuditService` singleton inyectable que registra acciones (CREATE,
      UPDATE, APPROVE, REJECT, BAN, UNBAN, DELETE) sobre las entidades del sistema, integrado
      manualmente en los service methods de mutación de eventos, usuarios, avisos y portadas;
      el fallo del registro nunca revierte la operación de negocio
    - [ ] **AUD-03**: Captura de la IP real del cliente detrás de Nginx mediante
      `app.set('trust proxy', 1)` y `req.ip` (sin parsear `x-forwarded-for` manualmente)
    - [ ] **AUD-04**: Endpoint `GET /api/admin/audit-logs` con filtros (entity, action,
      userId, dateFrom, dateTo), paginado, restringido a roles ADMIN y SUPER_ADMIN
    ```

    Luego, en la tabla `## Traceability`, agregar una fila nueva debajo de `HARD-01..04`:

    ```markdown
    | AUD-01..04 | Phase 7 | Pending |
    ```

    Finalmente, actualizar la línea `**Coverage:**` para reflejar 4 requisitos adicionales:
    cambiar `v1 requirements (excluyendo AUTH ya completado): 25` por
    `v1 requirements (excluyendo AUTH ya completado): 29`.
  </action>
  <acceptance_criteria>
    - `grep -c "AUD-0" .planning/REQUIREMENTS.md` devuelve al menos 5 (4 definiciones + 1 fila de trazabilidad)
    - `grep "### Audit" .planning/REQUIREMENTS.md` encuentra la sección
    - `grep "AUD-01..04 | Phase 7" .planning/REQUIREMENTS.md` encuentra la fila de trazabilidad
    - `grep "completado): 29" .planning/REQUIREMENTS.md` encuentra el coverage actualizado
  </acceptance_criteria>
  <verify>
    <automated>grep -q "### Audit" .planning/REQUIREMENTS.md && grep -q "AUD-04" .planning/REQUIREMENTS.md && grep -q "AUD-01..04 | Phase 7" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>REQUIREMENTS.md contiene la sección Audit con AUD-01..04 y la tabla de trazabilidad los mapea a Phase 7.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar Jest en el API NestJS</name>
  <read_first>apps/api/package.json, apps/api/tsconfig.json, .planning/phases/07-sistema-de-auditoria/07-RESEARCH.md</read_first>
  <files>apps/api/jest.config.js, apps/api/package.json</files>
  <action>
    El API NestJS no tiene Jest configurado (no hay `jest.config.js`, no hay campo `jest` en
    package.json, no existen `*.spec.ts`). Configurarlo ahora para que los planes 07-03..07-06
    puedan ejecutar tests automatizados.

    1. Instalar las devDependencies necesarias. Ejecutar dentro de `apps/api`:
       ```
       cd apps/api && npm install --save-dev jest@^29.7.0 ts-jest@^29.1.0 @types/jest@^29.5.0 @nestjs/testing@^11.0.1 supertest@^7.0.0 @types/supertest@^6.0.0
       ```
       (`@nestjs/testing` y `supertest` se usan para los tests e2e del endpoint en 07-03.)

    2. Crear `apps/api/jest.config.js` con esta configuración exacta (CommonJS, igual que el
       proyecto que usa `"module": "commonjs"` en tsconfig):
       ```js
       /** @type {import('ts-jest').JestConfigWithTsJest} */
       module.exports = {
         moduleFileExtensions: ['js', 'json', 'ts'],
         rootDir: '.',
         testRegex: '.*\\.(spec|e2e-spec)\\.ts$',
         transform: {
           '^.+\\.(t|j)s$': ['ts-jest', { tsconfig: 'tsconfig.json' }],
         },
         collectCoverageFrom: ['src/**/*.(t|j)s'],
         coverageDirectory: './coverage',
         testEnvironment: 'node',
         moduleNameMapper: {},
       };
       ```

    3. En `apps/api/package.json`, dentro de `"scripts"`, agregar:
       ```
       "test": "jest --no-coverage",
       "test:audit": "jest --testPathPattern=audit --no-coverage",
       "test:e2e": "jest --testRegex=.e2e-spec.ts$ --no-coverage"
       ```
       Mantener todos los scripts existentes (`build`, `dev`, `start`, etc.) sin cambios.
  </action>
  <acceptance_criteria>
    - `apps/api/jest.config.js` existe y contiene `ts-jest` y `testRegex`
    - `grep '"test"' apps/api/package.json` encuentra el script de test
    - `grep '"jest"' apps/api/package.json` encuentra jest en devDependencies
    - `cd apps/api && npx jest --passWithNoTests --no-coverage` termina con código 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && npx jest --passWithNoTests --no-coverage 2>&1 | tail -3</automated>
  </verify>
  <done>`npx jest --passWithNoTests` corre sin error de configuración; los scripts de test existen en package.json.</done>
</task>

<task type="auto">
  <name>Task 3: Declarar el logging de auditoría en la Política de Privacidad (Ley 21.719)</name>
  <read_first>apps/api/prisma/seed.ts</read_first>
  <files>apps/api/prisma/seed.ts</files>
  <action>
    El proyecto cumple activamente la Ley 21.719. La tabla `AuditLog` almacenará IP y
    userAgent, que son datos personales bajo esa ley. La Política de Privacidad debe
    declararlo. El contenido de la Política vive en el modelo `LegalDocument` con
    `type = PRIVACY_POLICY`.

    1. Abrir `apps/api/prisma/seed.ts` y localizar el upsert/creación del LegalDocument
       `PRIVACY_POLICY` (busca por `PRIVACY_POLICY`). Leer su contenido actual (`content`).

    2. Agregar al string de `content` de PRIVACY_POLICY un párrafo nuevo sobre auditoría.
       Insertarlo al final del contenido existente, antes de cualquier sección de cierre.
       Usar exactamente este texto (en español, formato consistente con el resto del
       documento — si el contenido usa Markdown, usar un encabezado `## Registros de auditoría`;
       si usa HTML, usar `<h2>`):

       ```
       ## Registros de auditoría

       Para garantizar la trazabilidad y la seguridad de la plataforma, Konbini registra
       las acciones administrativas y de publicación que realizan los usuarios sobre los
       contenidos del sistema (eventos, avisos y portadas). Cada registro incluye la fecha,
       la acción realizada, la entidad afectada y, cuando corresponde, la dirección IP y el
       agente de usuario (navegador) desde el que se originó la acción.

       La dirección IP y el agente de usuario constituyen datos personales conforme a la
       Ley N° 21.719. Se tratan con la única finalidad de auditoría y seguridad, y se
       conservan por un plazo máximo de 24 meses, transcurrido el cual son eliminados. El
       titular puede ejercer sus derechos de acceso, rectificación y supresión sobre estos
       registros conforme a la ley vigente.
       ```

    3. Si el seed solo crea el LegalDocument cuando no existe (upsert con `create`/`update`),
       asegurarse de que el bloque `update` también actualice el `content` — de lo contrario,
       en bases ya sembradas el cambio no se aplicará. Si el seed usa `create` puro sin
       `update`, convertirlo a `upsert` para que re-ejecutar el seed actualice el contenido.

    NO tocar el LegalDocument `TERMS_OF_SERVICE`. NO cambiar la estructura del seed más allá
    de lo necesario para que el nuevo párrafo se aplique.
  </action>
  <acceptance_criteria>
    - `grep -q "Registros de auditoría" apps/api/prisma/seed.ts` encuentra el nuevo párrafo
    - `grep -q "21.719" apps/api/prisma/seed.ts` encuentra la referencia legal
    - `grep -q "24 meses" apps/api/prisma/seed.ts` encuentra el plazo de retención
    - El bloque del LegalDocument PRIVACY_POLICY usa `upsert` o un `update` que incluye `content`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Registros de auditoría" apps/api/prisma/seed.ts && grep -q "24 meses" apps/api/prisma/seed.ts && grep -q "21.719" apps/api/prisma/seed.ts && echo OK</automated>
  </verify>
  <done>El contenido de PRIVACY_POLICY en el seed declara el registro de auditoría, IP/userAgent como datos personales y la retención de 24 meses.</done>
</task>

</tasks>

<verification>
- `grep "AUD-04" .planning/REQUIREMENTS.md` confirma los requisitos definidos
- `cd apps/api && npx jest --passWithNoTests --no-coverage` corre sin error
- `grep "Registros de auditoría" apps/api/prisma/seed.ts` confirma la declaración legal
</verification>

<success_criteria>
- REQUIREMENTS.md define AUD-01..04 y los mapea a Phase 7 en la trazabilidad
- Jest está configurado y ejecutable en `apps/api`
- La Política de Privacidad declara el logging de auditoría con retención de 24 meses
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-de-auditoria/07-01-SUMMARY.md`
</output>
