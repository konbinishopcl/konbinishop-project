---
phase: 07-sistema-de-auditoria
plan: 05
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - apps/api/src/users/users.service.ts
  - apps/api/src/users/users.controller.ts
  - apps/api/src/users/users.module.ts
  - apps/api/src/spots/spots.service.ts
  - apps/api/src/spots/spots.controller.ts
  - apps/api/src/spots/spots.module.ts
  - apps/api/src/heroes/heroes.service.ts
  - apps/api/src/heroes/heroes.controller.ts
  - apps/api/src/heroes/heroes.module.ts
autonomous: true
requirements: [AUD-02]
user_setup: []

must_haves:
  truths:
    - "Banear un usuario registra { action: BAN, entity: USER, entityId, userId (admin) }"
    - "Desbanear un usuario registra { action: UNBAN, entity: USER }"
    - "Eliminar un usuario registra { action: DELETE, entity: USER }"
    - "Cambiar el rol de un usuario registra { action: UPDATE, entity: USER, metadata: { prevRole, newRole } }"
    - "Aprobar/rechazar/bannear un aviso registra entity: AVISO; lo mismo para portadas con entity: PORTADA"
  artifacts:
    - path: "apps/api/src/users/users.service.ts"
      provides: "Auditoría en setBanned/remove/update; métodos reciben user+req"
      contains: "audit.log"
    - path: "apps/api/src/spots/spots.service.ts"
      provides: "Auditoría en approve/reject/ban con entity AVISO"
      contains: "AVISO"
    - path: "apps/api/src/heroes/heroes.service.ts"
      provides: "Auditoría en approve/reject/ban con entity PORTADA"
      contains: "PORTADA"
  key_links:
    - from: "apps/api/src/users/users.service.ts"
      to: "AuditService.log"
      via: "llamada manual tras setBanned/remove/update"
      pattern: "this\\.audit\\.log"
    - from: "apps/api/src/spots/spots.controller.ts"
      to: "spots.service métodos de moderación"
      via: "@CurrentUser() user + @Req() req añadidos a approve/reject/ban"
      pattern: "@Req\\(\\)"
    - from: "apps/api/src/heroes/heroes.module.ts"
      to: "AuditModule"
      via: "imports[]"
      pattern: "AuditModule"
---

<objective>
Instrumentar `UsersService`, `SpotsService` y `HeroesService` con `AuditService.log()`. A
diferencia de Events, estos servicios NO reciben hoy el `JwtUser` en sus métodos de
mutación/moderación — hay que añadir `user: JwtUser` y `req: Request` tanto en el service como
en el controller que lo invoca.

Purpose: Completa AUD-02 para usuarios, avisos (Spots → entity AVISO) y portadas (Heroes →
entity PORTADA). Es independiente de 07-04 (toca archivos disjuntos), así que corre en
paralelo en la Wave 3.

Output: Los tres servicios registran auditoría; los tres controllers pasan `user` y `req`; los
tres módulos importan `AuditModule`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-sistema-de-auditoria/07-RESEARCH.md
@apps/api/src/users/users.service.ts
@apps/api/src/users/users.controller.ts
@apps/api/src/spots/spots.service.ts
@apps/api/src/spots/spots.controller.ts
@apps/api/src/heroes/heroes.service.ts
@apps/api/src/heroes/heroes.controller.ts

<interfaces>
<!-- Contratos provistos por 07-03 — usar directamente. -->

AuditService — apps/api/src/audit/audit.service.ts:
  import { AuditService } from '../audit/audit.service';
  log(params: AuditLogParams): void   // fire-and-forget, no async, no await
  export interface AuditLogParams {
    userId: number | null;
    action: AuditAction;   // 'CREATE'|'UPDATE'|'DELETE'|'APPROVE'|'REJECT'|'BAN'|'UNBAN'
    entity: AuditEntity;   // 'EVENT'|'USER'|'AVISO'|'PORTADA'
    entityId: number;
    metadata?: Record<string, unknown>;
    req?: Request;         // import type { Request } from 'express'
  }

AuditModule — apps/api/src/audit/audit.module.ts: exporta AuditService.

JwtUser — apps/api/src/auth/current-user.decorator.ts:
  type JwtUser = { sub: number; email: string; role: string };
  Decorador: import { CurrentUser, type JwtUser } from '../auth/current-user.decorator';

Estado ACTUAL de las firmas (a modificar — ninguna recibe user/req hoy):
  UsersService.update(id, dto)              UsersController.update(@Param id, @Body dto)
  UsersService.setBanned(id, blocked)       UsersController.ban(@Param id, @Body('blocked') blocked)
  UsersService.remove(id)                   UsersController.remove(@Param id)
  SpotsService.approve(id)                  SpotsController.approve(@Param id)
  SpotsService.reject(id, reason)           SpotsController.reject(@Param id, @Body dto)
  SpotsService.ban(id, reason)              SpotsController.ban(@Param id, @Body dto)
  HeroesService.approve(id)                 HeroesController.approve(@Param id)
  HeroesService.reject(id, reason)          HeroesController.reject(@Param id, @Body dto)
  HeroesService.ban(id, reason)             HeroesController.ban(@Param id, @Body dto)

Mapeo entidad → enum: Spot = AVISO, Hero = PORTADA, User = USER.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrumentar UsersService + UsersController</name>
  <read_first>apps/api/src/users/users.service.ts, apps/api/src/users/users.controller.ts, apps/api/src/users/users.module.ts, apps/api/src/audit/audit.service.ts</read_first>
  <files>apps/api/src/users/users.service.ts, apps/api/src/users/users.controller.ts, apps/api/src/users/users.module.ts</files>
  <action>
    **1. `users.service.ts`** — inyectar `AuditService` (`import { AuditService } from
    '../audit/audit.service';`, `private readonly audit: AuditService` en el constructor).
    Importar `import type { Request } from 'express';` y
    `import type { JwtUser } from '../auth/current-user.decorator';`.

    Modificar las firmas y agregar auditoría:
    - `setBanned(id: number, blocked: boolean, actor: JwtUser, req?: Request)`:
      tras el `prisma.user.update`, llamar
      `this.audit.log({ userId: actor.sub, action: blocked ? 'BAN' : 'UNBAN', entity: 'USER', entityId: id, req });`
    - `remove(id: number, actor: JwtUser, req?: Request)`:
      tras `prisma.user.delete`, llamar
      `this.audit.log({ userId: actor.sub, action: 'DELETE', entity: 'USER', entityId: id, req });`
    - `update(id: number, dto: UpdateUserDto, actor: JwtUser, req?: Request)`:
      antes del update, leer el usuario actual para conocer su `role` previo
      (`const before = await this.prisma.user.findUnique({ where: { id } });` — o reutilizar
      el `ensure`). Tras el update, SOLO si `dto.role` está presente y es distinto del rol
      previo, registrar:
      `this.audit.log({ userId: actor.sub, action: 'UPDATE', entity: 'USER', entityId: id, metadata: { prevRole: before.role, newRole: dto.role }, req });`
      Si el `role` no cambia, NO registrar nada (evita ruido por ediciones de nombre/email).

    Usar el nombre de parámetro `actor` (no `user`) para no chocar con variables locales
    llamadas `user`. `log()` se llama sin `await`.

    **2. `users.controller.ts`** — pasar `@CurrentUser()` y `@Req()` a los handlers `update`,
    `ban`, `remove`.
    - Agregar `Req` al import de `@nestjs/common`; agregar
      `import { CurrentUser, type JwtUser } from '../auth/current-user.decorator';` y
      `import type { Request } from 'express';`.
    - Cambios exactos:
      ```ts
      update(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateUserDto,
             @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.users.update(id, dto, user, req);
      }
      ban(@Param('id', ParseIntPipe) id: number, @Body('blocked') blocked: boolean | undefined,
          @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.users.setBanned(id, blocked ?? true, user, req);
      }
      remove(@Param('id', ParseIntPipe) id: number, @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.users.remove(id, user, req);
      }
      ```
    - NO tocar `findRecent`, `findAll`, `create`.

    **3. `users.module.ts`** — agregar `import { AuditModule } from '../audit/audit.module';`
    y añadir `AuditModule` al array `imports` (junto a `AuthModule`).
  </action>
  <acceptance_criteria>
    - `grep -c "this.audit.log" apps/api/src/users/users.service.ts` devuelve 3
    - `grep -q "blocked ? 'BAN' : 'UNBAN'" apps/api/src/users/users.service.ts`
    - `grep -q "prevRole" apps/api/src/users/users.service.ts` y `grep -q "newRole" apps/api/src/users/users.service.ts`
    - `grep -c "@Req()" apps/api/src/users/users.controller.ts` devuelve 3
    - `grep -q "AuditModule" apps/api/src/users/users.module.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && test "$(grep -c 'this.audit.log' src/users/users.service.ts)" = "3" && grep -q "prevRole" src/users/users.service.ts && test "$(grep -c '@Req()' src/users/users.controller.ts)" = "3" && grep -q "AuditModule" src/users/users.module.ts && echo OK</automated>
  </verify>
  <done>UsersService audita BAN/UNBAN/DELETE/UPDATE-de-rol; UsersController pasa user+req; UsersModule importa AuditModule.</done>
</task>

<task type="auto">
  <name>Task 2: Instrumentar SpotsService + SpotsController (entity AVISO)</name>
  <read_first>apps/api/src/spots/spots.service.ts, apps/api/src/spots/spots.controller.ts, apps/api/src/spots/spots.module.ts, apps/api/src/audit/audit.service.ts</read_first>
  <files>apps/api/src/spots/spots.service.ts, apps/api/src/spots/spots.controller.ts, apps/api/src/spots/spots.module.ts</files>
  <action>
    Los Spots se auditan con `entity: 'AVISO'` (nombre comercial). Se auditan SOLO las
    acciones de moderación `approve`, `reject`, `ban` (no CREATE/UPDATE/DELETE de avisos por
    organizador — alcance v1).

    **1. `spots.service.ts`** — inyectar `AuditService`. Importar
    `import type { Request } from 'express';` y
    `import type { JwtUser } from '../auth/current-user.decorator';`.

    Modificar firmas y agregar auditoría (los métodos NO reciben user/req hoy):
    - `approve(id: number, actor: JwtUser, req?: Request)`: tras `prisma.spot.update`, llamar
      `this.audit.log({ userId: actor.sub, action: 'APPROVE', entity: 'AVISO', entityId: id, req });`
    - `reject(id: number, reason: string, actor: JwtUser, req?: Request)`: tras el update,
      `this.audit.log({ userId: actor.sub, action: 'REJECT', entity: 'AVISO', entityId: id, metadata: { reason }, req });`
    - `ban(id: number, reason: string, actor: JwtUser, req?: Request)`: tras el update,
      `this.audit.log({ userId: actor.sub, action: 'BAN', entity: 'AVISO', entityId: id, metadata: { reason }, req });`
    `log()` sin `await`, antes del bloque de mail. NO tocar `create`, `update`, `remove`,
    `findActive`, `quota`, `findMine`.

    **2. `spots.controller.ts`** — los handlers `approve`, `reject`, `ban` están protegidos
    con `@Roles('ADMIN','SUPER_ADMIN')` pero no reciben el usuario. Agregar `@CurrentUser()` y
    `@Req()`:
    - Agregar `Req` al import de `@nestjs/common`; `CurrentUser` y `JwtUser` ya se importan en
      el archivo (verificar; si no, agregar
      `import { CurrentUser, type JwtUser } from '../auth/current-user.decorator';`). Agregar
      `import type { Request } from 'express';`.
    - Cambios exactos:
      ```ts
      approve(@Param('id', ParseIntPipe) id: number, @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.spots.approve(id, user, req);
      }
      reject(@Param('id', ParseIntPipe) id: number, @Body() dto: RejectEventDto,
             @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.spots.reject(id, dto.reason, user, req);
      }
      ban(@Param('id', ParseIntPipe) id: number, @Body() dto: RejectEventDto,
          @CurrentUser() user: JwtUser, @Req() req: Request) {
        return this.spots.ban(id, dto.reason, user, req);
      }
      ```

    **3. `spots.module.ts`** — agregar `import { AuditModule } from '../audit/audit.module';`
    y añadir `AuditModule` al array `imports`.
  </action>
  <acceptance_criteria>
    - `grep -c "this.audit.log" apps/api/src/spots/spots.service.ts` devuelve 3
    - `grep -c "entity: 'AVISO'" apps/api/src/spots/spots.service.ts` devuelve 3
    - `grep -c "@Req()" apps/api/src/spots/spots.controller.ts` devuelve 3
    - `grep -q "AuditModule" apps/api/src/spots/spots.module.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && test "$(grep -c 'this.audit.log' src/spots/spots.service.ts)" = "3" && test "$(grep -c "entity: 'AVISO'" src/spots/spots.service.ts)" = "3" && test "$(grep -c '@Req()' src/spots/spots.controller.ts)" = "3" && grep -q "AuditModule" src/spots/spots.module.ts && echo OK</automated>
  </verify>
  <done>SpotsService audita approve/reject/ban con entity AVISO; SpotsController pasa user+req; SpotsModule importa AuditModule.</done>
</task>

<task type="auto">
  <name>Task 3: Instrumentar HeroesService + HeroesController (entity PORTADA) y compilar</name>
  <read_first>apps/api/src/heroes/heroes.service.ts, apps/api/src/heroes/heroes.controller.ts, apps/api/src/heroes/heroes.module.ts, apps/api/src/audit/audit.service.ts</read_first>
  <files>apps/api/src/heroes/heroes.service.ts, apps/api/src/heroes/heroes.controller.ts, apps/api/src/heroes/heroes.module.ts</files>
  <action>
    Los Heroes se auditan con `entity: 'PORTADA'` (nombre comercial). Solo las acciones de
    moderación `approve`, `reject`, `ban`. Patrón idéntico al de Spots (Task 2), cambiando
    `spot`→`hero`, `AVISO`→`PORTADA` y los paths.

    **1. `heroes.service.ts`** — inyectar `AuditService`. Importar `Request` y `JwtUser`.
    - `approve(id: number, actor: JwtUser, req?: Request)`: tras `prisma.hero.update`,
      `this.audit.log({ userId: actor.sub, action: 'APPROVE', entity: 'PORTADA', entityId: id, req });`
    - `reject(id: number, reason: string, actor: JwtUser, req?: Request)`:
      `this.audit.log({ userId: actor.sub, action: 'REJECT', entity: 'PORTADA', entityId: id, metadata: { reason }, req });`
    - `ban(id: number, reason: string, actor: JwtUser, req?: Request)`:
      `this.audit.log({ userId: actor.sub, action: 'BAN', entity: 'PORTADA', entityId: id, metadata: { reason }, req });`
    `log()` sin `await`. NO tocar `create`, `update`, `remove`, `findActive`, `quota`,
    `findMine`, `assertQuotaAvailable`, `assertMaxDays`.

    **2. `heroes.controller.ts`** — agregar `@CurrentUser()` y `@Req()` a `approve`, `reject`,
    `ban`. `CurrentUser`/`JwtUser` ya se importan en el archivo (verificar); agregar `Req` al
    import de `@nestjs/common` e `import type { Request } from 'express';`. Mismas firmas que
    en Spots Task 2 (`this.heroes.approve(id, user, req)`, etc.).

    **3. `heroes.module.ts`** — agregar `import { AuditModule } from '../audit/audit.module';`
    y añadir `AuditModule` al array `imports`.

    **4. Compilación final** — tras los cambios de las 3 tasks, ejecutar
    `cd apps/api && npx tsc --noEmit -p tsconfig.json` y resolver cualquier error de tipos
    (típicamente: un controller que no pasa `req`, o un service cuya firma no coincide). El
    plan no se da por terminado hasta que `tsc` pase limpio.
  </action>
  <acceptance_criteria>
    - `grep -c "this.audit.log" apps/api/src/heroes/heroes.service.ts` devuelve 3
    - `grep -c "entity: 'PORTADA'" apps/api/src/heroes/heroes.service.ts` devuelve 3
    - `grep -c "@Req()" apps/api/src/heroes/heroes.controller.ts` devuelve 3
    - `grep -q "AuditModule" apps/api/src/heroes/heroes.module.ts`
    - `cd apps/api && npx tsc --noEmit -p tsconfig.json` termina con código 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && test "$(grep -c 'this.audit.log' src/heroes/heroes.service.ts)" = "3" && test "$(grep -c "entity: 'PORTADA'" src/heroes/heroes.service.ts)" = "3" && grep -q "AuditModule" src/heroes/heroes.module.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <done>HeroesService audita approve/reject/ban con entity PORTADA; HeroesController pasa user+req; el API completo compila limpio.</done>
</task>

</tasks>

<verification>
- `grep -c "this.audit.log"` = 3 en cada uno de users/spots/heroes service
- `entity: 'AVISO'` aparece 3 veces en spots.service.ts; `entity: 'PORTADA'` 3 veces en heroes.service.ts
- `npx tsc --noEmit` compila el API completo con las nuevas firmas
</verification>

<success_criteria>
- Banear/desbanear/eliminar un usuario y cambiar su rol generan AuditLog (entity USER)
- Aprobar/rechazar/bannear un aviso genera AuditLog con entity AVISO
- Aprobar/rechazar/bannear una portada genera AuditLog con entity PORTADA
- Los tres controllers pasan el `JwtUser` actor y el `Request`; el API compila limpio
</success_criteria>

<output>
After completion, create `.planning/phases/07-sistema-de-auditoria/07-05-SUMMARY.md`
</output>
