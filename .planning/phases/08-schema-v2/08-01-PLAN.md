---
phase: 08-schema-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - apps/api/prisma/schema.prisma
autonomous: true
requirements: [SCH-01]
must_haves:
  truths:
    - "REQUIREMENTS.md contiene las definiciones SCH-01..SCH-06 bajo una sección v2"
    - "Existe el enum UserType con valores PERSON y ORGANIZATION en schema.prisma"
    - "El modelo User tiene los campos: type, handle (unique), isVerified, twoFactorCode, twoFactorExpiry"
    - "El handle es único globalmente (constraint @unique en handle)"
    - "Existe una migración aplicada con nombre que contiene 'sch01_user_v2'"
    - "pnpm prisma validate exits 0; pnpm prisma generate exits 0; pnpm tsc --noEmit exits 0"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Enum UserType + 5 campos nuevos en User"
      contains: "enum UserType"
    - path: "apps/api/prisma/migrations"
      provides: "Carpeta de migración generada por Prisma para SCH-01"
      contains: "sch01_user_v2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Sección v2 con SCH-01..SCH-06 listados"
      contains: "SCH-01"
  key_links:
    - from: "apps/api/prisma/schema.prisma (model User)"
      to: "Prisma Client tipado"
      via: "pnpm prisma generate"
      pattern: "type:\\s*UserType"
    - from: "apps/api/prisma/schema.prisma (User.handle)"
      to: "constraint @unique en MySQL"
      via: "migration SQL"
      pattern: "handle.*@unique"
---

<objective>
Agregar las definiciones de los requirements SCH-01..SCH-06 a REQUIREMENTS.md (primer plan de la fase, así el resto se ancla a IDs ya documentados) y extender el modelo `User` del schema Prisma con los campos de v2: tipo de cuenta (PERSON|ORGANIZATION), handle único, badge Verificado y campos de 2FA.

Purpose: SCH-01 habilita las dos identidades clave de v2 (personas y organizaciones bajo la misma tabla User) y deja listo el campo `handle` que reemplazará a `Profile.slug` como identificador público. Sin este plan ningún otro plan de la fase tiene un IDs documentado al que referenciar.

Output: REQUIREMENTS.md con sección v2; schema.prisma con `enum UserType` y 5 campos nuevos en User; una migración Prisma aplicada y el cliente regenerado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-schema-v2/08-RESEARCH.md
@docs/API-RULES.md
@apps/api/prisma/schema.prisma
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estado actual del modelo User (líneas 322-350 de schema.prisma) — el ejecutor debe respetar estos campos y solo AGREGAR los nuevos. -->

```prisma
model User {
  id           Int     @id @default(autoincrement())
  email        String  @unique
  passwordHash String?
  googleId     String? @unique
  firstname    String?
  lastname     String?
  rut          String?
  isCompany    Boolean @default(false)
  role         Role    @default(AUTHENTICATED)
  confirmed    Boolean @default(false)
  blocked      Boolean @default(false)

  resetToken       String?
  resetTokenExpiry DateTime?

  events         Event[] @relation("EventOwner")
  approvedEvents Event[] @relation("EventApprover")
  rejectedEvents Event[] @relation("EventRejecter")
  spots          Spot[]  @relation("SpotOwner")
  heroes         Hero[]  @relation("HeroOwner")
  orders         Order[]
  likes          Like[]
  profile        Profile?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

NO modificar: campos existentes ni relaciones existentes. La fusión Profile↔User NO ocurre en esta fase (Open Question #2 — resuelta vía KEY DECISION #1: Profile permanece separado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar SCH-01..SCH-06 a REQUIREMENTS.md como sección v2</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (estado actual — verificar que solo tiene v1)
    - .planning/ROADMAP.md (sección "Phase 8: Schema v2" con la descripción autoritativa de cada SCH)
    - .planning/phases/08-schema-v2/08-RESEARCH.md (líneas 23-34, tabla de Phase Requirements)
  </read_first>
  <action>
    Al final de REQUIREMENTS.md (después de la sección de v1, antes del cierre), agregar exactamente la siguiente sección. NO modificar nada de v1; solo APPEND.

    ```markdown
    ---

    ## v2 Requirements — Milestone "Plataforma completa"

    > v2 introduce cobro al organizador, organizaciones con membresías, 2FA + Google OAuth,
    > notificaciones internas, transferencias, servicios y CRM. El schema cambia
    > sustancialmente — esta sección documenta los requirements de cada fase.

    ### Schema v2 (Phase 8)

    - [ ] **SCH-01**: Modelo `User` extendido con `type: UserType (PERSON|ORGANIZATION)`,
      `handle` (String unique global), `isVerified` (Boolean default false), `twoFactorCode`
      (String?), `twoFactorExpiry` (DateTime?). Migración Prisma aplicada y `prisma generate`
      regenera el cliente sin errores de tipo.
    - [ ] **SCH-02**: Modelos `Country`, `State`, `City` (jerarquía 3-nivel) reemplazan
      `Region` y `Commune`. `Event.cityId` reemplaza `Event.regionId`/`Event.communeId`.
      Seeder con datos de Chile (1 país + 16 states + ~350 cities). Catalog module y DTOs
      actualizados; `pnpm tsc --noEmit` pasa.
    - [ ] **SCH-03**: Modelos `OrgMember` (userId, orgId, role: OWNER|MEMBER, unique
      (userId, orgId)) y `OrgInvitation` (token unique, email, orgId, expiresAt). El enum
      `OrgRole` se define en este plan.
    - [ ] **SCH-04**: Modelos `Settings` (key PK, value String), `Notification` (userId u
      orgId, read, type, payload Json), `SavedEvent` (userId+eventId unique), `Subscription`
      (userId u orgId, status, cycle dates, credits), `Transfer` (itemType enum + itemId Int
      polymórfico, status, fromUserId, toOrgId).
    - [ ] **SCH-05**: `Category` extendida con `icon`, `color`, `minDays`, `maxDays`,
      `order`. Enum `OrderItemType` agrega `ARTICLE`. `OrderItem` agrega FK opcional
      `articleId`. `Order` agrega `orgId` (FK opcional a User type ORGANIZATION,
      validación en service layer). `Article` agrega `status: PublicationStatus`,
      `statusReason: String?`, `userId: Int?` para artículos patrocinados.
    - [ ] **SCH-06**: Modelos `ServiceRequest` (type: ServiceType, name, email, eventName,
      eventDate, eventPlace, stage: CrmStage), `ServiceOption` (type: ServiceType, label,
      active, order), `CrmEntry` (modelo nuevo — pipeline unificado para CONTACT,
      PHOTOGRAPHY, CONTENT con stage: CrmStage), `CrmNote` (FK a CrmEntry, autor, content).
      `ContactMessage` se mantiene sin cambios para backwards compatibility (decisión D8-CRM
      del RESEARCH: CrmEntry independiente, no extensión de ContactMessage).
    ```

    Marcar todos los SCH-XX como `[ ]` (pendientes) — se irán cerrando a medida que cada plan complete.
  </action>
  <verify>
    <automated>grep -c "SCH-0[1-6]" .planning/REQUIREMENTS.md | awk '{ if ($1 >= 6) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## v2 Requirements" .planning/REQUIREMENTS.md` exits 0
    - `grep -c "SCH-0[1-6]" .planning/REQUIREMENTS.md` >= 6
    - `grep -q "SCH-01" .planning/REQUIREMENTS.md && grep -q "SCH-06" .planning/REQUIREMENTS.md` exits 0
    - La sección v1 original NO fue modificada (verificar con `grep -q "AUTH-01" .planning/REQUIREMENTS.md` — sigue presente)
    - `grep -q "ContactMessage se mantiene sin cambios" .planning/REQUIREMENTS.md` exits 0 (KEY DECISION #2 reflejada en SCH-06)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md tiene la sección v2 con SCH-01..SCH-06 documentados, cada uno con su
    descripción canónica, sin perturbar las secciones de v1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender modelo User con campos v2 + migración Prisma</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/*</files>
  <read_first>
    - apps/api/prisma/schema.prisma (líneas 322-350 — modelo User actual)
    - .planning/phases/08-schema-v2/08-RESEARCH.md (líneas 314-333 — snippet SCH-01)
    - apps/api/prisma/migrations (listar `ls apps/api/prisma/migrations/` para conocer las migraciones existentes y entender la convención de nombres)
    - docs/API-RULES.md (sección "Tipos de usuario" y "Perfil público / Organizador" para confirmar semántica de type y handle)
  </read_first>
  <action>
    En `apps/api/prisma/schema.prisma`:

    **(a) Agregar el enum `UserType` justo ANTES del bloque `enum Role`** (sección Usuarios, línea 316 aprox):

    ```prisma
    enum UserType {
      PERSON
      ORGANIZATION
    }
    ```

    **(b) En el modelo `User`** (líneas 322-350), agregar los siguientes 5 campos. Insertar después del campo `blocked` y ANTES del bloque de "Recuperación de contraseña":

    ```prisma
      // v2: tipo de cuenta — PERSON con credenciales; ORGANIZATION sin login propio
      type            UserType  @default(PERSON)
      // v2: identificador público único global (personas y orgs comparten namespace)
      handle          String?   @unique
      // v2: badge Verificado — asignado/revocado solo por SUPER_ADMIN
      isVerified      Boolean   @default(false)
      // v2: 2FA por email — código de 6 dígitos + expiración (10 min recomendado)
      twoFactorCode   String?
      twoFactorExpiry DateTime?
    ```

    NO tocar ningún otro campo. NO tocar las relaciones (events, spots, heroes, orders, likes, profile, approvedEvents, rejectedEvents). NO agregar relaciones nuevas (las relaciones a OrgMember, OrgInvitation, Notification etc. se agregarán en sus planes correspondientes — esta tarea es solo extensión escalar).

    **(c) Validar y crear la migración:**

    ```bash
    cd apps/api
    pnpm prisma validate
    pnpm prisma migrate dev --name sch01_user_v2
    pnpm prisma generate
    pnpm tsc --noEmit
    ```

    Si `prisma migrate dev` detecta que la DB tiene datos incompatibles (poco probable: solo agrega columnas nullable + 1 con default), aceptar el reset. El asunto de Open Question #1 (datos reales) se aborda en Plan 02 — aquí los cambios son aditivos (nullable o con default) por lo que no destruyen datos.
  </action>
  <verify>
    <automated>cd apps/api && pnpm prisma validate && pnpm prisma generate && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enum UserType {" apps/api/prisma/schema.prisma` exits 0
    - `grep -A1 "enum UserType" apps/api/prisma/schema.prisma | grep -q "PERSON"` exits 0
    - `grep -A2 "enum UserType" apps/api/prisma/schema.prisma | grep -q "ORGANIZATION"` exits 0
    - `grep -E "type\\s+UserType\\s+@default\\(PERSON\\)" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -E "handle\\s+String\\?\\s+@unique" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -E "isVerified\\s+Boolean\\s+@default\\(false\\)" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -q "twoFactorCode" apps/api/prisma/schema.prisma && grep -q "twoFactorExpiry" apps/api/prisma/schema.prisma` exits 0
    - `ls apps/api/prisma/migrations | grep -q "sch01_user_v2"` exits 0
    - `cd apps/api && pnpm prisma validate` exits 0
    - `cd apps/api && pnpm prisma generate` exits 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    El modelo User tiene los 5 campos nuevos + el enum UserType existe. La migración
    sch01_user_v2 está aplicada y el cliente Prisma regenerado expone los nuevos tipos
    a TypeScript sin romper la compilación.
  </done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md contiene la sección v2 con todos los SCH-XX (grep)
- schema.prisma contiene enum UserType + campos type/handle/isVerified/twoFactorCode/twoFactorExpiry
- Existe la carpeta de migración `apps/api/prisma/migrations/*_sch01_user_v2/`
- `pnpm prisma validate` exits 0
- `pnpm prisma generate` exits 0
- `pnpm tsc --noEmit` exits 0 (cliente regenerado, ningún consumer actual usa estos campos todavía)
</verification>

<success_criteria>
SCH-01 implementado: User v2 documentado en REQUIREMENTS.md y materializado en schema.prisma con migración aplicada. El Plan 02 puede ahora referenciar `User` con los nuevos campos sin reabrir Open Question #2 (Profile separado, decisión D8-PROFILE locked).
</success_criteria>

<output>
Crear `.planning/phases/08-schema-v2/08-01-SUMMARY.md` documentando:
- Campos exactos agregados a User
- Nombre de la migración generada (timestamp + sch01_user_v2)
- Decisión locked: Profile permanece separado (no fusión)
- Decisión locked: handle es global unique entre PERSON y ORGANIZATION
- Confirmación de que pnpm tsc --noEmit pasa
</output>
