---
phase: 08-schema-v2
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - apps/api/prisma/schema.prisma
autonomous: true
requirements: [SCH-05]
must_haves:
  truths:
    - "Category tiene los 5 campos nuevos: icon, color, minDays, maxDays, order"
    - "El enum OrderItemType incluye ARTICLE además de EVENT, SPOT, HERO"
    - "OrderItem tiene FK opcional articleId (Int?) y la relación article (Article? @relation)"
    - "La relación existente User↔Order queda nombrada explícitamente como UserOrders (Order.owner y User.orders)"
    - "Order tiene FK opcional orgId con relación a User (con nombre OrgOrders)"
    - "Article tiene status: PublicationStatus, statusReason: String?, userId: Int? (dueño para artículos patrocinados), orderItems: OrderItem[]"
    - "@@unique([orderId, type]) en OrderItem sigue intacto"
    - "pnpm prisma validate exits 0; migración aplicada; pnpm tsc --noEmit pasa (el código existente no usa los campos nuevos todavía)"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Category v2 + Order.orgId + OrderItem.articleId + Article.status/userId/statusReason + relaciones User↔Order nombradas (UserOrders, OrgOrders)"
      contains: "ARTICLE"
    - path: "apps/api/prisma/migrations"
      provides: "Migración sch05_category_orders_v2 aplicada"
      contains: "sch05_category_orders_v2"
  key_links:
    - from: "OrderItem.articleId"
      to: "Article.id"
      via: "FK opcional"
      pattern: "article\\s+Article\\?\\s+@relation"
    - from: "Order.userId (owner)"
      to: "User.id (relación dueño)"
      via: "FK obligatoria con nombre UserOrders (renombrada en este plan)"
      pattern: "owner\\s+User\\s+@relation\\(\"UserOrders\""
    - from: "Order.orgId"
      to: "User.id (con type=ORGANIZATION, validado en service)"
      via: "FK opcional con nombre OrgOrders"
      pattern: "org\\s+User\\?\\s+@relation\\(\"OrgOrders\""
    - from: "Article.userId"
      to: "User.id"
      via: "FK opcional con nombre ArticleOwner"
      pattern: "owner\\s+User\\?\\s+@relation\\(\"ArticleOwner\""
    - from: "Article.status"
      to: "enum PublicationStatus (ya existente)"
      via: "campo enum"
      pattern: "status\\s+PublicationStatus\\s+@default\\(DRAFT\\)"
---

<objective>
Extender los modelos Category, Order, OrderItem y Article para v2:
1. Category gana campos visuales y de reglas de negocio (icon, color, minDays, maxDays, order)
2. OrderItemType admite ARTICLE como cuarto tipo de ítem comprable
3. OrderItem admite FK opcional articleId
4. Order admite orgId para el contexto de compra "en nombre de" una organización
5. Article gana estados (DRAFT→APPROVED) y dueño (userId) para habilitar artículos patrocinados

Purpose: SCH-05 cierra la brecha entre el flujo de eventos/spots/heroes y la nueva categoría de Article patrocinado, agrega el contexto org en el carrito, y dota a Category de los metadatos UI que el frontend de v2 espera. Incorpora KEY DECISION #4 (Article.status absorbido aquí, no en plan separado).

Output: schema.prisma extendido con todos los cambios listados; migración sch05_category_orders_v2 aplicada; cliente Prisma regenerado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-schema-v2/08-RESEARCH.md
@.planning/phases/08-schema-v2/08-04-SUMMARY.md
@docs/API-RULES.md
@apps/api/prisma/schema.prisma

<interfaces>
<!-- Estado actual de los modelos a tocar (schema.prisma) -->

<!-- Category (líneas 41-51) — solo escalares simples hoy: -->
```prisma
model Category {
  id          Int      @id @default(autoincrement())
  name        String?
  slug        String   @unique
  description String?
  pricePerDay Int      @default(1000)
  events      Event[]
  heroes      Hero[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

<!-- Enum OrderItemType (líneas 266-270): -->
```prisma
enum OrderItemType {
  EVENT
  SPOT
  HERO
}
```

<!-- OrderItem (líneas 287-311) — ya tiene event/spot/hero relations + @@unique([orderId, type]) -->

<!-- Order (líneas 272-285) — sin orgId hoy. IMPORTANTE: la relación owner↔User es SIN NOMBRE: -->
```prisma
// En Order:
owner   User    @relation(fields: [userId], references: [id])
userId  Int
// En User:
orders  Order[]
```

<!-- ⚠️ Pitfall #6 / Prisma: agregar una segunda relación entre los mismos dos modelos exige -->
<!-- nombrar AMBAS relaciones (la existente y la nueva). Por eso Task 2 renombra la existente -->
<!-- a "UserOrders" antes de agregar "OrgOrders". -->

<!-- Article (líneas 64-76) — sin status/userId/orderItems hoy -->
```prisma
model Article {
  id        Int      @id @default(autoincrement())
  title     String
  slug      String   @unique
  excerpt   String?
  content   String   @db.Text
  image     String?
  tags      Tag[]
  events    Event[]
  likes     Like[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

<!-- Enum PublicationStatus ya existe (líneas 80-87) — DRAFT, PENDING_PAYMENT, PENDING_MODERATION, APPROVED, REJECTED, BANNED -->
<!-- Article reusará este enum (no se crea uno nuevo). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extender Category con campos v2 (icon, color, minDays, maxDays, order)</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (líneas 41-51 modelo Category actual)
    - docs/API-RULES.md (sección "Categorías" — confirma "name, slug, icon, color, pricePerDay, minDays, maxDays, order")
    - .planning/phases/08-schema-v2/08-RESEARCH.md (líneas 453-460 snippet Category v2)
  </read_first>
  <action>
    En `apps/api/prisma/schema.prisma`, en el modelo `Category`, agregar los siguientes campos después del campo `pricePerDay` y antes de la relación `events`:

    ```prisma
      // v2: metadatos visuales para el frontend
      icon        String?            // lucide icon name, ej. "calendar"
      color       String?            // hex (#FF6B00) o CSS class
      // v2: rango permitido de días al publicar un evento de esta categoría
      minDays     Int                @default(1)
      maxDays     Int                @default(30)
      // v2: ordenamiento manual en listados del frontend (0 = primero)
      order       Int                @default(0)
    ```

    NO modificar ningún otro campo de Category. NO tocar pricePerDay (default 1000 se mantiene).
  </action>
  <verify>
    <automated>cd apps/api && pnpm prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "icon\\s+String\\?" apps/api/prisma/schema.prisma | head -1` retorna 1 línea
    - `grep -E "color\\s+String\\?" apps/api/prisma/schema.prisma | head -1` retorna 1 línea (en Category, no en Hero)
    - `grep -E "minDays\\s+Int\\s+@default\\(1\\)" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -E "maxDays\\s+Int\\s+@default\\(30\\)" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -E "order\\s+Int\\s+@default\\(0\\)" apps/api/prisma/schema.prisma | head -1` retorna 1 línea
    - `cd apps/api && pnpm prisma validate` exits 0
  </acceptance_criteria>
  <done>
    Category tiene los 5 campos nuevos con defaults razonables. El schema sigue válido.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OrderItemType += ARTICLE, OrderItem.articleId, renombrar User↔Order existente + agregar Order.orgId (OrgOrders), Article v2 + migración</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/*</files>
  <read_first>
    - apps/api/prisma/schema.prisma (enum OrderItemType, modelos OrderItem/Order/User/Article; revisar especialmente Order.owner línea ~278 y User.orders línea ~344 — AMBAS son relaciones sin nombre)
    - .planning/phases/08-schema-v2/08-RESEARCH.md (líneas 462-503 snippets completos SCH-05; líneas 297-308 Pitfalls #5 y #6)
    - docs/API-RULES.md (secciones "Carrito (Orders)" — confirma ARTICLE como tipo soportado; sección "Artículos" — confirma flujo DRAFT→APPROVED para artículos patrocinados con userId como dueño)
  </read_first>
  <action>
    Editar `apps/api/prisma/schema.prisma`. **CRÍTICO:** las ediciones (c) + (d) deben hacerse JUNTAS en el mismo paso de edición — Prisma exige que ambos extremos de una relación nombrada se declaren simultáneamente o `prisma validate` falla con "Ambiguous relation detected between User and Order".

    **(a) Extender enum `OrderItemType`** — agregar ARTICLE como cuarto valor:

    ```prisma
    enum OrderItemType {
      EVENT
      SPOT
      HERO
      ARTICLE
    }
    ```

    NOTA Pitfall #5: el constraint `@@unique([orderId, type])` queda intacto. Esto significa que una orden puede tener máximo 1 ARTICLE (igual que 1 EVENT, 1 SPOT, 1 HERO). Si la regla cambia en el futuro, se migra el constraint en otra fase.

    **(b) En `model OrderItem`**, agregar la FK opcional a Article. Insertar junto a los otros FK opcionales (event, spot, hero):

    ```prisma
      article   Article? @relation(fields: [articleId], references: [id])
      articleId Int?
    ```

    Y agregar el índice correspondiente al final del modelo, junto a los otros `@@index`:

    ```prisma
      @@index([articleId])
    ```

    **(c) En `model Order`, primero RENOMBRAR la relación owner existente a `"UserOrders"`** (hoy no tiene nombre — línea ~278). Esto es prerrequisito obligatorio antes de poder agregar una segunda relación entre los mismos dos modelos (Pitfall #6 de Prisma: "if there are multiple relations between the same two models, you need to disambiguate them with a name").

    - ANTES (línea ~278):
      ```prisma
      owner   User    @relation(fields: [userId], references: [id])
      userId  Int
      ```
    - DESPUÉS:
      ```prisma
      owner   User    @relation("UserOrders", fields: [userId], references: [id])
      userId  Int
      ```

    **Luego, en el mismo `model Order`**, agregar el campo `orgId` (FK opcional a User con nombre relacional `OrgOrders`):

    ```prisma
      // v2: contexto de organización al pagar (carrito de la org en vez de la persona).
      // FK apunta a User con type=ORGANIZATION — validación en service layer (Pitfall #6).
      org    User?  @relation("OrgOrders", fields: [orgId], references: [id])
      orgId  Int?
    ```

    Y agregar el índice:

    ```prisma
      @@index([orgId])
    ```

    **(d) En `model User`, primero RENOMBRAR la relación inversa existente a `"UserOrders"`** (hoy es solo `orders Order[]` sin nombre — línea ~344). Esta edición debe quedar en el MISMO commit / mismo guardado de archivo que (c) para evitar un estado intermedio con `prisma validate` rojo:

    - ANTES (línea ~344):
      ```prisma
      orders         Order[]
      ```
    - DESPUÉS:
      ```prisma
      orders         Order[] @relation("UserOrders")
      ```

    **Luego, en el mismo `model User`**, agregar la relación inversa `OrgOrders` junto a las otras relaciones de v2:

    ```prisma
      orgOrders      Order[] @relation("OrgOrders")
    ```

    **Validación intermedia obligatoria antes de continuar con (e)/(f):**

    ```bash
    cd apps/api && pnpm prisma validate
    ```

    Debe exit 0. Si falla con "Ambiguous relation detected between User and Order", revisar que tanto Order.owner como User.orders tengan exactamente `@relation("UserOrders", ...)` / `@relation("UserOrders")`. Si falta cualquiera de los dos extremos, Prisma todavía ve dos relaciones sin nombre y rechaza.

    **(e) En `model Article`**, agregar los campos para artículos patrocinados (KEY DECISION #4 — Plan 05 absorbe Article v2). Insertar después del campo `image` y antes de la relación `tags`:

    ```prisma
      // v2: artículos patrocinados — siguen el mismo flujo de estados que Event/Spot/Hero
      status       PublicationStatus @default(DRAFT)
      statusReason String?
      // v2: dueño del artículo patrocinado (null para artículos editoriales legacy)
      owner        User?             @relation("ArticleOwner", fields: [userId], references: [id])
      userId       Int?
    ```

    Y agregar la relación inversa al final del modelo Article, junto a `events Event[]` y `likes Like[]`:

    ```prisma
      orderItems   OrderItem[]
    ```

    Y agregar índice:

    ```prisma
      @@index([userId])
      @@index([status])
    ```

    **(f) En `model User`**, agregar la relación inversa `ArticleOwner`:

    ```prisma
      articles   Article[] @relation("ArticleOwner")
    ```

    **(g) Validar, migrar, generar:**

    ```bash
    cd apps/api
    pnpm prisma validate
    pnpm prisma migrate dev --name sch05_category_orders_v2
    pnpm prisma generate
    pnpm tsc --noEmit
    ```

    Los cambios son aditivos excepto por el rename de la relación User↔Order (de "sin nombre" a "UserOrders"). El rename a nivel de schema NO cambia las columnas en MySQL (la FK `userId` y la tabla `Order` siguen iguales): Prisma solo persiste el nombre relacional en metadata interna. La migración esperada por `prisma migrate dev` debe ser puramente aditiva (nuevas columnas `orgId`, `articleId`, `userId` en Article, nuevos `status`/`statusReason`/`orgOrders`/etc.) sin DROP/RECREATE de las FKs existentes.

    Pitfall #2 advierte que en producción agregar valor a enum MySQL en tablas grandes puede ser lento — para el tamaño actual no es problema.

    Verificación adicional: re-correr el seed para confirmar que nada de la data existente se rompió:

    ```bash
    cd apps/api && pnpm prisma:seed
    ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm prisma validate && pnpm prisma migrate dev --name sch05_category_orders_v2 && pnpm prisma generate && pnpm tsc --noEmit && pnpm prisma:seed</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A5 "enum OrderItemType" apps/api/prisma/schema.prisma | grep -q "ARTICLE"` exits 0
    - `grep -E "article\\s+Article\\?\\s+@relation" apps/api/prisma/schema.prisma` retorna 1 línea (en OrderItem)
    - `grep -E "articleId\\s+Int\\?" apps/api/prisma/schema.prisma` retorna 1 línea
    - `grep -q "@@index(\\[articleId\\])" apps/api/prisma/schema.prisma` exits 0
    - `grep -q "@relation(\"UserOrders\"" apps/api/prisma/schema.prisma` exits 0 — debe encontrar AMBOS extremos
    - `grep -c "@relation(\"UserOrders\"" apps/api/prisma/schema.prisma` retorna 2 (Order.owner y User.orders)
    - `grep -q "@relation(\"OrgOrders\"" apps/api/prisma/schema.prisma` exits 0
    - `grep -c "@relation(\"OrgOrders\"" apps/api/prisma/schema.prisma` retorna 2 (Order.org y User.orgOrders)
    - `grep -E "orgId\\s+Int\\?" apps/api/prisma/schema.prisma` retorna >= 1 línea (al menos en Order)
    - `grep -q "@@index(\\[orgId\\])" apps/api/prisma/schema.prisma` exits 0
    - `grep -q "@relation(\"ArticleOwner\"" apps/api/prisma/schema.prisma` exits 0
    - `grep -E "status\\s+PublicationStatus\\s+@default\\(DRAFT\\)" apps/api/prisma/schema.prisma | wc -l` retorna >= 4 (Event, Spot, Hero, Article)
    - `grep -q "orderItems\\s*OrderItem\\[\\]" apps/api/prisma/schema.prisma` exits 0 (la línea en Article)
    - `grep -q "articles\\s*Article\\[\\]\\s*@relation(\"ArticleOwner\")" apps/api/prisma/schema.prisma` exits 0
    - `ls apps/api/prisma/migrations | grep -q "sch05_category_orders_v2"` exits 0
    - `cd apps/api && pnpm prisma validate` exits 0 (no debe aparecer "Ambiguous relation detected between User and Order")
    - `cd apps/api && pnpm tsc --noEmit` exits 0
    - `cd apps/api && pnpm prisma:seed` exits 0 (data existente no rota)
  </acceptance_criteria>
  <done>
    OrderItemType admite ARTICLE; OrderItem puede referenciar un Article; la relación
    User↔Order existente quedó nombrada como UserOrders (prerrequisito Prisma); Order puede
    pertenecer al contexto de una org vía la nueva relación OrgOrders; Article tiene
    status + dueño + relación a OrderItem. Migración sch05_category_orders_v2 aplicada
    y seed corre limpio.
  </done>
</task>

</tasks>

<verification>
- Category con 5 campos nuevos
- OrderItemType incluye ARTICLE
- OrderItem.articleId presente
- Order.orgId presente
- Relación User↔Order existente renombrada explícitamente a "UserOrders" (Order.owner y User.orders)
- Nueva relación User↔Order "OrgOrders" agregada (Order.org y User.orgOrders)
- Article con status, statusReason, userId, orderItems[]
- Relación inversa ArticleOwner declarada en User
- Migración sch05_category_orders_v2 aplicada
- pnpm prisma validate / tsc --noEmit / pnpm prisma:seed todos exit 0
</verification>

<success_criteria>
SCH-05 implementado. El schema soporta el contexto org en órdenes, el tipo ARTICLE en el carrito, y el flujo completo de artículos patrocinados. Plan 06 puede agregar los modelos de servicios y CRM sobre un schema ya estable.
</success_criteria>

<output>
Crear `.planning/phases/08-schema-v2/08-05-SUMMARY.md` documentando:
- Category: 5 campos nuevos con defaults
- OrderItemType: ARTICLE agregado
- OrderItem.articleId con FK opcional
- Relación User↔Order existente renombrada a "UserOrders" (prerrequisito Prisma para agregar OrgOrders sin ambigüedad)
- Order.orgId con nueva relación OrgOrders
- Article: status (default DRAFT), statusReason, userId (relación ArticleOwner), orderItems[]
- Decisión locked KEY #4: Article.status absorbido en SCH-05 (no Plan 07 separado)
- Decisión locked Pitfall #5: @@unique([orderId, type]) se mantiene — máximo 1 ARTICLE por orden
- Decisión locked Pitfall #6: enforcement de user.type='ORGANIZATION' para Order.orgId es responsabilidad del service layer
- Notas para callers existentes: si algún código TS importa `Prisma.OrderInclude` o similar y usaba `orders` sin discriminar, el nombre del campo no cambia (sigue siendo `orders` en User y `owner` en Order); solo cambia el metadata interno de Prisma. No se esperan breaks en código existente.
</output>
</output>
