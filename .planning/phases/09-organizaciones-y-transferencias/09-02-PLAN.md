---
phase: 09-organizaciones-y-transferencias
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/common/org-context/org-context.types.ts
  - apps/api/src/common/org-context/org-context.guard.ts
  - apps/api/src/common/org-context/org-context.decorator.ts
  - apps/api/src/common/org-context/org-context.module.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - ORG-01
must_haves:
  truths:
    - "Cualquier endpoint anotado con @UseGuards(OrgContextGuard) y @OrgContext() recibe el contexto si X-Org-Context está presente y el user es miembro"
    - "Si X-Org-Context apunta a una org de la que el user NO es miembro, la request retorna 403"
    - "Si X-Org-Context apunta a un id que no existe o cuyo type !== ORGANIZATION, la request retorna 404"
    - "Si X-Org-Context apunta a una org blocked, la request retorna 403"
    - "Si el header está ausente, el guard permite continuar y @OrgContext() devuelve null (modo personal)"
  artifacts:
    - path: "apps/api/src/common/org-context/org-context.types.ts"
      provides: "Tipo OrgContextDto compartido"
      contains: "OrgContextDto"
    - path: "apps/api/src/common/org-context/org-context.guard.ts"
      provides: "Guard que valida X-Org-Context y popula req.orgContext"
      contains: "OrgContextGuard"
    - path: "apps/api/src/common/org-context/org-context.decorator.ts"
      provides: "Decorator @OrgContext() para inyectar el contexto en handlers"
      contains: "OrgContext"
    - path: "apps/api/src/common/org-context/org-context.module.ts"
      provides: "Standalone module exportando guard/decorator (sin depender de OrganizationsModule)"
      contains: "OrgContextModule"
  key_links:
    - from: "apps/api/src/common/org-context/org-context.guard.ts"
      to: "prisma.orgMember (validación de membresía) + prisma.user (validación type=ORGANIZATION)"
      via: "PrismaService inyectado"
      pattern: "prisma\\.orgMember\\.findUnique"
    - from: "apps/api/src/common/org-context/org-context.decorator.ts"
      to: "req.orgContext (set por el guard)"
      via: "ctx.switchToHttp().getRequest().orgContext"
      pattern: "createParamDecorator"
---

<objective>
Crear el módulo standalone `common/org-context/` que provee el guard `OrgContextGuard`, el decorator `@OrgContext()` y el tipo `OrgContextDto`. Estos artefactos son importados por cualquier módulo que necesite operar bajo contexto de organización (transfers, events, spots, heroes, orders en 09-04 y 09-05).

El guard lee el header `X-Org-Context: <orgId>`, valida que el usuario autenticado es miembro de esa org y expone `req.orgContext: { orgId: number; role: OrgRole }`. Si el header está ausente, el guard permite continuar y `@OrgContext()` devuelve `null` (modo personal).

Purpose: ORG-01. Es el contrato compartido entre la mayoría de fases v2. Implementarlo como **módulo standalone** (NO dentro de OrganizationsModule) evita dependencias circulares cuando `transfers` y otros módulos lo importen.

Output: Módulo `OrgContextModule` registrado globalmente en AppModule, exportando guard + decorator + tipo, listo para ser inyectado vía `@UseGuards(JwtAuthGuard, OrgContextGuard)` en cualquier controller.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/prisma/schema.prisma
@docs/API-RULES.md
@apps/api/src/auth/jwt-auth.guard.ts
@apps/api/src/auth/current-user.decorator.ts
@apps/api/src/app.module.ts

<interfaces>
<!-- Contratos clave que el executor necesita. -->

From apps/api/src/auth/current-user.decorator.ts:
```typescript
export type JwtUser = { sub: number; email: string; role: string };
```

From apps/api/prisma/schema.prisma (modelos involucrados):
```prisma
enum OrgRole { OWNER MEMBER }

model OrgMember {
  userId Int
  orgId  Int
  role   OrgRole @default(MEMBER)
  @@unique([userId, orgId])
}

model User {
  id      Int
  type    UserType  // PERSON | ORGANIZATION
  blocked Boolean
}
```

Patrón del guard existente (apps/api/src/auth/jwt-auth.guard.ts):
- `canActivate(ctx): Promise<boolean>`
- `ctx.switchToHttp().getRequest<Request>()`
- Asigna `(req as Request & { user: ... }).user = payload` para que @CurrentUser() lo lea.

Este nuevo guard sigue el mismo patrón pero asigna `req.orgContext` en vez de `req.user`. Debe correr DESPUÉS de JwtAuthGuard (decisión: el orden en `@UseGuards(JwtAuthGuard, OrgContextGuard)` es Nest-deterministic — JwtAuthGuard primero).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Definir OrgContextDto type + decorator @OrgContext()</name>
  <read_first>
    - apps/api/src/auth/current-user.decorator.ts (patrón exacto del decorator a copiar)
    - apps/api/prisma/schema.prisma (enum OrgRole — debe importarse de '@prisma/client')
  </read_first>
  <files>
    - apps/api/src/common/org-context/org-context.types.ts
    - apps/api/src/common/org-context/org-context.decorator.ts
  </files>
  <action>
    Crear `apps/api/src/common/org-context/org-context.types.ts`:
    ```typescript
    import type { OrgRole } from '@prisma/client';

    /**
     * Contexto de organización resuelto desde el header X-Org-Context.
     * El guard lo asigna a req.orgContext; null cuando no hay header (modo personal).
     */
    export type OrgContextDto = {
      orgId: number;
      role: OrgRole;
    };
    ```

    Crear `apps/api/src/common/org-context/org-context.decorator.ts`:
    ```typescript
    import { createParamDecorator, ExecutionContext } from '@nestjs/common';
    import type { OrgContextDto } from './org-context.types';

    /**
     * Inyecta el contexto de organización resuelto por OrgContextGuard.
     * Devuelve null cuando el endpoint se llamó sin el header X-Org-Context.
     */
    export const OrgContext = createParamDecorator(
      (_data: unknown, ctx: ExecutionContext): OrgContextDto | null => {
        const req = ctx.switchToHttp().getRequest();
        return (req.orgContext as OrgContextDto | undefined) ?? null;
      },
    );
    ```

    Augmentar el tipo `Request` de express agregando `apps/api/src/common/org-context/org-context.types.ts` la siguiente declaración al final:
    ```typescript
    declare module 'express-serve-static-core' {
      interface Request {
        orgContext?: OrgContextDto;
      }
    }
    ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "org-context" || echo "Types + decorator OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export type OrgContextDto" apps/api/src/common/org-context/org-context.types.ts` → exit 0
    - `grep -q "orgId: number" apps/api/src/common/org-context/org-context.types.ts` → exit 0
    - `grep -q "role: OrgRole" apps/api/src/common/org-context/org-context.types.ts` → exit 0
    - `grep -q "export const OrgContext" apps/api/src/common/org-context/org-context.decorator.ts` → exit 0
    - `grep -q "createParamDecorator" apps/api/src/common/org-context/org-context.decorator.ts` → exit 0
    - `grep -q "declare module 'express-serve-static-core'" apps/api/src/common/org-context/org-context.types.ts` → exit 0
  </acceptance_criteria>
  <done>Tipos y decorator compilan sin errores. Request tiene `orgContext` augmentado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar OrgContextGuard con validación de header + membresía</name>
  <read_first>
    - apps/api/src/auth/jwt-auth.guard.ts (patrón exacto: canActivate, PrismaService inyectado, asignación a req)
    - apps/api/utils/prisma/prisma.service.ts (PrismaService — método findUnique disponible)
    - apps/api/prisma/schema.prisma (OrgMember @@unique([userId, orgId]) → composite key se llama userId_orgId)
  </read_first>
  <files>
    - apps/api/src/common/org-context/org-context.guard.ts
  </files>
  <action>
    Crear `apps/api/src/common/org-context/org-context.guard.ts`:

    ```typescript
    import {
      CanActivate,
      ExecutionContext,
      ForbiddenException,
      Injectable,
      NotFoundException,
      UnauthorizedException,
    } from '@nestjs/common';
    import type { Request } from 'express';
    import { PrismaService } from '../../../utils/prisma/prisma.service';
    import type { JwtUser } from '../../auth/current-user.decorator';
    import type { OrgContextDto } from './org-context.types';

    /**
     * Lee el header X-Org-Context y valida que el usuario autenticado sea miembro
     * de esa organización. Si el header está ausente, permite continuar (req.orgContext queda undefined).
     *
     * REQUIERE que JwtAuthGuard se haya ejecutado antes (consume req.user).
     */
    @Injectable()
    export class OrgContextGuard implements CanActivate {
      constructor(private readonly prisma: PrismaService) {}

      async canActivate(ctx: ExecutionContext): Promise<boolean> {
        const req = ctx.switchToHttp().getRequest<Request>();
        const raw = req.headers['x-org-context'];
        if (!raw) return true; // modo personal: el endpoint se ejecuta sin contexto de org

        const user = (req as Request & { user?: JwtUser }).user;
        if (!user) throw new UnauthorizedException('OrgContextGuard requiere JwtAuthGuard previo');

        const orgId = Number(Array.isArray(raw) ? raw[0] : raw);
        if (!Number.isInteger(orgId) || orgId <= 0) {
          throw new ForbiddenException('X-Org-Context inválido');
        }

        // 1. Validar que la org existe y es del tipo ORGANIZATION y no está blocked.
        const org = await this.prisma.user.findUnique({
          where: { id: orgId },
          select: { id: true, type: true, blocked: true },
        });
        if (!org || org.type !== 'ORGANIZATION') {
          throw new NotFoundException('Organización no encontrada');
        }
        if (org.blocked) {
          throw new ForbiddenException('Organización bloqueada');
        }

        // 2. Validar membresía del usuario autenticado.
        const member = await this.prisma.orgMember.findUnique({
          where: { userId_orgId: { userId: user.sub, orgId } },
          select: { role: true },
        });
        if (!member) {
          throw new ForbiddenException('No eres miembro de esta organización');
        }

        const context: OrgContextDto = { orgId, role: member.role };
        (req as Request & { orgContext: OrgContextDto }).orgContext = context;
        return true;
      }
    }
    ```

    Nota: la ruta relativa a PrismaService es `../../../utils/prisma/prisma.service` porque `common/org-context` está dos niveles bajo `src/`. Verificar tras crear.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "org-context\\.guard" || echo "Guard OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class OrgContextGuard" apps/api/src/common/org-context/org-context.guard.ts` → exit 0
    - `grep -q "x-org-context" apps/api/src/common/org-context/org-context.guard.ts` → exit 0
    - `grep -q "prisma.orgMember.findUnique" apps/api/src/common/org-context/org-context.guard.ts` → exit 0
    - `grep -q "type !== 'ORGANIZATION'" apps/api/src/common/org-context/org-context.guard.ts` → exit 0
    - `grep -q "org.blocked" apps/api/src/common/org-context/org-context.guard.ts` → exit 0
    - `grep -q "if (!raw) return true" apps/api/src/common/org-context/org-context.guard.ts` → exit 0 (verifica el "modo personal" cuando ausente)
    - El guard NO inyecta `JwtService` ni `ConfigService` — solo `PrismaService`.
  </acceptance_criteria>
  <done>Guard compila, devuelve true sin header, valida membresía + tipo ORGANIZATION + blocked cuando el header está presente, asigna `req.orgContext`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear OrgContextModule global + registrar en AppModule</name>
  <read_first>
    - apps/api/src/auth/auth.module.ts (patrón de module que exporta guards reutilizables)
    - apps/api/src/app.module.ts (dónde insertar el import + uso de @Global decorator)
    - apps/api/utils/prisma/prisma.module.ts (PrismaModule ya es global; el guard puede inyectar PrismaService sin importar nada)
  </read_first>
  <files>
    - apps/api/src/common/org-context/org-context.module.ts
    - apps/api/src/app.module.ts
  </files>
  <action>
    Crear `apps/api/src/common/org-context/org-context.module.ts`:

    ```typescript
    import { Global, Module } from '@nestjs/common';
    import { OrgContextGuard } from './org-context.guard';

    /**
     * Módulo global que provee OrgContextGuard. El decorator @OrgContext() y el tipo
     * OrgContextDto se importan directamente desde sus archivos — no necesitan provider.
     *
     * Marcado @Global() para que cualquier controller pueda usar @UseGuards(OrgContextGuard)
     * sin importar este módulo en cada feature module.
     */
    @Global()
    @Module({
      providers: [OrgContextGuard],
      exports: [OrgContextGuard],
    })
    export class OrgContextModule {}
    ```

    Editar `apps/api/src/app.module.ts`:
    - Importar: `import { OrgContextModule } from './common/org-context/org-context.module';`
    - Agregarlo al array `imports`. Ubicarlo justo después de `AuthModule` (línea 34 aprox) para que se cargue temprano.

    NO crear barrel file (`index.ts`). Los consumidores importan directamente:
    - `import { OrgContextGuard } from 'src/common/org-context/org-context.guard'`
    - `import { OrgContext } from 'src/common/org-context/org-context.decorator'`
    - `import type { OrgContextDto } from 'src/common/org-context/org-context.types'`
  </action>
  <verify>
    <automated>cd apps/api && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` desde apps/api exit 0
    - `grep -q "@Global" apps/api/src/common/org-context/org-context.module.ts` → exit 0
    - `grep -q "OrgContextModule" apps/api/src/app.module.ts` → exit 0
    - `grep -q "providers.*OrgContextGuard" apps/api/src/common/org-context/org-context.module.ts` → exit 0
    - `grep -q "exports.*OrgContextGuard" apps/api/src/common/org-context/org-context.module.ts` → exit 0
    - Curl smoke (manual): un endpoint dummy con `@UseGuards(JwtAuthGuard, OrgContextGuard)` + `@OrgContext()` devuelve null sin header, devuelve `{orgId, role}` con header válido, devuelve 403 con header de org no-miembro.
  </acceptance_criteria>
  <done>Módulo global registrado, la API arranca, el guard está disponible para inyección en transfers (09-04) y demás módulos (09-05).</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` en apps/api exit 0
- `pnpm build` en apps/api exit 0
- Verificación de import resolution: el guard puede resolverse desde transfers/events/spots/heroes/orders sin warnings de circular dependency
</verification>

<success_criteria>
ORG-01 cubierto: `@UseGuards(OrgContextGuard)` + `@OrgContext() ctx: OrgContextDto | null` funciona en cualquier controller del proyecto. El header `X-Org-Context` es validado contra OrgMember en cada request. 09-04 (transfers) y 09-05 (integraciones) pueden empezar inmediatamente al usar este contrato.
</success_criteria>

<output>
After completion, create `.planning/phases/09-organizaciones-y-transferencias/09-02-SUMMARY.md`
</output>
