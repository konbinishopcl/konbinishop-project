---
phase: 09-organizaciones-y-transferencias
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/api/src/organizations/organizations.controller.ts
  - apps/api/src/organizations/organizations.service.ts
  - apps/api/src/organizations/dto/invite-member.dto.ts
  - apps/api/src/organizations/dto/update-member-role.dto.ts
  - apps/api/utils/templates/mail.templates.ts
  - apps/api/services/mailgun/mail.service.ts
autonomous: true
requirements:
  - ORG-03
must_haves:
  truths:
    - "GET /organizations/:id/members lista a todos los miembros si quien consulta es miembro o admin"
    - "POST /organizations/:id/members/invite genera token UUID con expiresAt=now+72h y envía email; solo OWNER puede invitar"
    - "POST /organizations/invitations/:token/accept agrega al user autenticado como MEMBER y borra la invitación"
    - "Invitación expirada (expiresAt < now) o inexistente devuelve 401"
    - "PATCH /organizations/:id/members/:userId cambia role; no se puede degradar al último OWNER"
    - "DELETE /organizations/:id/members/:userId elimina al miembro; OWNER no puede eliminarse si es el último"
  artifacts:
    - path: "apps/api/src/organizations/dto/invite-member.dto.ts"
      provides: "DTO con email del invitado"
      contains: "InviteMemberDto"
    - path: "apps/api/src/organizations/dto/update-member-role.dto.ts"
      provides: "DTO con role nuevo"
      contains: "UpdateMemberRoleDto"
    - path: "apps/api/utils/templates/mail.templates.ts"
      provides: "Plantilla orgInvitationTemplate"
      contains: "orgInvitationTemplate"
    - path: "apps/api/services/mailgun/mail.service.ts"
      provides: "Método sendOrgInvitation"
      contains: "sendOrgInvitation"
  key_links:
    - from: "apps/api/src/organizations/organizations.service.ts (invite)"
      to: "prisma.orgInvitation.create + MailService.sendOrgInvitation"
      via: "crypto.randomUUID() para token"
      pattern: "crypto\\.randomUUID"
    - from: "apps/api/src/organizations/organizations.service.ts (acceptInvitation)"
      to: "prisma.orgMember.create + prisma.orgInvitation.delete"
      via: "$transaction"
      pattern: "prisma\\.\\$transaction"
    - from: "apps/api/src/organizations/organizations.service.ts (removeMember/changeRole)"
      to: "validación 'último OWNER' con count(role=OWNER) > 1"
      via: "Service-layer enforcement"
      pattern: "OrgRole.OWNER"
---

<objective>
Extender el módulo `organizations` con membresías e invitaciones. Crea 5 endpoints REST: listar miembros, invitar por email (token UUID, 72h), aceptar invitación, cambiar rol, eliminar miembro. Añade la plantilla de email `orgInvitationTemplate` y el método `sendOrgInvitation` al MailService.

Purpose: ORG-03. Cierra el ciclo organizacional — sin invitaciones, una organización solo tiene al OWNER (su creador) y no es realmente colaborativa. Estos endpoints son consumidos por el dashboard del frontend (Phase 11+).

Output: 5 endpoints REST en `OrganizationsController`, métodos correspondientes en `OrganizationsService`, plantilla MJML + helper de mail. Validación de "último OWNER" enforced en service layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/api/prisma/schema.prisma
@docs/API-RULES.md
@apps/api/utils/templates/mail.templates.ts
@apps/api/services/mailgun/mail.service.ts
@.planning/phases/09-organizaciones-y-transferencias/09-01-SUMMARY.md

<interfaces>
<!-- Contratos que el executor necesita -->

From apps/api/prisma/schema.prisma (OrgInvitation ya existe post Phase 8):
```prisma
model OrgInvitation {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  email     String
  orgId     Int
  org       User     @relation("OrgInvitations", fields: [orgId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model OrgMember {
  userId Int
  orgId  Int
  role   OrgRole  // OWNER | MEMBER
  @@unique([userId, orgId])
}
```

From apps/api/services/mailgun/mail.service.ts (existing methods to follow as pattern):
```typescript
async sendPasswordReset(to: string, resetUrl: string): Promise<void>
async sendWelcome(to: string, name: string): Promise<void>
```

From apps/api/src/organizations/organizations.service.ts (existe desde 09-01):
- `OrganizationsService` con métodos `create`, `findOne`, `update`, `remove` ya implementados.
- Helper privado `assertOwnerOrAdmin(orgId, user)` reusable.

From OrganizationsModule (09-01):
- Imports: `AuthModule, MailgunModule, AuditModule` — MailService ya está disponible para inyección.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear DTOs + plantilla email + método MailService.sendOrgInvitation</name>
  <read_first>
    - apps/api/utils/templates/mail.templates.ts (patrón exacto de las plantillas existentes — passwordResetTemplate como modelo)
    - apps/api/services/mailgun/mail.service.ts (firma de sendPasswordReset como modelo)
    - apps/api/src/organizations/dto/create-organization.dto.ts (creado en 09-01, patrón de DTO)
  </read_first>
  <files>
    - apps/api/src/organizations/dto/invite-member.dto.ts
    - apps/api/src/organizations/dto/update-member-role.dto.ts
    - apps/api/utils/templates/mail.templates.ts
    - apps/api/services/mailgun/mail.service.ts
  </files>
  <action>
    **1. DTO `InviteMemberDto`** (`apps/api/src/organizations/dto/invite-member.dto.ts`):
    ```typescript
    import { IsEmail } from 'class-validator';
    export class InviteMemberDto {
      @IsEmail()
      email!: string;
    }
    ```

    **2. DTO `UpdateMemberRoleDto`** (`apps/api/src/organizations/dto/update-member-role.dto.ts`):
    ```typescript
    import { IsEnum } from 'class-validator';
    import { OrgRole } from '@prisma/client';
    export class UpdateMemberRoleDto {
      @IsEnum(OrgRole)
      role!: OrgRole; // 'OWNER' | 'MEMBER'
    }
    ```

    **3. Agregar `orgInvitationTemplate` al final de `apps/api/utils/templates/mail.templates.ts`**:
    ```typescript
    export function orgInvitationTemplate(orgName: string, inviteUrl: string, expiresInHours: number) {
      return {
        subject: `Invitación a unirte a ${orgName} en Konbini`,
        html: `
          <div style="font-family:sans-serif;max-width:560px;margin:0 auto;padding:24px;">
            <h2 style="color:#111;">Has sido invitado a ${orgName}</h2>
            <p>Has recibido una invitación para unirte a <strong>${orgName}</strong> en Konbini.</p>
            <p style="margin:24px 0;">
              <a href="${inviteUrl}" style="background:#FF6B00;color:#fff;padding:12px 24px;border-radius:6px;text-decoration:none;display:inline-block;">Aceptar invitación</a>
            </p>
            <p style="color:#666;font-size:14px;">Este enlace expira en ${expiresInHours} horas.</p>
            <p style="color:#666;font-size:14px;">Si no esperabas esta invitación, puedes ignorar este correo.</p>
          </div>
        `,
      };
    }
    ```
    Seguir EXACTAMENTE el patrón del export `passwordResetTemplate` (verificar firma exacta del retorno `{ subject, html }`).

    **4. Agregar método a `apps/api/services/mailgun/mail.service.ts`**:
    - Importar `orgInvitationTemplate` en la lista de imports al inicio del archivo.
    - Agregar dentro de la clase `MailService`:
    ```typescript
    async sendOrgInvitation(to: string, orgName: string, inviteUrl: string, expiresInHours = 72): Promise<void> {
      const { subject, html } = orgInvitationTemplate(orgName, inviteUrl, expiresInHours);
      await this.mailgun.send({ to, subject, html });
    }
    ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(invite-member|update-member-role|mail\\.templates|mail\\.service)" || echo "DTOs + mail OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class InviteMemberDto" apps/api/src/organizations/dto/invite-member.dto.ts` → exit 0
    - `grep -q "class UpdateMemberRoleDto" apps/api/src/organizations/dto/update-member-role.dto.ts` → exit 0
    - `grep -q "orgInvitationTemplate" apps/api/utils/templates/mail.templates.ts` → exit 0
    - `grep -q "sendOrgInvitation" apps/api/services/mailgun/mail.service.ts` → exit 0
    - El método importa `orgInvitationTemplate` desde el barrel de templates.
  </acceptance_criteria>
  <done>DTOs creados, plantilla agregada, MailService extendido. TS compila.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar métodos de membresía + invitación en OrganizationsService</name>
  <read_first>
    - apps/api/src/organizations/organizations.service.ts (creado en 09-01 — extender, no reemplazar)
    - apps/api/src/auth/auth.service.ts (patrón de manejo de tokens y expiración — buscar resetToken)
    - apps/api/prisma/schema.prisma (relaciones inversas en User: memberships, orgMembers, orgInvitations)
  </read_first>
  <files>
    - apps/api/src/organizations/organizations.service.ts
  </files>
  <action>
    Extender la clase `OrganizationsService` con 5 métodos públicos:

    **1. `async listMembers(orgId: number, user: JwtUser)`**
    - Verificar que orgId es ORGANIZATION (reusar el guard pattern del findOne existente).
    - Verificar que el user es miembro o ADMIN+. Si no → `ForbiddenException`.
    - `return prisma.orgMember.findMany({ where: { orgId }, include: { user: { select: { id, email, firstname, lastname, handle } } }, orderBy: { createdAt: 'asc' } })`

    **2. `async inviteMember(orgId: number, dto: InviteMemberDto, user: JwtUser, req?: Request)`**
    - Reusar `assertOwnerOrAdmin(orgId, user)` del 09-01.
    - Verificar que `dto.email` no corresponde a un user que YA es miembro: query `prisma.user.findUnique({ where: { email: dto.email } })` → si existe, `prisma.orgMember.findUnique({ where: { userId_orgId: { userId: existing.id, orgId } } })` → si existe → `ConflictException('Este usuario ya es miembro')`.
    - Generar token con `import { randomUUID } from 'crypto'`: `const token = randomUUID()` (sin hash — single-use, 72h, va en URL).
    - `const expiresAt = new Date(Date.now() + 72 * 60 * 60 * 1000)` (72 horas).
    - `await prisma.orgInvitation.create({ data: { token, email: dto.email, orgId, expiresAt } })`
    - Construir URL: `const frontendUrl = this.config.get<string>('FRONTEND_URL', 'http://localhost:3000'); const inviteUrl = '${frontendUrl}/invitations/${token}'`.
    - Cargar nombre de la org: `const org = await prisma.user.findUnique({ where: { id: orgId }, select: { firstname: true } })`.
    - `await this.mail.sendOrgInvitation(dto.email, org.firstname ?? 'Konbini', inviteUrl, 72)`. Envolver en try/catch — el fallo del mail NO debe revertir la invitación creada (igual que audit). Log warning.
    - Audit: `this.audit.log({ userId: user.sub, action: 'CREATE', entity: 'USER', entityId: orgId, metadata: { invitation: dto.email }, req })`.
    - Return `{ id: invitation.id, email: dto.email, expiresAt }` (NO devolver el token plano).

    NOTA: Inyectar `ConfigService` y `MailService` si no están ya en el constructor — verificar `OrganizationsService` actual (creado en 09-01) y agregar al constructor si faltan.

    **3. `async acceptInvitation(token: string, user: JwtUser, req?: Request)`**
    - `const invitation = await prisma.orgInvitation.findUnique({ where: { token } })`
    - Si null → `UnauthorizedException('Invitación inválida')`.
    - Si `invitation.expiresAt < new Date()` → eliminar y `UnauthorizedException('Invitación expirada')`.
    - Verificar email match: cargar `user` real para obtener email: `const me = await prisma.user.findUnique({ where: { id: user.sub }, select: { email: true } })`. Si `me.email !== invitation.email` → `ForbiddenException('Esta invitación es para otro email')`.
    - Verificar que no es ya miembro: `prisma.orgMember.findUnique({ where: { userId_orgId: { userId: user.sub, orgId: invitation.orgId } } })` → si existe, borrar la invitación y devolver `{ alreadyMember: true }`.
    - En `prisma.$transaction`:
      - `tx.orgMember.create({ data: { userId: user.sub, orgId: invitation.orgId, role: OrgRole.MEMBER } })`
      - `tx.orgInvitation.delete({ where: { id: invitation.id } })`
    - Audit CREATE/USER orgId con metadata `{ acceptedInvitation: invitation.id }`.
    - Return `{ orgId: invitation.orgId, role: 'MEMBER' }`.

    **4. `async changeMemberRole(orgId: number, memberUserId: number, dto: UpdateMemberRoleDto, user: JwtUser, req?: Request)`**
    - `assertOwnerOrAdmin(orgId, user)`.
    - Cargar membership: `const member = await prisma.orgMember.findUnique({ where: { userId_orgId: { userId: memberUserId, orgId } } })`. Si null → `NotFoundException`.
    - Si bajando OWNER→MEMBER, verificar que NO es el único OWNER: `const ownerCount = await prisma.orgMember.count({ where: { orgId, role: OrgRole.OWNER } })`. Si `member.role === 'OWNER' && dto.role === 'MEMBER' && ownerCount <= 1` → `BadRequestException('No puedes degradar al único OWNER de la organización')`.
    - `await prisma.orgMember.update({ where: { userId_orgId: { userId: memberUserId, orgId } }, data: { role: dto.role } })`.
    - Audit UPDATE/USER orgId con metadata `{ memberUserId, newRole: dto.role }`.
    - Return updated member.

    **5. `async removeMember(orgId: number, memberUserId: number, user: JwtUser, req?: Request)`**
    - `assertOwnerOrAdmin(orgId, user)`.
    - Cargar member; si null → `NotFoundException`.
    - Si `member.role === 'OWNER'` → contar OWNERs como en (4); si ≤1 → `BadRequestException('No puedes eliminar al único OWNER')`.
    - `await prisma.orgMember.delete({ where: { userId_orgId: { userId: memberUserId, orgId } } })`.
    - Audit DELETE/USER orgId con metadata `{ memberUserId }`.
    - Return `{ removed: true }`.

    Importar `randomUUID` de `'crypto'` y `OrgRole` de `'@prisma/client'`.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "organizations\\.service" || echo "Service OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "randomUUID" apps/api/src/organizations/organizations.service.ts` → exit 0
    - `grep -q "72 \\* 60 \\* 60 \\* 1000" apps/api/src/organizations/organizations.service.ts` → exit 0 (72h expiry literal)
    - `grep -q "OrgRole.OWNER" apps/api/src/organizations/organizations.service.ts` → exit 0
    - `grep -q "ownerCount" apps/api/src/organizations/organizations.service.ts` → exit 0 (verifica enforcement del último OWNER)
    - `grep -q "sendOrgInvitation" apps/api/src/organizations/organizations.service.ts` → exit 0
    - `grep -q "acceptInvitation" apps/api/src/organizations/organizations.service.ts` → exit 0
    - `grep -q "expiresAt < new Date()" apps/api/src/organizations/organizations.service.ts` → exit 0
  </acceptance_criteria>
  <done>Los 5 métodos existen en el service, compilan, y enforcen las reglas de "último OWNER" y expiración 72h.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire endpoints en OrganizationsController</name>
  <read_first>
    - apps/api/src/organizations/organizations.controller.ts (existe desde 09-01 — extender con nuevos endpoints)
    - apps/api/src/events/events.controller.ts (patrón de routes anidadas y param parsing)
  </read_first>
  <files>
    - apps/api/src/organizations/organizations.controller.ts
  </files>
  <action>
    Extender `OrganizationsController` con los siguientes endpoints (todos requieren JwtAuthGuard, ninguno requiere OrgContextGuard porque la org se identifica por `:id` en la URL):

    ```typescript
    @Get(':id/members')
    @UseGuards(JwtAuthGuard)
    listMembers(@Param('id', ParseIntPipe) id: number, @CurrentUser() user: JwtUser) {
      return this.service.listMembers(id, user);
    }

    @Post(':id/members/invite')
    @UseGuards(JwtAuthGuard)
    inviteMember(
      @Param('id', ParseIntPipe) id: number,
      @Body() dto: InviteMemberDto,
      @CurrentUser() user: JwtUser,
      @Req() req: Request,
    ) {
      return this.service.inviteMember(id, dto, user, req);
    }

    @Post('invitations/:token/accept')
    @UseGuards(JwtAuthGuard)
    acceptInvitation(
      @Param('token') token: string,
      @CurrentUser() user: JwtUser,
      @Req() req: Request,
    ) {
      return this.service.acceptInvitation(token, user, req);
    }

    @Patch(':id/members/:userId')
    @UseGuards(JwtAuthGuard)
    changeMemberRole(
      @Param('id', ParseIntPipe) id: number,
      @Param('userId', ParseIntPipe) userId: number,
      @Body() dto: UpdateMemberRoleDto,
      @CurrentUser() user: JwtUser,
      @Req() req: Request,
    ) {
      return this.service.changeMemberRole(id, userId, dto, user, req);
    }

    @Delete(':id/members/:userId')
    @UseGuards(JwtAuthGuard)
    removeMember(
      @Param('id', ParseIntPipe) id: number,
      @Param('userId', ParseIntPipe) userId: number,
      @CurrentUser() user: JwtUser,
      @Req() req: Request,
    ) {
      return this.service.removeMember(id, userId, user, req);
    }
    ```

    Importar `InviteMemberDto` y `UpdateMemberRoleDto` desde sus archivos. La ruta `invitations/:token/accept` está deliberadamente SIN el prefijo `:id` (la invitación se identifica por token, no por org id).

    IMPORTANTE: en Nest, las rutas estáticas (`invitations/:token/accept`) deben declararse ANTES de rutas dinámicas (`:id/...`) para evitar conflictos. Verificar el orden de los decoradores en el controller: poner `acceptInvitation` arriba del bloque o usar prefix distinto. La práctica recomendada: declarar las rutas en este orden — `create, findOne, update, remove, listMembers, inviteMember, acceptInvitation, changeMemberRole, removeMember`. La ruta `invitations/:token/accept` no colisiona con `:id/members/invite` porque el primer segmento es distinto.
  </action>
  <verify>
    <automated>cd apps/api && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` desde apps/api exit 0
    - `grep -q "@Get(':id/members')" apps/api/src/organizations/organizations.controller.ts` → exit 0
    - `grep -q "@Post(':id/members/invite')" apps/api/src/organizations/organizations.controller.ts` → exit 0
    - `grep -q "invitations/:token/accept" apps/api/src/organizations/organizations.controller.ts` → exit 0
    - `grep -q "@Patch(':id/members/:userId')" apps/api/src/organizations/organizations.controller.ts` → exit 0
    - `grep -q "@Delete(':id/members/:userId')" apps/api/src/organizations/organizations.controller.ts` → exit 0
    - Smoke manual: crear org (09-01), invitar email X, capturar token de DB, accept con user de email X → resulta en OrgMember(userId=X, orgId=orgX, role=MEMBER); intentar accept con token expirado → 401.
  </acceptance_criteria>
  <done>Los 5 endpoints están registrados, la API arranca, y el flujo invitar→aceptar funciona end-to-end con el email recibido vía Mailgun.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` exit 0
- `pnpm build` exit 0
- Smoke: invitation pipeline (create → email → accept → member created → token deleted)
- Reglas de "último OWNER" verificadas con 2 OWNERs (degradar uno funciona) y con 1 OWNER (degradar/eliminar devuelve 400)
</verification>

<success_criteria>
ORG-03 cubierto: invitaciones por email con expiración de 72h, aceptación que crea OrgMember, cambio de rol y remoción de miembros con safeguards del último OWNER. Email funciona vía Mailgun.
</success_criteria>

<output>
After completion, create `.planning/phases/09-organizaciones-y-transferencias/09-03-SUMMARY.md`
</output>
