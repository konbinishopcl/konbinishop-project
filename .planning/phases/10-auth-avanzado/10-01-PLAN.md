---
plan: "10-01"
phase: 10
wave: 1
title: "2FA por email"
goal: "Login y registro local devuelven pendingToken; usuario confirma con código de 6 dígitos enviado por Mailgun y recibe JWT definitivo"
status: pending
autonomous: true
files_modified:
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/jwt-auth.guard.ts
  - apps/api/src/auth/two-fa.guard.ts
  - apps/api/src/auth/dto/verify-2fa.dto.ts
  - apps/api/services/mailgun/mail.service.ts
  - apps/api/utils/templates/mail.templates.ts
---

## Goal

Implementar 2FA por email (AUTH-01): `login()` y `register()` ya no emiten JWT definitivo, sino un `pendingToken` JWT de 15 min con claim `twoFaPending: true`. Se envía un código de 6 dígitos al email (hash SHA-256 en BD, expira 10 min). `POST /auth/2fa/verify` valida el código y emite el JWT real; `POST /auth/2fa/resend` reenvía código nuevo.

## Context

Schema ya tiene `twoFactorCode: String?` y `twoFactorExpiry: DateTime?` en `User` (Phase 8 sch). El patrón de tokens hasheados (SHA-256) está implementado en `forgotPassword`/`resetPassword` — replicarlo. `MailService` ya está inyectado en `AuthService`. `JwtService.sign()` acepta segundo argumento `{ expiresIn }`. Guard pattern: ver `JwtAuthGuard` en `apps/api/src/auth/jwt-auth.guard.ts` para estructura.

Archivos clave:
- `apps/api/src/auth/auth.service.ts` — métodos `register()`, `login()`, helpers `sign()` y `hashToken()` existentes
- `apps/api/src/auth/auth.controller.ts` — controller con `@ApiTags('auth')`, `@Controller('auth')`
- `apps/api/services/mailgun/mail.service.ts` — patrón `sendPasswordReset(to, url)`
- `apps/api/utils/templates/mail.templates.ts` — `renderTemplate({ title, greeting, body, ctaLabel?, ctaUrl? })` + `esc()` para escapar HTML
- `apps/api/src/auth/jwt-auth.guard.ts` — patrón guard a replicar
- `apps/api/src/auth/dto/change-password.dto.ts` — patrón DTO con `@ApiProperty` + `class-validator`

Constraint clave: el código 2FA se genera con `Math.floor(100000 + Math.random() * 900000).toString()` y **NUNCA** se guarda en claro — usar `this.hashToken(code)` (SHA-256 ya implementado, líneas 47-49).

Nuevos métodos privados/helpers en `AuthService`:
- `private sign2FaPending(user: User): string` — JWT 15min con `{ sub, email, twoFaPending: true }`
- `private async issueTwoFaCode(user: User): Promise<void>` — genera código, hashea, guarda en BD, envía email
- `verifyTwoFa(pendingPayload, code)` — público; valida y emite JWT definitivo
- `resendTwoFa(pendingPayload)` — público; busca user, vuelve a llamar `issueTwoFaCode`

## Tasks

### Task 1: Crear DTO `VerifyTwoFaDto`

**File:** `apps/api/src/auth/dto/verify-2fa.dto.ts`

Crear DTO con un solo campo `code: string` validado con `@IsString()`, `@Length(6, 6)`, `@Matches(/^\d{6}$/)`. Incluir `@ApiProperty({ example: '123456', minLength: 6, maxLength: 6 })`. Seguir el patrón de `change-password.dto.ts`.

No se necesita DTO para resend: el endpoint solo consume el `pendingToken` desde header (Authorization Bearer) vía el guard.

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "verify-2fa\.dto" || echo "OK"</verify>

### Task 2: Actualizar `JwtAuthGuard` — rechazar tokens pending

**File:** `apps/api/src/auth/jwt-auth.guard.ts`

El `JwtAuthGuard` actual no inspeciona claims especiales, lo que permite que un `pendingToken` (2FA) o `onboardingToken` acceda a cualquier endpoint protegido. Agregar rechazo explícito de estos tokens.

Modificar `canActivate()` — después del bloque `try/catch` de `jwt.verify()` y antes de la consulta a `prisma`, agregar:

```ts
// Rechazar tokens de estados intermedios — solo JWTs definitivos acceden a endpoints normales
if ((payload as Record<string, unknown>).twoFaPending || (payload as Record<string, unknown>).onboardingPending) {
  throw new UnauthorizedException('Token pendiente no válido para esta operación');
}
```

El type declaration de `payload` cambia de `{ sub: number }` a `{ sub: number; twoFaPending?: boolean; onboardingPending?: boolean }` para evitar el cast.

Código completo del método `canActivate()` después de la modificación:

```ts
async canActivate(ctx: ExecutionContext): Promise<boolean> {
  const req = ctx.switchToHttp().getRequest<Request>();
  const header = req.headers.authorization;
  if (!header?.startsWith('Bearer ')) {
    throw new UnauthorizedException();
  }
  let payload: { sub: number; twoFaPending?: boolean; onboardingPending?: boolean };
  try {
    payload = this.jwt.verify(header.slice(7));
  } catch {
    throw new UnauthorizedException();
  }
  // Rechazar tokens de estados intermedios — solo JWTs definitivos acceden a endpoints normales
  if (payload.twoFaPending || payload.onboardingPending) {
    throw new UnauthorizedException('Token pendiente no válido para esta operación');
  }
  const user = await this.prisma.user.findUnique({
    where: { id: payload.sub },
    select: { id: true, blocked: true },
  });
  if (!user || user.blocked) throw new UnauthorizedException();
  (req as Request & { user: unknown }).user = payload;
  return true;
}
```

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "jwt-auth\.guard" || echo "OK"</verify>

### Task 3: Crear `TwoFaGuard`

**File:** `apps/api/src/auth/two-fa.guard.ts`

Crear `@Injectable() class TwoFaGuard implements CanActivate`. Estructura idéntica a `JwtAuthGuard` (mismo `apps/api/src/auth/jwt-auth.guard.ts`) pero:
1. Verifica firma del JWT con `this.jwt.verify(token)`.
2. Verifica que el payload tenga `twoFaPending === true` — si no, lanza `UnauthorizedException('Token no es de 2FA pendiente')`.
3. NO consulta `prisma.user` (el usuario puede no estar confirmado todavía y no queremos overhead extra; ya se valida `blocked` en `verifyTwoFa`).
4. Inyecta el payload completo en `req.user` igual que `JwtAuthGuard`.

Type del payload: `{ sub: number; email: string; twoFaPending: true; iat: number; exp: number }`.

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "two-fa\.guard" || echo "OK"</verify>

### Task 3: Agregar template y método de mail para código 2FA

**Files:** `apps/api/utils/templates/mail.templates.ts`, `apps/api/services/mailgun/mail.service.ts`

En `mail.templates.ts`, agregar al final del archivo (después de `transferRejectedTemplate`):

```ts
export function twoFactorCodeTemplate(code: string): { subject: string; html: string } {
  return {
    subject: 'Tu código de verificación — Konbini',
    html: renderTemplate({
      title: 'Código de verificación',
      greeting: 'Tu código de verificación',
      body: `Usa el siguiente código para completar el inicio de sesión en Konbini:<br/><br/><div style="font-size:32px;font-weight:bold;letter-spacing:8px;text-align:center;background:#f4f4f4;padding:20px;border-radius:8px;color:#1a1a2e">${esc(code)}</div><br/>Este código es válido por <strong>10 minutos</strong>. Si no lo solicitaste, ignora este correo.`,
    }),
  };
}
```

En `mail.service.ts`:
1. Agregar `twoFactorCodeTemplate` al import desde `'../../utils/templates/mail.templates'`.
2. Agregar método:
```ts
async sendTwoFactorCode(to: string, code: string): Promise<void> {
  const { subject, html } = twoFactorCodeTemplate(code);
  await this.mailgun.send({ to, subject, html });
}
```

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(mail\.templates|mail\.service)" || echo "OK"</verify>

### Task 4: Modificar `AuthService` — agregar 2FA helpers y modificar login/register

**File:** `apps/api/src/auth/auth.service.ts`

**4.1 — Agregar helpers privados (después de `sign()` línea 33):**

```ts
/** Firma un JWT pendiente de 2FA (15 min) — solo válido para POST /auth/2fa/{verify,resend}. */
private sign2FaPending(user: User): string {
  return this.jwt.sign(
    { sub: user.id, email: user.email, twoFaPending: true },
    { expiresIn: '15m' },
  );
}

/** Genera un código de 6 dígitos, lo guarda hasheado (SHA-256, 10 min) y lo envía por email. */
private async issueTwoFaCode(user: User): Promise<void> {
  const code = Math.floor(100000 + Math.random() * 900000).toString();
  await this.prisma.user.update({
    where: { id: user.id },
    data: {
      twoFactorCode: this.hashToken(code),
      twoFactorExpiry: new Date(Date.now() + 10 * 60 * 1000),
    },
  });
  await this.mail.sendTwoFactorCode(user.email, code);
  if (process.env.NODE_ENV === 'development' && !this.config.get('MAILGUN_API_KEY')) {
    this.logger.debug(`2FA code (dev only) for ${user.email}: ${code}`);
  }
}
```

**4.2 — Modificar `register()` (línea 72-90):**

Reemplazar el `return { token: this.sign(user), user: this.sanitize(user) };` final por:

```ts
await this.issueTwoFaCode(user);
return { pendingToken: this.sign2FaPending(user), twoFaRequired: true as const };
```

Mantener todo el resto del método igual (creación de profile, sendWelcome). El `sendWelcome` debe seguir disparándose porque la cuenta YA existe — solo el JWT definitivo se aplaza.

**4.3 — Modificar `login()` (línea 92-99):**

Reemplazar el `return { token: this.sign(user), user: this.sanitize(user) };` final por:

```ts
await this.issueTwoFaCode(user);
return { pendingToken: this.sign2FaPending(user), twoFaRequired: true as const };
```

**4.4 — Agregar métodos públicos `verifyTwoFa` y `resendTwoFa` (al final de la clase, antes de la llave de cierre):**

```ts
/** Valida el código 2FA y emite el JWT definitivo. El payload viene del TwoFaGuard. */
async verifyTwoFa(userId: number, code: string) {
  const user = await this.prisma.user.findUnique({ where: { id: userId } });
  if (!user) throw new UnauthorizedException();
  if (user.blocked) throw new UnauthorizedException('Tu cuenta está bloqueada');
  if (
    !user.twoFactorCode ||
    !user.twoFactorExpiry ||
    user.twoFactorExpiry < new Date() ||
    user.twoFactorCode !== this.hashToken(code)
  ) {
    throw new UnauthorizedException('Código inválido o expirado');
  }
  await this.prisma.user.update({
    where: { id: user.id },
    data: { twoFactorCode: null, twoFactorExpiry: null },
  });
  return { token: this.sign(user), user: this.sanitize(user) };
}

/** Reenvía un nuevo código 2FA al email del usuario pendiente. */
async resendTwoFa(userId: number) {
  const user = await this.prisma.user.findUnique({ where: { id: userId } });
  if (!user) throw new UnauthorizedException();
  if (user.blocked) throw new UnauthorizedException('Tu cuenta está bloqueada');
  await this.issueTwoFaCode(user);
  return { ok: true };
}
```

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "auth\.service" || echo "OK"</verify>

### Task 5: Agregar endpoints en `AuthController`

**File:** `apps/api/src/auth/auth.controller.ts`

1. Agregar imports:
```ts
import { VerifyTwoFaDto } from './dto/verify-2fa.dto';
import { TwoFaGuard } from './two-fa.guard';
```

2. Agregar los dos endpoints (después del endpoint `login`, antes de `google`):

```ts
@Post('2fa/verify')
@UseGuards(TwoFaGuard)
@ApiBearerAuth()
@ApiOperation({ summary: 'Verificar código 2FA y obtener JWT definitivo' })
verifyTwoFa(@Body() dto: VerifyTwoFaDto, @CurrentUser() user: JwtUser) {
  return this.auth.verifyTwoFa(user.sub, dto.code);
}

@Post('2fa/resend')
@UseGuards(TwoFaGuard)
@ApiBearerAuth()
@ApiOperation({ summary: 'Reenviar código 2FA al email' })
resendTwoFa(@CurrentUser() user: JwtUser) {
  return this.auth.resendTwoFa(user.sub);
}
```

Nota: `JwtUser` type ya está importado vía `current-user.decorator` y es compatible — el `TwoFaGuard` inyecta un payload con `sub`, `email` y `twoFaPending`. El `role` no se inyecta pero `JwtUser` lo tiene como `string`; si TypeScript reclama, agregar un type `TwoFaUser` en el guard y exportarlo. Verificar primero compilación con el cast actual; si falla, exportar `TwoFaUser = { sub: number; email: string; twoFaPending: true }` desde `two-fa.guard.ts` y usar `@CurrentUser() user: TwoFaUser` en los dos handlers.

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "auth\.controller" || echo "OK"</verify>

### Task 6: Verificación final del plan

**File:** N/A

Compilación completa sin errores. Confirmar que los nuevos endpoints aparecen en Swagger (no requiere ejecutar el server, solo `tsc --noEmit` debe pasar limpio).

<verify>cd apps/api && pnpm tsc --noEmit</verify>

## Verification Gate

```bash
cd /home/gab/Code/konbini-project/apps/api && pnpm tsc --noEmit
```

Debe terminar sin errores. Cambios funcionales (sin tests e2e):
- `login()` y `register()` retornan `{ pendingToken, twoFaRequired: true }`
- `POST /auth/2fa/verify` con `Authorization: Bearer <pendingToken>` y body `{ code }` retorna `{ token, user }` definitivo
- `POST /auth/2fa/resend` con `Authorization: Bearer <pendingToken>` retorna `{ ok: true }` y reenvía email
- Códigos guardados como SHA-256, expiran a los 10 min
- pendingToken expira a los 15 min y solo es aceptado por `TwoFaGuard`
