---
plan: "10-03"
phase: 10
wave: 3
title: "Change email"
goal: "Usuario autenticado puede solicitar cambio de email; recibe token de confirmación 24h en el nuevo email; al confirmar se actualiza el email del User"
status: pending
autonomous: true
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/dto/change-email-request.dto.ts
  - apps/api/src/auth/dto/change-email-confirm.dto.ts
  - apps/api/services/mailgun/mail.service.ts
  - apps/api/utils/templates/mail.templates.ts
---

## Goal

Implementar flujo de cambio de email (AUTH-03):
- `POST /auth/change-email/request` (autenticado con JWT definitivo) — recibe `{ newEmail }`, valida que no esté en uso, genera token de 32 bytes hasheado SHA-256 con expiración 24h, guarda `pendingEmail` + `emailChangeToken` + `emailChangeTokenExpiry`, envía email de confirmación al **nuevo** email
- `POST /auth/change-email/confirm` (público, sin JWT) — recibe `{ token }`, busca usuario por hash, valida expiración, mueve `pendingEmail` → `email` y limpia campos

Requiere migración Prisma para agregar 3 campos al modelo `User`.

## Context

**Dependencia:** Plan 10-01 establece el patrón de tokens hasheados (SHA-256) y email transaccional. Plan 10-02 no es bloqueante para este plan, pero ambos planes anteriores deben estar mergeados para evitar conflictos en `auth.service.ts` y `auth.controller.ts`.

Schema `User` (líneas 369-423 de `apps/api/prisma/schema.prisma`) **NO contiene** los campos `pendingEmail`, `emailChangeToken`, ni `emailChangeTokenExpiry` — verificado. Por tanto, se requiere migración nueva.

Patrón ya implementado a replicar:
- `resetToken: String?` + `resetTokenExpiry: DateTime?` en User (líneas 393-394)
- `forgotPassword()` y `resetPassword()` en `auth.service.ts` (líneas 167-217) — usar como template

`MailService.sendPasswordReset(to, resetUrl)` es el template a copiar para `sendEmailChangeConfirmation(to, confirmUrl)`.

Constraints clave:
- El email de confirmación va al **NUEVO** email (no al actual) — para verificar que el usuario tiene acceso a esa bandeja
- El token expira en 24h (más generoso que reset password, que es 1h)
- `confirm` NO requiere JWT — solo el token (caso de uso: el usuario podría confirmar desde otro dispositivo donde no esté loggeado)
- Si el nuevo email ya está en uso por otro user → `ConflictException` en request
- Si el nuevo email es igual al actual → `BadRequestException`
- URL frontend: usar `FRONTEND_URL` env (igual que reset) — ruta: `/cambiar-email?token=...`

## Tasks

### Task 1: Migración Prisma — agregar campos a `User`

**File:** `apps/api/prisma/schema.prisma`

Localizar el modelo `User` (línea 369). Después del bloque de `resetToken`/`resetTokenExpiry` (líneas 392-394), agregar:

```prisma
  // v2: cambio de email — pendingEmail es el email nuevo a confirmar, token SHA-256, expira 24h
  pendingEmail            String?
  emailChangeToken        String?
  emailChangeTokenExpiry  DateTime?
```

Luego ejecutar la migración:

```bash
cd /home/gab/Code/konbini-project/apps/api && pnpm prisma migrate dev --name sch07_email_change
```

Esto debe generar el archivo `prisma/migrations/{timestamp}_sch07_email_change/migration.sql` y regenerar el Prisma Client. Si MySQL responde sin errores, los tres campos quedan disponibles tipados en `User`.

**Importante:** NO agregar `@unique` en `pendingEmail` — un email puede estar en `pendingEmail` de un usuario y simultáneamente ser el `email` actual de otro hasta que se confirme. La validación de "ya en uso" se hace en service layer.

<verify>cd /home/gab/Code/konbini-project/apps/api && pnpm prisma migrate status && pnpm tsc --noEmit 2>&1 | grep -E "(pendingEmail|emailChangeToken)" || echo "OK"</verify>

### Task 2: Crear DTOs `ChangeEmailRequestDto` y `ChangeEmailConfirmDto`

**Files:**
- `apps/api/src/auth/dto/change-email-request.dto.ts`
- `apps/api/src/auth/dto/change-email-confirm.dto.ts`

`change-email-request.dto.ts`:
```ts
import { ApiProperty } from '@nestjs/swagger';
import { IsEmail } from 'class-validator';

export class ChangeEmailRequestDto {
  @ApiProperty({ example: 'nuevo@correo.com', description: 'Nuevo email a confirmar' })
  @IsEmail()
  newEmail: string;
}
```

`change-email-confirm.dto.ts`:
```ts
import { ApiProperty } from '@nestjs/swagger';
import { IsString, MinLength } from 'class-validator';

export class ChangeEmailConfirmDto {
  @ApiProperty({ example: 'a1b2c3...', description: 'Token de confirmación recibido por email' })
  @IsString()
  @MinLength(32)
  token: string;
}
```

Seguir el patrón de DTOs existentes (`change-password.dto.ts`, `forgot-password.dto.ts`).

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "change-email" || echo "OK"</verify>

### Task 3: Agregar template y método de mail para confirmación de cambio de email

**Files:** `apps/api/utils/templates/mail.templates.ts`, `apps/api/services/mailgun/mail.service.ts`

En `mail.templates.ts`, agregar al final (después de `twoFactorCodeTemplate` que creó el plan 10-01):

```ts
export function emailChangeTemplate(confirmUrl: string): { subject: string; html: string } {
  return {
    subject: 'Confirma tu nuevo email — Konbini',
    html: renderTemplate({
      title: 'Confirma tu nuevo email',
      greeting: '¿Cambias de email?',
      body: `Recibimos una solicitud para cambiar el email de tu cuenta de Konbini a esta dirección.<br/><br/>Haz clic en el botón de abajo para confirmar el cambio. Este enlace es válido por <strong>24 horas</strong>.<br/><br/>Si no solicitaste este cambio, ignora este correo — tu email actual seguirá siendo el válido.`,
      ctaLabel: 'Confirmar nuevo email',
      ctaUrl: confirmUrl,
    }),
  };
}
```

En `mail.service.ts`:
1. Agregar `emailChangeTemplate` al import.
2. Agregar método:
```ts
async sendEmailChangeConfirmation(to: string, confirmUrl: string): Promise<void> {
  const { subject, html } = emailChangeTemplate(confirmUrl);
  await this.mailgun.send({ to, subject, html });
}
```

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(mail\.templates|mail\.service)" || echo "OK"</verify>

### Task 4: Actualizar `sanitize()` + agregar `requestEmailChange()` y `confirmEmailChange()` en `AuthService`

**File:** `apps/api/src/auth/auth.service.ts`

**4.0 — Actualizar `sanitize()` (líneas 36-44) para eliminar los 3 campos nuevos de las respuestas:**

Los campos `pendingEmail`, `emailChangeToken`, `emailChangeTokenExpiry` se añaden al modelo `User` en Task 1. Sin actualizar `sanitize()`, estos datos (incluyendo el hash del token) se filtrarían en cada respuesta de `/auth/me`, `/auth/login`, etc.

Agregar los 3 campos al destructuring de `sanitize()`:

```ts
private sanitize(user: User) {
  const {
    passwordHash: _p,
    resetToken: _t,
    resetTokenExpiry: _e,
    pendingEmail: _pe,
    emailChangeToken: _ect,
    emailChangeTokenExpiry: _ecte,
    ...safe
  } = user;
  return safe;
}
```

**4.1 — Agregar métodos públicos al final de la clase (después de `resetPassword`):**

```ts
/**
 * Paso 1 del cambio de email: genera token (24h) y lo envía al NUEVO email.
 * El email actual no recibe nada hasta que se confirme.
 */
async requestEmailChange(userId: number, newEmail: string) {
  const user = await this.prisma.user.findUnique({ where: { id: userId } });
  if (!user) throw new UnauthorizedException();
  if (user.email === newEmail) {
    throw new BadRequestException('El nuevo email es igual al actual');
  }
  const taken = await this.prisma.user.findUnique({ where: { email: newEmail } });
  if (taken) {
    throw new ConflictException('Ese email ya está en uso');
  }
  const token = randomBytes(32).toString('hex');
  await this.prisma.user.update({
    where: { id: user.id },
    data: {
      pendingEmail: newEmail,
      emailChangeToken: this.hashToken(token),
      emailChangeTokenExpiry: new Date(Date.now() + 24 * 60 * 60 * 1000),
    },
  });
  const frontendUrl = this.config.get<string>('FRONTEND_URL', 'http://localhost:3000');
  const confirmUrl = `${frontendUrl}/cambiar-email?token=${token}`;
  await this.mail.sendEmailChangeConfirmation(newEmail, confirmUrl);
  if (process.env.NODE_ENV === 'development' && !this.config.get('MAILGUN_API_KEY')) {
    this.logger.debug(`Email change URL (dev only): ${confirmUrl}`);
  }
  return { ok: true };
}

/** Paso 2: valida el token, mueve pendingEmail → email y limpia los campos. */
async confirmEmailChange(token: string) {
  const user = await this.prisma.user.findFirst({
    where: { emailChangeToken: this.hashToken(token) },
  });
  if (
    !user ||
    !user.emailChangeTokenExpiry ||
    user.emailChangeTokenExpiry < new Date() ||
    !user.pendingEmail
  ) {
    throw new BadRequestException('El enlace de cambio de email es inválido o expiró');
  }
  // Race condition: el pendingEmail puede haberse tomado entre request y confirm.
  const taken = await this.prisma.user.findUnique({ where: { email: user.pendingEmail } });
  if (taken && taken.id !== user.id) {
    await this.prisma.user.update({
      where: { id: user.id },
      data: { pendingEmail: null, emailChangeToken: null, emailChangeTokenExpiry: null },
    });
    throw new ConflictException('Ese email ya está en uso');
  }
  await this.prisma.user.update({
    where: { id: user.id },
    data: {
      email: user.pendingEmail,
      pendingEmail: null,
      emailChangeToken: null,
      emailChangeTokenExpiry: null,
    },
  });
  return { ok: true };
}
```

Notas:
- `ConflictException` ya está importado al inicio del archivo (línea 3).
- `randomBytes` y `createHash` (via `hashToken`) ya están en uso (líneas 11, 47-49).
- El `findFirst` por `emailChangeToken` no usa índice — pero igual que `resetToken`, es aceptable por volumen bajo. Si Phase futura requiere índice, agregar en otra migración.

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "auth\.service" || echo "OK"</verify>

### Task 5: Agregar endpoints en `AuthController`

**File:** `apps/api/src/auth/auth.controller.ts`

1. Agregar imports:
```ts
import { ChangeEmailRequestDto } from './dto/change-email-request.dto';
import { ChangeEmailConfirmDto } from './dto/change-email-confirm.dto';
```

2. Agregar endpoints (después de `@Patch('password')`, al final del controller):

```ts
@Post('change-email/request')
@UseGuards(JwtAuthGuard)
@ApiBearerAuth()
@ApiOperation({ summary: 'Solicitar cambio de email — envía link de confirmación al nuevo email' })
requestEmailChange(@Body() dto: ChangeEmailRequestDto, @CurrentUser() user: JwtUser) {
  return this.auth.requestEmailChange(user.sub, dto.newEmail);
}

@Post('change-email/confirm')
@ApiOperation({ summary: 'Confirmar cambio de email con el token recibido' })
confirmEmailChange(@Body() dto: ChangeEmailConfirmDto) {
  return this.auth.confirmEmailChange(dto.token);
}
```

Nota: `confirm` NO lleva `@UseGuards(JwtAuthGuard)` — el token actúa como única prueba de propiedad del nuevo email.

<verify>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "auth\.controller" || echo "OK"</verify>

### Task 6: Verificación final del plan

**File:** N/A

Compilación limpia + Prisma Client actualizado.

<verify>cd apps/api && pnpm tsc --noEmit && pnpm prisma generate</verify>

## Verification Gate

```bash
cd /home/gab/Code/konbini-project/apps/api && pnpm tsc --noEmit && pnpm prisma migrate status
```

Debe terminar sin errores y la migración `sch07_email_change` debe estar aplicada. Cambios funcionales:
- `User` ahora tiene `pendingEmail`, `emailChangeToken`, `emailChangeTokenExpiry`
- `POST /auth/change-email/request` con `Authorization: Bearer <JWT>` + body `{ newEmail }`:
  - 200 + `{ ok: true }` si éxito (envía email al nuevo)
  - 400 si `newEmail === email actual`
  - 409 si email en uso
- `POST /auth/change-email/confirm` con body `{ token }`:
  - 200 + `{ ok: true }` actualiza `email` y limpia campos
  - 400 si token inválido o expirado
  - 409 si el email pendiente fue tomado por otro user mientras tanto
- Token hasheado SHA-256, expira en 24h
- Email de confirmación va al nuevo email (no al actual)
