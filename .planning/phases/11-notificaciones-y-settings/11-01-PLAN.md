---
phase: 11-notificaciones-y-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/notifications/notifications.module.ts
  - apps/api/src/notifications/notifications.controller.ts
  - apps/api/src/notifications/notifications.service.ts
  - apps/api/src/notifications/dto/query-notifications.dto.ts
  - apps/api/src/notifications/dto/create-notification.dto.ts
  - apps/api/src/app.module.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CFG-01
must_haves:
  truths:
    - "Un servicio interno puede crear una notificación llamando a NotificationService.create({type,title,body?,payload?,userId?,orgId?})"
    - "Un usuario autenticado puede listar sus notificaciones paginadas vía GET /notifications"
    - "Un usuario autenticado obtiene el conteo de no-leídas vía GET /notifications/unread-count"
    - "Un usuario puede marcar una notificación específica como leída vía PATCH /notifications/:id/read"
    - "Un usuario puede marcar todas sus notificaciones como leídas vía PATCH /notifications/read-all"
    - "Con header X-Org-Context, los endpoints devuelven/operan sobre notificaciones del orgId"
    - "Sin header X-Org-Context, los endpoints operan sobre notificaciones del userId del JWT"
    - "Un usuario no puede marcar como leída una notificación de otro usuario u otra org (404 o 403)"
  artifacts:
    - path: "apps/api/src/notifications/notifications.module.ts"
      provides: "NotificationsModule que exporta NotificationService"
      contains: "exports: [NotificationsService]"
    - path: "apps/api/src/notifications/notifications.service.ts"
      provides: "NotificationService.create() + listMine + unreadCount + markRead + markAllRead"
      min_lines: 80
    - path: "apps/api/src/notifications/notifications.controller.ts"
      provides: "4 endpoints con guards y Swagger"
      min_lines: 50
    - path: "apps/api/src/notifications/dto/query-notifications.dto.ts"
      provides: "Paginación page+limit con validación"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Definición de CFG-01, CFG-02, CFG-03 en sección v2"
      contains: "CFG-01"
  key_links:
    - from: "apps/api/src/notifications/notifications.controller.ts"
      to: "JwtAuthGuard"
      via: "@UseGuards en clase"
      pattern: "UseGuards.*JwtAuthGuard"
    - from: "apps/api/src/notifications/notifications.controller.ts"
      to: "OrgContextGuard / @OrgContext()"
      via: "decorator de parámetro en GET endpoints"
      pattern: "@OrgContext"
    - from: "apps/api/src/app.module.ts"
      to: "NotificationsModule"
      via: "imports en AppModule"
      pattern: "NotificationsModule"
---

<objective>
Crear el módulo `notifications` en NestJS con un service interno (`create()` fire-and-forget)
y cuatro endpoints REST para que el frontend consulte y administre las notificaciones del
usuario autenticado (o de su organización vía X-Org-Context). Este plan cierra CFG-01 y
deja `NotificationService` listo para que el plan 11-02 lo inyecte en los módulos existentes.

También documenta CFG-01..03 en `REQUIREMENTS.md` (Phase 07-01 estableció ese patrón) para
que las verificaciones de cobertura de requisitos no fallen.

Purpose: Sistema de notificaciones internas — base que el resto de v2 (suscripciones,
contenido, servicios) reutilizará. Sin sistema de notificaciones, los flujos de aprobación
de eventos/spots/heroes/transferencias quedan silenciosos para el usuario.

Output: Módulo `notifications` autocontenido (module/controller/service/DTOs), exportado
desde `NotificationsModule`, importado en `AppModule`, y definiciones CFG-01..03 en
REQUIREMENTS.md.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-notificaciones-y-settings/11-CONTEXT.md
@apps/api/src/audit/audit.module.ts
@apps/api/src/audit/audit.service.ts
@apps/api/src/audit/audit.controller.ts
@apps/api/src/app.module.ts
@apps/api/prisma/schema.prisma

<interfaces>
<!-- Contratos clave que el executor necesita. Extraídos del codebase. -->

Modelo Prisma (apps/api/prisma/schema.prisma:649-676):
```prisma
model Settings {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}

model Notification {
  id        Int              @id @default(autoincrement())
  type      NotificationType
  title     String
  body      String?          @db.Text
  payload   Json             @default("{}")
  read      Boolean          @default(false)
  user      User?            @relation("UserNotifications", fields: [userId], references: [id], onDelete: Cascade)
  userId    Int?
  org       User?            @relation("OrgNotifications",  fields: [orgId],  references: [id], onDelete: Cascade)
  orgId     Int?
  createdAt DateTime         @default(now())
  @@index([userId, read])
  @@index([orgId, read])
  @@index([createdAt])
}

enum NotificationType {
  EVENT_APPROVED EVENT_REJECTED EVENT_BANNED
  SPOT_APPROVED  SPOT_REJECTED  SPOT_BANNED
  HERO_APPROVED  HERO_REJECTED  HERO_BANNED
  ARTICLE_APPROVED ARTICLE_REJECTED ARTICLE_BANNED
  ORG_INVITATION
  TRANSFER_REQUEST TRANSFER_ACCEPTED TRANSFER_REJECTED
  SUBSCRIPTION_ACTIVATED SUBSCRIPTION_CANCELLED
  SYSTEM
}
```

JwtUser (apps/api/src/auth/current-user.decorator.ts):
```typescript
export type JwtUser = { sub: number; email: string; role: 'AUTHENTICATED'|'ADMIN'|'SUPER_ADMIN' };
```

OrgContextDto (apps/api/src/common/org-context/org-context.types.ts):
```typescript
export type OrgContextDto = { orgId: number; role: OrgRole };
// Decorator: @OrgContext() orgContext: OrgContextDto | null
```

Patrón AuditModule (referencia) — apps/api/src/audit/audit.module.ts:
```typescript
@Module({
  imports: [AuthModule],          // provee JwtAuthGuard / RolesGuard
  controllers: [AuditController],
  providers: [AuditService],
  exports: [AuditService],        // EXACTAMENTE este formato — los otros módulos lo importan
})
export class AuditModule {}
```

Patrón fire-and-forget (referencia) — audit.service.ts:29-50:
```typescript
log(params: AuditLogParams): void {
  this.prisma.auditLog.create({ data: {...} })
    .catch((err) => this.logger.error('insert failed', err));
}
// SIN await. SIN try/catch en caller. void no Promise<void>.
```

OrgContextGuard global — apps/api/src/common/org-context/org-context.module.ts está marcado
@Global(), así que NO se importa en NotificationsModule pero SÍ se puede usar
@UseGuards(OrgContextGuard) y @OrgContext() en el controller.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear NotificationsModule + Service + DTOs (CFG-01 core)</name>
  <read_first>
    - apps/api/src/audit/audit.module.ts (patrón exports)
    - apps/api/src/audit/audit.service.ts (patrón fire-and-forget void)
    - apps/api/prisma/schema.prisma líneas 602-676 (NotificationType + Notification + Settings)
    - apps/api/src/auth/current-user.decorator.ts (JwtUser shape)
    - apps/api/src/common/org-context/org-context.types.ts (OrgContextDto shape)
  </read_first>
  <files>
    apps/api/src/notifications/notifications.module.ts,
    apps/api/src/notifications/notifications.service.ts,
    apps/api/src/notifications/dto/query-notifications.dto.ts,
    apps/api/src/notifications/dto/create-notification.dto.ts
  </files>
  <action>
    Crear el módulo `notifications` siguiendo el patrón exacto de `audit`:

    **apps/api/src/notifications/notifications.module.ts**:
    ```typescript
    import { Module } from '@nestjs/common';
    import { AuthModule } from '../auth/auth.module';
    import { NotificationsController } from './notifications.controller';
    import { NotificationsService } from './notifications.service';

    @Module({
      imports: [AuthModule], // provee JwtAuthGuard
      controllers: [NotificationsController],
      providers: [NotificationsService],
      exports: [NotificationsService], // 11-02 lo importa desde events/spots/heroes/orgs/transfers
    })
    export class NotificationsModule {}
    ```
    (NOTA: el controller se crea en Task 2 — este archivo lo referencia ya).

    **apps/api/src/notifications/dto/create-notification.dto.ts**:
    Tipo interno (no DTO HTTP) — exportar `CreateNotificationParams`:
    ```typescript
    import type { NotificationType, Prisma } from '@prisma/client';
    export interface CreateNotificationParams {
      type: NotificationType;
      title: string;
      body?: string;
      payload?: Prisma.InputJsonValue; // cast obligatorio (lección Phase 07)
      userId?: number;                  // exactamente uno de userId/orgId
      orgId?: number;
    }
    ```

    **apps/api/src/notifications/dto/query-notifications.dto.ts**:
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    import { Type } from 'class-transformer';
    import { IsInt, IsOptional, Max, Min } from 'class-validator';

    export class QueryNotificationsDto {
      @ApiPropertyOptional({ default: 1, minimum: 1 })
      @IsOptional() @Type(() => Number) @IsInt() @Min(1)
      page?: number = 1;

      @ApiPropertyOptional({ default: 20, minimum: 1, maximum: 50 })
      @IsOptional() @Type(() => Number) @IsInt() @Min(1) @Max(50)
      limit?: number = 20;
    }
    ```

    **apps/api/src/notifications/notifications.service.ts** — implementar:
    ```typescript
    import { ForbiddenException, Injectable, Logger, NotFoundException } from '@nestjs/common';
    import { Prisma } from '@prisma/client';
    import { PrismaService } from '../../utils/prisma/prisma.service';
    import type { JwtUser } from '../auth/current-user.decorator';
    import type { OrgContextDto } from '../common/org-context/org-context.types';
    import type { CreateNotificationParams } from './dto/create-notification.dto';
    import { QueryNotificationsDto } from './dto/query-notifications.dto';

    @Injectable()
    export class NotificationsService {
      private readonly logger = new Logger(NotificationsService.name);
      constructor(private readonly prisma: PrismaService) {}

      /**
       * Crea una notificación — FIRE-AND-FORGET. Nunca lanza ni propaga.
       * Patrón idéntico a AuditService.log(): void, no Promise<void>.
       * Exactamente uno de userId/orgId debe estar presente; si no, se loguea warning y se omite.
       */
      create(params: CreateNotificationParams): void {
        if ((!params.userId && !params.orgId) || (params.userId && params.orgId)) {
          this.logger.warn(`Notification skipped: exactly one of userId/orgId required (type=${params.type})`);
          return;
        }
        this.prisma.notification.create({
          data: {
            type: params.type,
            title: params.title,
            body: params.body ?? null,
            payload: (params.payload ?? {}) as Prisma.InputJsonValue,
            userId: params.userId ?? null,
            orgId: params.orgId ?? null,
          },
        }).catch((err: unknown) =>
          this.logger.error('Notification insert failed', err instanceof Error ? err.stack : String(err)),
        );
      }

      /**
       * Lista paginada de notificaciones del recipient (userId u orgId según orgContext).
       * Orden: más recientes primero.
       */
      async listMine(query: QueryNotificationsDto, user: JwtUser, orgContext: OrgContextDto | null) {
        const page = query.page ?? 1;
        const limit = Math.min(query.limit ?? 20, 50);
        const where: Prisma.NotificationWhereInput = orgContext
          ? { orgId: orgContext.orgId }
          : { userId: user.sub };

        const [items, total] = await this.prisma.$transaction([
          this.prisma.notification.findMany({
            where,
            orderBy: { createdAt: 'desc' },
            skip: (page - 1) * limit,
            take: limit,
          }),
          this.prisma.notification.count({ where }),
        ]);
        return { items, total, page, limit, totalPages: Math.ceil(total / limit) };
      }

      async unreadCount(user: JwtUser, orgContext: OrgContextDto | null): Promise<{ count: number }> {
        const where: Prisma.NotificationWhereInput = orgContext
          ? { orgId: orgContext.orgId, read: false }
          : { userId: user.sub, read: false };
        const count = await this.prisma.notification.count({ where });
        return { count };
      }

      /**
       * Marca una notificación como leída. Solo el destinatario puede hacerlo.
       * Si la notificación es de orgContext distinto al del caller, lanza 404.
       */
      async markRead(id: number, user: JwtUser, orgContext: OrgContextDto | null) {
        const n = await this.prisma.notification.findUnique({ where: { id } });
        if (!n) throw new NotFoundException('Notificación no encontrada');

        const isMine = orgContext
          ? n.orgId === orgContext.orgId
          : n.userId === user.sub;
        if (!isMine) throw new NotFoundException('Notificación no encontrada');

        return this.prisma.notification.update({
          where: { id },
          data: { read: true },
        });
      }

      async markAllRead(user: JwtUser, orgContext: OrgContextDto | null) {
        const where: Prisma.NotificationWhereInput = orgContext
          ? { orgId: orgContext.orgId, read: false }
          : { userId: user.sub, read: false };
        const { count } = await this.prisma.notification.updateMany({
          where,
          data: { read: true },
        });
        return { updated: count };
      }
    }
    ```

    Notas:
    - Usar `read: false` filter en `markAllRead` para no tocar registros ya leídos (mejor performance).
    - `markRead` devuelve 404 (no 403) cuando la notificación pertenece a otro usuario — evita
      revelar la existencia de IDs ajenos.
    - PrismaService path: `../../utils/prisma/prisma.service` (verificado: existe `apps/api/utils/prisma/`).
    - `Prisma.InputJsonValue` cast obligatorio para `payload: Json` (lección Phase 07).
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/notif-task1.log; grep -q "error TS" /tmp/notif-task1.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/src/notifications/notifications.module.ts` exit 0
    - `test -f apps/api/src/notifications/notifications.service.ts` exit 0
    - `test -f apps/api/src/notifications/dto/query-notifications.dto.ts` exit 0
    - `test -f apps/api/src/notifications/dto/create-notification.dto.ts` exit 0
    - `grep -q "exports: \[NotificationsService\]" apps/api/src/notifications/notifications.module.ts` exit 0
    - `grep -q "create(params: CreateNotificationParams): void" apps/api/src/notifications/notifications.service.ts` exit 0
    - `grep -q "Prisma.InputJsonValue" apps/api/src/notifications/notifications.service.ts` exit 0
    - `grep -q "exactly one of userId/orgId" apps/api/src/notifications/notifications.service.ts` exit 0
    - `grep -qE "Max\(50\)" apps/api/src/notifications/dto/query-notifications.dto.ts` exit 0
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    NotificationsModule existe con exports correctos. NotificationsService implementa
    create() (void fire-and-forget) + listMine + unreadCount + markRead + markAllRead.
    DTOs validan paginación. `pnpm tsc --noEmit` pasa sin errores.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear NotificationsController + 4 endpoints con Swagger</name>
  <read_first>
    - apps/api/src/audit/audit.controller.ts (patrón @ApiTags + @ApiBearerAuth + @ApiOperation)
    - apps/api/src/notifications/notifications.service.ts (creado en Task 1)
    - apps/api/src/auth/current-user.decorator.ts (decorator @CurrentUser())
    - apps/api/src/common/org-context/org-context.decorator.ts (decorator @OrgContext())
    - apps/api/src/auth/jwt-auth.guard.ts
  </read_first>
  <files>apps/api/src/notifications/notifications.controller.ts</files>
  <action>
    Crear `apps/api/src/notifications/notifications.controller.ts`:

    ```typescript
    import {
      Controller, Get, Param, ParseIntPipe, Patch, Query, UseGuards,
    } from '@nestjs/common';
    import { ApiBearerAuth, ApiOperation, ApiResponse, ApiTags } from '@nestjs/swagger';
    import { CurrentUser, type JwtUser } from '../auth/current-user.decorator';
    import { JwtAuthGuard } from '../auth/jwt-auth.guard';
    import { OrgContext } from '../common/org-context/org-context.decorator';
    import type { OrgContextDto } from '../common/org-context/org-context.types';
    import { QueryNotificationsDto } from './dto/query-notifications.dto';
    import { NotificationsService } from './notifications.service';

    @ApiTags('notifications')
    @ApiBearerAuth()
    @UseGuards(JwtAuthGuard)
    @Controller('notifications')
    export class NotificationsController {
      constructor(private readonly notifications: NotificationsService) {}

      @Get()
      @ApiOperation({ summary: 'Listar notificaciones del usuario o de la org (X-Org-Context)' })
      @ApiResponse({ status: 200, description: 'Listado paginado' })
      list(
        @Query() query: QueryNotificationsDto,
        @CurrentUser() user: JwtUser,
        @OrgContext() orgContext: OrgContextDto | null,
      ) {
        return this.notifications.listMine(query, user, orgContext);
      }

      @Get('unread-count')
      @ApiOperation({ summary: 'Contar notificaciones no leídas del usuario o de la org' })
      @ApiResponse({ status: 200, description: '{ count: number }' })
      unreadCount(
        @CurrentUser() user: JwtUser,
        @OrgContext() orgContext: OrgContextDto | null,
      ) {
        return this.notifications.unreadCount(user, orgContext);
      }

      @Patch('read-all')
      @ApiOperation({ summary: 'Marcar todas las notificaciones del recipient como leídas' })
      @ApiResponse({ status: 200, description: '{ updated: number }' })
      markAllRead(
        @CurrentUser() user: JwtUser,
        @OrgContext() orgContext: OrgContextDto | null,
      ) {
        return this.notifications.markAllRead(user, orgContext);
      }

      @Patch(':id/read')
      @ApiOperation({ summary: 'Marcar una notificación específica como leída' })
      @ApiResponse({ status: 200, description: 'Notificación actualizada' })
      @ApiResponse({ status: 404, description: 'No encontrada o no pertenece al caller' })
      markRead(
        @Param('id', ParseIntPipe) id: number,
        @CurrentUser() user: JwtUser,
        @OrgContext() orgContext: OrgContextDto | null,
      ) {
        return this.notifications.markRead(id, user, orgContext);
      }
    }
    ```

    **CRÍTICO sobre orden de rutas**: `@Patch('read-all')` DEBE declararse ANTES de
    `@Patch(':id/read')` para que NestJS no intente parsear "read-all" como `:id` (ParseIntPipe
    fallaría con 400). El orden mostrado arriba es correcto.

    Path del decorator OrgContext: verificar antes — debería ser
    `../common/org-context/org-context.decorator` (mismo directorio que el guard y types).
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/notif-task2.log; grep -q "error TS" /tmp/notif-task2.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@ApiTags('notifications')" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@Controller('notifications')" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@Get('unread-count')" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@Patch('read-all')" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@Patch(':id/read')" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "@OrgContext()" apps/api/src/notifications/notifications.controller.ts` exit 0
    - `grep -q "JwtAuthGuard" apps/api/src/notifications/notifications.controller.ts` exit 0
    - Orden de rutas: línea de `'read-all'` debe estar antes de `':id/read'`:
      `awk '/@Patch\(/{print NR}' apps/api/src/notifications/notifications.controller.ts` →
      primer número < segundo número (verificable manualmente o con diff de greps)
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Controller expone los 4 endpoints documentados con Swagger, protegidos por JwtAuthGuard,
    soportando X-Org-Context vía @OrgContext() decorator. Orden de rutas evita conflicto
    ParseIntPipe con "read-all". `pnpm tsc --noEmit` pasa.
  </done>
</task>

<task type="auto">
  <name>Task 3: Registrar NotificationsModule en AppModule + documentar CFG-01..03 en REQUIREMENTS.md</name>
  <read_first>
    - apps/api/src/app.module.ts (imports list actual)
    - .planning/REQUIREMENTS.md (sección "v2 Requirements" final)
    - .planning/phases/11-notificaciones-y-settings/11-CONTEXT.md (alcance de cada requirement)
  </read_first>
  <files>apps/api/src/app.module.ts, .planning/REQUIREMENTS.md</files>
  <action>
    **Parte A — registrar NotificationsModule en AppModule:**

    1. En `apps/api/src/app.module.ts`, agregar el import al inicio (con los demás módulos
       de feature, alfabético o agrupado, igual que el resto):
       ```typescript
       import { NotificationsModule } from './notifications/notifications.module';
       ```
    2. Agregarlo al array `imports: [...]` del decorator @Module. Ubicación sugerida: junto
       a `AuditModule`, `OrganizationsModule`, `TransfersModule` (módulos cross-cutting de v2).

    **Parte B — documentar CFG-01..03 en REQUIREMENTS.md:**

    Phase 07-01 estableció que cada fase agrega sus IDs a REQUIREMENTS.md como parte del
    primer plan. Append al final del archivo (después de la sección SCH-06), una nueva
    sub-sección dentro de "v2 Requirements":

    ```markdown
    ### Notificaciones y Settings (Phase 11)

    - [ ] **CFG-01**: Módulo `notifications` con `NotificationService.create()` interno
      (fire-and-forget void) y endpoints autenticados:
      `GET /notifications` (paginado, soporta X-Org-Context),
      `GET /notifications/unread-count`,
      `PATCH /notifications/:id/read`,
      `PATCH /notifications/read-all`.
      Validación: paginación `page>=1`, `limit<=50` (default 20).
    - [ ] **CFG-02**: Auto-notificaciones en módulos existentes —
      `NotificationsModule` inyectado en `EventsModule`, `SpotsModule`, `HeroesModule`,
      `OrganizationsModule`, `TransfersModule`. Los services llaman a
      `notifications.create()` en aprobar/rechazar/banear (EVENT/SPOT/HERO),
      invitar miembro (ORG_INVITATION), y crear/aceptar/rechazar transferencia
      (TRANSFER_REQUEST/ACCEPTED/REJECTED). Recipient: si el dueño es `User.type=ORGANIZATION`
      → `orgId`, si no → `userId`.
    - [ ] **CFG-03**: Módulo `settings` con CRUD admin (`GET /settings`, `PATCH /settings`),
      endpoint público `GET /settings/public` que expone solo claves `SPOT_*` y `HERO_*`,
      y métodos internos `get/getNum/set/getPublic/getAll`. `SpotsService` y `HeroesService`
      migrados de `ConfigService.get()` a `SettingsService.getNum()` para todas las claves
      `SPOT_*` y `HERO_*`. Seed en `prisma/seed.ts` ya existe con las 8 claves requeridas
      (más 4 extras toleradas) — verificar sin duplicar.
    ```

    También actualizar la sección "Traceability" agregando la fila:
    ```
    | CFG-01..03 | Phase 11 | Pending |
    ```

    (Si la tabla "Traceability" solo cubre v1, agregar una nueva tabla "Traceability v2" o
    extender la existente — usar el criterio que mejor calce con el formato actual del archivo.)
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/notif-task3.log; grep -q "error TS" /tmp/notif-task3.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "NotificationsModule" apps/api/src/app.module.ts` exit 0 (al menos 2 matches: import + imports array)
    - `grep -c "NotificationsModule" apps/api/src/app.module.ts` retorna >= 2
    - `grep -q "from './notifications/notifications.module'" apps/api/src/app.module.ts` exit 0
    - `grep -q "CFG-01" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "CFG-02" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "CFG-03" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "Notificaciones y Settings (Phase 11)" .planning/REQUIREMENTS.md` exit 0
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
    - `cd apps/api &amp;&amp; pnpm build` exit 0 (NestJS arranca sin errores de DI)
  </acceptance_criteria>
  <done>
    NotificationsModule registrado en AppModule (import + array). REQUIREMENTS.md documenta
    CFG-01, CFG-02, CFG-03 con descripción suficiente para verificación de cobertura.
    El proyecto compila (`pnpm tsc --noEmit`) y construye (`pnpm build`) sin errores.
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm tsc --noEmit` pasa sin errores TS
- `cd apps/api && pnpm build` pasa (NestJS DI graph válido — NotificationsModule resuelto)
- `grep -rq "exports: \[NotificationsService\]" apps/api/src/notifications/` exit 0
- `grep -rq "NotificationsModule" apps/api/src/app.module.ts` exit 0
- `grep -E "CFG-0[1-3]" .planning/REQUIREMENTS.md` muestra 3 líneas
- Arranque manual de la API: `cd apps/api && pnpm start:dev` no falla en compile time
  (ejecutor puede verificar logs si lo considera)
</verification>

<success_criteria>
1. NotificationService.create() es invocable desde cualquier service que importe NotificationsModule
2. NotificationService.create() NO lanza errores (fire-and-forget)
3. Los 4 endpoints existen, están protegidos con JwtAuthGuard, respetan X-Org-Context
4. GET /notifications respeta page y limit (default 20, max 50)
5. PATCH /:id/read sobre notificación ajena devuelve 404
6. CFG-01..03 documentados en REQUIREMENTS.md (lectura humana valida)
7. Compilación TS sin errores
</success_criteria>

<output>
Crear `.planning/phases/11-notificaciones-y-settings/11-01-SUMMARY.md` con:
- Qué se creó (archivos, paths absolutos)
- Decisiones tomadas (formato InputJsonValue, void vs Promise, orden de rutas read-all/:id)
- Cómo lo consumirá el plan 11-02 (importar NotificationsModule, inyectar NotificationsService)
- Riesgos abiertos para 11-02 (resolución de recipient userId vs orgId basada en User.type)
</output>
