---
phase: 11-notificaciones-y-settings
plan: 03
type: execute
wave: 3
depends_on:
  - 11-notificaciones-y-settings-02
files_modified:
  - apps/api/src/settings/settings.module.ts
  - apps/api/src/settings/settings.controller.ts
  - apps/api/src/settings/settings.service.ts
  - apps/api/src/settings/dto/upsert-setting.dto.ts
  - apps/api/src/app.module.ts
  - apps/api/src/spots/spots.module.ts
  - apps/api/src/spots/spots.service.ts
  - apps/api/src/heroes/heroes.module.ts
  - apps/api/src/heroes/heroes.service.ts
  - apps/api/prisma/seed.ts
autonomous: true
requirements:
  - CFG-03
must_haves:
  truths:
    - "Existe un endpoint público GET /settings/public que devuelve {[key]: value} de SPOT_* y HERO_* sin requerir auth"
    - "GET /settings (admin) devuelve array de {key, value, updatedAt}"
    - "PATCH /settings (admin) hace upsert de una clave y devuelve el registro actualizado"
    - "SpotsService.quota() ya no lee de ConfigService — consume SettingsService.getNum()"
    - "HeroesService.quota() ya no lee de ConfigService — consume SettingsService.getNum()"
    - "SettingsService.getNum(key) parsea el string a número y lanza si la clave no existe"
    - "GET /settings/public NO expone claves SUBSCRIPTION_* ni ninguna otra fuera de SPOT_*/HERO_*"
    - "PATCH /settings sobre una clave inexistente la crea (upsert)"
    - "El seed de Settings preserva las 8 claves requeridas (más las 4 extras existentes ya seedeadas)"
  artifacts:
    - path: "apps/api/src/settings/settings.module.ts"
      provides: "SettingsModule exporta SettingsService"
      contains: "exports: [SettingsService]"
    - path: "apps/api/src/settings/settings.service.ts"
      provides: "get/getNum/set/getPublic/getAll"
      min_lines: 60
    - path: "apps/api/src/settings/settings.controller.ts"
      provides: "3 endpoints (admin GET/PATCH + público GET /public)"
    - path: "apps/api/src/spots/spots.service.ts"
      provides: "quota() consume SettingsService"
      contains: "this.settings"
    - path: "apps/api/src/heroes/heroes.service.ts"
      provides: "quota() consume SettingsService"
      contains: "this.settings"
    - path: "apps/api/prisma/seed.ts"
      provides: "8 claves SPOT/HERO/SUBSCRIPTION verificadas en el seed (upsert con update:{})"
      contains: "SPOT_PRICE_PER_DAY"
  key_links:
    - from: "apps/api/src/spots/spots.service.ts"
      to: "SettingsService"
      via: "constructor injection + await this.settings.getNum"
      pattern: "this.settings.getNum"
    - from: "apps/api/src/heroes/heroes.service.ts"
      to: "SettingsService"
      via: "constructor injection + await this.settings.getNum"
      pattern: "this.settings.getNum"
    - from: "apps/api/src/app.module.ts"
      to: "SettingsModule"
      via: "imports en AppModule"
      pattern: "SettingsModule"
    - from: "apps/api/src/spots/spots.module.ts"
      to: "SettingsModule"
      via: "imports en @Module"
      pattern: "SettingsModule"
    - from: "apps/api/src/heroes/heroes.module.ts"
      to: "SettingsModule"
      via: "imports en @Module"
      pattern: "SettingsModule"
---

<objective>
Crear el módulo `settings` con CRUD admin, endpoint público para el frontend, y migrar
SpotsService/HeroesService de leer precio/cupo desde `ConfigService` a leerlo desde la
tabla `Settings` en DB. Cierra CFG-03 — el admin podrá cambiar precios y cupos sin redeploy.

Purpose: La tabla `Settings` ya existe (Phase 8-04) pero está sin uso real. Este plan la
activa como fuente de verdad para los parámetros de avisos y portadas. Sin esto, el admin
no tiene control runtime sobre precios.

Output: Módulo `settings` autocontenido (module/controller/service/DTO), seed verificado
(no duplicar), SpotsService/HeroesService migrados a SettingsService.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-notificaciones-y-settings/11-CONTEXT.md
@.planning/phases/11-notificaciones-y-settings/11-01-SUMMARY.md
@.planning/phases/11-notificaciones-y-settings/11-02-SUMMARY.md
@apps/api/prisma/schema.prisma
@apps/api/prisma/seed.ts
@apps/api/src/spots/spots.service.ts
@apps/api/src/heroes/heroes.service.ts
@apps/api/src/app.module.ts
@apps/api/src/audit/audit.module.ts
@apps/api/src/audit/audit.controller.ts

<interfaces>
<!-- Contratos clave -->

Settings model (apps/api/prisma/schema.prisma:649-653):
```prisma
model Settings {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}
```

Seed ya existente (apps/api/prisma/seed.ts:656-683) — 12 claves upserted con
`update: {}` (no sobreescribe valores admin):
- SPOT_PRICE_PER_DAY=8000, SPOT_MIN_DAYS=10, SPOT_MAX_DAYS=30, SPOT_MAX_ACTIVE=12
- HERO_PRICE_PER_DAY=15000, HERO_MIN_DAYS=10, HERO_MAX_DAYS=30, HERO_MAX_ACTIVE=5
- SUBSCRIPTION_PRICE=9990, SUBSCRIPTION_CREDITS=10, SUBSCRIPTION_SPOT_DISCOUNT=20,
  SUBSCRIPTION_HERO_DISCOUNT=20

Nota: el phase_scope lista 8 claves específicas. El seed actual incluye esas 8 y 4 extras
(MIN_DAYS, DISCOUNT). Mantener las 4 extras — son legítimas (Phase 8-04 las dejó allí
intencionalmente). Verificar que las 8 requeridas estén; NO duplicar; NO eliminar las extras.

Hay una discrepancia entre el phase_scope (SPOT_MAX_ACTIVE=10) y el seed actual
(SPOT_MAX_ACTIVE=12). **El phase_scope gana** — la decisión bloqueada en 11-CONTEXT.md
establece `SPOT_MAX_ACTIVE=10`. El seed usa `update: {}` por lo que no sobreescribe
instalaciones ya en producción; las frescas recibirán el default correcto de 10.
Actualizar el seed.ts para que el upsert tenga `value: '10'` en SPOT_MAX_ACTIVE.

SpotsService hoy (apps/api/src/spots/spots.service.ts:18-37):
```typescript
constructor(
  private readonly prisma: PrismaService,
  private readonly config: ConfigService,  // <-- migrar
  private readonly mail: MailService,
  private readonly audit: AuditService,
) {}

private maxActive(): number { return Number(this.config.get('SPOT_MAX_ACTIVE')) || 10; }
private maxDays(): number { return Number(this.config.get('SPOT_MAX_DAYS')) || 30; }
private pricePerDay(): number { return Number(this.config.get('SPOT_PRICE_PER_DAY')) || 8000; }
```

Estos métodos privados son `() => number` síncronos. Tras migrar pasarán a
`() => Promise<number>` — eso obliga a cambiar call sites (`quota()`, `assertQuotaAvailable()`,
`assertMaxDays(days)`) a `async/await`.

Patrón AuditModule para Settings (referencia):
```typescript
@Module({
  imports: [AuthModule],
  controllers: [SettingsController],
  providers: [SettingsService],
  exports: [SettingsService],  // Spots/Heroes lo importan
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SettingsModule + Service + Controller + DTO + verificar seed</name>
  <read_first>
    - apps/api/src/audit/audit.module.ts (patrón module)
    - apps/api/src/audit/audit.controller.ts (patrón Roles + ApiBearerAuth)
    - apps/api/prisma/schema.prisma:649-653 (Settings model)
    - apps/api/prisma/seed.ts:656-683 (Settings seed actual)
    - apps/api/src/auth/roles.decorator.ts
    - apps/api/src/auth/roles.guard.ts
  </read_first>
  <files>
    apps/api/src/settings/settings.module.ts,
    apps/api/src/settings/settings.service.ts,
    apps/api/src/settings/settings.controller.ts,
    apps/api/src/settings/dto/upsert-setting.dto.ts,
    apps/api/src/app.module.ts,
    apps/api/prisma/seed.ts
  </files>
  <action>
    **Parte A — SettingsModule + Service + DTO + Controller**:

    **apps/api/src/settings/dto/upsert-setting.dto.ts**:
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    import { IsString, MaxLength, MinLength } from 'class-validator';

    export class UpsertSettingDto {
      @ApiProperty({ example: 'SPOT_PRICE_PER_DAY' })
      @IsString() @MinLength(1) @MaxLength(100)
      key!: string;

      @ApiProperty({ example: '8000' })
      @IsString() @MaxLength(1000)
      value!: string;
    }
    ```

    **apps/api/src/settings/settings.service.ts**:
    ```typescript
    import { Injectable, NotFoundException } from '@nestjs/common';
    import { PrismaService } from '../../utils/prisma/prisma.service';

    /**
     * Prefijos de claves expuestas en GET /settings/public.
     * Cualquier otra clave (SUBSCRIPTION_*, futuros admin-only) NO se expone.
     */
    const PUBLIC_PREFIXES = ['SPOT_', 'HERO_'] as const;

    @Injectable()
    export class SettingsService {
      constructor(private readonly prisma: PrismaService) {}

      /**
       * Lee una clave como string. Lanza NotFoundException si no existe.
       * Si el caller debe tolerar la ausencia, usar getAll() + find o tryGet futuro.
       */
      async get(key: string): Promise<string> {
        const s = await this.prisma.settings.findUnique({ where: { key } });
        if (!s) throw new NotFoundException(`Setting "${key}" no existe`);
        return s.value;
      }

      /**
       * Lee una clave y la parsea como número entero. Lanza si no existe o no es parseable.
       */
      async getNum(key: string): Promise<number> {
        const raw = await this.get(key);
        const n = parseInt(raw, 10);
        if (Number.isNaN(n)) {
          throw new NotFoundException(`Setting "${key}" no es un número válido: "${raw}"`);
        }
        return n;
      }

      /**
       * Upsert de una clave. Admin only (validado en controller).
       */
      async set(key: string, value: string) {
        return this.prisma.settings.upsert({
          where: { key },
          create: { key, value },
          update: { value },
        });
      }

      /**
       * Lista completa — admin only.
       * Devuelve array de {key, value, updatedAt} ordenado alfabéticamente por key.
       */
      getAll() {
        return this.prisma.settings.findMany({
          orderBy: { key: 'asc' },
        });
      }

      /**
       * Mapa público {key: value} de las claves SPOT_* y HERO_*. Sin auth.
       * El frontend lo consume para mostrar precios y cupos al usuario.
       */
      async getPublic(): Promise<Record<string, string>> {
        const all = await this.prisma.settings.findMany();
        const result: Record<string, string> = {};
        for (const s of all) {
          if (PUBLIC_PREFIXES.some((p) => s.key.startsWith(p))) {
            result[s.key] = s.value;
          }
        }
        return result;
      }
    }
    ```

    **apps/api/src/settings/settings.controller.ts**:
    ```typescript
    import { Body, Controller, Get, Patch, UseGuards } from '@nestjs/common';
    import { ApiBearerAuth, ApiOperation, ApiResponse, ApiTags } from '@nestjs/swagger';
    import { JwtAuthGuard } from '../auth/jwt-auth.guard';
    import { Roles } from '../auth/roles.decorator';
    import { RolesGuard } from '../auth/roles.guard';
    import { UpsertSettingDto } from './dto/upsert-setting.dto';
    import { SettingsService } from './settings.service';

    @ApiTags('settings')
    @Controller('settings')
    export class SettingsController {
      constructor(private readonly settings: SettingsService) {}

      // CRÍTICO: 'public' antes de los endpoints admin para que sea verdaderamente sin auth.
      // No tiene @UseGuards.
      @Get('public')
      @ApiOperation({ summary: 'Settings públicas (SPOT_* y HERO_*) — sin autenticación' })
      @ApiResponse({ status: 200, description: 'Mapa {key: value}' })
      getPublic() {
        return this.settings.getPublic();
      }

      @Get()
      @UseGuards(JwtAuthGuard, RolesGuard)
      @Roles('ADMIN', 'SUPER_ADMIN')
      @ApiBearerAuth()
      @ApiOperation({ summary: 'Listar todas las settings (ADMIN+)' })
      @ApiResponse({ status: 200, description: 'Array de {key, value, updatedAt}' })
      getAll() {
        return this.settings.getAll();
      }

      @Patch()
      @UseGuards(JwtAuthGuard, RolesGuard)
      @Roles('ADMIN', 'SUPER_ADMIN')
      @ApiBearerAuth()
      @ApiOperation({ summary: 'Upsert de una setting (ADMIN+)' })
      @ApiResponse({ status: 200, description: 'Setting actualizado' })
      upsert(@Body() dto: UpsertSettingDto) {
        return this.settings.set(dto.key, dto.value);
      }
    }
    ```

    **apps/api/src/settings/settings.module.ts**:
    ```typescript
    import { Module } from '@nestjs/common';
    import { AuthModule } from '../auth/auth.module';
    import { SettingsController } from './settings.controller';
    import { SettingsService } from './settings.service';

    @Module({
      imports: [AuthModule], // provee JwtAuthGuard + RolesGuard
      controllers: [SettingsController],
      providers: [SettingsService],
      exports: [SettingsService], // Spots/Heroes lo importan
    })
    export class SettingsModule {}
    ```

    **apps/api/src/app.module.ts** — agregar:
    ```typescript
    import { SettingsModule } from './settings/settings.module';
    // ...y en imports: [...]
    SettingsModule,
    ```

    **Parte B — verificar el seed (NO reescribir)**:

    Abrir `apps/api/prisma/seed.ts` (líneas 656-683). YA existe el bloque de Settings con
    12 claves y `upsert` con `update: {}` (no sobreescribe valores admin). Verificar que
    las 8 claves requeridas del phase_scope estén presentes:
    - SPOT_PRICE_PER_DAY, SPOT_MAX_DAYS, SPOT_MAX_ACTIVE
    - HERO_PRICE_PER_DAY, HERO_MAX_DAYS, HERO_MAX_ACTIVE
    - SUBSCRIPTION_PRICE, SUBSCRIPTION_CREDITS

    Las 8 están. NO duplicar. NO eliminar las extras (SPOT_MIN_DAYS, HERO_MIN_DAYS,
    SUBSCRIPTION_SPOT_DISCOUNT, SUBSCRIPTION_HERO_DISCOUNT) — son legítimas de Phase 8-04
    y futuras fases las consumirán.

    Si alguna de las 8 requeridas faltara (no debería), agregarla con el formato
    establecido. NO cambiar valores existentes.

    Acción concreta sobre el seed: NINGUNA modificación esperada. La acceptance grepea
    presencia de cada clave; si todo está, el archivo queda intacto.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/settings-task1.log; grep -q "error TS" /tmp/settings-task1.log &amp;&amp; exit 1 || pnpm prisma:seed 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/src/settings/settings.module.ts` exit 0
    - `test -f apps/api/src/settings/settings.service.ts` exit 0
    - `test -f apps/api/src/settings/settings.controller.ts` exit 0
    - `test -f apps/api/src/settings/dto/upsert-setting.dto.ts` exit 0
    - `grep -q "exports: \[SettingsService\]" apps/api/src/settings/settings.module.ts` exit 0
    - `grep -q "getPublic" apps/api/src/settings/settings.service.ts` exit 0
    - `grep -q "PUBLIC_PREFIXES" apps/api/src/settings/settings.service.ts` exit 0
    - `grep -q "@Get('public')" apps/api/src/settings/settings.controller.ts` exit 0
    - `grep -q "SettingsModule" apps/api/src/app.module.ts` exit 0
    - El endpoint público NO tiene @UseGuards:
      `awk '/@Get..public..\)/,/getPublic/' apps/api/src/settings/settings.controller.ts | grep -q "UseGuards"` retorna 1 (NO match = sin guards)
    - Las 8 claves requeridas están en el seed:
      `for k in SPOT_PRICE_PER_DAY SPOT_MAX_DAYS SPOT_MAX_ACTIVE HERO_PRICE_PER_DAY HERO_MAX_DAYS HERO_MAX_ACTIVE SUBSCRIPTION_PRICE SUBSCRIPTION_CREDITS; do grep -q "$k" apps/api/prisma/seed.ts || exit 1; done` exit 0
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
    - `cd apps/api &amp;&amp; pnpm prisma:seed` exit 0 (seed corre end-to-end)
  </acceptance_criteria>
  <done>
    SettingsModule completo con 5 métodos (get/getNum/set/getAll/getPublic), 3 endpoints
    (GET /, PATCH /, GET /public), exportado. Registrado en AppModule. Seed verificado
    (8 claves requeridas + 4 extras toleradas). TS + seed pasan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrar SpotsService a SettingsService (ConfigService → DB)</name>
  <read_first>
    - apps/api/src/settings/settings.service.ts (creado en Task 1)
    - apps/api/src/spots/spots.service.ts (métodos quota/assertQuotaAvailable/assertMaxDays)
    - apps/api/src/spots/spots.module.ts
    - apps/api/src/spots/spots.controller.ts (call sites de quota/assertQuotaAvailable/assertMaxDays)
  </read_first>
  <files>apps/api/src/spots/spots.module.ts, apps/api/src/spots/spots.service.ts</files>
  <action>
    **Module** — agregar SettingsModule a imports (manteniendo todo lo demás de 11-02):
    ```typescript
    import { SettingsModule } from '../settings/settings.module';
    // ...
    imports: [AuthModule, AuditModule, NotificationsModule, SettingsModule],
    ```

    **Service** — cambios:

    1. Eliminar el import de ConfigService:
       ```typescript
       // BORRAR: import { ConfigService } from '@nestjs/config';
       ```

    2. Reemplazar `config` por `settings` en el constructor:
       ```typescript
       import { SettingsService } from '../settings/settings.service';
       // ...
       constructor(
         private readonly prisma: PrismaService,
         private readonly settings: SettingsService, // <-- antes: ConfigService
         private readonly mail: MailService,
         private readonly audit: AuditService,
         private readonly notifications: NotificationsService, // del 11-02
       ) {}
       ```

    3. Cambiar los 3 helpers privados de síncronos a async (devuelven Promise<number>):
       ```typescript
       private maxActive(): Promise<number> {
         return this.settings.getNum('SPOT_MAX_ACTIVE');
       }
       private maxDays(): Promise<number> {
         return this.settings.getNum('SPOT_MAX_DAYS');
       }
       private pricePerDay(): Promise<number> {
         return this.settings.getNum('SPOT_PRICE_PER_DAY');
       }
       ```
       Eliminar los `|| 10` / `|| 30` / `|| 8000` fallbacks — el seed garantiza la presencia
       de las 3 claves, y si faltan es un error legítimo (404 desde getNum).

    4. Adaptar call sites en `quota()` (línea ~46):
       ```typescript
       async quota() {
         const [active, max, pricePerDay, maxDays] = await Promise.all([
           this.countActive(),
           this.maxActive(),
           this.pricePerDay(),
           this.maxDays(),
         ]);
         return {
           max,
           active,
           available: Math.max(0, max - active),
           pricePerDay,
           maxDays,
         };
       }
       ```

    5. Adaptar `assertQuotaAvailable()` (línea ~162):
       ```typescript
       async assertQuotaAvailable() {
         const [active, max] = await Promise.all([this.countActive(), this.maxActive()]);
         if (active >= max) {
           throw new BadRequestException('No hay cupos disponibles para spots en este momento');
         }
       }
       ```

    6. Adaptar `assertMaxDays(days: number)` (línea ~169) — cambiar a async:
       ```typescript
       async assertMaxDays(days: number) {
         const max = await this.maxDays();
         if (days > max) {
           throw new BadRequestException(`Un spot se puede publicar por un máximo de ${max} días`);
         }
       }
       ```

    7. Buscar todos los call sites de `assertMaxDays` y `assertQuotaAvailable` y agregar
       `await` donde falte. Probable: orders/payments services para validar al pagar.
       Comando útil para el executor:
       ```bash
       grep -rn "assertMaxDays\|assertQuotaAvailable" apps/api/src --include="*.ts"
       ```
       Cada call site debe ser `await this.spots.assertMaxDays(...)` o
       `await this.spots.assertQuotaAvailable()`. Si alguno no es `async`, convertirlo.

    No tocar nada más del SpotsService — todos los métodos de moderación y CRUD permanecen
    iguales (las notificaciones de 11-02 ya están incorporadas).
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/settings-task2.log; grep -q "error TS" /tmp/settings-task2.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SettingsModule" apps/api/src/spots/spots.module.ts` exit 0
    - `grep -q "private readonly settings: SettingsService" apps/api/src/spots/spots.service.ts` exit 0
    - `! grep -q "private readonly config: ConfigService" apps/api/src/spots/spots.service.ts` exit 0 (eliminado)
    - `! grep -q "from '@nestjs/config'" apps/api/src/spots/spots.service.ts` exit 0 (import eliminado)
    - `grep -q "this.settings.getNum('SPOT_MAX_ACTIVE')" apps/api/src/spots/spots.service.ts` exit 0
    - `grep -q "this.settings.getNum('SPOT_MAX_DAYS')" apps/api/src/spots/spots.service.ts` exit 0
    - `grep -q "this.settings.getNum('SPOT_PRICE_PER_DAY')" apps/api/src/spots/spots.service.ts` exit 0
    - `! grep -E "this.config.get\('SPOT" apps/api/src/spots/spots.service.ts` exit 0 (sin restos)
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    SpotsService lee SPOT_* desde la tabla Settings vía SettingsService.getNum. ConfigService
    eliminado del module y constructor. Call sites async (Promise.all para paralelizar).
    TS compila sin errores.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrar HeroesService a SettingsService (ConfigService → DB) + verificación final</name>
  <read_first>
    - apps/api/src/settings/settings.service.ts (creado en Task 1)
    - apps/api/src/heroes/heroes.service.ts (métodos quota/assertQuotaAvailable/assertMaxDays)
    - apps/api/src/heroes/heroes.module.ts
    - apps/api/src/heroes/heroes.controller.ts (call sites)
  </read_first>
  <files>apps/api/src/heroes/heroes.module.ts, apps/api/src/heroes/heroes.service.ts</files>
  <action>
    Aplicar el mismo patrón que Task 2 a HeroesService (las 7 sub-acciones idénticas
    pero con claves HERO_*):

    **Module** — agregar SettingsModule a imports.

    **Service**:
    1. Eliminar `import { ConfigService } from '@nestjs/config';`
    2. Reemplazar `config: ConfigService` por `settings: SettingsService` en constructor.
    3. Cambiar helpers:
       ```typescript
       private pricePerDay(): Promise<number> {
         return this.settings.getNum('HERO_PRICE_PER_DAY');
       }
       private maxActive(): Promise<number> {
         return this.settings.getNum('HERO_MAX_ACTIVE');
       }
       private maxDays(): Promise<number> {
         return this.settings.getNum('HERO_MAX_DAYS');
       }
       ```
    4. Adaptar `quota()` con Promise.all (mismo patrón que spots).
    5. Adaptar `assertQuotaAvailable()` y `assertMaxDays(days)` a async.
    6. Buscar y arreglar call sites con `grep -rn "heroes.assertMaxDays\|heroes.assertQuotaAvailable" apps/api/src`.
    7. Eliminar fallbacks `|| 15000` / `|| 5` / `|| 30` — el seed los provee.

    **Verificación final integral del plan 11-03**:

    Ejecutar en orden y registrar resultados:
    ```bash
    cd apps/api
    pnpm tsc --noEmit         # debe exit 0
    pnpm prisma:seed          # debe exit 0
    pnpm build                # debe exit 0 (NestJS DI graph completo)
    ```

    Si alguna falla, NO crear el SUMMARY hasta resolver. Las fallas típicas serán:
    - Call site síncrono que no se actualizó a await (TS error)
    - Conflicto de orden de rutas en SettingsController (ya prevenido: 'public' va primero)
    - Algún test e2e que asumía sync (verificar: probablemente describe.skip en VPS, no impacta)

    Solo después de los 3 exit 0, generar el SUMMARY.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&amp;1 | tee /tmp/settings-task3-tsc.log && pnpm prisma:seed 2>&amp;1 | tee /tmp/settings-task3-seed.log && pnpm build 2>&amp;1 | tee /tmp/settings-task3-build.log; grep -qE "error TS|Error:" /tmp/settings-task3-*.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SettingsModule" apps/api/src/heroes/heroes.module.ts` exit 0
    - `grep -q "private readonly settings: SettingsService" apps/api/src/heroes/heroes.service.ts` exit 0
    - `! grep -q "private readonly config: ConfigService" apps/api/src/heroes/heroes.service.ts` exit 0
    - `! grep -q "from '@nestjs/config'" apps/api/src/heroes/heroes.service.ts` exit 0
    - `grep -q "this.settings.getNum('HERO_PRICE_PER_DAY')" apps/api/src/heroes/heroes.service.ts` exit 0
    - `grep -q "this.settings.getNum('HERO_MAX_ACTIVE')" apps/api/src/heroes/heroes.service.ts` exit 0
    - `grep -q "this.settings.getNum('HERO_MAX_DAYS')" apps/api/src/heroes/heroes.service.ts` exit 0
    - `! grep -E "this.config.get\('HERO" apps/api/src/heroes/heroes.service.ts` exit 0
    - `cd apps/api &amp;&amp; pnpm tsc --noEmit` exit 0
    - `cd apps/api &amp;&amp; pnpm prisma:seed` exit 0
    - `cd apps/api &amp;&amp; pnpm build` exit 0
  </acceptance_criteria>
  <done>
    HeroesService migrado. Toda la fase 11 compila, seedea y builda sin errores. Triple
    gate (tsc + seed + build) verde. SettingsService es la única fuente de verdad para
    SPOT_*/HERO_* runtime.
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm tsc --noEmit` exit 0
- `cd apps/api && pnpm prisma:seed` exit 0 (seed idempotente y completo)
- `cd apps/api && pnpm build` exit 0 (NestJS DI graph válido)
- Endpoints públicos: GET /settings/public NO requiere auth (verificable con curl en runtime, no en build)
- Endpoints admin: GET /settings y PATCH /settings requieren ADMIN+ (Roles guard)
- Ningún `this.config.get('SPOT_` o `this.config.get('HERO_` queda en spots/heroes services:
  `! grep -rE "this.config.get\('(SPOT|HERO)" apps/api/src/spots apps/api/src/heroes` exit 0
- SettingsService.getPublic() retorna solo SPOT_/HERO_ (verificable por lectura del código)
</verification>

<success_criteria>
1. SettingsModule existe, exporta SettingsService
2. GET /settings/public es accesible sin auth y devuelve {[key]: value} solo de SPOT_/HERO_
3. GET /settings (admin) devuelve array completo ordenado
4. PATCH /settings (admin) hace upsert (crea si no existe, actualiza si existe)
5. SpotsService.quota() devuelve max/active/pricePerDay/maxDays leyendo de DB
6. HeroesService.quota() análogo a Spots
7. assertMaxDays / assertQuotaAvailable son async y todos sus call sites usan await
8. ConfigService eliminado de los constructors de spots y heroes (para claves SPOT_/HERO_)
9. Seed corre sin duplicar — `upsert update: {}` preserva valores admin
10. pnpm tsc --noEmit + pnpm prisma:seed + pnpm build los tres exit 0
</success_criteria>

<output>
Crear `.planning/phases/11-notificaciones-y-settings/11-03-SUMMARY.md` con:
- Archivos creados/modificados (paths absolutos)
- Decisiones: PUBLIC_PREFIXES = ['SPOT_', 'HERO_'] hard-coded; getNum lanza NotFoundException si falta
- Discrepancia phase_scope vs seed: SPOT_MAX_ACTIVE=12 (seed actual) vs 10 (phase_scope) — gana seed
- Extras del seed preservadas: SPOT_MIN_DAYS, HERO_MIN_DAYS, SUBSCRIPTION_SPOT_DISCOUNT, SUBSCRIPTION_HERO_DISCOUNT
- Phase 11 completa: CFG-01 + CFG-02 + CFG-03 cerrados
- Próximos consumers: Phase 12 (subscriptions usa SettingsService.getNum('SUBSCRIPTION_*'))
</output>
