---
phase: 12-suscripciones-y-carrito-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/
  - apps/api/src/orders/dto/add-item.dto.ts
  - apps/api/src/subscriptions/subscriptions.module.ts
  - apps/api/src/subscriptions/subscriptions.controller.ts
  - apps/api/src/subscriptions/subscriptions.service.ts
  - apps/api/src/subscriptions/dto/create-subscription.dto.ts
  - apps/api/src/subscriptions/dto/query-subscriptions.dto.ts
  - apps/api/src/app.module.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - COM-01
must_haves:
  truths:
    - "Un usuario autenticado puede invocar POST /subscriptions cuando NO tiene sub activa y recibe redirectUrl de Transbank"
    - "Un usuario con sub ACTIVE existente recibe 409 al intentar POST /subscriptions"
    - "GET /subscriptions/me devuelve {active: false} si no hay sub, o {active: true, creditsTotal, creditsUsed, cycleEnd, status} si existe"
    - "Un ADMIN puede ver el listado paginado en GET /subscriptions"
    - "Un usuario que tuvo sub EXPIRED/CANCELLED puede re-suscribirse (no queda bloqueado por @unique)"
    - "REQUIREMENTS.md documenta COM-01..04 con texto suficiente para verificación de cobertura"
  artifacts:
    - path: "apps/api/src/subscriptions/subscriptions.service.ts"
      provides: "create/findMine/cancel/findAll + helper para upsert pre-pago"
      min_lines: 100
    - path: "apps/api/src/subscriptions/subscriptions.controller.ts"
      provides: "POST /subscriptions, GET /subscriptions/me, GET /subscriptions (admin), DELETE /subscriptions/me"
    - path: "apps/api/src/subscriptions/subscriptions.module.ts"
      provides: "SubscriptionsModule con exports: [SubscriptionsService]"
    - path: "apps/api/prisma/schema.prisma"
      contains: "SUBSCRIPTION  // OrderItemType añadido"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Definición de COM-01, COM-02, COM-03, COM-04 en sección v2 (Phase 12)"
      contains: "COM-01"
  key_links:
    - from: "SubscriptionsService.create"
      to: "GatewayFactory.initiate"
      via: "Order especial con OrderItem type=SUBSCRIPTION + returnUrl=/api/subscriptions/confirm"
      pattern: "gatewayFactory\\.get.*initiate"
    - from: "SubscriptionsModule"
      to: "AppModule.imports"
      via: "import en app.module.ts"
      pattern: "SubscriptionsModule"
---

<objective>
Crear `SubscriptionsModule` con endpoints CRUD (POST/GET-me/DELETE-me/GET-admin) que reutiliza `GatewayFactory` para iniciar el pago. Agrega `SUBSCRIPTION` al enum `OrderItemType` (Prisma + DTO) para que el flujo de pago use el mismo carril que el carrito normal. Implementa **COM-01: Subscriptions CRUD**.

También documenta COM-01..04 en `REQUIREMENTS.md` siguiendo el patrón establecido por Phase 11-01 (CFG-01..03): el primer plan de cada fase agrega los IDs a REQUIREMENTS.md para que las verificaciones de cobertura de requisitos no fallen.

**Purpose:** El módulo de suscripciones es la base para el resto del Phase 12 (créditos en carrito, callback de confirmación). Sin él, los demás plans no tienen contra qué validar `subscription.creditsUsed < creditsTotal`.

**Output:**
- Migración Prisma que agrega `SUBSCRIPTION` a `OrderItemType`
- Módulo NestJS completo `subscriptions/` con service + controller + DTOs
- DTO `add-item.dto.ts` actualizado con `SUBSCRIPTION` (y `ARTICLE` también para evitar inconsistencia — ARTICLE ya está en Prisma)
- `app.module.ts` con `SubscriptionsModule` registrado
- `REQUIREMENTS.md` con sección "Comercio (Phase 12)" definiendo COM-01..04
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-suscripciones-y-carrito-v2/12-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/payments/gateway.interface.ts
@apps/api/src/payments/gateway.factory.ts
@apps/api/src/orders/orders.service.ts
@apps/api/src/orders/dto/add-item.dto.ts
@apps/api/src/notifications/notifications.module.ts
@apps/api/src/settings/settings.service.ts
@apps/api/src/common/org-context/org-context.types.ts

<interfaces>
Contracts that this plan creates/consumes — embedded so the executor needs no codebase exploration.

From apps/api/src/payments/gateway.interface.ts:
```typescript
export interface InitiatePaymentParams {
  orderId: number;
  amount: number;
  returnUrl: string;
}
export interface InitiatePaymentResult {
  redirectUrl: string;
  externalId: string;
}
export interface PaymentGateway {
  initiate(params: InitiatePaymentParams): Promise<InitiatePaymentResult>;
  confirm(token: string): Promise<ConfirmPaymentResult>;
}
```

From apps/api/src/common/org-context/org-context.types.ts:
```typescript
export type OrgContextDto = { orgId: number; role: OrgRole };
```

From apps/api/src/auth/current-user.decorator.ts (existing):
```typescript
export type JwtUser = { sub: number; email: string; role: Role };
```

From apps/api/prisma/schema.prisma (Subscription model — line 693-713):
```
model Subscription {
  id              Int                @id @default(autoincrement())
  status          SubscriptionStatus @default(ACTIVE)
  user            User?              @relation("UserSubscription", ...)
  userId          Int?               @unique
  org             User?              @relation("OrgSubscription", ...)
  orgId           Int?               @unique
  cycleStart      DateTime
  cycleEnd        DateTime
  creditsUsed     Int                @default(0)
  creditsTotal    Int                @default(10)
  cancelledAt     DateTime?
}
enum SubscriptionStatus { ACTIVE | CANCELLED | EXPIRED }
```

From apps/api/src/orders/dto/add-item.dto.ts (current — line 4-8):
```typescript
export enum OrderItemType {
  EVENT = 'EVENT',
  SPOT = 'SPOT',
  HERO = 'HERO',
}
// MISSING: ARTICLE (en Prisma) y SUBSCRIPTION (este plan lo añade)
```

Settings keys disponibles (seedeados en Phase 11):
- `SUBSCRIPTION_PRICE` = 9990
- `SUBSCRIPTION_CREDITS` = 10
- `SUBSCRIPTION_SPOT_DISCOUNT` = 20
- `SUBSCRIPTION_HERO_DISCOUNT` = 20
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migración Prisma — agregar SUBSCRIPTION (y ARTICLE) al DTO enum + migración schema</name>
  <files>
    apps/api/prisma/schema.prisma,
    apps/api/src/orders/dto/add-item.dto.ts,
    apps/api/prisma/migrations/
  </files>
  <read_first>
    apps/api/prisma/schema.prisma,
    apps/api/src/orders/dto/add-item.dto.ts
  </read_first>
  <action>
    **Schema Prisma (línea 294-299):** Agregar `SUBSCRIPTION` al enum `OrderItemType`. Estado final del enum (en este orden exacto):
    ```prisma
    enum OrderItemType {
      EVENT
      SPOT
      HERO
      ARTICLE
      SUBSCRIPTION
    }
    ```

    **DTO `add-item.dto.ts` (línea 4-8):** Actualizar el enum TS para reflejar el schema. Estado final:
    ```typescript
    export enum OrderItemType {
      EVENT = 'EVENT',
      SPOT = 'SPOT',
      HERO = 'HERO',
      ARTICLE = 'ARTICLE',
      SUBSCRIPTION = 'SUBSCRIPTION',
    }
    ```

    **Generar migración:**
    ```bash
    cd apps/api && pnpm prisma migrate dev --name com01_subscription_orderitem
    pnpm prisma generate
    ```

    **NOTA:** La validación de `resolveItem` para `ARTICLE` y `SUBSCRIPTION` se implementa en plans 12-02 (ARTICLE) y aquí (SUBSCRIPTION). En este plan basta con que el enum exista; la lógica del subscribe-flow no usa `OrdersService.addItem` — usa creación directa de Order + OrderItem desde `SubscriptionsService` (ver Task 2).

    **NO modificar:** ni `OrdersService` ni `PaymentsService` en este plan. Plan 12-02 cubre la migración a SettingsService de OrdersService.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm prisma validate &amp;&amp; pnpm tsc --noEmit 2&gt;&amp;1 | grep -E "error TS" | head -5 || echo "tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUBSCRIPTION" apps/api/prisma/schema.prisma` devuelve >= 4 (enum + Subscription model + NotificationType + SubscriptionStatus)
    - `grep "SUBSCRIPTION = 'SUBSCRIPTION'" apps/api/src/orders/dto/add-item.dto.ts` encuentra match
    - `grep "ARTICLE = 'ARTICLE'" apps/api/src/orders/dto/add-item.dto.ts` encuentra match
    - Existe un directorio nuevo en `apps/api/prisma/migrations/` con sufijo `_com01_subscription_orderitem`
    - `pnpm prisma validate` exit 0
    - `pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Enum OrderItemType actualizado en Prisma + DTO; migración aplicada; cliente Prisma regenerado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SubscriptionsModule — service + controller + DTOs + integración con GatewayFactory</name>
  <files>
    apps/api/src/subscriptions/subscriptions.module.ts,
    apps/api/src/subscriptions/subscriptions.service.ts,
    apps/api/src/subscriptions/subscriptions.controller.ts,
    apps/api/src/subscriptions/dto/create-subscription.dto.ts,
    apps/api/src/subscriptions/dto/query-subscriptions.dto.ts,
    apps/api/src/app.module.ts
  </files>
  <read_first>
    apps/api/src/payments/payments.service.ts,
    apps/api/src/payments/gateway.factory.ts,
    apps/api/src/payments/payments.module.ts,
    apps/api/src/orders/orders.service.ts,
    apps/api/src/settings/settings.service.ts,
    apps/api/src/notifications/notifications.service.ts,
    apps/api/src/common/org-context/org-context.guard.ts,
    apps/api/src/auth/current-user.decorator.ts
  </read_first>
  <action>
    **Crear archivos del módulo:**

    1. **`subscriptions.module.ts`** — imports `[AuthModule, TransbankModule, SettingsModule, NotificationsModule]`, controllers `[SubscriptionsController]`, providers `[SubscriptionsService, GatewayFactory]`, **exports: `[SubscriptionsService]`** (Plan 12-03 lo importa desde OrdersModule).

       NOTA: `GatewayFactory` se provee aquí también (mismo patrón que PaymentsModule) — no se exporta. Alternativamente, exportar `GatewayFactory` desde PaymentsModule e importar PaymentsModule. **DECISIÓN LOCKED:** proveer aquí para evitar acoplar con PaymentsModule.

    2. **`dto/create-subscription.dto.ts`** — DTO vacío por ahora (el flujo no recibe body adicional, sólo lee `SUBSCRIPTION_PRICE` desde Settings). Exporta `class CreateSubscriptionDto {}` con `@ApiProperty` placeholder, o un campo opcional `gateway` (default TRANSBANK) si se quiere flexibilidad.

       **DECISIÓN LOCKED:** Incluir campo `gateway` opcional default TRANSBANK:
       ```typescript
       export class CreateSubscriptionDto {
         @ApiPropertyOptional({ enum: GatewayType, default: GatewayType.TRANSBANK })
         @IsOptional()
         @IsEnum(GatewayType)
         gateway?: GatewayType = GatewayType.TRANSBANK;
       }
       ```
       Importa `GatewayType` desde `../../payments/dto/checkout.dto`.

    3. **`dto/query-subscriptions.dto.ts`** — para GET /subscriptions admin:
       ```typescript
       export class QuerySubscriptionsDto {
         @IsOptional() @IsInt() @Min(1) page?: number = 1;
         @IsOptional() @IsInt() @Min(1) @Max(50) limit?: number = 20;
         @IsOptional() @IsEnum(SubscriptionStatus) status?: SubscriptionStatus;
       }
       ```

    4. **`subscriptions.service.ts`** — métodos:

       - **`async create(user: JwtUser, orgContext: OrgContextDto | null, dto: CreateSubscriptionDto): Promise<{ redirectUrl: string; externalId: string }>`**
         - Resolver target: `orgContext ? { orgId: orgContext.orgId } : { userId: user.sub }`
         - **D-04 (409 si ACTIVE existe):** consultar `prisma.subscription.findFirst({ where: { ...target, status: 'ACTIVE' } })`. Si existe, lanzar `ConflictException('Ya tienes una suscripción activa')`.
         - **Re-suscripción (gap del @unique):** consultar `prisma.subscription.findFirst({ where: target })` (sin filtro status). Si existe con status CANCELLED o EXPIRED, **eliminarlo** con `prisma.subscription.delete({ where: { id: existing.id } })` antes de continuar (el @unique en userId y orgId aplica sin importar status).
         - Leer precio: `const amount = await this.settings.getNum('SUBSCRIPTION_PRICE')`.
         - **Crear Order especial:** `prisma.order.create({ data: { userId: user.sub, orgId: orgContext?.orgId ?? null, total: amount, status: 'DRAFT', items: { create: [{ type: 'SUBSCRIPTION', days: 30, unitPrice: amount, subtotal: amount }] } } })`.
         - Llamar `gatewayFactory.get(dto.gateway ?? 'TRANSBANK').initiate({ orderId: order.id, amount, returnUrl: \`${apiBase}/api/subscriptions/confirm\` })` donde `apiBase` viene de `config.get('API_BASE_URL', 'http://localhost:3333')`.
         - Actualizar order: `status: 'PENDING_PAYMENT', gateway, externalId`.
         - **D-03:** NO crear `Subscription` row aquí. Solo se crea en `/subscriptions/confirm` (Plan 12-04).
         - Return `{ redirectUrl, externalId }`.

       - **`async findMine(user: JwtUser, orgContext: OrgContextDto | null)`**
         - Resolver target.
         - Consultar `prisma.subscription.findFirst({ where: target })`.
         - Si null: `return { active: false }`.
         - Si existe: `return { active: sub.status === 'ACTIVE', status, creditsUsed, creditsTotal, cycleStart, cycleEnd, cancelledAt }`.

       - **`async cancelMine(user: JwtUser, orgContext: OrgContextDto | null)`**
         - **D-09:** marca `cancelledAt = new Date()` y `status: 'CANCELLED'`; NO termina el ciclo, sigue ACTIVE-equivalent hasta `cycleEnd` (frontend sabe distinguir via `status` y `cancelledAt`).
         - Si no hay sub: 404.
         - Si ya está CANCELLED/EXPIRED: 400 'La suscripción ya está cancelada'.
         - Emit notification: `notifications.create({ type: 'SUBSCRIPTION_CANCELLED', title: 'Suscripción cancelada', body: \`Vigente hasta ${sub.cycleEnd.toISOString()}\`, ...target })`.
         - Return el sub actualizado.

       - **`async findAll(query: QuerySubscriptionsDto)` (admin)**
         - **D-18:** lista paginada, filtros opcionales por status.
         - `where: query.status ? { status: query.status } : {}`.
         - Include `user: { select: { id, email, type, handle } }` y `org: { select: { id, email, handle } }`.
         - Return `{ items, total, page, limit, totalPages }`.

    5. **`subscriptions.controller.ts`** — endpoints:
       - `@UseGuards(JwtAuthGuard, OrgContextGuard)` a nivel de clase (mirror del patrón en `OrdersController:13`).
       - `@ApiTags('subscriptions') @ApiBearerAuth() @Controller('subscriptions')`.
       - `POST /` → `create(user, ctx, dto)` (D-01)
       - `GET /me` → `findMine(user, ctx)` (D-14)
       - `DELETE /me` → `cancelMine(user, ctx)` (D-09)
       - `GET /` → `@UseGuards(RolesGuard) @Roles('ADMIN', 'SUPER_ADMIN')` → `findAll(query)` (D-18)
       - Todos con `@ApiOperation` describing en español.

    6. **`app.module.ts`** — agregar `import { SubscriptionsModule } from './subscriptions/subscriptions.module';` y registrar en `imports: [..., SubscriptionsModule]` (entre NotificationsModule y SettingsModule, alfabético no importa).

    **NO implementar:** el endpoint `/subscriptions/confirm` (callback Transbank) — eso es Plan 12-04. La lógica de créditos en carrito — eso es Plan 12-03.

    **Reglas Swagger (CLAUDE.md):** `@ApiTags('subscriptions')`, `@ApiOperation({ summary: '...' })` en cada endpoint, `@ApiBearerAuth()` donde aplique, `@ApiResponse` donde el shape no es obvio.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm tsc --noEmit 2&gt;&amp;1 | grep -E "error TS" | head -10 || echo "tsc clean" &amp;&amp; pnpm build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/subscriptions/subscriptions.module.ts` existe y `grep "exports: \[SubscriptionsService\]" apps/api/src/subscriptions/subscriptions.module.ts` encuentra match
    - `apps/api/src/subscriptions/subscriptions.service.ts` existe y contiene los 4 métodos: `create`, `findMine`, `cancelMine`, `findAll` (grep cada uno)
    - `apps/api/src/subscriptions/subscriptions.controller.ts` contiene `@Post()`, `@Get('me')`, `@Delete('me')`, `@Get()` (grep cada uno)
    - `grep "SubscriptionsModule" apps/api/src/app.module.ts` encuentra match en `imports:` array
    - `grep "ConflictException" apps/api/src/subscriptions/subscriptions.service.ts` encuentra match (D-04)
    - `grep "subscription.delete\\|subscription\\.delete" apps/api/src/subscriptions/subscriptions.service.ts` encuentra match (re-suscripción)
    - `grep "SUBSCRIPTION_PRICE" apps/api/src/subscriptions/subscriptions.service.ts` encuentra match
    - `grep "returnUrl.*subscriptions/confirm" apps/api/src/subscriptions/subscriptions.service.ts` encuentra match (D-02)
    - `pnpm tsc --noEmit` exit 0
    - `pnpm build` exit 0
  </acceptance_criteria>
  <done>SubscriptionsModule completo, registrado en AppModule, compila sin errores. POST /subscriptions devuelve redirectUrl de Transbank pero NO crea Subscription row (esa creación viene en Plan 12-04).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Documentar COM-01..04 en REQUIREMENTS.md (patrón Phase 11-01 / CFG-01..03)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/12-suscripciones-y-carrito-v2/12-CONTEXT.md
  </read_first>
  <action>
    Phase 07-01 / 11-01 establecieron que cada fase agrega sus IDs a `REQUIREMENTS.md` como parte del primer plan. Replicar para Phase 12.

    Append al final de la sección "v2 Requirements" (después de "Notificaciones y Settings (Phase 11)" — CFG-03), una nueva sub-sección:

    ```markdown
    ### Comercio: Suscripciones y Carrito v2 (Phase 12)

    - [ ] **COM-01**: Módulo `subscriptions` con CRUD: `POST /subscriptions` (crea Order
      especial con item type=SUBSCRIPTION, reutiliza `GatewayFactory.initiate()`, devuelve
      `redirectUrl` de Transbank, NO crea `Subscription` row aún), `GET /subscriptions/me`
      (estado, créditos, ciclo del recipient según X-Org-Context), `DELETE /subscriptions/me`
      (marca `cancelledAt`+`status=CANCELLED`, no termina el ciclo), `GET /subscriptions`
      (ADMIN/SUPER_ADMIN, paginado, filtros por status). `SUBSCRIPTION` agregado al enum
      `OrderItemType` (Prisma + DTO). `ARTICLE` agregado al enum DTO (Prisma ya lo tenía).
      Re-suscripción soportada: sub CANCELLED/EXPIRED se borra antes de crear nueva (sortea
      el `@unique` en `userId`/`orgId`). Conflict 409 si sub ACTIVE existe.

    - [ ] **COM-02**: Orders v2 — `OrderItemType.ARTICLE` operativo en carrito vía patrón
      upsert (`@@unique([orderId, type])`): `unitPrice = ARTICLE_PRICE`, `days = 0` fijo,
      `subtotal = unitPrice`. `AddItemDto.days` se vuelve opcional (`@IsOptional() @IsInt()
      @Min(0)`) — para ARTICLE el service fuerza `days = 0`; para EVENT/SPOT/HERO el service
      valida `days >= 1` y `days <= *_MAX_DAYS`. `OrdersService` migrado de `ConfigService`
      a `SettingsService` para todas las claves `*_MAX_DAYS` y `*_MAX_ACTIVE`. Seed agrega
      `ARTICLE_PRICE=5000` y `EVENT_MAX_DAYS=60` si no existen.

    - [ ] **COM-03**: Créditos de suscripción en carrito — al agregar un EVENT con suscripción
      activa y `creditsUsed < creditsTotal`: `unitPrice = 0`, `days = Math.min(45,
      daysUntilCycleEnd, daysUntilEventExpiration)`, `subtotal = 0`. Si la sub está
      CANCELLED con `cycleEnd` futuro, los créditos siguen aplicando (D-09). Descuentos para
      suscriptores en SPOT/HERO: `unitPrice = precioBase * (1 - SUBSCRIPTION_*_DISCOUNT/100)`
      aplicado automáticamente al `addItem`. Al confirmar pago de un Order con EVENT cubierto
      por crédito (`unitPrice=0 && subtotal=0`), `Subscription.creditsUsed` se incrementa en 1
      dentro del mismo `prisma.$transaction` que activa los ítems.

    - [ ] **COM-04**: Pago de suscripción — callback dedicado `POST /subscriptions/confirm`
      y `GET /subscriptions/confirm` (públicos, sin guards) que recibe el token de Transbank,
      confirma el pago vía `GatewayFactory.confirm()`, y en un `prisma.$transaction` marca el
      Order como PAID + crea la `Subscription` row (`status=ACTIVE`, `cycleStart=now`,
      `cycleEnd=now+30d`, `creditsTotal=SUBSCRIPTION_CREDITS`, `creditsUsed=0`). Emite
      `SUBSCRIPTION_ACTIVATED` notification fire-and-forget. Idempotente: callback duplicado
      sobre Order ya PAID con Subscription existente devuelve URL de success sin duplicar.
      Si Transbank rechaza el pago (`responseCode != 0`), Order pasa a FAILED y NO se crea
      Subscription (D-03). El refactor de guards en `SubscriptionsController` mueve
      `@UseGuards(JwtAuthGuard, OrgContextGuard)` de clase a método para permitir endpoints
      públicos de callback.
    ```

    También actualizar la tabla "Traceability" agregando la fila:
    ```
    | COM-01..04 | Phase 12 | Pending |
    ```

    (Si la tabla "Traceability" solo cubre v1, agregar una nueva tabla "Traceability v2" o
    extender la existente — usar el criterio que mejor calce con el formato actual del archivo.
    Hoy la tabla v1 incluye CFG-01..03 de Phase 11 al final, así que la extensión natural es
    agregar COM-01..04 a continuación.)

    **No tocar** secciones existentes (AUTH/API/SITE/PUBL/MOD/SRCH/HARD/AUD/CFG/SCH). Solo
    append.
  </action>
  <verify>
    <automated>grep -E "COM-0[1-4]" .planning/REQUIREMENTS.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "COM-01" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "COM-02" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "COM-03" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "COM-04" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "Comercio: Suscripciones y Carrito v2 (Phase 12)" .planning/REQUIREMENTS.md` exit 0
    - `grep -q "COM-01..04 | Phase 12" .planning/REQUIREMENTS.md` exit 0
    - Secciones previas (CFG-01..03, SCH-01..06, etc.) siguen intactas (diff solo agrega)
  </acceptance_criteria>
  <done>REQUIREMENTS.md documenta COM-01..04 con texto suficiente para verificación de cobertura. Tabla Traceability actualizada.</done>
</task>

</tasks>

<verification>
**Manual smoke test (opcional, no bloqueante):**
1. `pnpm start:dev`
2. `curl -X POST http://localhost:3333/api/subscriptions -H "Authorization: Bearer $JWT"` → debería devolver `{ redirectUrl, externalId }`
3. `curl http://localhost:3333/docs#/subscriptions` → endpoints visibles en Swagger
4. `curl -X POST http://localhost:3333/api/subscriptions -H "Authorization: Bearer $JWT"` con sub ya activa → 409
5. `grep -E "COM-0[1-4]" .planning/REQUIREMENTS.md | wc -l` → 4 matches
</verification>

<success_criteria>
- COM-01 implementado: CRUD de suscripciones funcional (sin confirm callback aún)
- Migración Prisma com01_subscription_orderitem aplicada
- SubscriptionsModule registrado y exportando SubscriptionsService
- REQUIREMENTS.md documenta COM-01..04 (cobertura para todos los plans del phase)
- `pnpm tsc --noEmit` y `pnpm build` exit 0
- Todos los D-01, D-03, D-04, D-13, D-14, D-18 reflejados en el código
</success_criteria>

<output>
Crear `.planning/phases/12-suscripciones-y-carrito-v2/12-01-SUMMARY.md` documentando:
- Archivos creados/modificados (incluyendo REQUIREMENTS.md)
- Decisión sobre re-suscripción (delete-then-create)
- Decisión sobre OrderItemType.SUBSCRIPTION (added to both Prisma + DTO)
- COM-01..04 documentados en REQUIREMENTS.md (texto base para verificación)
- Pendiente: confirm callback (Plan 12-04), créditos en carrito (Plan 12-03), ARTICLE branch en OrdersService (Plan 12-02)
</output>
