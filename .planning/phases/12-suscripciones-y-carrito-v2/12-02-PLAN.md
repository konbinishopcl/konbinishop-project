---
phase: 12-suscripciones-y-carrito-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/orders/orders.service.ts
  - apps/api/src/orders/orders.module.ts
  - apps/api/prisma/seed.ts
autonomous: true
requirements:
  - COM-02
must_haves:
  truths:
    - "Un usuario puede agregar un ARTICLE al carrito vía PUT /orders/:id/items con type=ARTICLE + articleId, y queda con unitPrice = ARTICLE_PRICE, days = 0, subtotal = ARTICLE_PRICE"
    - "Las validaciones de maxDays para EVENT/SPOT/HERO se leen de SettingsService, no de ConfigService"
    - "Quitar un ARTICLE del carrito vía DELETE /orders/:id/items/ARTICLE funciona como con SPOT/HERO/EVENT"
    - "Al hacer seed, las claves ARTICLE_PRICE y EVENT_MAX_DAYS existen en tabla Settings"
  artifacts:
    - path: "apps/api/src/orders/orders.service.ts"
      provides: "ARTICLE upsert + SettingsService usage en lugar de ConfigService"
      contains: "case OrderItemType.ARTICLE"
    - path: "apps/api/src/orders/orders.module.ts"
      provides: "import SettingsModule"
    - path: "apps/api/prisma/seed.ts"
      contains: "ARTICLE_PRICE"
  key_links:
    - from: "OrdersService.resolveItem"
      to: "Article model"
      via: "prisma.article.findUnique + validación de ownership + status DRAFT"
      pattern: "prisma\\.article\\.findUnique"
    - from: "OrdersService.maxDays"
      to: "SettingsService.getNum"
      via: "lectura de EVENT_MAX_DAYS, HERO_MAX_DAYS, SPOT_MAX_DAYS"
      pattern: "settings\\.getNum\\("
---

<objective>
Implementar **COM-02: Orders v2** — agregar soporte de `OrderItemType.ARTICLE` (upsert en carrito), y migrar `OrdersService` de `ConfigService` a `SettingsService` para todas las claves `*_MAX_DAYS` (consistencia con SpotsService/HeroesService migrados en Phase 11). Agregar `ARTICLE_PRICE` y `EVENT_MAX_DAYS` al seed si no existen.

**Purpose:** El carrito v2 debe aceptar artículos patrocinados como ítem de pago (Phase 13 dependerá de esto). La migración a SettingsService es un cierre de la tarea iniciada en Phase 11-03 que no incluyó OrdersService.

**Output:**
- `OrdersService.resolveItem` con rama `ARTICLE`
- `OrdersService.maxDays` usando `SettingsService.getNum` en lugar de `ConfigService.get`
- `assertSpotQuota` y `assertHeroQuota` usando `SettingsService` (alineación)
- `OrdersModule` con `SettingsModule` importado
- Seed con `ARTICLE_PRICE` y `EVENT_MAX_DAYS` agregados al array `settingsDefaults`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-suscripciones-y-carrito-v2/12-CONTEXT.md
@apps/api/src/orders/orders.service.ts
@apps/api/src/orders/orders.module.ts
@apps/api/src/orders/dto/add-item.dto.ts
@apps/api/src/settings/settings.service.ts
@apps/api/src/settings/settings.module.ts
@apps/api/prisma/seed.ts

<interfaces>
From apps/api/src/settings/settings.service.ts (exports):
```typescript
export class SettingsService {
  async get(key: string): Promise<string>;
  async getNum(key: string): Promise<number>;  // lanza NotFoundException si clave no existe
  async set(key: string, value: string): Promise<Settings>;
  getAll(): Promise<Settings[]>;
  async getPublic(): Promise<Record<string, string>>;
}
```

From apps/api/src/orders/orders.service.ts (línea 29-33 — método actual a reemplazar):
```typescript
private maxDays(type: OrderItemType): number {
  if (type === OrderItemType.EVENT) return Number(this.config.get('EVENT_MAX_DAYS')) || 60;
  if (type === OrderItemType.HERO) return Number(this.config.get('HERO_MAX_DAYS')) || 30;
  return Number(this.config.get('SPOT_MAX_DAYS')) || 30;
}
```

From apps/api/prisma/schema.prisma (Article — línea 84-106):
```
model Article {
  id        Int  @id
  title     String
  status    PublicationStatus @default(DRAFT)
  userId    Int?
  // ...
}
```

ARTICLE ya existe en `OrderItemType` (Prisma schema:298) — el DTO se actualiza en Plan 12-01 Task 1.

Settings keys disponibles (seedeados en Phase 11): SPOT_MAX_DAYS=30, HERO_MAX_DAYS=30. NO existen aún: EVENT_MAX_DAYS, ARTICLE_PRICE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar ARTICLE_PRICE y EVENT_MAX_DAYS al seed</name>
  <files>apps/api/prisma/seed.ts</files>
  <read_first>apps/api/prisma/seed.ts</read_first>
  <action>
    Editar `apps/api/prisma/seed.ts` en el array `settingsDefaults` (línea 660-673). Agregar 2 entradas al final del array, **antes del cierre `];`**:
    ```typescript
    { key: 'EVENT_MAX_DAYS',              value: '60' },
    { key: 'ARTICLE_PRICE',               value: '5000' },
    ```

    Estado final del array `settingsDefaults` esperado (14 entradas):
    ```typescript
    const settingsDefaults: { key: string; value: string }[] = [
      { key: 'SPOT_PRICE_PER_DAY',          value: '8000' },
      { key: 'SPOT_MIN_DAYS',               value: '10' },
      { key: 'SPOT_MAX_DAYS',               value: '30' },
      { key: 'SPOT_MAX_ACTIVE',             value: '10' },
      { key: 'HERO_PRICE_PER_DAY',          value: '15000' },
      { key: 'HERO_MIN_DAYS',               value: '10' },
      { key: 'HERO_MAX_DAYS',               value: '30' },
      { key: 'HERO_MAX_ACTIVE',             value: '5' },
      { key: 'SUBSCRIPTION_PRICE',          value: '9990' },
      { key: 'SUBSCRIPTION_CREDITS',        value: '10' },
      { key: 'SUBSCRIPTION_SPOT_DISCOUNT',  value: '20' },
      { key: 'SUBSCRIPTION_HERO_DISCOUNT',  value: '20' },
      { key: 'EVENT_MAX_DAYS',              value: '60' },
      { key: 'ARTICLE_PRICE',               value: '5000' },
    ];
    ```

    El log de confirmación ya dice "12 defaults" — actualizar el string o dejarlo dinámico usando `${settingsDefaults.length}` (verificar si ya está dinámico; si dice "12 defaults" hard-coded, cambiar a `${settingsDefaults.length} defaults`).

    Ejecutar seed para verificar:
    ```bash
    cd apps/api && pnpm prisma:seed
    ```

    **Patrón `upsert + update:{}`** preserva valores ya cambiados por admin (line 676-680 lo confirma) — seguro re-ejecutar.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm prisma:seed 2&gt;&amp;1 | tail -10 &amp;&amp; pnpm prisma db execute --stdin &lt;&lt;&lt; "SELECT \`key\`, \`value\` FROM Settings WHERE \`key\` IN ('ARTICLE_PRICE','EVENT_MAX_DAYS');" 2&gt;&amp;1 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ARTICLE_PRICE" apps/api/prisma/seed.ts` encuentra match con value '5000'
    - `grep "EVENT_MAX_DAYS" apps/api/prisma/seed.ts` encuentra match con value '60'
    - `pnpm prisma:seed` exit 0 con log mostrando `Settings seeded: 14 defaults` (o número dinámico)
    - Query directa MySQL devuelve 2 rows con keys ARTICLE_PRICE y EVENT_MAX_DAYS
  </acceptance_criteria>
  <done>Settings table contiene ARTICLE_PRICE=5000 y EVENT_MAX_DAYS=60.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OrdersService — migrar a SettingsService + agregar branch ARTICLE en resolveItem</name>
  <files>
    apps/api/src/orders/orders.service.ts,
    apps/api/src/orders/orders.module.ts
  </files>
  <read_first>
    apps/api/src/orders/orders.service.ts,
    apps/api/src/orders/orders.module.ts,
    apps/api/src/settings/settings.service.ts,
    apps/api/src/spots/spots.service.ts,
    apps/api/src/heroes/heroes.service.ts
  </read_first>
  <action>
    **1. `orders.module.ts`** — importar `SettingsModule`:
    ```typescript
    import { SettingsModule } from '../settings/settings.module';
    // ...
    imports: [AuthModule, SettingsModule],
    ```

    **2. `orders.service.ts` — reemplazos sistemáticos:**

    a) **Constructor** — reemplazar `private readonly config: ConfigService` con `private readonly settings: SettingsService` y `private readonly config: ConfigService` puede quedar si se usa para `API_BASE_URL`/otros (verificar — el grep muestra que sólo se usa para las claves Settings, entonces remover `ConfigService` completamente).

       Imports finales:
       - Mantener: `PrismaService`, `JwtUser`, `OrgContextDto`, `AddItemDto`, `OrderItemType`, `OrderStatus`, `PublicationStatus`
       - Quitar: `ConfigService` from `@nestjs/config`
       - Agregar: `import { SettingsService } from '../settings/settings.service';`

    b) **`maxDays(type)` (línea 29-33)** — convertir a async y usar `SettingsService.getNum`:
       ```typescript
       private async maxDays(type: OrderItemType): Promise<number> {
         if (type === OrderItemType.EVENT) return this.settings.getNum('EVENT_MAX_DAYS');
         if (type === OrderItemType.HERO) return this.settings.getNum('HERO_MAX_DAYS');
         if (type === OrderItemType.SPOT) return this.settings.getNum('SPOT_MAX_DAYS');
         // ARTICLE y SUBSCRIPTION no tienen maxDays (precio fijo / 30 días fijos)
         return 0;
       }
       ```

       En el caller (`addItem`, línea 71-74): cambiar a `const maxDays = await this.maxDays(dto.type);`. Skip la validación si maxDays === 0 (ARTICLE/SUBSCRIPTION).

       **Específicamente para ARTICLE**: el caller debe forzar `dto.days = 0` y NO validar maxDays. Ver siguiente punto.

    c) **`resolveItem(dto, user, orgContext)` (línea 110-148)** — agregar branch ARTICLE antes del EVENT branch:
       ```typescript
       if (dto.type === OrderItemType.ARTICLE) {
         if (!dto.articleId) throw new BadRequestException('articleId es requerido para ítems de tipo ARTICLE');
         const article = await this.prisma.article.findUnique({ where: { id: dto.articleId } });
         if (!article) throw new NotFoundException('Artículo no encontrado');
         if (article.userId !== ownerId) throw new ForbiddenException('Este artículo no pertenece al contexto actual');
         if (article.status !== PublicationStatus.DRAFT) {
           throw new BadRequestException('Solo se pueden agregar artículos en estado DRAFT al carrito');
         }
         const unitPrice = await this.settings.getNum('ARTICLE_PRICE');
         return { unitPrice, eventId: null, spotId: null, heroId: null, articleId: dto.articleId };
       }
       ```

       **Actualizar el return shape de `resolveItem` y los otros branches** para incluir `articleId: null` (SPOT/HERO/EVENT branches devuelven `articleId: null`).

    d) **`AddItemDto`** — el campo `articleId` ya debería existir en el DTO (verificar `apps/api/src/orders/dto/add-item.dto.ts`). Si no existe, agregar:
       ```typescript
       @ApiPropertyOptional({ example: 5, description: 'ID del artículo en DRAFT (requerido cuando type = ARTICLE)' })
       @IsOptional()
       @IsInt()
       articleId?: number;
       ```
       (Plan 12-01 Task 1 ya agregó ARTICLE al enum; este Task agrega el campo `articleId` al DTO si falta.)

    e) **`addItem` upsert (línea 83-87)** — agregar `articleId` al create/update:
       ```typescript
       const { unitPrice, eventId, spotId, heroId, articleId } = await this.resolveItem(dto, user, orgContext);
       const days = dto.type === OrderItemType.ARTICLE ? 0 : dto.days;
       const subtotal = dto.type === OrderItemType.ARTICLE ? unitPrice : days * unitPrice;

       await this.prisma.orderItem.upsert({
         where: { orderId_type: { orderId: order.id, type: dto.type } },
         create: { orderId: order.id, type: dto.type, days, unitPrice, subtotal, eventId, spotId, heroId, articleId },
         update: { days, unitPrice, subtotal, eventId, spotId, heroId, articleId },
       });
       ```

    f) **`assertSpotQuota` (línea 150-158)** y **`assertHeroQuota` (línea 160-168)** — migrar a SettingsService:
       ```typescript
       private async assertSpotQuota() {
         const maxActive = await this.settings.getNum('SPOT_MAX_ACTIVE');
         // ... rest stays the same
       }
       private async assertHeroQuota() {
         const maxActive = await this.settings.getNum('HERO_MAX_ACTIVE');
         // ...
       }
       ```

    g) **`ITEM_INCLUDE` (línea 16-20)** — agregar `article: true`:
       ```typescript
       const ITEM_INCLUDE = {
         event: { include: { category: true } },
         spot: true,
         hero: { include: { category: true } },
         article: true,
       } as const;
       ```

    **NO MODIFICAR EN ESTE PLAN:**
    - `PaymentsService` — la migración de ConfigService allí queda fuera de scope (sigue siendo COM-02 conceptualmente pero implementarlo aquí amplía el blast radius; se documenta como deferred).
    - El branch para `SUBSCRIPTION` en resolveItem — el flujo de subscriptions usa creación directa de Order/OrderItem desde `SubscriptionsService.create` (Plan 12-01), no pasa por `addItem`.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm tsc --noEmit 2&gt;&amp;1 | grep -E "error TS" | head -10 || echo "tsc clean" &amp;&amp; pnpm build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ConfigService" apps/api/src/orders/orders.service.ts` NO encuentra match (ConfigService removido)
    - `grep "this.settings.getNum" apps/api/src/orders/orders.service.ts | wc -l` devuelve >= 5 (EVENT_MAX_DAYS, HERO_MAX_DAYS, SPOT_MAX_DAYS, SPOT_MAX_ACTIVE, HERO_MAX_ACTIVE, ARTICLE_PRICE)
    - `grep "case OrderItemType.ARTICLE\\|OrderItemType.ARTICLE" apps/api/src/orders/orders.service.ts` encuentra al menos 1 match en resolveItem
    - `grep "prisma.article.findUnique" apps/api/src/orders/orders.service.ts` encuentra match
    - `grep "articleId" apps/api/src/orders/orders.service.ts | wc -l` devuelve >= 3 (en resolveItem + upsert create + upsert update)
    - `grep "SettingsModule" apps/api/src/orders/orders.module.ts` encuentra match
    - `pnpm tsc --noEmit` exit 0
    - `pnpm build` exit 0
  </acceptance_criteria>
  <done>OrdersService usa SettingsService para todas las claves config, ARTICLE branch implementado en resolveItem, OrdersModule importa SettingsModule.</done>
</task>

</tasks>

<verification>
**Manual smoke test (opcional, no bloqueante):**
1. `pnpm start:dev`
2. Crear un Article DRAFT como organizador.
3. `curl -X PUT http://localhost:3333/api/orders/$ORDER_ID/items -H "Authorization: Bearer $JWT" -d '{"type":"ARTICLE","articleId":1,"days":0}'`
4. Verificar respuesta: subtotal = 5000, total acumulado.
5. `curl -X DELETE http://localhost:3333/api/orders/$ORDER_ID/items/ARTICLE` → ítem eliminado.
</verification>

<success_criteria>
- COM-02 implementado: ARTICLE como tipo de ítem operativo en carrito
- OrdersService migrado de ConfigService a SettingsService
- Seed contiene ARTICLE_PRICE y EVENT_MAX_DAYS
- `pnpm tsc --noEmit` y `pnpm build` exit 0
- Decisiones D-15, D-16, D-17 reflejadas en el código
</success_criteria>

<output>
Crear `.planning/phases/12-suscripciones-y-carrito-v2/12-02-SUMMARY.md` documentando:
- Archivos modificados
- Decisión sobre `days = 0` para ARTICLE (fijo, no por día) — D-16
- Decisión de NO migrar PaymentsService a SettingsService en este plan (deferred)
- Confirmación de que el DTO `add-item.dto.ts` quedó con ARTICLE + SUBSCRIPTION (Plan 12-01 + 12-02)
</output>
