---
phase: 13-contenido-avanzado
plan: 03
type: execute
wave: 2
depends_on:
  - 13-contenido-avanzado/13-02
files_modified:
  - apps/api/src/users/users.service.ts
  - apps/api/src/users/users.controller.ts
  - apps/api/src/users/dto/update-organizer.dto.ts
  - apps/api/src/users/dto/set-verified.dto.ts
autonomous: true
requirements:
  - CNT-03
must_haves:
  truths:
    - "GET /users/:handle es público (sin JWT) y devuelve datos públicos del user/org + eventos APPROVED recientes + (si type=ORGANIZATION) artículos patrocinados APPROVED"
    - "GET /users/:handle devuelve 404 si el handle no existe o el user está blocked"
    - "PATCH /users/me/organizer permite al usuario autenticado actualizar bio y website en su Profile (1:1 con User)"
    - "PATCH /users/:id/verified solo permite SUPER_ADMIN cambiar User.isVerified; ADMIN o AUTHENTICATED reciben 403"
    - "Personas y organizaciones comparten el namespace de handle — GET /users/:handle resuelve a cualquiera de los dos tipos"
    - "El handle se busca case-sensitive (matching exact en Prisma findUnique)"
  artifacts:
    - path: "apps/api/src/users/users.service.ts"
      provides: "findByHandle(handle), updateOrganizer(userId, dto), setVerified(id, isVerified, actor)"
      contains: "findByHandle"
    - path: "apps/api/src/users/users.controller.ts"
      provides: "GET /users/:handle (público), PATCH /users/me/organizer, PATCH /users/:id/verified (SUPER_ADMIN)"
      contains: "/users/:handle"
    - path: "apps/api/src/users/dto/update-organizer.dto.ts"
      provides: "DTO con bio?: string y website?: string"
      contains: "bio"
    - path: "apps/api/src/users/dto/set-verified.dto.ts"
      provides: "DTO con isVerified: boolean"
      contains: "isVerified"
  key_links:
    - from: "UsersController.findByHandle"
      to: "UsersService.findByHandle"
      via: "GET /users/:handle SIN guards (público)"
      pattern: "findByHandle"
    - from: "UsersService.updateOrganizer"
      to: "prisma.profile.upsert"
      via: "Actualizar bio/website en Profile (upsert porque puede no existir)"
      pattern: "profile.upsert"
    - from: "UsersController.setVerified"
      to: "UsersService.setVerified"
      via: "PATCH /users/:id/verified con @Roles('SUPER_ADMIN')"
      pattern: "SUPER_ADMIN"
---

<objective>
Implementar **CNT-03: Perfil público v2** — endpoint público `GET /users/:handle` que devuelve perfil de persona u organización con sus eventos aprobados (y artículos patrocinados si es organización) (D-11); endpoint autenticado `PATCH /users/me/organizer` para actualizar bio y website (D-12); endpoint SUPER_ADMIN `PATCH /users/:id/verified` para asignar/revocar el badge Verificado (D-13).

**Purpose:** Habilita el perfil público de organizadores/orgs en `/@handle`. Cierra el loop del namespace handle global (personas y orgs comparten — D-14). Es la primera vista pública del perfil v2 más allá del listado de eventos.

**Output:**
- DTO `UpdateOrganizerDto` con `bio?`, `website?`
- DTO `SetVerifiedDto` con `isVerified: boolean`
- `UsersService.findByHandle(handle)` — devuelve user/org pública con eventos + (si org) artículos
- `UsersService.updateOrganizer(userId, dto)` — upsert Profile con bio/website
- `UsersService.setVerified(id, isVerified, actor, req)` — actualiza isVerified + audit
- `UsersController.findByHandle` — endpoint público sin guards
- `UsersController.updateOrganizer` — endpoint con JwtAuthGuard
- `UsersController.setVerified` — endpoint con JwtAuth + Roles SUPER_ADMIN
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-contenido-avanzado/13-CONTEXT.md
@.planning/phases/13-contenido-avanzado/13-02-PLAN.md
@apps/api/src/users/users.service.ts
@apps/api/src/users/users.controller.ts
@apps/api/src/users/users.module.ts
@apps/api/prisma/schema.prisma

<interfaces>
**Existing schema (apps/api/prisma/schema.prisma):**

```typescript
model User {
  id: number; email: string; firstname?: string; lastname?: string;
  type: UserType; // PERSON | ORGANIZATION
  handle?: string @unique;
  isVerified: boolean @default(false);
  blocked: boolean @default(false);
  profile?: Profile;  // 1:1 inverse
  events: Event[] @relation("EventOwner");
  articles: Article[] @relation("ArticleOwner");
}

model Profile {
  id: number; slug: string @unique;
  displayName?: string; bio?: string;
  avatar?: string; banner?: string; website?: string;
  instagram?: string; tiktok?: string; facebook?: string;
  x?: string; youtube?: string; twitch?: string; linkedin?: string;
  userId: number @unique; // 1:1 con User
}

enum UserType { PERSON, ORGANIZATION }
enum PublicationStatus { ..., APPROVED, ... }
```

**Existing helpers (apps/api/src/users/users.service.ts):**
- `private async ensure(id: number)` — valida existencia, throws NotFoundException
- `USER_SELECT` const — campos públicos sin passwordHash
- `AuditService.log({ userId, action, entity, entityId, metadata?, req })` — pattern para audit

**Audit enum coverage:** `AuditEntity` incluye `USER` (verificado en Phase 07-02). `AuditAction.UPDATE` aplica para setVerified.

**Pattern existing — ban (users.service.ts:81-92):**
- update + audit.log
- Misma estructura para setVerified
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear DTOs UpdateOrganizerDto y SetVerifiedDto</name>
  <files>
    apps/api/src/users/dto/update-organizer.dto.ts,
    apps/api/src/users/dto/set-verified.dto.ts
  </files>
  <read_first>
    apps/api/src/users/dto/update-user.dto.ts,
    apps/api/src/users/dto/create-user.dto.ts
  </read_first>
  <action>
    Crear DTOs siguiendo patrón existente (class-validator + @nestjs/swagger, sin @nestjs/mapped-types).

    **`apps/api/src/users/dto/update-organizer.dto.ts`:**
    ```typescript
    import { ApiPropertyOptional } from '@nestjs/swagger';
    import { IsOptional, IsString, MaxLength, IsUrl } from 'class-validator';

    export class UpdateOrganizerDto {
      @ApiPropertyOptional({ example: 'Organizamos festivales de música electrónica desde 2015.', maxLength: 500 })
      @IsOptional()
      @IsString()
      @MaxLength(500)
      bio?: string;

      @ApiPropertyOptional({ example: 'https://miorganizacion.cl' })
      @IsOptional()
      @IsString()
      @IsUrl({ require_protocol: true })
      website?: string;
    }
    ```

    **`apps/api/src/users/dto/set-verified.dto.ts`:**
    ```typescript
    import { ApiProperty } from '@nestjs/swagger';
    import { IsBoolean } from 'class-validator';

    export class SetVerifiedDto {
      @ApiProperty({ example: true, description: 'true = otorgar badge Verificado, false = revocar' })
      @IsBoolean()
      isVerified: boolean;
    }
    ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "update-organizer|set-verified" | head -5; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class UpdateOrganizerDto" apps/api/src/users/dto/update-organizer.dto.ts` returns 0
    - `grep -q "bio" apps/api/src/users/dto/update-organizer.dto.ts` returns 0
    - `grep -q "website" apps/api/src/users/dto/update-organizer.dto.ts` returns 0
    - `grep -q "@IsUrl" apps/api/src/users/dto/update-organizer.dto.ts` returns 0
    - `grep -q "class SetVerifiedDto" apps/api/src/users/dto/set-verified.dto.ts` returns 0
    - `grep -q "isVerified" apps/api/src/users/dto/set-verified.dto.ts` returns 0
    - `grep -q "@IsBoolean" apps/api/src/users/dto/set-verified.dto.ts` returns 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Ambos DTOs existen con validaciones correctas y tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar findByHandle + updateOrganizer + setVerified en UsersService</name>
  <files>
    apps/api/src/users/users.service.ts
  </files>
  <read_first>
    apps/api/src/users/users.service.ts,
    apps/api/src/users/users.module.ts,
    apps/api/prisma/schema.prisma
  </read_first>
  <action>
    EXTENDER `UsersService`. NO reescribir lo existente. Mantener findAll, findRecent, create, update, setBanned, remove, ensure. Agregar 3 métodos.

    **1. Import al inicio:**
    ```typescript
    import { PublicationStatus, UserType } from '@prisma/client';
    import { UpdateOrganizerDto } from './dto/update-organizer.dto';
    ```

    **2. AGREGAR `async findByHandle(handle: string)`:**
    ```typescript
    async findByHandle(handle: string) {
      const user = await this.prisma.user.findUnique({
        where: { handle },
        select: {
          id: true,
          handle: true,
          firstname: true,
          lastname: true,
          type: true,
          isVerified: true,
          blocked: true,
          createdAt: true,
          profile: {
            select: {
              displayName: true, bio: true, avatar: true, banner: true, website: true,
              instagram: true, tiktok: true, facebook: true, x: true, youtube: true, twitch: true, linkedin: true,
            },
          },
        },
      });
      if (!user || user.blocked) throw new NotFoundException('Usuario no encontrado');

      // Eventos APPROVED recientes (max 12)
      const events = await this.prisma.event.findMany({
        where: { userId: user.id, status: PublicationStatus.APPROVED },
        orderBy: { createdAt: 'desc' },
        take: 12,
        include: {
          city: { include: { state: { include: { country: true } } } },
          category: true,
          dates: true,
        },
      });

      // Si es ORGANIZATION, traer artículos patrocinados APPROVED
      let articles: unknown[] = [];
      if (user.type === UserType.ORGANIZATION) {
        articles = await this.prisma.article.findMany({
          where: { userId: user.id, status: PublicationStatus.APPROVED },
          orderBy: { createdAt: 'desc' },
          take: 12,
          include: { tags: true },
        });
      }

      // No exponer blocked al cliente — quitar del response
      const { blocked, ...publicUser } = user;
      return { ...publicUser, events, articles };
    }
    ```

    **3. AGREGAR `async updateOrganizer(userId: number, dto: UpdateOrganizerDto)`:**
    - Profile es 1:1 con User pero `Profile.slug @unique` es required. Si no existe perfil, crear con slug = `user-{userId}` o similar — verificar si el User tiene Profile ya creado en el flujo de auth/register. Si no existe, hay que crear. Usar upsert sobre `userId @unique`:
    ```typescript
    async updateOrganizer(userId: number, dto: UpdateOrganizerDto) {
      // Verificar que el user existe
      await this.ensure(userId);

      // Profile.slug es required y @unique — para crear si no existe usamos un slug derivado del userId
      const fallbackSlug = `user-${userId}`;
      const profile = await this.prisma.profile.upsert({
        where: { userId },
        update: {
          ...(dto.bio !== undefined && { bio: dto.bio }),
          ...(dto.website !== undefined && { website: dto.website }),
        },
        create: {
          userId,
          slug: fallbackSlug,
          bio: dto.bio ?? null,
          website: dto.website ?? null,
        },
        select: {
          id: true, bio: true, website: true,
          avatar: true, banner: true, displayName: true,
        },
      });
      return profile;
    }
    ```

    **4. AGREGAR `async setVerified(id: number, isVerified: boolean, actor: JwtUser, req?: Request)`:**
    ```typescript
    async setVerified(id: number, isVerified: boolean, actor: JwtUser, req?: Request) {
      const before = await this.ensure(id);
      const result = await this.prisma.user.update({
        where: { id },
        data: { isVerified },
        select: USER_SELECT,
      });
      // Audit solo si cambió
      if (before.isVerified !== isVerified) {
        this.audit.log({
          userId: actor.sub,
          action: 'UPDATE',
          entity: 'USER',
          entityId: id,
          metadata: { prevVerified: before.isVerified, newVerified: isVerified },
          req,
        });
      }
      return result;
    }
    ```

    **NOTA D-13:** NO emitir notificación — NotificationType.VERIFICATION_GRANTED no existe en el enum (decisión explícita en CONTEXT.md).

    **NOTA `USER_SELECT`:** El select existente NO incluye `isVerified`, `handle`, `type`. Agregar estos campos a USER_SELECT para que el response los exponga:
    ```typescript
    const USER_SELECT = {
      id: true,
      email: true,
      firstname: true,
      lastname: true,
      rut: true,
      isCompany: true,
      role: true,
      confirmed: true,
      blocked: true,
      // v2 fields
      type: true,
      handle: true,
      isVerified: true,
      createdAt: true,
      updatedAt: true,
    };
    ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "users\.service" | head -5; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async findByHandle" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "async updateOrganizer" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "async setVerified" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "profile.upsert" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "UserType.ORGANIZATION" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "PublicationStatus.APPROVED" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "isVerified:" apps/api/src/users/users.service.ts` returns 0
    - `grep -q "handle:" apps/api/src/users/users.service.ts` returns 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>UsersService tiene los 3 métodos nuevos, USER_SELECT incluye type/handle/isVerified, audit solo en setVerified si cambió, tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Agregar GET /users/:handle + PATCH /users/me/organizer + PATCH /users/:id/verified en UsersController</name>
  <files>
    apps/api/src/users/users.controller.ts
  </files>
  <read_first>
    apps/api/src/users/users.controller.ts,
    apps/api/src/users/users.service.ts
  </read_first>
  <action>
    EXTENDER `UsersController`. Mantener todos los handlers existentes. Agregar 3 endpoints.

    **CRÍTICO orden de rutas en NestJS** — declarar rutas específicas ANTES de patterns con params:
    1. `@Get('recent')` (existente)
    2. `@Get('me/saved-events')` (de Plan 13-02)
    3. `@Patch('me/organizer')` (NUEVO — debe ir antes que `@Patch(':id')` para no ser interpretado como `:id=me`)
    4. `@Get(':handle')` (NUEVO — string, sin ParseIntPipe, va al final de los GET)
    5. `@Patch(':id/verified')` (NUEVO — específico, antes que `@Patch(':id')`)
    6. Patches existentes con `:id` (al final)

    NestJS resuelve por **orden de declaración + path matching**. `/me/organizer` no chocará con `/:id` numérico (ParseIntPipe rechaza 'me'). Pero declárelo en orden explícito.

    **1. Imports adicionales:**
    ```typescript
    import { UpdateOrganizerDto } from './dto/update-organizer.dto';
    import { SetVerifiedDto } from './dto/set-verified.dto';
    ```

    **2. AGREGAR `@Patch('me/organizer')` (declarar antes de `@Patch(':id')`):**
    ```typescript
    @Patch('me/organizer')
    @ApiBearerAuth()
    @UseGuards(JwtAuthGuard)
    @ApiOperation({ summary: 'Actualizar bio y website del organizador autenticado' })
    updateOrganizer(@Body() dto: UpdateOrganizerDto, @CurrentUser() user: JwtUser) {
      return this.users.updateOrganizer(user.sub, dto);
    }
    ```

    **3. AGREGAR `@Patch(':id/verified')` (declarar antes de `@Patch(':id')`):**
    ```typescript
    @Patch(':id/verified')
    @ApiBearerAuth()
    @UseGuards(JwtAuthGuard, RolesGuard)
    @Roles('SUPER_ADMIN')
    @ApiOperation({ summary: 'Asignar o revocar el badge Verificado (SUPER_ADMIN)' })
    setVerified(
      @Param('id', ParseIntPipe) id: number,
      @Body() dto: SetVerifiedDto,
      @CurrentUser() user: JwtUser,
      @Req() req: Request,
    ) {
      return this.users.setVerified(id, dto.isVerified, user, req);
    }
    ```

    **4. AGREGAR `@Get(':handle')` (PÚBLICO, sin guards — declarar al final de los GETs):**
    ```typescript
    @Get(':handle')
    @ApiOperation({ summary: 'Perfil público por handle (persona u organización)' })
    findByHandle(@Param('handle') handle: string) {
      return this.users.findByHandle(handle);
    }
    ```

    **NOTA:** Como `@Get(':handle')` usa string sin ParseIntPipe y `@Get()` (findAll) requiere JwtAuth, hay potencial conflicto si llega un GET sin handle. Pero NestJS resolverá `/users` → `@Get()` (findAll) y `/users/foo` → `@Get(':handle')`. No hay ambigüedad.

    **ORDEN FINAL recomendado en el controller (después de modificaciones de Plan 13-02 + este plan):**
    1. `@Get('recent')` — público
    2. `@Get('me/saved-events')` — JWT (de Plan 13-02)
    3. `@Get()` findAll — ADMIN+
    4. `@Post()` create — SUPER_ADMIN
    5. `@Patch('me/organizer')` — JWT (NUEVO)
    6. `@Patch(':id/verified')` — SUPER_ADMIN (NUEVO)
    7. `@Patch(':id')` update — SUPER_ADMIN (existente)
    8. `@Patch(':id/ban')` — SUPER_ADMIN (existente)
    9. `@Delete(':id')` — SUPER_ADMIN (existente)
    10. `@Get(':handle')` — PÚBLICO (NUEVO — al final)

    El orden 10 (`:handle`) intencionalmente está al final para no capturar rutas más específicas. Aunque NestJS por defecto registra todas las rutas y matchea por path exacto, declarar específicas-primero es mejor práctica defensiva.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "users\.controller" | head -5; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@Patch('me/organizer')" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "@Patch(':id/verified')" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "@Get(':handle')" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "UpdateOrganizerDto" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "SetVerifiedDto" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "@Roles('SUPER_ADMIN')" apps/api/src/users/users.controller.ts` returns 0
    - `grep -q "findByHandle" apps/api/src/users/users.controller.ts` returns 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
    - `cd apps/api && pnpm build 2>&1 | tail -5` exits 0
  </acceptance_criteria>
  <done>Controller expone GET /users/:handle (público), PATCH /users/me/organizer (JWT), PATCH /users/:id/verified (SUPER_ADMIN); orden de rutas declarado explícitamente; build pasa.</done>
</task>

</tasks>

<verification>
- Build: `cd apps/api && pnpm build` exits 0
- TypeScript: `cd apps/api && pnpm tsc --noEmit` exits 0
- Test público handle: `curl http://localhost:3001/users/algun-handle | jq` debe devolver user/org public + events + (si org) articles
- Test 404: `curl -i http://localhost:3001/users/handle-inexistente` → HTTP 404
- Test update organizer: `curl -X PATCH http://localhost:3001/users/me/organizer -H "Authorization: Bearer <token>" -H "Content-Type: application/json" -d '{"bio":"Hola"}'` → 200
- Test verified SUPER_ADMIN: `curl -X PATCH http://localhost:3001/users/1/verified -H "Authorization: Bearer <super_token>" -H "Content-Type: application/json" -d '{"isVerified":true}'` → 200
- Test verified non-super: con token ADMIN → 403
</verification>

<success_criteria>
- 3 endpoints nuevos en UsersController funcionales
- GET /users/:handle público y resolución de blocked/inexistente → 404
- PATCH /users/me/organizer hace upsert en Profile (crea si no existe)
- PATCH /users/:id/verified restringido a SUPER_ADMIN, ADMIN recibe 403, audita el cambio
- Build + tsc pasan sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/13-contenido-avanzado/13-03-SUMMARY.md`
</output>
