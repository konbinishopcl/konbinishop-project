---
phase: 13-contenido-avanzado
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/catalog/dto/create-category.dto.ts
  - apps/api/src/catalog/dto/update-category.dto.ts
  - apps/api/src/orders/orders.service.ts
autonomous: true
requirements:
  - CNT-04
must_haves:
  truths:
    - "POST /categories acepta icon, color, minDays, maxDays, order además de los campos existentes; sin ellos los defaults del schema aplican (icon=null, color=null, minDays=1, maxDays=30, order=0)"
    - "PATCH /categories/:id acepta los mismos 5 campos opcionales y actualiza solo los presentes"
    - "GET /categories devuelve los 5 campos en la respuesta (sin cambios — Prisma findMany ya retorna todas las columnas)"
    - "OrdersService.addItem para EVENT valida days contra Math.min(category.maxDays, EVENT_MAX_DAYS) en lugar de solo EVENT_MAX_DAYS"
    - "Si event.category no existe, se usa solo EVENT_MAX_DAYS global"
    - "Si category.minDays > 1, addItem para EVENT rechaza con BadRequestException si days < category.minDays"
    - "Validación de cap por categoría NO aplica a EVENT con crédito de suscripción (días se calculan internamente, no se validan vs maxDays usuario-input)"
  artifacts:
    - path: "apps/api/src/catalog/dto/create-category.dto.ts"
      provides: "DTO extendido con icon, color, minDays, maxDays, order"
      contains: "minDays"
    - path: "apps/api/src/catalog/dto/update-category.dto.ts"
      provides: "DTO extendido con los 5 campos opcionales"
      contains: "maxDays"
    - path: "apps/api/src/orders/orders.service.ts"
      provides: "addItem usa category.maxDays como cap adicional; minDays validation para EVENT"
      contains: "category.maxDays"
  key_links:
    - from: "CatalogService.createCategory"
      to: "Category DTO con campos v2"
      via: "Prisma.category.create({ data: dto }) ya pasa todos los campos del DTO al modelo (data: dto pattern)"
      pattern: "data: dto"
    - from: "OrdersService.addItem (EVENT)"
      to: "category.maxDays + category.minDays"
      via: "Cap calculation Math.min(category.maxDays, EVENT_MAX_DAYS) + minDays check"
      pattern: "category\\.maxDays|category\\.minDays"
---

<objective>
Implementar **CNT-04: Category v2** — extender DTOs `CreateCategoryDto` y `UpdateCategoryDto` con los campos v2 ya presentes en el schema (`icon`, `color`, `minDays`, `maxDays`, `order`) (D-15); integrar `category.minDays` y `category.maxDays` en la validación del carrito de EVENT (D-17).

**Purpose:** Expone los campos visuales y de validación per-categoría que el schema ya soporta pero los DTOs admin no aceptaban. La integración en el carrito permite que diferentes categorías tengan ventanas de publicación distintas (ej: "Eventos exposición" permite 1-90 días, "Eventos puntuales" 1-7 días).

**Output:**
- `CreateCategoryDto` con `icon?`, `color?`, `minDays?`, `maxDays?`, `order?` (validación class-validator)
- `UpdateCategoryDto` con los mismos campos opcionales
- `OrdersService.addItem` con cap por categoría aplicado a EVENT (sin crédito)
- `OrdersService.addItem` con minDays check para EVENT
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-contenido-avanzado/13-CONTEXT.md
@apps/api/src/catalog/dto/create-category.dto.ts
@apps/api/src/catalog/dto/update-category.dto.ts
@apps/api/src/catalog/catalog.service.ts
@apps/api/src/orders/orders.service.ts
@apps/api/prisma/schema.prisma

<interfaces>
**Existing schema (apps/api/prisma/schema.prisma:53-71):**

```typescript
model Category {
  id: number; name?: string; slug: string @unique;
  description?: string; pricePerDay: number @default(1000);
  icon?: string;       // v2: lucide icon name
  color?: string;      // v2: hex or CSS class
  minDays: number @default(1);  // v2
  maxDays: number @default(30); // v2
  order: number @default(0);    // v2
}
```

**Existing CatalogService (apps/api/src/catalog/catalog.service.ts:158-167):**
```typescript
async createCategory(dto: CreateCategoryDto) {
  await this.assertUniqueSlug('category', dto.slug);
  return this.prisma.category.create({ data: dto });  // <-- spread directo del DTO
}

async updateCategory(id: number, dto: UpdateCategoryDto) {
  await this.findCategory(id);
  if (dto.slug) await this.assertUniqueSlug('category', dto.slug, id);
  return this.prisma.category.update({ where: { id }, data: dto });  // <-- spread directo
}
```

**NOTA IMPORTANTE:** `CatalogService.createCategory/updateCategory` ya hace `data: dto` (spread directo). Por tanto, si los DTOs incluyen los campos v2, llegan automáticamente al `prisma.create/update` sin tocar el service. Solo modificar DTOs.

**Existing OrdersService.addItem (apps/api/src/orders/orders.service.ts:83-145):**
- `maxDays(type)` helper retorna global desde Settings — NO recibe contexto del evento (limitación a refactorizar para D-17)
- `resolveItem` ya carga `event.category` (línea 226-228, `include: { category: true }`)
- EVENT con crédito skipea validación de días (línea 93-100)
- EVENT sin crédito: `dto.days` validado contra `maxDays(EVENT)` global

**Refactor approach D-17:**
- Mover la validación de days para EVENT del helper genérico `maxDays(type)` al flujo específico de EVENT en `addItem`. Para SPOT/HERO/ARTICLE mantener `maxDays(type)` global.
- Para EVENT (sin crédito): cargar el event una vez con category, calcular `effectiveMaxDays = Math.min(category?.maxDays ?? Infinity, globalMax)`, validar `days <= effectiveMaxDays` y `days >= (category?.minDays ?? 1)`.
- Para EVENT con crédito: NO validar (días los calcula el sistema).

**Pattern referencia — seed con categorías existentes (decisión D-18):**
- No tocar seed; categorías existentes mantendrán icon=null y color=null hasta que el admin las configure vía dashboard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender CreateCategoryDto y UpdateCategoryDto con icon, color, minDays, maxDays, order</name>
  <files>
    apps/api/src/catalog/dto/create-category.dto.ts,
    apps/api/src/catalog/dto/update-category.dto.ts
  </files>
  <read_first>
    apps/api/src/catalog/dto/create-category.dto.ts,
    apps/api/src/catalog/dto/update-category.dto.ts,
    apps/api/prisma/schema.prisma
  </read_first>
  <action>
    EXTENDER ambos DTOs con los 5 campos v2 del schema. NO eliminar campos existentes.

    **`apps/api/src/catalog/dto/create-category.dto.ts` — AGREGAR campos:**

    Mantener los 4 existentes (name, slug, description, pricePerDay) y agregar:
    ```typescript
    @ApiPropertyOptional({ example: 'calendar', description: 'Nombre de icono Lucide (ej. "calendar", "music")' })
    @IsOptional()
    @IsString()
    icon?: string;

    @ApiPropertyOptional({ example: '#FF6B00', description: 'Color en hex o clase CSS' })
    @IsOptional()
    @IsString()
    color?: string;

    @ApiPropertyOptional({ example: 1, description: 'Mínimo de días al publicar un evento de esta categoría', default: 1 })
    @IsOptional()
    @IsInt()
    @Min(1)
    minDays?: number;

    @ApiPropertyOptional({ example: 30, description: 'Máximo de días al publicar un evento de esta categoría', default: 30 })
    @IsOptional()
    @IsInt()
    @Min(1)
    maxDays?: number;

    @ApiPropertyOptional({ example: 0, description: 'Orden manual en listados (0 = primero)', default: 0 })
    @IsOptional()
    @IsInt()
    @Min(0)
    order?: number;
    ```

    `IsInt`, `IsOptional`, `IsString`, `Min` ya importados de class-validator. `ApiPropertyOptional` ya importado de @nestjs/swagger.

    **`apps/api/src/catalog/dto/update-category.dto.ts` — AGREGAR los mismos 5 campos:**

    Idénticos al DTO de create, todos opcionales (ya lo son por defecto en update).

    **NOTA:** El service `CatalogService.createCategory` hace `data: dto` (spread). Una vez los campos están en el DTO, Prisma los persiste automáticamente. NO requiere tocar service.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "create-category|update-category" | head -5; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "icon?" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `grep -q "color?" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `grep -q "minDays?" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `grep -q "maxDays?" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `grep -q "order?" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `grep -q "icon?" apps/api/src/catalog/dto/update-category.dto.ts` returns 0
    - `grep -q "color?" apps/api/src/catalog/dto/update-category.dto.ts` returns 0
    - `grep -q "minDays?" apps/api/src/catalog/dto/update-category.dto.ts` returns 0
    - `grep -q "maxDays?" apps/api/src/catalog/dto/update-category.dto.ts` returns 0
    - `grep -q "order?" apps/api/src/catalog/dto/update-category.dto.ts` returns 0
    - `grep -qE "@Min\\(1\\).*\\n.*minDays|@Min\\(1\\).*\\n.*maxDays" apps/api/src/catalog/dto/create-category.dto.ts || grep -q "@Min(1)" apps/api/src/catalog/dto/create-category.dto.ts` returns 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Ambos DTOs aceptan los 5 campos v2 con validaciones; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor OrdersService.addItem para usar category.minDays/maxDays en EVENT (sin crédito)</name>
  <files>
    apps/api/src/orders/orders.service.ts
  </files>
  <read_first>
    apps/api/src/orders/orders.service.ts,
    apps/api/prisma/schema.prisma
  </read_first>
  <action>
    MODIFICAR `OrdersService.addItem` para integrar `category.minDays` y `category.maxDays` en la validación de días para EVENT. NO romper la lógica de crédito (D-17 explícito: "días los calcula el sistema, no se validan vs user input"). NO afectar SPOT/HERO/ARTICLE.

    **Approach:** Cargar el evento con categoría temprano si type=EVENT (para acceder a min/maxDays). Aplicar validación condicional.

    **Modificación específica en `addItem` (apps/api/src/orders/orders.service.ts líneas 83-145):**

    Después de la línea ~91 (`const hasCredit = ...`) y ANTES de la línea ~94 (`const needsDays = ...`), agregar:

    ```typescript
    // CNT-04 D-17: para EVENT sin crédito, cargar category para usar category.minDays/maxDays
    let categoryMinDays: number | null = null;
    let categoryMaxDays: number | null = null;
    if (dto.type === OrderItemType.EVENT && !hasCredit && dto.eventId) {
      const eventForCap = await this.prisma.event.findUnique({
        where: { id: dto.eventId },
        select: { category: { select: { minDays: true, maxDays: true } } },
      });
      categoryMinDays = eventForCap?.category?.minDays ?? null;
      categoryMaxDays = eventForCap?.category?.maxDays ?? null;
    }
    ```

    Luego MODIFICAR la validación de días (líneas ~98-105). Reemplazar:
    ```typescript
    if (needsDays && (dto.days === undefined || dto.days < 1)) {
      throw new BadRequestException(`days es requerido y debe ser >= 1 para ítems de tipo ${dto.type}`);
    }

    const maxDays = await this.maxDays(dto.type);
    if (maxDays > 0 && dto.days !== undefined && dto.days > maxDays) {
      throw new BadRequestException(`Máximo ${maxDays} días para ${dto.type.toLowerCase()}`);
    }
    ```

    Por:
    ```typescript
    if (needsDays && (dto.days === undefined || dto.days < 1)) {
      throw new BadRequestException(`days es requerido y debe ser >= 1 para ítems de tipo ${dto.type}`);
    }

    // CNT-04 D-17: para EVENT sin crédito, cap = min(category.maxDays, EVENT_MAX_DAYS global)
    // Para SPOT/HERO el global aplica solo (no hay category cap por ahora)
    const globalMax = await this.maxDays(dto.type);
    let effectiveMax = globalMax;
    if (dto.type === OrderItemType.EVENT && !hasCredit && categoryMaxDays !== null) {
      effectiveMax = Math.min(globalMax, categoryMaxDays);
    }
    if (effectiveMax > 0 && dto.days !== undefined && dto.days > effectiveMax) {
      throw new BadRequestException(`Máximo ${effectiveMax} días para ${dto.type.toLowerCase()}`);
    }

    // CNT-04 D-17: minDays solo aplica a EVENT sin crédito
    if (dto.type === OrderItemType.EVENT && !hasCredit && categoryMinDays !== null && categoryMinDays > 1) {
      if (dto.days !== undefined && dto.days < categoryMinDays) {
        throw new BadRequestException(`Mínimo ${categoryMinDays} días para esta categoría de evento`);
      }
    }
    ```

    **CRÍTICO — NO tocar lo siguiente:**
    - `assertSpotQuota` y `assertHeroQuota` (líneas 108-109)
    - `resolveItem` lógica (líneas 165-255) — el cap pricePerDay viene de category.pricePerDay ahí, sin cambios
    - EVENT con crédito skipea esta validación (la condición `!hasCredit` garantiza eso)
    - ARTICLE sigue con días=0 fijo

    **JUSTIFICACIÓN del approach:**
    - 1 query extra solo cuando type=EVENT && !hasCredit (no degrada performance en otros tipos)
    - No requiere refactor de `maxDays(type)` helper (sigue siendo type-only, sin contexto)
    - La query es lightweight (select solo category.minDays/maxDays)
    - Para EVENT con crédito, los días los calcula `addItem` con Math.min(45, daysUntilCycleEnd, daysUntilEventExpiration) — el cap por categoría NO debería aplicarse (D-17 implícito: los créditos son uniformes, no per-category)
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "orders\.service" | head -5; test $? -ne 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "categoryMinDays" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -q "categoryMaxDays" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -q "category.maxDays" apps/api/src/orders/orders.service.ts || grep -q "maxDays: true" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -q "category.minDays" apps/api/src/orders/orders.service.ts || grep -q "minDays: true" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -q "effectiveMax" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -q "Math.min(globalMax, categoryMaxDays)" apps/api/src/orders/orders.service.ts` returns 0
    - `grep -qE "Mínimo.*días|Mínimo \\${categoryMinDays}" apps/api/src/orders/orders.service.ts` returns 0
    - `cd apps/api && pnpm tsc --noEmit` exits 0
    - `cd apps/api && pnpm build 2>&1 | tail -5` exits 0
  </acceptance_criteria>
  <done>OrdersService.addItem usa Math.min(globalMax, category.maxDays) como cap para EVENT sin crédito; valida minDays; SPOT/HERO/ARTICLE/EVENT-con-crédito intactos; build pasa.</done>
</task>

</tasks>

<verification>
- Build: `cd apps/api && pnpm build` exits 0
- TypeScript: `cd apps/api && pnpm tsc --noEmit` exits 0
- Test POST create category con campos v2: `curl -X POST http://localhost:3001/categories -H "Authorization: Bearer <admin>" -H "Content-Type: application/json" -d '{"name":"Test","slug":"test-cat","icon":"music","color":"#FF6B00","minDays":2,"maxDays":15,"order":1}'` → 201 con campos persistidos
- Test PATCH update con campos v2: `curl -X PATCH http://localhost:3001/categories/1 -H "Authorization: Bearer <admin>" -H "Content-Type: application/json" -d '{"maxDays":7}'` → 200
- Test cap por categoría: con categoría maxDays=7, agregar EVENT de esa categoría con days=10 → 400 "Máximo 7 días"
- Test minDays: con categoría minDays=3, agregar EVENT con days=2 → 400 "Mínimo 3 días"
- Test EVENT con crédito ignora cap categoría: usuario suscrito agrega EVENT (sin days) → calcula días con Math.min(45,...), no valida vs maxDays user input
</verification>

<success_criteria>
- DTOs de Category v2 aceptan los 5 campos nuevos con validación class-validator
- Crear/editar categoría persiste los campos en DB (sin tocar service)
- GET /categories devuelve los campos (sin cambios — Prisma findMany ya retorna columnas v2)
- OrdersService.addItem para EVENT respeta category.maxDays y category.minDays
- EVENT con crédito sigue sin validación de días (intacto)
- Build + tsc pasan
</success_criteria>

<output>
After completion, create `.planning/phases/13-contenido-avanzado/13-04-SUMMARY.md`
</output>
