---
phase: 14-servicios-y-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/services.module.ts
  - apps/api/src/services/services.service.ts
  - apps/api/src/services/services.controller.ts
  - apps/api/src/services/dto/create-service-request.dto.ts
  - apps/api/src/services/dto/create-service-option.dto.ts
  - apps/api/src/services/dto/update-service-option.dto.ts
  - apps/api/src/services/dto/query-service-requests.dto.ts
  - apps/api/src/app.module.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SVC-01, SVC-02]
must_haves:
  truths:
    - "POST /services/photography crea ServiceRequest type=PHOTOGRAPHY (público, sin auth)"
    - "POST /services/content-creators crea ServiceRequest type=CONTENT (público, sin auth)"
    - "GET /services/photography/options devuelve solo opciones active=true ordenadas por order (público)"
    - "GET /services/content-creators/options devuelve solo opciones active=true ordenadas por order (público)"
    - "GET /services/photography y GET /services/content-creators listan solicitudes paginadas (ADMIN+)"
    - "CRUD admin de options funcional (POST/PATCH/DELETE para PHOTOGRAPHY y CONTENT)"
    - "DELETE de option con requests vinculados marca active=false (no borra físicamente)"
    - "REQUIREMENTS.md contiene la sección Servicios y CRM con SVC-01..05"
  artifacts:
    - path: "apps/api/src/services/services.module.ts"
      provides: "NestJS module ServicesModule"
      contains: "ServicesModule"
    - path: "apps/api/src/services/services.service.ts"
      provides: "ServicesService con createRequest(), getOptions(), CRUD options"
      contains: "ServicesService"
    - path: "apps/api/src/services/services.controller.ts"
      provides: "Controller con 10 endpoints (2 POST públicos, 2 GET options públicos, 2 GET admin requests, 6 CRUD options admin)"
      contains: "ServicesController"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Documentación de SVC-01..05 (D-24)"
      contains: "### Servicios y CRM (Phase 14)"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "ServicesModule"
      via: "import + entry en imports[]"
      pattern: "ServicesModule"
    - from: "apps/api/src/services/services.controller.ts"
      to: "ServicesService"
      via: "constructor injection"
      pattern: "private readonly services: ServicesService"
---

<objective>
Crear el módulo `services` que cubre SVC-01 (endpoints públicos `POST /services/photography` y `POST /services/content-creators`, además de `GET /services/*/options` públicos) y SVC-02 (CRUD admin de opciones, GET admin de solicitudes). También documenta los 5 requirements SVC-01..05 en REQUIREMENTS.md (D-24).

Purpose: Los formularios públicos de cotización son la puerta de entrada del pipeline CRM. La integración con CRM se hace en plan 14-04. Documentar SVC-01..05 en REQUIREMENTS.md es prerrequisito de cobertura.
Output: Módulo `services` registrado en AppModule + REQUIREMENTS.md actualizado con sección "Servicios y CRM (Phase 14)".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-servicios-y-crm/14-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/contact/contact.module.ts
@apps/api/src/contact/contact.controller.ts
@apps/api/src/app.module.ts
@apps/api/src/auth/jwt-auth.guard.ts
@apps/api/src/auth/roles.guard.ts
@apps/api/src/auth/roles.decorator.ts

<interfaces>
<!-- Modelos Prisma (ya existen en schema desde Phase 8) -->

enum ServiceType { PHOTOGRAPHY | CONTENT }
enum CrmStage { NEW | CONTACTED | NEGOTIATING | WON | LOST }

model ServiceOption {
  id: Int @id @default(autoincrement())
  type: ServiceType
  label: String
  active: Boolean @default(true)
  order: Int @default(0)
  requests: ServiceRequest[]   // many-to-many
}

model ServiceRequest {
  id: Int @id @default(autoincrement())
  type: ServiceType
  name: String
  email: String
  eventName: String?
  eventDate: DateTime? @db.Date
  eventPlace: String?
  options: ServiceOption[]     // many-to-many
}

<!-- Roles / Guards existentes (no inventar) -->
JwtAuthGuard          @ apps/api/src/auth/jwt-auth.guard.ts
RolesGuard            @ apps/api/src/auth/roles.guard.ts
@Roles('ADMIN', 'SUPER_ADMIN')  @ apps/api/src/auth/roles.decorator.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ServicesModule + Service + DTOs + endpoints públicos (SVC-01)</name>
  <files>
    apps/api/src/services/services.module.ts (NEW),
    apps/api/src/services/services.service.ts (NEW),
    apps/api/src/services/services.controller.ts (NEW),
    apps/api/src/services/dto/create-service-request.dto.ts (NEW),
    apps/api/src/app.module.ts (MODIFY)
  </files>
  <read_first>
    - apps/api/src/contact/contact.module.ts (patrón module simple)
    - apps/api/src/contact/contact.controller.ts (patrón controller con Roles)
    - apps/api/src/contact/contact.service.ts (patrón service con PrismaService)
    - apps/api/src/contact/dto/create-contact.dto.ts (patrón DTO con class-validator + Swagger)
    - apps/api/src/auth/jwt-auth.guard.ts (firma de JwtAuthGuard)
    - apps/api/src/auth/roles.guard.ts (firma de RolesGuard)
    - apps/api/src/auth/roles.decorator.ts (`@Roles('ADMIN','SUPER_ADMIN')`)
    - apps/api/src/app.module.ts (lista de imports actual)
    - apps/api/prisma/schema.prisma (modelos ServiceOption, ServiceRequest, ServiceType)
  </read_first>
  <action>
**Crear `apps/api/src/services/dto/create-service-request.dto.ts`** con class-validator + @nestjs/swagger:

```ts
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import { Type } from 'class-transformer';
import { IsArray, IsDateString, IsEmail, IsInt, IsOptional, IsString, MinLength } from 'class-validator';

export class CreateServiceRequestDto {
  @ApiProperty({ example: 'Juan Pérez' })
  @IsString()
  @MinLength(2)
  name: string;

  @ApiProperty({ example: 'juan@example.com' })
  @IsEmail()
  email: string;

  @ApiPropertyOptional({ example: 'Cumpleaños 30 años' })
  @IsOptional()
  @IsString()
  eventName?: string;

  @ApiPropertyOptional({ example: '2026-08-15' })
  @IsOptional()
  @IsDateString()
  eventDate?: string;

  @ApiPropertyOptional({ example: 'Salón Las Condes' })
  @IsOptional()
  @IsString()
  eventPlace?: string;

  @ApiPropertyOptional({ example: [1, 3], type: [Number] })
  @IsOptional()
  @IsArray()
  @IsInt({ each: true })
  @Type(() => Number)
  optionIds?: number[];
}
```

**Crear `apps/api/src/services/services.service.ts`** con métodos `createRequest(dto, type: ServiceType)` y `getActiveOptions(type: ServiceType)`. El resto (CRUD options, list requests admin) se agrega en Task 2.

```ts
import { Injectable } from '@nestjs/common';
import { ServiceType } from '@prisma/client';
import { PrismaService } from '../../utils/prisma/prisma.service';
import { CreateServiceRequestDto } from './dto/create-service-request.dto';

@Injectable()
export class ServicesService {
  constructor(private readonly prisma: PrismaService) {}

  // SVC-01 (D-01, D-02). Devuelve solo {id, type, name, email, createdAt}.
  async createRequest(dto: CreateServiceRequestDto, type: ServiceType) {
    const data: any = {
      type,
      name: dto.name,
      email: dto.email,
      eventName: dto.eventName ?? null,
      eventDate: dto.eventDate ? new Date(dto.eventDate) : null,
      eventPlace: dto.eventPlace ?? null,
    };
    if (dto.optionIds && dto.optionIds.length) {
      data.options = { connect: dto.optionIds.map((id) => ({ id })) };
    }
    const req = await this.prisma.serviceRequest.create({
      data,
      select: { id: true, type: true, name: true, email: true, createdAt: true },
    });
    return req;
  }

  // SVC-01 (D-03). Público — solo active=true ordenado por order asc.
  async getActiveOptions(type: ServiceType) {
    return this.prisma.serviceOption.findMany({
      where: { type, active: true },
      orderBy: { order: 'asc' },
      select: { id: true, label: true, order: true },
    });
  }
}
```

NOTA: La integración con CrmEntry (D-21, D-22) se hace en plan 14-04 (no en este plan). Aquí `createRequest` solo crea el ServiceRequest.

**Crear `apps/api/src/services/services.controller.ts`** con los 4 endpoints públicos:

```ts
import { Body, Controller, Get, Post } from '@nestjs/common';
import { ApiOperation, ApiTags } from '@nestjs/swagger';
import { ServiceType } from '@prisma/client';
import { ServicesService } from './services.service';
import { CreateServiceRequestDto } from './dto/create-service-request.dto';

@ApiTags('services')
@Controller('services')
export class ServicesController {
  constructor(private readonly services: ServicesService) {}

  @Post('photography')
  @ApiOperation({ summary: 'Solicitar cotización de fotografía (público)' })
  createPhotography(@Body() dto: CreateServiceRequestDto) {
    return this.services.createRequest(dto, ServiceType.PHOTOGRAPHY);
  }

  @Post('content-creators')
  @ApiOperation({ summary: 'Solicitar cotización de creadores de contenido (público)' })
  createContent(@Body() dto: CreateServiceRequestDto) {
    return this.services.createRequest(dto, ServiceType.CONTENT);
  }

  @Get('photography/options')
  @ApiOperation({ summary: 'Opciones activas de fotografía (público)' })
  listPhotographyOptions() {
    return this.services.getActiveOptions(ServiceType.PHOTOGRAPHY);
  }

  @Get('content-creators/options')
  @ApiOperation({ summary: 'Opciones activas de creadores (público)' })
  listContentOptions() {
    return this.services.getActiveOptions(ServiceType.CONTENT);
  }
}
```

**Crear `apps/api/src/services/services.module.ts`**:

```ts
import { Module } from '@nestjs/common';
import { ServicesController } from './services.controller';
import { ServicesService } from './services.service';
import { AuthModule } from '../auth/auth.module';

@Module({
  imports: [AuthModule],
  controllers: [ServicesController],
  providers: [ServicesService],
  exports: [ServicesService],
})
export class ServicesModule {}
```

NO importar CrmModule (D-19, D-23 — patrón de no acoplamiento cross-módulo).

**Modificar `apps/api/src/app.module.ts`**: agregar `ServicesModule`:
1. Añadir import (después de la línea `import { SubscriptionsModule } from './subscriptions/subscriptions.module';`): `import { ServicesModule } from './services/services.module';`
2. Añadir `ServicesModule,` al final del array `imports` (después de `SubscriptionsModule,`).

Anchor string para Edit: `SubscriptionsModule,` → reemplazar por `SubscriptionsModule,\n    ServicesModule,`. NO tocar otras entradas (plan 14-02 añade CrmModule en paralelo — usa anchor distinto).
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(services|error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/api/src/services/services.module.ts apps/api/src/services/services.service.ts apps/api/src/services/services.controller.ts apps/api/src/services/dto/create-service-request.dto.ts` lista los 4 archivos
    - `grep -c "ServicesModule" apps/api/src/app.module.ts` devuelve >=2 (import + entry)
    - `grep -E "@Post\\('photography'\\)|@Post\\('content-creators'\\)|@Get\\('photography/options'\\)|@Get\\('content-creators/options'\\)" apps/api/src/services/services.controller.ts` devuelve 4 líneas
    - `grep "ServiceType.PHOTOGRAPHY\|ServiceType.CONTENT" apps/api/src/services/services.controller.ts` devuelve 4 ocurrencias (2 POST + 2 GET)
    - `grep "CrmModule\|crmEntry" apps/api/src/services/services.service.ts` devuelve 0 (la integración CRM es plan 14-04)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos en `src/services/`
  </acceptance_criteria>
  <done>El módulo Services está registrado en NestJS y expone los 4 endpoints públicos. Los DTOs validan con class-validator. La integración CRM queda pendiente para plan 14-04.</done>
</task>

<task type="auto">
  <name>Task 2: CRUD admin de ServiceOption + GET admin de ServiceRequest (SVC-02)</name>
  <files>
    apps/api/src/services/services.service.ts (MODIFY — añadir métodos),
    apps/api/src/services/services.controller.ts (MODIFY — añadir endpoints),
    apps/api/src/services/dto/create-service-option.dto.ts (NEW),
    apps/api/src/services/dto/update-service-option.dto.ts (NEW),
    apps/api/src/services/dto/query-service-requests.dto.ts (NEW)
  </files>
  <read_first>
    - apps/api/src/services/services.service.ts (creado en Task 1)
    - apps/api/src/services/services.controller.ts (creado en Task 1)
    - apps/api/src/notifications/notifications.controller.ts (patrón paginación + Roles)
    - apps/api/src/notifications/dto/query-notifications.dto.ts (patrón query DTO con page/limit)
    - apps/api/prisma/schema.prisma (modelos ServiceOption, ServiceRequest, índices)
  </read_first>
  <action>
**Crear `apps/api/src/services/dto/create-service-option.dto.ts`**:

```ts
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import { IsBoolean, IsInt, IsOptional, IsString, MinLength } from 'class-validator';

export class CreateServiceOptionDto {
  @ApiProperty({ example: 'Fotografía de eventos' })
  @IsString()
  @MinLength(2)
  label: string;

  @ApiPropertyOptional({ default: true })
  @IsOptional()
  @IsBoolean()
  active?: boolean;

  @ApiPropertyOptional({ default: 0 })
  @IsOptional()
  @IsInt()
  order?: number;
}
```

**Crear `apps/api/src/services/dto/update-service-option.dto.ts`** — todos los campos opcionales (sin @nestjs/mapped-types, patrón del proyecto):

```ts
import { ApiPropertyOptional } from '@nestjs/swagger';
import { IsBoolean, IsInt, IsOptional, IsString, MinLength } from 'class-validator';

export class UpdateServiceOptionDto {
  @ApiPropertyOptional()
  @IsOptional()
  @IsString()
  @MinLength(2)
  label?: string;

  @ApiPropertyOptional()
  @IsOptional()
  @IsBoolean()
  active?: boolean;

  @ApiPropertyOptional()
  @IsOptional()
  @IsInt()
  order?: number;
}
```

D-07: El campo `type` NO se incluye en UpdateServiceOptionDto — no se puede cambiar post-creación.

**Crear `apps/api/src/services/dto/query-service-requests.dto.ts`** con paginación estándar:

```ts
import { ApiPropertyOptional } from '@nestjs/swagger';
import { Type } from 'class-transformer';
import { IsInt, IsOptional, Max, Min } from 'class-validator';

export class QueryServiceRequestsDto {
  @ApiPropertyOptional({ default: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @ApiPropertyOptional({ default: 20, maximum: 50 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(50)
  limit?: number = 20;
}
```

**Modificar `apps/api/src/services/services.service.ts`** — añadir métodos al final de la clase (NO tocar `createRequest` ni `getActiveOptions`):

```ts
// SVC-02 (D-04): GET admin paginado con options incluidas.
async listRequests(type: ServiceType, query: QueryServiceRequestsDto) {
  const page = query.page ?? 1;
  const limit = query.limit ?? 20;
  const [items, total] = await this.prisma.$transaction([
    this.prisma.serviceRequest.findMany({
      where: { type },
      include: { options: { select: { id: true, label: true } } },
      orderBy: { createdAt: 'desc' },
      skip: (page - 1) * limit,
      take: limit,
    }),
    this.prisma.serviceRequest.count({ where: { type } }),
  ]);
  return { items, total, page, pageSize: limit, totalPages: Math.ceil(total / limit) };
}

// SVC-02 (D-06): CREATE option.
async createOption(dto: CreateServiceOptionDto, type: ServiceType) {
  return this.prisma.serviceOption.create({
    data: {
      type,
      label: dto.label,
      active: dto.active ?? true,
      order: dto.order ?? 0,
    },
  });
}

// SVC-02 (D-07): UPDATE option (no permite cambiar type).
async updateOption(id: number, dto: UpdateServiceOptionDto) {
  const existing = await this.prisma.serviceOption.findUnique({ where: { id } });
  if (!existing) throw new NotFoundException('Opción no encontrada');
  return this.prisma.serviceOption.update({
    where: { id },
    data: {
      ...(dto.label !== undefined && { label: dto.label }),
      ...(dto.active !== undefined && { active: dto.active }),
      ...(dto.order !== undefined && { order: dto.order }),
    },
  });
}

// SVC-02 (D-08): DELETE option — soft-delete si tiene requests vinculados.
async deleteOption(id: number) {
  const existing = await this.prisma.serviceOption.findUnique({
    where: { id },
    include: { _count: { select: { requests: true } } },
  });
  if (!existing) throw new NotFoundException('Opción no encontrada');
  if (existing._count.requests > 0) {
    await this.prisma.serviceOption.update({ where: { id }, data: { active: false } });
    return { softDeleted: true, requestsCount: existing._count.requests };
  }
  await this.prisma.serviceOption.delete({ where: { id } });
  return { deleted: true };
}
```

Añadir imports en services.service.ts: `import { NotFoundException } from '@nestjs/common';`, `import { CreateServiceOptionDto } from './dto/create-service-option.dto';`, `import { UpdateServiceOptionDto } from './dto/update-service-option.dto';`, `import { QueryServiceRequestsDto } from './dto/query-service-requests.dto';`.

**Modificar `apps/api/src/services/services.controller.ts`** — añadir 8 endpoints admin (GET requests x2, CRUD options x2 tipos x3 = 6):

```ts
// Imports a añadir en el controller:
import { Delete, Param, ParseIntPipe, Patch, Query, UseGuards } from '@nestjs/common';
import { ApiBearerAuth } from '@nestjs/swagger';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { RolesGuard } from '../auth/roles.guard';
import { Roles } from '../auth/roles.decorator';
import { CreateServiceOptionDto } from './dto/create-service-option.dto';
import { UpdateServiceOptionDto } from './dto/update-service-option.dto';
import { QueryServiceRequestsDto } from './dto/query-service-requests.dto';

// Endpoints a añadir DESPUÉS de los 4 públicos existentes:

@Get('photography')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Listar solicitudes de fotografía (ADMIN+)' })
listPhotographyRequests(@Query() query: QueryServiceRequestsDto) {
  return this.services.listRequests(ServiceType.PHOTOGRAPHY, query);
}

@Get('content-creators')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Listar solicitudes de creadores (ADMIN+)' })
listContentRequests(@Query() query: QueryServiceRequestsDto) {
  return this.services.listRequests(ServiceType.CONTENT, query);
}

@Post('photography/options')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Crear opción de fotografía (ADMIN+)' })
createPhotographyOption(@Body() dto: CreateServiceOptionDto) {
  return this.services.createOption(dto, ServiceType.PHOTOGRAPHY);
}

@Patch('photography/options/:id')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Actualizar opción de fotografía (ADMIN+)' })
updatePhotographyOption(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateServiceOptionDto) {
  return this.services.updateOption(id, dto);
}

@Delete('photography/options/:id')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Eliminar opción de fotografía (soft-delete si tiene requests) (ADMIN+)' })
deletePhotographyOption(@Param('id', ParseIntPipe) id: number) {
  return this.services.deleteOption(id);
}

@Post('content-creators/options')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Crear opción de creadores (ADMIN+)' })
createContentOption(@Body() dto: CreateServiceOptionDto) {
  return this.services.createOption(dto, ServiceType.CONTENT);
}

@Patch('content-creators/options/:id')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Actualizar opción de creadores (ADMIN+)' })
updateContentOption(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateServiceOptionDto) {
  return this.services.updateOption(id, dto);
}

@Delete('content-creators/options/:id')
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@ApiBearerAuth()
@ApiOperation({ summary: 'Eliminar opción de creadores (soft-delete si tiene requests) (ADMIN+)' })
deleteContentOption(@Param('id', ParseIntPipe) id: number) {
  return this.services.deleteOption(id);
}
```

D-05: Un único `ServicesController` con rutas planas — NO crear sub-routers.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(services|error TS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Post\|@Get\|@Patch\|@Delete" apps/api/src/services/services.controller.ts` devuelve 12 (4 públicos + 2 GET admin + 3 photo options + 3 content options = 12)
    - `grep -c "@Roles('ADMIN', 'SUPER_ADMIN')" apps/api/src/services/services.controller.ts` devuelve 8 (todos los endpoints admin)
    - `grep "softDeleted: true" apps/api/src/services/services.service.ts` devuelve 1 (D-08)
    - `grep "_count: { select: { requests: true } }" apps/api/src/services/services.service.ts` devuelve 1
    - `grep "page, pageSize: limit, totalPages" apps/api/src/services/services.service.ts` devuelve 1 (formato paginación estándar)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos en `src/services/`
  </acceptance_criteria>
  <done>ServicesController expone los 12 endpoints (4 públicos + 8 admin). Soft-delete funcional cuando hay requests vinculados. Paginación estándar del proyecto (items/total/page/pageSize/totalPages).</done>
</task>

<task type="auto">
  <name>Task 3: Documentar SVC-01..05 en REQUIREMENTS.md (D-24)</name>
  <files>.planning/REQUIREMENTS.md (MODIFY)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (sección actual termina en COM-04)
    - .planning/phases/14-servicios-y-crm/14-CONTEXT.md (texto exacto a documentar)
    - .planning/ROADMAP.md líneas 477-490 (resumen de SVC-01..05)
  </read_first>
  <action>
Añadir al final de `.planning/REQUIREMENTS.md` (después de la sección "### Comercio: Suscripciones y Carrito v2 (Phase 12)") una nueva sección con SVC-01..05.

Insertar ANTES de la línea `---` final (`*Requirements defined...`). Anchor para Edit: `### Comercio: Suscripciones y Carrito v2 (Phase 12)` — añadir DESPUÉS del bloque completo COM-01..04.

Texto a añadir:

```markdown
### Servicios y CRM (Phase 14)

- [ ] **SVC-01**: Endpoints públicos `POST /services/photography` y
  `POST /services/content-creators` crean `ServiceRequest` con su `type` correspondiente
  (PHOTOGRAPHY o CONTENT). Body con `name`, `email`, `eventName?`, `eventDate?`,
  `eventPlace?`, `optionIds?: number[]` (many-to-many vía `options: { connect }`).
  Respuesta `201 { id, type, name, email, createdAt }` (sin exponer datos sensibles).
  `GET /services/photography/options` y `GET /services/content-creators/options`
  devuelven solo opciones `active=true` ordenadas por `order` asc — **públicos sin auth**.
- [ ] **SVC-02**: CRUD admin (`POST/PATCH/DELETE /services/photography/options` y
  `/services/content-creators/options`) sobre `ServiceOption`. `PATCH` no permite
  cambiar `type` post-creación. `DELETE` con `requests` vinculados marca `active=false`
  (soft-delete) en lugar de borrar físicamente. `GET /services/photography` y
  `GET /services/content-creators` (ADMIN+) listan solicitudes paginadas con las
  opciones incluidas. Todos los endpoints de options + listados admin usan
  `JwtAuthGuard + RolesGuard + @Roles('ADMIN','SUPER_ADMIN')`.
- [ ] **SVC-03**: Módulo `crm` con pipeline unificado (CONTACT, PHOTOGRAPHY, CONTENT):
  `GET /crm` (paginado, filtros `?type=`, `?stage=`, `?assignedTo=`),
  `GET /crm/:id` (devuelve la entrada con notas y datos del source via
  `sourceType + sourceId`), `PATCH /crm/:id/stage` (body `{ stage, stageReason? }` —
  si `stage === 'LOST'`, `stageReason` es **requerido** o 400),
  `POST /crm/:id/notes` y `GET /crm/:id/notes` (notas internas del equipo).
  No existe `DELETE /crm/:id` — las entradas se conservan (mover a LOST). Todos los
  endpoints ADMIN+.
- [ ] **SVC-04**: `ContactService.create()` extendido para crear `ContactMessage` +
  `CrmEntry` (type=CONTACT, sourceType=CONTACT, sourceId=contactMsg.id, stage=NEW)
  en `prisma.$transaction` (callback form). El response del endpoint `POST /contact`
  NO cambia (sigue devolviendo solo el `ContactMessage`) — backwards compatible. No
  se importa `CrmModule` en `ContactModule` (se usa `PrismaService` directamente,
  patrón D-19).
- [ ] **SVC-05**: `ServicesService.createRequest()` extendido para crear
  `ServiceRequest` + `CrmEntry` (type según `crmTypeMap[ServiceType]`,
  sourceType=type, sourceId=serviceReq.id, stage=NEW) en `prisma.$transaction`
  (callback form — `connect` de options many-to-many requiere callback, D-22). No
  se importa `CrmModule` en `ServicesModule` (D-23).
```

**IMPORTANTE:**
- El texto va INMEDIATAMENTE DESPUÉS del último checkbox del bloque COM-04 (línea ~228) y ANTES del separador `---` y la línea `*Requirements defined...`.
- Mantener checkboxes como `[ ]` (no marcadas aún — se marcan al completar cada plan).
- No tocar la sección "## Traceability" ni añadir entradas SVC-* allí (esa sección no se actualizó para CFG/COM/CNT tampoco — no es bloqueante).
  </action>
  <verify>
    <automated>grep -c "SVC-0[1-5]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Servicios y CRM (Phase 14)" .planning/REQUIREMENTS.md` devuelve 1
    - `grep -c "\\*\\*SVC-01\\*\\*\\|\\*\\*SVC-02\\*\\*\\|\\*\\*SVC-03\\*\\*\\|\\*\\*SVC-04\\*\\*\\|\\*\\*SVC-05\\*\\*" .planning/REQUIREMENTS.md` devuelve 5
    - `grep "stageReason.*requerido" .planning/REQUIREMENTS.md` devuelve >=1 (SVC-03)
    - `grep "soft-delete\|active=false" .planning/REQUIREMENTS.md` devuelve >=1 (SVC-02)
    - `grep "prisma.\\$transaction" .planning/REQUIREMENTS.md` devuelve >=2 (SVC-04 y SVC-05)
  </acceptance_criteria>
  <done>REQUIREMENTS.md contiene la sección "Servicios y CRM (Phase 14)" con los 5 checkboxes SVC-01..05 sin marcar, siguiendo el patrón de las secciones existentes.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` pasa sin errores nuevos
- POST /services/photography y /services/content-creators responden 201 con body válido (curl manual opcional)
- GET /services/*/options responde 200 sin auth
- GET /services/* (admin) responde 401 sin token
- REQUIREMENTS.md tiene los 5 requirements SVC-01..05 documentados
</verification>

<success_criteria>
- ServicesModule registrado en AppModule, sin importar CrmModule
- 12 endpoints expuestos (4 públicos + 8 admin)
- DTOs validan con class-validator
- Soft-delete funcional en ServiceOption
- REQUIREMENTS.md actualizado con SVC-01..05
</success_criteria>

<output>
After completion, create `.planning/phases/14-servicios-y-crm/14-01-SUMMARY.md`
</output>
