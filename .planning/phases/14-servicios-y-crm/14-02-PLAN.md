---
phase: 14-servicios-y-crm
plan: 02
type: execute
wave: 2
depends_on:
  - 14-servicios-y-crm/14-01
files_modified:
  - apps/api/src/crm/crm.module.ts
  - apps/api/src/crm/crm.service.ts
  - apps/api/src/crm/crm.controller.ts
  - apps/api/src/crm/dto/query-crm.dto.ts
  - apps/api/src/crm/dto/update-crm-stage.dto.ts
  - apps/api/src/crm/dto/create-crm-note.dto.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements: [SVC-03]
must_haves:
  truths:
    - "GET /crm devuelve lista paginada con filtros opcionales type/stage/assignedTo (ADMIN+)"
    - "GET /crm/:id devuelve la entrada con notas y datos del source (polymorphic via sourceType+sourceId)"
    - "PATCH /crm/:id/stage actualiza stage; si stage=LOST sin stageReason responde 400"
    - "POST /crm/:id/notes crea CrmNote con authorId=actor.sub"
    - "GET /crm/:id/notes devuelve notas ordenadas por createdAt asc"
    - "No existe endpoint DELETE /crm/:id (D-16)"
  artifacts:
    - path: "apps/api/src/crm/crm.module.ts"
      provides: "NestJS module CrmModule"
      contains: "CrmModule"
    - path: "apps/api/src/crm/crm.service.ts"
      provides: "CrmService con list, findOne, updateStage, addNote, listNotes"
      contains: "CrmService"
    - path: "apps/api/src/crm/crm.controller.ts"
      provides: "CrmController con 5 endpoints ADMIN+"
      contains: "CrmController"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "CrmModule"
      via: "import + entry en imports[]"
      pattern: "CrmModule"
    - from: "apps/api/src/crm/crm.service.ts"
      to: "prisma.crmEntry/crmNote/contactMessage/serviceRequest"
      via: "PrismaService direct access"
      pattern: "prisma\\.(crmEntry|crmNote|contactMessage|serviceRequest)"
---

<objective>
Crear el módulo `crm` con el pipeline unificado (CONTACT, PHOTOGRAPHY, CONTENT) — cubre SVC-03. Expone 5 endpoints ADMIN+: list paginado, detalle con source resuelto polymorphicamente, update stage con validación LOST, y CRUD de notas.

Purpose: Centralizar la gestión comercial de leads de Contact + Services en una vista única tipo kanban. Sin endpoint DELETE — las entradas se conservan (mover a LOST).
Output: Módulo `crm` registrado en AppModule, listo para que ContactService (plan 14-03) y ServicesService (plan 14-04) escriban `CrmEntry` en sus transacciones.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-servicios-y-crm/14-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/app.module.ts
@apps/api/src/auth/jwt-auth.guard.ts
@apps/api/src/auth/roles.guard.ts
@apps/api/src/auth/roles.decorator.ts
@apps/api/src/auth/current-user.decorator.ts

<interfaces>
<!-- Modelos Prisma (ya existen en schema desde Phase 8) -->

enum CrmType { CONTACT | PHOTOGRAPHY | CONTENT }
enum CrmStage { NEW | CONTACTED | NEGOTIATING | WON | LOST }

model CrmEntry {
  id: Int @id @default(autoincrement())
  type: CrmType
  stage: CrmStage @default(NEW)
  stageReason: String?
  sourceType: CrmType
  sourceId: Int
  contactName: String
  contactEmail: String
  assignedTo: Int?
  notes: CrmNote[]
  createdAt: DateTime
  updatedAt: DateTime
}

model CrmNote {
  id: Int @id @default(autoincrement())
  content: String @db.Text
  authorId: Int?            // userId del admin (null si eliminado)
  crmEntryId: Int
  createdAt: DateTime
}

<!-- Decoradores y guards -->
JwtAuthGuard, RolesGuard, @Roles('ADMIN','SUPER_ADMIN'), @CurrentUser() user: JwtUser
type JwtUser = { sub: number; email: string; role: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear CrmModule + Service + DTOs (list/findOne/updateStage)</name>
  <files>
    apps/api/src/crm/crm.module.ts (NEW),
    apps/api/src/crm/crm.controller.ts (NEW — stub en Task 1, completo en Task 2),
    apps/api/src/crm/crm.service.ts (NEW),
    apps/api/src/crm/dto/query-crm.dto.ts (NEW),
    apps/api/src/crm/dto/update-crm-stage.dto.ts (NEW),
    apps/api/src/app.module.ts (MODIFY)
  </files>
  <read_first>
    - apps/api/src/contact/contact.module.ts (patrón module simple)
    - apps/api/src/notifications/notifications.service.ts (patrón service con paginación)
    - apps/api/src/notifications/dto/query-notifications.dto.ts (patrón query DTO)
    - apps/api/src/app.module.ts (estado actual de imports)
    - apps/api/prisma/schema.prisma líneas 749-844 (CrmType, CrmStage, CrmEntry, CrmNote, ServiceRequest, ContactMessage)
  </read_first>
  <action>
**Crear `apps/api/src/crm/dto/query-crm.dto.ts`** con paginación + filtros:

```ts
import { ApiPropertyOptional } from '@nestjs/swagger';
import { Type } from 'class-transformer';
import { IsEnum, IsInt, IsOptional, Max, Min } from 'class-validator';
import { CrmType, CrmStage } from '@prisma/client';

export class QueryCrmDto {
  @ApiPropertyOptional({ enum: CrmType })
  @IsOptional()
  @IsEnum(CrmType)
  type?: CrmType;

  @ApiPropertyOptional({ enum: CrmStage })
  @IsOptional()
  @IsEnum(CrmStage)
  stage?: CrmStage;

  @ApiPropertyOptional()
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  assignedTo?: number;

  @ApiPropertyOptional({ default: 1 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @ApiPropertyOptional({ default: 20, maximum: 50 })
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(50)
  limit?: number = 20;
}
```

**Crear `apps/api/src/crm/dto/update-crm-stage.dto.ts`** — validación cross-field se hace en el service (D-13):

```ts
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsString, MinLength } from 'class-validator';
import { CrmStage } from '@prisma/client';

export class UpdateCrmStageDto {
  @ApiProperty({ enum: CrmStage })
  @IsEnum(CrmStage)
  stage: CrmStage;

  @ApiPropertyOptional()
  @IsOptional()
  @IsString()
  @MinLength(3)
  stageReason?: string;
}
```

**Crear `apps/api/src/crm/crm.service.ts`** con métodos list/findOne/updateStage:

```ts
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { CrmStage, CrmType } from '@prisma/client';
import { PrismaService } from '../../utils/prisma/prisma.service';
import { QueryCrmDto } from './dto/query-crm.dto';
import { UpdateCrmStageDto } from './dto/update-crm-stage.dto';

@Injectable()
export class CrmService {
  constructor(private readonly prisma: PrismaService) {}

  // SVC-03 (D-11): GET /crm paginado + filtros, sin notas en el listado.
  async list(query: QueryCrmDto) {
    const page = query.page ?? 1;
    const limit = query.limit ?? 20;
    const where: any = {};
    if (query.type) where.type = query.type;
    if (query.stage) where.stage = query.stage;
    if (query.assignedTo !== undefined) where.assignedTo = query.assignedTo;

    const [items, total] = await this.prisma.$transaction([
      this.prisma.crmEntry.findMany({
        where,
        orderBy: { createdAt: 'desc' },
        skip: (page - 1) * limit,
        take: limit,
        select: {
          id: true,
          type: true,
          stage: true,
          stageReason: true,
          sourceType: true,
          sourceId: true,
          contactName: true,
          contactEmail: true,
          assignedTo: true,
          createdAt: true,
          updatedAt: true,
        },
      }),
      this.prisma.crmEntry.count({ where }),
    ]);
    return { items, total, page, pageSize: limit, totalPages: Math.ceil(total / limit) };
  }

  // SVC-03 (D-12): GET /crm/:id — entrada + notas + datos del source.
  async findOne(id: number) {
    const entry = await this.prisma.crmEntry.findUnique({
      where: { id },
      include: {
        notes: { orderBy: { createdAt: 'asc' } },
      },
    });
    if (!entry) throw new NotFoundException('Entrada CRM no encontrada');

    // Resolver source polymorphicamente
    let source: unknown = null;
    if (entry.sourceType === CrmType.CONTACT) {
      source = await this.prisma.contactMessage.findUnique({
        where: { id: entry.sourceId },
      });
    } else {
      // PHOTOGRAPHY | CONTENT → ServiceRequest
      source = await this.prisma.serviceRequest.findUnique({
        where: { id: entry.sourceId },
        include: { options: { select: { id: true, label: true } } },
      });
    }

    return { ...entry, source };
  }

  // SVC-03 (D-13): PATCH /crm/:id/stage — si stage=LOST, stageReason requerido.
  async updateStage(id: number, dto: UpdateCrmStageDto) {
    if (dto.stage === CrmStage.LOST && !dto.stageReason) {
      throw new BadRequestException('stageReason es requerido cuando stage=LOST');
    }
    const existing = await this.prisma.crmEntry.findUnique({ where: { id } });
    if (!existing) throw new NotFoundException('Entrada CRM no encontrada');
    return this.prisma.crmEntry.update({
      where: { id },
      data: {
        stage: dto.stage,
        stageReason: dto.stageReason ?? null,
      },
    });
  }
}
```

**Crear `apps/api/src/crm/crm.module.ts`**:

```ts
import { Module } from '@nestjs/common';
import { CrmController } from './crm.controller';
import { CrmService } from './crm.service';
import { AuthModule } from '../auth/auth.module';

@Module({
  imports: [AuthModule],
  controllers: [CrmController],
  providers: [CrmService],
})
export class CrmModule {}
```

NO exportar CrmService (D-19, D-23 — Contact/Services usan PrismaService directamente, no inyectan CrmService).

**Modificar `apps/api/src/app.module.ts`**: añadir `CrmModule`:
1. Añadir import (línea separada, después de los imports v2 existentes): `import { CrmModule } from './crm/crm.module';`
2. Añadir `CrmModule,` al final del array `imports`.

Anchor para Edit: usar `ServicesModule,` como referencia — este plan está en Wave 2 y Plan 14-01 (Wave 1) ya habrá añadido `ServicesModule,` antes de que este plan corra. Insertar `CrmModule,` después de `ServicesModule,`.

NOTA IMPORTANTE para Task 2: el controller se crea en Task 2 — `crm.module.ts` referencia `CrmController` que no existe aún. Crear `crm.controller.ts` como stub vacío al final de Task 1 si es necesario para que TypeScript compile, o agrupar Task 1+2 si el ejecutor lo prefiere (declarar stub al final de Task 1).

**STUB de controller para que TS compile en Task 1:** Crear `apps/api/src/crm/crm.controller.ts` con un cascarón vacío:

```ts
import { Controller, UseGuards } from '@nestjs/common';
import { ApiBearerAuth, ApiTags } from '@nestjs/swagger';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { RolesGuard } from '../auth/roles.guard';
import { Roles } from '../auth/roles.decorator';
import { CrmService } from './crm.service';

@ApiTags('crm')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles('ADMIN', 'SUPER_ADMIN')
@Controller('crm')
export class CrmController {
  constructor(private readonly crm: CrmService) {}
  // Endpoints se añaden en Task 2
}
```

Task 2 reemplazará el comentario por los 5 endpoints reales.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(crm|error TS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/api/src/crm/crm.module.ts apps/api/src/crm/crm.service.ts apps/api/src/crm/crm.controller.ts apps/api/src/crm/dto/query-crm.dto.ts apps/api/src/crm/dto/update-crm-stage.dto.ts` lista los 5 archivos
    - `grep -c "CrmModule" apps/api/src/app.module.ts` devuelve >=2 (import + entry)
    - `grep "CrmService" apps/api/src/crm/crm.module.ts | grep -v "exports"` devuelve líneas con CrmService (no en exports)
    - `grep "exports" apps/api/src/crm/crm.module.ts` devuelve 0 lines (NO se exporta CrmService)
    - `grep "stageReason es requerido cuando stage=LOST" apps/api/src/crm/crm.service.ts` devuelve 1
    - `grep "this.prisma.contactMessage.findUnique\|this.prisma.serviceRequest.findUnique" apps/api/src/crm/crm.service.ts` devuelve 2 (polymorphic source resolution)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos
  </acceptance_criteria>
  <done>CrmModule registrado en AppModule, CrmService con list/findOne/updateStage funcional, CrmController stub presente para que TS compile. Sin export de CrmService.</done>
</task>

<task type="auto">
  <name>Task 2: Notas CRM (addNote, listNotes) + endpoints completos del controller</name>
  <files>
    apps/api/src/crm/crm.service.ts (MODIFY — añadir métodos addNote, listNotes),
    apps/api/src/crm/crm.controller.ts (MODIFY — añadir 5 endpoints),
    apps/api/src/crm/dto/create-crm-note.dto.ts (NEW)
  </files>
  <read_first>
    - apps/api/src/crm/crm.service.ts (creado en Task 1)
    - apps/api/src/crm/crm.controller.ts (stub creado en Task 1)
    - apps/api/src/auth/current-user.decorator.ts (JwtUser type)
    - apps/api/src/notifications/notifications.controller.ts (patrón controller con @CurrentUser)
  </read_first>
  <action>
**Crear `apps/api/src/crm/dto/create-crm-note.dto.ts`**:

```ts
import { ApiProperty } from '@nestjs/swagger';
import { IsString, MinLength } from 'class-validator';

export class CreateCrmNoteDto {
  @ApiProperty({ example: 'Llamé al cliente, está interesado en el paquete básico.' })
  @IsString()
  @MinLength(3)
  content: string;
}
```

**Modificar `apps/api/src/crm/crm.service.ts`** — añadir métodos al final de la clase:

```ts
// Imports adicionales:
import { CreateCrmNoteDto } from './dto/create-crm-note.dto';

// Métodos a añadir:

// SVC-03 (D-14): POST /crm/:id/notes — authorId = actor.sub.
async addNote(crmEntryId: number, dto: CreateCrmNoteDto, authorId: number) {
  const entry = await this.prisma.crmEntry.findUnique({ where: { id: crmEntryId } });
  if (!entry) throw new NotFoundException('Entrada CRM no encontrada');
  return this.prisma.crmNote.create({
    data: {
      content: dto.content,
      authorId,
      crmEntryId,
    },
  });
}

// SVC-03 (D-15): GET /crm/:id/notes — orden createdAt asc.
async listNotes(crmEntryId: number) {
  const entry = await this.prisma.crmEntry.findUnique({ where: { id: crmEntryId } });
  if (!entry) throw new NotFoundException('Entrada CRM no encontrada');
  return this.prisma.crmNote.findMany({
    where: { crmEntryId },
    orderBy: { createdAt: 'asc' },
  });
}
```

**Modificar `apps/api/src/crm/crm.controller.ts`** — reemplazar el comentario `// Endpoints se añaden en Task 2` por los 5 endpoints (D-11..D-15):

```ts
// Imports a añadir:
import { Body, Get, Param, ParseIntPipe, Patch, Post, Query } from '@nestjs/common';
import { ApiOperation } from '@nestjs/swagger';
import { CurrentUser, type JwtUser } from '../auth/current-user.decorator';
import { QueryCrmDto } from './dto/query-crm.dto';
import { UpdateCrmStageDto } from './dto/update-crm-stage.dto';
import { CreateCrmNoteDto } from './dto/create-crm-note.dto';

// Endpoints DENTRO de la clase CrmController:

@Get()
@ApiOperation({ summary: 'Listar entradas CRM con filtros opcionales (ADMIN+)' })
list(@Query() query: QueryCrmDto) {
  return this.crm.list(query);
}

@Get(':id')
@ApiOperation({ summary: 'Ver una entrada CRM con notas y source (ADMIN+)' })
findOne(@Param('id', ParseIntPipe) id: number) {
  return this.crm.findOne(id);
}

@Patch(':id/stage')
@ApiOperation({ summary: 'Actualizar stage del CRM (LOST requiere stageReason) (ADMIN+)' })
updateStage(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateCrmStageDto) {
  return this.crm.updateStage(id, dto);
}

@Post(':id/notes')
@ApiOperation({ summary: 'Añadir nota interna a una entrada CRM (ADMIN+)' })
addNote(
  @Param('id', ParseIntPipe) id: number,
  @Body() dto: CreateCrmNoteDto,
  @CurrentUser() user: JwtUser,
) {
  return this.crm.addNote(id, dto, user.sub);
}

@Get(':id/notes')
@ApiOperation({ summary: 'Listar notas de una entrada CRM (ADMIN+)' })
listNotes(@Param('id', ParseIntPipe) id: number) {
  return this.crm.listNotes(id);
}
```

D-16: NO añadir endpoint `@Delete(':id')` — las entradas CRM no se eliminan (conservar historial). El admin debe mover a LOST.

D-17: NO añadir endpoint de asignación (`PATCH /crm/:id/assigned-to`) — diferido a Phase 15.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(crm|error TS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "@Get\\(\\)|@Get\\(':id'\\)|@Patch\\(':id/stage'\\)|@Post\\(':id/notes'\\)|@Get\\(':id/notes'\\)" apps/api/src/crm/crm.controller.ts` devuelve exactamente 5 ocurrencias
    - `grep "@Delete" apps/api/src/crm/crm.controller.ts` devuelve 0 (D-16)
    - `grep "addNote\|listNotes" apps/api/src/crm/crm.service.ts` devuelve >=4 (definiciones + return de la primera)
    - `grep "authorId" apps/api/src/crm/crm.service.ts` devuelve >=1 (en addNote)
    - `grep "user.sub" apps/api/src/crm/crm.controller.ts` devuelve 1 (en addNote handler)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos
  </acceptance_criteria>
  <done>CrmController expone los 5 endpoints (list, findOne, updateStage, addNote, listNotes). DELETE no existe. CrmService implementa todos los métodos referenciados.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` pasa sin errores nuevos
- GET /crm sin token → 401; con token AUTHENTICATED → 403; con ADMIN → 200
- PATCH /crm/:id/stage con `{stage: 'LOST'}` sin stageReason → 400
- POST /crm/:id/notes guarda authorId del actor
</verification>

<success_criteria>
- CrmModule registrado en AppModule
- 5 endpoints expuestos bajo /crm con guards ADMIN+
- No existe DELETE /crm/:id
- Source polymorphic resolution funciona (CONTACT → ContactMessage, PHOTOGRAPHY/CONTENT → ServiceRequest)
- LOST sin stageReason rechazado con 400
</success_criteria>

<output>
After completion, create `.planning/phases/14-servicios-y-crm/14-02-SUMMARY.md`
</output>
