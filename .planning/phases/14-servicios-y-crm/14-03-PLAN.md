---
phase: 14-servicios-y-crm
plan: 03
type: execute
wave: 2
depends_on: [14-02]
files_modified:
  - apps/api/src/contact/contact.service.ts
autonomous: true
requirements: [SVC-04]
must_haves:
  truths:
    - "POST /contact crea ContactMessage Y CrmEntry (type=CONTACT, sourceType=CONTACT, sourceId=contactMsg.id, stage=NEW) en la misma transacción"
    - "El response del endpoint POST /contact NO cambia — sigue devolviendo el ContactMessage (backwards compatible)"
    - "ContactModule NO importa CrmModule (D-19 — patrón de no acoplamiento)"
    - "ContactService usa prisma.$transaction callback form para garantizar atomicidad"
    - "contactName y contactEmail se denormalizan en CrmEntry (snapshot)"
  artifacts:
    - path: "apps/api/src/contact/contact.service.ts"
      provides: "create() extendido con creación dual transaccional ContactMessage + CrmEntry"
      contains: "prisma.crmEntry.create"
  key_links:
    - from: "apps/api/src/contact/contact.service.ts"
      to: "prisma.crmEntry"
      via: "$transaction callback form"
      pattern: "prisma\\.\\$transaction\\(async"
    - from: "ContactModule"
      to: "CrmModule"
      via: "NO ACOPLAMIENTO — sin imports"
      pattern: "DEBE_NO_EXISTIR"
---

<objective>
Integrar Contact con CRM (SVC-04): cuando un visitante envía el formulario público `POST /contact`, además del `ContactMessage` se crea automáticamente una `CrmEntry` (type=CONTACT, stage=NEW) en la misma transacción. El response NO cambia — sigue devolviendo solo el ContactMessage (backwards compatible). ContactModule no importa CrmModule (D-19).

Purpose: Toda solicitud de contacto pasa a la vista CRM unificada sin romper el contrato actual del endpoint público.
Output: `ContactService.create()` extendido con dual creation transaccional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-servicios-y-crm/14-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/contact/contact.service.ts
@apps/api/src/contact/contact.module.ts
@apps/api/src/contact/contact.controller.ts
@apps/api/src/contact/dto/create-contact.dto.ts
@apps/api/src/orders/orders.service.ts
@apps/api/src/audit/audit.service.ts

<interfaces>
<!-- ContactMessage model (schema.prisma líneas 487-495) -->
model ContactMessage {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  subject   String
  message   String   @db.Text
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}

<!-- CrmEntry model (schema.prisma líneas 806-830) -->
model CrmEntry {
  id          Int       @id @default(autoincrement())
  type        CrmType
  stage       CrmStage  @default(NEW)
  stageReason String?
  sourceType  CrmType
  sourceId    Int
  contactName  String
  contactEmail String
  assignedTo   Int?
  notes        CrmNote[]
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

enum CrmType { CONTACT | PHOTOGRAPHY | CONTENT }
enum CrmStage { NEW | CONTACTED | NEGOTIATING | WON | LOST }

<!-- ContactService.create() actual (signature a preservar) -->
async create(dto: CreateContactDto): Promise<ContactMessage>
// Side-effects existentes: mail.sendContactReceived + mail.sendContactNotify
</interfaces>

<!-- Patrón de referencia: $transaction callback form en OrdersService -->
<!-- Patrón de referencia: AuditService.log() usa PrismaService directamente sin importar módulo destino -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender ContactService.create() con creación transaccional de CrmEntry (SVC-04)</name>
  <files>apps/api/src/contact/contact.service.ts (MODIFY)</files>
  <read_first>
    - apps/api/src/contact/contact.service.ts (estado actual completo)
    - apps/api/src/contact/contact.module.ts (verificar que NO importa CrmModule)
    - apps/api/src/orders/orders.service.ts (patrón prisma.$transaction(async (tx) => {...}))
    - apps/api/prisma/schema.prisma líneas 806-830 (campos requeridos de CrmEntry)
  </read_first>
  <action>
**Modificar `apps/api/src/contact/contact.service.ts`** — reemplazar el método `create()` actual. NO tocar `findAll`, `findOne`, `markRead`, `remove`.

Estado actual del método (líneas 15-31 del archivo):

```ts
async create(dto: CreateContactDto) {
  const msg = await this.prisma.contactMessage.create({ data: dto });

  // Email de confirmación al remitente
  await this.mail.sendContactReceived(dto.email, dto.name).catch(() => {});

  // Notificación a los emails configurados en CONTACT_NOTIFY_EMAILS
  const raw = this.config.get<string>('CONTACT_NOTIFY_EMAILS', '');
  const recipients = raw.split(',').map((e) => e.trim()).filter(Boolean);
  if (recipients.length) {
    await this.mail
      .sendContactNotify(recipients, dto.name, dto.email, dto.subject, dto.message)
      .catch(() => {});
  }

  return msg;
}
```

**Nuevo método** (D-18, D-19, D-20):

```ts
async create(dto: CreateContactDto) {
  // D-18: ContactMessage + CrmEntry en la misma transacción (callback form).
  // D-19: usar PrismaService directamente — no se importa CrmModule.
  const msg = await this.prisma.$transaction(async (tx) => {
    const contactMsg = await tx.contactMessage.create({ data: dto });
    await tx.crmEntry.create({
      data: {
        type: 'CONTACT',
        stage: 'NEW',
        sourceType: 'CONTACT',
        sourceId: contactMsg.id,
        contactName: dto.name,
        contactEmail: dto.email,
      },
    });
    return contactMsg;
  });

  // Email de confirmación al remitente (sin tocar)
  await this.mail.sendContactReceived(dto.email, dto.name).catch(() => {});

  // Notificación a los emails configurados en CONTACT_NOTIFY_EMAILS (sin tocar)
  const raw = this.config.get<string>('CONTACT_NOTIFY_EMAILS', '');
  const recipients = raw.split(',').map((e) => e.trim()).filter(Boolean);
  if (recipients.length) {
    await this.mail
      .sendContactNotify(recipients, dto.name, dto.email, dto.subject, dto.message)
      .catch(() => {});
  }

  // D-20: retornar solo el ContactMessage — no exponer la CrmEntry al remitente.
  return msg;
}
```

**Decisiones críticas a respetar:**
- D-18: callback form `prisma.$transaction(async (tx) => { ... })`, NO batch array
- D-19: usar `this.prisma` directamente — NO importar CrmModule en ContactModule
- D-20: el response del endpoint sigue siendo el `ContactMessage` (sin la CrmEntry)
- El envío de emails queda FUERA de la transacción (idempotencia: si el email falla no se revierte el guardado)
- `tx.crmEntry.create` (no `this.prisma.crmEntry.create`) dentro del callback para garantizar atomicidad
- NO añadir `assignedTo` (D-17 — diferido a Phase 15)
- NO añadir `stageReason` (NEW no requiere reason)

**NO modificar:** ningún otro método. NO modificar `ContactModule.imports` ni añadir `CrmModule`.
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(contact|error TS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "prisma.\\$transaction(async" apps/api/src/contact/contact.service.ts` devuelve >=1 (callback form, no batch)
    - `grep "tx.crmEntry.create" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "tx.contactMessage.create" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "type: 'CONTACT'" apps/api/src/contact/contact.service.ts` devuelve >=1
    - `grep "sourceType: 'CONTACT'" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "sourceId: contactMsg.id" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "contactName: dto.name" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "contactEmail: dto.email" apps/api/src/contact/contact.service.ts` devuelve 1
    - `grep "CrmModule\|CrmService" apps/api/src/contact/contact.module.ts` devuelve 0 (no acoplamiento)
    - `grep "CrmService" apps/api/src/contact/contact.service.ts` devuelve 0 (no se inyecta CrmService)
    - `grep "return msg" apps/api/src/contact/contact.service.ts` devuelve >=1 (sigue retornando ContactMessage)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos
  </acceptance_criteria>
  <done>POST /contact crea ContactMessage + CrmEntry atómicamente. El envío de emails sigue funcionando fuera de la transacción. ContactModule sigue sin importar CrmModule. Backward compatible para clientes existentes.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` pasa
- POST /contact con body válido → 201 con ContactMessage; en DB existe CrmEntry con sourceId=contactMessage.id y type=CONTACT
- Si Prisma falla creando CrmEntry, no se guarda el ContactMessage (transacción)
- GET /crm?type=CONTACT (con ADMIN) lista el nuevo CrmEntry
</verification>

<success_criteria>
- ContactService.create() usa $transaction callback con dos creates
- ContactModule sin imports de CrmModule
- Response del endpoint público sin cambios (solo ContactMessage)
- Snapshot de contactName/contactEmail denormalizado en CrmEntry
</success_criteria>

<output>
After completion, create `.planning/phases/14-servicios-y-crm/14-03-SUMMARY.md`
</output>
