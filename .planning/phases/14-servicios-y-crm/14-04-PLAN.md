---
phase: 14-servicios-y-crm
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - apps/api/src/services/services.service.ts
autonomous: true
requirements: [SVC-05]
must_haves:
  truths:
    - "POST /services/photography crea ServiceRequest (type=PHOTOGRAPHY) Y CrmEntry (type=PHOTOGRAPHY, sourceType=PHOTOGRAPHY, sourceId=serviceReq.id, stage=NEW) en la misma transacción"
    - "POST /services/content-creators crea ServiceRequest (type=CONTENT) Y CrmEntry (type=CONTENT, sourceType=CONTENT, sourceId=serviceReq.id, stage=NEW) en la misma transacción"
    - "El response del endpoint público NO cambia — sigue devolviendo solo {id, type, name, email, createdAt}"
    - "ServicesModule NO importa CrmModule (D-23 — patrón de no acoplamiento)"
    - "El many-to-many de options usa $transaction callback form (D-22 — batch form no soporta connect)"
  artifacts:
    - path: "apps/api/src/services/services.service.ts"
      provides: "createRequest() refactor con dual creation transaccional ServiceRequest + CrmEntry + options connect"
      contains: "prisma.crmEntry.create"
  key_links:
    - from: "apps/api/src/services/services.service.ts"
      to: "prisma.crmEntry"
      via: "$transaction callback form con tx.crmEntry.create"
      pattern: "prisma\\.\\$transaction\\(async"
    - from: "ServicesModule"
      to: "CrmModule"
      via: "NO ACOPLAMIENTO — sin imports"
      pattern: "DEBE_NO_EXISTIR"
    - from: "ServiceRequest"
      to: "ServiceOption[]"
      via: "many-to-many connect dentro del callback"
      pattern: "options: \\{ connect:"
---

<objective>
Integrar Services con CRM (SVC-05): cuando se crea una solicitud de cotización vía `POST /services/photography` o `POST /services/content-creators`, además del `ServiceRequest` se crea una `CrmEntry` (type según `crmTypeMap[ServiceType]`, stage=NEW) en la misma transacción. El many-to-many de opciones se mantiene mediante `connect` dentro del callback (D-22 — batch form no soporta connect). ServicesModule no importa CrmModule (D-23).

Purpose: Toda solicitud de servicios pasa al pipeline CRM unificado sin romper el contrato del endpoint público ni el comportamiento many-to-many de options.
Output: `ServicesService.createRequest()` refactorizado con dual creation transaccional preservando el `connect` de options.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-servicios-y-crm/14-CONTEXT.md
@apps/api/prisma/schema.prisma
@apps/api/src/services/services.service.ts
@apps/api/src/services/services.module.ts
@apps/api/src/services/services.controller.ts
@apps/api/src/services/dto/create-service-request.dto.ts
@apps/api/src/contact/contact.service.ts
@apps/api/src/orders/orders.service.ts

<interfaces>
<!-- Estado tras plan 14-01 (Wave 1) -->

// ServicesService.createRequest(dto, type: ServiceType) actual:
// - prisma.serviceRequest.create({ data: { ..., options: { connect: ... } } })
// - retorna { id, type, name, email, createdAt }

// CrmEntry (schema.prisma 806-830) — campos requeridos:
// type, stage, sourceType, sourceId, contactName, contactEmail

// ServiceType (Prisma enum) === CrmType subset:
// PHOTOGRAPHY → CrmType.PHOTOGRAPHY
// CONTENT → CrmType.CONTENT
// (1:1 mapping — los enums tienen los mismos valores literales)
</interfaces>

<!-- Patrón de referencia clave: $transaction callback form en OrdersService (no batch). -->
<!-- D-22 LOCKED: batch form (array) NO soporta `connect`. DEBE usarse callback form. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar ServicesService.createRequest() con $transaction callback + CrmEntry (SVC-05)</name>
  <files>apps/api/src/services/services.service.ts (MODIFY)</files>
  <read_first>
    - apps/api/src/services/services.service.ts (estado tras plan 14-01, método createRequest actual)
    - apps/api/src/services/services.module.ts (verificar que NO importa CrmModule)
    - apps/api/src/contact/contact.service.ts (patrón ejecutado en plan 14-03 — referencia idéntica)
    - apps/api/src/orders/orders.service.ts (patrón $transaction(async (tx) => {...}))
    - apps/api/prisma/schema.prisma líneas 781-794 (ServiceRequest model + many-to-many options)
    - apps/api/prisma/schema.prisma líneas 806-830 (CrmEntry — campos requeridos)
  </read_first>
  <action>
**Modificar `apps/api/src/services/services.service.ts`** — reemplazar SOLO el método `createRequest()`. NO tocar `getActiveOptions`, `listRequests`, `createOption`, `updateOption`, `deleteOption`.

Estado actual del método (creado en plan 14-01):

```ts
async createRequest(dto: CreateServiceRequestDto, type: ServiceType) {
  const data: any = {
    type,
    name: dto.name,
    email: dto.email,
    eventName: dto.eventName ?? null,
    eventDate: dto.eventDate ? new Date(dto.eventDate) : null,
    eventPlace: dto.eventPlace ?? null,
  };
  if (dto.optionIds && dto.optionIds.length) {
    data.options = { connect: dto.optionIds.map((id) => ({ id })) };
  }
  const req = await this.prisma.serviceRequest.create({
    data,
    select: { id: true, type: true, name: true, email: true, createdAt: true },
  });
  return req;
}
```

**Nuevo método** (D-21, D-22, D-23):

```ts
async createRequest(dto: CreateServiceRequestDto, type: ServiceType) {
  // D-22: callback form de $transaction — batch form (array) no soporta `connect` para many-to-many.
  // D-23: usar PrismaService directamente (this.prisma) — sin importar CrmModule.
  const data: any = {
    type,
    name: dto.name,
    email: dto.email,
    eventName: dto.eventName ?? null,
    eventDate: dto.eventDate ? new Date(dto.eventDate) : null,
    eventPlace: dto.eventPlace ?? null,
  };
  if (dto.optionIds && dto.optionIds.length) {
    data.options = { connect: dto.optionIds.map((id) => ({ id })) };
  }

  // D-21: ServiceRequest + CrmEntry en la misma transacción.
  // D-21 LOCKED: usar crmTypeMap explícito aunque los valores sean iguales (D-21 lo especifica así).
  const crmTypeMap = { PHOTOGRAPHY: 'PHOTOGRAPHY', CONTENT: 'CONTENT' } as const;
  const req = await this.prisma.$transaction(async (tx) => {
    const serviceReq = await tx.serviceRequest.create({
      data,
      select: { id: true, type: true, name: true, email: true, createdAt: true },
    });
    await tx.crmEntry.create({
      data: {
        type: crmTypeMap[type], // CrmType explícito via crmTypeMap (D-21)
        stage: 'NEW',
        sourceType: crmTypeMap[type],
        sourceId: serviceReq.id,
        contactName: dto.name,
        contactEmail: dto.email,
      },
    });
    return serviceReq;
  });

  return req;
}
```

**Decisiones críticas a respetar:**
- D-21: `ServiceRequest` y `CrmEntry` en la misma transacción (atómica)
- D-22: usar `prisma.$transaction(async (tx) => {...})` (callback form), NO batch array — `connect` de many-to-many requiere callback
- D-23: usar `this.prisma` directamente — NO importar CrmModule en ServicesModule
- El response sigue siendo `{ id, type, name, email, createdAt }` (sin la CrmEntry — backward compatible con plan 14-01)
- Usar `tx.serviceRequest.create` y `tx.crmEntry.create` (NO `this.prisma.*`) DENTRO del callback
- `type` y `sourceType` reciben el mismo valor (PHOTOGRAPHY o CONTENT — Prisma castea string→enum)
- `stage: 'NEW'` literal (Prisma castea string→enum CrmStage)

**NO modificar:**
- `getActiveOptions`, `listRequests`, `createOption`, `updateOption`, `deleteOption`
- `services.module.ts` (no añadir CrmModule)
- `services.controller.ts` (sin cambios — el controller llama al mismo `createRequest`)
- DTOs (sin cambios)

**Verificación de no-acoplamiento:** Antes de commitear, confirmar:
- `grep "CrmModule\\|CrmService" apps/api/src/services/services.module.ts` debe devolver 0
- `grep "CrmService" apps/api/src/services/services.service.ts` debe devolver 0
  </action>
  <verify>
    <automated>cd apps/api && pnpm tsc --noEmit 2>&1 | grep -E "(services|error TS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "prisma.\\$transaction(async" apps/api/src/services/services.service.ts` devuelve >=1 (callback form, no batch)
    - `grep "tx.serviceRequest.create" apps/api/src/services/services.service.ts` devuelve 1
    - `grep "tx.crmEntry.create" apps/api/src/services/services.service.ts` devuelve 1
    - `grep "sourceId: serviceReq.id" apps/api/src/services/services.service.ts` devuelve 1
    - `grep "stage: 'NEW'" apps/api/src/services/services.service.ts` devuelve 1
    - `grep "crmTypeMap\[type\]" apps/api/src/services/services.service.ts` devuelve >=2 (type + sourceType)
    - `grep "options: { connect:" apps/api/src/services/services.service.ts` devuelve 1 (many-to-many preservado)
    - `grep "CrmModule\|CrmService" apps/api/src/services/services.module.ts` devuelve 0 (D-23 — no acoplamiento)
    - `grep "CrmService" apps/api/src/services/services.service.ts` devuelve 0 (no se inyecta)
    - `cd apps/api && pnpm tsc --noEmit` no reporta errores nuevos
  </acceptance_criteria>
  <done>POST /services/photography y POST /services/content-creators crean ServiceRequest + CrmEntry atómicamente. El many-to-many de options sigue funcionando vía `connect` dentro del callback. ServicesModule sigue sin importar CrmModule. Response del endpoint público sin cambios.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` pasa
- POST /services/photography con `optionIds: [1, 2]` → 201; en DB existen ServiceRequest con options conectadas + CrmEntry con type=PHOTOGRAPHY y sourceId=serviceReq.id
- POST /services/content-creators → CrmEntry con type=CONTENT
- GET /crm?type=PHOTOGRAPHY (con ADMIN) lista la nueva entrada
- Si crear CrmEntry falla, el ServiceRequest no se persiste (atomicidad)
</verification>

<success_criteria>
- ServicesService.createRequest() usa $transaction callback form
- ServiceRequest + CrmEntry creados atómicamente
- options many-to-many sigue funcionando via connect
- ServicesModule sin imports de CrmModule
- Response del endpoint público sin cambios (back-compat con plan 14-01)
</success_criteria>

<output>
After completion, create `.planning/phases/14-servicios-y-crm/14-04-SUMMARY.md`
</output>
