---
phase: 15-rediseno-ui-migracion-de-vistas
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - apps/website/app/(site)/page.tsx
  - apps/website/app/(site)/HomeView.tsx
  - apps/website/app/(site)/categoria/[cat]/page.tsx
  - apps/website/app/(site)/categoria/[cat]/CategoryView.tsx
  - apps/website/app/(site)/busqueda/page.tsx
  - apps/website/app/(site)/busqueda/SearchView.tsx
  - apps/website/app/(site)/evento/[slug]/page.tsx
  - apps/website/app/(site)/evento/[slug]/EventView.tsx
  - apps/website/app/crear/page.tsx
  - apps/website/app/crear/layout.tsx
  - apps/website/app/crear/FormContext.tsx
  - apps/website/app/crear/shared.tsx
  - apps/website/app/crear/1/Step1Client.tsx
  - apps/website/app/crear/2/Step2Client.tsx
  - apps/website/app/crear/3/Step3Client.tsx
autonomous: true
requirements:
  - UI-MIG-02
must_haves:
  truths:
    - "Home rinde HeroCarousel + Rails por categoría + strips (Spots/News/Services/LastJoined)"
    - "Categoría rinde nuevo layout con list-rows + sidebar de filtros con Pop components"
    - "Búsqueda rinde nuevo layout de resultados"
    - "Evento detalle rinde nuevo layout extendido (gallery, organizer block, related events)"
    - "crear/ (formulario multi-step) actualizado al diseño FormPage + Step1..4 con nuevas clases (D-04)"
    - "Cada vista vive en su PROPIO archivo separado del page.tsx (D-04, D-05)"
    - "pnpm --filter website build pasa exit 0"
  artifacts:
    - path: "apps/website/app/(site)/HomeView.tsx"
      provides: "Componente HomePage del diseño separado del page.tsx"
      contains: "export function HomeView"
    - path: "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx"
      provides: "Componente CategoryPage separado"
      contains: "export function CategoryView"
    - path: "apps/website/app/(site)/evento/[slug]/EventView.tsx"
      provides: "Componente EventPage separado"
      contains: "export function EventView"
    - path: "apps/website/app/crear/page.tsx"
      provides: "Form multi-step shell (FormPage del diseño)"
      contains: "form-shell"
    - path: "apps/website/app/crear/1/Step1Client.tsx"
      provides: "Step 1 (Información básica) actualizado al nuevo diseño"
      contains: "form-step"
  key_links:
    - from: "apps/website/app/(site)/page.tsx"
      to: "HomeView"
      via: "import { HomeView } from './HomeView'"
      pattern: "HomeView"
    - from: "apps/website/app/(site)/page.tsx"
      to: "HeroCarousel"
      via: "import { HeroCarousel } from '@/components/HeroCarousel'"
      pattern: "HeroCarousel"
    - from: "apps/website/app/(site)/evento/[slug]/EventView.tsx"
      to: "api.event(slug)"
      via: "consume API existente (D-15)"
      pattern: "api\\.events?\\("
    - from: "apps/website/app/crear/page.tsx"
      to: "FormContext + Step1..3 clients"
      via: "multi-step form wiring"
      pattern: "FormContext|Step[123]Client"
---

<objective>
Wave 2: Actualizar las vistas públicas YA EXISTENTES (home, categoría, búsqueda, evento detalle, formulario crear/) al nuevo diseño. Cada vista se separa en dos archivos:
- `page.tsx` (server component, hace fetch a la API y pasa props)
- `XxxView.tsx` (client component "use client" con la UI del diseño)

Esto cumple D-04 (cada vista en su archivo) y D-05 (subcomponentes grandes separados).

Preserva todas las integraciones API existentes (D-14): la home/categoría/evento ya consumen `lib/api.ts`. Solo se reescribe la capa visual.

**Cobertura `crear/`**: El formulario multi-step de creación de eventos (`apps/website/app/crear/`) está mapeado por D-04 al diseño FormPage + Step1..4. Se actualiza en este plan como vista existente.

Purpose: actualizar el sitio público al nuevo diseño sin romper APIs.
Output: 4 vistas existentes con UI nueva + cada una en su archivo separado + form multi-step actualizado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-CONTEXT.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-RESEARCH.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-01-PLAN.md

# Diseño fuente
@design/Konbini.html

# Vistas actuales (referencia para preservar integraciones API)
@apps/website/app/(site)/page.tsx
@apps/website/app/(site)/categoria/[cat]/page.tsx
@apps/website/app/(site)/busqueda/page.tsx
@apps/website/app/(site)/busqueda/SearchView.tsx
@apps/website/app/(site)/evento/[slug]/page.tsx
@apps/website/app/crear/page.tsx
@apps/website/app/crear/layout.tsx
@apps/website/app/crear/FormContext.tsx
@apps/website/app/crear/shared.tsx
@apps/website/app/crear/1/Step1Client.tsx
@apps/website/app/crear/2/Step2Client.tsx
@apps/website/app/crear/3/Step3Client.tsx

# Capa de datos (NO TOCAR)
@apps/website/lib/api.ts

<interfaces>
From apps/website/lib/api.ts:
```typescript
export const api = {
  events: (q?: EventsQuery) => Promise<ApiEventList>,
  event: (slug: string) => Promise<ApiEvent>,
  categories: () => Promise<ApiCategory[]>,
  heroes: () => Promise<ApiHero[]>,
  createEvent: (input, token) => Promise<ApiEvent>,
  updateEvent: (id, input, token) => Promise<ApiEvent>,
  // ...
};

export function toEventItem(e: ApiEvent): EventItem;
export function toHeroSlide(h: ApiHero): HeroSlide;
```

From Plan 01 (componentes base disponibles):
```typescript
import { Header, Footer, BrandMark } from "@/components/...";
import { EventCard, Poster, Rail, HeroCarousel } from "@/components/...";
import { Ic } from "@/components/icons";
import { useTheme, useUser } from "@/components/providers";
import { toast } from "sonner";
```

From apps/website/app/crear/FormContext.tsx (actual):
```typescript
export const FormContext = createContext<{ form, setForm, ... }>(...);
export function useForm(): FormContextValue;
```
Se preserva el contrato del context — solo se actualiza la UI de los steps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir HomePage (home) y separar en HomeView.tsx</name>
  <files>apps/website/app/(site)/page.tsx, apps/website/app/(site)/HomeView.tsx</files>
  <read_first>
    - apps/website/app/(site)/page.tsx (versión actual con fetch SSR — preservar la lógica de fetch)
    - apps/website/app/(site)/layout.tsx (verificar Header/Footer wrappers)
    - design/Konbini.html lines 2085-2104 (HomePage del diseño)
    - design/Konbini.html lines 1890-1952 (HeroCarousel — Plan 01 lo creó)
    - design/Konbini.html lines 1953-2084 (Rail — Plan 01 lo actualizó)
    - apps/website/lib/api.ts (funciones api.events, api.categories, api.heroes, toEventItem, toHeroSlide)
  </read_first>
  <action>
    Paso 1 — `apps/website/app/(site)/page.tsx`. MANTENER como Server Component que hace fetch SSR. Reemplazar el contenido por:
    ```tsx
    import { api, toEventItem, toHeroSlide } from "@/lib/api";
    import { HomeView } from "./HomeView";

    export default async function HomePage() {
      const [eventsRes, categories, heroes] = await Promise.all([
        api.events({ pageSize: 24 }),
        api.categories(),
        api.heroes(),
      ]);
      const items = eventsRes.items.map(toEventItem);
      const slides = heroes.map(toHeroSlide);
      return <HomeView items={items} categories={categories} slides={slides} />;
    }
    ```
    Preservar manejo de errores existente (try/catch o fallback a array vacío).

    Paso 2 — Crear `apps/website/app/(site)/HomeView.tsx` (NUEVO, client component). Estructura tomada de `design/Konbini.html` líneas 2085-2104:
    ```tsx
    "use client";
    import { HeroCarousel } from "@/components/HeroCarousel";
    import { Rail } from "@/components/Rail";
    import type { EventItem, HeroSlide, ApiCategory } from "@/lib/api";

    export function HomeView({
      items, categories, slides,
    }: { items: EventItem[]; categories: ApiCategory[]; slides: HeroSlide[] }) {
      // Agrupar items por categoría
      const byCategory = (slug: string) => items.filter(e => e.cat === slug);
      return (
        <main>
          <section className="container">
            <HeroCarousel slides={slides} />
          </section>

          {/* Rail "Hoy" — eventos con fecha hoy */}
          <Rail
            title="Hoy"
            jp="今日"
            items={items.filter(e => /* fecha = hoy */).slice(0, 12)}
            today
            hrefSeeAll="/busqueda?desde=hoy"
          />

          {/* Rails por categoría (top 4) */}
          {categories.slice(0, 4).map(cat => {
            const catItems = byCategory(cat.slug);
            if (catItems.length === 0) return null;
            return (
              <Rail
                key={cat.id}
                title={cat.name}
                jp={cat.metadata?.ja /* o fallback */}
                items={catItems.slice(0, 12)}
                hrefSeeAll={`/categoria/${cat.slug}`}
              />
            );
          })}

          {/* SpotsStrip, NewsRail, ServicesStrip, LastJoinedStrip — siguen patrón del diseño */}
          {/* Ver design/Konbini.html lines 2085-2104 para los strips adicionales */}
        </main>
      );
    }
    ```
    Mantener fidelidad al diseño en las clases CSS (.strip, .strip-grid, .spots-strip, etc. — añadidas en Plan 01 Task 1).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/(site)/HomeView.tsx" && grep -q "export function HomeView\\|export default function HomeView" "apps/website/app/(site)/HomeView.tsx" && grep -q "HomeView" "apps/website/app/(site)/page.tsx" && grep -q "HeroCarousel" "apps/website/app/(site)/HomeView.tsx" && grep -q "Rail" "apps/website/app/(site)/HomeView.tsx" && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "apps/website/app/(site)/HomeView.tsx"` (archivo creado)
    - `grep -q "\"use client\"" "apps/website/app/(site)/HomeView.tsx"` → 0
    - `grep -q "HomeView" "apps/website/app/(site)/page.tsx"` → 0 (page.tsx importa HomeView)
    - `grep -q "HeroCarousel" "apps/website/app/(site)/HomeView.tsx"` → 0
    - `grep -q "Rail" "apps/website/app/(site)/HomeView.tsx"` → 0
    - `grep -q "api\\." "apps/website/app/(site)/page.tsx"` → 0 (preserva integración API)
    - `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    HomePage usa HeroCarousel + Rails + strips del diseño. Fetch SSR preservado. HomeView.tsx existe en archivo separado. Build pasa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescribir CategoryPage y SearchPage al nuevo diseño</name>
  <files>apps/website/app/(site)/categoria/[cat]/page.tsx, apps/website/app/(site)/categoria/[cat]/CategoryView.tsx, apps/website/app/(site)/busqueda/page.tsx, apps/website/app/(site)/busqueda/SearchView.tsx</files>
  <read_first>
    - apps/website/app/(site)/categoria/[cat]/page.tsx (versión actual — preservar fetch SSR por slug)
    - apps/website/app/(site)/busqueda/page.tsx (versión actual — preservar parsing de query params)
    - apps/website/app/(site)/busqueda/SearchView.tsx (cliente actual — reescribir UI conservando lógica)
    - design/Konbini.html lines 2105-2305 (CategoryPage del diseño con list-rows + sidebar Pop filters)
    - design/Konbini.html lines 2306-2352 (SearchPage del diseño)
    - apps/website/lib/api.ts (api.events con filtros, ApiCategory)
  </read_first>
  <action>
    Paso 1 — `apps/website/app/(site)/categoria/[cat]/page.tsx`. Mantener server component:
    ```tsx
    import { api, toEventItem } from "@/lib/api";
    import { notFound } from "next/navigation";
    import { CategoryView } from "./CategoryView";

    export default async function CategoryPage({ params }: { params: Promise<{ cat: string }> }) {
      const { cat } = await params;
      const [categories, eventsRes] = await Promise.all([
        api.categories(),
        api.events({ category: cat, pageSize: 60 }),
      ]);
      const category = categories.find(c => c.slug === cat);
      if (!category) notFound();
      return (
        <CategoryView
          category={category}
          allCategories={categories}
          items={eventsRes.items.map(toEventItem)}
        />
      );
    }
    ```

    Paso 2 — Crear `apps/website/app/(site)/categoria/[cat]/CategoryView.tsx`. Estructura del diseño líneas 2105-2305:
    ```tsx
    "use client";
    import { useState } from "react";
    import Link from "next/link";
    import { EventCard } from "@/components/EventCard";
    import { Ic } from "@/components/icons";
    import type { EventItem, ApiCategory } from "@/lib/api";

    export function CategoryView({
      category, allCategories, items,
    }: { category: ApiCategory; allCategories: ApiCategory[]; items: EventItem[] }) {
      const [view, setView] = useState<"grid" | "list">("grid");
      const [filters, setFilters] = useState({/* fechaFrom, fechaTo, precio, ubicacion */});
      // Pop components (filtros): sidebar izquierdo con popovers
      // .cat-shell: grid 280px / 1fr
      // .cat-side: sticky sidebar con .pop trigger + panel
      // .cat-main: lista de eventos en .list-row (formato lista) o .cards-grid
      return (
        <main className="container cat-shell">
          <aside className="cat-side">
            <div className="cat-hero">
              <div className="eyebrow">{category.metadata?.ja /* o slug */}</div>
              <h1 className="display">{category.name}</h1>
              <p>{category.description}</p>
            </div>
            {/* Pop filters: fecha, precio, ubicación, tags */}
            {/* Cada filtro es un <details className="pop"> con summary y panel */}
          </aside>
          <section className="cat-main">
            <div className="cat-toolbar">
              <div className="count">{items.length} eventos</div>
              <div className="view-toggle">
                <button onClick={() => setView("grid")} className={view === "grid" ? "on" : ""}>Cards</button>
                <button onClick={() => setView("list")} className={view === "list" ? "on" : ""}>Lista</button>
              </div>
            </div>
            {view === "grid" ? (
              <div className="cards-grid">
                {items.map(e => <EventCard key={e.id} e={e} />)}
              </div>
            ) : (
              <div className="list-rows">
                {items.map(e => (
                  <Link key={e.id} className="list-row" href={`/evento/${e.slug ?? e.id}`}>
                    {/* row layout: miniatura + título + fecha + lugar + precio */}
                  </Link>
                ))}
              </div>
            )}
          </section>
        </main>
      );
    }
    ```

    Paso 3 — `apps/website/app/(site)/busqueda/page.tsx` (server component que parsea searchParams):
    ```tsx
    import { api, toEventItem } from "@/lib/api";
    import { SearchView } from "./SearchView";

    export default async function SearchPage({
      searchParams,
    }: { searchParams: Promise<{ q?: string; categoria?: string; region?: string; desde?: string; hasta?: string }> }) {
      const sp = await searchParams;
      const [categories, eventsRes] = await Promise.all([
        api.categories(),
        api.events({ q: sp.q, category: sp.categoria, /* ... */ pageSize: 60 }),
      ]);
      return (
        <SearchView
          initialQuery={sp.q ?? ""}
          initialFilters={{ categoria: sp.categoria, region: sp.region, desde: sp.desde, hasta: sp.hasta }}
          categories={categories}
          items={eventsRes.items.map(toEventItem)}
        />
      );
    }
    ```

    Paso 4 — REESCRIBIR `apps/website/app/(site)/busqueda/SearchView.tsx` siguiendo `design/Konbini.html` líneas 2306-2352:
    ```tsx
    "use client";
    import { useRouter, useSearchParams } from "next/navigation";
    import { useState } from "react";
    import { EventCard } from "@/components/EventCard";
    import { Ic } from "@/components/icons";
    import type { EventItem, ApiCategory } from "@/lib/api";

    export function SearchView({
      initialQuery, initialFilters, categories, items,
    }: { /* ... */ }) {
      const router = useRouter();
      const params = useSearchParams();
      const [q, setQ] = useState(initialQuery);
      // ... estados de filtros
      const submitSearch = () => {
        const usp = new URLSearchParams();
        if (q) usp.set("q", q);
        // ... resto de filtros
        router.push(`/busqueda?${usp.toString()}`);
      };
      return (
        <main className="container search-shell">
          <div className="search-hero">
            <div className="big-input">
              {Ic.search}
              <input value={q} onChange={e => setQ(e.target.value)} onKeyDown={e => e.key === "Enter" && submitSearch()} placeholder="Buscar eventos…" />
            </div>
            <div className="search-chips">{/* Filtros activos como chips */}</div>
          </div>
          <section className="search-results">
            <div className="count">{items.length} resultados</div>
            {items.length === 0 ? (
              <div className="empty">
                <h3>Sin resultados</h3>
                <p>Prueba con otra búsqueda o ajusta los filtros.</p>
              </div>
            ) : (
              <div className="cards-grid">
                {items.map(e => <EventCard key={e.id} e={e} />)}
              </div>
            )}
          </section>
        </main>
      );
    }
    ```
    Preservar lógica de sincronización URL ↔ filtros del SearchView actual (estaba en SRCH-04).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx" && grep -q "CategoryView" "apps/website/app/(site)/categoria/[cat]/page.tsx" && grep -q "cat-shell\\|cat-side\\|cat-main" "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx" && grep -q "search-shell\\|search-hero" "apps/website/app/(site)/busqueda/SearchView.tsx" && grep -q "useSearchParams\\|router.push" "apps/website/app/(site)/busqueda/SearchView.tsx" && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx"` (archivo creado)
    - `grep -q "\"use client\"" "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx"` → 0
    - `grep -q "cat-shell\\|cat-side\\|cat-main" "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx"` → 0 (clases del diseño)
    - `grep -q "CategoryView" "apps/website/app/(site)/categoria/[cat]/page.tsx"` → 0
    - `grep -q "search-shell\\|search-hero\\|search-results" "apps/website/app/(site)/busqueda/SearchView.tsx"` → 0
    - `grep -q "useSearchParams" "apps/website/app/(site)/busqueda/SearchView.tsx"` → 0 (URL sync preserved)
    - `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    CategoryView y SearchView reescritos al nuevo diseño en sus propios archivos. Lógica de fetch + URL sync preservada. Build pasa.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reescribir EventPage (detalle de evento) con nuevo diseño extendido</name>
  <files>apps/website/app/(site)/evento/[slug]/page.tsx, apps/website/app/(site)/evento/[slug]/EventView.tsx</files>
  <read_first>
    - apps/website/app/(site)/evento/[slug]/page.tsx (versión actual con fetch por slug — preservar)
    - design/Konbini.html lines 2533-2739 (EventPage del diseño — vista MUY expandida con gallery, organizer block, related, CTA externo)
    - apps/website/lib/api.ts (api.event, ApiEvent shape — ver campos: banner, poster, gallery, prices, dates, socialLinks, ticketUrl, owner, category, organizer)
  </read_first>
  <action>
    Paso 1 — `apps/website/app/(site)/evento/[slug]/page.tsx`. Server component:
    ```tsx
    import { api, toEventItem } from "@/lib/api";
    import { notFound } from "next/navigation";
    import { EventView } from "./EventView";

    export default async function EventPage({ params }: { params: Promise<{ slug: string }> }) {
      const { slug } = await params;
      const event = await api.event(slug).catch(() => null);
      if (!event) notFound();
      const relatedRes = await api.events({
        category: event.category?.slug,
        pageSize: 8,
      }).catch(() => ({ items: [] }));
      const related = relatedRes.items.filter(e => e.id !== event.id).slice(0, 6).map(toEventItem);
      return <EventView event={event} related={related} />;
    }

    export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) {
      const { slug } = await params;
      const event = await api.event(slug).catch(() => null);
      return {
        title: event ? `${event.title} · Konbini` : "Evento · Konbini",
        description: event?.description?.slice(0, 160) ?? undefined,
      };
    }
    ```

    Paso 2 — Crear `apps/website/app/(site)/evento/[slug]/EventView.tsx`. Estructura del diseño líneas 2533-2739:
    ```tsx
    "use client";
    import Link from "next/link";
    import { useState } from "react";
    import { toast } from "sonner";
    import { EventCard } from "@/components/EventCard";
    import { Ic } from "@/components/icons";
    import { useUser } from "@/components/providers";
    import { api, imageUrl, withUtm, type ApiEvent, type EventItem } from "@/lib/api";

    export function EventView({ event, related }: { event: ApiEvent; related: EventItem[] }) {
      const { user, token } = useUser();
      const [saved, setSaved] = useState(false);
      const handleSave = async () => {
        if (!user) {
          toast.error("Inicia sesión para guardar eventos");
          return;
        }
        // call api.saveEvent / unsaveEvent — Phase 13 endpoint
        setSaved(s => !s);
      };
      const handleShare = () => {
        navigator.clipboard.writeText(window.location.href);
        toast.success("Link copiado");
      };
      // Estructura: .ev-shell con secciones
      // .ev-hero: banner + título + fecha + ubicación + CTAs (save, share, ticketUrl)
      // .ev-gallery: galería de imágenes (gallery del event)
      // .ev-content: descripción markdown
      // .ev-aside: bloque organizador (avatar, name, link a /u/[handle]) + redes sociales
      // .ev-related: rail de eventos relacionados
      return (
        <main className="ev-shell container">
          <section className="ev-hero">
            <img className="ev-banner" src={imageUrl(event.banner)} alt={event.title} />
            <div className="ev-hero-info">
              <div className="eyebrow">{event.category?.name?.toUpperCase()}</div>
              <h1 className="display">{event.title}</h1>
              <div className="ev-meta">
                <span>{Ic.cal} {/* fecha */}</span>
                <span>{Ic.pin} {/* lugar */}</span>
              </div>
              <div className="ev-cta">
                <a className="btn primary lg" href={withUtm(event.ticketUrl ?? "#", "event-page")} target="_blank">
                  {Ic.ticket} Comprar entradas
                </a>
                <button className="btn ghost" onClick={handleSave}>
                  {Ic.heart} {saved ? "Guardado" : "Guardar"}
                </button>
                <button className="btn ghost" onClick={handleShare}>
                  {Ic.share} Compartir
                </button>
              </div>
            </div>
          </section>
          {/* .ev-gallery: grid de event.gallery */}
          {/* .ev-content: event.description en markdown render */}
          {/* .ev-aside: organizer card (link a /u/{event.owner?.handle}) + redes (event.socialLinks) */}
          {related.length > 0 && (
            <section className="ev-related">
              <h2 className="display">También te puede gustar <span className="jp">関連</span></h2>
              <div className="cards-grid">
                {related.map(e => <EventCard key={e.id} e={e} />)}
              </div>
            </section>
          )}
        </main>
      );
    }
    ```
    Notas:
    - El CTA "Comprar entradas" enlaza a `event.ticketUrl` (plataforma externa) — NO checkout interno.
    - `event.socialLinks` se renderiza usando `linkHref(type, value)` de api.ts.
    - `event.owner.handle` linkea a `/u/{handle}` (Plan 05 crea esa ruta).
    - Si el evento tiene `videos`, mostrar embed/preview.
    - Las clases CSS (`.ev-shell`, `.ev-hero`, `.ev-banner`, `.ev-meta`, `.ev-cta`, `.ev-gallery`, `.ev-content`, `.ev-aside`, `.ev-related`) deben existir en globals.css (añadidas en Plan 01).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/(site)/evento/[slug]/EventView.tsx" && grep -q "export function EventView" "apps/website/app/(site)/evento/[slug]/EventView.tsx" && grep -q "EventView" "apps/website/app/(site)/evento/[slug]/page.tsx" && grep -q "ev-shell\\|ev-hero\\|ev-related" "apps/website/app/(site)/evento/[slug]/EventView.tsx" && grep -q "ticketUrl" "apps/website/app/(site)/evento/[slug]/EventView.tsx" && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "apps/website/app/(site)/evento/[slug]/EventView.tsx"` (archivo creado)
    - `grep -q "\"use client\"" "apps/website/app/(site)/evento/[slug]/EventView.tsx"` → 0
    - `grep -q "ev-shell\\|ev-hero" "apps/website/app/(site)/evento/[slug]/EventView.tsx"` → 0
    - `grep -q "ticketUrl" "apps/website/app/(site)/evento/[slug]/EventView.tsx"` → 0 (CTA externo preservado)
    - `grep -q "EventView" "apps/website/app/(site)/evento/[slug]/page.tsx"` → 0
    - `grep -q "api\\.event" "apps/website/app/(site)/evento/[slug]/page.tsx"` → 0 (preserva integración)
    - `! grep -q "checkout" "apps/website/app/(site)/evento/[slug]/EventView.tsx"` (sin checkout interno — SITE-04)
    - `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    EventView reescrita con layout extendido del diseño. CTA "Comprar entradas" enlaza externamente. Galería, descripción, bloque de organizador y eventos relacionados presentes. Build pasa.
  </done>
</task>

<task type="auto">
  <name>Task 4: Actualizar formulario multi-step crear/ (FormPage + Step1..3 del diseño)</name>
  <files>apps/website/app/crear/page.tsx, apps/website/app/crear/layout.tsx, apps/website/app/crear/FormContext.tsx, apps/website/app/crear/shared.tsx, apps/website/app/crear/1/Step1Client.tsx, apps/website/app/crear/2/Step2Client.tsx, apps/website/app/crear/3/Step3Client.tsx</files>
  <read_first>
    - apps/website/app/crear/page.tsx (shell actual del formulario — preservar lógica de navegación entre steps y submit final)
    - apps/website/app/crear/layout.tsx (layout wrapper — verificar estructura actual)
    - apps/website/app/crear/FormContext.tsx (Context con estado del form — PRESERVAR contrato: setForm, form, validateStep, etc.)
    - apps/website/app/crear/shared.tsx (componentes compartidos del form — campos, dropzone, etc.)
    - apps/website/app/crear/1/Step1Client.tsx (Step 1 actual — campos básicos)
    - apps/website/app/crear/2/Step2Client.tsx (Step 2 actual — fechas/lugar)
    - apps/website/app/crear/3/Step3Client.tsx (Step 3 actual — multimedia/precios)
    - apps/website/lib/api.ts (api.createEvent, api.updateEvent — preservar integración)
    - design/Konbini.html lines 2740-2793 (FormPage del diseño — shell multi-step con sidebar de progreso)
    - design/Konbini.html lines 2794-2872 (Step1 del diseño — Información básica)
    - design/Konbini.html lines 2873-2954 (Step2 del diseño — Fechas y ubicación)
    - design/Konbini.html lines 2955-3030 (Step3 del diseño — Multimedia y precios)
    - design/Konbini.html lines 3031-3094 (Step4 del diseño — Revisión y submit, si aplica al actual)
  </read_first>
  <action>
    **Regla general:** PRESERVAR toda la lógica de:
    - `FormContext.tsx` (estado, validación, persistencia local)
    - Submit final (api.createEvent / api.updateEvent con token de useUser)
    - Navegación entre steps (router.push a /crear/1, /crear/2, /crear/3)
    - Manejo de errores y toasts existentes

    Solo se actualiza la capa visual: clases CSS del diseño, layout sidebar+main, indicadores de progreso, estilos de inputs/dropzone.

    **Paso 1 — `apps/website/app/crear/layout.tsx`**: Actualizar el wrapper del form para usar `.form-shell` del diseño:
    ```tsx
    "use client";
    import type { ReactNode } from "react";
    import { FormProvider } from "./FormContext";
    // Layout del diseño líneas 2740-2793:
    // <div className="form-shell">
    //   <aside className="form-side">
    //     <Link href="/"><BrandMark /></Link>
    //     {/* Lista de steps con indicador de progreso (form-step.on / form-step.done) */}
    //   </aside>
    //   <section className="form-main">{children}</section>
    // </div>
    export default function CrearLayout({ children }: { children: ReactNode }) {
      return (
        <FormProvider>
          <div className="form-shell">
            {/* sidebar + main con children */}
          </div>
        </FormProvider>
      );
    }
    ```
    Si el layout actual ya envuelve con FormProvider, solo cambiar las clases CSS y estructura HTML — NO romper el provider.

    **Paso 2 — `apps/website/app/crear/page.tsx`**: Actualizar la página index del form (redirect a Step 1 o landing del form). Si actualmente redirige a `/crear/1`, mantener. Si es landing, actualizar al layout del diseño con clases nuevas.

    **Paso 3 — `apps/website/app/crear/FormContext.tsx`**: NO CAMBIAR la lógica del Context. Solo verificar que sigue exportando `useForm()` con el mismo contrato. Si se actualiza algo, debe ser solo para añadir helpers visuales (ej. `currentStep`, `goToStep`) si no existen.

    **Paso 4 — `apps/website/app/crear/shared.tsx`**: Actualizar los componentes compartidos al nuevo diseño:
    - `Field` / `FieldRow`: clases `.form-field`, `.form-grid`
    - `Dropzone`: clase `.dropzone` del diseño
    - `FormNav`: barra inferior con botones "Anterior" / "Siguiente" / "Guardar" usando `.form-nav`
    - Indicadores visuales (errors, helper texts) con clases del diseño

    **Paso 5 — `apps/website/app/crear/1/Step1Client.tsx`** (diseño líneas 2794-2872): Step 1 — Información básica:
    - Layout: heading + form fields (titulo, categoría, descripción corta, descripción larga markdown)
    - PRESERVAR validación y guardado al context.
    - Clases: `.form-step`, `.form-step.on`, `.form-field`, `.form-grid`.

    **Paso 6 — `apps/website/app/crear/2/Step2Client.tsx`** (diseño líneas 2873-2954): Step 2 — Fechas y ubicación:
    - Fecha desde / hasta, lugar, dirección, ciudad/región, mapa opcional.
    - PRESERVAR validación.

    **Paso 7 — `apps/website/app/crear/3/Step3Client.tsx`** (diseño líneas 2955-3030 + 3031-3094 si Step4 está fusionado en Step3): Step 3 — Multimedia y precios + Revisión:
    - Subida de imagen banner/poster (Dropzone)
    - Galería
    - Tipos de entradas/precios
    - Botón submit final que llama `api.createEvent` o `api.updateEvent` (preservar lógica actual).
    - Si el form actual tiene 3 steps pero el diseño tiene 4 (Step4=revisión separada), MANTENER 3 steps y poner el resumen al final de Step3.
    - PRESERVAR todo: token de useUser, manejo de errores, redirect post-submit, toasts.

    **Clases CSS clave (del diseño):**
    - `.form-shell` (grid sidebar+main)
    - `.form-side`, `.form-main`
    - `.form-steps` (lista de pasos en sidebar)
    - `.form-step`, `.form-step.on`, `.form-step.done`
    - `.form-nav` (barra inferior con prev/next)
    - `.form-field`, `.form-grid` (grid de inputs)
    - `.dropzone` (área de drop de imágenes)

    Estas clases ya están en globals.css (añadidas en Plan 01 Task 1).

    **Regla anti-regresión:** Después de actualizar la UI, hacer un smoke test mental:
    1. ¿`useForm()` sigue funcionando igual? (mismos campos en el state)
    2. ¿El submit final sigue llamando a `api.createEvent`/`api.updateEvent` con el token correcto?
    3. ¿La navegación entre steps sigue funcionando con router.push?
    4. ¿Los errores se muestran (toast o inline)?

    Si alguna respuesta es "no" — está roto el contrato. Revertir y solo cambiar clases CSS.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f apps/website/app/crear/page.tsx && test -f apps/website/app/crear/layout.tsx && test -f apps/website/app/crear/FormContext.tsx && test -f apps/website/app/crear/shared.tsx && test -f apps/website/app/crear/1/Step1Client.tsx && test -f apps/website/app/crear/2/Step2Client.tsx && test -f apps/website/app/crear/3/Step3Client.tsx && grep -q "form-shell\\|form-side\\|form-main" apps/website/app/crear/layout.tsx && grep -q "form-step\\|form-field\\|form-grid" apps/website/app/crear/1/Step1Client.tsx && grep -q "useForm\\|FormContext" apps/website/app/crear/FormContext.tsx && grep -q "api\\.createEvent\\|api\\.updateEvent" apps/website/app/crear/3/Step3Client.tsx && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - **Archivos preservados (no eliminados):**
      - `test -f apps/website/app/crear/page.tsx`
      - `test -f apps/website/app/crear/layout.tsx`
      - `test -f apps/website/app/crear/FormContext.tsx`
      - `test -f apps/website/app/crear/shared.tsx`
      - `test -f apps/website/app/crear/1/Step1Client.tsx`
      - `test -f apps/website/app/crear/2/Step2Client.tsx`
      - `test -f apps/website/app/crear/3/Step3Client.tsx`
    - **Clases CSS del nuevo diseño aplicadas:**
      - `grep -q "form-shell\\|form-side\\|form-main" apps/website/app/crear/layout.tsx` → 0
      - `grep -q "form-step\\|form-field\\|form-grid" apps/website/app/crear/1/Step1Client.tsx` → 0
    - **Contrato del Context preservado:**
      - `grep -q "useForm\\|FormContext" apps/website/app/crear/FormContext.tsx` → 0
    - **Integración API preservada:**
      - `grep -q "api\\.createEvent\\|api\\.updateEvent" apps/website/app/crear/3/Step3Client.tsx` → 0
    - `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    Formulario multi-step crear/ actualizado al diseño FormPage + Step1..3. Clases CSS del diseño aplicadas. Lógica del Context y submit a API preservada. Build pasa.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter website build` debe pasar exit 0.
2. 4 archivos `XxxView.tsx` deben existir (HomeView, CategoryView, SearchView, EventView).
3. Cada `page.tsx` debe importar su `XxxView` correspondiente.
4. Las páginas siguen consumiendo `lib/api.ts` (no se rompió integración).
5. Formulario `/crear/` sigue funcionando end-to-end (crear evento → step 1 → 2 → 3 → submit).
</verification>

<success_criteria>
- HomeView, CategoryView, SearchView, EventView en archivos separados (D-04 cumplido)
- Formulario crear/ actualizado al diseño FormPage + Step1..3 (D-04 cumplido)
- Cada vista rinde el nuevo diseño con sus clases CSS específicas
- Fetch SSR preservado en cada page.tsx
- EventView usa ticketUrl externo (no hay checkout interno reintroducido)
- FormContext y submit a api.createEvent/api.updateEvent preservados
- Build verde
</success_criteria>

<output>
After completion, create `.planning/phases/15-rediseno-ui-migracion-de-vistas/15-02-SUMMARY.md` documenting:
- Estructura final de cada vista (page.tsx + View.tsx)
- Integraciones API preservadas
- Clases CSS del diseño usadas en cada vista
- Cambios en el formulario crear/ (clases CSS aplicadas, contrato del context preservado)
- Limitaciones conocidas (mock data, features de Phase futura)
</output>
