---
phase: 15-rediseno-ui-migracion-de-vistas
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - apps/website/app/dashboard/layout.tsx
  - apps/website/app/dashboard/page.tsx
  - apps/website/app/dashboard/AdminPage.tsx
  - apps/website/app/dashboard/admin.css
  - apps/website/app/dashboard/sections/HomeSection.tsx
  - apps/website/app/dashboard/sections/EventsSection.tsx
  - apps/website/app/dashboard/sections/ArticlesSection.tsx
  - apps/website/app/dashboard/sections/UsersSection.tsx
  - apps/website/app/dashboard/sections/PaymentsSection.tsx
  - apps/website/app/dashboard/sections/SubsSection.tsx
  - apps/website/app/dashboard/sections/InboxSection.tsx
  - apps/website/app/dashboard/sections/CRMSection.tsx
  - apps/website/app/dashboard/sections/CategoriesSection.tsx
  - apps/website/app/dashboard/sections/SpotsSection.tsx
  - apps/website/app/dashboard/sections/HeroesSection.tsx
  - apps/website/app/dashboard/sections/FAQSection.tsx
  - apps/website/app/dashboard/sections/ReportsSection.tsx
  - apps/website/app/dashboard/sections/LogsSection.tsx
  - apps/website/app/dashboard/sections/SettingsSection.tsx
  - apps/website/app/dashboard/modals/AdminFormModal.tsx
  - apps/website/app/dashboard/modals/AdminApproveModal.tsx
  - apps/website/app/dashboard/modals/AdminRejectModal.tsx
  - apps/website/app/dashboard/modals/AdminTransferModal.tsx
  - apps/website/app/dashboard/modals/AdminEventEditor.tsx
  - apps/website/app/dashboard/modals/AdminArticleEditor.tsx
autonomous: true
requirements:
  - UI-MIG-03
  - UI-ADMIN
must_haves:
  truths:
    - "Las 8 sub-rutas viejas de dashboard están eliminadas (events/, categories/, users/, payments/, settings/, logs/, reports/, help/)"
    - "Los 6 componentes admin viejos están eliminados (AdminSidebar, AdminTopbar, KpiCard, RevenueChart, PlaceholderView, icons)"
    - "dashboard/layout.tsx solo wrapea con AdminGuard (no AdminSidebar/AdminTopbar)"
    - "dashboard/page.tsx renderiza AdminPage con ?section= query param"
    - "AdminPage tiene sidebar con grupos y secciones del diseño"
    - "EventsSection conserva approve/reject via api.approveEvent/api.rejectEvent (D-14)"
    - "ArticlesSection conserva approve/reject via api.approveArticle/api.rejectArticle"
    - "admin.css contiene .admin-shell, .admin-side, .admin-main (no .admin/.sidebar/.content viejos)"
    - "pnpm --filter website build pasa exit 0"
  artifacts:
    - path: "apps/website/app/dashboard/AdminPage.tsx"
      provides: "Shell admin con sidebar + ruteo por ?section="
      contains: "useSearchParams"
    - path: "apps/website/app/dashboard/sections/EventsSection.tsx"
      provides: "Sección eventos con integraciones reales API"
      contains: "api.approveEvent"
    - path: "apps/website/app/dashboard/admin.css"
      provides: "CSS del nuevo admin shell"
      contains: ".admin-shell"
  key_links:
    - from: "apps/website/app/dashboard/page.tsx"
      to: "AdminPage"
      via: "import AdminPage from './AdminPage'"
      pattern: "AdminPage"
    - from: "apps/website/app/dashboard/AdminPage.tsx"
      to: "useSearchParams (next/navigation)"
      via: "client component"
      pattern: "useSearchParams"
    - from: "apps/website/app/dashboard/sections/EventsSection.tsx"
      to: "api.adminEvents / api.approveEvent / api.rejectEvent"
      via: "import { api } from '@/lib/api'"
      pattern: "api\\.(approveEvent|rejectEvent|adminEvents)"
---

<objective>
Wave 3 (paralelo con Plan 04): REESCRIBIR COMPLETO el admin panel `/dashboard/` siguiendo el nuevo diseño. Tres tareas:

1. **Demolición + nuevo shell**: Eliminar todas las sub-rutas viejas, los 6 componentes admin viejos y reescribir layout.tsx + page.tsx + AdminPage.tsx + admin.css.
2. **Secciones con API real**: EventsSection, ArticlesSection, UsersSection, CategoriesSection, SpotsSection, HeroesSection, LogsSection — preservan llamadas a `api.*` ya existentes (D-14).
3. **Secciones UI-only + modales**: PaymentsSection, SubsSection, InboxSection, CRMSection, FAQSection, ReportsSection, SettingsSection + 6 modales — UI fiel al diseño, mock data donde no hay API.

El nuevo admin es un SPA: `/dashboard?section=events` carga EventsSection en el mismo `dashboard/page.tsx`. Cambios de sección via `<Link>` de Next.js, NO sub-routes.

**Dependencia con Plan 02:** este plan depende de 15-02 además de 15-01 porque ambos consumen el mismo conjunto de clases CSS del rediseño (Plan 02 establece las clases de listado/tabla compartidas con admin). Wave 3 = max(wave_01=1, wave_02=2) + 1 = 3.

Purpose: actualizar el admin al rediseño completo del diseño preservando las integraciones API reales.
Output: admin panel rediseñado + 14 secciones + 6 modales en archivos separados.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-CONTEXT.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-RESEARCH.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-01-PLAN.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-02-PLAN.md

# Diseño fuente (admin completo)
@design/Konbini.html

# Estado actual del admin (referencia para preservar integraciones)
@apps/website/app/dashboard/page.tsx
@apps/website/app/dashboard/layout.tsx
@apps/website/app/dashboard/events/page.tsx
@apps/website/app/dashboard/users/page.tsx
@apps/website/app/dashboard/categories/page.tsx
@apps/website/app/dashboard/settings/page.tsx
@apps/website/app/dashboard/logs/page.tsx
@apps/website/app/dashboard/admin.css

# Componentes admin actuales (a eliminar)
@apps/website/components/admin/AdminSidebar.tsx
@apps/website/components/admin/AdminTopbar.tsx
@apps/website/components/admin/AdminGuard.tsx

# Capa de datos (NO TOCAR)
@apps/website/lib/api.ts

<interfaces>
APIs disponibles del proyecto (de `lib/api.ts`) que las secciones consumen:
```typescript
api.adminEvents(token, { pageSize?, status? }): Promise<ApiEventList>;
api.approveEvent(id, token): Promise<void>;
api.rejectEvent(id, reason, token): Promise<void>;
api.adminArticles?(token): Promise<ApiArticleList>;  // Verificar Phase 13
api.adminUsers(token): Promise<ApiUserList>;
api.adminCategories(token): Promise<ApiCategory[]>;
api.adminSpots(token): Promise<ApiSpotList>;
api.adminHeroes(token): Promise<ApiHeroList>;
api.adminLogs(token, filters): Promise<ApiLogList>;
api.adminSettings(token): Promise<Settings[]>;
```
Si alguna función no existe, usar fetch directo con el token del header `Authorization: Bearer ${token}` siguiendo el patrón de `lib/api.ts`.

From Plan 01:
```typescript
import { Ic } from "@/components/icons";
import { useUser } from "@/components/providers";
import { BrandMark } from "@/components/BrandMark";
import { toast } from "sonner";
```

ADMIN_NAV structure (extraer del diseño):
```typescript
const ADMIN_NAV = [
  { grp: "OPERACIÓN", items: [
    { id: "home", label: "Inicio", ic: "◉" },
    { id: "events", label: "Eventos", ic: "▦" },
    { id: "articles", label: "Artículos", ic: "▤" },
    { id: "users", label: "Usuarios", ic: "◎" },
  ]},
  { grp: "COMERCIO", items: [
    { id: "payments", label: "Pagos", ic: "$" },
    { id: "subs", label: "Suscripciones", ic: "★" },
  ]},
  { grp: "MENSAJES", items: [
    { id: "contact", label: "Contacto", ic: "✉" },
    { id: "photo", label: "Fotografía", ic: "◐" },
    { id: "creators", label: "Creadores", ic: "◑" },
    { id: "crm", label: "CRM", ic: "▣" },
  ]},
  { grp: "CATÁLOGO", items: [
    { id: "categories", label: "Categorías", ic: "#" },
    { id: "spots", label: "Avisos", ic: "▰" },
    { id: "heroes", label: "Portadas", ic: "▶" },
    { id: "faq", label: "FAQ", ic: "?" },
  ]},
  { grp: "SISTEMA", items: [
    { id: "reports", label: "Reportes", ic: "△" },
    { id: "logs", label: "Logs", ic: "≡" },
    { id: "settings", label: "Configuración", ic: "⚙" },
  ]},
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demolición + nuevo shell del admin (layout, page, AdminPage, admin.css)</name>
  <files>apps/website/app/dashboard/layout.tsx, apps/website/app/dashboard/page.tsx, apps/website/app/dashboard/AdminPage.tsx, apps/website/app/dashboard/admin.css</files>
  <read_first>
    - apps/website/app/dashboard/layout.tsx (versión actual con AdminSidebar/AdminTopbar — REEMPLAZAR todo)
    - apps/website/app/dashboard/page.tsx (versión actual con KPIs mock — REEMPLAZAR todo)
    - apps/website/app/dashboard/admin.css (CSS actual con .admin/.sidebar/.content — REEMPLAZAR todo)
    - apps/website/components/admin/AdminGuard.tsx (NO TOCAR — se sigue usando)
    - design/Konbini.html lines 5508-5599 (AdminPage shell del diseño con .admin-shell/.admin-side/.admin-main + ADMIN_NAV)
    - design/Konbini.html lines 1380-1495 (CSS del admin del diseño — .admin-shell, .admin-side, .admin-main, .nav-item, .grp, .brand-row, .who, .crumb)
  </read_first>
  <action>
    Paso 1 — BORRAR sub-rutas viejas del admin. Ejecutar:
    ```bash
    rm -f apps/website/app/dashboard/events/page.tsx
    rm -f apps/website/app/dashboard/users/page.tsx
    rm -f apps/website/app/dashboard/categories/page.tsx
    rm -f apps/website/app/dashboard/payments/page.tsx
    rm -f apps/website/app/dashboard/settings/page.tsx
    rm -f apps/website/app/dashboard/logs/page.tsx
    rm -f apps/website/app/dashboard/reports/page.tsx
    rm -f apps/website/app/dashboard/help/page.tsx
    rmdir apps/website/app/dashboard/events 2>/dev/null
    rmdir apps/website/app/dashboard/users 2>/dev/null
    rmdir apps/website/app/dashboard/categories 2>/dev/null
    rmdir apps/website/app/dashboard/payments 2>/dev/null
    rmdir apps/website/app/dashboard/settings 2>/dev/null
    rmdir apps/website/app/dashboard/logs 2>/dev/null
    rmdir apps/website/app/dashboard/reports 2>/dev/null
    rmdir apps/website/app/dashboard/help 2>/dev/null
    ```

    Paso 2 — BORRAR componentes admin viejos:
    ```bash
    rm -f apps/website/components/admin/AdminSidebar.tsx
    rm -f apps/website/components/admin/AdminTopbar.tsx
    rm -f apps/website/components/admin/KpiCard.tsx
    rm -f apps/website/components/admin/RevenueChart.tsx
    rm -f apps/website/components/admin/PlaceholderView.tsx
    rm -f apps/website/components/admin/icons.tsx
    ```
    NO borrar `apps/website/components/admin/AdminGuard.tsx` — se preserva.

    Paso 3 — REESCRIBIR `apps/website/app/dashboard/layout.tsx`:
    ```tsx
    import type { Metadata } from "next";
    import type { ReactNode } from "react";
    import { AdminGuard } from "@/components/admin/AdminGuard";
    import "./admin.css";

    export const metadata: Metadata = { title: "Konbini · Admin" };

    export default function AdminLayout({ children }: { children: ReactNode }) {
      return <AdminGuard>{children}</AdminGuard>;
    }
    ```
    NOTA: NO incluye `<div className="admin">` ni AdminSidebar/AdminTopbar — el shell vive en AdminPage.tsx.

    Paso 4 — REESCRIBIR `apps/website/app/dashboard/page.tsx`:
    ```tsx
    import { Suspense } from "react";
    import AdminPage from "./AdminPage";

    export default function DashboardPage() {
      return (
        <Suspense fallback={<div style={{ padding: 40 }}>Cargando…</div>}>
          <AdminPage />
        </Suspense>
      );
    }
    ```
    El Suspense es necesario porque AdminPage usa `useSearchParams()`.

    Paso 5 — CREAR `apps/website/app/dashboard/AdminPage.tsx` (NUEVO):
    ```tsx
    "use client";
    import { useSearchParams } from "next/navigation";
    import Link from "next/link";
    import { useState, useEffect } from "react";
    import { BrandMark } from "@/components/BrandMark";
    import { Ic } from "@/components/icons";
    import { useUser } from "@/components/providers";

    import HomeSection from "./sections/HomeSection";
    import EventsSection from "./sections/EventsSection";
    import ArticlesSection from "./sections/ArticlesSection";
    import UsersSection from "./sections/UsersSection";
    import PaymentsSection from "./sections/PaymentsSection";
    import SubsSection from "./sections/SubsSection";
    import InboxSection from "./sections/InboxSection";
    import CRMSection from "./sections/CRMSection";
    import CategoriesSection from "./sections/CategoriesSection";
    import SpotsSection from "./sections/SpotsSection";
    import HeroesSection from "./sections/HeroesSection";
    import FAQSection from "./sections/FAQSection";
    import ReportsSection from "./sections/ReportsSection";
    import LogsSection from "./sections/LogsSection";
    import SettingsSection from "./sections/SettingsSection";

    const ADMIN_NAV = [
      // Estructura del diseño líneas 5508-5599 (extraer EXACTO)
      { grp: "OPERACIÓN", items: [
        { id: "home", label: "Inicio", ic: "◉", crumb: "INICIO" },
        { id: "events", label: "Eventos", ic: "▦", crumb: "EVENTOS" },
        { id: "articles", label: "Artículos", ic: "▤", crumb: "ARTÍCULOS" },
        { id: "users", label: "Usuarios", ic: "◎", crumb: "USUARIOS" },
      ]},
      // ... resto de grupos (COMERCIO, MENSAJES, CATÁLOGO, SISTEMA)
    ];

    const SECTIONS: Record<string, React.ComponentType<{ kind?: string }>> = {
      home: HomeSection,
      events: EventsSection,
      articles: ArticlesSection,
      users: UsersSection,
      payments: PaymentsSection,
      subs: SubsSection,
      contact: InboxSection,
      photo: InboxSection,
      creators: InboxSection,
      crm: CRMSection,
      categories: CategoriesSection,
      spots: SpotsSection,
      heroes: HeroesSection,
      faq: FAQSection,
      reports: ReportsSection,
      logs: LogsSection,
      settings: SettingsSection,
    };

    export default function AdminPage() {
      const params = useSearchParams();
      const sel = params.get("section") ?? "home";
      const cur = ADMIN_NAV.flatMap(g => g.items).find(i => i.id === sel) ?? ADMIN_NAV[0].items[0];
      const [openGroups, setOpenGroups] = useState<Record<string, boolean>>(() => {
        const init: Record<string, boolean> = {};
        ADMIN_NAV.forEach(g => { init[g.grp] = true; });
        return init;
      });
      const Section = SECTIONS[sel] ?? HomeSection;
      const inboxKind = sel === "photo" ? "photo" : sel === "creators" ? "creators" : "contact";

      return (
        <div className="admin-shell">
          <aside className="admin-side">
            <div className="brand-row">
              <Link href="/"><BrandMark /></Link>
              <div className="role">SUPER ADMIN</div>
            </div>
            <div className="scroll">
              {ADMIN_NAV.map(g => (
                <div key={g.grp}>
                  <button className={`grp ${openGroups[g.grp] ? "open" : ""}`}
                          onClick={() => setOpenGroups(o => ({ ...o, [g.grp]: !o[g.grp] }))}>
                    <span className="chev">▶</span>
                    <span>{g.grp}</span>
                    <span style={{ marginLeft: "auto" }}>{g.items.length}</span>
                  </button>
                  <div className={`grp-items ${!openGroups[g.grp] ? "closed" : ""}`}>
                    {g.items.map(i => (
                      <Link key={i.id} href={`/dashboard?section=${i.id}`}
                            className={`nav-item ${sel === i.id ? "on" : ""}`}>
                        <span style={{ width: 18, textAlign: "center" }}>{i.ic}</span>
                        <span>{i.label}</span>
                      </Link>
                    ))}
                  </div>
                </div>
              ))}
            </div>
            <div className="who">
              {/* avatar + name + email del user */}
            </div>
          </aside>
          <div className="admin-main">
            <div className="admin-top">
              <div>
                <div className="crumb">DASHBOARD / {cur.crumb ?? cur.label.toUpperCase()}</div>
                <h1>{cur.label}</h1>
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <Link className="icon-btn" href="/" title="Ir al sitio">↗</Link>
                <button className="icon-btn">{Ic.search}</button>
                <div className="avatar">ET</div>
              </div>
            </div>
            <div className="admin-body">
              <Section kind={inboxKind} />
            </div>
          </div>
        </div>
      );
    }
    ```

    Paso 6 — REESCRIBIR COMPLETAMENTE `apps/website/app/dashboard/admin.css`. BORRAR todo el contenido viejo y poner el CSS del nuevo admin extraído de `design/Konbini.html` líneas 1380-1495. Incluir como mínimo:
    - `.admin-shell` (grid 260px 1fr min-height 100vh)
    - `.admin-side` (sticky top 0 height 100vh overflow-y auto bg var(--bg-2))
    - `.admin-main` (flex column overflow hidden)
    - `.admin-top` (border-bottom, padding, h1 styles)
    - `.admin-body` (flex 1 overflow-y auto padding)
    - `.brand-row`, `.role`, `.scroll`, `.who`, `.who .av`, `.who .nm`, `.who .em`
    - `.grp` (button collapsible), `.grp.open`, `.grp .chev`, `.grp-items`, `.grp-items.closed`
    - `.nav-item`, `.nav-item.on`, `.nav-item.super`, `.nav-item .ct`
    - `.crumb`, `.avatar`, `.icon-btn`
    - `.kpi-grid`, `.kpi`, `.kpi .l`, `.kpi .v`, `.kpi .d`, `.kpi .d.up`
    - `.panel`, `.panel .ph`, `.chart`, `.chart .bar`, `.bar .lbl`
    - `.table-wrap`, `.evt` (tabla), `.cell-evt`, `.cell-prod`, `.cell-date`, `.cell-price`, `.row-acts`, `.stat`, `.stat.rev`, `.stat.pub`, `.stat.rej`
    - `.filterbar`, `.search-shell`, `.empty`
    - `.confirm-bg`, `.confirm-card`

    NO incluir las reglas viejas `.admin`, `aside.sidebar`, `.sb-brand`, `.sb-link`, `main.content`, etc.

    Algunas de estas clases ya están en globals.css (Plan 01 Task 1) — si están duplicadas, MANTENER las de admin.css con scope local. No es problema: el cascade resuelve y el archivo se importa solo en dashboard/layout.tsx.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && ! test -f "apps/website/app/dashboard/events/page.tsx" && ! test -f "apps/website/app/dashboard/users/page.tsx" && ! test -f "apps/website/app/dashboard/categories/page.tsx" && ! test -f "apps/website/app/dashboard/payments/page.tsx" && ! test -f "apps/website/app/dashboard/settings/page.tsx" && ! test -f "apps/website/app/dashboard/logs/page.tsx" && ! test -f "apps/website/app/dashboard/reports/page.tsx" && ! test -f "apps/website/app/dashboard/help/page.tsx" && ! test -f "apps/website/components/admin/AdminSidebar.tsx" && ! test -f "apps/website/components/admin/AdminTopbar.tsx" && ! test -f "apps/website/components/admin/KpiCard.tsx" && ! test -f "apps/website/components/admin/RevenueChart.tsx" && ! test -f "apps/website/components/admin/PlaceholderView.tsx" && ! test -f "apps/website/components/admin/icons.tsx" && test -f "apps/website/components/admin/AdminGuard.tsx" && test -f "apps/website/app/dashboard/AdminPage.tsx" && grep -q "useSearchParams" apps/website/app/dashboard/AdminPage.tsx && grep -q "admin-shell" apps/website/app/dashboard/admin.css && grep -q "admin-shell\\|admin-side\\|admin-main" apps/website/app/dashboard/AdminPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - **Sub-rutas viejas eliminadas (negative criteria):**
      - `! test -f "apps/website/app/dashboard/events/page.tsx"`
      - `! test -f "apps/website/app/dashboard/users/page.tsx"`
      - `! test -f "apps/website/app/dashboard/categories/page.tsx"`
      - `! test -f "apps/website/app/dashboard/payments/page.tsx"`
      - `! test -f "apps/website/app/dashboard/settings/page.tsx"`
      - `! test -f "apps/website/app/dashboard/logs/page.tsx"`
      - `! test -f "apps/website/app/dashboard/reports/page.tsx"`
      - `! test -f "apps/website/app/dashboard/help/page.tsx"`
    - **Componentes viejos eliminados (negative criteria):**
      - `! test -f "apps/website/components/admin/AdminSidebar.tsx"`
      - `! test -f "apps/website/components/admin/AdminTopbar.tsx"`
      - `! test -f "apps/website/components/admin/KpiCard.tsx"`
      - `! test -f "apps/website/components/admin/RevenueChart.tsx"`
      - `! test -f "apps/website/components/admin/PlaceholderView.tsx"`
      - `! test -f "apps/website/components/admin/icons.tsx"`
    - **AdminGuard preservado:** `test -f "apps/website/components/admin/AdminGuard.tsx"`
    - **Nuevo shell creado:** `test -f "apps/website/app/dashboard/AdminPage.tsx"`
    - `grep -q "useSearchParams" apps/website/app/dashboard/AdminPage.tsx`
    - `grep -q "admin-shell" apps/website/app/dashboard/AdminPage.tsx`
    - `grep -q "admin-shell" apps/website/app/dashboard/admin.css`
    - `! grep -q "AdminSidebar\\|AdminTopbar" apps/website/app/dashboard/layout.tsx` (imports viejos eliminados)
    - `grep -q "AdminGuard" apps/website/app/dashboard/layout.tsx`
    - NOTA: build no se ejecuta aquí porque sections/ aún no existen (Task 2/3 las crean) — el build se verifica al final de Task 3.
  </acceptance_criteria>
  <done>
    Sub-rutas y componentes admin viejos eliminados. layout.tsx solo wrapea con AdminGuard. page.tsx renderiza AdminPage. AdminPage tiene shell con sidebar grupos + secciones via useSearchParams. admin.css contiene CSS del nuevo diseño.
  </done>
</task>

<task type="auto">
  <name>Task 2: Secciones con integraciones API reales (Events, Articles, Users, Categories, Spots, Heroes, Logs)</name>
  <files>apps/website/app/dashboard/sections/EventsSection.tsx, apps/website/app/dashboard/sections/ArticlesSection.tsx, apps/website/app/dashboard/sections/UsersSection.tsx, apps/website/app/dashboard/sections/CategoriesSection.tsx, apps/website/app/dashboard/sections/SpotsSection.tsx, apps/website/app/dashboard/sections/HeroesSection.tsx, apps/website/app/dashboard/sections/LogsSection.tsx, apps/website/app/dashboard/sections/HomeSection.tsx</files>
  <read_first>
    - apps/website/app/dashboard/events/page.tsx (lógica de aprobación/rechazo ANTES de borrarlo en Task 1 — leer en HEAD si ya fue borrado: `git show HEAD:apps/website/app/dashboard/events/page.tsx`)
    - apps/website/app/dashboard/users/page.tsx (lógica admin users)
    - apps/website/app/dashboard/categories/page.tsx (lógica CRUD categorías)
    - apps/website/app/dashboard/logs/page.tsx (consumo de api.adminLogs)
    - apps/website/lib/api.ts (todas las funciones admin*)
    - design/Konbini.html lines 5601-5680 (AdminHome del diseño)
    - design/Konbini.html lines 5681-5785 (AdminTable kind="events"/"articles"/"users")
    - design/Konbini.html lines 6046-6104 (AdminCategories del diseño)
    - design/Konbini.html lines 6105-6150 (AdminSpots + AdminHeroes)
    - design/Konbini.html lines 6235-6268 (AdminLogs)
  </read_first>
  <action>
    Crear las 8 secciones en `apps/website/app/dashboard/sections/`. Cada archivo:
    - "use client"
    - Importa `useUser` para obtener el token
    - Llama a la API real con `useEffect`
    - Usa las clases CSS del nuevo diseño (.kpi, .table-wrap, .evt, .stat, etc.)
    - Muestra `toast` de sonner en éxito/error
    - Exporta default (para que AdminPage.tsx importe limpio)

    **Paso 1 — `HomeSection.tsx`** (extraer diseño líneas 5601-5680):
    ```tsx
    "use client";
    import { useEffect, useState } from "react";
    import { useUser } from "@/components/providers";
    import { api, type ApiEvent } from "@/lib/api";

    export default function HomeSection() {
      const { token } = useUser();
      const [events, setEvents] = useState<ApiEvent[]>([]);
      const [loading, setLoading] = useState(true);
      useEffect(() => {
        if (!token) return;
        api.adminEvents(token, { pageSize: 100 })
          .then(r => setEvents(r.items))
          .finally(() => setLoading(false));
      }, [token]);
      const aprobados = events.filter(e => e.isApproved && !e.isRejected).length;
      const pendientes = events.filter(e => !e.isApproved && !e.isRejected).length;
      return (
        <>
          <div className="kpi-grid">
            <div className="kpi"><div className="l">EVENTOS PUBLICADOS</div><div className="v">{aprobados}</div></div>
            <div className="kpi"><div className="l">EN REVISIÓN</div><div className="v">{pendientes}</div></div>
            <div className="kpi"><div className="l">INGRESOS MES</div><div className="v">$3.8M</div></div>
            <div className="kpi"><div className="l">SUSCRIPTORES</div><div className="v">87</div></div>
          </div>
          {/* Panel de cola de revisión + Panel por categoría (del diseño líneas 5611-5680) */}
        </>
      );
    }
    ```

    **Paso 2 — `EventsSection.tsx`** (PRESERVAR lógica de `events/page.tsx` viejo):
    ```tsx
    "use client";
    import { useEffect, useMemo, useState } from "react";
    import Link from "next/link";
    import { toast } from "sonner";
    import { useUser } from "@/components/providers";
    import { Ic } from "@/components/icons";
    import { api, imageUrl, type ApiEvent } from "@/lib/api";

    type Status = "rev" | "pub" | "rej";
    const STAT_LABEL: Record<Status, string> = { rev: "En revisión", pub: "Publicado", rej: "Rechazado" };
    function statusOf(e: ApiEvent): Status {
      if (e.isRejected) return "rej";
      if (e.isApproved) return "pub";
      return "rev";
    }

    export default function EventsSection() {
      const { token } = useUser();
      const [events, setEvents] = useState<ApiEvent[]>([]);
      const [loading, setLoading] = useState(true);
      const [search, setSearch] = useState("");
      const [statFilter, setStatFilter] = useState<"all" | Status>("all");
      const [busy, setBusy] = useState<number | null>(null);

      useEffect(() => {
        if (!token) return;
        api.adminEvents(token, { pageSize: 100 })
          .then(r => setEvents(r.items))
          .catch(e => toast.error(e instanceof Error ? e.message : "Error al cargar"))
          .finally(() => setLoading(false));
      }, [token]);

      const filtered = useMemo(() => {
        let res = events;
        if (statFilter !== "all") res = res.filter(e => statusOf(e) === statFilter);
        if (search) {
          const q = search.toLowerCase();
          res = res.filter(e => e.title.toLowerCase().includes(q) || (e.company ?? "").toLowerCase().includes(q));
        }
        return res;
      }, [events, statFilter, search]);

      const approve = async (e: ApiEvent) => {
        if (!token) return;
        setBusy(e.id);
        try {
          await api.approveEvent(e.id, token);
          setEvents(list => list.map(x => x.id === e.id ? { ...x, isApproved: true, isRejected: false, rejectedReason: null } : x));
          toast.success("Evento aprobado");
        } catch (ex) {
          toast.error(ex instanceof Error ? ex.message : "Error al aprobar");
        } finally { setBusy(null); }
      };

      const reject = async (e: ApiEvent) => {
        if (!token) return;
        const reason = window.prompt("Motivo del rechazo:");
        if (reason === null) return;
        if (reason.trim().length < 3) {
          toast.error("Motivo: mínimo 3 caracteres");
          return;
        }
        setBusy(e.id);
        try {
          await api.rejectEvent(e.id, reason.trim(), token);
          setEvents(list => list.map(x => x.id === e.id ? { ...x, isApproved: false, isRejected: true, rejectedReason: reason.trim() } : x));
          toast.success("Evento rechazado");
        } catch (ex) {
          toast.error(ex instanceof Error ? ex.message : "Error al rechazar");
        } finally { setBusy(null); }
      };

      return (
        <>
          {/* filterbar con search + select por status */}
          {/* table-wrap > table.evt con thead/tbody según diseño líneas 5681-5785 */}
          {/* Por cada fila: cell-evt (thumb+título), cell-prod, cell-date, cell-price, stat, row-acts (Ver/Aprobar/Rechazar) */}
        </>
      );
    }
    ```

    **Paso 3 — `ArticlesSection.tsx`** (similar a Events, integración con `api.adminArticles` + `api.approveArticle` + `api.rejectArticle` — Phase 13 CNT-01).
    Si las funciones no existen en `lib/api.ts`, usar fetch directo:
    ```typescript
    fetch(`${API_URL}/api/articles/admin?status=DRAFT`, { headers: { Authorization: `Bearer ${token}` } })
    fetch(`${API_URL}/api/articles/${id}/approve`, { method: "PATCH", headers: { Authorization: `Bearer ${token}` } })
    fetch(`${API_URL}/api/articles/${id}/reject`, { method: "PATCH", body: JSON.stringify({ reason }), ... })
    ```

    **Paso 4 — `UsersSection.tsx`** (preserva CRUD de `users/page.tsx` viejo).
    `api.adminUsers(token)`, `api.adminUserCreate/Update/Delete/Ban`.

    **Paso 5 — `CategoriesSection.tsx`** (preserva CRUD de `categories/page.tsx` viejo, con campos v2: icon, color, minDays, maxDays, order — Phase 13 CNT-04).
    `api.adminCategories(token)`, `api.adminCategoryCreate/Update/Delete`.

    **Paso 6 — `SpotsSection.tsx`** (CRUD de avisos con `api.adminSpots`).

    **Paso 7 — `HeroesSection.tsx`** (CRUD de portadas con `api.adminHeroes`).

    **Paso 8 — `LogsSection.tsx`** (lectura de `api.adminLogs(token, filters)` con filtros entity/action/dateFrom/dateTo — Phase 7).

    Reglas generales:
    - Cada sección rinde el layout del diseño con clases CSS de admin.css/globals.css
    - Cada acción muestra toast (`toast.success` / `toast.error`)
    - Cada error de API se atrapa con try/catch
    - Loading states: mensaje "Cargando…" o skeleton mínimo
    - Empty state: div className="empty" con h3 + p
    - NO usar `useRouter` del diseño-mock — usar `next/navigation`
    - El export default es OBLIGATORIO (AdminPage importa con `import X from './sections/X'`)
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/dashboard/sections/HomeSection.tsx" && test -f "apps/website/app/dashboard/sections/EventsSection.tsx" && test -f "apps/website/app/dashboard/sections/ArticlesSection.tsx" && test -f "apps/website/app/dashboard/sections/UsersSection.tsx" && test -f "apps/website/app/dashboard/sections/CategoriesSection.tsx" && test -f "apps/website/app/dashboard/sections/SpotsSection.tsx" && test -f "apps/website/app/dashboard/sections/HeroesSection.tsx" && test -f "apps/website/app/dashboard/sections/LogsSection.tsx" && grep -q "api.approveEvent" apps/website/app/dashboard/sections/EventsSection.tsx && grep -q "api.rejectEvent" apps/website/app/dashboard/sections/EventsSection.tsx && grep -q "api.adminUsers\\|adminUsers" apps/website/app/dashboard/sections/UsersSection.tsx && grep -q "approve\\|reject" apps/website/app/dashboard/sections/ArticlesSection.tsx && grep -q "from \"sonner\"" apps/website/app/dashboard/sections/EventsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Los 8 archivos existen:
      - `test -f "apps/website/app/dashboard/sections/HomeSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/EventsSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/ArticlesSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/UsersSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/CategoriesSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/SpotsSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/HeroesSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/LogsSection.tsx"`
    - **Integraciones API preservadas:**
      - `grep -q "api.approveEvent" apps/website/app/dashboard/sections/EventsSection.tsx` → 0
      - `grep -q "api.rejectEvent" apps/website/app/dashboard/sections/EventsSection.tsx` → 0
      - `grep -q "api.adminEvents" apps/website/app/dashboard/sections/EventsSection.tsx` → 0
      - `grep -q "adminUsers" apps/website/app/dashboard/sections/UsersSection.tsx` → 0
      - `grep -q "adminCategories" apps/website/app/dashboard/sections/CategoriesSection.tsx` → 0
      - `grep -q "approve\\|reject" apps/website/app/dashboard/sections/ArticlesSection.tsx` → 0
      - `grep -q "adminLogs" apps/website/app/dashboard/sections/LogsSection.tsx` → 0
    - `grep -q "from \"sonner\"" apps/website/app/dashboard/sections/EventsSection.tsx` → 0 (toasts)
    - Cada archivo es client component: `grep -l "\"use client\"" apps/website/app/dashboard/sections/*.tsx | wc -l` ≥ 8
    - NOTA: build se verifica al final de Task 3 (cuando las 14 secciones existan).
  </acceptance_criteria>
  <done>
    8 secciones con API real creadas en `dashboard/sections/`. Cada una preserva las llamadas a `api.*` del admin viejo. Cada acción muestra toast. Loading/empty states implementados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Secciones UI-only + 6 modales + verificación build final</name>
  <files>apps/website/app/dashboard/sections/PaymentsSection.tsx, apps/website/app/dashboard/sections/SubsSection.tsx, apps/website/app/dashboard/sections/InboxSection.tsx, apps/website/app/dashboard/sections/CRMSection.tsx, apps/website/app/dashboard/sections/FAQSection.tsx, apps/website/app/dashboard/sections/ReportsSection.tsx, apps/website/app/dashboard/sections/SettingsSection.tsx, apps/website/app/dashboard/modals/AdminFormModal.tsx, apps/website/app/dashboard/modals/AdminApproveModal.tsx, apps/website/app/dashboard/modals/AdminRejectModal.tsx, apps/website/app/dashboard/modals/AdminTransferModal.tsx, apps/website/app/dashboard/modals/AdminEventEditor.tsx, apps/website/app/dashboard/modals/AdminArticleEditor.tsx</files>
  <read_first>
    - apps/website/app/dashboard/settings/page.tsx (lógica de settings actual ANTES de Task 1 — `git show HEAD:apps/website/app/dashboard/settings/page.tsx` si ya borrado)
    - apps/website/lib/api.ts (funciones que existan: adminSettings, adminFaq, etc.)
    - design/Konbini.html lines 5786-5878 (AdminPayments)
    - design/Konbini.html lines 5879-5921 (AdminSubs)
    - design/Konbini.html lines 5922-6005 (AdminInbox con kinds contact/photo/creators)
    - design/Konbini.html lines 6006-6045 (AdminCRM con kanban)
    - design/Konbini.html lines 6151-6190 (AdminFAQ)
    - design/Konbini.html lines 6191-6234 (AdminReports)
    - design/Konbini.html lines 6269-6310 (AdminSettings)
    - design/Konbini.html (modales: buscar AdminFormModal, AdminApproveModal, AdminRejectModal, AdminTransferModal, AdminEventEditor, AdminArticleEditor en el archivo)
  </read_first>
  <action>
    Paso 1 — Crear las 7 secciones UI-only en `apps/website/app/dashboard/sections/`:

    **`PaymentsSection.tsx`** (UI-only, mock data — D-14):
    ```tsx
    "use client";
    export default function PaymentsSection() {
      // Estructura del diseño líneas 5786-5878: tabla de pagos con filtros, KPIs de ingresos.
      // Mock data hard-codeada para visualización.
      return (<>{/* layout fiel al diseño */}</>);
    }
    ```

    **`SubsSection.tsx`** (UI-only):
    ```tsx
    "use client";
    export default function SubsSection() {
      // Estructura líneas 5879-5921: tabla de suscriptores, status, ciclos, créditos.
      // Mock data.
      return (<>{/* layout */}</>);
    }
    ```

    **`InboxSection.tsx`** (recibe `kind?: "contact" | "photo" | "creators"`):
    ```tsx
    "use client";
    import { useUser } from "@/components/providers";
    import { useEffect, useState } from "react";

    export default function InboxSection({ kind = "contact" }: { kind?: string }) {
      const { token } = useUser();
      // Llamar a la API correspondiente:
      // - kind="contact" → fetch /api/contact (ADMIN)
      // - kind="photo" → fetch /api/services/photography (ADMIN)
      // - kind="creators" → fetch /api/services/content-creators (ADMIN)
      // Layout del diseño líneas 5922-6005: lista de mensajes/solicitudes con detalle expandible.
      return (<>{/* layout */}</>);
    }
    ```

    **`CRMSection.tsx`** (consumo opcional de `/api/crm` ADMIN — Phase 14):
    ```tsx
    "use client";
    export default function CRMSection() {
      // Kanban del diseño líneas 6006-6045: columnas NEW/CONTACTED/QUALIFIED/PROPOSAL/WON/LOST
      // Fetch /api/crm (paginado, filtros ?type=, ?stage=)
      // Drag-and-drop entre columnas → PATCH /api/crm/:id/stage
      return (<>{/* layout kanban */}</>);
    }
    ```

    **`FAQSection.tsx`** (UI-only, mock):
    ```tsx
    "use client";
    export default function FAQSection() {
      // Líneas 6151-6190: lista de FAQs con add/edit/delete.
      return (<>{/* layout */}</>);
    }
    ```

    **`ReportsSection.tsx`** (UI-only, charts mock):
    ```tsx
    "use client";
    export default function ReportsSection() {
      // Líneas 6191-6234: paneles de reportes con bar charts CSS.
      return (<>{/* layout */}</>);
    }
    ```

    **`SettingsSection.tsx`** (PRESERVAR lógica de `settings/page.tsx` viejo si existe — admin GET/PATCH a `/api/settings`):
    ```tsx
    "use client";
    import { useUser } from "@/components/providers";
    import { useEffect, useState } from "react";
    import { toast } from "sonner";

    export default function SettingsSection() {
      const { token } = useUser();
      const [settings, setSettings] = useState<Array<{ key: string; value: string }>>([]);
      useEffect(() => {
        if (!token) return;
        // Fetch /api/settings (admin)
      }, [token]);
      const save = async (key: string, value: string) => {
        // PATCH /api/settings con { [key]: value }
        toast.success("Guardado");
      };
      return (<>{/* layout del diseño líneas 6269-6310 */}</>);
    }
    ```

    Paso 2 — Crear los 6 modales en `apps/website/app/dashboard/modals/`:

    **`AdminFormModal.tsx`** — modal genérico (extraer del diseño, busca `AdminFormModal` en `design/Konbini.html`):
    ```tsx
    "use client";
    export function AdminFormModal({ title, onClose, onSubmit, children }: {
      title: string; onClose: () => void; onSubmit: () => void; children: React.ReactNode;
    }) {
      return (
        <div className="confirm-bg" onClick={onClose}>
          <div className="confirm-card" onClick={e => e.stopPropagation()}>
            <h3>{title}</h3>
            <form onSubmit={e => { e.preventDefault(); onSubmit(); }}>
              {children}
              <div style={{ display: "flex", gap: 10 }}>
                <button type="button" className="btn ghost" onClick={onClose}>Cancelar</button>
                <button type="submit" className="btn primary">Guardar</button>
              </div>
            </form>
          </div>
        </div>
      );
    }
    ```

    **`AdminApproveModal.tsx`** — confirma aprobación:
    ```tsx
    "use client";
    export function AdminApproveModal({ title, onClose, onConfirm }: { title: string; onClose: () => void; onConfirm: () => void }) {
      return (
        <div className="confirm-bg" onClick={onClose}>
          <div className="confirm-card" onClick={e => e.stopPropagation()}>
            <h3>¿Aprobar?</h3>
            <p>{title}</p>
            <div style={{ display: "flex", gap: 10 }}>
              <button className="btn ghost" onClick={onClose}>Cancelar</button>
              <button className="btn primary" onClick={onConfirm}>Aprobar</button>
            </div>
          </div>
        </div>
      );
    }
    ```

    **`AdminRejectModal.tsx`** — pide motivo (textarea + validación min 3 caracteres):
    ```tsx
    "use client";
    import { useState } from "react";
    export function AdminRejectModal({ title, onClose, onConfirm }: { title: string; onClose: () => void; onConfirm: (reason: string) => void }) {
      const [reason, setReason] = useState("");
      return (
        <div className="confirm-bg" onClick={onClose}>
          <div className="confirm-card" onClick={e => e.stopPropagation()}>
            <h3>Rechazar</h3>
            <p>{title}</p>
            <textarea value={reason} onChange={e => setReason(e.target.value)} placeholder="Motivo del rechazo (mínimo 3 caracteres)" />
            <div style={{ display: "flex", gap: 10 }}>
              <button className="btn ghost" onClick={onClose}>Cancelar</button>
              <button className="btn primary" disabled={reason.trim().length < 3} onClick={() => onConfirm(reason.trim())}>Rechazar</button>
            </div>
          </div>
        </div>
      );
    }
    ```

    **`AdminTransferModal.tsx`** — UI para transferir contenido entre cuentas/orgs (Phase 9).

    **`AdminEventEditor.tsx`** — formulario completo de creación/edición de evento (modo create/edit). Layout grande del diseño.

    **`AdminArticleEditor.tsx`** — formulario completo de creación/edición de artículo con MarkdownArea.

    Paso 3 — Verificar build final:
    ```bash
    pnpm --filter website build
    ```
    Debe pasar exit 0. Si falla, revisar imports y rutas.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/dashboard/sections/PaymentsSection.tsx" && test -f "apps/website/app/dashboard/sections/SubsSection.tsx" && test -f "apps/website/app/dashboard/sections/InboxSection.tsx" && test -f "apps/website/app/dashboard/sections/CRMSection.tsx" && test -f "apps/website/app/dashboard/sections/FAQSection.tsx" && test -f "apps/website/app/dashboard/sections/ReportsSection.tsx" && test -f "apps/website/app/dashboard/sections/SettingsSection.tsx" && test -f "apps/website/app/dashboard/modals/AdminFormModal.tsx" && test -f "apps/website/app/dashboard/modals/AdminApproveModal.tsx" && test -f "apps/website/app/dashboard/modals/AdminRejectModal.tsx" && test -f "apps/website/app/dashboard/modals/AdminTransferModal.tsx" && test -f "apps/website/app/dashboard/modals/AdminEventEditor.tsx" && test -f "apps/website/app/dashboard/modals/AdminArticleEditor.tsx" && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - Las 7 secciones UI-only existen:
      - `test -f "apps/website/app/dashboard/sections/PaymentsSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/SubsSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/InboxSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/CRMSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/FAQSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/ReportsSection.tsx"`
      - `test -f "apps/website/app/dashboard/sections/SettingsSection.tsx"`
    - Los 6 modales existen:
      - `test -f "apps/website/app/dashboard/modals/AdminFormModal.tsx"`
      - `test -f "apps/website/app/dashboard/modals/AdminApproveModal.tsx"`
      - `test -f "apps/website/app/dashboard/modals/AdminRejectModal.tsx"`
      - `test -f "apps/website/app/dashboard/modals/AdminTransferModal.tsx"`
      - `test -f "apps/website/app/dashboard/modals/AdminEventEditor.tsx"`
      - `test -f "apps/website/app/dashboard/modals/AdminArticleEditor.tsx"`
    - `grep -q "confirm-bg\\|confirm-card" apps/website/app/dashboard/modals/AdminApproveModal.tsx` → 0
    - `grep -q "InboxSection\\|kind" apps/website/app/dashboard/sections/InboxSection.tsx` → 0
    - **BUILD FINAL DEL PLAN 03:** `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    7 secciones UI-only + 6 modales creados. Admin panel completo funciona via `/dashboard?section=X`. Build pasa exit 0.
  </done>
</task>

</tasks>

<verification>
Al cerrar Plan 03:
1. `pnpm --filter website build` debe pasar exit 0.
2. Navegar a `/dashboard?section=events` debe renderizar EventsSection (manual).
3. Aprobar/rechazar un evento debe llamar API real (manual con curl o navegador).
4. Las sub-rutas viejas (`/dashboard/events`, `/dashboard/users`, etc.) deben devolver 404.
5. `grep -rn "AdminSidebar\|AdminTopbar" apps/website/` no debe encontrar nada.
</verification>

<success_criteria>
- 8 sub-rutas viejas eliminadas
- 6 componentes admin viejos eliminados
- AdminGuard preservado
- AdminPage es shell SPA con sidebar + ?section= routing
- 14 secciones creadas en archivos separados
- 6 modales creados en archivos separados
- admin.css contiene CSS del nuevo diseño (.admin-shell, .admin-side, .admin-main)
- EventsSection, ArticlesSection, UsersSection, CategoriesSection, SpotsSection, HeroesSection, LogsSection preservan APIs reales
- Build verde
</success_criteria>

<output>
After completion, create `.planning/phases/15-rediseno-ui-migracion-de-vistas/15-03-SUMMARY.md` documenting:
- Lista de archivos eliminados (sub-rutas viejas + componentes admin viejos)
- Lista de archivos creados (AdminPage, 14 secciones, 6 modales, admin.css reescrito)
- Mapeo section ID ↔ archivo de sección
- Cuáles secciones tienen API real vs mock data
- Integraciones API preservadas (approve/reject events, articles, users CRUD, categories CRUD, etc.)
</output>
