---
phase: 15-rediseno-ui-migracion-de-vistas
plan: 04
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - apps/website/app/login/page.tsx
  - apps/website/app/login/LoginView.tsx
  - apps/website/app/registro/page.tsx
  - apps/website/app/registro/RegistroView.tsx
  - apps/website/components/AuthShell.tsx
  - apps/website/app/(site)/cuenta/page.tsx
  - apps/website/app/(site)/cuenta/AccountShell.tsx
  - apps/website/app/(site)/cuenta/perfil/page.tsx
  - apps/website/app/(site)/cuenta/organizaciones/page.tsx
  - apps/website/app/(site)/cuenta/suscripcion/page.tsx
  - apps/website/app/(site)/cuenta/publicaciones/page.tsx
  - apps/website/app/(site)/cuenta/articulos/page.tsx
  - apps/website/app/(site)/cuenta/favoritos/page.tsx
  - apps/website/app/(site)/cuenta/mensajes/page.tsx
  - apps/website/app/(site)/cuenta/pagos/page.tsx
autonomous: true
requirements:
  - UI-MIG-04
must_haves:
  truths:
    - "Login y Registro rinden el nuevo diseño AuthPage con AuthShell wrapper"
    - "AuthShell es componente reutilizable compartido entre login y registro"
    - "/cuenta/ tiene sidebar de tabs con sub-rutas para perfil, organizaciones, suscripcion, publicaciones, articulos, favoritos, mensajes, pagos"
    - "Cada tab en su PROPIO archivo (D-05)"
    - "Login preserva integracion con api.login + setAuth(user, token)"
    - "Registro preserva integracion con api.register"
    - "pnpm --filter website build pasa exit 0"
  artifacts:
    - path: "apps/website/components/AuthShell.tsx"
      provides: "Wrapper visual compartido por login y registro"
      contains: "export function AuthShell"
    - path: "apps/website/app/(site)/cuenta/AccountShell.tsx"
      provides: "Layout con sidebar de tabs"
      contains: "export function AccountShell"
    - path: "apps/website/app/(site)/cuenta/perfil/page.tsx"
      provides: "Tab Perfil"
      contains: "default function"
  key_links:
    - from: "apps/website/app/login/LoginView.tsx"
      to: "api.login + setAuth"
      via: "useUser().setAuth"
      pattern: "api\\.login|setAuth"
    - from: "apps/website/app/(site)/cuenta/page.tsx"
      to: "AccountShell"
      via: "import { AccountShell } from './AccountShell'"
      pattern: "AccountShell"
---

<objective>
Wave 3 (paralelo con Plan 03): Reescribir las vistas de usuario:

1. **Auth pages** (login + registro): nuevo diseno con AuthShell wrapper compartido.
2. **Cuenta** (`/cuenta/`): expandir con sidebar de 8 tabs segun D-05, cada tab en su propio archivo:
   - perfil, organizaciones, suscripcion, publicaciones, articulos, favoritos, mensajes, pagos.

Preservar integraciones API existentes (login con setAuth, registro con setAuth).

**Dependencia con Plan 02:** este plan depende de 15-02 además de 15-01 porque las vistas de cuenta consumen patrones de listado (EventCard, layouts de listado) actualizados en Plan 02. Wave 3 = max(wave_01=1, wave_02=2) + 1 = 3.

Purpose: actualizar las vistas autenticadas del usuario al nuevo diseno con la expansion de tabs prometida en D-05.
Output: login/registro rediseñados + cuenta con 8 tabs en archivos separados.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-CONTEXT.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-RESEARCH.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-01-PLAN.md
@.planning/phases/15-rediseno-ui-migracion-de-vistas/15-02-PLAN.md

# Diseno fuente
@design/Konbini.html

# Vistas actuales
@apps/website/app/login/page.tsx
@apps/website/app/registro/page.tsx
@apps/website/app/(site)/cuenta/page.tsx

# Capa de datos (NO TOCAR)
@apps/website/lib/api.ts
@apps/website/components/providers.tsx

<interfaces>
From apps/website/lib/api.ts:
```typescript
api.login(email, password): Promise<AuthResponse>;
api.register(input): Promise<AuthResponse>;
api.googleLogin(idToken): Promise<AuthResponse>;
api.me(token): Promise<ApiUser>;
api.myEvents(token): Promise<ApiEventList>;
// Saved events / subscription / orgs / orders — funciones de Phase 9/11/12/13
```
Si una funcion no existe en api.ts, usar fetch directo con `Authorization: Bearer ${token}`.

From apps/website/components/providers.tsx (post Plan 01):
```typescript
useUser(): { user, token, ready, setAuth, setUser, logout };
useTheme(): { theme, setTheme, toggleTheme };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthShell + Login + Registro al nuevo diseno</name>
  <files>apps/website/components/AuthShell.tsx, apps/website/app/login/page.tsx, apps/website/app/login/LoginView.tsx, apps/website/app/registro/page.tsx, apps/website/app/registro/RegistroView.tsx</files>
  <read_first>
    - apps/website/app/login/page.tsx (version actual con api.login + setAuth — preservar logica)
    - apps/website/app/registro/page.tsx (version actual con api.register — preservar)
    - apps/website/components/GoogleLoginButton.tsx (boton Google OAuth existente — reutilizar)
    - design/Konbini.html lines 4790-4822 (AuthShell del diseno — wrapper visual)
    - design/Konbini.html lines 4823-4950 (AuthPage del diseno con tabs login/registro)
    - design/Konbini.html lines 3095-3164 (LoginPage del diseno — variante simple)
  </read_first>
  <action>
    Paso 1 — Crear `apps/website/components/AuthShell.tsx` (NUEVO):
    - Componente client "use client"
    - Props: `title: string`, `subtitle?: string`, `children: ReactNode`, `sideContent?: ReactNode`
    - Estructura: `<div className="auth-shell"> <div className="auth-card"> <Link href="/" className="auth-brand"><BrandMark /></Link> <div className="auth-hero"><h1 className="display">{title}</h1>{subtitle && <p>{subtitle}</p>}</div> <div className="auth-form">{children}</div> </div> {sideContent && <div className="auth-side">{sideContent}</div>} </div>`
    - Las clases `.auth-shell`, `.auth-card`, `.auth-side`, `.auth-form`, `.auth-hero`, `.auth-brand` ya estan en globals.css (Plan 01 Task 1).

    Paso 2 — REESCRIBIR `apps/website/app/login/page.tsx`:
    - Server component minimo que exporta metadata `{ title: "Ingresar - Konbini" }` y default function que retorna `<LoginView />`.
    - Importa: `import { LoginView } from "./LoginView";`

    Paso 3 — CREAR `apps/website/app/login/LoginView.tsx`:
    - "use client"
    - Imports: useState, useRouter+useSearchParams (next/navigation), Link, toast (sonner), AuthShell, GoogleLoginButton, Ic, useUser (providers), api+toUser (lib/api).
    - Hook: `const { setAuth } = useUser()`, `const router = useRouter()`, `const params = useSearchParams()`, `const redirect = params.get("redirect") ?? "/"`.
    - State: `email`, `password`, `busy`.
    - `submit(e)`: `e.preventDefault()`, setBusy(true), `try { const res = await api.login(email, password); setAuth(toUser(res.user), res.token); toast.success("Bienvenido"); router.push(redirect); } catch (ex) { toast.error(ex instanceof Error ? ex.message : "Error al iniciar sesion"); } finally { setBusy(false); }`.
    - JSX: `<AuthShell title="Bienvenido de vuelta" subtitle="Ingresa para continuar"><form onSubmit={submit}>...campos email+password...<button className="btn primary block" disabled={busy}>{busy ? "Ingresando..." : "Ingresar"}</button>...<div className="auth-or">o continuar con</div><GoogleLoginButton onSuccess={() => router.push(redirect)} /><div className="auth-links"><Link href="/registro">Crear cuenta</Link><Link href="/recuperar">Olvide mi contrasena</Link></div></form></AuthShell>`.
    - El layout exacto del form viene del diseño líneas 4823-4950.

    Paso 4 — REESCRIBIR `apps/website/app/registro/page.tsx`:
    - Server component minimo que exporta metadata `{ title: "Crear cuenta - Konbini" }` y retorna `<RegistroView />`.

    Paso 5 — CREAR `apps/website/app/registro/RegistroView.tsx`:
    - "use client"
    - State: `form = { firstname, lastname, email, password }`, `busy`.
    - `submit(e)`: `await api.register(form)`, `setAuth(toUser(res.user), res.token)`, `toast.success("Cuenta creada")`, `router.push("/")`.
    - JSX: `<AuthShell title="Crear cuenta" subtitle="Unete a la comunidad">...4 inputs + boton + GoogleLoginButton + link a /login</AuthShell>`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f apps/website/components/AuthShell.tsx && test -f apps/website/app/login/LoginView.tsx && test -f apps/website/app/registro/RegistroView.tsx && grep -q "export function AuthShell" apps/website/components/AuthShell.tsx && grep -q "api.login" apps/website/app/login/LoginView.tsx && grep -q "setAuth" apps/website/app/login/LoginView.tsx && grep -q "api.register" apps/website/app/registro/RegistroView.tsx && grep -q "AuthShell" apps/website/app/login/LoginView.tsx && grep -q "AuthShell" apps/website/app/registro/RegistroView.tsx && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/website/components/AuthShell.tsx`
    - `test -f apps/website/app/login/LoginView.tsx`
    - `test -f apps/website/app/registro/RegistroView.tsx`
    - `grep -q "export function AuthShell" apps/website/components/AuthShell.tsx` → 0
    - `grep -q "auth-shell" apps/website/components/AuthShell.tsx` → 0
    - `grep -q "auth-card" apps/website/components/AuthShell.tsx` → 0
    - `grep -q "api.login" apps/website/app/login/LoginView.tsx` → 0 (integracion preservada)
    - `grep -q "setAuth" apps/website/app/login/LoginView.tsx` → 0
    - `grep -q "api.register" apps/website/app/registro/RegistroView.tsx` → 0
    - `grep -q "AuthShell" apps/website/app/login/LoginView.tsx` → 0
    - `grep -q "AuthShell" apps/website/app/registro/RegistroView.tsx` → 0
    - `grep -q "GoogleLoginButton" apps/website/app/login/LoginView.tsx` → 0 (Google OAuth preservado)
    - `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    AuthShell creado como wrapper compartido. Login y Registro rediseñados en archivos View separados. Integracion API preservada. Build pasa.
  </done>
</task>

<task type="auto">
  <name>Task 2: /cuenta/ con AccountShell + 4 tabs (perfil, organizaciones, suscripcion, publicaciones)</name>
  <files>apps/website/app/(site)/cuenta/page.tsx, apps/website/app/(site)/cuenta/AccountShell.tsx, apps/website/app/(site)/cuenta/perfil/page.tsx, apps/website/app/(site)/cuenta/organizaciones/page.tsx, apps/website/app/(site)/cuenta/suscripcion/page.tsx, apps/website/app/(site)/cuenta/publicaciones/page.tsx</files>
  <read_first>
    - apps/website/app/(site)/cuenta/page.tsx (version actual)
    - design/Konbini.html lines 3724-3774 (AccountPage shell del diseno con sidebar de tabs)
    - design/Konbini.html lines 3775-3854 (AccPerfil — datos personales, change password, change email)
    - design/Konbini.html lines 3855-3957 (AccOrg / AccOrgs — organizaciones del user)
    - design/Konbini.html lines 3904-3957 (AccSubs — suscripcion)
    - design/Konbini.html lines 3958-4031 (AccEventos / DashboardPage — mis publicaciones)
    - design/Konbini.html lines 3165-3399 (DashboardPage organizador — listado de eventos propios)
  </read_first>
  <action>
    Paso 1 — `apps/website/app/(site)/cuenta/page.tsx`. REEMPLAZAR contenido por redirect:
    ```tsx
    import { redirect } from "next/navigation";
    export default function CuentaIndexPage() {
      redirect("/cuenta/perfil");
    }
    ```

    Paso 2 — CREAR `apps/website/app/(site)/cuenta/AccountShell.tsx`:
    - "use client"
    - Const TABS array con 8 entradas: `{ id, label, href }` para perfil, organizaciones, suscripcion, publicaciones, articulos, favoritos, mensajes, pagos. Cada href es `/cuenta/<id>`.
    - Hook: `const { user } = useUser()`, `const pathname = usePathname()`.
    - JSX: `<main className="container acc-shell"><aside className="acc-side"><div className="acc-user"><div className="avatar">{user?.firstname?.[0] ?? "?"}</div><div><div className="nm">{user?.firstname} {user?.lastname}</div><div className="em">{user?.email}</div></div></div><nav>{TABS.map(t => <Link key={t.id} href={t.href} className={\`acc-tab ${pathname === t.href ? "on" : ""}\`}>{t.label}</Link>)}</nav></aside><section className="acc-main">{children}</section></main>`.
    - Export: `export function AccountShell({ children }: { children: ReactNode })`.

    Paso 3 — CREAR `apps/website/app/(site)/cuenta/perfil/page.tsx`:
    - "use client", default function PerfilPage.
    - Hook useUser para user/token/setUser.
    - State: bio, website, telefono, etc. inicializados desde user.
    - `saveProfile()`: fetch PATCH /api/users/me (o api.updateProfile si existe en api.ts), muestra toast.
    - Botones para abrir "Change password" modal y "Change email" modal (Phase 10 AUTH-04).
    - JSX envuelto en `<AccountShell><h1 className="display">Perfil</h1>...formulario del diseño líneas 3775-3854...</AccountShell>`.

    Paso 4 — CREAR `apps/website/app/(site)/cuenta/organizaciones/page.tsx`:
    - "use client"
    - useEffect carga fetch /api/organizations/mine (Phase 9 ORG-02) con `Authorization: Bearer ${token}`.
    - Lista de orgs con `role` (OWNER/MEMBER), boton "Crear organizacion" (POST /api/organizations), botones de invitar miembros (Phase 9 ORG-03).
    - JSX envuelto en `<AccountShell><h1>Organizaciones</h1>...layout del diseño líneas 3855-3903...</AccountShell>`.

    Paso 5 — CREAR `apps/website/app/(site)/cuenta/suscripcion/page.tsx`:
    - "use client"
    - useEffect carga GET /api/subscriptions/me (Phase 12 COM-01).
    - Si hay sub: mostrar status, creditsUsed/creditsTotal, cycleStart, cycleEnd, cancelledAt, boton "Cancelar suscripcion" (DELETE /api/subscriptions/me).
    - Si no hay sub: boton "Suscribirme" (POST /api/subscriptions, redirect a Transbank URL).
    - JSX en `<AccountShell><h1>Suscripcion</h1>...layout líneas 3904-3957...</AccountShell>`.

    Paso 6 — CREAR `apps/website/app/(site)/cuenta/publicaciones/page.tsx`:
    - "use client"
    - useEffect: `api.myEvents(token)` (Phase 1/3 PUBL-04).
    - Por cada evento: thumb (imageUrl(e.poster ?? e.banner)), titulo, estado (En revision / Publicado / Rechazado con motivo), fecha.
    - Botones: Ver (Link a /evento/[slug]), Editar (Link a /crear?id=X), Eliminar (DELETE /events/:id con confirm).
    - Header con boton "Crear evento" (Link a /crear).
    - JSX en `<AccountShell><div header><h1>Mis publicaciones</h1><Link className="btn primary" href="/crear">Crear evento</Link></div>...listado del diseño líneas 3958-4031 + 3165-3399...</AccountShell>`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/(site)/cuenta/AccountShell.tsx" && test -f "apps/website/app/(site)/cuenta/perfil/page.tsx" && test -f "apps/website/app/(site)/cuenta/organizaciones/page.tsx" && test -f "apps/website/app/(site)/cuenta/suscripcion/page.tsx" && test -f "apps/website/app/(site)/cuenta/publicaciones/page.tsx" && grep -q "export function AccountShell" "apps/website/app/(site)/cuenta/AccountShell.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/perfil/page.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/publicaciones/page.tsx" && grep -q "redirect" "apps/website/app/(site)/cuenta/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "apps/website/app/(site)/cuenta/AccountShell.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/perfil/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/organizaciones/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/suscripcion/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/publicaciones/page.tsx"` (creado)
    - `grep -q "export function AccountShell" "apps/website/app/(site)/cuenta/AccountShell.tsx"` → 0
    - `grep -q "acc-shell" "apps/website/app/(site)/cuenta/AccountShell.tsx"` → 0
    - `grep -q "acc-side" "apps/website/app/(site)/cuenta/AccountShell.tsx"` → 0
    - `grep -q "acc-tab" "apps/website/app/(site)/cuenta/AccountShell.tsx"` → 0
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/perfil/page.tsx"` → 0
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/publicaciones/page.tsx"` → 0
    - `grep -q "redirect" "apps/website/app/(site)/cuenta/page.tsx"` → 0 (index redirige)
    - NOTA: build se verifica al final de Task 3 cuando los 8 tabs existan.
  </acceptance_criteria>
  <done>
    AccountShell con sidebar de 8 tabs. 4 tabs creados (perfil, organizaciones, suscripcion, publicaciones) en archivos separados.
  </done>
</task>

<task type="auto">
  <name>Task 3: /cuenta/ — 4 tabs restantes (articulos, favoritos, mensajes, pagos) + build final</name>
  <files>apps/website/app/(site)/cuenta/articulos/page.tsx, apps/website/app/(site)/cuenta/favoritos/page.tsx, apps/website/app/(site)/cuenta/mensajes/page.tsx, apps/website/app/(site)/cuenta/pagos/page.tsx</files>
  <read_first>
    - design/Konbini.html lines 4032-4052 (AccArticulos — mis articulos)
    - design/Konbini.html lines 4053-4064 (AccFavoritos — favoritos guardados)
    - design/Konbini.html lines 4065-4091 (AccMensajes — mensajes/notifications)
    - design/Konbini.html lines 4092-4118 (AccPagos — historial de pagos)
    - apps/website/app/(site)/cuenta/AccountShell.tsx (creado en Task 2)
  </read_first>
  <action>
    Paso 1 — Crear los 4 tabs restantes con la misma estructura: client component que envuelve en `<AccountShell>`.

    **`articulos/page.tsx`** (diseño líneas 4032-4052):
    - "use client", default function ArticulosPage.
    - useEffect: fetch GET /api/articles?mine=true con Bearer token (Phase 13 CNT-01).
    - Lista de articulos propios con status (DRAFT / SUBMITTED / APPROVED / REJECTED con statusReason).
    - Botones por articulo: Ver, Editar, Eliminar.
    - Header con boton "Crear articulo" → Link a `/crear/articulo`.
    - JSX en `<AccountShell><div header><h1>Mis articulos</h1><Link className="btn primary" href="/crear/articulo">Crear articulo</Link></div>...listado...</AccountShell>`.

    **`favoritos/page.tsx`** (diseño líneas 4053-4064):
    - "use client", default function FavoritosPage.
    - useEffect: fetch GET /api/users/me/saved-events con Bearer token (Phase 13 CNT-02).
    - Si vacio: `<div className="empty"><h3>Sin favoritos</h3><p>Guarda eventos desde sus paginas.</p></div>`.
    - Si hay: grid de EventCard usando toEventItem.
    - JSX en `<AccountShell><h1>Favoritos</h1>{vacio o cards-grid}</AccountShell>`.

    **`mensajes/page.tsx`** (diseño líneas 4065-4091):
    - "use client", default function MensajesPage.
    - useEffect: fetch GET /api/notifications?page=1&limit=20 con Bearer token (Phase 11 CFG-01).
    - Lista de notificaciones con read/unread, agrupadas por tipo.
    - Boton "Marcar todas como leidas" → PATCH /api/notifications/read-all (toast en exito).
    - Click en notificacion → PATCH /api/notifications/:id/read.
    - JSX en `<AccountShell><h1>Mensajes</h1>...listado del diseño...</AccountShell>`.

    **`pagos/page.tsx`** (diseño líneas 4092-4118):
    - "use client", default function PagosPage.
    - useEffect: fetch GET /api/orders/mine con Bearer token (Phase 12 COM-02).
    - Historial de pagos con estado (PENDING/PAID/FAILED), total, items (EVENT/SPOT/HERO/ARTICLE/SUBSCRIPTION), fecha.
    - JSX en `<AccountShell><h1>Pagos</h1>...tabla del diseño...</AccountShell>`.

    Paso 2 — Verificar build final del plan:
    ```bash
    pnpm --filter website build
    ```
    Debe pasar exit 0.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && test -f "apps/website/app/(site)/cuenta/articulos/page.tsx" && test -f "apps/website/app/(site)/cuenta/favoritos/page.tsx" && test -f "apps/website/app/(site)/cuenta/mensajes/page.tsx" && test -f "apps/website/app/(site)/cuenta/pagos/page.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/articulos/page.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/favoritos/page.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/mensajes/page.tsx" && grep -q "AccountShell" "apps/website/app/(site)/cuenta/pagos/page.tsx" && pnpm --filter website build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "apps/website/app/(site)/cuenta/articulos/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/favoritos/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/mensajes/page.tsx"` (creado)
    - `test -f "apps/website/app/(site)/cuenta/pagos/page.tsx"` (creado)
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/articulos/page.tsx"` → 0
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/favoritos/page.tsx"` → 0
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/mensajes/page.tsx"` → 0
    - `grep -q "AccountShell" "apps/website/app/(site)/cuenta/pagos/page.tsx"` → 0
    - `grep -q "EventCard" "apps/website/app/(site)/cuenta/favoritos/page.tsx"` → 0 (usa EventCard)
    - **BUILD FINAL DEL PLAN 04:** `pnpm --filter website build` exit 0
  </acceptance_criteria>
  <done>
    8 tabs de /cuenta/ creados en archivos separados. AccountShell wrapeа cada uno. Build pasa exit 0.
  </done>
</task>

</tasks>

<verification>
Al cerrar Plan 04:
1. `pnpm --filter website build` debe pasar exit 0.
2. /login y /registro deben rendir AuthShell con el nuevo diseño (manual).
3. /cuenta debe redirigir a /cuenta/perfil (manual).
4. Cada tab debe estar accesible via su URL directa (manual).
5. Login debe funcionar end-to-end: form → api.login → setAuth → router.push (manual).
</verification>

<success_criteria>
- AuthShell componente reutilizable creado
- LoginView + RegistroView en archivos separados de page.tsx
- /cuenta/ redirige a /cuenta/perfil
- 8 tabs creados en archivos separados (D-05 cumplido)
- AccountShell con sidebar de 8 tabs y user info
- Login preserva api.login + setAuth + GoogleLoginButton
- Registro preserva api.register + setAuth
- Build verde
</success_criteria>

<output>
After completion, create `.planning/phases/15-rediseno-ui-migracion-de-vistas/15-04-SUMMARY.md` documenting:
- Estructura AuthShell + LoginView/RegistroView
- Estructura AccountShell + 8 tabs
- Integraciones API preservadas en login/registro
- APIs consumidas por cada tab de /cuenta/
- Limitaciones (features sin API real aún)
</output>
