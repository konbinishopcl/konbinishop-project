---
phase: 16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/dashboard/events/EventForm.tsx
autonomous: true
requirements:
  - UI16-FORM-01  # Parity: formulario igual al diseño (arrays dinámicos + uploads + galería)
  - UI16-FORM-02  # Footer correcto (sin select, CTA labels exactos)
  - UI16-FORM-03  # Subida de imágenes con file picker y preview

must_haves:
  truths:
    - "El status-select NO aparece dentro del .form-foot (sticky footer)."
    - "El CTA primario muestra 'Crear en revisión →' cuando mode=create y form.status=PENDING_MODERATION."
    - "El CTA primario muestra 'Guardar y publicar →' cuando mode=edit y form.status=APPROVED."
    - "Banner y Poster se suben con file picker (.upload-box) y muestran preview antes del submit."
    - "Prices, dates, socials y videos son arrays dinámicos con botón '+ Agregar otra ...'."
    - "La galería tiene 8 slots en grid de 4 columnas, cada slot es una upload-box clickeable."
    - "El status-select solo es visible para usuarios con role=ADMIN o SUPER_ADMIN, dentro del Panel 06 en el cuerpo del form."
    - "Para usuarios no-admin, status se inicializa en 'PENDING_MODERATION' y el Panel 06 no se renderiza."
    - "El footer contiene exactamente, en orden: <span> info text, <Link> 'Cancelar', <button> 'Guardar borrador', <button> CTA dinámico."
  artifacts:
    - path: "apps/website/app/dashboard/events/EventForm.tsx"
      provides: "EventForm component reescrito y tipo InitialEvent actualizado con gallery?: string[]"
      exports: ["EventForm", "InitialEvent"]
      min_lines: 400
  key_links:
    - from: "EventForm.tsx footer CTA button"
      to: "handleSubmit(form.status)"
      via: "onClick handler"
      pattern: "handleSubmit\\(form\\.status\\)"
    - from: "EventForm.tsx 'Guardar borrador' button"
      to: "handleSubmit(\"DRAFT\")"
      via: "onClick handler"
      pattern: "handleSubmit\\(\"DRAFT\"\\)"
    - from: "EventForm.tsx Panel 06 visibility"
      to: "useUser().user.role"
      via: "conditional render"
      pattern: "(role\\s*===\\s*\"ADMIN\"|role\\s*===\\s*\"SUPER_ADMIN\")"
    - from: "ImageUploadBox onFile"
      to: "api.uploadImage(file, token)"
      via: "called inside handleSubmit before building payload"
      pattern: "api\\.uploadImage"
---

<objective>
Reescribir `apps/website/app/dashboard/events/EventForm.tsx` para alcanzar paridad visual y funcional con `design/app.jsx` (FormPage), manteniendo la estructura de 5 paneles siempre visibles (NO wizard, NO acordeón) y respetando el contrato bloqueado del UI-SPEC.

Purpose: Corregir el formulario que el usuario reportó como roto:
  1. El status-select estaba mal ubicado en el footer (causaba que la CTA mostrara label incorrecto).
  2. Los campos escalares de precios/fechas/sociales/videos no permitían múltiples entradas.
  3. Banner/poster usaban inputs de URL en vez de upload boxes con file picker.
  4. No existía galería de imágenes.

Output:
  - `EventForm.tsx` reescrito completamente (un solo archivo, ~500 líneas).
  - Tipo `InitialEvent` extendido con `gallery?: string[]`.
  - Sin cambios en `new/page.tsx`, `[id]/edit/page.tsx`, `api.ts`, `globals.css`, ni en el backend.

Out of scope (NO tocar):
  - Campo "Venue" (Open Question #1 del RESEARCH — no existe en el schema backend, diferido).
  - `AdminEventEditor.tsx` (archivo separado con acordeón — dejarlo intacto).
  - `api.ts` (preservado por decisión Phase 15-05).
  - Tests automatizados (no hay infra de tests en apps/website; validación es manual + grep).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 16 contracts (LOCKED — no revisitar)
@.planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-RESEARCH.md
@.planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-UI-SPEC.md

# Source of truth del diseño
@design/app.jsx

# Estado actual del form (a reescribir)
@apps/website/app/dashboard/events/EventForm.tsx

# Consumers (NO tocar, pero verificar compatibilidad de InitialEvent)
@apps/website/app/dashboard/events/new/page.tsx
@apps/website/app/dashboard/events/[id]/edit/page.tsx

# APIs disponibles
@apps/website/lib/api.ts
@apps/website/components/providers.tsx
@apps/api/src/events/dto/create-event.dto.ts

# CSS classes (usar como-están, NO recrear)
@apps/website/app/globals.css

<interfaces>
<!-- Contratos extraídos del codebase. El executor usa esto directamente sin explorar. -->

From `apps/website/lib/api.ts`:
```typescript
export function imageUrl(path?: string | null): string;
// Convierte "/uploads/x.jpg" → "/api/media/uploads/x.jpg"
// Si path es http(s), retorna tal cual.
// Si path es null/undefined/"", retorna "".

export const api = {
  uploadImage: (file: File, token: string) => Promise<{ url: string; filename: string }>;
  // POST multipart a /upload con Bearer token; retorna URL relativa tipo "/uploads/abc.jpg".
};
```

From `apps/website/components/providers.tsx`:
```typescript
export function useUser(): {
  user: User | null;
  token: string | null;
  ready: boolean;
  setAuth: (user: User, token: string) => void;
  setUser: (user: User | null) => void;
  logout: () => void;
};
```

From `apps/website/lib/data.ts`:
```typescript
export type Role = "SUPER_ADMIN" | "ADMIN" | "AUTHENTICATED";
export type User = { id: number; name: string; email: string; role: Role; ... };
```

From `apps/api/src/events/dto/create-event.dto.ts` (backend acepta arrays — NO requiere cambios):
```typescript
class CreateEventDto {
  title: string;              // required, 2-200
  company?: string;
  description: string;        // required, min 10
  about?: string;
  address: string;            // required, min 2
  addressNumber?: string;
  ticketUrl?: string;
  banner?: string;            // path tipo "/uploads/x.jpg" o URL http
  poster?: string;
  gallery?: string[];         // array de paths
  cityId?: number;
  categoryId?: number;
  prices?: { name: string; price?: number }[];
  dates?:  { date?: string; startTime?: string; endTime?: string }[];
  socialLinks?: { link?: string }[];
  videos?:      { link?: string }[];
}
```

From `apps/website/app/globals.css` (USAR estas clases, NO recrear estilos):
- `.panel`               — contenedor de cada sección (background var(--surface), border, border-radius, padding 22px)
- `.field` + `.field label` + `.field input/textarea/select`  — campo estándar
- `.grid-2`, `.grid-3`   — grids de columnas
- `.input-prefix` + `.suffix`  — input con prefijo/sufijo ($/CLP, @, ▶, https://)
- `.price-row`           — grid de tarifa (1fr 200px 40px)
- `.add-line`            — botón "+ Agregar otra ..." (ancho completo, dashed border)
- `.upload-grid`         — grid 2fr 1fr para banner + poster
- `.upload-box`          — caja dashed para upload (aspect-ratio 16/9 default)
- `.upload-box.tall`     — variante poster (aspect-ratio 3/4)
- `.form-foot`           — sticky footer con `position: fixed; bottom: 0; left: 0; right: 0` (NO recrear con inline)
- `.form-foot > .container`  — display:flex; justify-content:space-between; align-items:center
- `.btn.ghost`, `.btn.dark`, `.btn.primary`  — variantes de botón
- `.icon-btn`            — botón circular 40x40 (para × de eliminar)
- `.help`                — texto de ayuda 12px var(--ink-3)

Current `InitialEvent` (línea 43-65 de EventForm.tsx) — extender con `gallery?: string[]`:
```typescript
export type InitialEvent = {
  id: number;
  title: string;
  company: string | null;
  description: string;
  about: string | null;
  address: string;
  addressNumber: string | null;
  ticketUrl: string | null;
  banner: string | null;
  poster: string | null;
  gallery?: string[];                              // ← AÑADIR
  categoryId: number | null;
  cityId: number | null;
  status: string;
  prices:      { name: string; price: number }[];
  dates:       { date: string | null; startTime: string | null; endTime: string | null }[];
  socialLinks: { link: string | null }[];
  videos:      { link: string | null }[];
  city?: { id: number; name: string; state?: { id: number; slug: string; country?: { slug: string } } } | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tipos, estado inicial, helpers y componente ImageUploadBox</name>
  <files>apps/website/app/dashboard/events/EventForm.tsx</files>
  <read_first>
    - apps/website/app/dashboard/events/EventForm.tsx (estado actual completo — leer todo el archivo)
    - apps/website/lib/api.ts (firmas exactas de api.uploadImage e imageUrl)
    - apps/website/components/providers.tsx (firma exacta de useUser)
    - apps/api/src/events/dto/create-event.dto.ts (shape de CreateEventDto que el payload debe cumplir)
    - design/app.jsx líneas 423-695 (FormPage + Step1/Step2/Step3 — fuente de verdad del shape)
    - .planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-RESEARCH.md (Pattern 3: state shape; Pattern 4: upload-box; Pattern 6: InitialEvent init)
  </read_first>
  <action>
    Reescribe la primera mitad de `apps/website/app/dashboard/events/EventForm.tsx` con los siguientes elementos. NO renderizar todavía el JSX de los paneles 01-06 ni el footer en esta tarea — la tarea 2 los añade. Esta tarea deja el archivo en un estado donde compila y exporta `EventForm` (puede devolver `<div>TODO panels</div>` temporalmente al final).

    **1. Mantén exactos estos imports al inicio:**
    ```tsx
    "use client";
    import Link from "next/link";
    import { useRouter } from "next/navigation";
    import { useCallback, useEffect, useRef, useState } from "react";
    import { toast } from "sonner";
    import { useUser } from "@/components/providers";
    import { api, imageUrl } from "@/lib/api";
    ```

    **2. Tipos auxiliares y de catálogo:**
    ```tsx
    type Category = { id: number; name: string; slug: string };
    type Country  = { id: number; name: string; slug: string };
    type State    = { id: number; name: string; slug: string };
    type City     = { id: number; name: string };

    type PriceRow  = { name: string; amount: string };
    type DateRow   = { date: string; startTime: string; endTime: string };
    type LinkRow   = { link: string };
    type ImageSlot = { file: File | null; url: string };
    ```

    **3. `FormData` (reemplaza el existente — arrays en vez de escalares):**
    ```tsx
    type FormData = {
      title:         string;
      company:       string;
      description:   string;
      about:         string;
      address:       string;
      addressNumber: string;
      ticketUrl:     string;
      categoryId:    string;
      countrySlug:   string;
      stateSlug:     string;
      cityId:        string;

      isFree:  boolean;
      prices:  PriceRow[];
      dates:   DateRow[];
      socials: LinkRow[];
      videos:  LinkRow[];

      banner:  ImageSlot;
      poster:  ImageSlot;
      gallery: ImageSlot[];   // siempre 8 elementos

      status: "APPROVED" | "PENDING_MODERATION" | "DRAFT";
    };
    ```

    **4. `InitialEvent` exportado (extiende el existente con `gallery?: string[]`):**
    ```tsx
    export type InitialEvent = {
      id:            number;
      title:         string;
      company:       string | null;
      description:   string;
      about:         string | null;
      address:       string;
      addressNumber: string | null;
      ticketUrl:     string | null;
      banner:        string | null;
      poster:        string | null;
      gallery?:      string[];
      categoryId:    number | null;
      cityId:        number | null;
      status:        string;
      prices:        { name: string; price: number }[];
      dates:         { date: string | null; startTime: string | null; endTime: string | null }[];
      socialLinks:   { link: string | null }[];
      videos:        { link: string | null }[];
      city?: { id: number; name: string; state?: { id: number; slug: string; country?: { slug: string } } } | null;
    };

    interface Props {
      mode:     "create" | "edit";
      initial?: InitialEvent;
    }
    ```

    **5. Helper `SectionHead` (conservar el del archivo original; idéntico look — badge naranja con número + título display):**
    Idéntico al SectionHead actual (líneas 74-90 del EventForm original). NO modificar.

    **6. Helper `Checkbox` (conservar el del original):**
    Idéntico al Checkbox actual (líneas 94-115). NO modificar.

    **7. Componente nuevo `ImageUploadBox` (file picker + preview + remove):**
    ```tsx
    function ImageUploadBox({
      variant,        // "banner" | "poster" | "gallery"
      slot,           // ImageSlot
      onPick,         // (file: File) => void
      onRemove,       // () => void
      label,          // texto principal vacío
      hint,           // small bajo el label
    }: {
      variant: "banner" | "poster" | "gallery";
      slot: ImageSlot;
      onPick: (file: File) => void;
      onRemove: () => void;
      label: string;
      hint?: string;
    }) {
      const inputRef = useRef<HTMLInputElement>(null);

      // Preview: si hay file, usar URL.createObjectURL; si hay url (del server), usar imageUrl()
      const previewSrc = slot.file
        ? URL.createObjectURL(slot.file)
        : slot.url ? imageUrl(slot.url) : "";

      // Limpiar object URL al desmontar
      useEffect(() => {
        if (!slot.file) return;
        const u = URL.createObjectURL(slot.file);
        return () => URL.revokeObjectURL(u);
      }, [slot.file]);

      if (previewSrc) {
        const aspect = variant === "poster" ? "3/4" : variant === "gallery" ? "1/1" : "16/9";
        return (
          <div style={{ position: "relative", aspectRatio: aspect, borderRadius: "var(--r)", overflow: "hidden", background: "var(--surface-2)" }}>
            <img src={previewSrc} alt="" style={{ width: "100%", height: "100%", objectFit: "cover", display: "block" }} />
            <button
              type="button"
              onClick={onRemove}
              aria-label="Eliminar imagen"
              className="icon-btn"
              style={{ position: "absolute", top: 8, right: 8, width: 32, height: 32, background: "rgba(15,12,10,.75)", color: "#fff", borderColor: "rgba(255,255,255,.15)" }}
            >
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
            </button>
          </div>
        );
      }

      const boxClass = variant === "poster" ? "upload-box tall" : "upload-box";
      const extraStyle = variant === "gallery" ? { aspectRatio: "1/1" as const, padding: 14 } : undefined;

      return (
        <>
          <div className={boxClass} style={extraStyle} onClick={() => inputRef.current?.click()}>
            <div className="ic" style={variant === "gallery" ? { width: 28, height: 28 } : undefined}>
              {/* upload icon */}
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M17 8l-5-5-5 5M12 3v12"/>
              </svg>
            </div>
            <div style={{ fontWeight: 500, color: "var(--ink-2)", fontSize: variant === "gallery" ? 12 : 14 }}>{label}</div>
            {hint && <small style={{ fontSize: variant === "gallery" ? 10 : 11 }}>{hint}</small>}
          </div>
          <input
            ref={inputRef}
            type="file"
            accept="image/jpeg,image/png,image/webp"
            style={{ display: "none" }}
            onChange={(e) => {
              const f = e.target.files?.[0];
              if (f) onPick(f);
              e.target.value = "";   // permitir re-pick del mismo archivo
            }}
          />
        </>
      );
    }
    ```

    **8. Función `EventForm` exportada con estado inicial y handlers de arrays. AÑADIR al body de la función ANTES del return:**

    ```tsx
    export function EventForm({ mode, initial }: Props) {
      const router = useRouter();
      const { token, user } = useUser();
      const isAdmin = user?.role === "ADMIN" || user?.role === "SUPER_ADMIN";
      const [busy, setBusy] = useState(false);

      // Catálogo
      const [categories, setCategories] = useState<Category[]>([]);
      const [countries,  setCountries]  = useState<Country[]>([]);
      const [states,     setStates]     = useState<State[]>([]);
      const [cities,     setCities]     = useState<City[]>([]);

      // ── Inicialización del form ────────────────────────────────────────────
      const [form, setForm] = useState<FormData>(() => {
        const initialGallery: ImageSlot[] = Array(8).fill(null).map((_, i) => ({
          file: null,
          url:  initial?.gallery?.[i] ?? "",
        }));

        // Default status: admin puede elegir, no-admin queda en PENDING_MODERATION
        let initStatus: FormData["status"] = "PENDING_MODERATION";
        if (initial?.status === "APPROVED") initStatus = "APPROVED";
        else if (initial?.status === "DRAFT") initStatus = "DRAFT";
        else if (initial?.status === "PENDING_MODERATION") initStatus = "PENDING_MODERATION";

        return {
          title:         initial?.title         ?? "",
          company:       initial?.company       ?? "",
          description:   initial?.description   ?? "",
          about:         initial?.about         ?? "",
          address:       initial?.address       ?? "",
          addressNumber: initial?.addressNumber ?? "",
          ticketUrl:     initial?.ticketUrl     ?? "",
          categoryId:    initial?.categoryId != null ? String(initial.categoryId) : "",
          countrySlug:   initial?.city?.state?.country?.slug ?? "",
          stateSlug:     initial?.city?.state?.slug          ?? "",
          cityId:        initial?.cityId != null ? String(initial.cityId) : "",

          isFree: initial ? (initial.prices.length === 0 || initial.prices[0]?.price === 0) : false,

          prices: initial?.prices.length
            ? initial.prices.map((p) => ({ name: p.name, amount: String(p.price) }))
            : [{ name: "General", amount: "" }],

          dates: initial?.dates.length
            ? initial.dates.map((d) => ({
                date:      d.date ? d.date.slice(0, 10) : "",
                startTime: d.startTime ?? "",
                endTime:   d.endTime   ?? "",
              }))
            : [{ date: "", startTime: "", endTime: "" }],

          socials: initial?.socialLinks.length
            ? initial.socialLinks.map((l) => ({ link: l.link ?? "" }))
            : [{ link: "" }],

          videos: initial?.videos.length
            ? initial.videos.map((v) => ({ link: v.link ?? "" }))
            : [{ link: "" }],

          banner:  { file: null, url: initial?.banner ?? "" },
          poster:  { file: null, url: initial?.poster ?? "" },
          gallery: initialGallery,

          status: initStatus,
        };
      });

      const set = <K extends keyof FormData>(k: K, v: FormData[K]) =>
        setForm((f) => ({ ...f, [k]: v }));

      // ── Helpers para arrays dinámicos ──────────────────────────────────────
      const updatePrice  = (i: number, k: keyof PriceRow, v: string) =>
        set("prices", form.prices.map((p, j) => j === i ? { ...p, [k]: v } : p));
      const addPrice     = () => set("prices", [...form.prices, { name: "", amount: "" }]);
      const removePrice  = (i: number) => set("prices", form.prices.filter((_, j) => j !== i));

      const updateDate   = (i: number, k: keyof DateRow, v: string) =>
        set("dates", form.dates.map((d, j) => j === i ? { ...d, [k]: v } : d));
      const addDate      = () => set("dates", [...form.dates, { date: "", startTime: "", endTime: "" }]);
      const removeDate   = (i: number) => set("dates", form.dates.filter((_, j) => j !== i));

      const updateSocial = (i: number, v: string) =>
        set("socials", form.socials.map((s, j) => j === i ? { link: v } : s));
      const addSocial    = () => set("socials", [...form.socials, { link: "" }]);
      const removeSocial = (i: number) => set("socials", form.socials.filter((_, j) => j !== i));

      const updateVideo  = (i: number, v: string) =>
        set("videos", form.videos.map((vv, j) => j === i ? { link: v } : vv));
      const addVideo     = () => set("videos", [...form.videos, { link: "" }]);
      const removeVideo  = (i: number) => set("videos", form.videos.filter((_, j) => j !== i));

      const updateGallery = (i: number, slot: ImageSlot) =>
        set("gallery", form.gallery.map((g, j) => j === i ? slot : g));

      // ── Catálogo (cascade igual al original — NO modificar) ────────────────
      const fetchCatalog = useCallback(async () => {
        if (!token) return;
        const h = { Authorization: `Bearer ${token}` };
        const [cats, ctrs] = await Promise.all([
          fetch("/api/categories", { headers: h }).then((r) => r.json()).catch(() => []),
          fetch("/api/countries",  { headers: h }).then((r) => r.json()).catch(() => []),
        ]);
        setCategories(Array.isArray(cats) ? cats : []);
        setCountries( Array.isArray(ctrs) ? ctrs : []);
      }, [token]);
      useEffect(() => { fetchCatalog(); }, [fetchCatalog]);

      useEffect(() => {
        if (!token || !form.countrySlug) { setStates([]); return; }
        fetch(`/api/states?country=${encodeURIComponent(form.countrySlug)}`, {
          headers: { Authorization: `Bearer ${token}` },
        }).then((r) => r.json()).then((d) => setStates(Array.isArray(d) ? d : [])).catch(() => setStates([]));
      }, [token, form.countrySlug]);

      useEffect(() => {
        if (!token || !form.stateSlug) { setCities([]); return; }
        fetch(`/api/cities?state=${encodeURIComponent(form.stateSlug)}`, {
          headers: { Authorization: `Bearer ${token}` },
        }).then((r) => r.json()).then((d) => setCities(Array.isArray(d) ? d : [])).catch(() => setCities([]));
      }, [token, form.stateSlug]);

      // ── handleSubmit (con upload de imágenes pendientes) ───────────────────
      async function handleSubmit(targetStatus: "APPROVED" | "PENDING_MODERATION" | "DRAFT") {
        if (!token) { toast.error("No autenticado"); return; }
        if (!form.title.trim()) { toast.error("El título es requerido"); return; }
        if (targetStatus !== "DRAFT") {
          if (form.description.trim().length < 10) { toast.error("La descripción debe tener al menos 10 caracteres"); return; }
          if (!form.address.trim())                 { toast.error("La dirección es requerida"); return; }
        }

        setBusy(true);
        try {
          // 1) Subir imágenes pendientes (banner / poster / galería) — solo las que tienen .file
          let bannerUrl = form.banner.url;
          if (form.banner.file) {
            const r = await api.uploadImage(form.banner.file, token);
            bannerUrl = r.url;
            // limpiar file para evitar re-upload en el próximo handleSubmit (Pitfall #4 del RESEARCH)
            set("banner", { file: null, url: r.url });
          }

          let posterUrl = form.poster.url;
          if (form.poster.file) {
            const r = await api.uploadImage(form.poster.file, token);
            posterUrl = r.url;
            set("poster", { file: null, url: r.url });
          }

          const galleryUrls: string[] = [];
          const newGallery: ImageSlot[] = [...form.gallery];
          for (let i = 0; i < form.gallery.length; i++) {
            const g = form.gallery[i];
            if (g.file) {
              const r = await api.uploadImage(g.file, token);
              newGallery[i] = { file: null, url: r.url };
              galleryUrls.push(r.url);
            } else if (g.url) {
              galleryUrls.push(g.url);
            }
          }
          set("gallery", newGallery);

          // 2) Construir payload
          const payload = {
            title:         form.title.trim(),
            company:       form.company.trim()       || undefined,
            description:   form.description.trim(),
            about:         form.about.trim()         || undefined,
            address:       form.address.trim(),
            addressNumber: form.addressNumber.trim() || undefined,
            ticketUrl:     form.ticketUrl.trim()     || undefined,
            banner:        bannerUrl                 || undefined,
            poster:        posterUrl                 || undefined,
            gallery:       galleryUrls.length ? galleryUrls : undefined,
            categoryId:    form.categoryId ? Number(form.categoryId) : undefined,
            cityId:        form.cityId     ? Number(form.cityId)     : undefined,

            prices: form.isFree
              ? [{ name: "Entrada", price: 0 }]
              : form.prices
                  .filter((p) => p.amount)
                  .map((p) => ({ name: p.name || "General", price: Number(p.amount) })),

            dates: form.dates
              .filter((d) => d.date)
              .map((d) => ({
                date:      d.date,
                startTime: d.startTime || undefined,
                endTime:   d.endTime   || undefined,
              })),

            socialLinks: form.socials.filter((s) => s.link.trim()).map((s) => ({ link: s.link.trim() })),
            videos:      form.videos .filter((v) => v.link.trim()).map((v) => ({ link: v.link.trim() })),
          };

          // 3) Submit
          const url    = mode === "create" ? "/api/events" : `/api/events/${initial!.id}`;
          const method = mode === "create" ? "POST" : "PATCH";

          const r = await fetch(url, {
            method,
            headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" },
            body: JSON.stringify(payload),
          });

          if (!r.ok) {
            const err = await r.json().catch(() => ({}));
            throw new Error((err as { message?: string }).message ?? "Error al guardar");
          }

          const saved = await r.json() as { id: number; status?: string };

          // 4) Si admin pidió APPROVED pero el backend lo dejó en PENDING_MODERATION, aprobar explícitamente
          if (targetStatus === "APPROVED" && saved.status !== "APPROVED") {
            await fetch(`/api/events/${saved.id}/approve`, {
              method: "PATCH",
              headers: { Authorization: `Bearer ${token}` },
            });
          }

          toast.success(mode === "create" ? "Evento creado" : "Evento actualizado", {
            description: targetStatus === "APPROVED"
              ? "Publicado directamente"
              : targetStatus === "PENDING_MODERATION"
                ? "En revisión"
                : "Guardado como borrador",
          });
          router.push("/dashboard/events");
        } catch (ex) {
          toast.error(ex instanceof Error ? ex.message : "Error al guardar");
        } finally {
          setBusy(false);
        }
      }

      // Render temporal (la tarea 2 reemplaza esto con los paneles + footer)
      return <div style={{ maxWidth: 900, margin: "0 auto", paddingBottom: 100 }}>TODO: panels + footer</div>;
    }
    ```

    Después de escribir este contenido, ejecuta `pnpm --filter website tsc --noEmit` desde la raíz del repo para validar tipos. Si falla, corrige errores de tipo antes de marcar la tarea como completa.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && pnpm --filter website tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "type ImageSlot" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match.
    - `grep -n "type PriceRow\|type DateRow\|type LinkRow" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 3 matches.
    - `grep -n "gallery?: string\[\]" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (in `InitialEvent` type).
    - `grep -n "prices:  PriceRow\[\]\|prices: PriceRow\[\]" apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match (in `FormData` type).
    - `grep -n "function ImageUploadBox" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match.
    - `grep -n "api.uploadImage" apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match (inside handleSubmit).
    - `grep -n "URL.createObjectURL" apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match (in ImageUploadBox preview).
    - `grep -n "import { useUser }\|import { api, imageUrl }\|import { useUser } from" apps/website/app/dashboard/events/EventForm.tsx` shows the imports of useUser, api, imageUrl present.
    - `grep -nE "user\?.role *=== *\"ADMIN\"|user\?.role *=== *\"SUPER_ADMIN\"" apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match (isAdmin derivation).
    - `grep -n "Array(8).fill(null)" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (8-slot gallery init).
    - `grep -n "handleSubmit(\"DRAFT\")\|handleSubmit('DRAFT')" apps/website/app/dashboard/events/EventForm.tsx` — NOT required in this task (footer is task 3), but if present, must use exactly `handleSubmit("DRAFT")`.
    - `pnpm --filter website tsc --noEmit` exits with code 0 (no TS errors). Inspect output: must NOT contain `error TS`.
    - File still exports `EventForm` and `InitialEvent`: `grep -E "^export (function EventForm|type InitialEvent)" apps/website/app/dashboard/events/EventForm.tsx` returns exactly 2 matches.
  </acceptance_criteria>
  <done>
    El archivo `EventForm.tsx` compila con `tsc --noEmit`. Exporta `EventForm` (función) y `InitialEvent` (tipo extendido con `gallery?: string[]`). Contiene los tipos auxiliares (`ImageSlot`, `PriceRow`, `DateRow`, `LinkRow`), el componente `ImageUploadBox`, la inicialización del estado con arrays y 8 slots de galería, los helpers de arrays dinámicos, el cascade de catálogo preservado, y `handleSubmit` que sube imágenes vía `api.uploadImage` antes de construir el payload. El return aún es un placeholder — la tarea 2 lo reemplaza.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render de paneles 01-06 (cuerpo del formulario)</name>
  <files>apps/website/app/dashboard/events/EventForm.tsx</files>
  <read_first>
    - apps/website/app/dashboard/events/EventForm.tsx (estado tras tarea 1)
    - design/app.jsx líneas 476-695 (Step1, Step2, Step3 — fuente del shape de cada panel)
    - apps/website/app/globals.css líneas 307-346 (clases .panel, .field, .grid-2, .grid-3, .price-row, .add-line, .upload-grid, .upload-box, .input-prefix)
    - .planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-UI-SPEC.md (Labels de secciones bloqueados; Layout y Contratos de Interacción)
    - .planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-RESEARCH.md (Pattern 5: price-row layout; Mapeo diseño → paneles)
  </read_first>
  <action>
    Reemplaza el return placeholder de la tarea 1 con el render completo de los 6 paneles. La estructura exterior es:

    ```tsx
    return (
      <div style={{ maxWidth: 900, margin: "0 auto", paddingBottom: 100 }}>

        {/* Back link */}
        <Link href="/dashboard/events" style={{ display: "inline-flex", alignItems: "center", gap: 6, color: "var(--ink-3)", fontSize: 13, marginBottom: 24 }}>
          ◀ Volver a eventos
        </Link>

        {/* Page title */}
        <div style={{ marginBottom: 28 }}>
          <h1 style={{ fontFamily: "var(--font-display)", fontWeight: 700, fontSize: 24, margin: 0 }}>
            {mode === "create" ? "Crear evento" : "Editar evento"}
          </h1>
          <p style={{ fontSize: 13, color: "var(--ink-3)", margin: "6px 0 0" }}>
            {mode === "create"
              ? "Creando como admin · sin checkout ni upsell."
              : `Editando evento #${initial?.id} · no se notifica al organizador.`}
          </p>
        </div>

        {/* Paneles 01-06 */}
        ... ver detalle abajo ...

        {/* Sticky footer */}
        {/* ← La tarea 3 añade aquí el footer */}
      </div>
    );
    ```

    **Panel 01 — Información básica:** clase `panel`, marginBottom 16. Contiene:
    - `<SectionHead n="01" title="Información básica" />`
    - `.grid-2` con campos: Título (input text required *), Empresa organizadora (input text)
    - `.grid-2` con campos: Categoría (select de `categories`, opción vacía "Sin categoría"), URL de entradas externa (input url)
    - `.field` Descripción corta (textarea rows=3) required *
    - `.field` Sobre el evento (textarea rows=6, marginBottom 0)

    Required asterisk: `<span style={{ color: "var(--err)" }}>*</span>` después del label.

    **Panel 02 — Precio:** clase `panel`, marginBottom 16. Contiene:
    - `<SectionHead n="02" title="Precio" />`
    - `<Checkbox checked={form.isFree} onChange={() => set("isFree", !form.isFree)} label="Evento gratuito" />`
    - Si `!form.isFree`: render del array `form.prices` usando `.price-row` (grid 1fr 200px 40px):
      ```tsx
      {form.prices.map((p, i) => (
        <div className="price-row" key={i}>
          <div className="field" style={{ margin: 0 }}>
            <label>Nombre de tarifa</label>
            <input type="text" placeholder="Ej: General, VIP, Estudiante" value={p.name}
                   onChange={(e) => updatePrice(i, "name", e.target.value)} />
          </div>
          <div className="field" style={{ margin: 0 }}>
            <label>Precio</label>
            <div className="input-prefix">
              <span>$</span>
              <input type="number" min="0" placeholder="0" value={p.amount}
                     onChange={(e) => updatePrice(i, "amount", e.target.value)} />
              <span className="suffix">CLP</span>
            </div>
          </div>
          <div style={{ display: "flex", alignItems: "end" }}>
            {form.prices.length > 1 && (
              <button type="button" className="icon-btn" aria-label="Eliminar" onClick={() => removePrice(i)}>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
              </button>
            )}
          </div>
        </div>
      ))}
      <button type="button" className="add-line" onClick={addPrice}>+ Agregar otra tarifa</button>
      ```

    **Panel 03 — Fechas, horario y ubicación:** clase `panel`, marginBottom 16.
    - `<SectionHead n="03" title="Fechas, horario y ubicación" />`
    - Render array `form.dates` con `.grid-3` (Fecha date / Hora inicio time / Hora término time) más botón × cuando length>1, seguido de `.add-line` con texto exacto `"+ Agregar otro día"`.
    - `.grid-3`: select País / select Región / select Ciudad (preservar comportamiento del cascade original — set countrySlug resetea state+city, set stateSlug resetea city; selects deshabilitados si no hay padre).
    - `.grid-2` con `marginBottom: 0`: Dirección (required *) / Número o Piso.

    **Panel 04 — Multimedia (Opcional):** clase `panel`, marginBottom 16.
    - `<SectionHead n="04" title="Multimedia" sub="Opcional" />`
    - `.upload-grid` con dos columnas:
      - Banner: label "Banner (horizontal · 16:9)" + `<ImageUploadBox variant="banner" slot={form.banner} onPick={(f) => set("banner", { file: f, url: form.banner.url })} onRemove={() => set("banner", { file: null, url: "" })} label="Sube una imagen horizontal" hint="JPG / PNG · máx 5MB · sin texto sobreimpreso" />`
      - Poster: label "Poster (vertical · 2:3)" + `<ImageUploadBox variant="poster" slot={form.poster} onPick={(f) => set("poster", { file: f, url: form.poster.url })} onRemove={() => set("poster", { file: null, url: "" })} label="Poster oficial" hint="JPG / PNG · 1200×1800" />`
    - Bloque "Galería": `<div style={{ marginTop: 18 }}>` con label "Galería" en var(--ink-2), 13px, fontWeight 500, marginBottom 8.
    - Grid de 8 slots: `<div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 10 }}>`
      ```tsx
      {form.gallery.map((g, i) => (
        <ImageUploadBox key={i} variant="gallery" slot={g}
                        onPick={(f) => updateGallery(i, { file: f, url: g.url })}
                        onRemove={() => updateGallery(i, { file: null, url: "" })}
                        label={`Imagen ${i + 1}`} />
      ))}
      ```
    - `<div className="help" style={{ marginTop: 10 }}>Hasta 8 imágenes. Aparecerán en la sección "Galería" del evento.</div>`
    - Bloque "Videos": label "Videos" (mismo estilo que Galería), seguido de render array `form.videos`:
      ```tsx
      {form.videos.map((v, i) => (
        <div key={i} style={{ display: "flex", gap: 10, marginBottom: 10 }}>
          <div className="input-prefix" style={{ flex: 1 }}>
            <span>▶</span>
            <input type="url" placeholder="https://youtube.com/watch?v=..." value={v.link}
                   onChange={(e) => updateVideo(i, e.target.value)} />
          </div>
          {form.videos.length > 1 && (
            <button type="button" className="icon-btn" aria-label="Eliminar" onClick={() => removeVideo(i)}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round"><path d="M18 6 6 18M6 6l12 12"/></svg>
            </button>
          )}
        </div>
      ))}
      <button type="button" className="add-line" onClick={addVideo}>+ Agregar otro video</button>
      ```

    **Panel 05 — Redes sociales (Opcional):** clase `panel`, marginBottom 16.
    - `<SectionHead n="05" title="Redes sociales" sub="Opcional" />`
    - Render array `form.socials` con el mismo patrón que videos pero con prefijo `@` y placeholder `"instagram.com/tu-evento"`. Botón "+ Agregar otra red social".

    **Panel 06 — Administración (admin-only):** SOLO renderizar si `isAdmin === true`.
    ```tsx
    {isAdmin && (
      <div className="panel" style={{ marginBottom: 16 }}>
        <SectionHead n="06" title="Administración" sub="Solo admin" />
        <div className="field" style={{ marginBottom: 0 }}>
          <label>Estado de publicación</label>
          <select value={form.status} onChange={(e) => set("status", e.target.value as FormData["status"])}>
            <option value="APPROVED">Publicado (directo)</option>
            <option value="PENDING_MODERATION">En revisión</option>
            <option value="DRAFT">Borrador</option>
          </select>
          <div className="help">Define en qué estado queda el evento al guardarlo.</div>
        </div>
      </div>
    )}
    ```

    Después de escribir el contenido, ejecuta `pnpm --filter website tsc --noEmit` para verificar tipos.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && pnpm --filter website tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<SectionHead n=' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 6 (one per panel — Panel 06 is conditional but the JSX still contains 6 SectionHead elements).
    - `grep -n 'n="01"\|n="02"\|n="03"\|n="04"\|n="05"\|n="06"' apps/website/app/dashboard/events/EventForm.tsx` returns 6 lines.
    - `grep -n 'title="Información básica"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n 'title="Precio"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n 'title="Fechas, horario y ubicación"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n 'title="Multimedia"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n 'title="Redes sociales"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n 'title="Administración"' apps/website/app/dashboard/events/EventForm.tsx` returns 1 match.
    - `grep -n '+ Agregar otra tarifa\|+ Agregar otro día\|+ Agregar otro video\|+ Agregar otra red social' apps/website/app/dashboard/events/EventForm.tsx` returns at least 4 matches (one per dynamic array).
    - `grep -c 'className="add-line"' apps/website/app/dashboard/events/EventForm.tsx` returns at least 4 matches.
    - `grep -c 'className="price-row"' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match.
    - `grep -n 'className="upload-grid"' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (banner + poster row).
    - `grep -c '<ImageUploadBox' apps/website/app/dashboard/events/EventForm.tsx` returns at least 3 matches (banner, poster, gallery slots inside .map — minimum 3 source occurrences).
    - `grep -n 'gridTemplateColumns: "repeat(4, 1fr)"' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (gallery grid 4 cols).
    - `grep -n '{isAdmin &&' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (Panel 06 conditional render).
    - `grep -n 'Hasta 8 imágenes' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match.
    - `grep -c '<select' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 5 (4 selects de catálogo: Categoría, País, Región, Ciudad + 1 select de status en Panel 06). NO debe haber más selects en otros lugares.
    - `pnpm --filter website tsc --noEmit` exits with code 0 (no TS errors).
  </acceptance_criteria>
  <done>
    El cuerpo del formulario renderiza los 6 paneles según el UI-SPEC: 01 Información básica (campos escalares), 02 Precio (checkbox + array prices con .price-row), 03 Fechas/horario/ubicación (array dates + cascade country/state/city + dirección), 04 Multimedia (upload-box banner+poster en upload-grid + galería 8 slots en grid 4col + array videos), 05 Redes sociales (array socials con @-prefix), 06 Administración (admin-only, con status-select). El archivo sigue compilando con tsc --noEmit limpio.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Footer sticky (con CTA dinámico) — sin status-select</name>
  <files>apps/website/app/dashboard/events/EventForm.tsx</files>
  <read_first>
    - apps/website/app/dashboard/events/EventForm.tsx (estado tras tarea 2)
    - .planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-UI-SPEC.md (CTA Principal — label dinámico BLOQUEADO; Footer sticky — contrato de layout CRÍTICO)
    - .planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-RESEARCH.md (Pattern 1: Footer correcto; Pattern 2: tabla de labels; Pitfall #1 y #2)
    - apps/website/app/globals.css líneas 348-354 (clases .form-foot, .form-foot > .container)
  </read_first>
  <action>
    Añade al final del JSX (justo antes del cierre del `</div>` raíz del componente, dentro del return), el footer sticky. El footer DEBE usar la clase CSS `.form-foot` y NO redefinir position:fixed con inline styles.

    **CRÍTICO:** Este footer NO contiene ningún `<select>`. Si el código actual tiene un select en el footer (Pitfall #1), DEBE eliminarse en esta tarea. El status-select ya está en Panel 06 (cuerpo) tras la tarea 2.

    **Label del CTA principal — derivación obligatoria:**

    ```tsx
    // Compute CTA label from mode × form.status — tabla BLOQUEADA en UI-SPEC
    const ctaLabel = busy
      ? "Guardando…"
      : mode === "create"
        ? form.status === "APPROVED"           ? "Crear y publicar →"
          : form.status === "PENDING_MODERATION" ? "Crear en revisión →"
          :                                        "Crear borrador →"
        : form.status === "APPROVED"           ? "Guardar y publicar →"
          : form.status === "PENDING_MODERATION" ? "Guardar en revisión →"
          :                                        "Guardar borrador →";
    ```

    Coloca esta derivación dentro del componente (después de los handlers, antes del return).

    **Estructura exacta del footer (añadir como ÚLTIMO hijo del div raíz `maxWidth: 900`):**

    ```tsx
    <div className="form-foot">
      <div className="container">
        {/* IZQUIERDA: solo texto informativo (NUNCA un select) */}
        <span style={{ fontSize: 13, color: "var(--ink-3)" }}>
          {mode === "create"
            ? "Creando como admin · sin checkout ni upsell."
            : `Editando evento #${initial?.id} · no se notifica al organizador.`}
        </span>

        {/* DERECHA: 3 botones en orden fijo */}
        <div style={{ display: "flex", gap: 10 }}>
          <Link href="/dashboard/events" className="btn ghost">Cancelar</Link>
          <button
            type="button"
            className="btn dark"
            disabled={busy}
            onClick={() => handleSubmit("DRAFT")}
          >
            Guardar borrador
          </button>
          <button
            type="button"
            className="btn primary"
            disabled={busy}
            onClick={() => handleSubmit(form.status)}
          >
            {ctaLabel}
          </button>
        </div>
      </div>
    </div>
    ```

    **Eliminar cualquier resto del footer anterior** (el footer original usaba `position: fixed` con inline styles y contenía un `<select>` con "Estado:" — todo eso debe quedar borrado).

    **Validación negativa post-edit:**
    - Confirma que NO existe la string `"Enviar a revisión →"` en el archivo (era el label incorrecto del original).
    - Confirma que NO existe la string `"Estado:"` en el archivo (solo aparecía dentro del footer viejo; el Panel 06 usa el label `"Estado de publicación"` que sí está permitido).
    - Confirma que dentro del bloque `<div className="form-foot">` no hay ningún `<select`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && pnpm --filter website tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'className="form-foot"' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match.
    - `grep -nE 'position: *"fixed".*bottom: *0' apps/website/app/dashboard/events/EventForm.tsx` returns NO matches (no inline fixed footer left over).
    - The 6 CTA labels appear verbatim in the file. Each of these greps returns at least 1 match:
      - `grep -n 'Crear y publicar →' apps/website/app/dashboard/events/EventForm.tsx`
      - `grep -n 'Crear en revisión →' apps/website/app/dashboard/events/EventForm.tsx`
      - `grep -n 'Crear borrador →' apps/website/app/dashboard/events/EventForm.tsx`
      - `grep -n 'Guardar y publicar →' apps/website/app/dashboard/events/EventForm.tsx`
      - `grep -n 'Guardar en revisión →' apps/website/app/dashboard/events/EventForm.tsx`
      - `grep -n 'Guardar borrador →' apps/website/app/dashboard/events/EventForm.tsx`
    - The OLD incorrect label is GONE: `grep -n 'Enviar a revisión →' apps/website/app/dashboard/events/EventForm.tsx` returns NO matches.
    - The "Estado:" label of the OLD footer is GONE: `grep -nE '"Estado:"|>Estado:<' apps/website/app/dashboard/events/EventForm.tsx` returns NO matches. (The Panel 06 label is `"Estado de publicación"` which contains "Estado" but ends with " de publicación" — this is different.)
    - The dark "Guardar borrador" button calls handleSubmit("DRAFT"): `grep -n 'handleSubmit("DRAFT")' apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match.
    - The primary CTA button calls handleSubmit(form.status): `grep -n 'handleSubmit(form.status)' apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match.
    - The footer contains the Link to /dashboard/events with btn ghost: `grep -nE '<Link[^>]*href="/dashboard/events"[^>]*className="btn ghost"' apps/website/app/dashboard/events/EventForm.tsx` returns at least 1 match.
    - **CRITICAL: No `<select>` inside `.form-foot`.** Run:
      ```
      awk '/className="form-foot"/,/<\/div>[[:space:]]*<\/div>[[:space:]]*\);/' apps/website/app/dashboard/events/EventForm.tsx | grep -c '<select'
      ```
      Result MUST be 0. (Captures the footer JSX block and counts selects — must be zero.) If awk is unavailable, equivalent check: extract lines from `className="form-foot"` to end of file and grep for `<select` — must be 0.
    - `grep -n 'const ctaLabel' apps/website/app/dashboard/events/EventForm.tsx` returns exactly 1 match (label computed via the locked table).
    - `pnpm --filter website tsc --noEmit` exits with code 0 (no TS errors).
    - Manual verification (executor records in summary): visit `/dashboard/events/new` and `/dashboard/events/{id}/edit` — confirm visually that footer has only info text on the left and 3 buttons on the right, no select; change the status in Panel 06 and verify the CTA label updates per the table.
  </acceptance_criteria>
  <done>
    El footer sticky usa exclusivamente la clase `.form-foot` (sin inline `position: fixed`). Contiene en orden: span con texto informativo (izquierda), Link "Cancelar", button "Guardar borrador" (handleSubmit DRAFT), button CTA primario (handleSubmit form.status) con label derivado de la tabla bloqueada del UI-SPEC. No queda ningún `<select>` ni la string "Estado:" dentro del footer. La string "Enviar a revisión →" (label incorrecto previo) está completamente removida del archivo. El proyecto compila sin errores TS.
  </done>
</task>

</tasks>

<verification>
**Verificación de fase (después de las 3 tareas):**

1. **Build limpio:**
   ```bash
   cd /home/gab/Code/konbini-project && pnpm --filter website tsc --noEmit
   ```
   Debe terminar con código 0, sin `error TS`.

2. **Smoke test manual (executor lo hace y registra en SUMMARY):**
   - Arranca dev server (`pnpm --filter website dev`).
   - Visita `http://localhost:3000/dashboard/events/new` como admin.
   - Verifica: 5 paneles + Panel 06 admin visibles. Footer sticky abajo con texto + 3 botones, sin select.
   - Cambia el status en Panel 06 a "Publicado (directo)" → CTA muestra "Crear y publicar →".
   - Cambia a "En revisión" → CTA muestra "Crear en revisión →".
   - Cambia a "Borrador" → CTA muestra "Crear borrador →".
   - Click en una upload-box de banner → abre file picker → seleccionar imagen → preview aparece.
   - Click "+ Agregar otra tarifa" → fila nueva en .price-row.
   - Visita `/dashboard/events/{id}/edit` de un evento existente con banner → preview ya cargado, mismo flujo de CTAs (mode=edit).

3. **Greps de regresión (deben pasar todos):**
   - `grep -c '<select' EventForm.tsx` = 5 (4 catálogo + 1 status en Panel 06).
   - `grep -c 'className="form-foot"' EventForm.tsx` = 1.
   - `grep -c 'Enviar a revisión' EventForm.tsx` = 0 (label viejo eliminado).
   - Las 6 CTA labels presentes; `handleSubmit("DRAFT")` y `handleSubmit(form.status)` presentes; `isAdmin` correctamente derivado de `useUser().user?.role`.

4. **Compatibilidad de consumidores:**
   - `apps/website/app/dashboard/events/new/page.tsx` no se tocó — sigue importando `EventForm` (sin tipo).
   - `apps/website/app/dashboard/events/[id]/edit/page.tsx` no se tocó — sigue importando `EventForm, InitialEvent`. La extensión `gallery?: string[]` es opcional, así que el backend que aún no devuelva `gallery` no rompe el tipo.
</verification>

<success_criteria>
- **Paridad con diseño:** Los 5 paneles del cuerpo siguen el shape y orden del UI-SPEC; el Panel 06 admin se renderiza condicional.
- **Footer correcto:** Sin select, con texto informativo a la izquierda y 3 botones a la derecha en el orden Cancelar / Guardar borrador / CTA dinámico.
- **CTA labels exactos:** Las 6 combinaciones mode × status producen las strings literales bloqueadas en UI-SPEC (verificable por grep).
- **Uploads funcionales:** Banner, poster y galería se suben con file picker, muestran preview inmediato vía `URL.createObjectURL`, y suben al server en `handleSubmit` vía `api.uploadImage`. El estado se limpia tras upload para evitar re-uploads (Pitfall #4).
- **Arrays dinámicos operativos:** prices, dates, socials, videos pueden agregar/eliminar filas; gallery tiene 8 slots fijos.
- **Cero regresiones:** `pnpm --filter website tsc --noEmit` pasa limpio; `/dashboard/events/new` y `/dashboard/events/{id}/edit` cargan sin errores; los pages consumidores no requieren cambios.
- **Smoke manual del usuario:** El usuario (que reportó el bug original) puede crear un evento como admin y ver el CTA actualizarse según el status seleccionado en Panel 06, sin el select en el footer.
</success_criteria>

<output>
Tras completar las 3 tareas:

1. Generar `.planning/phases/16-necesito-que-el-formulario-de-eventos-del-dashboard-sea-igual-al-formulario-de-eventos-del-dashboard-de-design/16-01-SUMMARY.md` con:
   - Lista de cambios realizados (qué se borró, qué se añadió).
   - Resultado de `tsc --noEmit`.
   - Resultado de los greps de acceptance criteria (capturar contadores exactos).
   - Notas del smoke test manual (qué se probó en navegador).
   - Cualquier decisión de discreción tomada (ej: si se eliminó tags/AI-suggest, si se omitió venue por defecto, etc.).
   - Pitfalls evitados explícitamente (referencia a Pitfall #1, #2, #3, #4, #5, #6 del RESEARCH).

2. Commit con mensaje:
   ```
   feat(dashboard/events): EventForm con arrays dinámicos, uploads y footer correcto

   - Footer sin status-select (status va en Panel 06 admin-only)
   - CTA labels exactos según UI-SPEC (6 combinaciones mode × status)
   - Banner/poster/galería con file picker + preview + api.uploadImage
   - prices/dates/socials/videos como arrays dinámicos
   - InitialEvent extendido con gallery?: string[]
   ```
</output>
