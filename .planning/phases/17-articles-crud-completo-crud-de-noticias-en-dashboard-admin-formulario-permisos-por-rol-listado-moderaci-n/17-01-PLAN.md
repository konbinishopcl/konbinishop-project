---
phase: 17-articles-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/articles/articles.controller.ts
  - apps/api/src/articles/articles.service.ts
autonomous: true
requirements:
  - ART-08  # GET /articles/mine — listar artículos propios del organizador
  - ART-09  # Ownership en PATCH/DELETE — owner OR admin
user_setup: []

must_haves:
  truths:
    - "Un organizador autenticado puede listar SUS PROPIOS artículos via GET /articles/mine"
    - "Un organizador puede editar (PATCH) y eliminar (DELETE) un artículo cuyo userId coincida con su user.sub"
    - "Un usuario no-owner / no-admin recibe 403 Forbidden al intentar editar/eliminar artículo ajeno"
    - "ADMIN y SUPER_ADMIN siguen pudiendo editar/eliminar cualquier artículo (sin regresiones)"
    - "GET /articles/mine respeta el org context (X-Org-Context) — devuelve artículos del orgId si presente"
  artifacts:
    - path: "apps/api/src/articles/articles.controller.ts"
      provides: "Endpoint GET /articles/mine + PATCH/DELETE con guards relajados (JwtAuth, no RolesGuard estricto)"
      contains: "@Get('mine')"
    - path: "apps/api/src/articles/articles.service.ts"
      provides: "findMine(user, orgContext) + assertOwnerOrAdmin(article, user) helper"
      contains: "findMine"
  key_links:
    - from: "ArticlesController.update / remove"
      to: "ArticlesService.assertOwnerOrAdmin"
      via: "service call before mutation"
      pattern: "assertOwnerOrAdmin"
    - from: "ArticlesController.findMine"
      to: "ArticlesService.findMine"
      via: "service delegate"
      pattern: "this.articles.findMine"
---

<objective>
Habilitar el CRUD del organizador a nivel API. Hoy `PATCH /articles/:id` y `DELETE /articles/:id` requieren rol ADMIN+, y no existe `GET /articles/mine`. El usuario decidió (decisión #2) que el organizador debe poder gestionar sus propios artículos patrocinados desde `/cuenta` — esta fase desbloquea esa capacidad sin abrir agujeros (ownership check service-side).

Purpose: Sin estos cambios, el frontend del organizador en `/cuenta/articulos` no tiene endpoints reales (hoy llama a `?mine=true` que no funciona). Plan 17-04 depende de este plan.
Output: 1 endpoint nuevo (`GET /articles/mine`) + ownership check en `update` y `remove`. Sin cambios de schema. Patrón idéntico a `events/mine` (ver `apps/api/src/events/events.controller.ts:52` + `events.service.ts`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-RESEARCH.md

# Source de verdad — el endpoint /mine de events es el patrón EXACTO a replicar
@apps/api/src/events/events.controller.ts
@apps/api/src/events/events.service.ts

# Source actual a modificar
@apps/api/src/articles/articles.controller.ts
@apps/api/src/articles/articles.service.ts

<interfaces>
<!-- Tipos clave que el executor necesita. -->

Article model (Prisma): Article.userId es Int? (nullable — null = editorial, non-null = sponsored).
JwtUser shape (from apps/api/src/auth/current-user.decorator.ts):
```typescript
export type JwtUser = { sub: number; email: string; role: 'AUTHENTICATED' | 'ADMIN' | 'SUPER_ADMIN' };
```

OrgContextDto shape (from apps/api/src/common/org-context/org-context.types.ts):
```typescript
export type OrgContextDto = { orgId: number } | null;
```

Patrón findMine en events (apps/api/src/events/events.service.ts — buscar 'findMine'):
- ownerId = orgContext?.orgId ?? user.sub
- where: { userId: ownerId }
- include ARTICLE_INCLUDE
- orderBy createdAt desc

Patrón assert ownership (apps/api/src/events/events.service.ts — buscar 'assertOwnerOrAdmin' o ensureOwnership):
- if user.role === 'ADMIN' || 'SUPER_ADMIN' → pass
- if article.userId === null → solo admin puede tocarla (editorial)
- if article.userId !== user.sub && article.userId !== orgContext?.orgId → ForbiddenException
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar GET /articles/mine + service.findMine()</name>
  <files>apps/api/src/articles/articles.controller.ts, apps/api/src/articles/articles.service.ts</files>
  <read_first>
    - apps/api/src/articles/articles.controller.ts (estado actual)
    - apps/api/src/articles/articles.service.ts (ARTICLE_INCLUDE, patrones existentes)
    - apps/api/src/events/events.controller.ts (buscar @Get('mine') — replicar EXACTO)
    - apps/api/src/events/events.service.ts (buscar findMine — replicar EXACTO)
    - apps/api/src/common/org-context/org-context.guard.ts (entender pass-through con X-Org-Context ausente)
  </read_first>
  <behavior>
    - GET /articles/mine sin token → 401 (JwtAuthGuard rechaza)
    - GET /articles/mine con token de USER personal → devuelve articles where userId === user.sub
    - GET /articles/mine con header X-Org-Context: <orgId> → devuelve articles where userId === orgId
    - Response shape: array de Article con tags + _count.likes (ARTICLE_INCLUDE), orderBy createdAt desc
    - Cada item incluye isSponsored = (userId !== null) — siempre true en /mine porque editoriales no tienen userId
  </behavior>
  <action>
    1. En `apps/api/src/articles/articles.service.ts`, agregar método `findMine` después de `findById` (~ línea 110):

    ```typescript
    findMine(user: JwtUser, orgContext: OrgContextDto | null = null) {
      const ownerId = orgContext?.orgId ?? user.sub;
      return this.prisma.article.findMany({
        where: { userId: ownerId },
        include: ARTICLE_INCLUDE,
        orderBy: { createdAt: 'desc' },
      }).then((items) => items.map((a) => ({ ...a, isSponsored: a.userId !== null })));
    }
    ```

    2. En `apps/api/src/articles/articles.controller.ts`, agregar endpoint ANTES de `@Get(':slug')` (CRÍTICO: si va después, `:slug` capturaría "mine" como slug literal):

    ```typescript
    @Get('mine')
    @UseGuards(JwtAuthGuard, OrgContextGuard)
    @ApiBearerAuth()
    @ApiOperation({ summary: 'Listar artículos del usuario autenticado (u org si X-Org-Context)' })
    findMine(@CurrentUser() user: JwtUser, @OrgContext() ctx: OrgContextDto | null) {
      return this.articles.findMine(user, ctx);
    }
    ```

    3. Importar `OrgContextGuard` y `OrgContext` y `OrgContextDto` desde `../common/org-context/...` — ya están importados parcialmente, verificar línea 25-27 del controller.

    4. Verificar orden de rutas en controller: `@Get()` (línea 38) → `@Get('mine')` NUEVO → `@Get(':slug')` (línea 46). NestJS usa orden de declaración para route matching cuando hay conflictos.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/api && pnpm build && grep -n "findMine\|@Get('mine')" src/articles/articles.controller.ts src/articles/articles.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@Get('mine')" apps/api/src/articles/articles.controller.ts` retorna línea entre @Get() y @Get(':slug')
    - `grep -n "findMine" apps/api/src/articles/articles.service.ts` retorna definición del método
    - `pnpm build` en apps/api compila sin errores TypeScript
    - El controller importa `OrgContextGuard`, `OrgContext`, y `OrgContextDto` (grep para verificar)
    - Smoke manual: `curl -H "Authorization: Bearer $TOKEN" http://localhost:3333/api/articles/mine` → JSON array (vacío o con items, NO 404)
  </acceptance_criteria>
  <done>
    Endpoint GET /articles/mine responde 200 con array de artículos del owner. Build TS verde.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relajar guards de PATCH/DELETE /articles/:id — owner OR admin</name>
  <files>apps/api/src/articles/articles.controller.ts, apps/api/src/articles/articles.service.ts</files>
  <read_first>
    - apps/api/src/articles/articles.controller.ts (decoradores actuales en update/remove — líneas 78-94)
    - apps/api/src/articles/articles.service.ts (update y remove — líneas 129-154)
    - apps/api/src/events/events.service.ts (buscar 'ForbiddenException' y patrón ownership check en update/remove de events)
  </read_first>
  <behavior>
    - PATCH /articles/:id con token de owner del artículo → 200, actualiza
    - PATCH /articles/:id con token de admin → 200, actualiza (sin regresión)
    - PATCH /articles/:id con token de otro usuario (no owner, no admin) → 403 Forbidden
    - PATCH /articles/:id contra artículo editorial (userId=null) por usuario no-admin → 403 (solo admin edita editoriales)
    - DELETE /articles/:id mismo patrón que PATCH
    - Con header X-Org-Context: <orgId>, el ownership check usa orgId como effective user
  </behavior>
  <action>
    1. En `apps/api/src/articles/articles.service.ts`, agregar helper privado después de `findById`:

    ```typescript
    private assertOwnerOrAdmin(
      article: { userId: number | null },
      user: JwtUser,
      orgContext: OrgContextDto | null = null,
    ): void {
      const isAdmin = user.role === 'ADMIN' || user.role === 'SUPER_ADMIN';
      if (isAdmin) return;
      if (article.userId === null) {
        throw new ForbiddenException('Solo administradores pueden modificar artículos editoriales');
      }
      const effectiveUserId = orgContext?.orgId ?? user.sub;
      if (article.userId !== effectiveUserId) {
        throw new ForbiddenException('No tienes permiso para modificar este artículo');
      }
    }
    ```

    2. Modificar `update(id, dto)` para aceptar user + orgContext:

    ```typescript
    async update(id: number, dto: UpdateArticleDto, user: JwtUser, orgContext: OrgContextDto | null = null) {
      const article = await this.findById(id);
      this.assertOwnerOrAdmin(article, user, orgContext);
      // ... resto del método igual (slug conflict check + update)
    }
    ```

    3. Modificar `remove(id)` igual:

    ```typescript
    async remove(id: number, user: JwtUser, orgContext: OrgContextDto | null = null) {
      const article = await this.findById(id);
      this.assertOwnerOrAdmin(article, user, orgContext);
      await this.prisma.article.delete({ where: { id } });
      return { deleted: true };
    }
    ```

    4. En `articles.controller.ts`:
       - Cambiar `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles('ADMIN','SUPER_ADMIN')` en update/remove por `@UseGuards(JwtAuthGuard, OrgContextGuard)` (sin RolesGuard, sin @Roles).
       - Pasar `user` y `ctx` a los service calls:

    ```typescript
    @Patch(':id')
    @UseGuards(JwtAuthGuard, OrgContextGuard)
    @ApiBearerAuth()
    @ApiOperation({ summary: 'Editar artículo (owner o ADMIN+)' })
    update(
      @Param('id', ParseIntPipe) id: number,
      @Body() dto: UpdateArticleDto,
      @CurrentUser() user: JwtUser,
      @OrgContext() ctx: OrgContextDto | null,
    ) {
      return this.articles.update(id, dto, user, ctx);
    }

    @Delete(':id')
    @UseGuards(JwtAuthGuard, OrgContextGuard)
    @ApiBearerAuth()
    @ApiOperation({ summary: 'Eliminar artículo (owner o ADMIN+)' })
    remove(
      @Param('id', ParseIntPipe) id: number,
      @CurrentUser() user: JwtUser,
      @OrgContext() ctx: OrgContextDto | null,
    ) {
      return this.articles.remove(id, user, ctx);
    }
    ```

    5. Importar `ForbiddenException` de `@nestjs/common` en service si no está ya (ya está — línea 4).

    6. NO tocar approve/reject/ban — siguen siendo ADMIN+ only (moderación).
    7. NO tocar POST / (create editorial) — sigue siendo ADMIN+ only.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/api && pnpm build && grep -c "assertOwnerOrAdmin" src/articles/articles.service.ts && grep -E "@Roles\\('ADMIN', 'SUPER_ADMIN'\\)" src/articles/articles.controller.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assertOwnerOrAdmin" apps/api/src/articles/articles.service.ts` retorna >= 3 (definición + 2 usos en update/remove)
    - El controller en `update` y `remove` ya NO tiene `@Roles('ADMIN', 'SUPER_ADMIN')` (sí debe seguir en approve/reject/ban/POST)
    - `grep -E "@Roles\\('ADMIN', 'SUPER_ADMIN'\\)" apps/api/src/articles/articles.controller.ts | wc -l` retorna exactamente 4 (POST create + approve + reject + ban — no más)
    - `pnpm build` compila sin errores
    - Smoke: como organizador propietario, `curl -X PATCH -H "Authorization: Bearer $OWNER_TOKEN" .../articles/$MINE_ID` con body válido → 200
    - Smoke: como otro usuario, mismo PATCH → 403
  </acceptance_criteria>
  <done>
    Owner o admin pueden PATCH/DELETE; otros usuarios reciben 403. Approve/reject/ban siguen ADMIN-only.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` en apps/api pasa sin errores
- Endpoint nuevo /articles/mine responde 200/401 correctamente
- update/remove respetan ownership (200 owner, 200 admin, 403 otro)
- Sin regresiones en approve/reject/ban (siguen ADMIN-only — 4 ocurrencias de @Roles)
</verification>

<success_criteria>
1. GET /articles/mine retorna lista filtrada por owner
2. PATCH /articles/:id acepta owner o admin
3. DELETE /articles/:id acepta owner o admin
4. Moderación (approve/reject/ban) sigue ADMIN-only
5. Build limpio
</success_criteria>

<output>
After completion, create `.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-01-SUMMARY.md`
</output>
