---
phase: 17-articles-crud
plan: 03
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - apps/website/app/dashboard/sections/ArticlesSection.tsx
  - apps/website/app/dashboard/articles/new/page.tsx
  - apps/website/app/dashboard/articles/[slug]/edit/page.tsx
  - apps/website/app/dashboard/DashboardShell.tsx
  - apps/website/app/dashboard/modals/AdminArticleEditor.tsx
autonomous: true
requirements:
  - ART-01  # ArticlesSection carga datos reales de /api/articles
  - ART-02  # Approve/reject/ban llaman a la API y recargan lista
  - ART-07  # Rutas /dashboard/articles/new y /[slug]/edit muestran crumbs correctos
user_setup: []

must_haves:
  truths:
    - "ArticlesSection lista artículos REALES desde GET /api/articles (admin ve todos los estados)"
    - "Filtros (Todos / Borrador / En revisión / Publicado / Rechazado / Baneado) llaman al endpoint con ?status= correcto"
    - "Paginación funciona — page, pageSize, totalPages reflejan respuesta del API"
    - "Acción Aprobar llama PATCH /api/articles/:id/approve y recarga lista"
    - "Acción Rechazar llama PATCH /api/articles/:id/reject con motivo y recarga lista"
    - "Acción Banear llama PATCH /api/articles/:id/ban con motivo y recarga lista"
    - "Acción Eliminar llama DELETE /api/articles/:id con confirmación 'ELIMINAR' typed"
    - "Botón '+ Crear artículo' navega a /dashboard/articles/new con ArticleForm mode=create variant=admin"
    - "Botón 'Editar' navega a /dashboard/articles/{slug}/edit con ArticleForm mode=edit variant=admin"
    - "Breadcrumb del DashboardShell muestra 'ARTÍCULOS / NUEVO' y 'ARTÍCULOS / EDITAR' correctamente"
    - "El componente roto AdminArticleEditor (modal con endpoint inexistente) queda eliminado o deprecado"
  artifacts:
    - path: "apps/website/app/dashboard/sections/ArticlesSection.tsx"
      provides: "Listado dashboard admin con datos reales, paginación, filtros, modales conectados"
      contains: "fetchArticles"
      min_lines: 400
    - path: "apps/website/app/dashboard/articles/new/page.tsx"
      provides: "Página de creación de artículo admin — renderiza ArticleForm mode=create variant=admin"
      contains: "ArticleForm"
    - path: "apps/website/app/dashboard/articles/[slug]/edit/page.tsx"
      provides: "Página de edición admin — fetch /api/articles/:slug, renderiza ArticleForm mode=edit variant=admin"
      contains: "ArticleForm"
    - path: "apps/website/app/dashboard/DashboardShell.tsx"
      provides: "Entradas SUB_PAGES + regex match para articles/new y articles/[slug]/edit"
      contains: "/dashboard/articles/new"
  key_links:
    - from: "ArticlesSection.tsx"
      to: "/api/articles"
      via: "fetch con Authorization header"
      pattern: "fetch\\(`/api/articles\\?"
    - from: "ArticlesSection.tsx (acciones)"
      to: "/api/articles/:id/{approve,reject,ban}"
      via: "fetch PATCH"
      pattern: "/api/articles/.*\\/(approve|reject|ban)"
    - from: "/dashboard/articles/new/page.tsx"
      to: "ArticleForm component"
      via: "import + render con variant=admin mode=create"
      pattern: "variant=\"admin\""
    - from: "/dashboard/articles/[slug]/edit/page.tsx"
      to: "/api/articles/:slug"
      via: "fetch en useEffect"
      pattern: "fetch.*\\/api\\/articles\\/"
---

<objective>
Conectar la sección admin de artículos a la API real (hoy es 100% mock) y agregar las rutas `/dashboard/articles/new` y `/dashboard/articles/[slug]/edit` que renderizan `<ArticleForm variant="admin" />` (del Plan 17-02). Actualizar `DashboardShell` con los breadcrumbs correspondientes. Eliminar el modal roto `AdminArticleEditor` (endpoint inexistente — bug pre-existente identificado por advisor).

Purpose: Admin necesita CRUD completo + moderación real. Hoy ArticlesSection muestra eventos mock como si fueran artículos y los modales no llaman al backend. Este plan cierra ese gap.
Output: ArticlesSection conectada, 2 rutas nuevas, breadcrumb actualizado, modal roto eliminado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-RESEARCH.md

# Patrón a replicar — EventsSection conectada con paginación, modales, acciones
@apps/website/app/dashboard/sections/EventsSection.tsx

# Estado actual a reescribir (mock)
@apps/website/app/dashboard/sections/ArticlesSection.tsx

# Modal roto a eliminar (endpoint /api/articles/edit no existe)
@apps/website/app/dashboard/modals/AdminArticleEditor.tsx

# Shell que necesita entradas SUB_PAGES
@apps/website/app/dashboard/DashboardShell.tsx

# Componente que se importa (del Plan 17-02)
# apps/website/app/dashboard/articles/ArticleForm.tsx (creado por 17-02)

<interfaces>
<!-- Interfaces clave. NO explorar el codebase, usar estas. -->

ArticleForm component (creado por Plan 17-02):
```typescript
import { ArticleForm, type InitialArticle } from "../ArticleForm";

interface Props {
  mode: "create" | "edit";
  variant: "admin" | "sponsored";
  initial?: InitialArticle;
}

export type InitialArticle = {
  id: number;
  title: string;
  slug: string;
  excerpt: string | null;
  content: string;
  image: string | null;
  status: string;
  tags: { id: number; name: string; slug: string }[];
  events?: { id: number; title: string }[];
};
```

Article API response (GET /api/articles — admin ve todos):
```typescript
type ApiArticle = {
  id: number;
  title: string;
  slug: string;
  excerpt: string | null;
  image: string | null;
  status: "DRAFT" | "PENDING_PAYMENT" | "PENDING_MODERATION" | "APPROVED" | "REJECTED" | "BANNED";
  statusReason: string | null;
  userId: number | null;
  createdAt: string;
  tags: { id: number; name: string; slug: string }[];
  _count: { likes: number };
  isSponsored: boolean;
};

type PaginatedResponse = {
  items: ApiArticle[];
  total: number;
  page: number;
  pageSize: number;
  totalPages: number;
};
```

useUser() hook:
```typescript
const { token, user } = useUser();
```

API endpoints (TODOS via /api proxy con Authorization Bearer):
- GET /api/articles?page=&pageSize=&status=  → PaginatedResponse
- GET /api/articles/:slug  → single ApiArticle (con detalles)
- PATCH /api/articles/:id  → editar (Plan 17-01 relajó guards)
- DELETE /api/articles/:id → eliminar (Plan 17-01 relajó guards)
- PATCH /api/articles/:id/approve
- PATCH /api/articles/:id/reject { reason }
- PATCH /api/articles/:id/ban { reason }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescribir ArticlesSection con datos reales + paginación + modales conectados</name>
  <files>apps/website/app/dashboard/sections/ArticlesSection.tsx</files>
  <read_first>
    - apps/website/app/dashboard/sections/EventsSection.tsx (PATRÓN COMPLETO — replicar 1:1 adaptando a articles)
    - apps/website/app/dashboard/sections/ArticlesSection.tsx (estado actual mock — reemplazar)
    - design/Konbini.html (buscar "a-table" — confirmar columnas del listado de artículos)
  </read_first>
  <behavior>
    - Carga inicial: GET /api/articles?page=1&pageSize=12 con token admin → lista todos los estados
    - Filtros: chips "Todos / Borrador / En revisión / Publicado / Rechazado / Baneado" — al hacer click, resetea a page=1 y refetch
    - Columnas: ARTÍCULO (thumb + title + tags chips) · AUTOR (userId — "Editorial" si null, "ID #N" si sponsored, mejor si se puede mostrar nombre) · CREADO (fmtDate) · ESTADO (pill) · ACCIONES
    - Acciones por estado:
      - DRAFT: Editar (link), Publicar (modal approve), Eliminar (confirm "ELIMINAR")
      - PENDING_MODERATION: Editar, ✓ Aprobar (modal), ✕ Rechazar (modal con motivo)
      - APPROVED: Editar, Banear (confirm "BANEAR" + motivo)
      - REJECTED: Editar, Re-revisar (modal approve), Eliminar (confirm "ELIMINAR")
      - BANNED: Editar, Restaurar (modal approve)
    - Paginación: idéntica a EventsSection (pageWindows + chevrons)
    - Botón "+ Crear artículo" → Link a /dashboard/articles/new
    - Botón "Editar" → Link a /dashboard/articles/{slug}/edit (usando slug, no id — D-#URL del advisor)
    - Loading state, empty state, error toast — exactly como EventsSection
  </behavior>
  <action>
    Reemplazar TODO el contenido de `apps/website/app/dashboard/sections/ArticlesSection.tsx`.

    Estructura (replicar EventsSection adaptado):

    ```typescript
    "use client";
    import Link from "next/link";
    import { useCallback, useEffect, useState } from "react";
    import { toast } from "sonner";
    import { useUser } from "@/components/providers";

    // ── Types ────────────────────────────────────────────────
    type ApiStatus = "DRAFT" | "PENDING_PAYMENT" | "PENDING_MODERATION" | "APPROVED" | "REJECTED" | "BANNED";
    type DisplayStatus = "Borrador" | "En revisión" | "Publicado" | "Rechazado" | "Baneado";

    type ApiArticle = {
      id: number;
      title: string;
      slug: string;
      excerpt: string | null;
      image: string | null;
      status: ApiStatus;
      statusReason: string | null;
      userId: number | null;
      createdAt: string;
      tags: { id: number; name: string; slug: string }[];
      _count: { likes: number };
      isSponsored: boolean;
    };

    type ModalState =
      | { type: "approve"; item: ApiArticle }
      | { type: "reject";  item: ApiArticle }
      | { type: "ban";     item: ApiArticle }
      | { type: "delete";  item: ApiArticle }
      | null;

    const STATUS_API: Record<string, string | undefined> = {
      "Todos":       undefined,
      "Borrador":    "DRAFT",
      "En revisión": "PENDING_MODERATION",
      "Publicado":   "APPROVED",
      "Rechazado":   "REJECTED",
      "Baneado":     "BANNED",
    };

    const FILTER_STATUSES = ["Todos", "Borrador", "En revisión", "Publicado", "Rechazado", "Baneado"];
    const PER_PAGE_OPTIONS = [12, 24, 48];

    // ── Helpers ──────────────────────────────────────────────
    const MONTHS = ["ENE","FEB","MAR","ABR","MAY","JUN","JUL","AGO","SEP","OCT","NOV","DIC"];

    function toDisplay(s: ApiStatus): DisplayStatus {
      if (s === "DRAFT") return "Borrador";
      if (s === "PENDING_MODERATION") return "En revisión";
      if (s === "APPROVED") return "Publicado";
      if (s === "REJECTED") return "Rechazado";
      return "Baneado";
    }

    function statusCls(s: DisplayStatus) {
      if (s === "Borrador") return "dft";
      if (s === "En revisión") return "rev";
      if (s === "Publicado") return "pub";
      return "rej";
    }

    function fmtDate(d: string) {
      const dt = new Date(d);
      return `${dt.getUTCDate()} ${MONTHS[dt.getUTCMonth()]} ${dt.getUTCFullYear()}`;
    }

    function imageSrc(image: string | null) {
      if (!image) return null;
      if (image.startsWith("http")) return image;
      return `/api/media${image}`;
    }

    function pageWindows(current: number, total: number): (number | "…")[] {
      if (total <= 7) return Array.from({ length: total }, (_, i) => i + 1);
      const pages: (number | "…")[] = [];
      const addPage = (p: number) => { if (!pages.includes(p)) pages.push(p); };
      addPage(1);
      if (current > 3) pages.push("…");
      for (let p = Math.max(2, current - 1); p <= Math.min(total - 1, current + 1); p++) addPage(p);
      if (current < total - 2) pages.push("…");
      addPage(total);
      return pages;
    }

    // ── Chevron icons ────────────────────────────────────────
    const ChevL = () => (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="15 18 9 12 15 6" /></svg>
    );
    const ChevR = () => (
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="9 18 15 12 9 6" /></svg>
    );

    // ── Modals ───────────────────────────────────────────────
    // COPIAR LITERALMENTE AdminApproveModal, AdminRejectModal, ConfirmDialog de EventsSection.tsx
    // (omitir AdminTransferModal — articles no soporta transferencia en este plan)
    // ... [el executor copia las 3 funciones intactas, solo cambia kind="artículo" donde aplique]

    // ── Main ─────────────────────────────────────────────────
    export default function ArticlesSection() {
      const { token } = useUser();
      const [page, setPage] = useState(1);
      const [perPage, setPerPage] = useState(12);
      const [total, setTotal] = useState(0);
      const [totalPages, setTotalPages] = useState(1);
      const [activeFilter, setActiveFilter] = useState("Todos");
      const [articles, setArticles] = useState<ApiArticle[]>([]);
      const [loading, setLoading] = useState(true);
      const [modal, setModal] = useState<ModalState>(null);
      const close = () => setModal(null);

      const fetchArticles = useCallback(async (p: number, ps: number, filter: string) => {
        if (!token) return;
        setLoading(true);
        try {
          const statusParam = STATUS_API[filter];
          const params = new URLSearchParams({
            page: String(p),
            pageSize: String(ps),
            ...(statusParam ? { status: statusParam } : {}),
          });
          const r = await fetch(`/api/articles?${params}`, {
            headers: { Authorization: `Bearer ${token}` },
          });
          if (!r.ok) throw new Error("Error al cargar artículos");
          const data = await r.json();
          setArticles(data.items ?? []);
          setTotal(data.total ?? 0);
          setTotalPages(data.totalPages ?? 1);
        } catch (ex) {
          toast.error(ex instanceof Error ? ex.message : "Error al cargar artículos");
        } finally {
          setLoading(false);
        }
      }, [token]);

      useEffect(() => { fetchArticles(page, perPage, activeFilter); }, [fetchArticles, page, perPage, activeFilter]);

      // Acciones (replicar patrón EventsSection: doApprove, doReject, doBan, doDelete)
      async function doApprove(item: ApiArticle) {
        try {
          const r = await fetch(`/api/articles/${item.id}/approve`, {
            method: "PATCH",
            headers: { Authorization: `Bearer ${token}` },
          });
          if (!r.ok) throw new Error("Error al aprobar");
          toast.success("Artículo aprobado y publicado");
          fetchArticles(page, perPage, activeFilter);
        } catch (ex) { toast.error(ex instanceof Error ? ex.message : "Error al aprobar"); }
      }

      async function doReject(item: ApiArticle, reason: string) {
        try {
          const r = await fetch(`/api/articles/${item.id}/reject`, {
            method: "PATCH",
            headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" },
            body: JSON.stringify({ reason }),
          });
          if (!r.ok) throw new Error("Error al rechazar");
          toast.success("Artículo rechazado");
          fetchArticles(page, perPage, activeFilter);
        } catch (ex) { toast.error(ex instanceof Error ? ex.message : "Error al rechazar"); }
      }

      async function doBan(item: ApiArticle, reason: string) {
        try {
          const r = await fetch(`/api/articles/${item.id}/ban`, {
            method: "PATCH",
            headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" },
            body: JSON.stringify({ reason }),
          });
          if (!r.ok) throw new Error("Error al banear");
          toast.success("Artículo baneado");
          fetchArticles(page, perPage, activeFilter);
        } catch (ex) { toast.error(ex instanceof Error ? ex.message : "Error al banear"); }
      }

      async function doDelete(item: ApiArticle) {
        try {
          const r = await fetch(`/api/articles/${item.id}`, {
            method: "DELETE",
            headers: { Authorization: `Bearer ${token}` },
          });
          if (!r.ok) throw new Error("Error al eliminar");
          toast.success("Artículo eliminado");
          fetchArticles(page, perPage, activeFilter);
        } catch (ex) { toast.error(ex instanceof Error ? ex.message : "Error al eliminar"); }
      }

      const from = total === 0 ? 0 : (page - 1) * perPage + 1;
      const to = Math.min(page * perPage, total);

      return (
        <>
          {/* Filtros + crear */}
          <div style={{ display: "flex", gap: 10, marginBottom: 14, flexWrap: "wrap", alignItems: "center" }}>
            {FILTER_STATUSES.map((s) => (
              <button key={s} className={`sel${activeFilter === s ? " on" : ""}`} onClick={() => { setActiveFilter(s); setPage(1); }}>
                {s}
              </button>
            ))}
            <Link href="/dashboard/articles/new" className="btn primary" style={{ marginLeft: "auto", padding: "9px 16px", fontSize: 13 }}>
              ＋ Crear artículo
            </Link>
          </div>

          {/* Tabla */}
          {loading ? (
            <div className="panel" style={{ padding: 32, textAlign: "center", color: "var(--ink-3)", fontSize: 13 }}>
              Cargando artículos…
            </div>
          ) : articles.length === 0 ? (
            <div className="panel" style={{ padding: 32, textAlign: "center", color: "var(--ink-3)", fontSize: 13 }}>
              No hay artículos{activeFilter !== "Todos" ? ` con estado "${activeFilter}"` : ""}.
            </div>
          ) : (
            <div className="panel" style={{ padding: 0 }}>
              <table className="a-table">
                <thead>
                  <tr>
                    <th>ARTÍCULO</th>
                    <th>AUTOR</th>
                    <th>CREADO</th>
                    <th>ESTADO</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody>
                  {articles.map((a) => {
                    const status = toDisplay(a.status);
                    const img = imageSrc(a.image);
                    return (
                      <tr key={a.id}>
                        <td>
                          <div className="cell-evt">
                            <div className="thumb-sm">
                              {img ? (
                                /* eslint-disable-next-line @next/next/no-img-element */
                                <img src={img} alt="" style={{ width: "100%", height: "100%", objectFit: "cover", borderRadius: 6 }} />
                              ) : (
                                <div className="pic" style={{ background: "linear-gradient(135deg, #a25cff, #5b39ff)" }} />
                              )}
                            </div>
                            <div>
                              <div className="ti">{a.title}</div>
                              <div className="su">{a.tags.slice(0, 3).map(t => t.name).join(" · ") || "Sin tags"}</div>
                            </div>
                          </div>
                        </td>
                        <td style={{ fontSize: 13 }}>
                          {a.userId === null ? <span style={{ color: "var(--ink-3)" }}>Editorial</span> : `Usuario #${a.userId}`}
                          <br />
                          <span style={{ color: "var(--ink-3)", fontFamily: "var(--font-mono)", fontSize: 11 }}>
                            {a.isSponsored ? "Patrocinado" : "Editorial"}
                          </span>
                        </td>
                        <td style={{ fontFamily: "var(--font-mono)", fontSize: 12 }}>{fmtDate(a.createdAt)}</td>
                        <td>
                          <span className={`stat-pill ${statusCls(status)}`}>
                            <span className="dot" />{status}
                          </span>
                        </td>
                        <td>
                          <div className="row-act">
                            {/* Action buttons por estado — replicar lógica de EventsSection */}
                            {status === "Borrador" && (
                              <>
                                <Link href={`/dashboard/articles/${a.slug}/edit`}><button>Editar</button></Link>
                                <button className="ok"  onClick={() => setModal({ type: "approve", item: a })}>Publicar</button>
                                <button className="bad" onClick={() => setModal({ type: "delete",  item: a })}>Eliminar</button>
                              </>
                            )}
                            {status === "En revisión" && (
                              <>
                                <Link href={`/dashboard/articles/${a.slug}/edit`}><button>Editar</button></Link>
                                <button className="ok"  onClick={() => setModal({ type: "approve", item: a })}>✓ Aprobar</button>
                                <button className="bad" onClick={() => setModal({ type: "reject",  item: a })}>✕ Rechazar</button>
                              </>
                            )}
                            {status === "Publicado" && (
                              <>
                                <Link href={`/dashboard/articles/${a.slug}/edit`}><button>Editar</button></Link>
                                <button className="bad" onClick={() => setModal({ type: "ban", item: a })}>Banear</button>
                              </>
                            )}
                            {status === "Rechazado" && (
                              <>
                                <Link href={`/dashboard/articles/${a.slug}/edit`}><button>Editar</button></Link>
                                <button onClick={() => setModal({ type: "approve", item: a })}>Re-revisar</button>
                                <button className="bad" onClick={() => setModal({ type: "delete", item: a })}>Eliminar</button>
                              </>
                            )}
                            {status === "Baneado" && (
                              <>
                                <Link href={`/dashboard/articles/${a.slug}/edit`}><button>Editar</button></Link>
                                <button className="ok" onClick={() => setModal({ type: "approve", item: a })}>Restaurar</button>
                              </>
                            )}
                          </div>
                        </td>
                      </tr>
                    );
                  })}
                </tbody>
              </table>
            </div>
          )}

          {/* Paginación — copiar pag-bar de EventsSection */}
          {!loading && total > 0 && (
            <div className="pag-bar">
              {/* ... idéntico a EventsSection, cambiar texto a "artículo(s)" */}
            </div>
          )}

          {/* Modales — copiar lógica EXACTA de EventsSection (sin transfer) */}
          {modal?.type === "approve" && (() => {
            const item = modal.item;
            return <AdminApproveModal kind="artículo" onClose={close} onApprove={() => { close(); doApprove(item); }} />;
          })()}
          {modal?.type === "reject" && (() => {
            const item = modal.item;
            return <AdminRejectModal kind="artículo" onClose={close} onReject={(reason) => { close(); doReject(item, reason); }} />;
          })()}
          {modal?.type === "ban" && (() => {
            const item = modal.item;
            return (
              <ConfirmDialog
                danger
                title="¿Banear artículo?"
                message="El artículo dejará de ser público inmediatamente. Puedes restaurarlo después."
                typeToConfirm="BANEAR"
                confirmLabel="Sí, banear"
                onConfirm={() => { close(); doBan(item, "Baneado por administrador"); }}
                onClose={close}
              />
            );
          })()}
          {modal?.type === "delete" && (() => {
            const item = modal.item;
            return (
              <ConfirmDialog
                danger
                title="¿Eliminar artículo?"
                message={`"${item.title}" se eliminará permanentemente.`}
                typeToConfirm="ELIMINAR"
                confirmLabel="Sí, eliminar"
                onConfirm={() => { close(); doDelete(item); }}
                onClose={close}
              />
            );
          })()}
        </>
      );
    }
    ```

    NOTAS:
    - Las 3 funciones modal (AdminApproveModal, AdminRejectModal, ConfirmDialog) deben copiarse LITERAL desde EventsSection.tsx (sin cambios, solo kind="artículo" en las llamadas).
    - NO incluir AdminTransferModal — articles no soporta transferencia en esta fase (puede agregarse en fase futura).
    - NO usar el viejo `AdminApproveModal` con regenerador de tags AI — sí incluirlo, es decoración existente; el callback solo pasa `tags` string que NO se envía al API (la API no tiene endpoint para tags en approve). Esto es coherente con EventsSection que también ignora el `tags` param.
    - Usar `a.slug` para construir las edit URLs (decisión advisor: zero API changes).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && grep -c "fetch(\`/api/articles\\?" app/dashboard/sections/ArticlesSection.tsx && grep -c "/dashboard/articles/new" app/dashboard/sections/ArticlesSection.tsx && grep -c "EVENTS = \\[" app/dashboard/sections/ArticlesSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetch(\`/api/articles\\?" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna >= 1
    - `grep -cE "/api/articles/.*\\/(approve|reject|ban)" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna >= 3 (las 3 acciones)
    - `grep -c "EVENTS = \\[" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna 0 (mock eliminado)
    - `grep -c "ArticlesSection" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna >= 1 (componente sigue exportado)
    - `grep -c "/dashboard/articles/new" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna 1
    - `grep -cE "/dashboard/articles/.*\\/edit" apps/website/app/dashboard/sections/ArticlesSection.tsx` retorna >= 5 (1 link en cada estado)
    - `pnpm tsc --noEmit` pasa
  </acceptance_criteria>
  <done>
    ArticlesSection reescrita, sin mock, conectada al API real, acciones funcionando, links a crear/editar.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear rutas /dashboard/articles/new + /[slug]/edit + actualizar DashboardShell breadcrumb + eliminar AdminArticleEditor roto</name>
  <files>apps/website/app/dashboard/articles/new/page.tsx, apps/website/app/dashboard/articles/[slug]/edit/page.tsx, apps/website/app/dashboard/DashboardShell.tsx, apps/website/app/dashboard/modals/AdminArticleEditor.tsx</files>
  <read_first>
    - apps/website/app/dashboard/events/new/page.tsx (patrón de la ruta /new — replicar)
    - apps/website/app/dashboard/events/[id]/edit/page.tsx (patrón de edit — adaptar a slug)
    - apps/website/app/dashboard/DashboardShell.tsx (líneas 110-130 — SUB_PAGES + editMatch)
    - apps/website/app/dashboard/modals/AdminArticleEditor.tsx (modal roto — confirmar que llama a /api/articles/edit que no existe)
    - apps/website/app/dashboard/articles/ArticleForm.tsx (creado por 17-02 — interface InitialArticle)
  </read_first>
  <behavior>
    - GET /dashboard/articles/new → renderiza `<ArticleForm mode="create" variant="admin" />`
    - GET /dashboard/articles/{slug}/edit → fetch GET /api/articles/{slug} con token, mapea a InitialArticle, renderiza `<ArticleForm mode="edit" variant="admin" initial={...} />`
    - DashboardShell muestra crumb "ARTÍCULOS / NUEVO" en /dashboard/articles/new
    - DashboardShell muestra crumb "ARTÍCULOS / EDITAR" en /dashboard/articles/{slug}/edit
    - AdminArticleEditor.tsx queda eliminado (no se importa en ningún lado verificable con grep, ver Task)
  </behavior>
  <action>
    PASO 1 — Crear `apps/website/app/dashboard/articles/new/page.tsx`:

    ```typescript
    import { ArticleForm } from "../ArticleForm";

    export default function NewArticlePage() {
      return <ArticleForm mode="create" variant="admin" />;
    }
    ```

    PASO 2 — Crear `apps/website/app/dashboard/articles/[slug]/edit/page.tsx`:

    ```typescript
    "use client";
    import { useEffect, useState } from "react";
    import { useParams } from "next/navigation";
    import { useUser } from "@/components/providers";
    import { ArticleForm, type InitialArticle } from "../../ArticleForm";

    export default function EditArticlePage() {
      const { slug } = useParams<{ slug: string }>();
      const { token } = useUser();
      const [initial, setInitial] = useState<InitialArticle | null>(null);
      const [error, setError] = useState<string | null>(null);

      useEffect(() => {
        if (!token || !slug) return;
        fetch(`/api/articles/${slug}`, {
          headers: { Authorization: `Bearer ${token}` },
        })
          .then((r) => {
            if (!r.ok) throw new Error("Artículo no encontrado");
            return r.json();
          })
          .then((data) => {
            // Map API response to InitialArticle shape
            setInitial({
              id: data.id,
              title: data.title,
              slug: data.slug,
              excerpt: data.excerpt,
              content: data.content,
              image: data.image,
              status: data.status,
              tags: data.tags ?? [],
              events: data.events ?? [],
            });
          })
          .catch((ex) => setError(ex instanceof Error ? ex.message : "Error al cargar"));
      }, [token, slug]);

      if (error) return <div style={{ padding: 32, color: "var(--err)" }}>{error}</div>;
      if (!initial) return <div style={{ padding: 32 }}>Cargando…</div>;

      return <ArticleForm mode="edit" variant="admin" initial={initial} />;
    }
    ```

    PASO 3 — Actualizar `apps/website/app/dashboard/DashboardShell.tsx`:

    Localizar el bloque SUB_PAGES (~ línea 110) y agregar entrada:

    ```typescript
    const SUB_PAGES: Record<string, { label: string; crumb: string }> = {
      "/dashboard/settings/terms":   { label: "Términos y condiciones", crumb: "CONFIGURACIÓN / TÉRMINOS" },
      "/dashboard/settings/privacy": { label: "Política de privacidad", crumb: "CONFIGURACIÓN / PRIVACIDAD" },
      "/dashboard/settings/cookies": { label: "Política de cookies",    crumb: "CONFIGURACIÓN / COOKIES" },
      "/dashboard/events/new":       { label: "Crear evento",           crumb: "EVENTOS / NUEVO" },
      "/dashboard/articles/new":     { label: "Crear artículo",         crumb: "ARTÍCULOS / NUEVO" },  // ← NUEVO
    };
    ```

    Localizar el bloque editMatch (~ línea 122) y agregar regex para articles edit:

    ```typescript
    const editMatch = pathname.match(/^\/dashboard\/events\/(\d+)\/edit$/);
    if (editMatch) {
      SUB_PAGES[pathname] = { label: "Editar evento", crumb: "EVENTOS / EDITAR" };
    }

    // ← NUEVO: articles usa slug (string), no id (number)
    const articleEditMatch = pathname.match(/^\/dashboard\/articles\/([^/]+)\/edit$/);
    if (articleEditMatch) {
      SUB_PAGES[pathname] = { label: "Editar artículo", crumb: "ARTÍCULOS / EDITAR" };
    }
    ```

    PASO 4 — Eliminar `apps/website/app/dashboard/modals/AdminArticleEditor.tsx`:

    - Verificar primero con `grep -r "AdminArticleEditor" apps/website` que NO se importa en ningún lugar (advisor verificó que es huérfano).
    - Si efectivamente no se usa: `rm apps/website/app/dashboard/modals/AdminArticleEditor.tsx`.
    - Si se usa en algún lugar inesperado: REEMPLAZAR el import por un comment "// AdminArticleEditor deprecated — use /dashboard/articles/new" y dejar el archivo para que el grep no rompa, pero noticar en el SUMMARY.

    PASO 5 — Verificación cruzada: si el directorio `apps/website/app/dashboard/modals/` queda vacío tras el rm, eliminarlo también.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && ls app/dashboard/articles/new/page.tsx app/dashboard/articles/\[slug\]/edit/page.tsx && grep -c "/dashboard/articles/new" app/dashboard/DashboardShell.tsx && grep -cE "articleEditMatch|articles.*edit" app/dashboard/DashboardShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `apps/website/app/dashboard/articles/new/page.tsx` existe
    - Archivo `apps/website/app/dashboard/articles/[slug]/edit/page.tsx` existe
    - `grep -c "/dashboard/articles/new" apps/website/app/dashboard/DashboardShell.tsx` retorna 1 (entrada en SUB_PAGES)
    - `grep -cE "articleEditMatch|/dashboard/articles/.*edit" apps/website/app/dashboard/DashboardShell.tsx` >= 1 (regex match agregado)
    - `grep -rl "AdminArticleEditor" apps/website/app/ 2>/dev/null | wc -l` retorna 0 (componente roto eliminado, sin imports residuales)
    - `pnpm tsc --noEmit` en apps/website pasa sin errores
    - Smoke browser: navegar a /dashboard/articles → ver lista real (no mock); click "+ Crear" → form sin accordion; click "Editar" en una fila → form con datos pre-cargados
  </acceptance_criteria>
  <done>
    Rutas /new y /[slug]/edit funcionan, breadcrumbs muestran texto correcto, modal roto eliminado.
  </done>
</task>

</tasks>

<verification>
- ArticlesSection carga datos reales (sin mock EVENTS)
- Acciones approve/reject/ban/delete llaman a la API correcta y recargan
- Rutas /dashboard/articles/new y /[slug]/edit montan ArticleForm correctamente
- DashboardShell muestra crumbs específicos para articles
- AdminArticleEditor.tsx eliminado, no quedan imports residuales
- Type-check del workspace verde
</verification>

<success_criteria>
1. ArticlesSection lista artículos reales con paginación y filtros funcionales
2. Botones de moderación (Aprobar/Rechazar/Banear/Eliminar) ejecutan llamadas reales y refrescan lista
3. /dashboard/articles/new y /dashboard/articles/[slug]/edit renderizan ArticleForm correctamente
4. Breadcrumb del DashboardShell refleja la ruta actual de artículos
5. Modal huérfano AdminArticleEditor eliminado del codebase
</success_criteria>

<output>
After completion, create `.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-03-SUMMARY.md`
</output>
