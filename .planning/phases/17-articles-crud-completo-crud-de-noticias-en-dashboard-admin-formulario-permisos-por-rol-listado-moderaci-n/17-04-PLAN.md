---
phase: 17-articles-crud
plan: 04
type: execute
wave: 3
depends_on: [17-01, 17-02]
files_modified:
  - apps/website/app/(site)/cuenta/articulos/page.tsx
  - apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx
  - apps/website/app/(site)/crear-articulo/page.tsx
  - apps/website/app/(site)/upsell/UpsellView.tsx
autonomous: true
requirements:
  - ART-11  # Organizador lista sus artículos desde /cuenta/articulos via GET /articles/mine
  - ART-12  # Organizador crea artículo patrocinado via /crear-articulo (POST /articles/sponsored)
  - ART-13  # Organizador edita / elimina sus propios artículos (gracias a guards relajados de 17-01)
  - ART-14  # Bug-fix: /cuenta/articulos enlazaba a /crear/articulo (404) — ahora a /crear-articulo
  - ART-15  # Upsell post-evento usa el flujo nuevo: reemplaza form inline roto por link a /crear-articulo (decisión #3)
user_setup: []

must_haves:
  truths:
    - "Organizador autenticado en /cuenta/articulos ve la lista de SUS artículos vía GET /api/articles/mine"
    - "Cada fila muestra: título, estado pill, fecha creada, statusReason (si rechazado), botones Ver/Editar/Eliminar"
    - "Botón '+ Solicitar artículo' navega a /crear-articulo (NO al broken /crear/articulo)"
    - "/crear-articulo renderiza ArticleForm mode=create variant=sponsored"
    - "/cuenta/articulos/{slug}/edit renderiza ArticleForm mode=edit variant=sponsored con datos pre-cargados"
    - "Eliminar artículo llama DELETE /api/articles/:id (autorizado por 17-01 ownership check) y filtra de la lista"
    - "La page sigue requiriendo autenticación — redirige a /login si no hay user (patrón existente)"
    - "AccountShell sigue presente — visual idéntico a otras subrutas de /cuenta"
    - "UpsellView del flujo /crear ya NO contiene el form inline roto de artículo — el botón \"Sí, agregar artículo\" lleva a /crear-articulo (decisión #3 conectada)"
  artifacts:
    - path: "apps/website/app/(site)/cuenta/articulos/page.tsx"
      provides: "Listado de artículos propios del organizador (REWRITE — hoy llama a endpoint inexistente ?mine=true)"
      contains: "/api/articles/mine"
    - path: "apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx"
      provides: "Página de edición de artículo propio del organizador (NUEVO)"
      contains: "variant=\"sponsored\""
    - path: "apps/website/app/(site)/crear-articulo/page.tsx"
      provides: "Página de creación de artículo patrocinado (NUEVO — corrige link roto /crear/articulo)"
      contains: "variant=\"sponsored\""
    - path: "apps/website/app/(site)/upsell/UpsellView.tsx"
      provides: "Upsell post-evento — opción de artículo ahora redirige a /crear-articulo en vez de form inline roto"
      contains: "/crear-articulo"
  key_links:
    - from: "/cuenta/articulos/page.tsx"
      to: "/api/articles/mine"
      via: "fetch con Authorization Bearer"
      pattern: "fetch.*\\/api\\/articles\\/mine"
    - from: "/cuenta/articulos/page.tsx (botón crear)"
      to: "/crear-articulo"
      via: "Link href"
      pattern: "/crear-articulo"
    - from: "/crear-articulo/page.tsx"
      to: "ArticleForm"
      via: "import + render con variant=sponsored mode=create"
      pattern: "variant=\"sponsored\""
    - from: "/cuenta/articulos/[slug]/edit/page.tsx"
      to: "/api/articles/:slug"
      via: "fetch para cargar artículo a editar"
      pattern: "fetch.*\\/api\\/articles\\/"
    - from: "UpsellView.tsx (botón artículo)"
      to: "/crear-articulo"
      via: "router.push o Link href"
      pattern: "/crear-articulo"
---

<objective>
Habilitar el CRUD del organizador en `/cuenta/articulos`. Decisión de usuario #2: "Organizador ve y gestiona SUS PROPIOS artículos patrocinados desde /cuenta — crear, editar, eliminar los suyos." Decisión #3: el upsell de /crear ya tiene la opción de artículo, pero falta la página `/crear-articulo` (hoy el link en /cuenta/articulos apunta a /crear/articulo que NO existe — bug pre-existente identificado por advisor).

Purpose: Cierra el loop del organizador. Sin este plan, el organizador solo puede crear artículos como upsell post-evento (via /crear/upsell), pero no puede ver, editar, ni eliminar los suyos.
Output: 1 page reescrita + 2 pages nuevas. Reusa `<ArticleForm variant="sponsored" />` del Plan 17-02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-RESEARCH.md

# Page actual a reescribir (hoy usa ?mine=true que NO existe + enlace a /crear/articulo roto)
@apps/website/app/(site)/cuenta/articulos/page.tsx

# Shell que envuelve las pages de cuenta — preservar
@apps/website/app/(site)/cuenta/AccountShell.tsx

# Patrón de otras subrutas /cuenta — para consistencia
@apps/website/app/(site)/cuenta/publicaciones

# Componente importado (creado por Plan 17-02)
# apps/website/app/dashboard/articles/ArticleForm.tsx

<interfaces>
<!-- Contratos clave. NO buscar en codebase, usar directos. -->

ArticleForm component (creado por Plan 17-02):
```typescript
import { ArticleForm, type InitialArticle } from "@/app/dashboard/articles/ArticleForm";

interface Props {
  mode: "create" | "edit";
  variant: "admin" | "sponsored";
  initial?: InitialArticle;
}

export type InitialArticle = {
  id: number;
  title: string;
  slug: string;
  excerpt: string | null;
  content: string;
  image: string | null;
  status: string;
  tags: { id: number; name: string; slug: string }[];
  events?: { id: number; title: string }[];
};
```

GET /articles/mine response (creado por Plan 17-01) — array de ArticleApi:
```typescript
type ArticleApi = {
  id: number;
  title: string;
  slug: string;
  excerpt: string | null;
  image: string | null;
  status: "DRAFT" | "PENDING_PAYMENT" | "PENDING_MODERATION" | "APPROVED" | "REJECTED" | "BANNED";
  statusReason: string | null;
  userId: number;  // siempre presente en /mine
  createdAt: string;
  tags: { id: number; name: string; slug: string }[];
  _count: { likes: number };
  isSponsored: boolean;  // siempre true en /mine
};
```

useUser() hook:
```typescript
const { user, token, ready } = useUser();
```

AccountShell (apps/website/app/(site)/cuenta/AccountShell.tsx):
```typescript
import { AccountShell } from "../AccountShell";
// Renderiza sidebar + body. Body recibe children.
// El tab "Artículos" ya existe en el menú (href="/cuenta/articulos").
```

Decisión del usuario lockeada:
- D-#3: Upsell de /crear ya tiene opción de artículo patrocinado. La ruta /crear-articulo (NUEVA) cubre el flujo "Solicitar artículo nuevo" desde /cuenta. Es deliberadamente distinta del upsell — el upsell agrega al carrito tras crear evento, /crear-articulo va al ArticleForm completo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reescribir /cuenta/articulos con datos reales + link correcto + eliminar funcional</name>
  <files>apps/website/app/(site)/cuenta/articulos/page.tsx</files>
  <read_first>
    - apps/website/app/(site)/cuenta/articulos/page.tsx (estado actual — usa ?mine=true broken, link a /crear/articulo roto)
    - apps/website/app/(site)/cuenta/AccountShell.tsx (shell — preservar)
    - apps/website/app/(site)/cuenta/publicaciones/page.tsx (si existe, patrón de listado de eventos propios — para consistencia visual)
  </read_first>
  <behavior>
    - Si no hay user (después de ready) → redirect a /login?returnTo=/cuenta/articulos (patrón existente, preservar)
    - Si hay user + token → fetch GET /api/articles/mine
    - Render dentro de AccountShell: header h1 "Mis artículos" + Link a /crear-articulo + lista
    - Cada fila (acc-list-row): título + meta (createdAt formateada + statusReason si rechazado) + pill estado + botones (Ver si APPROVED, Editar, Eliminar)
    - Click "Ver" → Link href=/noticias/{slug} (solo si APPROVED)
    - Click "Editar" → Link href=/cuenta/articulos/{slug}/edit
    - Click "Eliminar" → confirm() → DELETE /api/articles/:id → setArticles((p) => filter)
    - Empty state coherente con el actual
  </behavior>
  <action>
    Reemplazar TODO `apps/website/app/(site)/cuenta/articulos/page.tsx`:

    ```typescript
    "use client";

    import { useEffect, useState } from "react";
    import Link from "next/link";
    import { useRouter } from "next/navigation";
    import { toast } from "sonner";
    import { AccountShell } from "../AccountShell";
    import { useUser } from "@/components/providers";

    type ApiStatus = "DRAFT" | "PENDING_PAYMENT" | "PENDING_MODERATION" | "APPROVED" | "REJECTED" | "BANNED";

    interface MyArticle {
      id: number;
      title: string;
      slug: string;
      status: ApiStatus;
      statusReason: string | null;
      createdAt: string;
      tags: { id: number; name: string; slug: string }[];
    }

    const STATUS_META: Record<ApiStatus, { label: string; color: string }> = {
      DRAFT:               { label: "Borrador",       color: "var(--ink-3)" },
      PENDING_PAYMENT:     { label: "Pago pendiente", color: "var(--warn)" },
      PENDING_MODERATION:  { label: "En revisión",    color: "var(--warn)" },
      APPROVED:            { label: "Publicado",      color: "var(--ok)" },
      REJECTED:            { label: "Rechazado",      color: "var(--err)" },
      BANNED:              { label: "Baneado",        color: "var(--err)" },
    };

    export default function ArticulosPage() {
      const { user, token, ready } = useUser();
      const router = useRouter();
      const [articles, setArticles] = useState<MyArticle[]>([]);
      const [loading, setLoading] = useState(true);

      useEffect(() => {
        if (ready && !user) {
          router.replace("/login?returnTo=/cuenta/articulos");
          return;
        }
        if (!token) return;
        fetch("/api/articles/mine", {
          headers: { Authorization: `Bearer ${token}` },
        })
          .then((r) => {
            if (!r.ok) throw new Error("Error al cargar tus artículos");
            return r.json();
          })
          .then((data) => setArticles(Array.isArray(data) ? data : []))
          .catch((ex) => {
            toast.error(ex instanceof Error ? ex.message : "Error al cargar");
            setArticles([]);
          })
          .finally(() => setLoading(false));
      }, [ready, user, token, router]);

      if (!ready || !user) {
        return (
          <main style={{ minHeight: "60vh", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--ink-3)" }}>
            {ready ? "Redirigiendo al inicio de sesión…" : "Verificando acceso…"}
          </main>
        );
      }

      const handleDelete = async (id: number) => {
        if (!token) return;
        if (!confirm("¿Eliminar este artículo? Esta acción no se puede deshacer.")) return;
        try {
          const r = await fetch(`/api/articles/${id}`, {
            method: "DELETE",
            headers: { Authorization: `Bearer ${token}` },
          });
          if (!r.ok) throw new Error("No se pudo eliminar");
          setArticles((prev) => prev.filter((a) => a.id !== id));
          toast.success("Artículo eliminado");
        } catch (ex) {
          toast.error(ex instanceof Error ? ex.message : "No se pudo eliminar");
        }
      };

      return (
        <AccountShell>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", marginBottom: 4 }}>
            <h1>Mis artículos</h1>
            <Link className="btn primary" href="/crear-articulo">＋ Solicitar artículo</Link>
          </div>
          <p className="lead">Artículos patrocinados que pediste a Konbini. Pasan por revisión editorial antes de publicarse.</p>

          {loading ? (
            <div className="acc-section" style={{ color: "var(--ink-3)", fontSize: 14 }}>Cargando artículos…</div>
          ) : articles.length === 0 ? (
            <div className="acc-section" style={{ textAlign: "center", padding: "24px 0" }}>
              <div style={{ color: "var(--ink-3)", fontSize: 14 }}>No tienes artículos aún.</div>
              <Link className="btn primary" href="/crear-articulo" style={{ marginTop: 14, display: "inline-block" }}>
                Crear tu primer artículo
              </Link>
            </div>
          ) : (
            <div className="acc-section">
              {articles.map((a) => {
                const sm = STATUS_META[a.status] ?? { label: a.status, color: "var(--ink-3)" };
                return (
                  <div key={a.id} className="acc-list-row">
                    <div className="main">
                      <div className="t">{a.title}</div>
                      <div className="m">
                        {new Date(a.createdAt).toLocaleDateString("es-CL", { day: "numeric", month: "short", year: "numeric" })}
                        {a.tags.length > 0 && ` · ${a.tags.slice(0, 3).map(t => t.name).join(", ")}`}
                        {a.statusReason && (
                          <>
                            <br />
                            <span style={{ color: "var(--err)", fontSize: 12 }}>Motivo: {a.statusReason}</span>
                          </>
                        )}
                      </div>
                    </div>
                    <span className="pill" style={{
                      color: sm.color,
                      borderColor: `color-mix(in oklab, ${sm.color} 30%, transparent)`,
                      background: `color-mix(in oklab, ${sm.color} 10%, transparent)`,
                    }}>
                      {sm.label}
                    </span>
                    <div style={{ display: "flex", gap: 8 }}>
                      {a.status === "APPROVED" && (
                        <Link className="btn ghost" href={`/noticias/${a.slug}`} style={{ fontSize: 12, padding: "6px 12px" }}>
                          Ver
                        </Link>
                      )}
                      <Link className="btn ghost" href={`/cuenta/articulos/${a.slug}/edit`} style={{ fontSize: 12, padding: "6px 12px" }}>
                        Editar
                      </Link>
                      <button
                        className="btn ghost"
                        style={{ fontSize: 12, padding: "6px 12px", color: "var(--err)" }}
                        onClick={() => handleDelete(a.id)}
                      >
                        Eliminar
                      </button>
                    </div>
                  </div>
                );
              })}
            </div>
          )}
        </AccountShell>
      );
    }
    ```

    NOTAS:
    - El link de "+ Solicitar artículo" cambia de `/crear/articulo` (404) a `/crear-articulo` (creado en Task 3).
    - Endpoint cambia de `?mine=true` (no soportado) a `/articles/mine` (creado en Plan 17-01).
    - El botón Editar usa slug (consistente con admin dashboard, decisión advisor).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && grep -c "/api/articles/mine" "app/(site)/cuenta/articulos/page.tsx" && grep -c "/crear/articulo" "app/(site)/cuenta/articulos/page.tsx" && grep -c "/crear-articulo" "app/(site)/cuenta/articulos/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/articles/mine" apps/website/app/(site)/cuenta/articulos/page.tsx` retorna >= 1
    - `grep -c "mine=true" apps/website/app/(site)/cuenta/articulos/page.tsx` retorna 0 (bug eliminado)
    - `grep -c "/crear/articulo" apps/website/app/(site)/cuenta/articulos/page.tsx` retorna 0 (link roto eliminado)
    - `grep -c "/crear-articulo" apps/website/app/(site)/cuenta/articulos/page.tsx` >= 1 (nuevo link)
    - `grep -c "/cuenta/articulos/.*/edit" apps/website/app/(site)/cuenta/articulos/page.tsx` >= 1 (link a edit)
    - `pnpm tsc --noEmit` pasa
  </acceptance_criteria>
  <done>
    /cuenta/articulos carga datos reales, link de creación apunta a ruta existente, eliminar funcional.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Crear /cuenta/articulos/[slug]/edit page</name>
  <files>apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx</files>
  <read_first>
    - apps/website/app/(site)/cuenta/AccountShell.tsx
    - apps/website/app/dashboard/articles/ArticleForm.tsx (interface InitialArticle creada por 17-02)
    - apps/website/app/(site)/cuenta/articulos/page.tsx (acabado de reescribir — confirmar shape de articles para mapping)
  </read_first>
  <behavior>
    - Ruta /cuenta/articulos/{slug}/edit
    - Si no hay user → redirect a /login?returnTo=
    - Fetch GET /api/articles/{slug} con token
    - Si 404 → mensaje de error
    - Si OK → mapear response a InitialArticle, renderizar `<ArticleForm mode="edit" variant="sponsored" initial={...} />` dentro de AccountShell
    - Si el usuario NO es owner del artículo → backend devolverá 403 al hacer PATCH (gracias a 17-01) y el toast del form mostrará el error
  </behavior>
  <action>
    Crear `apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx`:

    ```typescript
    "use client";

    import { useEffect, useState } from "react";
    import { useParams, useRouter } from "next/navigation";
    import { AccountShell } from "../../AccountShell";
    import { useUser } from "@/components/providers";
    import { ArticleForm, type InitialArticle } from "@/app/dashboard/articles/ArticleForm";

    export default function EditMyArticlePage() {
      const { slug } = useParams<{ slug: string }>();
      const { user, token, ready } = useUser();
      const router = useRouter();
      const [initial, setInitial] = useState<InitialArticle | null>(null);
      const [error, setError] = useState<string | null>(null);

      useEffect(() => {
        if (ready && !user) {
          router.replace(`/login?returnTo=/cuenta/articulos/${slug}/edit`);
          return;
        }
        if (!token || !slug) return;
        fetch(`/api/articles/${slug}`, {
          headers: { Authorization: `Bearer ${token}` },
        })
          .then((r) => {
            if (!r.ok) throw new Error("Artículo no encontrado");
            return r.json();
          })
          .then((data) => {
            setInitial({
              id: data.id,
              title: data.title,
              slug: data.slug,
              excerpt: data.excerpt,
              content: data.content,
              image: data.image,
              status: data.status,
              tags: data.tags ?? [],
              events: data.events ?? [],
            });
          })
          .catch((ex) => setError(ex instanceof Error ? ex.message : "Error al cargar"));
      }, [ready, user, token, slug, router]);

      if (!ready || !user) {
        return (
          <main style={{ minHeight: "60vh", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--ink-3)" }}>
            {ready ? "Redirigiendo al inicio de sesión…" : "Verificando acceso…"}
          </main>
        );
      }

      return (
        <AccountShell>
          {error ? (
            <div style={{ padding: 32, color: "var(--err)" }}>{error}</div>
          ) : !initial ? (
            <div style={{ padding: 32, color: "var(--ink-3)" }}>Cargando artículo…</div>
          ) : (
            <ArticleForm mode="edit" variant="sponsored" initial={initial} />
          )}
        </AccountShell>
      );
    }
    ```

    NOTAS:
    - Import del ArticleForm con path absoluto `@/app/dashboard/articles/ArticleForm` (Next.js soporta esto si tsconfig tiene paths configurado — verificar; si no, ajustar a `../../../../dashboard/articles/ArticleForm`).
    - El form en variant=sponsored redirigirá a /cuenta/articulos tras submit (lógica interna del componente).
    - El ArticleForm ya maneja toast de éxito y errores (incluido 403 si no es owner).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && ls "app/(site)/cuenta/articulos/[slug]/edit/page.tsx" && grep -c "variant=\"sponsored\"" "app/(site)/cuenta/articulos/[slug]/edit/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx` existe
    - `grep -c "variant=\"sponsored\"" apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx` retorna 1
    - `grep -c "mode=\"edit\"" apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx` retorna 1
    - `grep -c "AccountShell" apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx` >= 2 (import + uso)
    - `grep -c "ArticleForm" apps/website/app/(site)/cuenta/articulos/[slug]/edit/page.tsx` >= 2 (import + uso)
    - `pnpm tsc --noEmit` pasa
  </acceptance_criteria>
  <done>
    Página de edición del organizador funciona — carga artículo por slug, renderiza ArticleForm sponsored.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear /crear-articulo page (fix link roto + flujo creación organizador)</name>
  <files>apps/website/app/(site)/crear-articulo/page.tsx</files>
  <read_first>
    - apps/website/app/(site)/cuenta/AccountShell.tsx
    - apps/website/app/dashboard/articles/ArticleForm.tsx (interface Props creada por 17-02)
    - apps/website/app/crear/upsell/page.tsx (para entender el patrón de header de las páginas /crear)
  </read_first>
  <behavior>
    - Ruta /crear-articulo (top-level, NO bajo /cuenta para mantener simplicidad de routing y consistencia con /crear)
    - Si no hay user → redirect a /login?returnTo=/crear-articulo
    - Renderiza ArticleForm con mode="create" variant="sponsored" — el form maneja:
      - submit POST /api/articles/sponsored (siempre crea con status DRAFT)
      - redirect a /cuenta/articulos tras éxito
    - Page wrapper: usar AccountShell para consistencia visual con resto de /cuenta (decisión de scope — evita crear nuevo shell)
  </behavior>
  <action>
    Crear `apps/website/app/(site)/crear-articulo/page.tsx`:

    ```typescript
    "use client";

    import { useEffect } from "react";
    import { useRouter } from "next/navigation";
    import { AccountShell } from "../cuenta/AccountShell";
    import { useUser } from "@/components/providers";
    import { ArticleForm } from "@/app/dashboard/articles/ArticleForm";

    export default function CrearArticuloPage() {
      const { user, ready } = useUser();
      const router = useRouter();

      useEffect(() => {
        if (ready && !user) {
          router.replace("/login?returnTo=/crear-articulo");
        }
      }, [ready, user, router]);

      if (!ready || !user) {
        return (
          <main style={{ minHeight: "60vh", display: "flex", alignItems: "center", justifyContent: "center", color: "var(--ink-3)" }}>
            {ready ? "Redirigiendo al inicio de sesión…" : "Verificando acceso…"}
          </main>
        );
      }

      return (
        <AccountShell>
          <ArticleForm mode="create" variant="sponsored" />
        </AccountShell>
      );
    }
    ```

    NOTAS:
    - La ruta vive bajo `(site)/` para que pase por el layout principal del sitio (header, footer) en vez del shell admin.
    - Se reusa AccountShell para que el organizador vea el sidebar de su cuenta y entienda el contexto.
    - NO crear directorio adicional `/cuenta/crear-articulo` — el link de cuenta/articulos apunta a `/crear-articulo` (decisión locked).
    - El form en variant=sponsored redirigirá a /cuenta/articulos tras submit exitoso.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && ls "app/(site)/crear-articulo/page.tsx" && grep -c "variant=\"sponsored\"" "app/(site)/crear-articulo/page.tsx" && grep -c "mode=\"create\"" "app/(site)/crear-articulo/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Archivo `apps/website/app/(site)/crear-articulo/page.tsx` existe
    - `grep -c "variant=\"sponsored\"" apps/website/app/(site)/crear-articulo/page.tsx` retorna 1
    - `grep -c "mode=\"create\"" apps/website/app/(site)/crear-articulo/page.tsx` retorna 1
    - `grep -c "ArticleForm" apps/website/app/(site)/crear-articulo/page.tsx` >= 2 (import + uso)
    - Navegar a /crear-articulo en browser → renderiza ArticleForm (sin error 404)
    - `pnpm tsc --noEmit` pasa
  </acceptance_criteria>
  <done>
    /crear-articulo existe y monta ArticleForm sponsored. El link broken pre-existente queda corregido.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Reemplazar ArticleForm inline roto de UpsellView por redirect a /crear-articulo</name>
  <files>apps/website/app/(site)/upsell/UpsellView.tsx</files>
  <read_first>
    - apps/website/app/(site)/upsell/UpsellView.tsx (estado actual — el componente interno ArticleForm en líneas 199-306 con payload roto)
    - apps/website/app/(site)/crear-articulo/page.tsx (creado en Task 3 — destino del redirect)
  </read_first>
  <behavior>
    - El bloque "PASO 3 / 3 · OPCIONAL — ¿Quieres un artículo patrocinado?" de UpsellView sigue presente con el copy editorial intacto.
    - Botón "Sí, agregar artículo" YA NO abre form inline — en su lugar, navega a /crear-articulo (vía router.push o Link).
    - Botón "No, gracias — ir al carrito" se mantiene (goCart).
    - El componente interno `ArticleForm` (función definida líneas 199-306) queda ELIMINADO del archivo (es código muerto + tiene payload roto: `videoUrl` no existe en CreateSponsoredArticleDto).
    - El state `open.article` ya no es necesario — el botón redirige directamente, sin toggle.
    - SpotForm y HeroForm (los otros dos forms inline) NO se tocan — fuera de scope.
  </behavior>
  <action>
    Editar `apps/website/app/(site)/upsell/UpsellView.tsx`:

    PASO 1 — ELIMINAR completamente la función `ArticleForm` interna (líneas ~199-306, todo el bloque `/* ─── ArticleForm ─── */`).

    PASO 2 — En el bloque PASO 3 del componente principal `UpsellView` (líneas ~381-397), reemplazar el switch open.article / inline form por un par de botones que redirijan o vayan al carrito:

    ANTES (líneas ~385-396):
    ```typescript
    {step === 3 && (
      <div className="body">
        {!open.article ? (
          <div className="ups-cta-row">
            <button className="btn primary" onClick={() => setOpen({ ...open, article: true })}>Sí, agregar artículo</button>
            <button className="btn ghost" onClick={goCart}>No, gracias — ir al carrito</button>
          </div>
        ) : (
          <ArticleForm onAdd={goCart} onCancel={() => setOpen({ ...open, article: false })} />
        )}
      </div>
    )}
    ```

    DESPUÉS:
    ```typescript
    {step === 3 && (
      <div className="body">
        <div className="ups-cta-row">
          <button className="btn primary" onClick={() => router.push("/crear-articulo")}>Sí, agregar artículo</button>
          <button className="btn ghost" onClick={goCart}>No, gracias — ir al carrito</button>
        </div>
      </div>
    )}
    ```

    PASO 3 — En la interfaz `useState<{ spot?: boolean; hero?: boolean; article?: boolean }>` (línea ~312), eliminar el campo `article?: boolean` (ya no se usa):
    ```typescript
    const [open, setOpen] = useState<{ spot?: boolean; hero?: boolean }>({});
    ```

    PASO 4 — Verificar imports: `router` ya está importado (línea ~3: `import { useRouter } from "next/navigation"`); `Link` está importado pero ya no es necesario solo para esta task — dejarlo si SpotForm/HeroForm lo usan, no tocar.

    PASO 5 — Verificar que tras la edición:
    - El archivo NO contiene "function ArticleForm" (eliminado completamente)
    - El archivo NO contiene "videoUrl" (era campo del payload roto)
    - El archivo NO contiene "isSponsored: true" (era flag del payload roto — no existe en DTO)
    - El archivo SÍ contiene `router.push("/crear-articulo")` exactamente una vez
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project/apps/website && pnpm tsc --noEmit && grep -c "function ArticleForm" "app/(site)/upsell/UpsellView.tsx" && grep -c "router.push(\"/crear-articulo\")" "app/(site)/upsell/UpsellView.tsx" && grep -c "videoUrl\|isSponsored: true" "app/(site)/upsell/UpsellView.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function ArticleForm" apps/website/app/(site)/upsell/UpsellView.tsx` retorna 0 (función eliminada)
    - `grep -c "videoUrl" apps/website/app/(site)/upsell/UpsellView.tsx` retorna 0 (payload roto eliminado)
    - `grep -c "isSponsored: true" apps/website/app/(site)/upsell/UpsellView.tsx` retorna 0 (flag roto eliminado)
    - `grep -c "/crear-articulo" apps/website/app/(site)/upsell/UpsellView.tsx` retorna >= 1 (redirect agregado)
    - `grep -c "function SpotForm\|function HeroForm" apps/website/app/(site)/upsell/UpsellView.tsx` retorna 2 (los otros forms NO se tocan)
    - `pnpm tsc --noEmit` pasa
    - Smoke: navegar a /crear/upsell → llegar a Paso 3 → click "Sí, agregar artículo" → URL cambia a /crear-articulo (no form inline)
  </acceptance_criteria>
  <done>
    UpsellView conecta el flujo de artículo con /crear-articulo (decisión #3). Form inline roto eliminado.
  </done>
</task>

</tasks>

<verification>
- /cuenta/articulos carga artículos del organizador (no más fetch broken)
- /cuenta/articulos/[slug]/edit renderiza ArticleForm con datos
- /crear-articulo existe y funciona
- Type-check verde
- Smoke browser: usuario logged-in puede ver lista, crear nuevo, editar, eliminar artículo propio
</verification>

<success_criteria>
1. Organizador ve sus artículos reales en /cuenta/articulos
2. Botón "Solicitar artículo" navega a página existente
3. Botón "Editar" carga el artículo en form pre-poblado
4. Botón "Eliminar" funciona y filtra la lista localmente
5. /crear-articulo está montado con ArticleForm variant=sponsored
6. Sin imports residuales del bug ?mine=true
</success_criteria>

<output>
After completion, create `.planning/phases/17-articles-crud-completo-crud-de-noticias-en-dashboard-admin-formulario-permisos-por-rol-listado-moderaci-n/17-04-SUMMARY.md`
</output>
