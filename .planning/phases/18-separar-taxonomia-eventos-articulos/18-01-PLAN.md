---
phase: 18-separar-taxonomia-eventos-articulos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05]
must_haves:
  truths:
    - "Después de migrar, SELECT COUNT(*) FROM event_categories === SELECT COUNT(*) FROM Category (todas las categorías quedaron copiadas)"
    - "SELECT COUNT(*) FROM article_tags === SELECT COUNT(*) FROM Tag (todos los tags quedaron copiados)"
    - "SELECT COUNT(*) FROM _ArticleToArticleTag === SELECT COUNT(*) FROM _ArticleToTag (todos los vínculos artículo↔tag se preservaron)"
    - "Todo Event que tenía categoryId ahora tiene eventCategoryId con el mismo valor (Event.categoryId no se elimina aún)"
    - "Todo Hero que tenía categoryId ahora tiene eventCategoryId con el mismo valor (los heroes del carrusel home conservan su categoría visible)"
    - "Las tablas viejas Category, Tag, _ArticleToTag, Event.categoryId, Hero.categoryId SIGUEN existiendo (no se borran en este plan)"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Modelos EventCategory, EventTag, ArticleCategory, ArticleTag; relaciones Event.eventCategory + Event.eventTags + Hero.eventCategory + Article.articleCategory + Article.articleTags; Category, Tag, Event.categoryId, Hero.categoryId aún presentes"
      contains: "model EventCategory"
    - path: "apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql"
      provides: "DDL para 4 tablas nuevas + join tables + columnas eventCategoryId/articleCategoryId nullable + DML INSERT INTO ... SELECT que preserva IDs"
      contains: "INSERT INTO `event_categories`"
  key_links:
    - from: "apps/api/prisma/schema.prisma"
      to: "event_categories table"
      via: "model EventCategory @@map(\"event_categories\")"
      pattern: "@@map\\(\"event_categories\"\\)"
    - from: "Event.eventCategoryId"
      to: "event_categories.id"
      via: "FK en migration.sql"
      pattern: "REFERENCES `event_categories`"
    - from: "Hero.eventCategoryId"
      to: "event_categories.id"
      via: "FK en migration.sql — Hero reutiliza EventCategory (decisión locked)"
      pattern: "REFERENCES `event_categories`"
    - from: "_ArticleToArticleTag"
      to: "_ArticleToTag (copia por DML)"
      via: "INSERT INTO _ArticleToArticleTag (A, B) SELECT A, B FROM _ArticleToTag"
      pattern: "INSERT INTO `_ArticleToArticleTag`"
---

<objective>
Crear los modelos `EventCategory`, `EventTag`, `ArticleCategory`, `ArticleTag` en Prisma SIN eliminar `Category` ni `Tag`, generar la migración SQL hand-edited que (a) crea las tablas nuevas, (b) copia los datos desde las tablas viejas preservando IDs, (c) añade columnas `eventCategoryId` / `articleCategoryId` nullable a `Event` / `Hero` / `Article` y las backfilea desde `categoryId`.

Purpose: Schema aditivo-primero (RESEARCH "Pattern 1"). Después de este plan, ambos sets de tablas coexisten y los datos están duplicados. Esto desbloquea los planes 18-02 (backend) y 18-03 (frontend) para migrar la lectura/escritura al nuevo modelo SIN romper el sistema.

Output: schema.prisma actualizado + carpeta `apps/api/prisma/migrations/sch08_split_taxonomies/` con migration.sql conteniendo DDL + DML; `prisma generate` regenera el cliente con los nuevos modelos disponibles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-separar-taxonomia-eventos-articulos/18-RESEARCH.md
@.planning/phases/18-separar-taxonomia-eventos-articulos/18-VALIDATION.md
@apps/api/prisma/schema.prisma

<interfaces>
<!-- Estado actual de los modelos a tocar (extraído de schema.prisma) -->

Category (líneas 54-72 schema.prisma):
```prisma
model Category {
  id          Int      @id @default(autoincrement())
  name        String?
  slug        String   @unique
  description String?
  pricePerDay Int      @default(1000)
  icon        String?
  color       String?
  minDays     Int      @default(1)
  maxDays     Int      @default(30)
  order       Int      @default(0)
  events      Event[]
  heroes      Hero[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

Tag (líneas 74-81):
```prisma
model Tag {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  slug      String    @unique
  articles  Article[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}
```

Event (líneas 191-243) — campo a añadir: `eventCategoryId Int?` + relación `eventCategory EventCategory? @relation(fields: [eventCategoryId], references: [id])` + relación implícita `eventTags EventTag[]`. El campo `categoryId Int?` y `category Category? @relation(...)` SE MANTIENEN intactos.

Hero (líneas 124-155) — campo a añadir: `eventCategoryId Int?` + relación `eventCategory EventCategory? @relation(fields: [eventCategoryId], references: [id])`. El campo `categoryId Int?` y `category Category? @relation(...)` SE MANTIENEN intactos. (Decisión locked: Hero reutiliza EventCategory, opción A del RESEARCH.)

Article (líneas 85-108) — campos a añadir: `articleCategoryId Int?` + relación `articleCategory ArticleCategory? @relation(fields: [articleCategoryId], references: [id])` + relación implícita `articleTags ArticleTag[]`. El campo `tags Tag[]` SE MANTIENE intacto.

Naming de join tables implícitas Prisma:
- `Article.tags Tag[]` ↔ `Tag.articles Article[]` genera `_ArticleToTag` (columnas A=Article.id, B=Tag.id)
- `Article.articleTags ArticleTag[]` ↔ `ArticleTag.articles Article[]` generará `_ArticleToArticleTag` (columnas A=Article.id, B=ArticleTag.id)
- `Event.eventTags EventTag[]` ↔ `EventTag.events Event[]` generará `_EventToEventTag` (columnas A=Event.id, B=EventTag.id)

Ejemplo de migration.sql existente en `apps/api/prisma/migrations/20260524235433_sch05_category_orders_v2/migration.sql` muestra el estilo MySQL/Prisma con backticks, ENUM declarado inline, y CREATE INDEX/AddForeignKey al final.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Añadir EventCategory, EventTag, ArticleCategory, ArticleTag al schema.prisma y crear relaciones aditivas en Event/Hero/Article</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (estado actual — NO eliminar nada, solo añadir)
    - .planning/phases/18-separar-taxonomia-eventos-articulos/18-RESEARCH.md (Pattern 1 schema aditivo)
  </read_first>
  <action>
Editar `apps/api/prisma/schema.prisma`. NO eliminar `model Category`, `model Tag`, `Event.categoryId`, `Event.category`, `Hero.categoryId`, `Hero.category`, ni `Article.tags`. Todo es aditivo.

1) Inmediatamente después de `model Tag { ... }` (después de la línea ~81), añadir los 4 modelos nuevos:

```prisma
// ─── Taxonomías separadas (Phase 18) ───
// Aditivo-primero: coexisten con Category/Tag hasta el plan 18-04.

model EventCategory {
  id          Int      @id @default(autoincrement())
  name        String?
  slug        String   @unique
  description String?
  pricePerDay Int      @default(1000)
  icon        String?
  color       String?
  minDays     Int      @default(1)
  maxDays     Int      @default(30)
  order       Int      @default(0)
  events      Event[]
  heroes      Hero[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("event_categories")
}

model EventTag {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  slug      String   @unique
  events    Event[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("event_tags")
}

model ArticleCategory {
  id          Int       @id @default(autoincrement())
  name        String?
  slug        String    @unique
  description String?
  articles    Article[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@map("article_categories")
}

model ArticleTag {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  slug      String    @unique
  articles  Article[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@map("article_tags")
}
```

2) En `model Event` (líneas 191-243), DENTRO del bloque actual (no eliminar nada), añadir DESPUÉS de la línea `categoryId Int?` (línea 224) y antes de `articles Article[]`:

```prisma
  // Phase 18 — taxonomía separada (coexiste con category mientras se migra)
  eventCategory   EventCategory? @relation(fields: [eventCategoryId], references: [id])
  eventCategoryId Int?
  eventTags       EventTag[]
```

Y añadir al final del bloque `Event`, junto a los otros `@@index`, una nueva línea:
```prisma
  @@index([eventCategoryId])
```

3) En `model Hero` (líneas 124-155), DENTRO del bloque (no eliminar nada), añadir DESPUÉS de la línea `categoryId Int?` (línea 135) y antes de `owner User @relation(...)`:

```prisma
  // Phase 18 — taxonomía separada (coexiste con category mientras se migra)
  eventCategory   EventCategory? @relation(fields: [eventCategoryId], references: [id])
  eventCategoryId Int?
```

Y añadir al final del bloque `Hero`, junto a los otros `@@index`, una nueva línea:
```prisma
  @@index([eventCategoryId])
```

4) En `model Article` (líneas 85-108), DENTRO del bloque (no eliminar nada), añadir DESPUÉS de la línea `tags Tag[]` (línea 99) y antes de `events Event[]`:

```prisma
  // Phase 18 — taxonomía separada (coexiste con tags mientras se migra)
  articleCategory   ArticleCategory? @relation(fields: [articleCategoryId], references: [id])
  articleCategoryId Int?
  articleTags       ArticleTag[]
```

Y añadir al final del bloque `Article`, junto a los otros `@@index`, una nueva línea:
```prisma
  @@index([articleCategoryId])
```

5) Validar sintaxis con `npx prisma validate` (cwd: `apps/api`). NO ejecutar `prisma migrate dev` aún (eso es Task 2).
  </action>
  <verify>
    <automated>cd apps/api && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model EventCategory" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "model EventTag" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "model ArticleCategory" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "model ArticleTag" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "@@map(\"event_categories\")" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "@@map(\"article_tags\")" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "eventCategoryId Int?" apps/api/prisma/schema.prisma` retorna `2` (Event + Hero)
    - `grep -c "articleCategoryId Int?" apps/api/prisma/schema.prisma` retorna `1` (Article)
    - `grep -c "eventTags       EventTag\[\]" apps/api/prisma/schema.prisma` retorna `1`
    - `grep -c "articleTags       ArticleTag\[\]" apps/api/prisma/schema.prisma` retorna `1`
    - `model Category` y `model Tag` SIGUEN existiendo: `grep -c "^model Category " apps/api/prisma/schema.prisma` retorna `1` y `grep -c "^model Tag " apps/api/prisma/schema.prisma` retorna `1`
    - `Event.categoryId Int?` y `Hero.categoryId Int?` y `Article.tags Tag[]` siguen presentes (NO eliminar)
    - `cd apps/api && npx prisma validate` imprime "The schema at prisma/schema.prisma is valid 🚀"
  </acceptance_criteria>
  <done>
    Schema validado por Prisma sin errores; los 4 modelos nuevos están definidos; las columnas `eventCategoryId` y `articleCategoryId` están declaradas nullable en sus modelos; relaciones M:M implícitas para `eventTags` y `articleTags` están declaradas; los modelos legacy Category/Tag y sus FKs siguen intactos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generar migración Prisma hand-edited (sch08_split_taxonomies) con DDL + DML que crea tablas, copia datos preservando IDs y backfillea FKs nuevas</name>
  <files>apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql</files>
  <read_first>
    - apps/api/prisma/schema.prisma (debe estar ya modificado por Task 1)
    - apps/api/prisma/migrations/20260524235433_sch05_category_orders_v2/migration.sql (ejemplo de estilo MySQL: backticks, ENUM inline, AddForeignKey al final)
    - .planning/phases/18-separar-taxonomia-eventos-articulos/18-RESEARCH.md (Pattern 2 SQL INSERT INTO SELECT, Pitfall 2 join table rename, Critical Constraint OrdersService)
  </read_first>
  <action>
**Estrategia:** generar el skeleton DDL con `prisma migrate dev --create-only` y luego hand-editarlo para añadir los INSERTs de datos antes de aplicar.

1) Detener el servidor API si está corriendo. Asegurarse de que `DATABASE_URL` apunta a una DB con datos reales (o de seed) en `Category`, `Tag`, `_ArticleToTag`, `Event.categoryId`, `Hero.categoryId`.

2) Desde `apps/api/`, generar la migración SIN aplicarla:
```bash
cd apps/api && npx prisma migrate dev --create-only --name sch08_split_taxonomies
```

3) Abrir `apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql`. Prisma habrá generado el DDL para las 4 tablas nuevas (`event_categories`, `event_tags`, `article_categories`, `article_tags`), las join tables (`_ArticleToArticleTag`, `_EventToEventTag`), y las columnas `eventCategoryId` / `articleCategoryId` en Event/Hero/Article más sus FKs e índices.

4) **REORDENAR y AÑADIR DML** dentro del archivo SQL. El orden final debe ser:

```sql
-- ── A) DDL — crear tablas nuevas (lo que Prisma generó) ──
-- (Mantén tal cual los CREATE TABLE `event_categories`, `event_tags`,
--  `article_categories`, `article_tags`, `_ArticleToArticleTag`, `_EventToEventTag`
--  que Prisma generó.)

-- ── B) DDL — añadir columnas nullable a Event/Hero/Article (lo que Prisma generó) ──
-- (Mantén tal cual los ALTER TABLE `Event` ADD COLUMN `eventCategoryId`,
--  `Hero` ADD COLUMN `eventCategoryId`, `Article` ADD COLUMN `articleCategoryId`.)
-- (Mantén los CREATE INDEX que Prisma generó para esas columnas.)

-- ── C) DML — copiar datos de tablas viejas a nuevas PRESERVANDO IDS ──
--    Antes de los AddForeignKey, así si fallan los datos podemos diagnosticar
--    sin tener constraints rotos.

INSERT INTO `event_categories` (`id`, `name`, `slug`, `description`, `pricePerDay`, `icon`, `color`, `minDays`, `maxDays`, `order`, `createdAt`, `updatedAt`)
SELECT `id`, `name`, `slug`, `description`, `pricePerDay`, `icon`, `color`, `minDays`, `maxDays`, `order`, `createdAt`, `updatedAt`
FROM `Category`;

INSERT INTO `article_tags` (`id`, `name`, `slug`, `createdAt`, `updatedAt`)
SELECT `id`, `name`, `slug`, `createdAt`, `updatedAt`
FROM `Tag`;

-- Copia la join table implícita Article↔Tag → Article↔ArticleTag.
-- Las columnas A y B preservan los mismos IDs porque Tag.id se copió 1:1 a ArticleTag.id.
INSERT INTO `_ArticleToArticleTag` (`A`, `B`)
SELECT `A`, `B`
FROM `_ArticleToTag`;

-- Backfill Event.eventCategoryId desde Event.categoryId (mismo valor — IDs preservados arriba).
UPDATE `Event`
SET `eventCategoryId` = `categoryId`
WHERE `categoryId` IS NOT NULL;

-- Backfill Hero.eventCategoryId desde Hero.categoryId (decisión locked: Hero reutiliza EventCategory).
UPDATE `Hero`
SET `eventCategoryId` = `categoryId`
WHERE `categoryId` IS NOT NULL;

-- ── D) Constraints / FKs nuevas (lo que Prisma generó al final) ──
-- (Mantén tal cual los AddForeignKey para event_categories↔Event/Hero,
--  article_categories↔Article, _ArticleToArticleTag↔Article+ArticleTag,
--  _EventToEventTag↔Event+EventTag.)
```

Si Prisma generó las DML en otro orden, ELIMINAR las partes auto-generadas que no apliquen y reescribir siguiendo el orden A→B→C→D arriba.

NO añadir `DROP TABLE Category`, `DROP TABLE Tag`, `DROP TABLE _ArticleToTag`, ni `ALTER TABLE Event DROP COLUMN categoryId`, ni `ALTER TABLE Hero DROP COLUMN categoryId`. Esos van en el plan 18-04 (otra migración).

5) Aplicar la migración:
```bash
cd apps/api && npx prisma migrate dev
```

(Sin `--create-only` esta vez — aplica el SQL editado.)

6) Regenerar el cliente Prisma:
```bash
cd apps/api && npx prisma generate
```

7) Validar con queries SQL directas a la DB:
```bash
# Reemplaza con tu cliente MySQL. Sustituye los valores esperados.
mysql -e "SELECT COUNT(*) AS c FROM event_categories" $DATABASE_NAME
mysql -e "SELECT COUNT(*) AS c FROM Category" $DATABASE_NAME
# Las dos cifras deben ser iguales.
```
  </action>
  <verify>
    <automated>cd apps/api && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - El directorio `apps/api/prisma/migrations/sch08_split_taxonomies/` existe y contiene `migration.sql`
    - `grep -c "CREATE TABLE \`event_categories\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "CREATE TABLE \`article_tags\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "CREATE TABLE \`_ArticleToArticleTag\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "CREATE TABLE \`_EventToEventTag\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "INSERT INTO \`event_categories\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "INSERT INTO \`article_tags\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "INSERT INTO \`_ArticleToArticleTag\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1`
    - `grep -c "UPDATE \`Event\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1` (backfill eventCategoryId)
    - `grep -c "UPDATE \`Hero\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `1` (backfill eventCategoryId)
    - El SQL NO contiene `DROP TABLE \`Category\``, `DROP TABLE \`Tag\``, `DROP TABLE \`_ArticleToTag\``: `grep -cE "DROP TABLE \`(Category|Tag|_ArticleToTag)\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `0`
    - El SQL NO contiene `ALTER TABLE \`Event\` DROP COLUMN \`categoryId\``: `grep -c "DROP COLUMN \`categoryId\`" apps/api/prisma/migrations/sch08_split_taxonomies/migration.sql` retorna `0`
    - `cd apps/api && npx prisma migrate status` reporta "Database schema is up to date!"
    - SQL paridad de filas (ejecutar contra la DB):
      - `SELECT COUNT(*) FROM event_categories` === `SELECT COUNT(*) FROM Category`
      - `SELECT COUNT(*) FROM article_tags` === `SELECT COUNT(*) FROM Tag`
      - `SELECT COUNT(*) FROM _ArticleToArticleTag` === `SELECT COUNT(*) FROM _ArticleToTag`
      - `SELECT COUNT(*) FROM Event WHERE categoryId IS NOT NULL AND eventCategoryId IS NULL` retorna `0`
      - `SELECT COUNT(*) FROM Hero WHERE categoryId IS NOT NULL AND eventCategoryId IS NULL` retorna `0`
    - Prisma Client regenerado: `node -e "const { PrismaClient } = require('./apps/api/node_modules/.prisma/client'); const p = new PrismaClient(); p.eventCategory.findMany().then(r => { console.log(r.length); process.exit(0); })"` imprime un número (no lanza "Property 'eventCategory' does not exist").
  </acceptance_criteria>
  <done>
    Migración `sch08_split_taxonomies` aplicada a la DB; las 4 tablas nuevas existen con datos copiados 1:1 desde las viejas; `Event.eventCategoryId` y `Hero.eventCategoryId` y `Article.articleCategoryId` están backfilled; las tablas viejas Category/Tag/_ArticleToTag SIGUEN EXISTIENDO; `prisma generate` regeneró el cliente con `prisma.eventCategory`, `prisma.eventTag`, `prisma.articleCategory`, `prisma.articleTag` disponibles.
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && npx prisma validate` → "valid 🚀"
- `cd apps/api && npx prisma migrate status` → "up to date"
- Conteo SQL: las 5 paridades de must_haves.truths pasan
- `npx prisma generate` regenera el cliente sin warnings sobre modelos faltantes
</verification>

<success_criteria>
- Las 4 tablas nuevas (`event_categories`, `event_tags`, `article_categories`, `article_tags`) existen en la DB
- Las join tables `_ArticleToArticleTag` y `_EventToEventTag` existen
- `Event.eventCategoryId`, `Hero.eventCategoryId`, `Article.articleCategoryId` existen como columnas nullable con FK
- Los datos de `Category` están duplicados en `event_categories` con los mismos IDs
- Los datos de `Tag` están duplicados en `article_tags` con los mismos IDs
- Las relaciones `_ArticleToTag` están duplicadas en `_ArticleToArticleTag`
- Las tablas Category/Tag/_ArticleToTag/Event.categoryId/Hero.categoryId siguen intactas (cleanup es 18-04)
</success_criteria>

<output>
After completion, create `.planning/phases/18-separar-taxonomia-eventos-articulos/18-01-SUMMARY.md` siguiendo el template de summary, dejando explícito:
- Cuántas filas se copiaron a cada tabla nueva
- Cuántos Events y Heros fueron backfilled
- El SHA de la migration generada
- Recordatorio para 18-04: queda pendiente DROP de tablas viejas
</output>
