---
phase: 19-rediseno-completo-de-noticias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/globals.css
  - apps/website/lib/api.ts
  - apps/website/components/ArticleCard.tsx
  - apps/website/app/(site)/noticias/NoticiasListView.tsx
autonomous: true
requirements: [NEWS-09, NEWS-10]
must_haves:
  truths:
    - "El ArticleCard muestra un botón de like sobre la imagen con contador optimistic"
    - "El ArticleCard muestra el tiempo de lectura calculado en .a-meta"
    - "El ArticleCard muestra un badge de eventos relacionados si existen"
    - "Las clases CSS .mega-bg, .mega-inner, .mega-flat, .mega-aside, .mega-cta, .a-like existen en globals.css"
    - "El tipo ApiArticleCategory incluye nameJa en lib/api.ts"
    - "El tipo ApiArticle incluye _count y events en lib/api.ts"
  artifacts:
    - path: "apps/website/components/ArticleCard.tsx"
      provides: "Componente ArticleCard reutilizable con like button + reading time"
      exports: ["ArticleCard"]
    - path: "apps/website/app/globals.css"
      provides: "Clases CSS .mega-*, .a-like, @keyframes megaIn"
      contains: ".mega-bg"
    - path: "apps/website/lib/api.ts"
      provides: "Tipos actualizados ApiArticleCategory (nameJa) y ApiArticle (_count)"
      exports: ["ApiArticleCategory", "ApiArticle"]
  key_links:
    - from: "apps/website/components/ArticleCard.tsx"
      to: "/api/articles/{id}/like"
      via: "fetch POST/DELETE on toggle"
      pattern: "articles.*like"
    - from: "apps/website/app/(site)/noticias/NoticiasListView.tsx"
      to: "apps/website/components/ArticleCard.tsx"
      via: "import ArticleCard"
      pattern: "import.*ArticleCard"
---

<objective>
Establece los cimientos de CSS y tipos para todo el rediseño de Noticias: agrega las clases `.mega-*` y `.a-like` al archivo global CSS, actualiza los tipos TypeScript en lib/api.ts, y extrae ArticleCard como componente compartido con botón like + tiempo de lectura.

Purpose: Los planes 19-02, 19-03 y 19-04 dependen de estos tipos y de este componente. Al extraerlo aquí evitamos duplicar código en hub page y category page.

Output: `ArticleCard.tsx` exportable, `globals.css` con todas las clases nuevas, `lib/api.ts` con tipos extendidos, `NoticiasListView.tsx` usando el nuevo componente.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-rediseno-completo-de-noticias/19-CONTEXT.md

<!-- Archivo de referencia visual — leer las secciones indicadas antes de implementar -->
<!-- design/Konbini.html líneas 979-1011 (CSS .mega-*), 2113-2157 (ArticleCard) -->
</context>

<interfaces>
<!-- Estado actual de ApiArticleCategory en lib/api.ts — se extiende en Task 1 -->
```typescript
// ANTES (lib/api.ts línea 106-111):
export type ApiArticleCategory = {
  id: number;
  name: string | null;
  slug: string;
  description: string | null;
};

// DESPUÉS (agregar nameJa):
export type ApiArticleCategory = {
  id: number;
  name: string | null;
  nameJa: string | null;   // ← nuevo
  slug: string;
  description: string | null;
};
```

<!-- Estado actual del ArticleCard en NoticiasListView.tsx -->
<!-- El componente está inline a partir de línea 176, dentro del .map() -->
<!-- Exportar como ArticleCard.tsx recibe: article: ApiArticle, priority?: boolean -->

<!-- Tipo ApiArticle actual (definido en noticias/page.tsx línea 23-38) -->
<!-- Se mueve a lib/api.ts y se extiende con _count -->
```typescript
// ApiArticle en lib/api.ts (nuevo, trasladar desde noticias/page.tsx):
export type ApiArticle = {
  id: number;
  title: string;
  slug: string;
  excerpt: string | null;
  content: string;
  image: string | null;
  status: string;
  userId: number | null;
  isSponsored: boolean;
  createdAt: string;
  articleCategory: ApiArticleCategory | null;
  articleTags: { id: number; name: string; slug: string }[];
  tags: { id: number; name: string; slug: string }[];
  events?: ApiArticleEvent[];
  _count?: { likes: number };  // ← nuevo
};

export type ApiArticleEvent = {
  id: number;
  slug: string;
  title: string;
  poster: string | null;
  banner: string | null;
  dates: { id: number; date: string | null }[];
  city: { name: string } | null;
  category: { name: string; slug: string } | null;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar tipos en lib/api.ts y agregar CSS en globals.css</name>
  <files>apps/website/lib/api.ts, apps/website/app/globals.css</files>
  <action>
**En `apps/website/lib/api.ts`:**

1. Agregar `nameJa?: string | null` al tipo `ApiArticleCategory` (después de `slug`):
   ```typescript
   export type ApiArticleCategory = {
     id: number;
     name: string | null;
     nameJa?: string | null;
     slug: string;
     description: string | null;
   };
   ```

2. Agregar los tipos `ApiArticleEvent` y `ApiArticle` al final de la sección "// ─────────────────────────── Contenido ──────────────────────────" (antes de `ApiCountry`). Estos tipos actualmente viven en `apps/website/app/(site)/noticias/page.tsx` — moverlos aquí. Agregar `_count?: { likes: number }` a `ApiArticle`. Exportar ambos:
   ```typescript
   export type ApiArticleEvent = {
     id: number;
     slug: string;
     title: string;
     poster: string | null;
     banner: string | null;
     dates: { id: number; date: string | null }[];
     city: { name: string } | null;
     category: { name: string; slug: string } | null;
   };

   export type ApiArticle = {
     id: number;
     title: string;
     slug: string;
     excerpt: string | null;
     content: string;
     image: string | null;
     status: string;
     userId: number | null;
     isSponsored: boolean;
     createdAt: string;
     articleCategory: ApiArticleCategory | null;
     articleTags: { id: number; name: string; slug: string }[];
     tags: { id: number; name: string; slug: string }[];
     events?: ApiArticleEvent[];
     _count?: { likes: number };
   };
   ```

3. Agregar métodos `articles` y `article` al objeto `api`:
   ```typescript
   articles: (query?: { page?: number; pageSize?: number; articleCategory?: string }) =>
     request<{ items: ApiArticle[]; total: number; page: number; pageSize: number; totalPages: number }>(
       `/articles${qs(query ?? {})}`
     ),
   article: (slug: string) => request<ApiArticle>(`/articles/${slug}`),
   likeArticle: (id: number, token: string) =>
     request<{ ok: boolean }>(`/articles/${id}/like`, { method: "POST" }, token),
   unlikeArticle: (id: number, token: string) =>
     request<{ ok: boolean }>(`/articles/${id}/like`, { method: "DELETE" }, token),
   ```

**En `apps/website/app/globals.css`:**

Agregar al final del archivo (antes del último cierre si lo hay) el bloque de CSS nuevo para mega menú y like button. Copiar exactamente del design/Konbini.html líneas 979-1011:

```css
/* ───── news mega menu ───── */
.mega-bg { position: fixed; left: 0; right: 0; top: var(--mega-top, 72px); z-index: 70; background: color-mix(in oklab, var(--surface) 96%, transparent); backdrop-filter: blur(24px) saturate(160%); border-bottom: 1px solid var(--line); border-top: 1px solid var(--line); box-shadow: 0 24px 60px -20px rgba(0,0,0,.4); animation: megaIn .18s cubic-bezier(.2,.7,.3,1) both; }
@keyframes megaIn { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: translateY(0); } }
.mega-inner { max-width: 1280px; margin: 0 auto; padding: 28px 32px; display: grid; grid-template-columns: 280px 1fr; gap: 36px; }
.mega-aside { border-right: 1px solid var(--line); padding-right: 28px; }
.mega-aside .label { font-family: var(--font-mono); font-size: 10px; letter-spacing: .18em; color: var(--ink-3); text-transform: uppercase; margin-bottom: 14px; }
.mega-aside h3 { font-family: var(--font-display); font-size: 32px; letter-spacing: -.02em; margin: 0 0 8px; line-height: 1.05; }
.mega-aside p { color: var(--ink-3); font-size: 13px; line-height: 1.55; margin: 0 0 22px; }
.mega-aside .mega-cta { display: inline-flex; align-items: center; gap: 6px; padding: 10px 16px; border-radius: 999px; background: var(--ink); color: var(--bg); font-size: 13px; font-weight: 600; cursor: pointer; }
.mega-flat { columns: 4; column-gap: 32px; }
.mega-flat button { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 7px 8px; border-radius: 8px; color: var(--ink-2); font-size: 14px; font-weight: 500; text-align: left; transition: background .12s, color .12s; width: 100%; break-inside: avoid; }
.mega-flat button:hover { background: var(--surface-2); color: var(--ink); }
.mega-flat button.on { color: var(--ink); background: var(--surface-2); }
.mega-flat button .ja { font-family: var(--font-mono); font-size: 10px; color: var(--ink-3); letter-spacing: .04em; flex-shrink: 0; }
.mega-flat button.on .ja { color: var(--accent); }
@media (max-width: 960px) {
  .mega-inner { grid-template-columns: 1fr; gap: 22px; padding: 22px; }
  .mega-aside { border-right: 0; border-bottom: 1px solid var(--line); padding-right: 0; padding-bottom: 18px; }
  .mega-flat { columns: 2; }
}
@media (max-width: 640px) {
  .mega-bg { display: none; }
}

/* ───── article card like button ───── */
.a-like { position: absolute; bottom: 10px; right: 10px; display: inline-flex; align-items: center; gap: 5px; padding: 5px 10px; border-radius: 999px; background: rgba(0,0,0,.55); backdrop-filter: blur(8px); color: #fff; font-size: 12px; font-weight: 600; border: 1px solid rgba(255,255,255,.15); transition: background .15s, color .15s; }
.a-like:hover { background: rgba(0,0,0,.75); }
.a-like.on { background: var(--accent); border-color: var(--accent); }
.a-like.on svg { fill: #fff; }
.a-rel { display: inline-flex; align-items: center; gap: 4px; }
```
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && grep -q "nameJa" apps/website/lib/api.ts && grep -q "mega-bg" apps/website/app/globals.css && grep -q "a-like" apps/website/app/globals.css && grep -q "ApiArticle" apps/website/lib/api.ts && echo "OK"</automated>
  </verify>
  <done>lib/api.ts exporta ApiArticleCategory con nameJa, ApiArticle con _count, ApiArticleEvent, y métodos articles/article/likeArticle/unlikeArticle. globals.css tiene .mega-bg, .mega-flat, .mega-aside, .mega-cta, .a-like y @keyframes megaIn.</done>
</task>

<task type="auto">
  <name>Task 2: Extraer ArticleCard como componente compartido con like + tiempo de lectura</name>
  <files>apps/website/components/ArticleCard.tsx, apps/website/app/(site)/noticias/NoticiasListView.tsx, apps/website/app/(site)/noticias/page.tsx</files>
  <action>
**Crear `apps/website/components/ArticleCard.tsx`:**

Este componente extrae la lógica del `article.art-card` de `NoticiasListView.tsx` y agrega el botón like y tiempo de lectura según design/Konbini.html líneas 2117-2157.

```typescript
"use client";
import Link from "next/link";
import { useState } from "react";
import { imageUrl } from "@/lib/api";
import type { ApiArticle } from "@/lib/api";
import { useUser } from "@/components/providers";

// ── Helpers ────────────────────────────────────────────────────────────────

export function formatDate(iso: string): string {
  const d = new Date(iso);
  const meses = ["ENE","FEB","MAR","ABR","MAY","JUN","JUL","AGO","SEP","OCT","NOV","DIC"];
  return `${d.getUTCDate()} ${meses[d.getUTCMonth()]} ${d.getUTCFullYear()}`;
}

export function getCat(a: ApiArticle): string {
  if (a.articleCategory?.name) return a.articleCategory.name;
  if (a.articleTags?.length)   return a.articleTags[0].name;
  if (a.tags?.length)          return a.tags[0].name;
  return "Noticias";
}

export function readingTime(content: string): string {
  const words = content.trim().split(/\s+/).length;
  const mins = Math.max(1, Math.ceil(words / 200));
  return `${mins} min`;
}

// ── Component ──────────────────────────────────────────────────────────────

interface Props {
  article: ApiArticle;
  /** Si true, el h3 usa font-size 28px (para uso en featured/big cards) */
  big?: boolean;
}

export function ArticleCard({ article: a, big = false }: Props) {
  const { user, token } = useUser();
  const [liked, setLiked]   = useState(false);
  const [likes, setLikes]   = useState(a._count?.likes ?? 0);
  const [toastMsg, setToastMsg] = useState<string | null>(null);

  // Simple local toast (no dep externa — desaparece en 2.5s)
  function showToast(msg: string) {
    setToastMsg(msg);
    setTimeout(() => setToastMsg(null), 2500);
  }

  async function toggleLike(ev: React.MouseEvent) {
    ev.preventDefault();
    ev.stopPropagation();

    if (!user || !token) {
      showToast("Inicia sesión para dar like");
      return;
    }

    // Optimistic update
    const next = !liked;
    setLiked(next);
    setLikes((c) => c + (next ? 1 : -1));

    try {
      await fetch(`/api/articles/${a.id}/like`, {
        method: next ? "POST" : "DELETE",
        headers: { Authorization: `Bearer ${token}` },
      });
    } catch {
      // Revert on error
      setLiked(!next);
      setLikes((c) => c + (next ? -1 : 1));
    }
  }

  const relatedCount = a.events?.length ?? 0;
  const rt = readingTime(a.content ?? "");

  return (
    <Link href={`/noticias/${a.slug}`} style={{ textDecoration: "none" }}>
      <article className="art-card" style={{ position: "relative" }}>
        {/* Toast local */}
        {toastMsg && (
          <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: "var(--surface)", border: "1px solid var(--line)", borderRadius: 12, padding: "12px 20px", fontSize: 13, zIndex: 99, whiteSpace: "nowrap", boxShadow: "var(--shadow)" }}>
            {toastMsg}
          </div>
        )}

        <div className="a-img" style={{ position: "relative" }}>
          {a.image ? (
            <img
              src={imageUrl(a.image)}
              alt={a.title}
              style={{ position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover" }}
            />
          ) : (
            <div style={{ position: "absolute", inset: 0, background: "linear-gradient(135deg,#2a1410,#4a1820)" }} />
          )}
          {a.isSponsored && <span className="sponsor">ARTÍCULO PATROCINADO</span>}

          {/* Like button */}
          <button
            className={`a-like ${liked ? "on" : ""}`}
            onClick={toggleLike}
            title={liked ? "Quitar like" : "Me gusta"}
          >
            <svg width="13" height="13" viewBox="0 0 24 24" fill={liked ? "currentColor" : "none"} stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
              <path d="M7 22V11"/>
              <path d="M2 13a2 2 0 0 1 2-2h3v11H4a2 2 0 0 1-2-2v-7z"/>
              <path d="M22 11.5a2.5 2.5 0 0 0-2.5-2.5h-5l1-4.5a2 2 0 0 0-3.9-.9L7 11v11h11.4a2 2 0 0 0 2-1.7l1.5-7.3a2.5 2.5 0 0 0 .1-1.5z"/>
            </svg>
            <span>{likes >= 1000 ? (likes / 1000).toFixed(1) + "k" : likes}</span>
          </button>
        </div>

        <div className="a-cat">{getCat(a)}</div>
        <h3 className="a-title" style={big ? { fontSize: 28 } : undefined}>{a.title}</h3>
        <div className="a-meta">
          <span>{formatDate(a.createdAt)}</span>
          <span>·</span>
          <span>{rt} lectura</span>
          {relatedCount > 0 && (
            <>
              <span>·</span>
              <span className="a-rel">
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round">
                  <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.72"/>
                  <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.72-1.72"/>
                </svg>
                {relatedCount} evento{relatedCount > 1 ? "s" : ""} relacionado{relatedCount > 1 ? "s" : ""}
              </span>
            </>
          )}
        </div>
      </article>
    </Link>
  );
}
```

**Nota sobre `token`:** El hook `useUser()` actualmente expone `{ user, logout }`. Revisar `apps/website/components/providers.tsx` para ver si expone `token`. Si no lo expone directamente, leer el token desde `localStorage.getItem("token")` dentro de `toggleLike` (no en el render). Adaptar según lo que exista.

**Actualizar `apps/website/app/(site)/noticias/NoticiasListView.tsx`:**

1. Eliminar las funciones helpers `formatDate`, `getCat` del archivo (ahora en ArticleCard.tsx).
2. Reemplazar el bloque `<Link key={a.id}...><article className="art-card">...</article></Link>` (líneas 176-216) con:
   ```tsx
   <ArticleCard key={a.id} article={a} />
   ```
3. Agregar el import: `import { ArticleCard } from "@/components/ArticleCard";`
4. Cambiar el import de `ApiArticle`: ya no lo importa desde `./page` sino desde `@/lib/api`:
   ```typescript
   import type { ApiArticle } from "@/lib/api";
   ```

**Actualizar `apps/website/app/(site)/noticias/page.tsx`:**

1. Eliminar las definiciones de `ApiArticle` y `ApiArticleEvent` (ahora viven en lib/api.ts).
2. Cambiar la importación a:
   ```typescript
   import type { ApiArticle, ApiArticleCategory } from "@/lib/api";
   ```
3. La función `fetchArticleCategories` ya existe en `api.articleCategories()`. Mantener la implementación local directa (con fetch a process.env) por eficiencia en SSR — no cambiar.
4. Aumentar `PAGE_SIZE` de 24 a 50 para que el hub page tenga suficientes artículos para rails.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && npx tsc --noEmit --project apps/website/tsconfig.json 2>&1 | grep -E "error TS" | head -20 || echo "TypeScript OK"</automated>
  </verify>
  <done>
  - `apps/website/components/ArticleCard.tsx` existe y exporta `ArticleCard`, `formatDate`, `getCat`, `readingTime`
  - `NoticiasListView.tsx` importa `ArticleCard` desde `@/components/ArticleCard` y ya no define el `article` inline
  - `page.tsx` importa `ApiArticle` desde `@/lib/api`
  - TypeScript no reporta errores relacionados con estos cambios
  </done>
</task>

</tasks>

<verification>
Después de ambas tasks:
1. `grep -q "nameJa" apps/website/lib/api.ts` → exit 0
2. `grep -q "mega-bg" apps/website/app/globals.css` → exit 0
3. `grep -q "a-like" apps/website/app/globals.css` → exit 0
4. `ls apps/website/components/ArticleCard.tsx` → existe
5. TypeScript compile sin errores en los archivos modificados
</verification>

<success_criteria>
- globals.css tiene todas las clases `.mega-*`, `.a-like`, y `@keyframes megaIn` copiadas exactamente del design
- lib/api.ts exporta `ApiArticleCategory` con `nameJa?: string | null`, `ApiArticle` con `_count?: { likes: number }`, métodos `likeArticle`/`unlikeArticle`
- `ArticleCard.tsx` es un componente "use client" independiente que renderiza like button (optimistic toggle, POST/DELETE /api/articles/{id}/like) y tiempo de lectura
- `NoticiasListView.tsx` usa `<ArticleCard article={a} />` en lugar del HTML inline
- `page.tsx` de noticias ya no define tipos duplicados
</success_criteria>

<output>
Crear `.planning/phases/19-rediseno-completo-de-noticias/19-01-SUMMARY.md` con:
- Archivos modificados y qué cambió en cada uno
- Confirmación de que `nameJa` retorna de la API (campo en schema.prisma confirmado)
- Nota sobre la ubicación del token en useUser() (si es localStorage o context)
</output>
