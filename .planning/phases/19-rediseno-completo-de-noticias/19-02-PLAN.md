---
phase: 19-rediseno-completo-de-noticias
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - apps/website/components/NewsMegaMenu.tsx
  - apps/website/components/Header.tsx
autonomous: true
requirements: [NEWS-01]
must_haves:
  truths:
    - "Al hacer hover sobre el link 'Noticias' en el navbar, aparece un mega menú con todas las categorías"
    - "El mega menú lista las categorías en 4 columnas (columns: 4), ordenadas alfabéticamente"
    - "Cada item muestra el nombre a la izquierda y el texto japonés (nameJa) a la derecha en .ja"
    - "El aside izquierdo tiene el label NOTICIAS · ニュース, un h3 'Lo último.', y un CTA button"
    - "Al hacer clic en una categoría, navega a /noticias/categoria/{slug}"
    - "El mega menú se cierra al mover el mouse fuera del .mega-bg wrapper"
    - "En mobile (max-width: 640px), el mega menú está oculto via CSS"
  artifacts:
    - path: "apps/website/components/NewsMegaMenu.tsx"
      provides: "Componente mega menú de noticias"
      exports: ["NewsMegaMenu"]
    - path: "apps/website/components/Header.tsx"
      provides: "Header con hover trigger para mega menú en el link Noticias"
  key_links:
    - from: "apps/website/components/Header.tsx"
      to: "apps/website/components/NewsMegaMenu.tsx"
      via: "import + onMouseEnter en el botón Noticias"
      pattern: "NewsMegaMenu"
    - from: "apps/website/components/NewsMegaMenu.tsx"
      to: "/api/article-categories"
      via: "fetch en useEffect al montar"
      pattern: "article-categories"
---

<objective>
Implementar el mega menú de Noticias en el navbar: un dropdown que aparece al hovear el link "Noticias", muestra todas las categorías en layout de 4 columnas ordenadas alfabéticamente, y navega a `/noticias/categoria/{slug}`.

Purpose: NEWS-01 del rediseño. El mega menú es la entrada principal de descubrimiento de categorías de noticias.

Output: `NewsMegaMenu.tsx` nuevo, `Header.tsx` modificado para triggear el mega menú.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-rediseno-completo-de-noticias/19-CONTEXT.md
@.planning/phases/19-rediseno-completo-de-noticias/19-01-SUMMARY.md

<!-- Leer antes de implementar: -->
<!-- design/Konbini.html líneas 1891-1916 (NewsMegaMenu function) -->
<!-- apps/website/components/Header.tsx — estructura actual del navbar -->
</context>

<interfaces>
<!-- CSS classes ya disponibles en globals.css (agregadas en plan 19-01): -->
<!-- .mega-bg, .mega-inner, .mega-aside, .mega-flat, .mega-cta -->
<!-- .mega-flat button, .mega-flat button.on, .mega-flat button .ja -->

<!-- Tipo disponible en lib/api.ts (extendido en plan 19-01): -->
```typescript
export type ApiArticleCategory = {
  id: number;
  name: string | null;
  nameJa?: string | null;
  slug: string;
  description: string | null;
};
```

<!-- Header.tsx estructura actual (línea 141-158): -->
<!-- El botón "Noticias" está en <nav className="cats"> -->
<!-- onClick={() => router.push("/noticias")} -->
<!-- La variable `active` ya detecta pathname.startsWith("/noticias") → "news" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear NewsMegaMenu.tsx</name>
  <files>apps/website/components/NewsMegaMenu.tsx</files>
  <action>
Crear el componente `apps/website/components/NewsMegaMenu.tsx`:

```typescript
"use client";
import { useEffect, useState } from "react";
import { useRouter, usePathname } from "next/navigation";
import type { ApiArticleCategory } from "@/lib/api";

interface Props {
  onClose: () => void;
}

export function NewsMegaMenu({ onClose }: Props) {
  const router = useRouter();
  const pathname = usePathname();
  const [categories, setCategories] = useState<ApiArticleCategory[]>([]);

  // Fetch categories on mount
  useEffect(() => {
    fetch("/api/article-categories")
      .then((r) => (r.ok ? r.json() : []))
      .then((data: ApiArticleCategory[]) => {
        // Sort alphabetically by name
        const sorted = [...data].sort((a, b) =>
          (a.name ?? a.slug).localeCompare(b.name ?? b.slug, "es")
        );
        setCategories(sorted);
      })
      .catch(() => setCategories([]));
  }, []);

  // Detect current category slug from pathname (e.g. /noticias/categoria/anime)
  const currentSlug = pathname.startsWith("/noticias/categoria/")
    ? pathname.split("/noticias/categoria/")[1]?.split("/")[0]
    : null;

  function go(slug: string | null) {
    onClose();
    if (slug === null) {
      router.push("/noticias");
    } else {
      router.push(`/noticias/categoria/${slug}`);
    }
  }

  return (
    <div
      className="mega-bg"
      style={{ "--mega-top": "72px" } as React.CSSProperties}
      onMouseLeave={onClose}
    >
      <div className="mega-inner">
        <aside className="mega-aside">
          <div className="label">NOTICIAS · ニュース</div>
          <h3>
            Lo último<span style={{ color: "var(--accent)" }}>.</span>
          </h3>
          <p>
            Cobertura editorial diaria de anime, manga, gaming y cultura otaku
            — donde la comunidad llega primero.
          </p>
          <button className="mega-cta" onClick={() => go(null)}>
            Ver todas las noticias
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </button>
        </aside>

        <div className="mega-flat">
          {categories.map((c) => (
            <button
              key={c.slug}
              className={currentSlug === c.slug ? "on" : ""}
              onClick={() => go(c.slug)}
            >
              <span>{c.name ?? c.slug}</span>
              {c.nameJa && <span className="ja">{c.nameJa}</span>}
            </button>
          ))}
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && ls apps/website/components/NewsMegaMenu.tsx && grep -q "mega-bg" apps/website/components/NewsMegaMenu.tsx && grep -q "mega-flat" apps/website/components/NewsMegaMenu.tsx && echo "OK"</automated>
  </verify>
  <done>NewsMegaMenu.tsx existe, tiene className="mega-bg" wrapper con onMouseLeave, layout .mega-aside + .mega-flat, y fetch de article-categories.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar mega menú en Header.tsx</name>
  <files>apps/website/components/Header.tsx</files>
  <action>
Modificar `apps/website/components/Header.tsx` para agregar el trigger del mega menú en el link "Noticias":

**1. Agregar import del componente y el estado:**

Al inicio del archivo agregar:
```typescript
import { NewsMegaMenu } from "./NewsMegaMenu";
```

Dentro del cuerpo del componente `Header`, agregar nuevo estado después de los existentes:
```typescript
const [newsMenuOpen, setNewsMenuOpen] = useState(false);
```

**2. Reemplazar el botón "Noticias" simple:**

Buscar (líneas ~141-145):
```tsx
<button
  className={active === "news" ? "active" : ""}
  onClick={() => router.push("/noticias")}
>
  Noticias
</button>
```

Reemplazar por un wrapper con hover que activa el mega menú:
```tsx
<div
  style={{ position: "relative" }}
  onMouseEnter={() => setNewsMenuOpen(true)}
>
  <button
    className={active === "news" ? "active" : ""}
    onClick={() => router.push("/noticias")}
  >
    Noticias
    <svg
      width="12"
      height="12"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2.5"
      strokeLinecap="round"
      style={{ marginLeft: 4, opacity: 0.6, transform: newsMenuOpen ? "rotate(180deg)" : "none", transition: "transform .2s" }}
    >
      <polyline points="6 9 12 15 18 9" />
    </svg>
  </button>
</div>
```

**3. Renderizar el mega menú fuera del `<header>` pero dentro del componente:**

Inmediatamente antes del cierre `</header>` (o dentro del componente justo antes del return del overlay mobile), agregar:
```tsx
{newsMenuOpen && (
  <NewsMegaMenu onClose={() => setNewsMenuOpen(false)} />
)}
```

**Nota importante:** El componente `Header` ya es `"use client"`, así que se puede usar `useState` sin cambios en la directiva. El `NewsMegaMenu` se renderiza fuera del `<header>` HTML pero dentro del componente React, lo que es correcto para que el `position: fixed` del `.mega-bg` funcione.

**4. Cerrar el menú al hacer clic en cualquier otro elemento del nav:**

Agregar `onClick={() => setNewsMenuOpen(false)}` a los botones existentes de categorías de eventos (las que están en `topCats.map`) para que el mega menú se cierre si el usuario navega a otro lado. Esto es opcional pero mejora UX — implementar si es sencillo sin introducir bugs.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && grep -q "NewsMegaMenu" apps/website/components/Header.tsx && grep -q "newsMenuOpen" apps/website/components/Header.tsx && echo "OK"</automated>
  </verify>
  <done>
  - Header.tsx importa NewsMegaMenu
  - El botón "Noticias" tiene onMouseEnter que abre el mega menú
  - El mega menú se renderiza condicionalmente con `{newsMenuOpen && <NewsMegaMenu ... />}`
  - TypeScript no reporta errores en Header.tsx
  </done>
</task>

</tasks>

<verification>
1. `grep -q "NewsMegaMenu" apps/website/components/Header.tsx` → exit 0
2. `grep -q "onMouseEnter" apps/website/components/Header.tsx` → exit 0
3. `ls apps/website/components/NewsMegaMenu.tsx` → existe
4. `npx tsc --noEmit --project apps/website/tsconfig.json 2>&1 | grep error | wc -l` → 0
</verification>

<success_criteria>
- Hovear sobre "Noticias" en el navbar renderiza el mega menú con layout .mega-aside (izquierda) + .mega-flat (derecha)
- Mover el mouse fuera del mega menú lo cierra (onMouseLeave en .mega-bg)
- Click en una categoría navega a /noticias/categoria/{slug}
- Click en "Ver todas las noticias" navega a /noticias
- En mobile (max-width: 640px), el mega menú está oculto por CSS
- No hay errores TypeScript
</success_criteria>

<output>
Crear `.planning/phases/19-rediseno-completo-de-noticias/19-02-SUMMARY.md` con:
- Cómo se integró el trigger (onMouseEnter vs hover)
- Nota sobre si fue necesario ajustar el z-index del header vs el mega menú
- Estado del token en useUser (si el plan 01 lo documentó)
</output>
