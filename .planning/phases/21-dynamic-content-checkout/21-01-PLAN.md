---
phase: 21-dynamic-content-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/settings/settings.service.ts
  - apps/api/src/stats/stats.service.ts
  - apps/api/src/stats/stats.controller.ts
  - apps/website/lib/api.ts
  - apps/website/lib/site.ts
  - apps/website/.env
  - apps/website/.env.example
autonomous: true
requirements: [DYN-01, DYN-02, DYN-05, DYN-06, PAY-01]
must_haves:
  truths:
    - "GET /settings/public returns SPOT_*, HERO_*, EVENT_* and SUBSCRIPTION_* keys without auth"
    - "GET /stats/public returns real approved-event and organizer counts without auth"
    - "Frontend lib/api.ts exposes settingsPublic, statsPublic, ordersDraft, addOrderItem, removeOrderItem, getOrder and checkout methods"
    - "Social links and contact emails are available as NEXT_PUBLIC_* env vars surfaced through lib/site.ts"
  artifacts:
    - path: "apps/api/src/settings/settings.service.ts"
      provides: "getPublic() exposing SPOT_/HERO_/EVENT_/SUBSCRIPTION_ prefixes"
      contains: "PUBLIC_PREFIXES"
    - path: "apps/api/src/stats/stats.controller.ts"
      provides: "Public GET /stats/public endpoint (no guard)"
      contains: "stats/public"
    - path: "apps/website/lib/api.ts"
      provides: "orders/payments/settings/stats client methods"
      contains: "settingsPublic"
    - path: "apps/website/lib/site.ts"
      provides: "SOCIAL_* and CONTACT_* exports from NEXT_PUBLIC env vars"
      contains: "INSTAGRAM_URL"
  key_links:
    - from: "apps/website/lib/api.ts"
      to: "/settings/public"
      via: "request('/settings/public')"
      pattern: "settings/public"
    - from: "apps/api/src/stats/stats.controller.ts"
      to: "StatsService.getPublicStats"
      via: "controller method without UseGuards"
      pattern: "getPublicStats|stats/public"
---

<objective>
Lay the data foundation for the whole phase: expose the public settings/stats the
frontend needs, surface social/contact config as env vars, and add every
orders/payments/settings/stats method to `lib/api.ts` in one place so the parallel
wave-2/3 plans never collide on `api.ts`.

Purpose: All later plans consume real prices, quotas, stats, cart and payment
endpoints. Centralizing the client surface here (the Phase 20-02 pattern) prevents
merge conflicts across parallel plans.
Output: Extended `/settings/public`, new `/stats/public`, complete `api.ts` client
surface, and `NEXT_PUBLIC_*` env vars wired through `lib/site.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# RESEARCH-VS-REALITY — read before coding:
# - `GET /settings/public` ALREADY EXISTS at apps/api/src/settings/settings.controller.ts:16.
#   This plan EXTENDS it (adds prefixes); it does NOT create a new controller.
# - `GET /stats` ALREADY EXISTS at apps/api/src/stats/stats.controller.ts but is ADMIN-ONLY.
#   This plan ADDS a separate public method/route `GET /stats/public` (no guard). Do NOT
#   change the existing admin endpoint and do NOT merge counts into /settings/public.
# - Settings keys are seeded in apps/api/prisma/seed.ts (~line 920+). Confirmed keys:
#   SPOT_PRICE_PER_DAY, SPOT_MAX_ACTIVE, SPOT_MIN_DAYS, SPOT_MAX_DAYS,
#   HERO_PRICE_PER_DAY, HERO_MAX_ACTIVE, HERO_MAX_DAYS, EVENT_MAX_DAYS, ARTICLE_PRICE,
#   SUBSCRIPTION_PRICE (currently 9990, NOT 29990 — that's why we make it dynamic),
#   SUBSCRIPTION_CREDITS (10), SUBSCRIPTION_SPOT_DISCOUNT (20), SUBSCRIPTION_HERO_DISCOUNT (20).
#   There is NO EVENT_PRICE_PER_DAY key (events price from category.pricePerDay) and NO
#   SUBSCRIPTION_CREDIT_DAYS key. Do NOT assume keys exist — getPublic() returns whatever
#   matches the prefixes, so consumers must tolerate missing keys with fallbacks.

@apps/api/src/settings/settings.service.ts
@apps/api/src/settings/settings.controller.ts
@apps/api/src/stats/stats.service.ts
@apps/api/src/stats/stats.controller.ts
@apps/website/lib/site.ts

<interfaces>
<!-- Client transport (apps/website/lib/api.ts). Reuse these — do not re-invent. -->
- `request<T>(path, options?, token?)` — base helper. Pass user JWT as `token` for
  authenticated calls; it sets `Authorization: Bearer`. The Next proxy at
  apps/website/app/api/[...path]/route.ts forwards Authorization and adds X-API-Key.
- `api` object is the single export consumers use. Add new methods to it.

<!-- Order shape returned by GET /orders/draft and PUT /orders/:id/items
     (from apps/api/src/orders/orders.service.ts ITEM_INCLUDE). The Order has:
     { id, userId, orgId, status, total, gateway, externalId, items: OrderItem[] }
     Each OrderItem: { id, type: 'EVENT'|'SPOT'|'HERO'|'ARTICLE'|'SUBSCRIPTION',
       days, unitPrice, subtotal, eventId, spotId, heroId, articleId,
       event?: {...with eventCategory}, spot?, hero?: {...with eventCategory}, article? } -->

<!-- Checkout: POST /payments/:orderId/checkout body { gateway: 'TRANSBANK' }
     returns { redirectUrl: string, externalId: string } (JwtAuthGuard required). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend public settings prefixes</name>
  <files>apps/api/src/settings/settings.service.ts</files>
  <action>
    In settings.service.ts, change the `PUBLIC_PREFIXES` constant from
    `['SPOT_', 'HERO_'] as const` to `['SPOT_', 'HERO_', 'EVENT_', 'SUBSCRIPTION_'] as const`.
    Do NOT use a blanket whitelist — only these four prefixes. ARTICLE_PRICE and any
    future admin-only keys must stay private. `getPublic()` already iterates and filters
    by prefix, so no other change is needed in this file.

    Implements DYN-01 (real prices/quotas exposed publicly).
  </action>
  <verify>
    <automated>cd apps/api && curl -s http://localhost:3333/api/settings/public | grep -o "SUBSCRIPTION_PRICE" && curl -s http://localhost:3333/api/settings/public | grep -o "EVENT_MAX_DAYS"</automated>
  </verify>
  <done>GET /api/settings/public returns SPOT_*, HERO_*, EVENT_* and SUBSCRIPTION_* keys; ARTICLE_PRICE is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add public stats endpoint</name>
  <files>apps/api/src/stats/stats.service.ts, apps/api/src/stats/stats.controller.ts</files>
  <action>
    Add a NEW method `getPublicStats()` to StatsService (keep the existing admin
    `getStats()` untouched). It runs a small `$transaction` of two counts:
    - approvedEvents: `prisma.event.count({ where: { status: PublicationStatus.APPROVED } })`
    - organizers: `prisma.user.count({ where: { type: 'ORGANIZATION' } })`
      (import the enum value the schema uses; User.type is the org/person discriminator
      referenced in OrdersService.getOrCreateDraft — match that exact literal/enum).
    Return `{ approvedEvents, organizers }`. Use PublicationStatus already imported.

    In stats.controller.ts add a public route ABOVE the existing admin `@Get()` (mirror
    the SettingsController pattern where 'public' is declared with NO `@UseGuards`):
      @Get('public')
      @ApiOperation({ summary: 'KPIs públicos (sin auth)' })
      getPublic() { return this.stats.getPublicStats(); }
    Do NOT add JwtAuthGuard/RolesGuard to this route. Note the 244K Instagram follower
    figure is brand content, NOT a DB count — it stays in an env var (see Task 4), not here.

    Implements DYN-02 (about-page stats from real DB counts).
  </action>
  <verify>
    <automated>cd apps/api && curl -s http://localhost:3333/api/stats/public | grep -o "approvedEvents" && curl -s -o /dev/null -w "%{http_code}" http://localhost:3333/api/stats | grep -q "401" && echo "admin-still-guarded"</automated>
  </verify>
  <done>GET /api/stats/public returns {approvedEvents, organizers} without auth; GET /api/stats still requires auth (401 unauthenticated).</done>
</task>

<task type="auto">
  <name>Task 3: Add orders/payments/settings/stats methods to lib/api.ts</name>
  <files>apps/website/lib/api.ts</files>
  <action>
    Add these methods to the exported `api` object (place near spots/heroes methods).
    Follow the existing style (arrow functions calling `request<T>`):

    Settings/Stats (no token):
      settingsPublic: () => request&lt;Record&lt;string, string&gt;&gt;("/settings/public"),
      statsPublic:    () => request&lt;{ approvedEvents: number; organizers: number }&gt;("/stats/public"),

    Orders (require token — pass user JWT through):
      ordersDraft:    (token: string) => request&lt;ApiOrder&gt;("/orders/draft", {}, token),
      getOrder:       (id: number, token: string) => request&lt;ApiOrder&gt;(`/orders/${id}`, {}, token),
      addOrderItem:   (orderId: number, body: AddOrderItemInput, token: string) =>
        request&lt;ApiOrder&gt;(`/orders/${orderId}/items`, { method: "PUT", body: JSON.stringify(body) }, token),
      removeOrderItem:(orderId: number, type: OrderItemKind, token: string) =>
        request&lt;ApiOrder&gt;(`/orders/${orderId}/items/${type}`, { method: "DELETE" }, token),

    Payments (require token):
      checkout:       (orderId: number, gateway: "TRANSBANK", token: string) =>
        request&lt;{ redirectUrl: string; externalId: string }&gt;(`/payments/${orderId}/checkout`, { method: "POST", body: JSON.stringify({ gateway }) }, token),

    Define and export the supporting types ABOVE the `api` object (near ApiSpot/ApiHero):
      export type OrderItemKind = "EVENT" | "SPOT" | "HERO" | "ARTICLE" | "SUBSCRIPTION";
      export type ApiOrderItem = {
        id: number; type: OrderItemKind; days: number; unitPrice: number; subtotal: number;
        eventId: number | null; spotId: number | null; heroId: number | null; articleId: number | null;
        event?: { id: number; title: string; slug: string; poster: string | null; banner: string | null; eventCategory: ApiEventCategory | null } | null;
        spot?: { id: number; title: string; image: string | null } | null;
        hero?: { id: number; title: string; image: string; eventCategory: ApiEventCategory | null } | null;
        article?: { id: number; title: string } | null;
      };
      export type ApiOrder = {
        id: number; userId: number; orgId: number | null;
        status: "DRAFT" | "PENDING_PAYMENT" | "PAID" | "FAILED";
        total: number; gateway: string | null; externalId: string | null;
        items: ApiOrderItem[];
      };
      export type AddOrderItemInput = {
        type: OrderItemKind; days?: number;
        eventId?: number; spotId?: number; heroId?: number; articleId?: number;
      };

    Do NOT remove or rename any existing api.ts method. These are additive.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -v "node_modules" | (! grep -i "lib/api.ts")</automated>
  </verify>
  <done>lib/api.ts exports ApiOrder/ApiOrderItem/AddOrderItemInput/OrderItemKind and api.{settingsPublic,statsPublic,ordersDraft,getOrder,addOrderItem,removeOrderItem,checkout}; typecheck passes for api.ts.</done>
</task>

<task type="auto">
  <name>Task 4: Add NEXT_PUBLIC social/contact env vars and surface via lib/site.ts</name>
  <files>apps/website/.env, apps/website/.env.example, apps/website/lib/site.ts</files>
  <action>
    Append to apps/website/.env (gitignored) AND document the same keys (without secret
    values) in apps/website/.env.example:
      NEXT_PUBLIC_INSTAGRAM_URL=https://instagram.com/konbinishop.cl
      NEXT_PUBLIC_TIKTOK_URL=https://tiktok.com/@konbinishop.cl
      NEXT_PUBLIC_DISCORD_URL=
      NEXT_PUBLIC_FACEBOOK_URL=https://facebook.com/konbinishop
      NEXT_PUBLIC_INSTAGRAM_FOLLOWERS=244K
      NEXT_PUBLIC_INSTAGRAM_HANDLE=@konbinishop.cl
      NEXT_PUBLIC_CONTACT_EMAIL=hola@konbini.cl
      NEXT_PUBLIC_ABUSE_EMAIL=abuso@konbini.cl
      NEXT_PUBLIC_PRIVACY_EMAIL=privacidad@konbini.cl

    In apps/website/lib/site.ts, export each with a sensible fallback (so a missing env
    var never breaks render), e.g.:
      export const INSTAGRAM_URL = process.env.NEXT_PUBLIC_INSTAGRAM_URL ?? "https://instagram.com/konbinishop.cl";
      export const TIKTOK_URL = process.env.NEXT_PUBLIC_TIKTOK_URL ?? "https://tiktok.com/@konbinishop.cl";
      export const DISCORD_URL = process.env.NEXT_PUBLIC_DISCORD_URL ?? "";
      export const FACEBOOK_URL = process.env.NEXT_PUBLIC_FACEBOOK_URL ?? "https://facebook.com/konbinishop";
      export const INSTAGRAM_FOLLOWERS = process.env.NEXT_PUBLIC_INSTAGRAM_FOLLOWERS ?? "244K";
      export const INSTAGRAM_HANDLE = process.env.NEXT_PUBLIC_INSTAGRAM_HANDLE ?? "@konbinishop.cl";
      export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? "hola@konbini.cl";
      export const ABUSE_EMAIL = process.env.NEXT_PUBLIC_ABUSE_EMAIL ?? "abuso@konbini.cl";
      export const PRIVACY_EMAIL = process.env.NEXT_PUBLIC_PRIVACY_EMAIL ?? "privacidad@konbini.cl";
    Keep existing SITE_URL / SITE_HOST exports intact.

    Implements DYN-05 (social links via env) and DYN-06 (contact emails via env).
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; grep -q "INSTAGRAM_URL" lib/site.ts &amp;&amp; grep -q "CONTACT_EMAIL" lib/site.ts &amp;&amp; grep -q "NEXT_PUBLIC_INSTAGRAM_URL" .env.example</automated>
  </verify>
  <done>lib/site.ts exports all social/contact constants with fallbacks; .env and .env.example document the NEXT_PUBLIC_* keys.</done>
</task>

</tasks>

<verification>
- `curl /api/settings/public` includes SUBSCRIPTION_PRICE and EVENT_MAX_DAYS; excludes ARTICLE_PRICE.
- `curl /api/stats/public` returns {approvedEvents, organizers}; `curl /api/stats` is still 401 without token.
- `pnpm exec tsc --noEmit` in apps/website reports no errors in lib/api.ts or lib/site.ts.
- `apps/api && pnpm build` (or `pnpm exec tsc --noEmit -p tsconfig.build.json`) compiles.
</verification>

<success_criteria>
Public settings now include event/subscription pricing; a public stats endpoint exists;
the frontend client has every orders/payments/settings/stats method it needs; and social
+ contact config is available via env-backed constants. No existing endpoint or api.ts
method changed behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dynamic-content-checkout/21-01-SUMMARY.md`
documenting: final PUBLIC_PREFIXES value, the public stats shape, the exact api.ts method
signatures and type exports added, and the env var keys introduced.
</output>
