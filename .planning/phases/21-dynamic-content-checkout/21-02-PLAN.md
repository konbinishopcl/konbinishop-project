---
phase: 21-dynamic-content-checkout
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/website/app/(site)/precios/page.tsx
  - apps/website/app/(site)/precios/PricingView.tsx
  - apps/website/app/(site)/HomeView.tsx
  - apps/website/app/(site)/upgrade/page.tsx
  - apps/website/app/(site)/cuenta/suscripcion/page.tsx
autonomous: true
requirements: [DYN-01, DYN-03]
must_haves:
  truths:
    - "Pricing page shows real prices from /settings/public (event base, subscription price, spot/hero per-day)"
    - "Home page placement/pricing copy reflects real settings values, not hardcoded $4.990 / 12 cupos / +500"
    - "Upgrade and subscription pages show real subscription price, credits and discounts"
    - "If a settings key is missing the page renders with a sensible fallback (never blank/NaN)"
  artifacts:
    - path: "apps/website/app/(site)/precios/page.tsx"
      provides: "Async server component fetching settings + stats, passing as props"
      contains: "settingsPublic"
    - path: "apps/website/app/(site)/precios/PricingView.tsx"
      provides: "Pricing cards rendering values from props (no hardcoded prices)"
    - path: "apps/website/app/(site)/upgrade/page.tsx"
      provides: "Upgrade page with dynamic subscription price/credits/discount"
  key_links:
    - from: "apps/website/app/(site)/precios/page.tsx"
      to: "/settings/public"
      via: "api.settingsPublic() server-side"
      pattern: "settingsPublic"
    - from: "apps/website/app/(site)/precios/PricingView.tsx"
      to: "props from page.tsx"
      via: "PricingView receives settings prop"
      pattern: "settings"
---

<objective>
Remove every hardcoded price/quota from the public pricing surfaces (pricing page, home
placement section, upgrade, subscription) and drive them from `/settings/public`.

Purpose: DYN-01/DYN-03 — prices must reflect the live Settings table (note: seeded
SUBSCRIPTION_PRICE is 9990, not the design's 29.990 — exactly why hardcoding is wrong).
Output: Pricing surfaces that read real values, with SSR where the page is a server
component (per the SSR-first project memory) and client fetch where the page is already
a client component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# DESIGN: never invent UI. Pricing layout = design/Konbini.html .price-hero/.price-grid
# (lines ~3998-4060). The 3-card structure and FAQ accordion already match in PricingView;
# only the numbers change. Read the design block before editing copy.
# SSR-FIRST (project memory): precios/page.tsx and HomeView's host page are server
# components → fetch settings there and pass as props. upgrade/page.tsx is currently a
# server-ish page and cuenta/suscripcion/page.tsx is "use client" with useUser →
# client fetch via api.settingsPublic() in those.

@apps/website/app/(site)/precios/PricingView.tsx
@apps/website/app/(site)/precios/page.tsx
@apps/website/app/(site)/HomeView.tsx
@apps/website/app/(site)/upgrade/page.tsx
@apps/website/app/(site)/cuenta/suscripcion/page.tsx
@apps/website/lib/api.ts

<interfaces>
<!-- From 21-01 (lib/api.ts) -->
- api.settingsPublic() => Promise&lt;Record&lt;string, string&gt;&gt;  // values are STRINGS — parseInt before math
- api.statsPublic() => Promise&lt;{ approvedEvents: number; organizers: number }&gt;
<!-- Keys that exist (tolerate missing — use ?? fallback):
  SPOT_PRICE_PER_DAY, HERO_PRICE_PER_DAY, SPOT_MIN_DAYS, SPOT_MAX_DAYS, HERO_MAX_DAYS,
  SPOT_MAX_ACTIVE, HERO_MAX_ACTIVE, EVENT_MAX_DAYS, SUBSCRIPTION_PRICE,
  SUBSCRIPTION_CREDITS, SUBSCRIPTION_SPOT_DISCOUNT, SUBSCRIPTION_HERO_DISCOUNT.
  There is NO EVENT_PRICE_PER_DAY — events price from category.pricePerDay. For the
  "Publicación de evento — desde $X / día" card, fetch api.eventCategories() and use the
  min pricePerDay across categories (fallback 4990 if empty). NO SUBSCRIPTION_CREDIT_DAYS
  key — keep "45 días" as literal copy (it is editorial, not a setting). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSR pricing page + dynamic PricingView</name>
  <files>apps/website/app/(site)/precios/page.tsx, apps/website/app/(site)/precios/PricingView.tsx</files>
  <action>
    Convert precios/page.tsx to an async server component that fetches in parallel:
    `const [settings, categories] = await Promise.all([api.settingsPublic(), api.eventCategories()]);`
    Wrap in try/catch; on failure pass `settings={}` and `categories=[]` so PricingView
    falls back. Pass `settings` and a derived `eventMinPrice` (min of categories[].pricePerDay,
    fallback 4990) as props to &lt;PricingView /&gt;.

    In PricingView.tsx, accept `{ settings, eventMinPrice }: { settings: Record<string,string>; eventMinPrice: number }`.
    Add a small helper `const n = (k: string, fb: number) => parseInt(settings[k] ?? "", 10) || fb;`
    and a `formatCLP` (reuse the es-CL pattern already in the codebase). Replace the hardcoded
    numbers, keeping the EXACT design markup/classes:
    - Card 1 "Publicación de evento": `$desde {formatCLP(eventMinPrice)}` (design shows "$4.990 / día").
      Keep the list item "Publicación de 10 a {n('EVENT_MAX_DAYS',60)} días".
    - Card 2 featured "Suscripción mensual": `{formatCLP(n('SUBSCRIPTION_PRICE',9990))} CLP / mes`;
      list: "&lt;strong&gt;{n('SUBSCRIPTION_CREDITS',10)} créditos&lt;/strong&gt; de publicación al mes",
      "Cada crédito = 45 días de publicación" (literal — no setting),
      "&lt;strong&gt;{n('SUBSCRIPTION_SPOT_DISCOUNT',20)}% off&lt;/strong&gt; en avisos y portadas".
    - Card 3 "Avisos y portadas": `desde {formatCLP(n('SPOT_PRICE_PER_DAY',8000))}`;
      list: "Avisos: {n('SPOT_MAX_ACTIVE',12)} cupos globales", "Portadas: {n('HERO_MAX_ACTIVE',5)} cupos en home",
      "Mínimo {n('SPOT_MIN_DAYS',10)} días, máximo {n('SPOT_MAX_DAYS',30)}".
    Leave the FAQ accordion copy as-is (editorial). Do NOT add "use client" to PricingView
    unless it already has it — it can stay a server-rendered component receiving props.

    Implements DYN-01, DYN-03.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep -E "precios/(page|PricingView)") &amp;&amp; (! grep -E "4\.990|29\.990|\\\$8\.000|\\\$15\.000" "app/(site)/precios/PricingView.tsx")</automated>
  </verify>
  <done>Pricing page is async, fetches settings+categories; PricingView renders all prices from props; no hardcoded price strings remain in PricingView.tsx; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic HomeView placement/pricing copy</name>
  <files>apps/website/app/(site)/HomeView.tsx</files>
  <action>
    HomeView's hardcoded copy: "12 cupos · placement global..." (~line 43),
    "Únete a +500 organizadores" (~line 194), "Publica desde $4.990 / día..." (~line 196).

    HomeView is rendered by a server page. Add a `settings?: Record<string,string>` and
    `stats?: { approvedEvents: number; organizers: number }` prop (optional, defaulting to
    {} / undefined). Update HomeView's host page.tsx (apps/website/app/(site)/page.tsx) to
    fetch `api.settingsPublic()` and `api.statsPublic()` in its existing async data load
    (Promise.all, try/catch fallback) and pass them down. If the host page is NOT async,
    keep HomeView copy resilient: read via the same `n()` helper with fallbacks.

    Replace:
    - "12 cupos · placement global..." → "{n('SPOT_MAX_ACTIVE',12)} cupos · placement global en home y categorías."
    - "Únete a +500 organizadores" → "Únete a +{stats?.organizers ?? 500} organizadores"
    - "Publica desde $4.990 / día..." → "Publica desde {formatCLP(eventMinPrice ?? 4990)} / día o suscríbete y publica hasta {n('SUBSCRIPTION_CREDITS',10)} eventos al mes."
    Preserve all surrounding markup/classes exactly. If wiring stats through the home page
    is more than a one-line addition, it's acceptable to fetch only settings and keep the
    "+500" as a fallback constant — document that choice in the SUMMARY.

    Implements DYN-01, DYN-03.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "HomeView") &amp;&amp; (! grep -E "12 cupos|\\+500 organizadores|desde \\\$4\.990" "app/(site)/HomeView.tsx")</automated>
  </verify>
  <done>Home placement/pricing copy reflects settings (and stats where wired); no hardcoded "12 cupos", "+500 organizadores", "$4.990" strings remain; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic upgrade + subscription pages</name>
  <files>apps/website/app/(site)/upgrade/page.tsx, apps/website/app/(site)/cuenta/suscripcion/page.tsx</files>
  <action>
    upgrade/page.tsx hardcodes: "10 créditos al mes" / "45 días" (line ~5), "20% descuento"
    (line ~6), "Suscribirme por $29.990/mes" (line ~53). cuenta/suscripcion/page.tsx is
    "use client" and references credits/discount copy too.

    For upgrade/page.tsx: if it is a server component, make it async and
    `const s = await api.settingsPublic().catch(() => ({}))`. If client, add a useEffect +
    api.settingsPublic() and a settings state (default {}). Use the `n()` helper +
    `formatCLP`. Replace:
    - "10 créditos al mes" → "{n('SUBSCRIPTION_CREDITS',10)} créditos al mes"
    - "20% descuento" → "{n('SUBSCRIPTION_SPOT_DISCOUNT',20)}% descuento"
    - "Suscribirme por $29.990/mes" → "Suscribirme por {formatCLP(n('SUBSCRIPTION_PRICE',9990))}/mes"
    - "45 días" → keep literal (no setting).

    For cuenta/suscripcion/page.tsx (already client + useUser): add
    `api.settingsPublic()` to the existing data load (it already does a fetch in useEffect),
    store settings in state, and replace any hardcoded "10 créditos"/"20%"/price copy with
    the `n()` helper. Keep the live sub data (creditsUsed/creditsTotal from /subscriptions/me)
    rendering exactly as before — only the static price/discount copy becomes dynamic.

    Preserve all markup/classes. Implements DYN-03.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep -E "upgrade/page|suscripcion/page") &amp;&amp; (! grep -E "29\.990|29990" "app/(site)/upgrade/page.tsx")</automated>
  </verify>
  <done>Upgrade + subscription pages show subscription price, credits and discount from settings; no hardcoded $29.990 remains in upgrade page; typecheck passes.</done>
</task>

</tasks>

<verification>
- Visit /precios — three cards show event min price, subscription price (9990, not 29990), spot/hero per-day from DB.
- Visit / — placement section shows real cupo count; pricing copy uses real values.
- Visit /upgrade — "Suscribirme por $9.990/mes" (matches seeded value) and dynamic credits/discount.
- `pnpm exec tsc --noEmit` clean for all touched files. grep finds no hardcoded $4.990/$29.990/$8.000/$15.000 in the touched views.
</verification>

<success_criteria>
All public pricing surfaces render from /settings/public with graceful fallbacks; design
markup preserved exactly; no hardcoded prices/quotas remain in PricingView, HomeView,
upgrade, or subscription pages.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dynamic-content-checkout/21-02-SUMMARY.md`
noting which pages became SSR vs client-fetch, the `n()`/`formatCLP` helper location, and
whether home stats were wired or left as fallback.
</output>
