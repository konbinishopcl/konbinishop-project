---
phase: 21-dynamic-content-checkout
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/website/app/(site)/crear-producto/[kind]/CreateProductView.tsx
  - apps/website/app/(site)/upsell/UpsellView.tsx
autonomous: true
requirements: [DYN-01, DYN-04]
must_haves:
  truths:
    - "Create-product spot/hero forms show per-day price and quota from real data, not $8.000/$15.000/9-of-12"
    - "Upsell spot/hero steps show per-day price and quota from real data"
    - "Day-range total in the form recalculates with the live per-day price"
    - "The working create-then-add-to-cart order logic is unchanged"
  artifacts:
    - path: "apps/website/app/(site)/crear-producto/[kind]/CreateProductView.tsx"
      provides: "Spot/hero forms with dynamic pricePerDay + quota text"
      contains: "spotsQuota"
    - path: "apps/website/app/(site)/upsell/UpsellView.tsx"
      provides: "Upsell steps with dynamic price + quota"
  key_links:
    - from: "apps/website/app/(site)/crear-producto/[kind]/CreateProductView.tsx"
      to: "/spots/quota and /heroes/quota"
      via: "api.spotsQuota() / api.heroesQuota() in useEffect"
      pattern: "spotsQuota|heroesQuota"
---

<objective>
Remove the last hardcoded prices and "Cupo: 9 / 12" / "Cupo: 3 / 5" quota strings from the
two product-creation surfaces (CreateProductView and UpsellView), driving them from the
real quota endpoints and settings. Do not touch the working order/cart logic.

Purpose: DYN-01/DYN-04 — forms must show real per-day price and real live quota.
Output: Spot/hero forms whose price and quota text are dynamic, with the day-total math
using the live per-day price.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# DESIGN: never invent UI. Form layout = design/Konbini.html SpotForm/HeroForm +
# CreateProductPage (lines ~4972-5146) and UpsellPage (lines ~5148-5214). The quota text
# pattern is "Cupo: 9 / 12 ocupados" → render as "{active} / {max} ocupados". The price
# pill is "$8.000 / día" → "{formatCLP(pricePerDay)} / día". Keep all markup/classes.

# IMPORTANT — DO NOT REGRESS WORKING CODE:
# CreateProductView already creates the spot/hero in DRAFT then calls /api/orders/draft +
# PUT /api/orders/:id/items (lines ~138-194). That logic is correct — leave it alone.
# This plan ONLY swaps the hardcoded PRICE constant, the day-total help text, and the
# TITLES quota text. Use api.spotsQuota()/api.heroesQuota() (which return
# { max, active, available, pricePerDay, maxDays }) so price AND live quota come from one call.

@apps/website/app/(site)/crear-producto/[kind]/CreateProductView.tsx
@apps/website/app/(site)/upsell/UpsellView.tsx
@apps/website/lib/api.ts

<interfaces>
<!-- From lib/api.ts (already exists, predates this phase): -->
- api.spotsQuota()  => Promise&lt;ApiQuota&gt;
- api.heroesQuota() => Promise&lt;ApiQuota&gt;
- type ApiQuota = { max: number; active: number; available: number; pricePerDay: number; maxDays: number }
<!-- From 21-01 (optional, if a non-quota price is ever needed): api.settingsPublic() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic price + quota in CreateProductView</name>
  <files>apps/website/app/(site)/crear-producto/[kind]/CreateProductView.tsx</files>
  <action>
    CreateProductView is "use client". Add a quota fetch on mount:
      const [quota, setQuota] = useState&lt;ApiQuota | null&gt;(null);
      useEffect(() => {
        if (kind === "spot") api.spotsQuota().then(setQuota).catch(() => {});
        else if (kind === "hero") api.heroesQuota().then(setQuota).catch(() => {});
      }, [kind]);

    Replace usages of the hardcoded `PRICE` constant (line ~34):
    - Derive `const pricePerDay = kind === "articulo" ? null : (quota?.pricePerDay ?? (kind === "spot" ? 8000 : 15000));`
      Keep `articulo` → null. Remove the `PRICE` const (or repurpose only as the fallback literal).
    - Sidebar "Precio por día" and "Total" already use `pricePerDay`/`total` — they will now
      reflect the dynamic value automatically once `pricePerDay` is derived from quota.
    - Day-range help text in the spot form (line ~285) hardcodes "$8.000 CLP / día →
      ${(days*8000)...}". Change to use `pricePerDay`:
      "{formatCLP(pricePerDay ?? 0)} CLP / día → &lt;strong&gt;{formatCLP((pricePerDay ?? 0) * days)} CLP total&lt;/strong&gt;".
      Do the same for the hero form help text (line ~349, "$15.000...").
    - TITLES quota strings (lines ~17-18): replace the literal "Cupo: 9 / 12." and
      "Cupo: 3 / 5." Since TITLES is module-scope and can't read state, move the quota
      sentence out of TITLES into the rendered `&lt;p&gt;` subtitle: render
      `{sub}{quota ? ` Cupo: ${quota.active} / ${quota.max} ocupados.` : ""}` and strip the
      hardcoded "Cupo: ..." text from the TITLES entries (leave the descriptive prefix).
    Add a local `formatCLP` (es-CL) if not already present. Import ApiQuota type from "@/lib/api".

    Implements DYN-01, DYN-04. Do NOT modify handleSubmit's order/cart logic.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "CreateProductView") &amp;&amp; (! grep -E "Cupo: 9 / 12|Cupo: 3 / 5|days \* 8000|days \* 15000|\\\$8\.000 CLP / día|\\\$15\.000 CLP / día" "app/(site)/crear-producto/[kind]/CreateProductView.tsx")</automated>
  </verify>
  <done>Spot/hero per-day price and total come from quota.pricePerDay; quota text shows live active/max; no hardcoded $8.000/$15.000/"Cupo: 9 / 12" remain; order/cart logic untouched; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic price + quota in UpsellView</name>
  <files>apps/website/app/(site)/upsell/UpsellView.tsx</files>
  <action>
    UpsellView hardcodes "$8.000 CLP / día", "$15.000 CLP / día" and
    "Cupo: 9 / 12 ocupados" / "Cupo: 3 / 5 ocupados" in the spot/hero upsell steps.

    Add quota fetches on mount (both spot and hero, since both steps appear):
      const [spotQuota, setSpotQuota] = useState&lt;ApiQuota | null&gt;(null);
      const [heroQuota, setHeroQuota] = useState&lt;ApiQuota | null&gt;(null);
      useEffect(() => {
        api.spotsQuota().then(setSpotQuota).catch(() => {});
        api.heroesQuota().then(setHeroQuota).catch(() => {});
      }, []);
    Replace the price pill text in the spot step with
    `{formatCLP(spotQuota?.pricePerDay ?? 8000)} / día` and the hero step with
    `{formatCLP(heroQuota?.pricePerDay ?? 15000)} / día`. Replace the quota copy:
    spot step "...Cupo: {spotQuota ? `${spotQuota.active} / ${spotQuota.max}` : "9 / 12"} ocupados."
    hero step likewise with heroQuota (fallback "3 / 5"). Keep min/max day copy
    ("10 días mínimo, 30 máximo") as literal unless trivially derivable from quota.maxDays.
    Preserve all markup/classes from the design. Add a local formatCLP if needed.

    Implements DYN-01, DYN-04. Do NOT change the upsell step-advance or any embedded
    SpotForm/HeroForm submit behavior (owned by Phase 20).
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "UpsellView") &amp;&amp; (! grep -E "\\\$8\.000 CLP / día|\\\$15\.000 CLP / día|Cupo: 9 / 12 ocupados|Cupo: 3 / 5 ocupados" "app/(site)/upsell/UpsellView.tsx")</automated>
  </verify>
  <done>Upsell spot/hero steps show price from quota.pricePerDay and live active/max quota; no hardcoded price/quota strings remain; upsell flow logic untouched; typecheck passes.</done>
</task>

</tasks>

<verification>
- Visit /crear-producto/spot and /crear-producto/hero — per-day price, day-total and quota
  text match the real quota endpoint response; the day slider total recalculates correctly.
- Walk the /upsell flow — spot and hero steps show real price and quota.
- grep finds no hardcoded $8.000/$15.000/"Cupo: 9 / 12"/"Cupo: 3 / 5" in the two views.
- `pnpm exec tsc --noEmit` clean for both files.
</verification>

<success_criteria>
Both product-creation surfaces render per-day price and quota from real data with sensible
fallbacks; design markup preserved; the working create-and-add-to-cart logic is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dynamic-content-checkout/21-03-SUMMARY.md`
noting how TITLES quota text was relocated out of module scope and the formatCLP location.
</output>
