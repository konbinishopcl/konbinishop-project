---
phase: 21-dynamic-content-checkout
plan: 04
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/website/components/Footer.tsx
  - apps/website/app/(site)/contacto/page.tsx
  - apps/website/app/(site)/nosotros/page.tsx
autonomous: true
requirements: [DYN-02, DYN-05, DYN-06]
must_haves:
  truths:
    - "Footer social links come from NEXT_PUBLIC env vars via lib/site.ts"
    - "Contact page emails and Instagram handle come from env vars via lib/site.ts"
    - "About page event/organizer stats come from /stats/public; Instagram followers from env"
    - "Missing env var falls back to a sensible default (no broken link/blank stat)"
  artifacts:
    - path: "apps/website/components/Footer.tsx"
      provides: "Social links from site.ts constants"
      contains: "INSTAGRAM_URL"
    - path: "apps/website/app/(site)/nosotros/page.tsx"
      provides: "Async server component fetching public stats"
      contains: "statsPublic"
  key_links:
    - from: "apps/website/app/(site)/nosotros/page.tsx"
      to: "/stats/public"
      via: "api.statsPublic() server-side"
      pattern: "statsPublic"
    - from: "apps/website/components/Footer.tsx"
      to: "lib/site.ts"
      via: "import { INSTAGRAM_URL, ... }"
      pattern: "from \"@/lib/site\"|from \"../lib/site\""
---

<objective>
Move social links and contact emails out of hardcoded JSX into the `lib/site.ts`
env-backed constants from 21-01, and connect the about-page stats to the real
`/stats/public` counts (Instagram follower count stays env-driven brand content).

Purpose: DYN-02 (real about stats), DYN-05 (social links via env), DYN-06 (contact emails
via env).
Output: Footer, contact, and about pages reading config from env/DB with fallbacks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# DESIGN: never invent UI. Footer = design/Konbini.html footer (5-col grid, .icon-btn
# socials, line ~1959). About = .about-hero/.about-stats/.about-team (lines ~1027-1042).
# The current nosotros stats are a 4-cell grid: [244K+ SEGUIDORES], [1.200+ EVENTOS],
# [300+ ORGANIZADORES], [2024 FUNDACIÓN]. Wire EVENTOS and ORGANIZADORES to /stats/public;
# keep SEGUIDORES from env (INSTAGRAM_FOLLOWERS) and FUNDACIÓN as the literal "2024".
# The team array (TEAM) stays hardcoded in code — it is content, not data.

@apps/website/components/Footer.tsx
@apps/website/app/(site)/contacto/page.tsx
@apps/website/app/(site)/nosotros/page.tsx
@apps/website/lib/site.ts

<interfaces>
<!-- From 21-01 (lib/site.ts) — import what each file needs: -->
- INSTAGRAM_URL, TIKTOK_URL, DISCORD_URL, FACEBOOK_URL (string; DISCORD may be "")
- INSTAGRAM_FOLLOWERS ("244K"), INSTAGRAM_HANDLE ("@konbinishop.cl")
- CONTACT_EMAIL, ABUSE_EMAIL, PRIVACY_EMAIL
<!-- From 21-01 (lib/api.ts): -->
- api.statsPublic() => Promise&lt;{ approvedEvents: number; organizers: number }&gt;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer social links from env</name>
  <files>apps/website/components/Footer.tsx</files>
  <action>
    Import `{ INSTAGRAM_URL, TIKTOK_URL, DISCORD_URL, FACEBOOK_URL }` from "@/lib/site".
    Replace the hardcoded hrefs:
    - Brand-block Instagram icon-btn href "https://instagram.com/konbinishop.cl" → {INSTAGRAM_URL}
    - Brand-block Facebook icon-btn href "https://facebook.com/konbinishop" → {FACEBOOK_URL}
    - Comunidad column: Instagram → {INSTAGRAM_URL}, TikTok → {TIKTOK_URL},
      Discord `href="#"` → {DISCORD_URL || "#"}.
    Keep the Google/Apple icon-btns as-is (placeholder "#"). Preserve all markup/classes,
    target/rel attributes, and the BrandMark. Footer is a server component (no "use client"),
    and these are NEXT_PUBLIC vars so they're readable at build/render time.

    Implements DYN-05.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "Footer") &amp;&amp; grep -q "INSTAGRAM_URL" components/Footer.tsx &amp;&amp; (! grep -E "instagram.com/konbinishop|tiktok.com/@konbinishop|facebook.com/konbinishop" components/Footer.tsx)</automated>
  </verify>
  <done>Footer imports social URLs from lib/site.ts; no hardcoded social domain strings remain; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Contact page emails + handle from env</name>
  <files>apps/website/app/(site)/contacto/page.tsx</files>
  <action>
    Import the needed constants from "@/lib/site" (CONTACT_EMAIL, ABUSE_EMAIL,
    PRIVACY_EMAIL, INSTAGRAM_URL, INSTAGRAM_HANDLE). Replace in the sidebar:
    - `mailto:hola@konbini.cl` + visible "hola@konbini.cl" → {CONTACT_EMAIL} (use it for
      both the href and the visible text).
    - Instagram link href "https://instagram.com/konbinishop.cl" → {INSTAGRAM_URL}; visible
      "@konbinishop.cl" → {INSTAGRAM_HANDLE}.
    - `mailto:abuso@konbini.cl` + visible text → {ABUSE_EMAIL}.
    - If a privacy email appears, use {PRIVACY_EMAIL}.
    Preserve all inline styles, classes, and the Ic.insta icon. If the page is a server
    component this is a direct substitution; if "use client", same (constants are static).

    Implements DYN-06.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "contacto/page") &amp;&amp; grep -q "CONTACT_EMAIL" "app/(site)/contacto/page.tsx" &amp;&amp; (! grep -E "hola@konbini\.cl|abuso@konbini\.cl" "app/(site)/contacto/page.tsx")</automated>
  </verify>
  <done>Contact emails and Instagram handle/link come from site.ts constants; no hardcoded konbini.cl emails remain; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: About page stats from /stats/public</name>
  <files>apps/website/app/(site)/nosotros/page.tsx</files>
  <action>
    Convert nosotros/page.tsx to an async server component (keep the existing
    `export const metadata`). At the top of the default export:
      const stats = await api.statsPublic().catch(() => ({ approvedEvents: 1200, organizers: 300 }));
    Import INSTAGRAM_FOLLOWERS from "@/lib/site". Update the stats grid array (currently
    [["244K+","SEGUIDORES INSTAGRAM"], ["1.200+","EVENTOS PUBLICADOS"], ["300+","ORGANIZADORES"],
    ["2024","AÑO DE FUNDACIÓN"]]) to:
      [
        [INSTAGRAM_FOLLOWERS, "SEGUIDORES INSTAGRAM"],
        [`${stats.approvedEvents.toLocaleString("es-CL")}+`, "EVENTOS PUBLICADOS"],
        [`${stats.organizers.toLocaleString("es-CL")}+`, "ORGANIZADORES"],
        ["2024", "AÑO DE FUNDACIÓN"],
      ]
    Keep the 244.000 mention in the prose paragraph (line ~104) as editorial copy or swap to
    INSTAGRAM_FOLLOWERS if trivial — document the choice. Leave the TEAM array hardcoded.
    Preserve the exact grid markup/classes.

    Implements DYN-02.
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep "nosotros/page") &amp;&amp; grep -q "statsPublic" "app/(site)/nosotros/page.tsx"</automated>
  </verify>
  <done>About page fetches /stats/public server-side; EVENTOS and ORGANIZADORES cells show real counts; SEGUIDORES from env; page renders if stats fetch fails (fallback); typecheck passes.</done>
</task>

</tasks>

<verification>
- Footer renders Instagram/TikTok/Facebook links from env; changing a NEXT_PUBLIC var and
  rebuilding changes the link.
- /contacto shows CONTACT_EMAIL/ABUSE_EMAIL and the Instagram handle from env.
- /nosotros stats grid shows real approved-event and organizer counts (verify against
  `curl /api/stats/public`); followers from env.
- `pnpm exec tsc --noEmit` clean for all three files.
</verification>

<success_criteria>
Footer, contact, and about pages read social/contact config from env and stats from the
public endpoint, all with graceful fallbacks and unchanged design markup.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dynamic-content-checkout/21-04-SUMMARY.md`
noting nosotros became async-server, and whether the prose 244.000 mention was swapped.
</output>
