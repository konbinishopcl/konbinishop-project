---
phase: 21-dynamic-content-checkout
plan: 06
type: execute
wave: 3
depends_on: [21-05]
files_modified:
  - apps/api/src/payments/payments.service.ts
  - apps/website/app/(site)/carrito/CartView.tsx
  - apps/website/app/(site)/carrito/exito/page.tsx
  - apps/website/app/(site)/carrito/error/page.tsx
autonomous: false
requirements: [PAY-05, PAY-06, PAY-07, PAY-08]
must_haves:
  truths:
    - "Pagar button calls POST /payments/:orderId/checkout and navigates the browser to the Transbank redirectUrl"
    - "After a successful sandbox payment the user lands on the success page showing real order details"
    - "After a failed/aborted payment the user lands on the error page with a reason-specific message"
    - "Backend callback redirects land on existing frontend routes (no 404)"
  artifacts:
    - path: "apps/api/src/payments/payments.service.ts"
      provides: "Callback redirects pointing at existing frontend cart routes"
      contains: "/carrito/exito"
    - path: "apps/website/app/(site)/carrito/exito/page.tsx"
      provides: "Success page fetching and showing real order data"
      contains: "getOrder"
    - path: "apps/website/app/(site)/carrito/error/page.tsx"
      provides: "Error page reading reason/code query params"
      contains: "reason"
  key_links:
    - from: "apps/website/app/(site)/carrito/CartView.tsx"
      to: "/payments/:orderId/checkout"
      via: "api.checkout(orderId, 'TRANSBANK', token) then window.location.href = redirectUrl"
      pattern: "api.checkout|redirectUrl"
    - from: "apps/api/src/payments/payments.service.ts"
      to: "frontend cart routes"
      via: "handleTransbankCallback return strings"
      pattern: "/carrito/(exito|error)"
---

<objective>
Wire the complete Transbank WebPay Plus payment flow: the Pagar button initiates checkout
and redirects to the gateway; the backend callback returns the browser to the EXISTING
frontend cart routes; the success and error pages render real, dynamic data.

Purpose: PAY-05/06/07/08 — close the checkout loop end to end on the Transbank sandbox.
Output: A working pay→gateway→confirm→result flow with real order data on the result pages.
This plan is NOT autonomous: the sandbox card entry happens in Transbank's hosted UI and
must be verified by a human (checkpoint task).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# ROUTE MISMATCH — the crux of this plan:
# PaymentsService.handleTransbankCallback (apps/api/src/payments/payments.service.ts)
# currently returns redirect URLs to `${FRONTEND_URL}/checkout/success?...` and
# `${FRONTEND_URL}/checkout/failed?...`. Those routes DO NOT EXIST in the frontend — the
# real pages are /carrito/exito and /carrito/error. We reconcile by changing the BACKEND
# strings (six occurrences → see Task 1). Do NOT create /checkout/* routes.
# FRONTEND_URL is already set in apps/api/.env (http://localhost:3000).

# DESIGN: success page = design/Konbini.html CartSuccessPage (lines ~4913-4953): green check,
# "PAGO CONFIRMADO · {code}" eyebrow, "¡Tu contenido está en revisión!", profile CTA,
# ServicesStrip, satisfaction stars, CTA row. The existing exito/page.tsx ALREADY matches
# this design — only make its data dynamic (real order). The design CartSuccessPage does NOT
# use a .ticket-result table (that belongs to a separate event-ticket checkout, out of scope).
# Error page = CartFailPage (lines ~4955-4970): red X, "El pago falló.", reason text, retry CTAs.

@apps/api/src/payments/payments.service.ts
@apps/website/app/(site)/carrito/CartView.tsx
@apps/website/app/(site)/carrito/exito/page.tsx
@apps/website/app/(site)/carrito/error/page.tsx
@apps/website/lib/api.ts

<interfaces>
<!-- From 21-01 (lib/api.ts): -->
- api.checkout(orderId, "TRANSBANK", token) => Promise&lt;{ redirectUrl: string; externalId: string }&gt;
- api.getOrder(id, token) => Promise&lt;ApiOrder&gt;  // { id, status, total, items[] }
<!-- Backend redirect query params produced by handleTransbankCallback:
     success: /carrito/exito?orderId=ID
     failure: /carrito/error?reason=aborted | reason=not_found | reason=invalid_state | orderId=ID&code=N
     Transbank sandbox test card: VISA 4051 8856 0044 6623, CVV 123, any future expiry,
     RUT 11.111.111-1, password 123 (approves). Card 5186 0595 5959 0568 declines. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point backend callback redirects at existing frontend routes</name>
  <files>apps/api/src/payments/payments.service.ts</files>
  <action>
    In handleTransbankCallback, replace every frontend path so it matches the routes that
    actually exist. Exact substitutions (string-for-string):
    - `${frontendUrl}/checkout/failed?reason=aborted`      → `${frontendUrl}/carrito/error?reason=aborted`
    - `${frontendUrl}/checkout/failed?reason=not_found`    → `${frontendUrl}/carrito/error?reason=not_found`
    - `${frontendUrl}/checkout/success?orderId=${order.id}` (duplicate-paid branch) → `${frontendUrl}/carrito/exito?orderId=${order.id}`
    - `${frontendUrl}/checkout/failed?reason=invalid_state` → `${frontendUrl}/carrito/error?reason=invalid_state`
    - `${frontendUrl}/checkout/failed?orderId=${order.id}&code=${confirmation.responseCode}` → `${frontendUrl}/carrito/error?orderId=${order.id}&code=${confirmation.responseCode}`
    - `${frontendUrl}/checkout/success?orderId=${order.id}` (final success) → `${frontendUrl}/carrito/exito?orderId=${order.id}`
    There are SIX occurrences total — change all of them. Do not touch the activation logic,
    the email send, or order status transitions.

    Implements PAY-06 (callback lands on real routes).
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; (! grep -E "/checkout/(success|failed)" src/payments/payments.service.ts) &amp;&amp; grep -c "/carrito/exito" src/payments/payments.service.ts &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.build.json 2>&amp;1 | (! grep "payments.service")</automated>
  </verify>
  <done>No /checkout/* paths remain in payments.service.ts; callbacks redirect to /carrito/exito and /carrito/error; API compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Pagar button + dynamic success/error pages</name>
  <files>apps/website/app/(site)/carrito/CartView.tsx, apps/website/app/(site)/carrito/exito/page.tsx, apps/website/app/(site)/carrito/error/page.tsx</files>
  <action>
    CartView (handleCheckout): if no order/empty → guard. Set a busy state, then:
      const { redirectUrl } = await api.checkout(order.id, "TRANSBANK", token);
      window.location.href = redirectUrl;   // full navigation to the gateway
    On error toast the message and clear busy. Replace the previous router.push("/carrito/exito")
    no-op. Keep the gateway selector (only WebPay enabled). Button label stays
    "Pagar {formatCLP(order.total)} →".

    exito/page.tsx: it currently reads orderId from searchParams but never fetches the order.
    Make the data dynamic WITHOUT breaking the existing design layout (keep the green check,
    profile CTA, services, satisfaction form, CTA row). Because the page needs the user JWT to
    fetch the order, fetch CLIENT-side: keep "use client", read orderId from searchParams (as
    now), use useUser() for the token, and on mount call api.getOrder(Number(orderId), token)
    → store order. Use it to fill:
    - eyebrow "PAGO CONFIRMADO · {orderId}" (already shows orderId — fine).
    - the paid amount sentence: "Pagaste &lt;strong&gt;{formatCLP(order.total)} CLP&lt;/strong&gt;.
      Un admin revisará tu {N} {N===1?'publicación':'publicaciones'} en menos de 24 horas hábiles."
      where N = order.items.length. If the fetch fails or no token, fall back to the current
      generic copy (do not crash). Leave the rest of the page as-is.

    error/page.tsx: it is currently fully hardcoded. Make it "use client", read
    searchParams { reason, code, orderId } via the same `use(searchParams)` pattern as exito.
    Map reason/code to a message under the "El pago falló." heading:
      - reason==='aborted'       → "Cancelaste el pago antes de completarlo. No se cobró nada."
      - reason==='not_found'     → "No encontramos la transacción. Intenta nuevamente."
      - reason==='invalid_state' → "Esta orden ya fue procesada o no está lista para pago."
      - code present (declined)  → "Transbank rechazó el pago (código {code}). Tus datos están a salvo — no se cobró nada."
      - default                  → keep the current generic Transbank message.
    Preserve the design markup (.thanks-shell, red X icon, retry CTA row pointing to /carrito).

    Implements PAY-05 (initiate + redirect), PAY-07 (success shows real order),
    PAY-08 (error shows reason-specific message).
  </action>
  <verify>
    <automated>cd apps/website &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | (! grep -E "carrito/(CartView|exito/page|error/page)") &amp;&amp; grep -q "api.checkout" "app/(site)/carrito/CartView.tsx" &amp;&amp; grep -q "getOrder" "app/(site)/carrito/exito/page.tsx" &amp;&amp; grep -q "reason" "app/(site)/carrito/error/page.tsx"</automated>
  </verify>
  <done>Pagar calls api.checkout and redirects to redirectUrl; success page fetches and shows real order total + item count; error page renders a reason/code-specific message; typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the end-to-end Transbank sandbox payment</name>
  <action>
    Human checkpoint — Claude cannot enter test-card details in Transbank's hosted sandbox UI.
    The executor must PAUSE here, ask the user to run the success and failure paths described
    in &lt;how-to-verify&gt;, and resume only on the user's signal. Do not mark the plan complete
    until the user confirms both paths.
  </action>
  <what-built>
    Full pay flow: Pagar → POST /payments/:orderId/checkout → Transbank sandbox → callback →
    /carrito/exito (paid) or /carrito/error (declined/aborted), with real order data on the
    result pages. Backend callbacks now redirect to /carrito/* (not /checkout/*).
  </what-built>
  <how-to-verify>
    Prereqs: API running on :3333, website on :3000, logged in as an organizer with at least
    one DRAFT spot/hero in the cart (create via /crear-producto/spot if needed).

    SUCCESS PATH:
    1. Open http://localhost:3000/carrito — confirm items + Total render.
    2. Click "Pagar $… →". The browser should navigate to the Transbank sandbox page.
    3. Enter test card: VISA 4051 8856 0044 6623, any future expiry, CVV 123. On the bank
       page use RUT 11.111.111-1 and password 123 to APPROVE.
    4. Expect to land on http://localhost:3000/carrito/exito?orderId=… showing the green
       check, "PAGO CONFIRMADO · {id}", and "Pagaste $… CLP" with the real total and a correct
       item count. Confirm the order moved to PAID (e.g. it no longer appears as the draft).

    FAILURE PATH:
    5. Repeat with a DRAFT order but on the bank page choose to REJECT/cancel (or use the
       declining card 5186 0595 5959 0568). Expect to land on /carrito/error with a
       reason/code-specific message and retry CTAs.

    Report: did both paths land on the correct /carrito/* pages with correct data? Any 404,
    wrong amount, or stuck state?
  </how-to-verify>
  <resume-signal>Type "approved" if both success and failure paths work, or describe what broke (URL landed on, message shown, amount).</resume-signal>
</task>

</tasks>

<verification>
- grep: no /checkout/* in payments.service.ts; CartView uses api.checkout + window.location.href.
- Success sandbox payment lands on /carrito/exito with real total and item count; order is PAID.
- Declined/aborted payment lands on /carrito/error with the matching message.
- `pnpm exec tsc --noEmit` clean (website) and API build clean.
</verification>

<success_criteria>
The checkout loop works end to end on the Transbank sandbox: initiate → gateway → callback →
correct existing frontend result route with real, dynamic order data. Human checkpoint
confirms both the approved and declined paths.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dynamic-content-checkout/21-06-SUMMARY.md`
documenting the six redirect-string changes, the success-page client-fetch approach, the
error reason→message map, and the checkpoint result.
</output>
