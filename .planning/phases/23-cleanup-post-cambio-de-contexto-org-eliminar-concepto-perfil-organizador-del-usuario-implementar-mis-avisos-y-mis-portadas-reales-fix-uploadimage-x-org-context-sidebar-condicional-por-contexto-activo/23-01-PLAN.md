---
phase: 23-cleanup-post-cambio-de-contexto-org
plan: "01"
type: execute
wave: 1
depends_on: []
subsystem: website-ui
tags: [cleanup, sidebar, organizador, carrito]
files_modified:
  - apps/website/app/(site)/cuenta/organizador/page.tsx
  - apps/website/app/(site)/cuenta/AccountShell.tsx
  - apps/website/app/(site)/carrito/exito/page.tsx
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-06]

must_haves:
  truths:
    - "El tab 'Organizador' ya no aparece en el sidebar de /cuenta"
    - "/cuenta/organizador retorna 404 (directorio eliminado)"
    - "El CTA de carrito/exito enlaza a /cuenta/perfil con copy '¿Tu perfil está completo?'"
  artifacts:
    - path: "apps/website/app/(site)/cuenta/AccountShell.tsx"
      provides: "Sidebar TABS array sin entrada organizador"
      contains: "mis-avisos"
    - path: "apps/website/app/(site)/carrito/exito/page.tsx"
      provides: "CTA corregido a /cuenta/perfil"
      contains: "/cuenta/perfil"
  key_links:
    - from: "apps/website/app/(site)/cuenta/AccountShell.tsx"
      to: "TABS array"
      via: "eliminación de la entrada organizador"
      pattern: "organizador"
    - from: "apps/website/app/(site)/carrito/exito/page.tsx"
      to: "/cuenta/perfil"
      via: "Link href"
      pattern: "/cuenta/perfil"
---

<objective>
Eliminar todos los vestigios de la UI del "perfil de organizador como modo del usuario": borrar la página `/cuenta/organizador`, quitar su tab del sidebar `AccountShell`, y corregir el CTA en la página de éxito del carrito para que apunte al perfil personal.

Purpose: Con el modelo post-Phase-22, las organizaciones son entidades separadas. El usuario opera como org vía `useUser().activeOrg`; no existe "cuenta de organizador" editable desde `/cuenta`. Estos restos generan rutas muertas y CTAs rotos.
Output: Directorio `organizador/` eliminado, sidebar limpio, CTA del carrito corregido.

**Nota sobre requirements:** Los IDs `CLEAN-01..CLEAN-07` son locales a Phase 23 (derivados de los 7 ítems in-scope de 23-CONTEXT.md). No tienen backref en ROADMAP.md (la fase no tenía IDs formales asignados).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-cleanup-post-cambio-de-contexto-org-eliminar-concepto-perfil-organizador-del-usuario-implementar-mis-avisos-y-mis-portadas-reales-fix-uploadimage-x-org-context-sidebar-condicional-por-contexto-activo/23-CONTEXT.md

Decisiones bloqueadas relevantes: 23-CONTEXT.md D-1 (eliminar /cuenta/organizador) y D-2 (carrito/exito CTA).

<interfaces>
<!-- TABS array actual en AccountShell.tsx (líneas 8-20). La entrada a eliminar es la línea 10. -->
TABS = [
  { id: "perfil",         href: "/cuenta/perfil",         label: "Perfil" },
  { id: "organizador",    href: "/cuenta/organizador",    label: "Organizador" },  // ← ELIMINAR
  { id: "organizaciones", href: "/cuenta/organizaciones", label: "Organizaciones" },
  ...resto sin tocar...
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar directorio /cuenta/organizador</name>
  <read_first>
    - apps/website/app/(site)/cuenta/organizador/page.tsx (confirmar que es el único archivo del directorio)
  </read_first>
  <files>apps/website/app/(site)/cuenta/organizador/page.tsx</files>
  <action>
    Eliminar el directorio completo `apps/website/app/(site)/cuenta/organizador/` con `rm -rf "apps/website/app/(site)/cuenta/organizador"`.
    Este directorio contiene un único archivo `page.tsx` que rendea el formulario de "perfil de organizador" y llama a `fetch("/api/users/me/organizer", { method: "PATCH" })` — endpoint que se elimina en el Plan 03.
    No queda ningún otro caller de ese endpoint en el frontend (verificado: grep `me/organizer` solo aparecía en este archivo).
  </action>
  <verify>
    <automated>test ! -d "apps/website/app/(site)/cuenta/organizador" && echo "OK: directorio eliminado"</automated>
  </verify>
  <acceptance_criteria>
    - `ls "apps/website/app/(site)/cuenta/organizador/"` retorna "No such file or directory"
    - `grep -rn "me/organizer" apps/website/` no retorna resultados
  </acceptance_criteria>
  <done>El directorio `cuenta/organizador/` ya no existe; ningún archivo del frontend referencia `/api/users/me/organizer`.</done>
</task>

<task type="auto">
  <name>Task 2: Quitar entrada organizador del TABS array en AccountShell</name>
  <read_first>
    - apps/website/app/(site)/cuenta/AccountShell.tsx (TABS array, líneas 8-20)
  </read_first>
  <files>apps/website/app/(site)/cuenta/AccountShell.tsx</files>
  <action>
    En el array `TABS` de `AccountShell.tsx`, eliminar exactamente esta línea (línea 10):
    `  { id: "organizador",    href: "/cuenta/organizador",    label: "Organizador" },`
    No tocar ninguna otra entrada del array ni el resto del componente. El array debe quedar con 10 entradas (perfil, organizaciones, suscripcion, publicaciones, mis-avisos, mis-portadas, articulos, favoritos, mensajes, pagos).
  </action>
  <verify>
    <automated>! grep -q "organizador" "apps/website/app/(site)/cuenta/AccountShell.tsx" && echo "OK: sin organizador" && grep -c "id:" "apps/website/app/(site)/cuenta/AccountShell.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "organizador" "apps/website/app/(site)/cuenta/AccountShell.tsx"` no retorna nada
    - `grep -c "id:" "apps/website/app/(site)/cuenta/AccountShell.tsx"` retorna 10
    - `grep "mis-avisos\|mis-portadas\|perfil" "apps/website/app/(site)/cuenta/AccountShell.tsx"` confirma que las demás entradas siguen presentes
  </acceptance_criteria>
  <done>El TABS array contiene 10 entradas, ninguna con id u href "organizador".</done>
</task>

<task type="auto">
  <name>Task 3: Corregir CTA de carrito/exito hacia /cuenta/perfil</name>
  <read_first>
    - apps/website/app/(site)/carrito/exito/page.tsx (bloque CTA en líneas ~153-162)
  </read_first>
  <files>apps/website/app/(site)/carrito/exito/page.tsx</files>
  <action>
    Realizar EXCLUSIVAMENTE dos cambios en el bloque CTA alrededor de la línea 153-161:
    1. Cambiar `<h3 ...>¿Tu perfil de organizador está completo?</h3>` → `<h3 ...>¿Tu perfil está completo?</h3>` (conservar los mismos atributos style del h3).
    2. Cambiar `<Link href="/cuenta?tab=org" className="btn dark">` → `<Link href="/cuenta/perfil" className="btn dark">`.
    El texto del botón "Completar mi perfil" se mantiene SIN cambios (per D-2).

    OUT OF SCOPE — NO TOCAR: el `<Link href="/cuenta?tab=eventos">` de la línea ~187 ("Ver mis eventos") NO está en el alcance de esta fase (D-2 solo cubre el CTA de línea 154). Dejarlo exactamente como está.
  </action>
  <verify>
    <automated>grep -q "¿Tu perfil está completo?" "apps/website/app/(site)/carrito/exito/page.tsx" && grep -q 'href="/cuenta/perfil"' "apps/website/app/(site)/carrito/exito/page.tsx" && ! grep -q 'href="/cuenta?tab=org"' "apps/website/app/(site)/carrito/exito/page.tsx" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "¿Tu perfil está completo?" "apps/website/app/(site)/carrito/exito/page.tsx"` encuentra el nuevo h3
    - `grep 'href="/cuenta/perfil"' "apps/website/app/(site)/carrito/exito/page.tsx"` encuentra el nuevo link
    - `grep 'tab=org' "apps/website/app/(site)/carrito/exito/page.tsx"` no retorna nada
    - `grep 'tab=eventos' "apps/website/app/(site)/carrito/exito/page.tsx"` SIGUE retornando 1 resultado (línea 187 intacta, out of scope)
    - `grep "Completar mi perfil" "apps/website/app/(site)/carrito/exito/page.tsx"` sigue presente
  </acceptance_criteria>
  <done>El CTA de perfil enlaza a /cuenta/perfil con copy "¿Tu perfil está completo?"; el link de "Ver mis eventos" permanece sin tocar.</done>
</task>

</tasks>

<verification>
- `test ! -d "apps/website/app/(site)/cuenta/organizador"` — directorio eliminado
- `grep -rn "organizador" "apps/website/app/(site)/cuenta/AccountShell.tsx"` — sin resultados
- `grep -rn "me/organizer" apps/website/` — sin resultados
- `grep 'href="/cuenta/perfil"' "apps/website/app/(site)/carrito/exito/page.tsx"` — encuentra el CTA corregido
- `cd apps/website && npx tsc --noEmit` (o el typecheck del proyecto) compila sin nuevos errores
</verification>

<success_criteria>
- Navegar a /cuenta no muestra el tab "Organizador" en el sidebar
- Navegar a /cuenta/organizador resulta en 404
- En carrito/exito el CTA "Completar mi perfil" lleva a /cuenta/perfil
- No quedan referencias en el frontend a `/api/users/me/organizer`
</success_criteria>

<output>
After completion, create `.planning/phases/23-cleanup-post-cambio-de-contexto-org-eliminar-concepto-perfil-organizador-del-usuario-implementar-mis-avisos-y-mis-portadas-reales-fix-uploadimage-x-org-context-sidebar-condicional-por-contexto-activo/23-01-SUMMARY.md`
</output>
