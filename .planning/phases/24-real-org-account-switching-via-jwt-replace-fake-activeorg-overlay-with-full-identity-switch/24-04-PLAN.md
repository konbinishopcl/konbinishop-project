---
phase: 24-real-org-account-switching-via-jwt
plan: 04
type: execute
wave: 3
depends_on: ["24-03"]
files_modified:
  - apps/website/components/UserMenu.tsx
  - apps/website/app/(site)/cuenta/AccountShell.tsx
  - apps/website/app/(site)/cuenta/perfil/page.tsx
autonomous: false
requirements: [SWITCH-09, SWITCH-10, SWITCH-11]

must_haves:
  truths:
    - "Selecting an org in the UserMenu performs a real switch (navbar avatar + name become the org) and a toast confirms it"
    - "The org list in the UserMenu is always fetched with the personal token (org JWTs have no memberships)"
    - "Switching back to personal restores the personal identity everywhere"
    - "The /cuenta sidebar reflects the current identity (org name + @handle when in org context) using user directly"
    - "In org context the /cuenta/perfil Danger Zone hides 'Cambiar contraseña' but keeps email + delete"
  artifacts:
    - path: "apps/website/components/UserMenu.tsx"
      provides: "real switchToOrg/switchBack wiring, personalToken-based org fetch"
      contains: "switchToOrg"
    - path: "apps/website/app/(site)/cuenta/AccountShell.tsx"
      provides: "isOrgContext-driven display via user directly"
      contains: "isOrgContext"
    - path: "apps/website/app/(site)/cuenta/perfil/page.tsx"
      provides: "Danger Zone hideInOrgContext filter"
      contains: "hideInOrgContext"
  key_links:
    - from: "UserMenu org button onClick"
      to: "providers switchToOrg"
      via: "await switchToOrg(org.id)"
      pattern: "switchToOrg\\(org\\.id\\)"
    - from: "UserMenu org list fetch"
      to: "/api/organizations/mine"
      via: "Authorization: Bearer personalToken ?? token"
      pattern: "personalToken \\?\\? token"
---

<objective>
Wire the UI consumers to the new provider API: rewrite UserMenu.tsx to call real `switchToOrg`/`switchBack` (fetching the org list with the personal token), update AccountShell.tsx to render identity from `user` + `isOrgContext` directly, and add the org-context Danger Zone filter to perfil/page.tsx. End the plan with a human-verify checkpoint that exercises the full switch round-trip.

Purpose: This is the final wave. It depends on Plan 03 (the rewritten providers.tsx context). After this, switching to an org is a real identity switch visible on every /cuenta page with zero page-level special-casing.
Output: rewritten UserMenu, updated AccountShell + perfil, and a verified end-to-end flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-real-org-account-switching-via-jwt-replace-fake-activeorg-overlay-with-full-identity-switch/24-CONTEXT.md

<interfaces>
<!-- After Plan 03, useUser() returns: -->
//   { user, token, ready, isOrgContext, personalUser, personalToken,
//     setAuth, setUser, logout, switchToOrg, switchBack }
//   - OrgEntry type NO LONGER exported from providers (UserMenu must drop that import)
//   - user.type: 'PERSON' | 'ORGANIZATION' | undefined
//   - user.handle: string | null | undefined
//   - user.name / user.initials already resolve to the org's name/initials in org context
//     (backend sets ORGANIZATION.firstname = org name; toUser maps it)

<!-- switchToOrg(orgId): Promise<void> — throws on network/403; caller shows toast -->
<!-- switchBack(): void -->

<!-- /api/organizations/mine returns: { id; firstname?; name?; handle? }[] (mapped to {id,name,handle} in current UserMenu) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite UserMenu.tsx for real switching (SWITCH-09)</name>
  <files>apps/website/components/UserMenu.tsx</files>
  <read_first>
    - apps/website/components/UserMenu.tsx (full file — current activeOrg/setActiveOrg usage throughout, org fetch line 26-34, avatar line 45-52, "Operando como" block line 57-73, "Cambiar de cuenta" block line 75-106)
    - apps/website/components/providers.tsx (after Plan 03 — the new useUser() shape; OrgEntry no longer exported)
    - .planning/phases/.../24-CONTEXT.md (SWITCH-09 section — exact handlers)
  </read_first>
  <action>
    Rewrite UserMenu.tsx to use the new provider API. The visual structure (avatar button, dropdown, "Operando como", "Cambiar de cuenta", nav links, logout) stays the same; only the data source and switch handlers change.

    **1a. Imports.** Change line 16 from `import { useUser, type OrgEntry } from "./providers";` to `import { useUser } from "./providers";` (OrgEntry no longer exported).

    **1b. Destructure** (line 19). Replace `const { user, token, logout, activeOrg, setActiveOrg } = useUser();` with:
    ```typescript
    const { user, token, personalToken, logout, switchToOrg, switchBack, isOrgContext } = useUser();
    ```

    **1c. Org list fetch** (line 26-34). Always fetch with the personal token (an org JWT has no memberships). Change the dependency and the Authorization header:
    ```typescript
    useEffect(() => {
      if (!open) return;
      const authToken = personalToken ?? token;
      if (!authToken) return;
      fetch("/api/organizations/mine", { headers: { Authorization: `Bearer ${authToken}` } })
        .then(r => r.ok ? r.json() : [])
        .then((data: { id: number; firstname?: string | null; name?: string | null; handle?: string | null }[]) =>
          setOrgs(data.map(o => ({ id: o.id, name: o.firstname ?? o.name ?? null, handle: o.handle ?? null })))
        )
        .catch(() => {});
    }, [open, token, personalToken]);
    ```

    **1d. Avatar button** (line 45-52). The user IS the org after switching, so `user.initials` already shows org initials. Replace the `activeOrg`-conditional styling with `isOrgContext`:
    - `style={{ ..., ...(isOrgContext ? { background: "linear-gradient(135deg,#7c3aed,#a855f7)", color: "#fff" } : {}) }}`
    - `title={user.name}`
    - button content: `{user.initials}`

    **1e. "Operando como" block** (line 57-73). Render from `user` directly:
    - avatar circle background: `isOrgContext ? "linear-gradient(135deg,#7c3aed,#a855f7)" : "linear-gradient(135deg, var(--accent), var(--accent-2))"`, content `{user.initials}`
    - name line: `{user.name}`
    - sub line: `{isOrgContext && user.handle ? \`@${user.handle}\` : user.email}`

    **1f. "Cambiar de cuenta" block** (line 75-106). 
    - "Cuenta personal" button: highlighted when `!isOrgContext`. onClick calls switchBack:
      ```typescript
      onClick={() => { switchBack(); close(); toast.success("Operando como cuenta personal"); }}
      ```
      The check mark `<Check />` shows when `!isOrgContext`. The avatar initials here should be the PERSONAL user's initials — but in org context `user.initials` is the org's. Use `personalUser` for the personal-row label: destructure `personalUser` too, and render `{(personalUser ?? user).initials}` and name `{(personalUser ?? user).name}` for the personal row (when not in org context, personalUser is null so falls back to user).
    - Org buttons (`orgs.map`): highlighted when `isOrgContext && user.id === org.id` (the active org user's id equals the org id). onClick performs the real switch with error handling per CONTEXT.md:
      ```typescript
      onClick={async () => {
        try {
          await switchToOrg(org.id);
          close();
          toast.success(`Operando como ${org.name ?? org.handle ?? "organización"}`);
        } catch {
          toast.error("No se pudo cambiar de cuenta");
        }
      }}
      ```
      Check mark shows when `isOrgContext && user.id === org.id`; otherwise show `@handle` as before.
    - Keep the "Crear organización" button (go to /cuenta/organizaciones) unchanged.

    Add `personalUser` to the destructure in 1b: `const { user, token, personalToken, personalUser, logout, switchToOrg, switchBack, isOrgContext } = useUser();`

    The nav links (Mi cuenta, Mis eventos, Suscripción, Favoritos), admin panel link, and logout button are unchanged.
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | head -20; echo "switchToOrg:$(grep -c 'switchToOrg' components/UserMenu.tsx) switchBack:$(grep -c 'switchBack' components/UserMenu.tsx) personalToken:$(grep -c 'personalToken ?? token' components/UserMenu.tsx) activeOrg_should_be_0:$(grep -c 'activeOrg\|setActiveOrg' components/UserMenu.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type OrgEntry" apps/website/components/UserMenu.tsx` returns `0` (import removed)
    - `apps/website/components/UserMenu.tsx` contains `await switchToOrg(org.id)` and `switchBack()`
    - `apps/website/components/UserMenu.tsx` contains `personalToken ?? token` for the org fetch Authorization
    - `apps/website/components/UserMenu.tsx` contains `isOrgContext` for avatar/active-state styling
    - `grep -c "activeOrg\|setActiveOrg" apps/website/components/UserMenu.tsx` returns `0`
    - `cd apps/website && npx tsc --noEmit` reports no new errors in UserMenu.tsx
  </acceptance_criteria>
  <done>The UserMenu lists the user's orgs (fetched with the personal token), switches to an org via switchToOrg with a success/error toast, and switches back via switchBack.</done>
</task>

<task type="auto">
  <name>Task 2: AccountShell identity + perfil Danger Zone conditional (SWITCH-10, SWITCH-11)</name>
  <files>apps/website/app/(site)/cuenta/AccountShell.tsx, apps/website/app/(site)/cuenta/perfil/page.tsx</files>
  <read_first>
    - apps/website/app/(site)/cuenta/AccountShell.tsx (full file — current activeOrg usage line 22, 31-49)
    - apps/website/app/(site)/cuenta/perfil/page.tsx (useUser line 206; Danger Zone rows line 327-348)
    - apps/website/components/providers.tsx (after Plan 03 — useUser() exposes isOrgContext)
    - .planning/phases/.../24-CONTEXT.md (SWITCH-10 + SWITCH-11 sections — exact code/table)
  </read_first>
  <action>
    **2a. AccountShell.tsx** — replace `activeOrg` with `isOrgContext`, rendering identity from `user` directly (the user IS the org after switching).
    - Line 22: change `const { user, activeOrg, logout } = useUser();` to `const { user, isOrgContext, logout } = useUser();`
    - Lines 31-35 (display values), use CONTEXT.md SWITCH-10:
      ```typescript
      const displayName = user?.name ?? "Usuario";
      const displayEmail = isOrgContext && user?.handle ? `@${user.handle}` : (user?.email ?? "");
      const displayInitials = user?.initials ?? "?";
      ```
    - Line 41 avatar style: change `style={activeOrg ? {...} : undefined}` to `style={isOrgContext ? { background: "linear-gradient(135deg,#7c3aed,#a855f7)", color: "#fff" } : undefined}`
    - Lines 45-49 "Operando como" badge: change `{activeOrg && (...)}` to `{isOrgContext && (...)}` (keep the inner markup, it already renders `displayName`).
    - The TABS array, nav, and logout stay unchanged.

    **2b. perfil/page.tsx** — add the org-context Danger Zone filter (SWITCH-11).
    - Line 206: change `const { user, token, logout, ready } = useUser();` to `const { user, token, logout, ready, isOrgContext } = useUser();`
    - Danger Zone rows (lines 327-331). Replace the inline array literal `.map(...)` so each row carries a `hideInOrgContext` flag and the array is filtered before mapping. Per CONTEXT.md SWITCH-11 (password hidden in org context; email + delete kept):
      ```typescript
      {[
        { k: "password", t: "Cambiar contraseña", d: "Requiere tu contraseña actual.", hideInOrgContext: true },
        { k: "email",    t: "Cambiar email",       d: "Confirmación obligatoria en el nuevo email antes de aplicar el cambio.", hideInOrgContext: false },
        { k: "delete",   t: "Eliminar cuenta",     d: "Acción permanente e irreversible. Ley 21.719 te permite ejercer este derecho en cualquier momento.", hideInOrgContext: false },
      ].filter(r => !isOrgContext || !r.hideInOrgContext).map((r) => (
        // ...existing row markup unchanged...
      ))}
      ```
      Keep the existing row JSX (the `<div className="acc-list-row">…` block at lines 332-347) exactly as-is — only add the `hideInOrgContext` field to each object and insert the `.filter(...)` before `.map(...)`.
    - The PasswordModal/EmailModal/DeleteModal components and their render conditions (lines 352-360) stay unchanged — in org context the password row is hidden so `modal === "password"` is never set.
    - Note: the profile form (firstname/lastname/bio) already reads `user` directly, so in org context it shows the org's data automatically with no change needed.
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | head -20; echo "shell_isorg:$(grep -c 'isOrgContext' 'app/(site)/cuenta/AccountShell.tsx') shell_activeorg_0:$(grep -c 'activeOrg' 'app/(site)/cuenta/AccountShell.tsx') perfil_hide:$(grep -c 'hideInOrgContext' 'app/(site)/cuenta/perfil/page.tsx')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeOrg" "apps/website/app/(site)/cuenta/AccountShell.tsx"` returns `0`
    - `apps/website/app/(site)/cuenta/AccountShell.tsx` contains `isOrgContext && user?.handle ? \`@${user.handle}\``
    - `apps/website/app/(site)/cuenta/AccountShell.tsx` contains `{isOrgContext && (` for the "Operando como" badge
    - `apps/website/app/(site)/cuenta/perfil/page.tsx` destructures `isOrgContext` from useUser
    - `apps/website/app/(site)/cuenta/perfil/page.tsx` contains `hideInOrgContext: true` on the password row and `.filter(r => !isOrgContext || !r.hideInOrgContext)`
    - `cd apps/website && npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>AccountShell shows org name + @handle + purple avatar + "Operando como" badge in org context (from user directly); perfil Danger Zone hides "Cambiar contraseña" in org context, keeps email + delete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify full org switch round-trip</name>
  <files>(no files — manual verification of the running apps)</files>
  <action>Pause for the human to perform the verification steps in <how-to-verify> below. This is a blocking checkpoint: do not proceed or mark the plan complete until the human types "approved". If the human reports a failure, diagnose and fix the relevant file (UserMenu.tsx, AccountShell.tsx, perfil/page.tsx, providers.tsx, or a backend file from Plans 01/02) before re-requesting verification.</action>
  <what-built>
    Real org identity switching end-to-end: POST /auth/switch-org issues an org JWT (sub=orgId); the frontend swaps the session; every /cuenta page reads the org's data with no special-casing; switchBack restores the personal session.
  </what-built>
  <how-to-verify>
    Prerequisites: both apps running (`cd apps/api && pnpm start:dev`, `cd apps/website && pnpm dev`), and a test account that is a member (OWNER or MEMBER) of at least one organization.

    1. Log in as the personal user. Open the avatar UserMenu — confirm "Operando como" shows your personal name + email, and your org(s) are listed under "Cambiar de cuenta".
    2. Click an org. Confirm: a toast "Operando como {org}" appears; the navbar avatar turns purple and shows the org initials; the menu now shows the org as active (check mark).
    3. Navigate to /cuenta/perfil. Confirm the sidebar shows the ORG name + @handle (not your personal name), the avatar is purple, and an "Operando como {org}" badge is visible. Confirm the profile form fields show the ORG's data (org name in the Nombre field), NOT your personal data.
    4. On /cuenta/perfil scroll to "Zona Danger". Confirm "Cambiar contraseña" is HIDDEN; "Cambiar email" and "Eliminar cuenta" are still shown.
    5. Reload the page (F5) while in org context. Confirm you stay in org context (sidebar still shows the org, not personal) — kb-personal-* keys preserved the session.
    6. Open the UserMenu → click "Cuenta personal". Confirm: a toast "Operando como cuenta personal"; navbar avatar reverts to your personal initials/color; /cuenta/perfil now shows YOUR personal data and the password row reappears in Zona Danger.
    7. (Attribution spot-check, optional) While in org context, create or edit an event/spot; confirm it is attributed to the org and the action appears in admin audit as performed by your personal user.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 steps pass, or describe what broke (which step, what you saw vs expected).</resume-signal>
  <verify>
    <automated>MISSING — manual checkpoint; verification is the human round-trip in how-to-verify. Automated proxy: `cd apps/website && npx tsc --noEmit` passes with zero errors across the whole website.</automated>
  </verify>
  <done>Human typed "approved" after confirming all 7 round-trip steps; switching to an org and back is a real identity switch visible on every /cuenta page.</done>
</task>

</tasks>

<verification>
- `cd apps/website && npx tsc --noEmit` passes for the entire website (all Plan-03 consumer errors now resolved).
- UserMenu: 0 activeOrg/setActiveOrg, uses switchToOrg/switchBack + personalToken fetch.
- AccountShell: 0 activeOrg, uses isOrgContext.
- perfil/page.tsx: hideInOrgContext filter present.
- Human verifies the full switch round-trip (checkpoint).
</verification>

<success_criteria>
- Switching to an org is a real identity switch visible on every /cuenta page.
- The org list is fetched with the personal token.
- switchBack restores the personal identity.
- Danger Zone hides password change in org context.
- Human-verify checkpoint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-real-org-account-switching-via-jwt-replace-fake-activeorg-overlay-with-full-identity-switch/24-04-SUMMARY.md`
</output>
