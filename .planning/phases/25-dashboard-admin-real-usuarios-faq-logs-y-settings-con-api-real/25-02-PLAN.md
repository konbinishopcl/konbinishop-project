---
phase: 25-dashboard-admin-real-usuarios-faq-logs-y-settings-con-api-real
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/website/app/dashboard/sections/UsersSection.tsx
autonomous: true
requirements: [DASH-ADM-02, DASH-ADM-03, DASH-ADM-04]
must_haves:
  truths:
    - "Opening the Usuarios section loads the real user list from GET /users"
    - "Clicking Banear/Restaurar persists to PATCH /users/:id/ban and the row updates"
    - "Clicking Ver opens a detail modal showing name, email, tipo, rol, handle, createdAt, banned status"
  artifacts:
    - path: "apps/website/app/dashboard/sections/UsersSection.tsx"
      provides: "Real user list + ban/unban + detail modal"
      contains: "api.adminUsers"
  key_links:
    - from: "apps/website/app/dashboard/sections/UsersSection.tsx"
      to: "api.adminUsers / api.banUser"
      via: "useEffect on mount + onClick handlers"
      pattern: "api\\.(adminUsers|banUser)"
---

<objective>
Replace the hardcoded USERS array in UsersSection with a real fetch to GET /users, wire ban/unban to PATCH /users/:id/ban, and add a "Ver" detail modal. Keep the existing table layout, filters, and CSS classes unchanged.

Purpose: DASH-ADM-02/03/04 — admin sees and moderates real users.
Output: UsersSection backed entirely by the API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-dashboard-admin-real-usuarios-faq-logs-y-settings-con-api-real/25-CONTEXT.md

<interfaces>
<!-- Provided by Plan 25-01 (already merged before this plan runs). Call these verbatim. -->
api.adminUsers(token: string): Promise<ApiAdminUser[]>
api.banUser(id: number, blocked: boolean, token: string): Promise<ApiAdminUser>

type ApiAdminUser = {
  id: number; email: string; firstname: string | null; lastname: string | null;
  rut: string | null; isCompany: boolean; role: 'USER'|'ADMIN'|'SUPER_ADMIN';
  confirmed: boolean; blocked: boolean; type: 'PERSON' | 'ORGANIZATION';
  handle: string | null; isVerified: boolean; createdAt: string; updatedAt: string;
};

useUser() from "@/components/providers" provides { token }.
</interfaces>

<constraints>
- GET /users is ADMIN+, but PATCH /users/:id/ban is SUPER_ADMIN only. An ADMIN will see the list but get 403 on ban — the try/catch + toast.error already handles this; no special UI needed.
- The "Ver" modal populates ENTIRELY from the already-loaded row object — NO extra fetch.
- No pagination (CONTEXT: load up to 100 in one call; GET /users returns all, render all).
- Filters "Todos/Persona/Organización/Baneado" stay client-side over the loaded array.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch real users + wire ban/unban</name>
  <files>apps/website/app/dashboard/sections/UsersSection.tsx</files>
  <read_first>
    - apps/website/app/dashboard/sections/UsersSection.tsx (current mock: USERS array lines 19-24, UserRow type, toggleBan local-only mutation, filter logic)
    - apps/website/app/dashboard/sections/EventsSection.tsx (lines 395-432 — the canonical useCallback+useEffect+token+try/catch/finally fetch pattern to replicate)
    - apps/website/lib/api.ts (api.adminUsers, api.banUser, ApiAdminUser type added by Plan 01)
  </read_first>
  <action>
    Rewrite UsersSection.tsx data layer:

    1. Add imports at top:
       `import { useCallback, useEffect, useState } from "react";`
       `import { toast } from "sonner";`
       `import { useUser } from "@/components/providers";`
       `import { api, type ApiAdminUser } from "@/lib/api";`

    2. DELETE the `USERS` mock array (lines 19-24) and the `UserRow` type (lines 8-15). Replace UserRow usage with ApiAdminUser.

    3. In the component:
       - `const { token } = useUser();`
       - `const [users, setUsers] = useState<ApiAdminUser[]>([]);`
       - `const [loading, setLoading] = useState(true);`
       - `const [busyId, setBusyId] = useState<number | null>(null);` (disable the ban button while in flight)

    4. Fetch on mount:
       ```typescript
       const fetchUsers = useCallback(async () => {
         if (!token) return;
         setLoading(true);
         try {
           const data = await api.adminUsers(token);
           setUsers(data);
         } catch (ex) {
           toast.error(ex instanceof Error ? ex.message : "Error al cargar usuarios");
         } finally {
           setLoading(false);
         }
       }, [token]);
       useEffect(() => { fetchUsers(); }, [fetchUsers]);
       ```

    5. Replace `toggleBan` with a real persisted handler (called from the ConfirmDialog onConfirm):
       ```typescript
       async function applyBan(u: ApiAdminUser, blocked: boolean) {
         if (!token) return;
         setBusyId(u.id);
         try {
           const updated = await api.banUser(u.id, blocked, token);
           setUsers((prev) => prev.map((x) => (x.id === u.id ? updated : x)));
           toast.success(blocked ? "Usuario baneado" : "Usuario restaurado");
         } catch (ex) {
           toast.error(ex instanceof Error ? ex.message : "No se pudo actualizar");
         } finally {
           setBusyId(null);
         }
       }
       ```

    6. Derive display values from ApiAdminUser inside the table map (replace the mock fields):
       - name: `[u.firstname, u.lastname].filter(Boolean).join(" ") || u.email`
       - avatar initials: first letters of name parts, or first 2 chars of email local part, uppercased
       - email cell: `u.email`
       - tipo: `u.type === "ORGANIZATION" ? "Organización" : "Persona"`
       - rol: map `u.role` → `u.role === "SUPER_ADMIN" ? "Super Admin" : u.role === "ADMIN" ? "Admin" : "Usuario"`
       - estado: `u.blocked ? "Baneado" : "Activo"` (drive the `stat-pill pub/rej` class and dot the same way the mock did)
       - ban button label: `u.blocked ? "Restaurar" : "Banear"`, onClick opens `setModal({ type: u.blocked ? "restore" : "ban", item: u })`

    7. Update the `filtered` logic to operate on ApiAdminUser:
       - "Todos" → all
       - "Baneado" → `u.blocked === true`
       - "Persona" → `u.type === "PERSON"`
       - "Organización" → `u.type === "ORGANIZATION"`

    8. Update ConfirmDialog wiring: `onConfirm={() => applyBan(modal.item, modal.type === "ban")}`.

    9. Add a loading guard: when `loading`, render a simple row/message (e.g. a `<tbody>` with one `<td colSpan={6}>Cargando…</td>`). Keep existing `panel`, `a-table`, `stat-pill`, `pill`, `row-act` classes.

    Keep the `key` on rows as `u.id` (not array index).
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && grep -c "api.adminUsers\|api.banUser" apps/website/app/dashboard/sections/UsersSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Mock removed: `grep -c "const USERS" apps/website/app/dashboard/sections/UsersSection.tsx` returns 0
    - `grep -q "api.adminUsers(token)" apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - `grep -q "api.banUser(u.id" apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - `grep -q "useUser()" apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - `grep -q "ApiAdminUser" apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - filters use type/blocked not mock fields: `grep -q 'u.type === "PERSON"' apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
  </acceptance_criteria>
  <done>UsersSection loads real users and ban/unban persists with optimistic row update.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Ver" user detail modal</name>
  <files>apps/website/app/dashboard/sections/UsersSection.tsx</files>
  <read_first>
    - apps/website/app/dashboard/sections/UsersSection.tsx (the ConfirmDialog component + confirm-bg/confirm-card classes — follow that visual style)
    - apps/website/app/dashboard/sections/SettingsSection.tsx (ConfirmDialog markup, confirm-bg/confirm-card pattern for reference)
  </read_first>
  <action>
    Add a user-detail modal to UsersSection.tsx, populated from the already-loaded row (no fetch).

    1. Extend the modal state union to include a view type:
       `{ type: "ban" | "restore" | "view"; item: ApiAdminUser }`

    2. Wire the existing "Ver" button: `onClick={() => setModal({ type: "view", item: u })}`.

    3. Add a `UserDetailModal` (inline component or inline JSX block) styled with `confirm-bg`/`confirm-card` (same as ConfirmDialog). Render these fields from `item`:
       - Nombre: `[firstname, lastname].filter(Boolean).join(" ") || email`
       - Email: `email`
       - Tipo: `type === "ORGANIZATION" ? "Organización" : "Persona"`
       - Rol: `role === "SUPER_ADMIN" ? "Super Admin" : role === "ADMIN" ? "Admin" : "Usuario"`
       - Handle: `handle ? "@" + handle : "—"`
       - Registrado: format `createdAt` as a readable date, e.g. `new Date(item.createdAt).toLocaleDateString("es-CL")`
       - Estado: `blocked ? "Baneado" : "Activo"` (reuse `stat-pill pub/rej` styling)
       - A single "Cerrar" button (`btn ghost`) that calls `setModal(null)`.

    4. Render it conditionally: `{modal?.type === "view" && (<UserDetailModal item={modal.item} onClose={() => setModal(null)} />)}`.

    Use plain label/value rows (e.g. `<div style={{ display:"flex", justifyContent:"space-between" }}>`). No new CSS files — inline styles + existing classes only.
  </action>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && grep -c 'type: "view"\|"view"' apps/website/app/dashboard/sections/UsersSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - View modal triggered: `grep -q 'setModal({ type: "view"' apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - Detail modal renders handle + createdAt: `grep -q "item.handle" apps/website/app/dashboard/sections/UsersSection.tsx` and `grep -q "item.createdAt" apps/website/app/dashboard/sections/UsersSection.tsx` both succeed
    - Uses confirm-card style: `grep -q "confirm-card" apps/website/app/dashboard/sections/UsersSection.tsx` succeeds
    - No extra fetch in the view modal: the modal references only `item.*` (verify the modal component takes an `item: ApiAdminUser` prop, not `token`)
  </acceptance_criteria>
  <done>Clicking "Ver" opens a modal with all 7 detail fields sourced from the loaded row.</done>
</task>

</tasks>

<verification>
- `cd apps/website && npx tsc --noEmit 2>&1 | grep "UsersSection.tsx"` produces no output.
- All grep acceptance criteria pass.
</verification>

<success_criteria>
- USERS mock array gone; list fetched via api.adminUsers.
- Ban/Restore persists via api.banUser with optimistic update + toast.
- "Ver" opens a detail modal (name, email, tipo, rol, handle, createdAt, estado) with no extra fetch.
- Existing table CSS/layout preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dashboard-admin-real-usuarios-faq-logs-y-settings-con-api-real/25-02-SUMMARY.md`
</output>
