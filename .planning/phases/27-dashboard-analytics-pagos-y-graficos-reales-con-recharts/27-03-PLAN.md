---
phase: 27-dashboard-analytics-pagos-y-graficos-reales-con-recharts
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - apps/website/app/dashboard/sections/HomeSection.tsx
autonomous: true
requirements: [DASH-ANLT-03, DASH-ANLT-04, DASH-ANLT-08]
must_haves:
  truths:
    - "HomeSection review queue shows the 5 most recent PENDING_MODERATION events from the API"
    - "Approving/rejecting a queue item calls the real approve/reject API and removes the row on success"
    - "HomeSection activity feed shows the 5 most recent audit logs from the API"
    - "The 'En Revisión' KPI reflects the real total of pending events; the revenue chart uses RevenueBarChart"
  artifacts:
    - path: "apps/website/app/dashboard/sections/HomeSection.tsx"
      provides: "Real review queue + activity feed + KPI + Recharts revenue chart"
      contains: "api.adminEvents"
  key_links:
    - from: "HomeSection.tsx"
      to: "GET /events?status=PENDING_MODERATION"
      via: "api.adminEvents(token, { status, pageSize })"
      pattern: "adminEvents\\(token"
    - from: "HomeSection.tsx"
      to: "GET /admin/audit-logs"
      via: "api.auditLogs"
      pattern: "auditLogs\\("
    - from: "HomeSection.tsx"
      to: "RevenueBarChart"
      via: "component import replacing .chart div"
      pattern: "RevenueBarChart"
---

<objective>
Wire HomeSection to real data: review queue from `GET /events?status=PENDING_MODERATION`, activity feed from `GET /admin/audit-logs`, the "En Revisión" KPI from the real pending total, and replace the CSS `.chart` bars with `<RevenueBarChart />`.

Purpose: HomeSection currently renders three hardcoded mock arrays (QUEUE, ACTIVITY, CHART_VALUES) plus CATEGORIES. This plan replaces the queue, activity, and chart-component with real wiring while keeping category stats and the revenue *values* mock (no aggregate endpoint exists).
Output: HomeSection.tsx fetching real queue + activity, real approve/reject, real "En Revisión" KPI, Recharts chart component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-CONTEXT.md
@.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-UI-SPEC.md

<interfaces>
From @/lib/api (created/extended in Plan 02):
- api.adminEvents(token, query) -> Promise ApiEventList where ApiEventList = { items: ApiEvent[]; total: number; page; pageSize; totalPages }
  query supports: { status?: "PENDING_MODERATION" | ...; pageSize?: number; page? }
- api.approveEvent(id: number, token) -> Promise ApiEvent
- api.rejectEvent(id: number, reason: string, token) -> Promise ApiEvent
- api.auditLogs(query, token) -> Promise ApiAuditLogList where query = { pageSize?: number; ... }, items: ApiAuditLog[]
- ApiEvent has: id (number), title, slug, company, poster, banner, status, owner?:{firstname,lastname,email}, eventCategory?:{name}, ...
- ApiAuditLog has: id, userId (number|null), action (AuditAction), entity (AuditEntity), entityId (number), metadata, createdAt (ISO string)

From @/components/providers:
- useUser() -> { token, ... } — token used for authenticated calls.

From @/components/charts/RevenueBarChart (created in Plan 02):
- import { RevenueBarChart, type RevenueDatum } from "@/components/charts/RevenueBarChart"
- Props: { data: { label: string; value: number }[] }

Existing modals (do not recreate):
- AdminApproveModal props: { title, description, onClose, onConfirm }
- AdminRejectModal props: { title, description, onClose, onConfirm } where onConfirm receives (reason: string)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire HomeSection queue + activity + KPI to real API</name>
  <read_first>
    - apps/website/app/dashboard/sections/HomeSection.tsx (current full file — note it is "use client", uses useState only, mock QUEUE/ACTIVITY/CATEGORIES/CHART arrays, AdminApproveModal/AdminRejectModal usage)
    - apps/website/app/dashboard/sections/LogsSection.tsx (reference fetch pattern: useUser() for token, useCallback fetch with try/catch + toast.error + finally setLoading(false), useEffect re-run on token)
    - apps/website/lib/api.ts (confirm ApiEvent, ApiAuditLog, adminEvents, auditLogs, approveEvent, rejectEvent signatures)
    - .planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-UI-SPEC.md (Data Loading States + Copywriting Contract: empty-state copy for queue and activity)
  </read_first>
  <action>
    Edit apps/website/app/dashboard/sections/HomeSection.tsx. Keep the JSX layout (kpi-grid, two-panel grids, cat-bar, modals) intact; replace the data sources.

    1. Add imports:
       import { useCallback, useEffect, useState } from "react";
       import { useUser } from "@/components/providers";
       import { api, type ApiEvent, type ApiAuditLog } from "@/lib/api";
       Keep `toast`, `AdminApproveModal`, `AdminRejectModal` imports.

    2. Remove the mock `QUEUE` and `ACTIVITY` consts and the `QueueItem` type. Keep `CATEGORIES`, `CHART_VALUES`, `CHART_LABELS` as mock (CONTEXT: category stats stay mock; revenue values stay mock — only the chart component changes).

    3. Component body — replace useState(QUEUE) with real state + fetch:
       const { token } = useUser();
       const [queue, setQueue] = useState ApiEvent[] ([]);
       const [pendingTotal, setPendingTotal] = useState(0);
       const [activity, setActivity] = useState ApiAuditLog[] ([]);
       const [modal, setModal] = useState ModalState (null);

       Define a useCallback `load` keyed on [token]:
         if (!token) return;
         try {
           const [evts, logs] = await Promise.all([
             api.adminEvents(token, { status: "PENDING_MODERATION", pageSize: 5 }),
             api.auditLogs({ pageSize: 5 }, token),
           ]);
           setQueue(evts.items);
           setPendingTotal(evts.total);
           setActivity(logs.items);
         } catch (ex) {
           toast.error(ex instanceof Error ? ex.message : "Inicio no disponible — intenta de nuevo");
         }
       Then: useEffect(() => { load(); }, [load]);

       `ModalState` becomes: { type: "approve" | "reject"; item: ApiEvent } | null

    4. handleApprove / handleReject call the real API (each: guard token, await api call, toast, filter row out of queue, decrement pendingTotal via Math.max(0, n-1), setModal(null) in finally, toast.error on catch):
       - approve: await api.approveEvent(id, token); toast.success("Evento aprobado");
       - reject:  await api.rejectEvent(id, reason, token); toast.error(`Evento rechazado — "${reason.slice(0, 40)}"`);

    5. Queue rendering: map over `queue` (ApiEvent[]). For each row:
       - title: r.title
       - meta: [r.company, r.eventCategory?.name].filter(Boolean).join(" · ")
       - thumbnail: r.poster ? an img tag with src={r.poster} (40x40, object-fit cover) inside the existing .thumb-sm wrapper : a <div className="pic poster-art" />. Drop the mock pa-N classes.
       - Approve/reject buttons keep exact copy "✓ Aprobar" / "✕ Rechazar"; onClick sets modal { type, item: r }.
       Empty state: when queue.length === 0, render the .empty panel (icon .ic + h3 + p) with heading "Sin eventos pendientes" and body "No hay eventos esperando revisión." instead of the rows.

    6. Activity feed rendering: map over `activity` (ApiAuditLog[]). Audit logs return raw scalars (no joined user name — degraded render per [Phase 25-04]):
       - actor label: a.userId == null ? "Sistema" : `Usuario #${a.userId}`
       - body: <strong>{actor}</strong> {a.action} <span style accent>{a.entity} #{a.entityId}</span>
       - time: new Date(a.createdAt).toLocaleString("es-CL")
       Keep the avatar circle using first letter of actor label; "Sistema" keeps the var(--ink-3) background, others keep the accent gradient.
       Empty state: when activity.length === 0, render .empty panel heading "Sin actividad reciente" body "No hay registros de actividad disponibles."

    7. KPIs: change the "EN REVISIÓN" KPI value from hardcoded 9 to {pendingTotal}. Leave "INGRESOS MES" ($3.8M), "EVENTOS PUBLICADOS" (142), "SUSCRIPTORES" (87) as mock — no aggregate endpoint (CONTEXT: revenue KPI mock; "Eventos publicados" stays mock to avoid a third fetch). Keep or remove the "↑ 3" delta on En Revisión as static decorative text — do NOT compute a fake delta.
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | grep -i "HomeSection"; echo done; grep -q "api.adminEvents(token" app/dashboard/sections/HomeSection.tsx && grep -q "auditLogs(" app/dashboard/sections/HomeSection.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HomeSection.tsx contains `import { useUser }` and `useUser()`
    - HomeSection.tsx contains `api.adminEvents(token, { status: "PENDING_MODERATION", pageSize: 5 })`
    - HomeSection.tsx contains `api.auditLogs({ pageSize: 5 }, token)`
    - HomeSection.tsx contains `api.approveEvent(` and `api.rejectEvent(`
    - HomeSection.tsx contains `{pendingTotal}` rendered in the EN REVISIÓN kpi
    - HomeSection.tsx contains the strings "Sin eventos pendientes" and "Sin actividad reciente"
    - HomeSection.tsx no longer contains the mock `const QUEUE` or `const ACTIVITY` declarations
    - `cd apps/website && npx tsc --noEmit` produces no errors referencing HomeSection.tsx
  </acceptance_criteria>
  <done>Queue, activity feed, approve/reject, and the En Revisión KPI are real; mock QUEUE/ACTIVITY removed; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the CSS .chart with RevenueBarChart</name>
  <read_first>
    - apps/website/app/dashboard/sections/HomeSection.tsx (the "Ingresos mensuales" panel containing the `<div className="chart">` block that maps CHART_VALUES to `<div className="bar">`)
    - apps/website/components/charts/RevenueBarChart.tsx (the component + RevenueDatum prop shape from Plan 02)
  </read_first>
  <action>
    In apps/website/app/dashboard/sections/HomeSection.tsx:

    1. Add the import:
       import { RevenueBarChart, type RevenueDatum } from "@/components/charts/RevenueBarChart";

    2. Build a chart data array from the (still-mock) CHART_VALUES / CHART_LABELS:
       const chartData: RevenueDatum[] = CHART_VALUES.map((value, i) => ({ label: CHART_LABELS[i], value }));

    3. Replace the entire `<div className="chart" role="img" ...>...CHART_VALUES.map(...)...</div>` block inside the "Ingresos mensuales" panel with:
       <RevenueBarChart data={chartData} />

    Keep the surrounding `<div className="panel">` and its `<div className="ph"><h3>Ingresos mensuales</h3>...ÚLTIMOS 12 MESES</div>` header untouched. Do NOT touch the "Por categoría" cat-bar panel (stays mock per CONTEXT — cat-bar is not migrated to Recharts).
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | grep -i "HomeSection"; echo done; grep -q "RevenueBarChart data={chartData}" app/dashboard/sections/HomeSection.tsx && ! grep -q 'className="chart"' app/dashboard/sections/HomeSection.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HomeSection.tsx contains `import { RevenueBarChart` from "@/components/charts/RevenueBarChart"
    - HomeSection.tsx contains `<RevenueBarChart data={chartData} />`
    - HomeSection.tsx contains `const chartData: RevenueDatum[]`
    - HomeSection.tsx no longer contains `className="chart"` (the CSS bar block is gone)
    - HomeSection.tsx STILL contains `className="cat-bar"` (Por categoría panel preserved)
    - `cd apps/website && npx tsc --noEmit` produces no errors referencing HomeSection.tsx
  </acceptance_criteria>
  <done>The revenue panel renders RevenueBarChart; the CSS .chart bars are removed; cat-bar untouched; tsc passes.</done>
</task>

</tasks>

<verification>
- `cd apps/website && npx tsc --noEmit` exits 0.
- HomeSection fetches via api.adminEvents (status PENDING_MODERATION) and api.auditLogs; renders RevenueBarChart.
- cat-bar "Por categoría" and the revenue KPI remain mock by design.
</verification>

<success_criteria>
- Loading the dashboard home as an admin shows the real pending-event queue (max 5), real recent audit logs (max 5), and the real pending total in the "En Revisión" KPI.
- Approving/rejecting an item hits the real endpoint and the row disappears on success.
- The revenue chart is a Recharts BarChart visually equivalent to the previous CSS bars.
</success_criteria>

<output>
After completion, create `.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-03-SUMMARY.md`
</output>
