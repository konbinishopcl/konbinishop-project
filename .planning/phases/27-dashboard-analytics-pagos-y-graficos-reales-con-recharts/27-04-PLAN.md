---
phase: 27-dashboard-analytics-pagos-y-graficos-reales-con-recharts
plan: 04
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - apps/website/app/dashboard/sections/PaymentsSection.tsx
autonomous: true
requirements: [DASH-ANLT-05, DASH-ANLT-09, DASH-ANLT-10]
must_haves:
  truths:
    - "PaymentsSection table shows real orders loaded from GET /payments"
    - "The detail modal shows the real order's buyer, gateway, items, and total"
    - "Exportar CSV downloads a CSV built from the real loaded payments"
  artifacts:
    - path: "apps/website/app/dashboard/sections/PaymentsSection.tsx"
      provides: "Real payments table + detail modal + CSV export"
      contains: "api.adminPayments"
  key_links:
    - from: "PaymentsSection.tsx"
      to: "GET /payments"
      via: "api.adminPayments(token)"
      pattern: "adminPayments\\(token"
---

<objective>
Wire PaymentsSection to real data from `GET /payments` (Plan 01): real table rows, real detail modal, and client-side CSV export from the loaded data. KPIs stay mock (no aggregate endpoint).

Purpose: PaymentsSection currently renders a hardcoded `ROWS` mock array with card/method/error fields that have no backing in the Order model. This plan binds it to the real ApiPayment shape and drops the unbacked fields.
Output: PaymentsSection.tsx loading real payments, rendering a real detail modal, and exporting real CSV.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-CONTEXT.md
@.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-UI-SPEC.md

<interfaces>
From @/lib/api (Plan 02):
- api.adminPayments(token: string) -> Promise ApiPayment[]
- ApiPayment = {
    id: number;
    status: "PAID" | "FAILED";
    total: number;            // whole pesos
    gateway: string | null;
    createdAt: string;        // ISO
    buyer: { name: string; handle: string | null; email: string };
    items: Array<{ type: string; title: string; days: number; subtotal: number }>;
  }
From @/components/providers: useUser() -> { token }

DATA REALITY (bake into the UI — do not invent fields):
- The Order model has NO card number, NO payment method beyond `gateway`, NO error/reason text.
  The Transbank flow reverts failed payments back to DRAFT, so FAILED rows will almost never appear;
  expect PAID-only data in practice. Filter is PAID/FAILED per CONTEXT, but render must not depend on FAILED.
- Therefore the modal's old "Tarjeta", "card", and "err" fields are dropped. Show "Pasarela" = gateway ?? "—".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load real payments into the table</name>
  <read_first>
    - apps/website/app/dashboard/sections/PaymentsSection.tsx (current full file — note the Payment mock type, ROWS array, buildCSV, the table, the detail modal with Tarjeta/card/err fields)
    - apps/website/app/dashboard/sections/SubsSection.tsx (reference loading/empty table pattern from Phase 26: "Cargando…" row, "Sin registros" row, useUser token, useCallback fetch)
    - apps/website/lib/api.ts (confirm ApiPayment shape and adminPayments signature)
    - .planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-UI-SPEC.md (Data Loading States; Copywriting: empty "Sin pagos registrados" / "No hay órdenes pagadas para mostrar.")
  </read_first>
  <action>
    Edit apps/website/app/dashboard/sections/PaymentsSection.tsx.

    1. Add imports:
       import { useCallback, useEffect, useState } from "react";
       import { useUser } from "@/components/providers";
       import { api, type ApiPayment } from "@/lib/api";
       Keep the toast import.

    2. Delete the mock `type Payment`, the `type PaySt`, and the `ROWS` array. Replace the local `Payment` usages with `ApiPayment`.

    3. Add a CLP formatter at module scope (Order.total is whole pesos):
       const formatCLP = (n: number) => "$" + n.toLocaleString("es-CL");
       Use this everywhere an amount is displayed (table MONTO column, modal item subtotals, modal Total). Replace the old pre-formatted `r.amount` strings.

    4. Add a date formatter:
       const formatDate = (iso: string) => new Date(iso).toLocaleDateString("es-CL", { day: "2-digit", month: "short", year: "numeric" });

    5. Component state + fetch:
       const { token } = useUser();
       const [rows, setRows] = useState ApiPayment[] ([]);
       const [loading, setLoading] = useState(true);
       const [detail, setDetail] = useState ApiPayment | null (null);

       const load = useCallback async keyed [token]:
         if (!token) return;
         setLoading(true);
         try { setRows(await api.adminPayments(token)); }
         catch (ex) { toast.error(ex instanceof Error ? ex.message : "Pagos no disponibles — intenta de nuevo"); }
         finally { setLoading(false); }
       useEffect(() => { load(); }, [load]);

    6. Update buildCSV to take ApiPayment[] and emit real columns:
       header: ["ID","Comprador","Productos","Monto","Fecha","Estado"]
       per row: [
         "TX-" + r.id,                                  // synthesize a display id from the numeric order id
         r.buyer.name,
         r.items.map(i => i.title).join(" + "),
         String(r.total),
         formatDate(r.createdAt),
         r.status === "PAID" ? "Aprobado" : "Fallido",
       ].map(field => `"${String(field).replace(/"/g, '""')}"`).join(",")
       Keep the blob/download/toast.success logic; export `buildCSV(rows)` (the loaded state), not the deleted ROWS.

    7. Table rendering: map over `rows`.
       - ID column: `TX-${r.id}` (mono font kept)
       - ORGANIZADOR column: r.buyer.name
       - PRODUCTO column: r.items.map(i => i.title).join(" + ") || "—"
       - MONTO column: formatCLP(r.total) (mono, bold)
       - FECHA column: formatDate(r.createdAt) (mono)
       - ESTADO column: stat-pill with class `pub` when r.status === "PAID" else `rej`; label "Aprobado"/"Fallido"
       - Detalle button: onClick={() => setDetail(r)}
       Loading state: while `loading`, render a single <tr> with a <td colSpan={7}> centered "Cargando…" in var(--ink-3), padding "16px 0".
       Empty state: when !loading && rows.length === 0, render a single <tr><td colSpan={7}> centered "Sin pagos registrados" — and optionally the body line — in var(--ink-3).
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | grep -i "PaymentsSection"; echo done; grep -q "api.adminPayments(token)" app/dashboard/sections/PaymentsSection.tsx && ! grep -q "const ROWS" app/dashboard/sections/PaymentsSection.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PaymentsSection.tsx contains `import { useUser }` and `api.adminPayments(token)`
    - PaymentsSection.tsx contains `type ApiPayment` import and uses `ApiPayment` (not the mock `Payment` type)
    - PaymentsSection.tsx no longer contains `const ROWS`
    - PaymentsSection.tsx contains `formatCLP` and `r.total`
    - PaymentsSection.tsx contains the loading string "Cargando…" and empty string "Sin pagos registrados"
    - PaymentsSection.tsx buildCSV signature accepts the loaded rows (called as `buildCSV(rows)`)
    - `cd apps/website && npx tsc --noEmit` produces no errors referencing PaymentsSection.tsx
  </acceptance_criteria>
  <done>The table shows real payments with loading + empty states; CSV exports the loaded data; mock ROWS removed; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the detail modal from real fields</name>
  <read_first>
    - apps/website/app/dashboard/sections/PaymentsSection.tsx (the `{detail && (...)}` modal block — note the confirm-bg/confirm-card structure, the info rows for Organizador/Fecha/Pasarela/Tarjeta, the ÍTEMS list, the err block, the action buttons)
    - .planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-UI-SPEC.md (Copywriting Contract: "Reembolsar" stub fires toast.warning; "Descargar comprobante" stub; modal max-width 560; "Cerrar")
  </read_first>
  <action>
    In the `{detail && (...)}` modal block of apps/website/app/dashboard/sections/PaymentsSection.tsx, rebind to ApiPayment fields:

    1. Header: id badge shows `TX-${detail.id}`. The h3 shows a product summary: `detail.items.map(i => i.title).join(" + ") || "Orden"`. The stat-pill uses `pub`/`rej` from detail.status ("Aprobado"/"Fallido").

    2. Info rows (the var(--surface-2) box) — replace the old [Organizador/Fecha/Pasarela/Tarjeta] array with REAL fields only (no card, no method beyond gateway):
       [
         ["Comprador", detail.buyer.handle ? `${detail.buyer.name} ${detail.buyer.handle}` : detail.buyer.name],
         ["Email", detail.buyer.email],
         ["Fecha", formatDate(detail.createdAt)],
         ["Pasarela", detail.gateway ?? "—"],
       ]
       Drop the "Tarjeta" row entirely.

    3. ÍTEMS list: map over detail.items, each row shows `item.title` (left) and `formatCLP(item.subtotal)` (right, mono). The Total row shows `formatCLP(detail.total)`.

    4. Remove the `detail.err` error block entirely (Order has no error text field).

    5. Action buttons:
       - "Descargar comprobante": keep as stub → onClick closes modal + toast.info("Descargando comprobante PDF…").
       - "Reembolsar": render ONLY when detail.status === "PAID"; keep as stub → onClick closes modal + toast.warning("Iniciando reembolso…"). (UI-SPEC: real confirmation dialog deferred.)
       - "Cerrar" (btn dark) closes the modal.
       Keep max-width 560 on the confirm-card.
  </action>
  <verify>
    <automated>cd apps/website && npx tsc --noEmit 2>&1 | grep -i "PaymentsSection"; echo done; grep -q "detail.buyer" app/dashboard/sections/PaymentsSection.tsx && grep -q "detail.gateway" app/dashboard/sections/PaymentsSection.tsx && ! grep -q "detail.err" app/dashboard/sections/PaymentsSection.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PaymentsSection.tsx modal contains `detail.buyer.name`, `detail.buyer.email`, `detail.gateway`
    - PaymentsSection.tsx contains `detail.items.map` rendering item.title and formatCLP(item.subtotal)
    - PaymentsSection.tsx no longer references `detail.err`, `detail.card`, or `detail.method`
    - PaymentsSection.tsx "Reembolsar" button is gated on `detail.status === "PAID"` and fires `toast.warning("Iniciando reembolso…")`
    - PaymentsSection.tsx contains `toast.info("Descargando comprobante PDF…")`
    - `cd apps/website && npx tsc --noEmit` produces no errors referencing PaymentsSection.tsx
  </acceptance_criteria>
  <done>The detail modal shows only real fields (buyer, email, fecha, pasarela, items, total); refund/comprobante remain stubs; tsc passes.</done>
</task>

</tasks>

<verification>
- `cd apps/website && npx tsc --noEmit` exits 0.
- PaymentsSection loads via api.adminPayments; table + modal + CSV all driven by ApiPayment.
- No unbacked fields (card, method, err) remain; KPIs stay mock by design.
</verification>

<success_criteria>
- As an admin, opening the Payments dashboard shows real PAID/FAILED orders with buyer, products, amount (CLP), date, and status.
- The detail modal shows real buyer/gateway/items/total; "Reembolsar" only on PAID and is a toast stub.
- "Exportar CSV" downloads a CSV of the loaded rows with quoted fields.
</success_criteria>

<output>
After completion, create `.planning/phases/27-dashboard-analytics-pagos-y-graficos-reales-con-recharts/27-04-SUMMARY.md`
</output>
