---
phase: 27.1-dashboard-gap-fixes
plan: "04"
subsystem: ui
tags: [react, next.js, dashboard, payments, useMemo]

# Dependency graph
requires:
  - phase: 27-dashboard-analytics
    provides: PaymentsSection with rows state loaded from api.adminPayments

provides:
  - PaymentsSection with computed KPIs from loaded rows (INGRESOS MES, HISTÓRICO, FALLIDOS)
  - COMPRADOR column header (was ORGANIZADOR)
  - Detail modal with only Cerrar button (fake PDF/refund buttons removed)

affects: [dashboard, payments-section]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useMemo over loaded rows for client-side KPI aggregation without extra API calls"

key-files:
  created: []
  modified:
    - apps/website/app/dashboard/sections/PaymentsSection.tsx

key-decisions:
  - "KPIs computed client-side from already-loaded rows — no new API calls needed"
  - "REEMBOLSOS KPI removed entirely — no backend refund data available"
  - "FALLIDOS replaces PENDIENTES — reflects actual FAILED row count from rows state"

patterns-established:
  - "useMemo over rows state for cheap derived KPIs — avoids extra API calls"

requirements-completed: [DASH-FIX-04]

# Metrics
duration: 10min
completed: 2026-05-30
---

# Phase 27.1 Plan 04: PaymentsSection KPIs + Cleanup Summary

**PaymentsSection KPIs now derived from loaded rows via useMemo; ORGANIZADOR renamed COMPRADOR; fake PDF download and refund buttons removed from detail modal**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-30T00:00:00Z
- **Completed:** 2026-05-30T00:10:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `useMemo` to compute three KPIs (INGRESOS MES, HISTÓRICO, FALLIDOS) from the already-loaded `rows` state — no new API calls required
- Removed REEMBOLSOS KPI entirely (no backend data)
- Renamed table column header from ORGANIZADOR to COMPRADOR
- Removed `Descargar comprobante` and `Reembolsar` fake buttons from detail modal; kept only `Cerrar`

## Task Commits

1. **Task 1: Computed KPIs + COMPRADOR column + remove fake buttons** - `7cd21d8` (feat)

## Files Created/Modified
- `apps/website/app/dashboard/sections/PaymentsSection.tsx` - Added useMemo KPI computation, renamed column, removed fake action buttons

## Decisions Made
- KPIs computed client-side from rows — follows plan exactly; no extra API calls needed since rows are already loaded
- FALLIDOS (count of FAILED rows) replaces PENDIENTES (was hardcoded 3) — more accurate label for actual meaning

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None — TypeScript passes clean, all acceptance criteria met on first attempt.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- PaymentsSection now shows real KPIs and correct buyer column
- Detail modal is clean — only Close button remains
- Ready for subsequent dashboard gap fix plans (27.1-05 etc.)

---
*Phase: 27.1-dashboard-gap-fixes*
*Completed: 2026-05-30*
