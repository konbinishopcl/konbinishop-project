---
phase: 27.1-dashboard-gap-fixes
plan: "06"
subsystem: dashboard/moderation
tags: [dashboard, moderation, ux, cleanup]
dependency_graph:
  requires: []
  provides: [honest-approve-flow-events, honest-approve-flow-articles]
  affects: [EventsSection, ArticlesSection]
tech_stack:
  added: []
  patterns: [ConfirmDialog reuse]
key_files:
  modified:
    - apps/website/app/dashboard/sections/EventsSection.tsx
    - apps/website/app/dashboard/sections/ArticlesSection.tsx
decisions:
  - "Deleted both inline AdminApproveModal components entirely — no refactor, clean removal"
  - "Approve uses ConfirmDialog without danger or typeToConfirm — positive action, no friction gate"
metrics:
  duration: "~4 minutes"
  completed: "2026-05-30"
  tasks_completed: 2
  files_modified: 2
---

# Phase 27.1 Plan 06: Remove Fake AI Tag Generator Summary

Remove the fake AI tag generator from the approve modal in EventsSection and ArticlesSection. Both sections had an inline `AdminApproveModal` with hardcoded tags, a `regenAI()` function using `setTimeout + Math.random`, and a spinner button — none of which connected to any real AI or passed tags to the backend. Replaced with the plain `ConfirmDialog` already present in each file.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | EventsSection — replace AI approve modal with ConfirmDialog | 0b06f04 | EventsSection.tsx |
| 2 | ArticlesSection — replace AI approve modal with ConfirmDialog | ad4a14e | ArticlesSection.tsx |

## What Was Built

- **EventsSection:** Inline `AdminApproveModal` (lines 109–150) deleted. Approve modal render replaced with `<ConfirmDialog title="¿Publicar este evento?" ...>`. No `danger`, no `typeToConfirm` — positive action, confirm button immediately enabled.
- **ArticlesSection:** Inline `AdminApproveModal` (lines 87–128) deleted. Approve modal render replaced with `<ConfirmDialog title="¿Publicar este artículo?" ...>`. Same pattern.
- Reject, ban, delete, and transfer modals in both files are unchanged.
- The shared `/modals/AdminApproveModal.tsx` (used by HomeSection) was not touched.

## Verification

- `grep "regenAI"` → nothing in either file
- `grep "Math.random"` → nothing in either file
- `grep "anime, cosplay"` → nothing in either file
- `grep "function AdminApproveModal"` → nothing in either file
- `grep "¿Publicar este evento?"` → present in EventsSection (line 594)
- `grep "¿Publicar este artículo?"` → present in ArticlesSection (line 466)
- `npx tsc --noEmit` → passes with no errors

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- EventsSection.tsx — FOUND and clean
- ArticlesSection.tsx — FOUND and clean
- Commit 0b06f04 — FOUND
- Commit ad4a14e — FOUND
