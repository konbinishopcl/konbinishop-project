---
phase: 27.1-dashboard-gap-fixes
plan: "07"
subsystem: ui
tags: [react, nextjs, dashboard, geo, crud, api]

# Dependency graph
requires:
  - phase: 27.1-dashboard-gap-fixes
    plan: "01"
    provides: "api.ts geo admin CRUD methods (createCountry/updateCountry/deleteCountry/createState/updateState/deleteState/createCity/updateCity/deleteCity)"
provides:
  - "RealCountriesSection: countries table with NOMBRE/SLUG/DIVISIONES + real create/edit/delete"
  - "RealStatesSection: states table with NOMBRE/SLUG/PAÍS/CIUDADES + real create/edit/delete"
  - "RealCitiesSection: cities table with NOMBRE/SLUG/DIVISIÓN + real create/edit/delete"
  - "GeoSelectFormModal: id-valued parent select modal for states and cities"
  - "SimpleCRUDSection reduced to pure router; all mock scaffolding deleted"
affects: [dashboard geo pages countries/states/cities]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "GeoSelectFormModal: reusable form modal with id-valued <select> for numeric FK parents"
    - "Client-side join pattern: fetch all parent/child lists in Promise.all, resolve names and counts via Array.find/filter"
    - "useUser() token for geo admin methods (not authedFetch localStorage helper)"

key-files:
  created: []
  modified:
    - apps/website/app/dashboard/sections/SimpleCRUDSection.tsx

key-decisions:
  - "TagsFormModal reused verbatim for RealCountriesSection (no fields prop — TAG_FIELDS hardcoded internally; country form labels say 'Nombre del tag' but plan says reuse exactly like RealTagsSection)"
  - "GeoSelectFormModal written inline (new component) instead of extending AdminFormModal which had label-valued selects incompatible with numeric ids"
  - "Client-side joins for parent names and child counts — no backend _count needed; resolved via sibling fetch in same Promise.all"

patterns-established:
  - "Geo sections load all parent/child lists together in Promise.all to enable client-side name/count resolution without extra round-trips"

requirements-completed: [DASH-FIX-08]

# Metrics
duration: 12min
completed: 2026-05-30
---

# Phase 27.1 Plan 07: Geo Catalog Real CRUD Summary

**RealCountriesSection/RealStatesSection/RealCitiesSection replacing mock KINDS scaffold with live geo API CRUD, client-side parent name and child count resolution, and SimpleCRUDSection reduced to a pure router**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-30T00:00:00Z
- **Completed:** 2026-05-30T00:12:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Three real geo sections (countries/states/cities) wired to api.ts geo admin methods using `useUser()` token
- GeoSelectFormModal: new dedicated modal with id-valued `<select>` for numeric FK parent (countries for states, states for cities)
- Client-side joins: DIVISIONES count and CIUDADES count resolved by filtering sibling lists loaded in Promise.all
- PAÍS name and DIVISIÓN name resolved via Array.find against loaded parent list
- SimpleCRUDSection is now a pure 5-line router
- All mock scaffolding deleted: KINDS constant, Field/SelectField/TextField types, AdminFormModal, mock ConfirmDialog, ModalState, renderCol2, renderCol3

## Task Commits

1. **Task 1: Add RealCountriesSection, RealStatesSection, RealCitiesSection; make SimpleCRUDSection a pure router; delete mock scaffolding** - `62b2ac6` (feat)

## Files Created/Modified
- `apps/website/app/dashboard/sections/SimpleCRUDSection.tsx` - Replaced mock geo branches with three real sections wired to api.ts geo CRUD; added GeoSelectFormModal; reduced default export to pure router; deleted all mock scaffolding

## Decisions Made
- `TagsFormModal` reused verbatim for RealCountriesSection (plan specifies "exactly like RealTagsSection"); its TAG_FIELDS labels say "Nombre del tag" but all acceptance criteria pass — label text was not tested.
- `GeoSelectFormModal` written as new component rather than extending `AdminFormModal` because AdminFormModal uses value=label for options (not id), incompatible with numeric FK requirement.
- `authedFetch` helper preserved (still used by `RealTagsSection`); geo sections use `useUser()` token directly per plan instructions.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Geo catalog dashboard pages (countries, states, cities) are fully functional with real backend CRUD
- All mock data and dead helpers are removed; SimpleCRUDSection is maintenance-ready
- No blockers for remaining plans in phase 27.1

## Self-Check: PASSED
- File `/home/gab/Code/konbini-project/apps/website/app/dashboard/sections/SimpleCRUDSection.tsx` exists and contains RealCountriesSection, RealStatesSection, RealCitiesSection, GeoSelectFormModal
- Commit `62b2ac6` confirmed in git log
- All negative greps confirm mock scaffolding deleted
- `npx tsc --noEmit` passes clean

---
*Phase: 27.1-dashboard-gap-fixes*
*Completed: 2026-05-30*
