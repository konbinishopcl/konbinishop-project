---
phase: 27.1-dashboard-gap-fixes
verified: 2026-05-30T00:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 27.1: Dashboard Gap Fixes — Verification Report

**Phase Goal:** Eliminar todos los datos mock y funcionalidades falsas que quedaron en el dashboard tras las fases 25-27: computar KPIs desde datos ya cargados (ingresos mes, total pagos, publicados, suscriptores); conectar catálogos geo (Países/Divisiones/Ciudades) a la API real; fijar breadcrumb para mostrar grupo padre en lugar de "DASHBOARD"; limpiar el modal de aprobación de eventos/artículos (quitar IA falsa); cargar config del plan desde /api/settings al montar SubsSection; renombrar columna "ORGANIZADOR" → "COMPRADOR" en pagos; computar top-organizadores desde pagos reales; hacer real "Ver toda →".

**Verified:** 2026-05-30
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | DASH-FIX-01: HomeSection KPIs computed from real API data | VERIFIED | `api.adminPayments`, `api.adminEvents`, `api.subscriptions` all called in `load()` Promise.all; state vars `monthRevenue`, `publishedTotal`, `subsTotal` set and rendered |
| 2 | DASH-FIX-02: Por categoría bars from real `/event-categories` `_count.events` | VERIFIED | `api.eventCategories()` called on mount; backend confirmed `include: { _count: { select: { events: true } } }` at catalog.service.ts lines 156/188; pct computed and rendered |
| 3 | DASH-FIX-03: "Ver toda →" navigates to /dashboard/events | VERIFIED | `<Link href="/dashboard/events" className="sel" ...>Ver toda →</Link>` in HomeSection line 184 |
| 4 | DASH-FIX-04: PaymentsSection KPIs computed from loaded rows; COMPRADOR column; fake buttons removed | VERIFIED | `useMemo` over `rows` produces `kpis.monthRevenue/historic/failed`; `COMPRADOR` column at line 104; no `Descargar`/`Reembolsar`/`REEMBOLSOS` strings remain |
| 5 | DASH-FIX-05: ReportsSection top-compradores from real PAID payments; eventos panel removed | VERIFIED | `topIngresos` useMemo groups by `p.buyer.name`; `TOP_INGRESOS`/`TOP_EVENTOS` constants gone; single panel renders "Top compradores (ingresos)" |
| 6 | DASH-FIX-06: SubsSection loads plan config from /api/settings on mount | VERIFIED | `useEffect([token])` fetches `/api/settings` with Bearer header, filters 4 plan keys, patches `plan` state via `setPlan((p) => ({ ...p, ...patch }))` |
| 7 | DASH-FIX-07: Breadcrumb shows nav group name (not always "DASHBOARD") | VERIFIED | `crumbPrefix = subPage?.crumb ?? \`${activeGroup} / ${cur.label.toUpperCase()}\`` at DashboardShell line 150; `{crumbPrefix}` rendered at line 210; no hardcoded "DASHBOARD /" string |
| 8 | DASH-FIX-08: Países/Divisiones/Ciudades wired to real API CRUD | VERIFIED | `RealCountriesSection`, `RealStatesSection`, `RealCitiesSection` defined in SimpleCRUDSection.tsx; all 9 api methods called (createCountry/updateCountry/deleteCountry/createState/updateState/deleteState/createCity/updateCity/deleteCity); SimpleCRUDSection routes to real sections at lines 749-751 |
| 9 | DASH-FIX-09: Approve modal in EventsSection/ArticlesSection has no fake AI | VERIFIED | `regenAI`, `Math.random`, `anime, cosplay`, `function AdminApproveModal` — none present in either file; `<ConfirmDialog title="¿Publicar este evento?">` and `<ConfirmDialog title="¿Publicar este artículo?">` confirmed with `onConfirm={() => { close(); doApprove(item); }}` |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Plan | Status | Details |
|----------|------|--------|---------|
| `apps/website/lib/api.ts` | 01 | VERIFIED | 9 geo admin CRUD methods present at lines 574-592; existing read-only methods untouched |
| `apps/website/app/dashboard/DashboardShell.tsx` | 02 | VERIFIED | `crumbPrefix` const at line 150; breadcrumb at line 210 uses it |
| `apps/website/app/dashboard/sections/SubsSection.tsx` | 02 | VERIFIED | `fetch("/api/settings")` with Authorization header in useEffect([token]) |
| `apps/website/app/dashboard/sections/HomeSection.tsx` | 03 | VERIFIED | Real KPIs, category bars, revenue chart from real data; `CHART_VALUES`/`CATEGORIES`/delta arrows all gone; `Link` for Ver toda |
| `apps/website/app/dashboard/sections/PaymentsSection.tsx` | 04 | VERIFIED | `useMemo` for KPIs; `FALLIDOS` KPI; `COMPRADOR` column; no fake buttons |
| `apps/website/app/dashboard/sections/ReportsSection.tsx` | 05 | VERIFIED | `topIngresos` useMemo at line 204; mock arrays deleted; single panel |
| `apps/website/app/dashboard/sections/EventsSection.tsx` | 06 | VERIFIED | `ConfirmDialog` for approve; no inline `AdminApproveModal`; `doApprove(item)` wired |
| `apps/website/app/dashboard/sections/ArticlesSection.tsx` | 06 | VERIFIED | `ConfirmDialog` for approve; no inline `AdminApproveModal`; `doApprove(item)` wired |
| `apps/website/app/dashboard/sections/SimpleCRUDSection.tsx` | 07 | VERIFIED | `RealCountriesSection`/`RealStatesSection`/`RealCitiesSection` defined; KINDS mock scaffolding deleted; no iso/flag columns |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| HomeSection `load()` | `api.adminPayments` + `api.subscriptions` + `api.adminEvents` + `api.eventCategories` | Promise.all on mount | WIRED | Line 38-43: all four calls in single Promise.all |
| HomeSection `categoryBars` | `/event-categories _count.events` | Backend include confirmed | WIRED | catalog.service.ts lines 156/188 include `_count.events` |
| HomeSection "Ver toda" | `/dashboard/events` | `<Link href=...>` (next/link) | WIRED | Line 184 |
| PaymentsSection KPIs | `rows` state | `useMemo` over loaded rows | WIRED | Line 47-59; rendered at lines 93-95 |
| ReportsSection `topIngresos` | `payments` state | `useMemo` grouping by `p.buyer.name` | WIRED | Line 204-213; rendered at lines 281-298 |
| SubsSection `useEffect` | `/api/settings` | `fetch` with `Authorization: Bearer` | WIRED | Lines 103-122 |
| DashboardShell `crumbPrefix` | `activeGroup` (existing const) | Nullish coalesce with `subPage?.crumb` | WIRED | Line 150; render at line 210 |
| `RealCountriesSection` | `api.createCountry/updateCountry/deleteCountry` | `useUser()` token | WIRED | Lines 343, 353, 363 |
| `RealStatesSection` | `api.createState/updateState/deleteState` | `useUser()` token | WIRED | Lines 477-494, 491, 505 |
| `RealCitiesSection` | `api.createCity/updateCity/deleteCity` | `useUser()` token | WIRED | Lines 629, 643, 657 |
| EventsSection approve modal | `doApprove(item)` | `ConfirmDialog onConfirm` | WIRED | Line 597: `onConfirm={() => { close(); doApprove(item); }}` |
| ArticlesSection approve modal | `doApprove(item)` | `ConfirmDialog onConfirm` | WIRED | Line 469: `onConfirm={() => { close(); doApprove(item); }}` |

### Requirements Coverage

| Requirement | Plan | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| DASH-FIX-01 | 03 | HomeSection real KPIs (ingresos, publicados, suscriptores) | SATISFIED | All 3 state vars + API calls verified |
| DASH-FIX-02 | 03 | Category bars from real event-categories _count | SATISFIED | API call + backend include confirmed |
| DASH-FIX-03 | 03 | "Ver toda →" navigates to /dashboard/events | SATISFIED | Link href confirmed |
| DASH-FIX-04 | 04 | PaymentsSection KPIs computed, COMPRADOR, fake buttons removed | SATISFIED | All sub-items confirmed |
| DASH-FIX-05 | 05 | ReportsSection top-compradores from real payments | SATISFIED | useMemo + render confirmed |
| DASH-FIX-06 | 02 | SubsSection loads plan config from /api/settings | SATISFIED | useEffect fetch confirmed |
| DASH-FIX-07 | 02 | DashboardShell breadcrumb shows group name | SATISFIED | crumbPrefix const + render confirmed |
| DASH-FIX-08 | 01+07 | Catálogos geo (Países/Divisiones/Ciudades) wired to real API | SATISFIED | 9 api.ts methods + 3 real sections confirmed |
| DASH-FIX-09 | 06 | Approve modal cleaned (no AI, no fake tags) | SATISFIED | No regenAI/Math.random/fake tags found |

### Anti-Patterns Found

No blocker anti-patterns detected.

The following `placeholder` strings were found in SimpleCRUDSection — these are `<input placeholder="...">` attribute values in form fields, not implementation stubs. They are correct UX placeholders (e.g., `placeholder="shonen"`, `placeholder="slug-automatico"`).

The `ORGANIZADOR` text found during scan appears in EventsSection events table, SpotsSection, and HeroesSection — these are unrelated to the PaymentsSection rename (DASH-FIX-04), which specifically targeted the payments buyer column. Those other table column names are outside this phase's scope.

### Human Verification Required

The following items cannot be fully confirmed programmatically but are low risk given the code evidence:

**1. Category bars display correctly when events exist**
Test: Log in as admin, navigate to /dashboard, check "Por categoría" panel
Expected: Non-zero bars proportional to event counts per category
Why human: _count.events backend confirmed present; client-side pct calculation verified; but actual visual rendering of proportional bars requires live data

**2. Breadcrumb shows correct group name per section**
Test: Navigate to COMERCIAL section, MODERACIÓN section, CONFIGURACIÓN section
Expected: "COMERCIAL / AVISOS", "MODERACIÓN / EVENTOS", etc. (not "DASHBOARD / ...")
Why human: crumbPrefix logic verified; but ADMIN_NAV group names are dynamic strings

**3. SubsSection prefills plan fields from saved settings**
Test: Navigate to /dashboard/subs; check if form shows previously saved values (not hardcoded defaults)
Expected: Fields reflect /api/settings values if any were previously saved
Why human: fetch+patch logic verified; but requires a populated /api/settings to observe the prefill

### TypeScript Check

`npx tsc --noEmit` run against `apps/website` — exited with no output (no errors). All new code in this phase compiles cleanly.

---

_Verified: 2026-05-30_
_Verifier: Claude (gsd-verifier)_
