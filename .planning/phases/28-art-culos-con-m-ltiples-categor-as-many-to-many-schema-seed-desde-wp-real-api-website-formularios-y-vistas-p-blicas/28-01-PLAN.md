---
phase: 28-articulos-multiples-categorias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "El modelo Article ya no tiene la columna articleCategoryId ni la relación FK articleCategory"
    - "Article tiene una relación implícita many-to-many articleCategories ArticleCategory[]"
    - "Existe la tabla pivot _ArticleToArticleCategory en la base de datos"
    - "El cliente Prisma regenerado expone article.articleCategories"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Article.articleCategories m2m + ArticleCategory.articles (sin articleCategoryId)"
      contains: "articleCategories ArticleCategory[]"
    - path: "apps/api/prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql"
      provides: "SQL hand-crafted que crea el pivot y elimina la FK"
      contains: "_ArticleToArticleCategory"
  key_links:
    - from: "schema.prisma Article"
      to: "schema.prisma ArticleCategory"
      via: "relación implícita m2m (Prisma genera _ArticleToArticleCategory)"
      pattern: "articleCategories ArticleCategory\\[\\]"
---

<objective>
Convertir la relación Article↔ArticleCategory de FK única (`articleCategoryId Int?`) a many-to-many
implícita de Prisma, replicando exactamente el patrón `ArticleTag` ya existente en el schema.

Purpose: Fundación bloqueante de Phase 28. Todo lo demás (seed, API, frontend) depende de que el
cliente Prisma regenerado exponga `article.articleCategories`. Hasta que esta migración aplique,
`seed.ts` y `articles.service.ts` quedan rotos a propósito — eso se arregla en Wave 2.
Output: schema.prisma actualizado, migración SQL hand-crafted aplicada y registrada, cliente regenerado.

IMPORTANTE — entorno no-interactivo: este proyecto NO usa `prisma migrate dev`. Las migraciones son
SQL escrito a mano, aplicado con `prisma migrate deploy` o registrado con `prisma migrate resolve --applied`
(ver STATE.md Phase 08-01 y Phase 18). Seguir el patrón de la migración sch08.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-CONTEXT.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-RESEARCH.md

<interfaces>
<!-- Estado actual del schema (líneas exactas) — extraído del codebase -->

apps/api/prisma/schema.prisma:
- Article (líneas 112-137): contiene
    articleCategory   ArticleCategory? @relation(fields: [articleCategoryId], references: [id])  // línea 124
    articleCategoryId Int?                                                                        // línea 125
    articleTags       ArticleTag[]                                                                // línea 126 (patrón m2m a replicar)
    @@index([articleCategoryId])                                                                 // línea 136
- ArticleCategory (líneas 86-97): YA contiene `articles Article[]` (línea 92) — porque hoy es el
  back-relation de la FK. Tras la migración este mismo campo pasa a ser el lado m2m. NO se borra ni
  se duplica; conserva su nombre `articles`.
- ArticleTag (líneas 99-108): blueprint del m2m implícito — `articles Article[]` (línea 103) + en
  Article `articleTags ArticleTag[]` (línea 126). El pivot _ArticleToArticleTag fue creado en sch08.

Migración de referencia (patrón SQL exacto del pivot):
apps/api/prisma/migrations/20260527213839_sch08_split_taxonomies/migration.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar schema.prisma — Article a many-to-many implícito</name>
  <read_first>
    - apps/api/prisma/schema.prisma (modelo Article líneas 112-137, ArticleCategory líneas 86-97, ArticleTag líneas 99-108)
  </read_first>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
    En el modelo `Article` (líneas 112-137):
    1. ELIMINAR la línea 124: `articleCategory   ArticleCategory? @relation(fields: [articleCategoryId], references: [id])`
    2. ELIMINAR la línea 125: `articleCategoryId Int?`
    3. ELIMINAR la línea 136: `@@index([articleCategoryId])`
    4. AGREGAR (donde estaba la relación de categoría, junto a `articleTags ArticleTag[]`):
       `articleCategories ArticleCategory[]`

    En el modelo `ArticleCategory` (líneas 86-97):
    - NO tocar la línea 92 `articles Article[]`. Ya existe y ahora es el lado m2m de la relación
      implícita (antes era el back-relation de la FK). Su nombre `articles` se conserva.

    Resultado esperado en Article: la línea de categoría es exactamente `articleCategories ArticleCategory[]`
    y NO debe quedar ninguna referencia a `articleCategoryId` ni `@@index([articleCategoryId])` en todo el archivo.
  </action>
  <verify>
    <automated>cd apps/api && grep -q "articleCategories ArticleCategory\[\]" prisma/schema.prisma && ! grep -q "articleCategoryId" prisma/schema.prisma && pnpm exec prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "articleCategoryId" apps/api/prisma/schema.prisma` devuelve 0
    - `grep "articleCategories ArticleCategory\[\]" apps/api/prisma/schema.prisma` devuelve 1 línea
    - `grep "articles    Article\[\]" apps/api/prisma/schema.prisma` (modelo ArticleCategory) sigue presente
    - `cd apps/api && pnpm exec prisma validate` exit 0
  </acceptance_criteria>
  <done>schema.prisma valida; Article usa m2m implícito; cero referencias a articleCategoryId.</done>
</task>

<task type="auto">
  <name>Task 2: Escribir migración SQL hand-crafted sch11 + registrar + regenerar cliente</name>
  <read_first>
    - apps/api/prisma/migrations/20260527213839_sch08_split_taxonomies/migration.sql (patrón pivot _ArticleToArticleTag)
    - apps/api/prisma/migrations/migration_lock.toml
  </read_first>
  <files>apps/api/prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql</files>
  <action>
    Crear el directorio `apps/api/prisma/migrations/20260529160000_sch11_article_category_m2m/` con un
    archivo `migration.sql` que contenga EXACTAMENTE este SQL (orden: A crear pivot → B FKs del pivot →
    C drop de la FK/índice/columna antigua):

    ```sql
    -- ── A) Create pivot table ──
    CREATE TABLE `_ArticleToArticleCategory` (
        `A` INTEGER NOT NULL,
        `B` INTEGER NOT NULL,
        UNIQUE INDEX `_ArticleToArticleCategory_AB_unique`(`A`, `B`),
        INDEX `_ArticleToArticleCategory_B_index`(`B`)
    ) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

    -- ── B) Add FKs for pivot (Prisma requires these) ──
    ALTER TABLE `_ArticleToArticleCategory`
      ADD CONSTRAINT `_ArticleToArticleCategory_A_fkey`
      FOREIGN KEY (`A`) REFERENCES `Article`(`id`) ON DELETE CASCADE ON UPDATE CASCADE;

    ALTER TABLE `_ArticleToArticleCategory`
      ADD CONSTRAINT `_ArticleToArticleCategory_B_fkey`
      FOREIGN KEY (`B`) REFERENCES `article_categories`(`id`) ON DELETE CASCADE ON UPDATE CASCADE;

    -- ── C) Drop the old FK column on Article ──
    ALTER TABLE `Article` DROP FOREIGN KEY `Article_articleCategoryId_fkey`;
    DROP INDEX `Article_articleCategoryId_idx` ON `Article`;
    ALTER TABLE `Article` DROP COLUMN `articleCategoryId`;
    ```

    Notas sobre nombres reales:
    - El nombre de la FK antigua de Prisma es `Article_articleCategoryId_fkey` (convención Prisma).
    - El índice antiguo es `Article_articleCategoryId_idx`. Si el ejecutor encuentra que el nombre real
      difiere, verificarlo con `SHOW CREATE TABLE Article;` y ajustar antes de aplicar.
    - La tabla de categorías se mapea a `article_categories` (ver `@@map` línea 96 del schema).

    Aplicar y registrar (entorno no-interactivo):
    - Intentar `cd apps/api && pnpm exec prisma migrate deploy` (aplica la migración pendiente).
    - Si la migración debe registrarse como ya aplicada sin re-ejecutar:
      `cd apps/api && pnpm exec prisma migrate resolve --applied 20260529160000_sch11_article_category_m2m`
    - SIEMPRE al final: `cd apps/api && pnpm exec prisma generate` para regenerar el cliente con
      `article.articleCategories`.
  </action>
  <verify>
    <automated>cd apps/api && ls prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql && grep -q "_ArticleToArticleCategory" prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql && pnpm exec prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `apps/api/prisma/migrations/20260529160000_sch11_article_category_m2m/migration.sql` existe
    - Contiene `CREATE TABLE \`_ArticleToArticleCategory\``
    - Contiene `ALTER TABLE \`Article\` DROP COLUMN \`articleCategoryId\``
    - `cd apps/api && pnpm exec prisma generate` exit 0
    - `grep -rq "articleCategories" apps/api/node_modules/.prisma/client/index.d.ts` (el cliente expone el campo)
  </acceptance_criteria>
  <done>Migración SQL existe, pivot creado, FK antigua eliminada, cliente Prisma regenerado.</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm exec prisma validate` → exit 0
- `cd apps/api && pnpm exec prisma generate` → exit 0
- `grep -c articleCategoryId apps/api/prisma/schema.prisma` → 0
- Cliente Prisma expone `article.articleCategories`

NOTA INTENCIONAL: Tras aplicar esta migración, `apps/api/src/articles/articles.service.ts` y
`apps/api/prisma/seed.ts` DEJAN de type-checkear (referencian el campo/include eliminado). Esto es
esperado y se corrige en Wave 2 (Plans 02 y 03). NO ejecutar `tsc --noEmit` del API como gate de este plan.
</verification>

<success_criteria>
- schema.prisma: Article usa `articleCategories ArticleCategory[]`, cero `articleCategoryId`
- Migración sch11 escrita a mano, aplicada/registrada, con pivot `_ArticleToArticleCategory`
- Cliente Prisma regenerado exponiendo la relación m2m
</success_criteria>

<output>
After completion, create `.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-01-SUMMARY.md`
</output>
