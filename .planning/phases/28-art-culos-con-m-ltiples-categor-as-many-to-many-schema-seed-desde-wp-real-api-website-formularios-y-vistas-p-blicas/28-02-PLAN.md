---
phase: 28-articulos-multiples-categorias
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps/api/prisma/update-article-categories.ts
  - apps/api/prisma/data/articles.json
  - apps/api/prisma/seed.ts
  - apps/api/prisma/export-wp-articles.ts
  - apps/api/prisma/recategorize-articles.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-08, D-12, D-13]
must_haves:
  truths:
    - "Existe un script idempotente que actualiza articles.json a categorySlugs[] sin tocar la DB"
    - "articles.json usa categorySlugs: string[] en vez de categorySlug: string | null"
    - "seed.ts conecta categorías por slug filtrando contra las categorías conocidas (sin P2025)"
    - "export-wp-articles.ts genera categorySlugs[] con catch-all de 3 ítems"
    - "recategorize-articles.ts ya no existe"
  artifacts:
    - path: "apps/api/prisma/update-article-categories.ts"
      provides: "Script JSON-only que cruza WP API por slug y produce categorySlugs[]"
      contains: "categorySlugs"
    - path: "apps/api/prisma/seed.ts"
      provides: "Loop de artículos con connect/set de articleCategories por slug filtrado"
      contains: "articleCategories"
  key_links:
    - from: "apps/api/prisma/seed.ts"
      to: "ArticleCategory (por slug)"
      via: "connect/set filtrado contra catBySlug"
      pattern: "articleCategories: knownSlugs"
---

<objective>
Reconstruir la capa de datos para múltiples categorías por artículo: crear el script de actualización
(`update-article-categories.ts`) que transforma `articles.json` a `categorySlugs[]`, actualizar `seed.ts`
para conectar categorías por slug (con filtro anti-P2025), actualizar `export-wp-articles.ts` para futuras
exportaciones, y eliminar `recategorize-articles.ts` (workaround obsoleto de la FK única).

Purpose: Sin esto, el seed no puede poblar la nueva relación m2m. Toda mutación de datos pasa por el
seeder (D-03), nunca directo a la DB.
Output: script nuevo, articles.json regenerado, seed.ts y export-wp actualizados, script obsoleto borrado.

Depende de Plan 01 (cliente Prisma regenerado con `article.articleCategories`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-CONTEXT.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-RESEARCH.md

<interfaces>
<!-- Referencias exactas extraídas del codebase -->

apps/api/prisma/seed.ts:
- Tipo de articlesData (líneas 291-303): hoy declara `categorySlug: string | null` (línea 298)
- catBySlug construido líneas 308-310 (mapa slug -> id de ArticleCategory en DB)
- Loop líneas 313-361:
    línea 314: `const articleCategoryId = art.categorySlug ? (catBySlug[art.categorySlug] ?? null) : null;`
    línea 335 (en update): `articleCategoryId,`
    línea 350 (en create): `articleCategoryId,`
    línea 337 (patrón tags, a imitar): `articleTags: tagIds.length ? { set: tagIds.map(id => ({ id })) } : undefined,`
    línea 354 (patrón tags create): `articleTags: tagIds.length ? { connect: tagIds.map(id => ({ id })) } : undefined,`

apps/api/prisma/export-wp-articles.ts:
- `cleanWpSlug` (líneas 170-176): slugify(decodeURIComponent(slug), {lower,strict,locale:'es'}) con try/catch
- CATCHALL actual (línea 282): `new Set(['cultura-otaku', 'uncategorized'])` — falta 'anime' (D-02/D-12)
- líneas 283-285: catSlugs mapeado, specific filtrado, `categorySlug = (specific[0] ?? catSlugs[0]) ?? null`
- línea 304: emite `categorySlug` (singular) en el objeto exportado
- fetch categorías (línea 217): `${WP_API}/categories?per_page=100&_fields=id,slug`
- fetch posts (línea 184): `_fields=id,slug,title,content,excerpt,date,categories,featured_media`

apps/api/prisma/recategorize-articles.ts: workaround que escribe directo a la DB — ELIMINAR (D-13).
apps/api/prisma/seed-article-categories.ts: NO TOCAR — seeder standalone de categorías, no afectado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear update-article-categories.ts + regenerar articles.json</name>
  <read_first>
    - apps/api/prisma/export-wp-articles.ts (cleanWpSlug líneas 170-176, fetch WP categorías/posts, catch-all)
    - apps/api/prisma/data/articles.json (estructura: array de objetos con campo `slug` y `categorySlug`)
  </read_first>
  <files>apps/api/prisma/update-article-categories.ts, apps/api/prisma/data/articles.json</files>
  <action>
    Crear `apps/api/prisma/update-article-categories.ts` — script idempotente, JSON-only, SIN conexión
    a la DB (D-01/D-03). Lógica:

    1. Constantes:
       - `const WP_API = 'https://konbinishop.com/wp-json/wp/v2';`
       - `const OUT_FILE = join(process.cwd(), 'prisma', 'data', 'articles.json');`
       - `const CATCHALL = new Set(['cultura-otaku', 'anime', 'uncategorized']);` (3 ítems — D-02)
    2. Copiar la función `cleanWpSlug` EXACTA desde export-wp-articles.ts (líneas 170-176):
       `slugify(decodeURIComponent(wpSlug), { lower: true, strict: true, locale: 'es' })` con try/catch
       que cae a `slugify(wpSlug, {...})`.
    3. Cargar articles.json -> `Map` por `a.slug` (los slugs en JSON ya están normalizados por cleanWpSlug).
    4. Fetch WP categorías: `${WP_API}/categories?per_page=100&_fields=id,slug` -> mapa `{ [id]: slug }`.
    5. Fetch TODOS los posts WP paginando: `${WP_API}/posts?per_page=100&page=N&status=publish&_fields=id,slug,categories`
       (NO bajar imágenes ni contenido — solo id,slug,categories).
    6. Para cada post: `const normalizedSlug = cleanWpSlug(post.slug);` (PITFALL 2 — el WP slug crudo puede
       traer %c3%b3 etc; sin normalizar no cruza). Buscar el artículo en el Map por normalizedSlug; si no
       existe, continuar.
    7. Derivar categorías: `catSlugs = (post.categories ?? []).map(id => wpCatSlug[id]).filter(Boolean)`.
       Catch-all (D-02): `const specific = catSlugs.filter(s => !CATCHALL.has(s)); const categorySlugs =
       specific.length > 0 ? specific : catSlugs;` (preservar orden WP — Open Q#2 discreción).
    8. Asignar `article.categorySlugs = categorySlugs;` y `delete article.categorySlug;` (reemplaza el campo viejo).
    9. Escribir `writeFileSync(OUT_FILE, JSON.stringify(articles, null, 2), 'utf-8')`.

    TOLERANCIA A WP CAÍDO (Research Open Q#1): envolver los fetch en try/catch. Si la WP API no responde,
    el script debe loggear un warning claro (`WP API no disponible — articles.json sin cambios`) y salir
    con exit 0 SIN sobrescribir el JSON. El JSON commiteado prevalece. El script es idempotente y re-ejecutable.

    Ejecutar el script para regenerar articles.json:
    `cd apps/api && pnpm exec ts-node prisma/update-article-categories.ts`
    Si WP no responde en este entorno, el ejecutor debe convertir articles.json igual mediante un
    one-off (o el propio script en modo offline): cada objeto debe pasar de `"categorySlug": "X"` /
    `"categorySlug": null` a `"categorySlugs": [...]` (un array; usar `["X"]` cuando había un slug, `[]`
    cuando era null), eliminando la clave `categorySlug`. El resultado final de articles.json NO debe
    contener ninguna clave `categorySlug` (singular).
  </action>
  <verify>
    <automated>cd apps/api && ls prisma/update-article-categories.ts && grep -q "categorySlugs" prisma/update-article-categories.ts && grep -q "'cultura-otaku', 'anime', 'uncategorized'" prisma/update-article-categories.ts && node -e "const a=require('./prisma/data/articles.json'); if(a.some(x=>'categorySlug' in x)) process.exit(1); if(!a.some(x=>'categorySlugs' in x)) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/update-article-categories.ts` existe
    - Contiene `new Set(['cultura-otaku', 'anime', 'uncategorized'])`
    - Contiene `cleanWpSlug` y `categorySlugs`
    - Ningún objeto de articles.json tiene la clave `categorySlug` (singular)
    - Al menos un objeto de articles.json tiene la clave `categorySlugs` (array)
    - articles.json es JSON válido (`node -e "require('./apps/api/prisma/data/articles.json')"` exit 0)
  </acceptance_criteria>
  <done>Script creado e idempotente; articles.json migrado a categorySlugs[] sin la clave singular.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar seed.ts — connect/set de articleCategories por slug filtrado</name>
  <read_first>
    - apps/api/prisma/seed.ts (tipo articlesData líneas 291-303, catBySlug 308-310, loop 313-361)
    - apps/api/prisma/data/articles.json (ahora con categorySlugs[])
  </read_first>
  <files>apps/api/prisma/seed.ts</files>
  <action>
    1. Tipo de articlesData (línea 298): cambiar `categorySlug: string | null;` por `categorySlugs: string[];`
    2. Eliminar línea 314: `const articleCategoryId = art.categorySlug ? (catBySlug[art.categorySlug] ?? null) : null;`
       Reemplazar por el filtro anti-P2025 (PITFALL 7 — WP tiene más categorías que la lista curada de
       seed; un slug desconocido lanza P2025 y el try/catch SALTA EL ARTÍCULO ENTERO = pérdida de datos):
       `const knownSlugs = (art.categorySlugs ?? []).filter(s => catBySlug[s] !== undefined);`
    3. En el bloque `update` (línea 335): eliminar `articleCategoryId,` y agregar:
       `articleCategories: { set: knownSlugs.map(slug => ({ slug })) },`
    4. En el bloque `create` (línea 350): eliminar `articleCategoryId,` y agregar:
       `articleCategories: knownSlugs.length ? { connect: knownSlugs.map(slug => ({ slug })) } : undefined,`

    NO auto-crear categorías al vuelo como se hace con tags (PITFALL 7): ArticleCategory tiene `nameJa`
    requerido para el MegaMenu; categorías auto-creadas tendrían nameJa null y contaminarían la navegación.
    El filtro `knownSlugs` simplemente omite slugs no curados — comportamiento seguro y deseado.

    Resultado: cero referencias a `articleCategoryId` ni `art.categorySlug` en seed.ts.
  </action>
  <verify>
    <automated>cd apps/api && ! grep -q "articleCategoryId" prisma/seed.ts && ! grep -q "art\.categorySlug\b" prisma/seed.ts && grep -q "knownSlugs" prisma/seed.ts && grep -q "articleCategories:" prisma/seed.ts && grep -q "categorySlugs: string\[\]" prisma/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "articleCategoryId" apps/api/prisma/seed.ts` devuelve 0
    - `grep "categorySlugs: string\[\]" apps/api/prisma/seed.ts` presente (tipo actualizado)
    - `grep "knownSlugs" apps/api/prisma/seed.ts` presente (filtro anti-P2025)
    - `grep "articleCategories: { set:" apps/api/prisma/seed.ts` presente
    - `grep "articleCategories: knownSlugs.length ? { connect:" apps/api/prisma/seed.ts` presente
  </acceptance_criteria>
  <done>seed.ts conecta categorías por slug con filtro anti-P2025; sin referencias a la FK antigua.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar export-wp-articles.ts (D-12) + eliminar recategorize-articles.ts (D-13)</name>
  <read_first>
    - apps/api/prisma/export-wp-articles.ts (CATCHALL línea 282, líneas 283-285, emisión categorySlug línea 304)
  </read_first>
  <files>apps/api/prisma/export-wp-articles.ts, apps/api/prisma/recategorize-articles.ts</files>
  <action>
    En `export-wp-articles.ts` (D-12 — para futuras re-exportaciones):
    1. Línea 282: cambiar `const CATCHALL = new Set(['cultura-otaku', 'uncategorized']);` por
       `const CATCHALL = new Set(['cultura-otaku', 'anime', 'uncategorized']);` (agregar 'anime' — D-02)
    2. Líneas 283-285: reemplazar la lógica de slug único por array:
       `const catSlugs = (post.categories ?? []).map((id: number) => wpCatSlug[id]).filter(Boolean) as string[];`
       `const specific = catSlugs.filter(s => !CATCHALL.has(s));`
       `const categorySlugs = specific.length > 0 ? specific : catSlugs;`
       (eliminar la línea que producía `const categorySlug = (specific.length > 0 ? specific[0] : catSlugs[0]) ?? null;`)
    3. Línea 304: en el objeto exportado, cambiar `categorySlug,` por `categorySlugs,`

    Eliminar el archivo obsoleto (D-13): `rm apps/api/prisma/recategorize-articles.ts`
    (era un workaround que escribía directo a la DB para la FK única; ya no aplica con m2m + seed-only).
  </action>
  <verify>
    <automated>cd apps/api && ! test -f prisma/recategorize-articles.ts && grep -q "'cultura-otaku', 'anime', 'uncategorized'" prisma/export-wp-articles.ts && grep -q "categorySlugs," prisma/export-wp-articles.ts && ! grep -qE "^\s*categorySlug," prisma/export-wp-articles.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/prisma/recategorize-articles.ts` falla (archivo eliminado)
    - `grep "'cultura-otaku', 'anime', 'uncategorized'" apps/api/prisma/export-wp-articles.ts` presente
    - `grep "categorySlugs," apps/api/prisma/export-wp-articles.ts` presente (emite el array)
    - El objeto exportado ya no emite `categorySlug,` (singular)
  </acceptance_criteria>
  <done>export-wp emite categorySlugs[] con catch-all de 3 ítems; recategorize-articles.ts eliminado.</done>
</task>

</tasks>

<verification>
- articles.json: cero claves `categorySlug` singular, presencia de `categorySlugs` array, JSON válido
- seed.ts: cero `articleCategoryId`, usa `knownSlugs` + `articleCategories` connect/set
- export-wp-articles.ts: catch-all de 3 ítems, emite `categorySlugs`
- recategorize-articles.ts no existe

NOTA SOBRE RE-SEED (verificación manual, NO automatizada en este plan): correr el seed completo
(`cd apps/api && pnpm exec ts-node prisma/seed.ts`) es DESTRUCTIVO (deleteMany + reconstruye 437 artículos)
y requiere la migración de Plan 01 ya aplicada. Es una verificación manual/checkpoint a nivel de fase
(ver 28-VALIDATION.md), no un gate automatizado de este plan.
</verification>

<success_criteria>
- update-article-categories.ts existe, idempotente, JSON-only, tolerante a WP caído
- articles.json migrado a categorySlugs[] (sin la clave singular)
- seed.ts conecta categorías por slug con filtro anti-P2025
- export-wp-articles.ts actualizado (D-12); recategorize-articles.ts eliminado (D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-02-SUMMARY.md`
</output>
