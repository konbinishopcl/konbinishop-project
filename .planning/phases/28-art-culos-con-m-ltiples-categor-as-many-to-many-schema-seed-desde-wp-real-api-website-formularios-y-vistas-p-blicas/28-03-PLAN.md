---
phase: 28-articulos-multiples-categorias
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps/api/src/articles/articles.service.ts
  - apps/api/src/articles/dto/create-article.dto.ts
  - apps/api/src/articles/dto/update-article.dto.ts
  - apps/api/src/articles/dto/create-sponsored-article.dto.ts
autonomous: true
requirements: [D-05, D-06, D-07]
must_haves:
  truths:
    - "Los 3 DTOs aceptan articleCategoryIds?: number[] en vez de articleCategoryId?: number"
    - "El include de artículos devuelve articleCategories (array) en vez de articleCategory (objeto)"
    - "El filtro ?articleCategory=slug funciona vía articleCategories: { some: { slug } }"
    - "create/update/createSponsored conectan/setean categorías múltiples"
    - "El API type-checkea (tsc --noEmit verde)"
  artifacts:
    - path: "apps/api/src/articles/articles.service.ts"
      provides: "include m2m, where some, connect/set de articleCategories"
      contains: "articleCategories: true"
    - path: "apps/api/src/articles/dto/create-article.dto.ts"
      provides: "articleCategoryIds?: number[]"
      contains: "articleCategoryIds"
  key_links:
    - from: "articles.service.ts findAll where"
      to: "Prisma articleCategories relation"
      via: "some filter por slug"
      pattern: "articleCategories: \\{ some: \\{ slug"
---

<objective>
Actualizar la capa API de artículos a la relación many-to-many: los 3 DTOs cambian
`articleCategoryId?: number` por `articleCategoryIds?: number[]` (D-05); el service cambia el `include`
de `articleCategory: true` a `articleCategories: true` (D-07), el `where` del filtro a
`articleCategories: { some: { slug } }` (D-06, misma interfaz externa `?articleCategory=slug`), y
create/update/createSponsored conectan/setean categorías múltiples.

Purpose: Sin esto el API no compila (el cliente Prisma de Plan 01 ya no expone `articleCategory`) y el
frontend no recibe `articleCategories[]`.
Output: service + 3 DTOs actualizados; API type-checkea.

Depende de Plan 01 (cliente Prisma regenerado). Corre en paralelo con Plan 02 (sin overlap de archivos:
02 toca prisma/, 03 toca src/articles/). El gate de `tsc --noEmit` del API completo solo pasa cuando
AMBOS planes (02 y 03) han aterrizado — por eso el verify de cada tarea de este plan se enfoca en grep +
tsc del propio servicio, y el `tsc --noEmit` completo es gate de cierre de wave.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-CONTEXT.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-RESEARCH.md

<interfaces>
<!-- Estado actual del service y DTOs (líneas exactas) — extraído del codebase -->

apps/api/src/articles/articles.service.ts:
- ARTICLE_INCLUDE (líneas 23-29): `articleTags: true,` (24) + `articleCategory: true,` (25)
- ARTICLE_DETAIL_INCLUDE (líneas 30-): `articleTags: true,` (31) + `articleCategory: true,` (32)
- findAll where (línea 69): `...(query.articleCategory && { articleCategory: { slug: query.articleCategory } }),`
  (la línea 68 es el equivalente para tags: `articleTags: { some: { slug: query.articleTag } }` — patrón a imitar)
- create (línea 148): `articleCategory: dto.articleCategoryId ? { connect: { id: dto.articleCategoryId } } : undefined,`
  (línea 147 tags: `articleTags: dto.articleTagIds?.length ? { connect: dto.articleTagIds.map((id) => ({ id })) } : undefined,`)
- update (líneas 172-179): tags usan `articleTags: { set: incomingTagIds.map(...) }` (172);
  categoría usa (174-178): `...(dto.articleCategoryId !== undefined && { articleCategory: dto.articleCategoryId ? { connect: { id } } : { disconnect: true } })`
- createSponsored (línea 215): `articleCategory: dto.articleCategoryId ? { connect: { id: dto.articleCategoryId } } : undefined,`

DTOs (campo de categoría a cambiar, mismo patrón que articleTagIds que ya es number[]):
- create-article.dto.ts líneas 45-48: `@IsOptional() @IsInt() articleCategoryId?: number;`
- update-article.dto.ts líneas 44-47: idéntico
- create-sponsored-article.dto.ts líneas 41-43: idéntico
(articleTagIds en cada DTO usa: `@IsOptional() @IsArray() @IsInt({ each: true }) articleTagIds?: number[];` — patrón exacto a copiar)

apps/api/src/articles/dto/query-articles.dto.ts: el filtro `articleCategory?: string` (slug) NO cambia
— la interfaz externa se conserva (D-06). NO tocar este archivo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar los 3 DTOs — articleCategoryId -> articleCategoryIds: number[]</name>
  <read_first>
    - apps/api/src/articles/dto/create-article.dto.ts (líneas 39-48: articleTagIds patrón + articleCategoryId)
    - apps/api/src/articles/dto/update-article.dto.ts
    - apps/api/src/articles/dto/create-sponsored-article.dto.ts
  </read_first>
  <files>apps/api/src/articles/dto/create-article.dto.ts, apps/api/src/articles/dto/update-article.dto.ts, apps/api/src/articles/dto/create-sponsored-article.dto.ts</files>
  <action>
    En CADA uno de los 3 DTOs, reemplazar el bloque de categoría singular:
    ```
    @ApiPropertyOptional({ example: 3, description: 'ID de la categoría del artículo (Phase 18+, opcional)' })
    @IsOptional()
    @IsInt()
    articleCategoryId?: number;
    ```
    por el bloque array (mismo patrón que `articleTagIds` ya presente en cada archivo):
    ```
    @ApiPropertyOptional({ type: [Number], example: [3, 5], description: 'IDs de categorías del artículo (many-to-many)' })
    @IsOptional()
    @IsArray()
    @IsInt({ each: true })
    articleCategoryIds?: number[];
    ```
    Verificar que `@IsArray` ya esté importado en cada archivo (lo está, porque articleTagIds lo usa).
    Resultado: cero `articleCategoryId?` y un `articleCategoryIds?: number[]` por archivo.
  </action>
  <verify>
    <automated>cd apps/api && for f in create-article update-article create-sponsored-article; do grep -q "articleCategoryIds?: number\[\]" src/articles/dto/$f.dto.ts || exit 1; grep -q "articleCategoryId?:" src/articles/dto/$f.dto.ts && exit 1; done; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 archivos contienen `articleCategoryIds?: number[]`
    - Ninguno de los 3 contiene `articleCategoryId?:` (singular)
    - Cada uno conserva la decoración `@IsArray() @IsInt({ each: true })` sobre el nuevo campo
  </acceptance_criteria>
  <done>Los 3 DTOs aceptan articleCategoryIds?: number[] con validación de array.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Actualizar articles.service.ts — include, where, create/update/createSponsored</name>
  <read_first>
    - apps/api/src/articles/articles.service.ts (ARTICLE_INCLUDE 23-29, ARTICLE_DETAIL_INCLUDE 30+, findAll where 63-70, create 147-150, update 164-180, createSponsored 213-218)
    - apps/api/src/articles/dto/create-article.dto.ts (campo articleCategoryIds tras Task 1)
  </read_first>
  <files>apps/api/src/articles/articles.service.ts</files>
  <action>
    1. ARTICLE_INCLUDE (línea 25): cambiar `articleCategory: true,` por `articleCategories: true,` (D-07).
    2. ARTICLE_DETAIL_INCLUDE (línea 32): cambiar `articleCategory: true,` por `articleCategories: true,` (D-07).
       AMBOS includes deben actualizarse (PITFALL: olvidar uno deja el detalle roto).
    3. findAll where (línea 69): cambiar
       `...(query.articleCategory && { articleCategory: { slug: query.articleCategory } }),`
       por
       `...(query.articleCategory && { articleCategories: { some: { slug: query.articleCategory } } }),`
       (D-06 — la interfaz externa `?articleCategory=slug` no cambia, solo la lógica Prisma).
    4. create (línea 148): cambiar
       `articleCategory: dto.articleCategoryId ? { connect: { id: dto.articleCategoryId } } : undefined,`
       por
       `articleCategories: dto.articleCategoryIds?.length ? { connect: dto.articleCategoryIds.map((id) => ({ id })) } : undefined,`
    5. update (líneas 174-178): reemplazar el bloque condicional de categoría singular
       `...(dto.articleCategoryId !== undefined && { articleCategory: dto.articleCategoryId ? { connect: { id: dto.articleCategoryId } } : { disconnect: true } }),`
       por (patrón `set` igual que articleTags — para m2m se usa `set`, NUNCA `disconnect: true`):
       `...(dto.articleCategoryIds !== undefined && { articleCategories: { set: dto.articleCategoryIds.map((id) => ({ id })) } }),`
    6. createSponsored (línea 215): cambiar
       `articleCategory: dto.articleCategoryId ? { connect: { id: dto.articleCategoryId } } : undefined,`
       por
       `articleCategories: dto.articleCategoryIds?.length ? { connect: dto.articleCategoryIds.map((id) => ({ id })) } : undefined,`

    Resultado: cero `articleCategory` (singular) y cero `articleCategoryId` en el service.
  </action>
  <verify>
    <automated>cd apps/api && { grep -q "articleCategory:" src/articles/articles.service.ts && exit 1; grep -q "articleCategoryId" src/articles/articles.service.ts && exit 1; [ "$(grep -c 'articleCategories: true' src/articles/articles.service.ts)" = "2" ] && grep -q "articleCategories: { some: { slug: query.articleCategory } }" src/articles/articles.service.ts && grep -q "articleCategories: { set: dto.articleCategoryIds" src/articles/articles.service.ts && echo ok; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "articleCategories: true" apps/api/src/articles/articles.service.ts` devuelve 2 (ambos includes)
    - `grep "articleCategories: { some: { slug: query.articleCategory } }" ...service.ts` presente
    - `grep "articleCategories: { set: dto.articleCategoryIds" ...service.ts` presente (update m2m)
    - `grep -c "articleCategory:" apps/api/src/articles/articles.service.ts` devuelve 0 (sin la relación singular; NO confundir con el query param `query.articleCategory` que se conserva por D-06)
    - `cd apps/api && pnpm exec tsc --noEmit` NO reporta errores en articles.service.ts ni en los 3 DTOs
  </acceptance_criteria>
  <done>articles.service.ts usa include m2m en ambos includes, where some, connect/set de categorías; sin singular.</done>
</task>

</tasks>

<verification>
- Los 3 DTOs: `articleCategoryIds?: number[]`, sin `articleCategoryId?`
- articles.service.ts: ambos includes con `articleCategories: true`, where `some`, create/update/createSponsored m2m
- Gate de cierre de wave (cuando Plans 02 y 03 hayan aterrizado):
  `cd apps/api && pnpm exec tsc --noEmit` → exit 0
  `cd apps/api && pnpm test --no-coverage` → verde
- Verificación manual (E2E, ver 28-VALIDATION.md): `curl "/api/articles?articleCategory=naruto"` devuelve
  artículos con esa categoría en su array `articleCategories`.
</verification>

<success_criteria>
- 3 DTOs migrados a articleCategoryIds?: number[]
- Service con include m2m (ambos), filtro some, create/update/createSponsored conectando categorías múltiples
- API type-checkea (gate de cierre de wave junto con Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-03-SUMMARY.md`
</output>
