---
phase: 28-articulos-multiples-categorias
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - apps/website/lib/api.ts
  - apps/website/components/ArticleCard.tsx
  - apps/website/app/(site)/noticias/NoticiasHubView.tsx
  - apps/website/app/(site)/noticias/[slug]/ArticleView.tsx
  - apps/website/components/SearchLightbox.tsx
  - apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx
  - apps/website/app/(site)/noticias/categoria/[slug]/NewsCategoryView.tsx
autonomous: true
requirements: [D-09]
must_haves:
  truths:
    - "ApiArticle.articleCategory (objeto) cambia a articleCategories: ApiArticleCategory[]"
    - "getCat() lee la primera categoría del array articleCategories[0]"
    - "Los rails del hub agrupan por articleCategories[0].slug"
    - "Todas las vistas de display compilan (website tsc verde)"
  artifacts:
    - path: "apps/website/lib/api.ts"
      provides: "ApiArticle.articleCategories: ApiArticleCategory[]"
      contains: "articleCategories: ApiArticleCategory[]"
    - path: "apps/website/components/ArticleCard.tsx"
      provides: "getCat() usando articleCategories[0]"
      contains: "articleCategories"
  key_links:
    - from: "NoticiasHubView rail grouping"
      to: "a.articleCategories[0].slug"
      via: "agrupación por primera categoría"
      pattern: "articleCategories\\?\\.\\[0\\]\\?\\.slug"
---

<objective>
Migrar el tipo y todos los componentes de DISPLAY del website a la relación many-to-many: cambiar
`ApiArticle.articleCategory` (objeto) por `articleCategories: ApiArticleCategory[]` en lib/api.ts, y
actualizar todos los sitios de lectura para usar `articleCategories[0]` (primera categoría — D-09).
NO se muestran multi-badges (deferido).

Purpose: Tras Plan 03 la API responde `articleCategories[]`. Este es el cambio de tipo fundacional del
website: en cuanto aterriza `lib/api.ts`, todo sitio que leía `articleCategory` deja de compilar — por
eso este plan actualiza el tipo Y todos sus consumidores de display en un solo plan.
Output: lib/api.ts + 6 archivos de display actualizados; website type-checkea.

Depende de Plan 03 (forma de respuesta del API). Plan 05 (formularios + admin list) depende de ESTE plan
porque importa `ApiArticle`/`ApiArticleCategory` de lib/api.ts — por eso Plan 05 es wave 4.

DECISIÓN DE DISEÑO (minimizar churn): se conserva el nombre exportado `getCat` (NO renombrar a
`primaryCategory` como sugería la research). `getCat` ya es importado por ambos NewsCategoryView; cambiar
solo sus internos cubre esas dos vistas automáticamente sin tocar imports. Fallback se mantiene "Noticias".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-CONTEXT.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-RESEARCH.md
@.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-UI-SPEC.md

<interfaces>
<!-- Estado actual (líneas exactas) — extraído del codebase -->

apps/website/lib/api.ts:
- ApiArticleCategory (líneas 253-260): tipo existente, NO cambia
- ApiArticle (líneas 273-289): línea 284 `articleCategory: ApiArticleCategory | null;` <- cambiar a array
- api.articles() y api.article() (líneas 557-561): query param `articleCategory?: string` NO cambia (D-06)

apps/website/components/ArticleCard.tsx:
- getCat exportado (líneas 18-23):
    `if (a.articleCategory?.name) return a.articleCategory.name;` (19) <- cambiar a articleCategories[0]
    líneas 20-22 (tags fallback) y 23 (`return "Noticias";`) se conservan
- uso en línea 113: `<div className="a-cat">{getCat(a)}</div>` (no cambia)

apps/website/app/(site)/noticias/NoticiasHubView.tsx (PITFALL 4 — rails agrupan por articleCategory):
- línea 30: `const slug = a.articleCategory?.slug;` <- a.articleCategories?.[0]?.slug
- línea 42: `.filter((a) => a.articleCategory?.slug === slug)` <- a.articleCategories?.[0]?.slug === slug
- línea 102: `DESTACADO · {(featured.articleCategory?.name ?? "NOTICIAS").toUpperCase()}` <- articleCategories?.[0]?.name
- línea 136: `{(a.articleCategory?.name ?? "NOTICIAS").toUpperCase()}` <- articleCategories?.[0]?.name
- línea 189: `{sponsored.articleCategory?.name ?? "NOTICIAS"} · PATROCINADO` <- articleCategories?.[0]?.name

apps/website/app/(site)/noticias/[slug]/ArticleView.tsx:
- getCat inline (líneas 26-27): `if (a.articleCategory?.name) return a.articleCategory.name;` <- articleCategories[0]
- uso líneas 96 y 286 (no cambian, llaman a getCat local)

apps/website/components/SearchLightbox.tsx:
- línea 81: `meta: [a.articleCategory?.name, a.createdAt?.slice(0, 10)]...` <- a.articleCategories?.[0]?.name

apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx  (plural — ruta real)
apps/website/app/(site)/noticias/categoria/[slug]/NewsCategoryView.tsx   (singular — segunda ruta misma vista)
- Ambas importan `getCat` de ArticleCard (línea 5) y lo usan (línea ~409/411). NO referencian
  `a.articleCategory` directamente — al actualizar los internos de getCat quedan cubiertas. El
  `articleCategory: category.slug` (líneas 88/90) es un query param a api.articles() y NO cambia (D-06).
  Solo verificar que compilan tras el cambio de tipo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cambiar el tipo ApiArticle + getCat (lib/api.ts + ArticleCard.tsx)</name>
  <read_first>
    - apps/website/lib/api.ts (ApiArticle líneas 273-289, ApiArticleCategory 253-260)
    - apps/website/components/ArticleCard.tsx (getCat líneas 18-23)
  </read_first>
  <files>apps/website/lib/api.ts, apps/website/components/ArticleCard.tsx</files>
  <action>
    En `lib/api.ts` ApiArticle (línea 284): cambiar
    `articleCategory: ApiArticleCategory | null;`
    por
    `articleCategories: ApiArticleCategory[];`
    (ApiArticleCategory ya existe, no se crea nada nuevo).

    En `ArticleCard.tsx` getCat (líneas 18-23): cambiar la línea 19
    `if (a.articleCategory?.name) return a.articleCategory.name;`
    por
    `if (a.articleCategories?.[0]?.name) return a.articleCategories[0].name;`
    Conservar el resto: fallback a articleTags[0].name, luego tags[0].name, luego `return "Noticias";` (D-09 fallback).
  </action>
  <verify>
    <automated>cd apps/website && grep -q "articleCategories: ApiArticleCategory\[\]" lib/api.ts && { grep -qE "articleCategory:" lib/api.ts && false || true; } ; grep -q "a.articleCategories?.\[0\]?.name" components/ArticleCard.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep "articleCategories: ApiArticleCategory\[\]" apps/website/lib/api.ts` presente
    - ApiArticle ya no contiene `articleCategory: ApiArticleCategory | null`
    - `grep "a.articleCategories?.\[0\]?.name" apps/website/components/ArticleCard.tsx` presente
    - getCat conserva el fallback `return "Noticias"`
  </acceptance_criteria>
  <done>ApiArticle usa articleCategories[]; getCat lee la primera categoría con fallback Noticias.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar consumidores de display (NoticiasHubView, ArticleView, SearchLightbox)</name>
  <read_first>
    - apps/website/app/(site)/noticias/NoticiasHubView.tsx (líneas 30, 42, 102, 136, 189)
    - apps/website/app/(site)/noticias/[slug]/ArticleView.tsx (getCat inline líneas 26-27)
    - apps/website/components/SearchLightbox.tsx (línea 81)
  </read_first>
  <files>apps/website/app/(site)/noticias/NoticiasHubView.tsx, apps/website/app/(site)/noticias/[slug]/ArticleView.tsx, apps/website/components/SearchLightbox.tsx</files>
  <action>
    NoticiasHubView.tsx — reemplazar las 5 referencias a `articleCategory` por `articleCategories?.[0]`
    (PITFALL 4 — los rails agrupan por categoría; si no se actualiza, renderean vacío):
    - línea 30: `const slug = a.articleCategories?.[0]?.slug;`
    - línea 42: `.filter((a) => a.articleCategories?.[0]?.slug === slug)`
    - línea 102: `DESTACADO · {(featured.articleCategories?.[0]?.name ?? "NOTICIAS").toUpperCase()}`
    - línea 136: `{(a.articleCategories?.[0]?.name ?? "NOTICIAS").toUpperCase()}`
    - línea 189: `{sponsored.articleCategories?.[0]?.name ?? "NOTICIAS"} · PATROCINADO`

    ArticleView.tsx — getCat inline (líneas 26-27): cambiar línea 27
    `if (a.articleCategory?.name) return a.articleCategory.name;`
    por
    `if (a.articleCategories?.[0]?.name) return a.articleCategories[0].name;`
    (conservar el resto del helper inline igual).

    SearchLightbox.tsx — línea 81: cambiar `a.articleCategory?.name` por `a.articleCategories?.[0]?.name`.

    Resultado: ningún archivo de website referencia `a.articleCategory` (singular) en lectura de datos.
  </action>
  <verify>
    <automated>cd apps/website && ! grep -rq "\.articleCategory?\." app/\(site\)/noticias components/SearchLightbox.tsx && grep -q "articleCategories?.\[0\]?.slug" "app/(site)/noticias/NoticiasHubView.tsx" && pnpm exec tsc --noEmit && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "\.articleCategory?\." apps/website/app/(site)/noticias apps/website/components/SearchLightbox.tsx` total 0
    - `grep "articleCategories?.\[0\]?.slug" apps/website/app/(site)/noticias/NoticiasHubView.tsx` presente (rail grouping)
    - ArticleView.tsx getCat usa `a.articleCategories?.[0]?.name`
    - `cd apps/website && pnpm exec tsc --noEmit` exit 0 (incluye ambos NewsCategoryView que importan getCat)
  </acceptance_criteria>
  <done>Todos los sitios de display usan articleCategories[0]; website type-checkea; rails agrupan por primera categoría.</done>
</task>

</tasks>

<verification>
- `cd apps/website && pnpm exec tsc --noEmit` → exit 0 (gate completo del website tras este plan)
- Cero referencias a `.articleCategory?.` (singular) en código de display del website
- Verificación manual (UI, ver 28-VALIDATION.md): `/noticias` muestra el badge de categoría en cada card
  (primera categoría) y los rails por categoría renderean artículos.
</verification>

<success_criteria>
- ApiArticle migrado a articleCategories[]
- getCat + todos los consumidores de display usan articleCategories[0] con fallback "Noticias" (D-09)
- Rails del hub agrupan por primera categoría (PITFALL 4 resuelto)
- Website type-checkea
</success_criteria>

<output>
After completion, create `.planning/phases/28-art-culos-con-m-ltiples-categor-as-many-to-many-schema-seed-desde-wp-real-api-website-formularios-y-vistas-p-blicas/28-04-SUMMARY.md`
</output>
