---
phase: 28.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/(site)/categoria/[cat]/CategoryView.tsx
  - apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx
  - apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "No 'Ciudad' dropdown appears in the CategoryView filter bar"
    - "No 'Tipo' dropdown appears in the NewsCategoryView filter bar"
    - "No 'Tipo' dropdown appears in the TagArticlesView filter bar"
    - "The 'Origen' filter (Editoriales/Patrocinados) still works in both news views"
    - "The active filter count and clearAll/clearFilters functions no longer reference the removed filters"
  artifacts:
    - path: "apps/website/app/(site)/categoria/[cat]/CategoryView.tsx"
      provides: "Filter bar without Ciudad dropdown"
      min_lines: 200
    - path: "apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx"
      provides: "Filter bar without Tipo dropdown"
      min_lines: 300
    - path: "apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx"
      provides: "Filter bar without Tipo dropdown"
      min_lines: 280
  key_links:
    - from: "CategoryView.tsx activeCount"
      to: "clearFilters function"
      via: "filterPrice, filterFormat, dateRange, filterQuick only"
      pattern: "filterCity"
    - from: "NewsCategoryView.tsx activeCount"
      to: "clearAll function"
      via: "origen, period, q only (no type)"
      pattern: "setType"
---

<objective>
Remove the broken "Ciudad" filter from CategoryView and the broken "Tipo" filter from NewsCategoryView and TagArticlesView. These filters show UI but never actually filter data (Article has no `type` field; filterCity was never applied to `filtered[]`). The deletion must be complete: state vars, constants, render blocks, and activeCount/clearAll references must all be removed to avoid lint errors and dangling refs.

Purpose: Eliminates misleading UI that implies functionality which doesn't exist. The filters are dead code.
Output: Three clean filter bars with working filters only; no dead state or unused constants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Ciudad filter from CategoryView.tsx</name>
  <files>apps/website/app/(site)/categoria/[cat]/CategoryView.tsx</files>
  <read_first>
    - apps/website/app/(site)/categoria/[cat]/CategoryView.tsx (full file — identify all usages of filterCity, CITY_OPTIONS, and the Ciudad dropdown block in the filter bar)
  </read_first>
  <action>
Remove the following items completely:

1. **Module-level constant** (line 19):
   ```typescript
   const CITY_OPTIONS = ["Todas las ciudades", "Santiago", "Valparaíso", "Concepción", "Antofagasta", "Viña del Mar"];
   ```

2. **State variable** in component (line 28):
   ```typescript
   const [filterCity, setFilterCity] = useState("Todas las ciudades");
   ```

3. **In `clearFilters` function** (line 53): remove the `setFilterCity("Todas las ciudades")` call.

4. **In `activeCount` computation** (around line 61): remove the `+ (filterCity !== "Todas las ciudades" ? 1 : 0)` term.

5. **The entire "Ciudad" dropdown block in the JSX** (lines 138–162): remove the `<div style={{ position: "relative" }}>` block that contains the Ciudad button and its CITY_OPTIONS dropdown menu. The Precio dropdown that follows it should remain.

After removal, the filter bar should have: Fecha, Precio, Formato dropdowns (all working), and the quick chips (Hoy/Esta semana/Este mes). The activeCount should count only: filterPrice, filterQuick, filterFormat, hasDateRange.
  </action>
  <acceptance_criteria>
    - `grep -c "CITY_OPTIONS\|filterCity\|setFilterCity" apps/website/app/\(site\)/categoria/\[cat\]/CategoryView.tsx` returns 0
    - `grep -c "filterPrice\|filterFormat\|filterQuick" apps/website/app/\(site\)/categoria/\[cat\]/CategoryView.tsx` returns at least 3 (working filters still present)
    - File compiles without TypeScript errors (verified in verify step)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && pnpm --filter website build 2>&1 | tail -10</automated>
  </verify>
  <done>CategoryView has no Ciudad dropdown, no filterCity state, no CITY_OPTIONS; activeCount and clearFilters updated; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Tipo filter from NewsCategoryView.tsx and TagArticlesView.tsx</name>
  <files>apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx, apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx</files>
  <read_first>
    - apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx (full file — locate TYPE_OPTIONS const at line 35, type state at line 71, setType in clearAll at line 154, type term in activeCount at line 146, and the Pop id="type" block in JSX around line 256)
    - apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx (full file — same structure, TYPE_OPTIONS at line 35, type state at line 60, setType in clearAll at line 133, and Pop id="type" in JSX around line 227)
  </read_first>
  <action>
Apply the SAME changes to BOTH files:

**In each file, remove:**

1. **Module-level constant** (line 35 in both):
   ```typescript
   const TYPE_OPTIONS = ["Todos", "Artículos", "Entrevistas", "Reseñas"] as const;
   ```

2. **State variable** in component (line 71 in NewsCategoryView, line 60 in TagArticlesView):
   ```typescript
   const [type, setType] = useState<string>("Todos");
   ```

3. **In `clearAll` function**: remove the `setType("Todos")` call. Keep `setOrigen("Todos")`, `setPeriod(null)`, `setQ("")`, `setPage(1)`.

4. **In `activeCount` computation**: remove the `+ (type !== "Todos" ? 1 : 0)` term. Keep origen, period, and q.trim() terms.

5. **The `<Pop id="type" ...>` JSX block** in the filter bar: remove the entire Pop component call for "Tipo" (the one with `id="type"` and `options={TYPE_OPTIONS}`). Keep `<Pop id="origen" ...>` (Origen filter — works correctly).

After removal, the filter bar in both views should have: period chips (Hoy/Esta semana), Origen dropdown, Search, Grid/Lista toggle, Sort — all of which work.

IMPORTANT: Do NOT touch the `noticias/categoria/[slug]` path (singular) — that is a redirect stub, not a view. Only modify the files listed above.
  </action>
  <acceptance_criteria>
    - `grep -c "TYPE_OPTIONS\|setType\|\"type\"\|type !==\|type !==" apps/website/app/\(site\)/noticias/categorias/\[slug\]/NewsCategoryView.tsx` returns 0
    - `grep -c "TYPE_OPTIONS\|setType" apps/website/app/\(site\)/noticias/tags/\[slug\]/TagArticlesView.tsx` returns 0
    - `grep -c "id=\"origen\"\|setOrigen\|ORIGEN_OPTIONS" apps/website/app/\(site\)/noticias/categorias/\[slug\]/NewsCategoryView.tsx` returns at least 2 (Origen filter preserved)
    - `grep -c "id=\"origen\"\|setOrigen\|ORIGEN_OPTIONS" apps/website/app/\(site\)/noticias/tags/\[slug\]/TagArticlesView.tsx` returns at least 2 (Origen filter preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/gab/Code/konbini-project && pnpm --filter website build 2>&1 | tail -10</automated>
  </verify>
  <done>Both NewsCategoryView and TagArticlesView have no Tipo dropdown, no TYPE_OPTIONS, no type state; Origen filter intact; activeCount and clearAll updated; build passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pnpm --filter website build` passes — no TypeScript errors
2. grep zero:
   - `grep -c "CITY_OPTIONS\|filterCity" apps/website/app/(site)/categoria/[cat]/CategoryView.tsx` → 0
   - `grep -c "TYPE_OPTIONS\|setType" apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx` → 0
   - `grep -c "TYPE_OPTIONS\|setType" apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx` → 0
3. grep positive (Origen filter still present):
   - `grep -c "ORIGEN_OPTIONS" apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx` → at least 1
   - `grep -c "ORIGEN_OPTIONS" apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx` → at least 1
</verification>

<success_criteria>
- Ciudad dropdown completely removed from CategoryView (state, constant, JSX, activeCount, clearFilters)
- Tipo dropdown completely removed from both NewsCategoryView and TagArticlesView (state, constant, JSX, activeCount, clearAll)
- Origen filter (Editoriales/Patrocinados) preserved and untouched in both news views
- All three files build without TypeScript errors
- No dangling references to removed state vars
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-sitio-p-blico-gap-fixes-faqs-desde-api-real-filtros-rotos-en-categor-a-y-noticias-autor-real-en-art-culos/28.1-03-SUMMARY.md`
</output>
