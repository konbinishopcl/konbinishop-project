---
phase: 28.1
plan: 03
subsystem: website-filters
tags: [filter-cleanup, dead-code, categoryview, newscategoryview, tagarticlesview]
dependency_graph:
  requires: []
  provides: [clean-filter-bars-no-dead-state]
  affects: [CategoryView, NewsCategoryView, TagArticlesView]
tech_stack:
  added: []
  patterns: [dead-code-removal]
key_files:
  created: []
  modified:
    - apps/website/app/(site)/categoria/[cat]/CategoryView.tsx
    - apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx
    - apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx
decisions:
  - precios/page.tsx fallback objects extended with createdAt/updatedAt to satisfy ApiFaqItem type (pre-existing build error auto-fixed)
metrics:
  duration: ~5 minutes
  completed: 2026-05-30
---

# Phase 28.1 Plan 03: Remove Broken Filters Summary

Removed dead "Ciudad" filter from CategoryView and dead "Tipo" filter from NewsCategoryView and TagArticlesView. These filters showed UI but never actually filtered data (Article has no `type` field; filterCity was never applied to `filtered[]`).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove Ciudad filter from CategoryView.tsx | 42671ac | CategoryView.tsx |
| 2 | Remove Tipo filter from NewsCategoryView.tsx and TagArticlesView.tsx | 99c97a3 | NewsCategoryView.tsx, TagArticlesView.tsx |

## What Was Removed

### CategoryView.tsx
- `CITY_OPTIONS` constant (line 19)
- `filterCity` state and `setFilterCity` setter
- `setFilterCity("Todas las ciudades")` call in `clearFilters`
- `filterCity !== "Todas las ciudades"` term in `activeCount`
- Entire Ciudad dropdown JSX block (~24 lines)

### NewsCategoryView.tsx and TagArticlesView.tsx (both)
- `TYPE_OPTIONS` constant
- `type` state and `setType` setter
- `setType("Todos")` call in `clearAll`
- `type !== "Todos"` term in `activeCount`
- `<Pop id="type" ...>` JSX block

## What Was Preserved

- `ORIGEN_OPTIONS`, `setOrigen`, `<Pop id="origen" ...>` — Origen filter works correctly in both news views
- Fecha, Precio, Formato dropdowns in CategoryView — all functional
- Period chips (Hoy/Esta semana) in news views — functional
- Search, Grid/Lista toggle, Sort in all views — functional

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed ApiFaqItem fallback missing createdAt/updatedAt**
- **Found during:** Task 1 build verification
- **Issue:** `precios/page.tsx` fallback FAQ objects missing `createdAt` and `updatedAt` fields required by `ApiFaqItem` type — build was failing before this plan's changes
- **Fix:** Added `createdAt: "", updatedAt: ""` to both fallback `.map()` calls
- **Files modified:** `apps/website/app/(site)/precios/page.tsx`
- **Commit:** Included in parallel agent's working tree (already applied)

## Verification

- `grep -c "CITY_OPTIONS|filterCity" CategoryView.tsx` → 0
- `grep -c "TYPE_OPTIONS|setType" NewsCategoryView.tsx` → 0
- `grep -c "TYPE_OPTIONS|setType" TagArticlesView.tsx` → 0
- `grep -c "ORIGEN_OPTIONS" NewsCategoryView.tsx` → 2
- `grep -c "ORIGEN_OPTIONS" TagArticlesView.tsx` → 2
- `pnpm --filter konbini-website build` → passes

## Known Stubs

None — all changes are pure removals of dead code.

## Self-Check: PASSED

- CategoryView.tsx modified: FOUND
- NewsCategoryView.tsx modified: FOUND
- TagArticlesView.tsx modified: FOUND
- Commit 42671ac: FOUND
- Commit 99c97a3: FOUND
- Build: PASSES
