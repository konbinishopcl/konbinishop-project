---
phase: 28.1-sitio-publico-gap-fixes
verified: 2026-05-30T00:00:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
---

# Phase 28.1: Sitio Publico Gap Fixes Verification Report

**Phase Goal:** Eliminar datos mock y funcionalidades rotas del sitio publico: (1) quitar el filtro "Ciudad" de `/categoria/[cat]`; (2) quitar el filtro "Tipo" de `/noticias/categoria/[slug]` y `/noticias/tags/[slug]`; (3) conectar `/preguntas-frecuentes` y la seccion FAQ de `/precios` a `GET /faq` real con fallback; (4) agregar `user` a `ARTICLE_DETAIL_INCLUDE` y derivar `authorName` real en `ArticleView.tsx`.

**Verified:** 2026-05-30
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                      | Status     | Evidence                                                                                            |
|----|------------------------------------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------------|
| 1  | Article detail page shows the author's real name when the article has a user                               | VERIFIED   | ArticleView.tsx line 95–100: IIFE derives `${u.firstname} ${u.lastname}`.trim()                     |
| 2  | Article detail page shows "Konbini Editorial" when no user or blank names                                  | VERIFIED   | Same IIFE: returns "Konbini Editorial" when `!u` or full.trim() === ""                              |
| 3  | ARTICLE_DETAIL_INCLUDE in the backend includes user with firstname+lastname                                 | VERIFIED   | articles.service.ts line 36: `user: { select: { firstname: true, lastname: true } }`               |
| 4  | /preguntas-frecuentes renders FAQs from GET /faq when the API responds                                     | VERIFIED   | preguntas-frecuentes/page.tsx line 28: `faqs = await api.faqAll()`                                  |
| 5  | /preguntas-frecuentes renders the hardcoded fallback array when the API is down                            | VERIFIED   | page.tsx line 30–38: catch block maps FAQS_FALLBACK tuples to ApiFaqItem objects                   |
| 6  | /precios FAQ section renders real FAQs from GET /faq with the same fallback behavior                       | VERIFIED   | precios/page.tsx line 41–44: separate faqAll() call with catch fallback                             |
| 7  | Both FAQ pages render a consistent single FAQ shape (not mixed tuples vs objects)                          | VERIFIED   | Both pages render using `item.question`/`item.answer`; no `q[0]`/`q[1]` tuple indexing             |
| 8  | No "Ciudad" dropdown appears in the CategoryView filter bar                                                | VERIFIED   | grep for CITY_OPTIONS/filterCity/setFilterCity in CategoryView.tsx returns 0 matches                |
| 9  | No "Tipo" dropdown appears in the NewsCategoryView filter bar                                              | VERIFIED   | grep for TYPE_OPTIONS/setType in NewsCategoryView.tsx returns 0 matches                             |
| 10 | No "Tipo" dropdown appears in the TagArticlesView filter bar                                               | VERIFIED   | grep for TYPE_OPTIONS/setType in TagArticlesView.tsx returns 0 matches                              |
| 11 | The "Origen" filter (Editoriales/Patrocinados) still works in both news views                              | VERIFIED   | ORIGEN_OPTIONS + setOrigen + Pop id="origen" present in both files (4+ occurrences each)            |
| 12 | The active filter count and clearAll/clearFilters no longer reference the removed filters                  | VERIFIED   | NewsCategoryView activeCount: origen/period/q only; TagArticlesView: same; CategoryView: price/quick/format only |

**Score:** 12/12 truths verified

---

### Required Artifacts

| Artifact                                                                          | Provides                                              | Status     | Details                                                                   |
|-----------------------------------------------------------------------------------|-------------------------------------------------------|------------|---------------------------------------------------------------------------|
| `apps/api/src/articles/articles.service.ts`                                       | ARTICLE_DETAIL_INCLUDE with user field                | VERIFIED   | Line 36: `user: { select: { firstname: true, lastname: true } }`          |
| `apps/website/lib/api.ts`                                                         | ApiArticle type with optional user field              | VERIFIED   | Line 290: `user?: { firstname: string; lastname: string } | null`         |
| `apps/website/app/(site)/noticias/[slug]/ArticleView.tsx`                         | Dynamic authorName derived from article.user          | VERIFIED   | Lines 95–100: IIFE with null guard and blank-names fallback               |
| `apps/website/app/(site)/preguntas-frecuentes/page.tsx`                           | Async SSR page calling api.faqAll() with fallback     | VERIFIED   | async function; faqAll() at line 28; FAQS_FALLBACK at line 12             |
| `apps/website/app/(site)/precios/page.tsx`                                        | Async SSR page fetching faqs separately from settings | VERIFIED   | faqAll() at line 41; separate try/catch from settings Promise.all         |
| `apps/website/app/(site)/precios/PricingView.tsx`                                 | PricingView accepting faqs prop, FAQS constant removed | VERIFIED  | Props type has `faqs: ApiFaqItem[]`; renders via item.question/item.answer |
| `apps/website/app/(site)/categoria/[cat]/CategoryView.tsx`                        | Filter bar without Ciudad dropdown                    | VERIFIED   | CITY_OPTIONS/filterCity/setFilterCity: 0 occurrences; working filters: 18+ |
| `apps/website/app/(site)/noticias/categorias/[slug]/NewsCategoryView.tsx`         | Filter bar without Tipo dropdown                      | VERIFIED   | TYPE_OPTIONS/setType: 0 occurrences; ORIGEN_OPTIONS intact                |
| `apps/website/app/(site)/noticias/tags/[slug]/TagArticlesView.tsx`                | Filter bar without Tipo dropdown                      | VERIFIED   | TYPE_OPTIONS/setType: 0 occurrences; ORIGEN_OPTIONS intact                |

---

### Key Link Verification

| From                                  | To                                          | Via                                                   | Status   | Details                                                                  |
|---------------------------------------|---------------------------------------------|-------------------------------------------------------|----------|--------------------------------------------------------------------------|
| articles.service.ts ARTICLE_DETAIL_INCLUDE | ArticleView.tsx authorName                 | user select → ApiArticle.user → IIFE                  | WIRED    | Backend includes user; type extended; ArticleView reads article.user     |
| preguntas-frecuentes/page.tsx         | /faq API endpoint                           | api.faqAll() in async server component                | WIRED    | faqAll() called at line 28; result rendered via item.question/item.answer |
| precios/page.tsx                      | PricingView.tsx                             | faqs prop passed from page to PricingView             | WIRED    | page.tsx line 47: `faqs={faqs}`; PricingView destructures and renders    |
| CategoryView.tsx activeCount          | clearFilters function                       | filterPrice, filterFormat, filterQuick only           | WIRED    | activeCount lines 57–60 references only price/quick/format; no filterCity |
| NewsCategoryView.tsx activeCount      | clearAll function                           | origen, period, q only (no type)                      | WIRED    | activeCount lines 143–146; clearAll lines 148–153; no setType            |
| TagArticlesView.tsx activeCount       | clearAll function                           | origen, period, q only (no type)                      | WIRED    | activeCount lines 124–127; clearAll line 130; no setType                 |

---

### Requirements Coverage

No requirement IDs declared in any plan's frontmatter — all plans have `requirements: []`. Coverage is goal-driven only, verified above.

---

### Anti-Patterns Found

No blockers or warnings found. Specific checks:

- No `TODO/FIXME/PLACEHOLDER` comments in modified files
- No empty handler stubs in filter removal files
- No tuple indexing (`q[0]`, `q[1]`) remaining in any FAQ-rendering file
- `const authorName = "Konbini Editorial"` hardcoded string: 0 occurrences (removed as planned)
- Module-level `const FAQS` in PricingView.tsx: 0 occurrences (removed as planned)
- `CITY_OPTIONS`/`filterCity`: 0 occurrences in CategoryView.tsx
- `TYPE_OPTIONS`/`setType`: 0 occurrences in both news view files

One notable implementation detail: the `ApiFaqItem` type has `createdAt`/`updatedAt` fields not mentioned in the plan's interface block. Both pages correctly include `createdAt: ""` and `updatedAt: ""` in their fallback objects to satisfy the type. This is a correct and complete implementation.

---

### Human Verification Required

The following behaviors require running the application to confirm end-to-end:

#### 1. Article author name display on real data

**Test:** Open an article detail page for an article known to have a real `userId` (e.g., a recently imported/created article).
**Expected:** Author name shows "Gabriel Burgos" (or the actual user's name), not "Konbini Editorial".
**Why human:** Requires a live API response confirming the user relation is returned from the backend and rendered correctly.

#### 2. FAQ pages showing live API data

**Test:** Open `/preguntas-frecuentes` and `/precios` with the API running.
**Expected:** FAQ items match the database content (count and text differ from the hardcoded fallback).
**Why human:** Requires a live API connection; SSR renders server-side and cannot be grepped.

#### 3. Filter bars have correct dropdown counts

**Test:** Open `/categoria/anime` and confirm no "Ciudad" dropdown; open `/noticias/categorias/anime` and confirm no "Tipo" dropdown.
**Expected:** Ciudad is gone from events category; Tipo is gone from news category; Origen still appears in news views.
**Why human:** Visual confirmation of rendered filter bar state.

---

## Gaps Summary

No gaps found. All four goal items are implemented, wired, and substantive:

1. **Ciudad filter removal** — `CITY_OPTIONS`, `filterCity`, and all related JSX/state in CategoryView.tsx fully removed.
2. **Tipo filter removal** — `TYPE_OPTIONS`, `type` state, and all related JSX/state in NewsCategoryView.tsx and TagArticlesView.tsx fully removed. Origen filter preserved in both.
3. **FAQ pages wired to API** — Both `/preguntas-frecuentes` and `/precios` are async SSR, call `api.faqAll()`, normalize to `ApiFaqItem` object shape, and fall back to hardcoded arrays on error/empty response. PricingView refactored to accept `faqs` as prop.
4. **Real author name in articles** — `ARTICLE_DETAIL_INCLUDE` includes `user` with firstname/lastname; `ApiArticle` typed with optional `user?` field; `ArticleView` derives `authorName` via IIFE with proper null and blank-name guards.

---

_Verified: 2026-05-30_
_Verifier: Claude (gsd-verifier)_
