---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/seeders/articles.ts
  - apps/strapi/seeders/heroes.ts
  - apps/strapi/seeders/spots.ts
  - apps/strapi/seeders/cleanup.ts
  - apps/strapi/src/index.ts
autonomous: true
requirements: [QUICK-SEEDERS]
must_haves:
  truths:
    - "All content types except Event have seeders"
    - "Seeders follow the existing konbini pattern (TS files with inline data, exported async functions)"
    - "Running APP_RUN_SEEDERS=true seeds article, hero, and spot data"
    - "Cleanup script removes seeded data for new content types too"
  artifacts:
    - path: "apps/strapi/seeders/articles.ts"
      provides: "Article seed data and populate function"
    - path: "apps/strapi/seeders/heroes.ts"
      provides: "Hero seed data and populate function"
    - path: "apps/strapi/seeders/spots.ts"
      provides: "Spot seed data and populate function"
  key_links:
    - from: "apps/strapi/src/index.ts"
      to: "apps/strapi/seeders/*.ts"
      via: "import + await in bootstrap"
      pattern: "import populate.* from.*seeders"
---

<objective>
Create seeders for all Strapi content types that don't have them yet (article, hero, spot), excluding Event per user request. Stats has no schema so it is skipped.

Purpose: Populate the database with sample/seed data for development and testing.
Output: Three new seeder files + updated index.ts bootstrap + updated cleanup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/strapi/seeders/categories.ts (existing seeder pattern to follow)
@apps/strapi/seeders/tags.ts (existing seeder pattern to follow)
@apps/strapi/seeders/regions.ts (existing seeder pattern with relations)
@apps/strapi/seeders/cleanup.ts (cleanup to update)
@apps/strapi/src/index.ts (bootstrap to update)
@apps/strapi/src/api/article/content-types/article/schema.json
@apps/strapi/src/api/hero/content-types/hero/schema.json
@apps/strapi/src/api/spot/content-types/spot/schema.json

<interfaces>
<!-- Existing seeder pattern from this project (NOT private-project JSON pattern) -->
<!-- Each seeder: TS file with inline data array, exports async function taking strapi -->

From apps/strapi/seeders/categories.ts:
```typescript
const categoriesData = [
  { name: "...", slug: "...", description: "..." },
];
const populateCategories = async (strapi: any) => { ... };
export default populateCategories;
```

From apps/strapi/src/index.ts:
```typescript
import populateCategories from "../seeders/categories";
// ... called in bootstrap:
await populateCategories(strapi);
```

Content type schemas (text fields only, skip media fields):

Article: title (string, required), slug (uid from title, required), excerpt (text), content (richtext, required), tags (relation manyToMany)
Hero: title (string, required), slug (uid from title), date (date, required), address (string, required), address_number (string, required), venue (string, required), link (string), region (relation oneToOne), commune (relation oneToOne), categories (relation manyToMany), expiration_date (date, required)
Spot: title (string), image (media), link (string), expiration_date (date)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seeder files for article, hero, and spot</name>
  <files>apps/strapi/seeders/articles.ts, apps/strapi/seeders/heroes.ts, apps/strapi/seeders/spots.ts</files>
  <action>
Create three new seeder files following the EXACT same pattern as the existing `categories.ts` and `tags.ts` seeders in this project:
- Each file has an inline data array at the top
- Each file exports a default async function `populate{ContentType}(strapi: any)`
- Uses `strapi.entityService.findMany` to check existence (filter by slug)
- Uses `strapi.entityService.create` to create new entries
- Logs progress with console.log

**articles.ts:**
- Data: 5-8 sample articles about gaming/anime/geek culture events (matching the konbini theme)
- Fields to seed: title, slug (use slugify or manually set), excerpt, content (use a realistic paragraph of richtext markdown)
- Do NOT seed: image (media), tags relation, events relation (these require existing media/entities)
- Check existence by slug

**heroes.ts:**
- Data: 3-5 sample hero banners for upcoming fictional events
- Fields to seed: title, slug, date (future dates like "2026-06-15"), address, address_number, venue, link, expiration_date (future date after event date)
- Do NOT seed: desktop_image, tablet_image, mobile_image, thumbnail (media fields), region, commune, categories (relations -- would need IDs from seeded data)
- NOTE: Hero schema has date, address, address_number, venue, expiration_date as required. Set reasonable sample values.
- Check existence by slug (generate slug from title using slugify import, same as regions.ts does)

**spots.ts:**
- Data: 3-5 sample advertising spots
- Fields to seed: title, link, expiration_date (future date)
- Do NOT seed: image (media)
- Check existence by title (spot has no slug field), use `strapi.entityService.findMany("api::spot.spot", { filters: { title: spot.title } })`

Import slugify in heroes.ts (already a dependency used in regions.ts).
  </action>
  <verify>
    <automated>ls -la apps/strapi/seeders/articles.ts apps/strapi/seeders/heroes.ts apps/strapi/seeders/spots.ts && npx tsc --noEmit --project apps/strapi/tsconfig.json 2>&1 | head -20 || echo "TS check done"</automated>
  </verify>
  <done>Three seeder files exist with sample data arrays and export default async functions matching the existing pattern</done>
</task>

<task type="auto">
  <name>Task 2: Update cleanup.ts and index.ts to include new seeders</name>
  <files>apps/strapi/seeders/cleanup.ts, apps/strapi/src/index.ts</files>
  <action>
**cleanup.ts** -- Add deletion of articles, heroes, and spots BEFORE the existing deletions (since articles might reference tags, delete articles first). Update the order to:
1. Delete all articles (may reference tags/events)
2. Delete all heroes (may reference regions/communes/categories)
3. Delete all spots
4. (existing) Delete all communes
5. (existing) Delete all categories
6. (existing) Delete all tags
7. (existing) Delete all regions

Add each with `strapi.db.query('api::article.article').deleteMany({ where: {} })` pattern, matching exactly how commune/category/tag/region deletions are written. Update the total count at the bottom to include new counts.

**index.ts** -- Add imports and calls for the three new seeders:
- `import populateArticles from "../seeders/articles";`
- `import populateHeroes from "../seeders/heroes";`
- `import populateSpots from "../seeders/spots";`

Add the calls AFTER the existing seeders (regions, categories, tags) since articles/heroes might reference categories/tags/regions:
```
await populateArticles(strapi);
console.log("Articles seeded successfully");

await populateHeroes(strapi);
console.log("Heroes seeded successfully");

await populateSpots(strapi);
console.log("Spots seeded successfully");
```
  </action>
  <verify>
    <automated>grep -c "populateArticles\|populateHeroes\|populateSpots" apps/strapi/src/index.ts && grep -c "article\|hero\|spot" apps/strapi/seeders/cleanup.ts</automated>
  </verify>
  <done>index.ts imports and calls all 6 seeders (categories, regions, tags, articles, heroes, spots). cleanup.ts deletes all 7 content types (articles, heroes, spots, communes, categories, tags, regions) in correct dependency order.</done>
</task>

</tasks>

<verification>
- All seeder files exist in apps/strapi/seeders/
- index.ts imports and calls all seeders in correct order
- cleanup.ts handles all seeded content types
- Code follows existing project patterns (not private-project JSON pattern)
- No media fields are seeded (would require actual file uploads)
- Event content type has NO seeder (excluded per requirement)
</verification>

<success_criteria>
- 3 new seeder files created: articles.ts, heroes.ts, spots.ts
- cleanup.ts updated with deletion for article, hero, spot
- index.ts updated to import and call all new seeders
- All content types except Event and Stats have seeders
- Pattern matches existing categories.ts/tags.ts/regions.ts style exactly
</success_criteria>

<output>
After completion, create `.planning/quick/260327-x7o-revisa-en-private-project-como-se-crean-/260327-x7o-SUMMARY.md`
</output>
