---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/seed.ts
  - apps/api/prisma/migrations/
  - apps/api/.env
  - apps/api/.env.example
  - apps/api/package.json
requirements: [QUICK-LOCAL-USERS]
must_haves:
  truths:
    - "La API ya NO usa Neon ni Neon Auth — la base es PostgreSQL local (WSL)"
    - "Existe un modelo User propio con email, passwordHash y un enum Role de 3 valores: SUPER_ADMIN, ADMIN, AUTHENTICATED"
    - "Las relaciones de Event a usuario (userId, approvedById, rejectedById) son FK reales contra User"
    - "El modelo Profile (que existía para Neon Auth) fue eliminado; sus campos se absorbieron en User"
    - "El schema ya no tiene directUrl (era específico de Neon)"
    - "La migración aplica limpia contra la base local y el seed crea un usuario por cada rol"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Modelo User + enum Role; Event con FK reales a User; sin Profile ni directUrl"
    - path: "apps/api/prisma/seed.ts"
      provides: "Seed de usuarios (uno por rol) con passwordHash bcrypt"
    - path: "apps/api/.env"
      provides: "DATABASE_URL apuntando a PostgreSQL local"
---

<objective>
Reemplazar Neon Auth (y Neon como base de datos) por un sistema de usuarios local en la API NestJS/Prisma.

Purpose: el usuario descartó Neon Auth y Neon. Se necesita un modelo de usuarios propio con 3 roles, sobre PostgreSQL local.
Output: schema con User + Role, base local migrada, seed con un usuario por rol.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/prisma/schema.prisma (estado actual: postgresql + directUrl Neon, modelo Profile)
@apps/api/prisma/seed.ts (seed actual con Profiles)
@.planning/quick/260520-q4m-exportar-strapi-a-prisma-neon-auth/260520-q4m-SUMMARY.md (decisión de Neon Auth que esto reemplaza)

<decisions>
- DB local: rol/base `konbini` ya existentes en el PostgreSQL 16 de WSL (creados en sesión previa).
- User reemplaza a Profile: email (unique), passwordHash (bcrypt), firstname, lastname, rut, isCompany, role, confirmed, blocked.
- enum Role { SUPER_ADMIN, ADMIN, AUTHENTICATED }, default AUTHENTICATED.
- Event.userId / approvedById / rejectedById pasan de String (ids Neon Auth) a Int FK contra User.
- Se elimina directUrl del datasource (era para el pooler de Neon).
- Se regenera la carpeta de migraciones desde cero (la migración previa apuntaba a Neon; no hay datos reales que preservar).
- Endpoints de auth (registro/login/JWT/guards) quedan FUERA de este quick — son el follow-up.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema — User + Role, quitar Profile/directUrl, FK en Event</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
Quitar directUrl del datasource. Reemplazar el modelo Profile por enum Role + modelo User.
Cambiar Event.userId/approvedById/rejectedById de String? a Int? con relaciones nombradas
(EventOwner/EventApprover/EventRejecter) hacia User. Agregar índices de las nuevas FK.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project/apps/api && yarn prisma validate 2>&1 | tail -3'</automated>
  </verify>
  <done>schema válido; User + Role presentes; sin Profile ni directUrl.</done>
</task>

<task type="auto">
  <name>Task 2: Apuntar a PostgreSQL local + bcryptjs</name>
  <files>apps/api/.env, apps/api/.env.example, apps/api/package.json</files>
  <action>
.env / .env.example → DATABASE_URL a postgresql local (konbini@localhost), sin DIRECT_URL.
package.json → agregar bcryptjs + @types/bcryptjs.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project && yarn install 2>&1 | tail -3'</automated>
  </verify>
  <done>.env apunta a local; bcryptjs instalado.</done>
</task>

<task type="auto">
  <name>Task 3: Migración limpia + seed de usuarios</name>
  <files>apps/api/prisma/migrations/, apps/api/prisma/seed.ts</files>
  <action>
Borrar prisma/migrations/ (migración Neon). Actualizar seed.ts: reemplazar los Profiles por
3 usuarios (uno por rol) con passwordHash bcrypt; los eventos referencian organizer/admin por id.
Ejecutar `migrate dev --name init` contra la base local y correr el seed.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project/apps/api && yarn prisma migrate status 2>&1 | tail -4'</automated>
  </verify>
  <done>Migración aplicada en la base local; seed crea 3 usuarios (uno por rol).</done>
</task>

</tasks>

<verification>
- `prisma validate` pasa; `prisma migrate status` muestra la migración aplicada en la base local.
- El seed corre y crea usuarios con los 3 roles; los eventos quedan ligados por FK.
- `nest build` sigue OK con el cliente regenerado.
- No quedan referencias a Neon ni a Profile en schema/seed/.env.
</verification>

<success_criteria>
- schema.prisma con User + enum Role (3 valores), sin Profile ni directUrl.
- DATABASE_URL local; migración limpia aplicada.
- seed con un usuario por rol y eventos ligados por FK.
</success_criteria>

<output>
After completion, create `.planning/quick/260520-r3t-sistema-usuarios-local-3-roles/260520-r3t-SUMMARY.md`
and add a row to the Quick Tasks table in `.planning/STATE.md`.
</output>
