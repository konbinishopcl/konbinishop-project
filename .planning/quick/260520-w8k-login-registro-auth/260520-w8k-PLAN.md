---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: [260520-r3t]
requirements: [QUICK-AUTH-LOGIN-REGISTRO]
must_haves:
  truths:
    - "La API NestJS expone POST /api/auth/register, POST /api/auth/login y GET /api/auth/me con JWT"
    - "El password se hashea con bcrypt al registrar y se verifica al hacer login"
    - "Hay un JwtAuthGuard y un RolesGuard + decorador @Roles para proteger endpoints por rol"
    - "Endpoints de gestión de usuarios (listar/crear/editar/bannear/eliminar) protegidos: listar = ADMIN+, mutaciones = solo SUPER_ADMIN"
    - "El website tiene /login (email + password) y /registro (2 pasos: paso 1 email, paso 2 password + nombre + apellido), ambos conectados a la API"
    - "Un usuario AUTHENTICATED no puede entrar a /admin; ADMIN y SUPER_ADMIN sí"
    - "Dentro de /admin, la sección Usuarios solo es accesible para SUPER_ADMIN"
    - "Sin login/registro vía RRSS — se omiten los botones sociales por ahora"
  artifacts:
    - path: "apps/api/src/auth/"
      provides: "Módulo de autenticación (controller, service, guards, decorators, DTOs)"
    - path: "apps/api/src/users/"
      provides: "Módulo de gestión de usuarios protegido por rol"
    - path: "apps/website/app/registro/page.tsx"
      provides: "Registro en 2 pasos copiado del login"
    - path: "apps/website/lib/api.ts"
      provides: "Cliente HTTP de la API con token Bearer"
---

<objective>
Integrar autenticación login + registro de punta a punta: endpoints reales en la API NestJS
(JWT + bcrypt + guards por rol) y las vistas del website conectadas, con control de acceso por rol.

Output: registro/login funcionales contra la API; acceso a /admin restringido por rol; gestión de
usuarios sólo para SUPER_ADMIN. Sin RRSS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/prisma/schema.prisma (User + enum Role ya existen — quick 260520-r3t)
@apps/api/src/app.module.ts
@apps/api/src/main.ts
@apps/website/app/login/page.tsx (login maqueta a conectar)
@apps/website/components/providers.tsx (useUser — estado de usuario)

<decisions>
- Auth con @nestjs/jwt + guard propio (sin passport) para menos dependencias.
- DTOs validados con class-validator + ValidationPipe global.
- CORS habilitado en la API para el origen del website.
- Token JWT guardado en localStorage del website (consistente con el useUser actual).
- Protección de rutas /admin: guard de cliente en el layout admin (el token vive en localStorage).
- El tipo User del website se extiende con id + role; se mapea el user de la API a ese shape.
- Registro recoge: email, password, confirmar password, nombre, apellido (rol AUTHENTICATED por defecto).
- La UI de gestión de usuarios queda como placeholder (no hay diseño) — sólo se gatea el acceso a SUPER_ADMIN.
- Botones de RRSS: se omiten de login/registro por ahora.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API — módulos auth y users</name>
  <files>apps/api/src/auth/, apps/api/src/users/, apps/api/src/app.module.ts, apps/api/src/main.ts, apps/api/.env, apps/api/.env.example, apps/api/package.json</files>
  <action>
Agregar deps @nestjs/jwt, class-validator, class-transformer. main.ts: enableCors + ValidationPipe global.
AuthModule: AuthService (register/login/validate, firma JWT), AuthController (POST register, POST login,
GET me), DTOs RegisterDto/LoginDto, JwtAuthGuard, RolesGuard, @Roles(), @CurrentUser().
UsersModule: UsersService + UsersController — GET /users (ADMIN+), POST/PATCH/DELETE /users/:id y
PATCH /users/:id/ban (SUPER_ADMIN). JWT_SECRET en .env. Registrar ambos módulos en AppModule.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project && pnpm --filter konbini-nest-api build 2>&1 | tail -3'</automated>
  </verify>
  <done>nest build OK; endpoints de auth y users compilados.</done>
</task>

<task type="auto">
  <name>Task 2: Website — cliente API, estado de auth, login y registro</name>
  <files>apps/website/lib/api.ts, apps/website/lib/data.ts, apps/website/components/providers.tsx, apps/website/app/login/page.tsx, apps/website/app/registro/page.tsx</files>
  <action>
lib/api.ts: cliente fetch con baseURL + token Bearer. lib/data.ts: User extendido con id + role + mapper
desde el user de la API. providers.tsx: useUser maneja token (localStorage). Login: email + password →
POST /auth/login. Registro: copia del login en 2 pasos (paso 1 email → Continuar → paso 2 password +
confirmar + nombre + apellido) → POST /auth/register. Sin botones de RRSS.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project && pnpm --filter konbini-website build 2>&1 | tail -4'</automated>
  </verify>
  <done>Build OK; /login y /registro conectados a la API.</done>
</task>

<task type="auto">
  <name>Task 3: Website — control de acceso por rol</name>
  <files>apps/website/components/admin/AdminGuard.tsx, apps/website/app/admin/layout.tsx, apps/website/components/admin/AdminSidebar.tsx, apps/website/app/admin/users/page.tsx, apps/website/components/Header.tsx</files>
  <action>
AdminGuard (cliente): si no hay user o el rol no es ADMIN/SUPER_ADMIN → redirige. Envolver el layout
admin. Sidebar: ocultar "Usuarios" si el rol no es SUPER_ADMIN. /admin/users: gatear a SUPER_ADMIN.
Header: mostrar enlace a /admin para ADMIN/SUPER_ADMIN.
  </action>
  <verify>
    <automated>wsl -e bash -lc 'cd /home/gab/Code/konbini-project && pnpm --filter konbini-website build 2>&1 | tail -4'</automated>
  </verify>
  <done>Build OK; /admin protegido por rol; Usuarios sólo SUPER_ADMIN.</done>
</task>

</tasks>

<verification>
- API: register crea usuario con password hasheado; login devuelve JWT; /me requiere token.
- Endpoints de users: listar exige ADMIN+, mutaciones exigen SUPER_ADMIN (403 si no).
- Website: login y registro funcionan contra la API levantada localmente.
- AUTHENTICATED redirigido fuera de /admin; ADMIN/SUPER_ADMIN entran; Usuarios sólo SUPER_ADMIN.
- pnpm build OK en api y website.
</verification>

<success_criteria>
- Auth full-stack funcionando: registro y login reales con JWT.
- Control de acceso por rol en el website.
- Gestión de usuarios protegida (API), acceso gateado (website).
</success_criteria>

<output>
After completion, create `.planning/quick/260520-w8k-login-registro-auth/260520-w8k-SUMMARY.md`
and add a row to the Quick Tasks table in `.planning/STATE.md`.
</output>
