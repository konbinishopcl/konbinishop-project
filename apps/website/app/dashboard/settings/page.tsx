import { PlaceholderView } from "@/components/admin/PlaceholderView";

export default function SettingsPage() {
  return (
    <PlaceholderView
      title="Configuración"
      ja="設定"
      subtitle="Branding, pasarelas de pago, integraciones y políticas."
    />
  );
}
